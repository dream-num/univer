import {
  AlignTextBothIcon,
  Button,
  COLOR_PICKER_COMPONENT,
  COMMON_LABEL_COMPONENT,
  CatalogueIcon,
  Checkbox,
  ComponentManager,
  ContextMenuPanel,
  CopyCommand,
  CutCommand,
  DeleteIcon,
  DocSettingIcon,
  FILE_PNG_CLIPBOARD_MIME_TYPE,
  FILE__BMP_CLIPBOARD_MIME_TYPE,
  FILE__JPEG_CLIPBOARD_MIME_TYPE,
  FILE__WEBP_CLIPBOARD_MIME_TYPE,
  FONT_FAMILY_COMPONENT,
  FONT_FAMILY_ITEM_COMPONENT,
  FONT_SIZE_COMPONENT,
  FONT_SIZE_LIST,
  H1Icon,
  H2Icon,
  H3Icon,
  H4Icon,
  H5Icon,
  HEADING_ITEM_COMPONENT,
  HEADING_LIST,
  HTML_CLIPBOARD_MIME_TYPE,
  HorizontallyIcon,
  ICanvasPopupService,
  IClipboardInterfaceService,
  IContextMenuService,
  ILayoutService,
  IMenuManagerService,
  IShortcutService,
  ISidebarService,
  IUIPartsService,
  InputNumber,
  LeftIcon,
  LeftJustifyingIcon,
  PLAIN_TEXT_CLIPBOARD_MIME_TYPE,
  PasteCommand,
  RectPopup,
  RightJustifyingIcon,
  Select,
  Slider,
  TextTypeIcon,
  TodoListDoubleIcon,
  ToolbarItem,
  Tooltip,
  borderClassName,
  clsx,
  connectInjector,
  getMenuHiddenObservable,
  imageMimeTypeSet,
  require_jsx_runtime,
  require_react,
  scrollbarClassName,
  useConfigValue,
  useDependency,
  useEvent,
  useObservable
} from "./chunk-VNXQUZSG.js";
import {
  FormulaRefRangeService,
  UniverSheetsFormulaPlugin
} from "./chunk-YVPSS3XX.js";
import {
  ClearSelectionAllCommand,
  CopySheetCommand,
  ERROR_TYPE_SET,
  LexerTreeBuilder,
  RefRangeService,
  RegisterOtherFormulaService,
  RemoveSheetCommand,
  RemoveSheetMutation,
  SetRangeValuesMutation,
  SetRangeValuesUndoMutationFactory,
  SheetInterceptorService,
  SheetsSelectionsService,
  WorksheetViewPermission,
  deserializeListOptions,
  deserializeRangeWithSheet,
  deserializeRangeWithSheetWithCache,
  getSheetCommandTarget,
  handleCommonDefaultRangeChangeWithEffectRefCommands,
  isReferenceString
} from "./chunk-Q6Y4PHRI.js";
import {
  DocBackground,
  DocumentSkeleton,
  DocumentViewModel,
  Documents,
  IRenderManagerService,
  Layer,
  Liquid,
  NORMAL_TEXT_SELECTION_PLUGIN_STYLE,
  Path,
  Rect,
  RegularPolygon,
  ScrollBar,
  ScrollTimer,
  Shape,
  UniverRenderEnginePlugin,
  Vector2,
  Viewport,
  getColor,
  getDocsTableRenderViewport,
  getLastColumn,
  getLastLine,
  getNextWheelZoomRatio,
  getNumberUnitValue,
  getOffsetRectForDom,
  getPageFromPath,
  getParagraphByGlyph,
  getSystemHighlightColor,
  getTableIdAndSliceIndex,
  hasListGlyph,
  isFirstGlyph,
  isIndentByGlyph,
  lineIterator,
  parseDataStreamToTree,
  pixelToPt,
  ptToPixel,
  pxToNum
} from "./chunk-XOCU2XR6.js";
import {
  BehaviorSubject,
  BuildTextUtils,
  COLORS,
  ColorKit,
  DEFAULT_DOCUMENT_PARAGRAPH_LINE_SPACING,
  DEFAULT_DOCUMENT_PARAGRAPH_SPACE_ABOVE,
  DEFAULT_DOCUMENT_PARAGRAPH_SPACE_BELOW,
  DEFAULT_EMPTY_DOCUMENT_VALUE,
  DEFAULT_STYLES,
  DOCS_FORMULA_BAR_EDITOR_UNIT_ID_KEY,
  DOCS_NORMAL_EDITOR_UNIT_ID_KEY,
  DOCS_ZEN_EDITOR_UNIT_ID_KEY,
  DataStreamTreeTokenType,
  DependentOn,
  Disposable,
  DisposableCollection,
  EDITOR_ACTIVATED,
  FOCUSING_COMMENT_EDITOR,
  FOCUSING_COMMON_DRAWINGS,
  FOCUSING_DOC,
  FOCUSING_EDITOR_STANDALONE,
  FOCUSING_UNIVER_EDITOR,
  ICommandService,
  IConfigService,
  IConfirmService,
  IContextService,
  IImageIoService,
  ILogService,
  IPermissionService,
  IResourceManagerService,
  IURLImageService,
  IUndoRedoService,
  IUniverInstanceService,
  Inject,
  Injector,
  InterceptorManager,
  JSONX,
  LRUMap,
  LifecycleService,
  LocaleService,
  MODERN_DOCUMENT_DEFAULT_MARGIN,
  MODERN_DOCUMENT_WIDTH,
  MemoryCursor,
  NAMED_STYLE_MAP,
  ObjectMatrix,
  Observable,
  PAGE_SIZE,
  PAPER_TYPES,
  PRESET_LIST_TYPE,
  Plugin,
  QuickListTypeMap,
  RBush,
  Range,
  Rectangle,
  RedoCommandId,
  RxDisposable,
  SHEET_EDITOR_UNITS,
  Subject,
  TextX,
  ThemeService,
  Tools,
  UndoCommandId,
  animationFrameScheduler,
  bufferDebounceTime,
  bufferTime,
  bufferWhen,
  checkForSubstrings,
  combineLatest,
  composeInterceptors,
  createIdentifier,
  createInterceptorKey,
  createInternalEditorID,
  dateKit,
  debounceTime,
  debounce_default,
  deepCompare,
  distinctUntilChanged,
  filter,
  first,
  fromEvent,
  fromEventSubject,
  generateRandomId,
  getBodySlice,
  getIntersectRange,
  getOriginCellValue,
  getPlainText,
  isCommentEditorID,
  isFormulaString,
  isInternalEditorID,
  isRangesEqual,
  isSafeUrl,
  lib_exports,
  map,
  merge,
  mergeOverrideWithDependencies,
  merge_default,
  normalizeBody,
  of,
  remove,
  require_dist,
  sequenceExecute,
  sequenceExecuteAsync,
  skip,
  skipParseTagNames,
  sortRules,
  sortRulesByDesc,
  switchMap,
  take,
  takeUntil,
  throttleTime,
  toDisposable,
  touchDependencies
} from "./chunk-EJSPXROI.js";
import {
  __decorateClass,
  __decorateParam,
  __publicField,
  __toESM
} from "./chunk-24OICD5T.js";

// ../packages/drawing/package.json
var package_default = {
  name: "@univerjs/drawing",
  version: "0.25.2",
  private: false,
  description: "Shared drawing models, commands, and services for Univer.",
  author: "DreamNum Co., Ltd. <developer@univer.ai>",
  license: "Apache-2.0",
  funding: {
    type: "opencollective",
    url: "https://opencollective.com/univer"
  },
  homepage: "https://univer.ai",
  repository: {
    type: "git",
    url: "https://github.com/dream-num/univer"
  },
  bugs: {
    url: "https://github.com/dream-num/univer/issues"
  },
  keywords: [
    "univer",
    "drawing",
    "graphics",
    "canvas",
    "plugin"
  ],
  exports: {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  main: "./src/index.ts",
  types: "./lib/types/index.d.ts",
  publishConfig: {
    access: "public",
    main: "./lib/es/index.js",
    module: "./lib/es/index.js",
    exports: {
      ".": {
        import: "./lib/es/index.js",
        require: "./lib/cjs/index.js",
        types: "./lib/types/index.d.ts"
      },
      "./*": {
        import: "./lib/es/*",
        require: "./lib/cjs/*",
        types: "./lib/types/index.d.ts"
      },
      "./lib/*": "./lib/*"
    }
  },
  directories: {
    lib: "lib"
  },
  files: [
    "lib"
  ],
  scripts: {
    test: "vitest run",
    "test:watch": "vitest",
    coverage: "vitest run --coverage",
    typecheck: "tsc --noEmit",
    "build:bundle": "univer-cli build",
    "build:types": "tsc -p tsconfig.node.json",
    build: "pnpm run build:bundle && pnpm run build:types"
  },
  peerDependencies: {
    rxjs: ">=7.0.0"
  },
  dependencies: {
    "@univerjs/core": "workspace:*",
    "ot-json1": "^1.0.2"
  },
  devDependencies: {
    "@univerjs-infra/shared": "workspace:*",
    rxjs: "^7.8.2",
    typescript: "^6.0.3",
    vitest: "^4.1.7"
  }
};

// ../packages/drawing/src/basics/config.ts
var DRAWING_IMAGE_WIDTH_LIMIT = 500;
var DRAWING_IMAGE_HEIGHT_LIMIT = 500;
var DRAWING_IMAGE_COUNT_LIMIT = 10;
var DRAWING_IMAGE_ALLOW_SIZE = 5 * 1024 * 1024;
function setDrawingImageAllowSize(size) {
  DRAWING_IMAGE_ALLOW_SIZE = size;
}
function getDrawingImageAllowSize() {
  return DRAWING_IMAGE_ALLOW_SIZE;
}
var DRAWING_IMAGE_ALLOW_IMAGE_LIST = ["image/png", "image/jpeg", "image/jpg", "image/gif", "image/bmp"];

// ../packages/drawing/src/services/drawing-manager.service.ts
var IDrawingManagerService = createIdentifier("univer.drawing-manager.service");

// ../packages/drawing/src/commands/operations/set-drawing-selected.operation.ts
var SetDrawingSelectedOperation = {
  id: "drawing.operation.set-drawing-selected",
  type: 1 /* OPERATION */,
  handler: (accessor, params) => {
    const drawingManagerService = accessor.get(IDrawingManagerService);
    if (params == null) {
      return false;
    }
    drawingManagerService.focusDrawing(params);
    return true;
  }
};

// ../packages/drawing/src/config/config.ts
var DRAWING_PLUGIN_CONFIG_KEY = "drawing.config";
var configSymbol = Symbol(DRAWING_PLUGIN_CONFIG_KEY);
var defaultPluginConfig = {};

// ../packages/drawing/src/services/drawing-manager-impl.service.ts
var json1 = __toESM(require_dist());
function assertSafeDrawingKeys(...keys) {
  for (const key of keys) {
    if (key === "__proto__" || key === "constructor" || key === "prototype") {
      throw new Error("Invalid drawing key");
    }
  }
}
function getOwn(record, key) {
  return record != null && Object.prototype.hasOwnProperty.call(record, key) ? record[key] : void 0;
}
function validateDrawingData(data) {
  for (const subUnitId of Object.keys(data)) {
    assertSafeDrawingKeys(subUnitId);
    const subUnit = data[subUnitId];
    if (subUnit == null ? void 0 : subUnit.data) {
      Object.keys(subUnit.data).forEach((key) => assertSafeDrawingKeys(key));
    }
    if (subUnit == null ? void 0 : subUnit.order) {
      subUnit.order.forEach((key) => assertSafeDrawingKeys(key));
    }
  }
}
var UnitDrawingService = class {
  constructor() {
    __publicField(this, "drawingManagerData", {});
    __publicField(this, "_oldDrawingManagerData", {});
    __publicField(this, "_focusDrawings", []);
    __publicField(this, "_remove$", new Subject());
    __publicField(this, "remove$", this._remove$.asObservable());
    __publicField(this, "_add$", new Subject());
    __publicField(this, "add$", this._add$.asObservable());
    __publicField(this, "_update$", new Subject());
    __publicField(this, "update$", this._update$.asObservable());
    __publicField(this, "_order$", new Subject());
    __publicField(this, "order$", this._order$.asObservable());
    __publicField(this, "_group$", new Subject());
    __publicField(this, "group$", this._group$.asObservable());
    __publicField(this, "_ungroup$", new Subject());
    __publicField(this, "ungroup$", this._ungroup$.asObservable());
    __publicField(this, "_refreshTransform$", new Subject());
    __publicField(this, "refreshTransform$", this._refreshTransform$.asObservable());
    __publicField(this, "_visible$", new Subject());
    __publicField(this, "visible$", this._visible$.asObservable());
    // private readonly _externalUpdate$ = new Subject<T[]>();
    // readonly externalUpdate$ = this._externalUpdate$.asObservable();
    __publicField(this, "_focus$", new Subject());
    __publicField(this, "focus$", this._focus$.asObservable());
    __publicField(this, "_featurePluginUpdate$", new Subject());
    __publicField(this, "featurePluginUpdate$", this._featurePluginUpdate$.asObservable());
    __publicField(this, "_featurePluginAdd$", new Subject());
    __publicField(this, "featurePluginAdd$", this._featurePluginAdd$.asObservable());
    __publicField(this, "_featurePluginRemove$", new Subject());
    __publicField(this, "featurePluginRemove$", this._featurePluginRemove$.asObservable());
    __publicField(this, "_featurePluginOrderUpdate$", new Subject());
    __publicField(this, "featurePluginOrderUpdate$", this._featurePluginOrderUpdate$.asObservable());
    __publicField(this, "_featurePluginGroupUpdate$", new Subject());
    __publicField(this, "featurePluginGroupUpdate$", this._featurePluginGroupUpdate$.asObservable());
    __publicField(this, "_featurePluginUngroupUpdate$", new Subject());
    __publicField(this, "featurePluginUngroupUpdate$", this._featurePluginUngroupUpdate$.asObservable());
    __publicField(this, "_visible", true);
    __publicField(this, "_editable", true);
  }
  dispose() {
    this._remove$.complete();
    this._add$.complete();
    this._update$.complete();
    this._order$.complete();
    this._focus$.complete();
    this._featurePluginUpdate$.complete();
    this._featurePluginAdd$.complete();
    this._featurePluginRemove$.complete();
    this._featurePluginOrderUpdate$.complete();
    this.drawingManagerData = {};
    this._oldDrawingManagerData = {};
  }
  visibleNotification(visibleParams) {
    this._visible$.next(visibleParams);
  }
  refreshTransform(updateParams) {
    updateParams.forEach((updateParam) => {
      const param = this._getCurrentBySearch(updateParam);
      if (param == null) {
        return;
      }
      param.transform = updateParam.transform;
      param.transforms = updateParam.transforms;
      param.isMultiTransform = updateParam.isMultiTransform;
    });
    this.refreshTransformNotification(updateParams);
  }
  getDrawingDataForUnit(unitId) {
    return getOwn(this.drawingManagerData, unitId) || {};
  }
  removeDrawingDataForUnit(unitId) {
    const subUnits = getOwn(this.drawingManagerData, unitId);
    if (subUnits == null) {
      return;
    }
    delete this.drawingManagerData[unitId];
    const drawings = [];
    Object.keys(subUnits).forEach((subUnitId) => {
      const subUnit = subUnits[subUnitId];
      if ((subUnit == null ? void 0 : subUnit.data) == null) {
        return;
      }
      Object.keys(subUnit.data).forEach((drawingId) => {
        drawings.push({ unitId, subUnitId, drawingId });
      });
    });
    if (drawings.length > 0) {
      this.removeNotification(drawings);
    }
  }
  registerDrawingData(unitId, data) {
    assertSafeDrawingKeys(unitId);
    validateDrawingData(data);
    this.drawingManagerData[unitId] = data;
  }
  initializeNotification(unitId) {
    const drawings = [];
    const data = getOwn(this.drawingManagerData, unitId);
    if (data == null) {
      return;
    }
    Object.keys(data).forEach((subUnitId) => {
      this._establishDrawingMap(unitId, subUnitId);
      const subUnitData = data[subUnitId];
      Object.keys(subUnitData.data).forEach((drawingId) => {
        const drawing = subUnitData.data[drawingId];
        drawing.unitId = unitId;
        drawing.subUnitId = subUnitId;
        drawings.push(drawing);
      });
    });
    if (drawings.length > 0) {
      this.addNotification(drawings);
    }
  }
  getDrawingData(unitId, subUnitId) {
    return this._getDrawingData(unitId, subUnitId);
  }
  // Use in doc only.
  setDrawingData(unitId, subUnitId, data) {
    assertSafeDrawingKeys(unitId, subUnitId);
    Object.keys(data).forEach((key) => assertSafeDrawingKeys(key));
    this._establishDrawingMap(unitId, subUnitId);
    this.drawingManagerData[unitId][subUnitId].data = data;
  }
  getBatchAddOp(insertParams) {
    const objects = [];
    const ops = [];
    const invertOps = [];
    insertParams.forEach((insertParam) => {
      const { op: op2, invertOp: invertOp2 } = this._addByParam(insertParam);
      objects.push({ unitId: insertParam.unitId, subUnitId: insertParam.subUnitId, drawingId: insertParam.drawingId });
      ops.push(op2);
      invertOps.push(invertOp2);
    });
    const op = ops.reduce(json1.type.compose, null);
    const invertOp = invertOps.reduce(json1.type.compose, null);
    const { unitId, subUnitId } = insertParams[0];
    return { undo: invertOp, redo: op, unitId, subUnitId, objects };
  }
  getBatchRemoveOpInOder(removeParams) {
    if (removeParams.length === 0) {
      return { undo: null, redo: null, unitId: "", subUnitId: "", objects: [] };
    }
    const orders = this.getDrawingOrder(removeParams[0].unitId, removeParams[0].subUnitId);
    const orderIndexMap = /* @__PURE__ */ new Map();
    orders.forEach((drawingId, index) => {
      orderIndexMap.set(drawingId, index);
    });
    removeParams.sort((a, b) => {
      var _a, _b;
      const indexA = (_a = orderIndexMap.get(a.drawingId)) != null ? _a : Number.NEGATIVE_INFINITY;
      const indexB = (_b = orderIndexMap.get(b.drawingId)) != null ? _b : Number.NEGATIVE_INFINITY;
      return indexA - indexB;
    });
    return this.getBatchRemoveOp(removeParams);
  }
  getBatchRemoveOp(removeParams) {
    var _a;
    const seenIds = /* @__PURE__ */ new Set();
    const allToRemove = [];
    removeParams.forEach((removeParam) => {
      const drawing = this.getDrawingByParam(removeParam);
      if ((drawing == null ? void 0 : drawing.drawingType) === 6 /* DRAWING_GROUP */) {
        const nested = this.getDrawingsByGroupNested(removeParam);
        if (nested) {
          const { flatChildren, groups } = nested;
          [...flatChildren != null ? flatChildren : [], ...groups].forEach((d) => {
            if (!seenIds.has(d.drawingId)) {
              seenIds.add(d.drawingId);
              allToRemove.push({ unitId: d.unitId, subUnitId: d.subUnitId, drawingId: d.drawingId });
            }
          });
        } else if (!seenIds.has(removeParam.drawingId)) {
          seenIds.add(removeParam.drawingId);
          allToRemove.push(removeParam);
        }
      } else if (!seenIds.has(removeParam.drawingId)) {
        seenIds.add(removeParam.drawingId);
        allToRemove.push(removeParam);
      }
    });
    const { unitId, subUnitId } = (_a = allToRemove[0]) != null ? _a : removeParams[0];
    const orderArr = this._getDrawingOrder(unitId, subUnitId);
    const orderIndexMap = /* @__PURE__ */ new Map();
    orderArr.forEach((id, idx) => orderIndexMap.set(id, idx));
    allToRemove.sort((a, b) => {
      var _a2, _b;
      const ia = (_a2 = orderIndexMap.get(a.drawingId)) != null ? _a2 : Number.NEGATIVE_INFINITY;
      const ib = (_b = orderIndexMap.get(b.drawingId)) != null ? _b : Number.NEGATIVE_INFINITY;
      return ia - ib;
    });
    const ops = [];
    const invertOps = [];
    allToRemove.forEach((removeParam) => {
      const { op: op2, invertOp: invertOp2 } = this._removeByParam(removeParam);
      ops.unshift(op2);
      invertOps.push(invertOp2);
    });
    const op = ops.reduce(json1.type.compose, null);
    const invertOp = invertOps.reduce(json1.type.compose, null);
    return { undo: invertOp, redo: op, unitId, subUnitId, objects: allToRemove };
  }
  getBatchUpdateOp(updateParams) {
    const objects = [];
    const ops = [];
    const invertOps = [];
    updateParams.forEach((updateParam) => {
      const { op: op2, invertOp: invertOp2 } = this._updateByParam(updateParam);
      objects.push({ unitId: updateParam.unitId, subUnitId: updateParam.subUnitId, drawingId: updateParam.drawingId });
      ops.push(op2);
      invertOps.push(invertOp2);
    });
    const op = ops.reduce(json1.type.compose, null);
    const invertOp = invertOps.reduce(json1.type.compose, null);
    const { unitId, subUnitId } = updateParams[0];
    return { undo: invertOp, redo: op, unitId, subUnitId, objects };
  }
  removeNotification(removeParams) {
    this._remove$.next(removeParams);
  }
  addNotification(insertParams) {
    this._add$.next(insertParams);
  }
  updateNotification(updateParams) {
    this._update$.next(updateParams);
  }
  orderNotification(orderParams) {
    this._order$.next(orderParams);
  }
  groupUpdateNotification(groupParams) {
    this._group$.next(groupParams);
  }
  ungroupUpdateNotification(groupParams) {
    this._ungroup$.next(groupParams);
  }
  refreshTransformNotification(refreshParams) {
    this._refreshTransform$.next(refreshParams);
  }
  getGroupDrawingOp(groupParams) {
    const ops = [];
    const { unitId, subUnitId } = groupParams[0].parent;
    groupParams.forEach((groupParam) => {
      ops.push(this._getGroupDrawingOp(groupParam));
    });
    const op = ops.reduce(json1.type.compose, null);
    const invertOp = json1.type.invertWithDoc(op, this.drawingManagerData);
    return { undo: invertOp, redo: op, unitId, subUnitId, objects: groupParams };
  }
  getUngroupDrawingOp(groupParams) {
    const ops = [];
    const { unitId, subUnitId } = groupParams[0].parent;
    groupParams.forEach((groupParam) => {
      ops.push(this._getUngroupDrawingOp(groupParam));
    });
    const op = ops.reduce(json1.type.compose, null);
    const invertOp = json1.type.invertWithDoc(op, this.drawingManagerData);
    return { undo: invertOp, redo: op, unitId, subUnitId, objects: groupParams };
  }
  getDrawingsByGroup(groupParam) {
    const { unitId, subUnitId, drawingId } = groupParam;
    const group = this.getDrawingByParam({ unitId, subUnitId, drawingId });
    if (group == null) {
      return [];
    }
    const drawings = this._getDrawingData(unitId, subUnitId);
    const children = [];
    Object.keys(drawings).forEach((key) => {
      const drawing = drawings[key];
      if (drawing.groupId === drawingId) {
        children.push(drawing);
      }
    });
    return children;
  }
  getDrawingsByGroupNested(groupSearch) {
    const { unitId, subUnitId } = groupSearch;
    const rootParam = this.getDrawingByParam(groupSearch);
    if (!rootParam) {
      return null;
    }
    const allDrawings = this._getDrawingData(unitId, subUnitId);
    const groupDerivedDrawingsIdMap = /* @__PURE__ */ new Map();
    Object.values(allDrawings).forEach((drawing) => {
      if (drawing.groupId != null) {
        if (!groupDerivedDrawingsIdMap.has(drawing.groupId)) {
          groupDerivedDrawingsIdMap.set(drawing.groupId, []);
        }
        groupDerivedDrawingsIdMap.get(drawing.groupId).push(drawing.drawingId);
      }
    });
    const flatChildren = [];
    const groups = [];
    const nestedIdRecord = {};
    const dfs = (param) => {
      var _a;
      const { drawingId } = param;
      const childrenIds = (_a = groupDerivedDrawingsIdMap.get(drawingId)) != null ? _a : [];
      nestedIdRecord[drawingId] = { drawingId, children: childrenIds };
      childrenIds.forEach((childId) => {
        const childParam = allDrawings[childId];
        if (!childParam) return;
        if (childParam.drawingType === 6 /* DRAWING_GROUP */) {
          dfs(childParam);
          groups.push(childParam);
        } else {
          flatChildren.push(childParam);
        }
      });
    };
    dfs(rootParam);
    groups.push(rootParam);
    return {
      nestedIdRecord,
      flatChildren,
      groups
    };
  }
  _getGroupDrawingOp(groupParam) {
    const { parent, children } = groupParam;
    const { unitId: groupUnitId, subUnitId: groupSubUnitId, drawingId: groupDrawingId } = parent;
    const ops = [];
    ops.push(
      json1.insertOp([groupUnitId, groupSubUnitId, "data" /* data */, groupDrawingId], parent)
    );
    let maxChildIndex = Number.NEGATIVE_INFINITY;
    children.forEach((child) => {
      const { unitId, subUnitId, drawingId } = child;
      const index = this._hasDrawingOrder({ unitId, subUnitId, drawingId });
      maxChildIndex = Math.max(maxChildIndex, index);
      ops.push(
        ...this._getUpdateParamCompareOp(child, this.getDrawingByParam({ unitId, subUnitId, drawingId }))
      );
    });
    if (maxChildIndex === Number.NEGATIVE_INFINITY) {
      maxChildIndex = this._getDrawingOrder(groupUnitId, groupSubUnitId).length;
    }
    ops.push(
      json1.insertOp([groupUnitId, groupSubUnitId, "order" /* order */, maxChildIndex], groupDrawingId)
    );
    return ops.reduce(json1.type.compose, null);
  }
  _getUngroupDrawingOp(groupParam) {
    const { parent, children } = groupParam;
    const { unitId: groupUnitId, subUnitId: groupSubUnitId, drawingId: groupDrawingId } = parent;
    const ops = [];
    children.forEach((child) => {
      const { unitId, subUnitId, drawingId } = child;
      ops.push(
        ...this._getUpdateParamCompareOp(child, this.getDrawingByParam({ unitId, subUnitId, drawingId }))
      );
    });
    ops.push(
      json1.removeOp([groupUnitId, groupSubUnitId, "data" /* data */, groupDrawingId], true)
    );
    ops.push(
      json1.removeOp([groupUnitId, groupSubUnitId, "order" /* order */, this._getDrawingOrder(groupUnitId, groupSubUnitId).indexOf(groupDrawingId)], true)
    );
    return ops.reduce(json1.type.compose, null);
  }
  applyJson1(unitId, subUnitId, jsonOp) {
    var _a;
    assertSafeDrawingKeys(unitId, subUnitId);
    json1.type.checkValidOp(jsonOp);
    const unitData = getOwn(this.drawingManagerData, unitId);
    const data = {
      ...this.drawingManagerData,
      [unitId]: {
        ...unitData,
        [subUnitId]: (_a = getOwn(unitData, subUnitId)) != null ? _a : { data: {}, order: [] }
      }
    };
    const nextData = json1.type.apply(data, jsonOp);
    for (const id of Object.keys(nextData)) {
      assertSafeDrawingKeys(id);
      validateDrawingData(nextData[id]);
    }
    this._oldDrawingManagerData = this.drawingManagerData;
    this.drawingManagerData = nextData;
  }
  featurePluginUpdateNotification(updateParams) {
    this._featurePluginUpdate$.next(updateParams);
  }
  featurePluginOrderUpdateNotification(drawingOrderUpdateParam) {
    this._featurePluginOrderUpdate$.next(drawingOrderUpdateParam);
  }
  featurePluginAddNotification(insertParams) {
    this._featurePluginAdd$.next(insertParams);
  }
  featurePluginRemoveNotification(removeParams) {
    this._featurePluginRemove$.next(removeParams);
  }
  featurePluginGroupUpdateNotification(groupParams) {
    this._featurePluginGroupUpdate$.next(groupParams);
  }
  featurePluginUngroupUpdateNotification(groupParams) {
    this._featurePluginUngroupUpdate$.next(groupParams);
  }
  getDrawingByParam(param) {
    return this._getCurrentBySearch(param);
  }
  getOldDrawingByParam(param) {
    return this._getOldBySearch(param);
  }
  getDrawingOKey(oKey) {
    const [unitId, subUnitId, drawingId] = oKey.split("#-#");
    return this._getCurrentBySearch({ unitId, subUnitId, drawingId });
  }
  focusDrawing(params) {
    if (params == null || params.length === 0) {
      this._focusDrawings = [];
      this._focus$.next([]);
      return;
    }
    const drawingParams = [];
    params.forEach((param) => {
      var _a;
      const { unitId, subUnitId, drawingId } = param;
      const item = (_a = this._getDrawingData(unitId, subUnitId)) == null ? void 0 : _a[drawingId];
      if (item != null) {
        drawingParams.push(item);
      }
    });
    if (drawingParams.length > 0) {
      this._focusDrawings = drawingParams;
      this._focus$.next(drawingParams);
    }
  }
  getFocusDrawings() {
    const drawingParams = [];
    this._focusDrawings.forEach((param) => {
      var _a;
      const { unitId, subUnitId, drawingId } = param;
      const item = (_a = this._getDrawingData(unitId, subUnitId)) == null ? void 0 : _a[drawingId];
      if (item != null) {
        drawingParams.push(item);
      }
    });
    return drawingParams;
  }
  getDrawingOrder(unitId, subUnitId) {
    return this._getDrawingOrder(unitId, subUnitId);
  }
  // Use in doc only.
  setDrawingOrder(unitId, subUnitId, order) {
    assertSafeDrawingKeys(unitId, subUnitId);
    order.forEach((key) => assertSafeDrawingKeys(key));
    this._establishDrawingMap(unitId, subUnitId);
    this.drawingManagerData[unitId][subUnitId].order = order;
  }
  orderUpdateNotification(orderParams) {
    this._order$.next(orderParams);
  }
  getForwardDrawingsOp(orderParams) {
    const { unitId, subUnitId, drawingIds } = orderParams;
    const ops = [];
    const orders = this.getDrawingOrder(unitId, subUnitId);
    const newIds = [...drawingIds];
    drawingIds.forEach((drawingId) => {
      const index = this._hasDrawingOrder({ unitId, subUnitId, drawingId });
      if (index === -1 || index === orders.length - 1) {
        return;
      }
      const op2 = json1.moveOp([unitId, subUnitId, "order" /* order */, index], [unitId, subUnitId, "order" /* order */, index + 1]);
      ops.push(op2);
      if (!newIds.includes(orders[index + 1])) {
        newIds.push(orders[index + 1]);
      }
    });
    const op = ops.reduce(json1.type.compose, null);
    const invertOp = json1.type.invertWithDoc(op, this.drawingManagerData);
    return { undo: invertOp, redo: op, unitId, subUnitId, objects: { ...orderParams, drawingIds: newIds } };
  }
  getBackwardDrawingOp(orderParams) {
    const { unitId, subUnitId, drawingIds } = orderParams;
    const ops = [];
    const orders = this.getDrawingOrder(unitId, subUnitId);
    const newIds = [...drawingIds];
    drawingIds.forEach((drawingId) => {
      const index = this._hasDrawingOrder({ unitId, subUnitId, drawingId });
      if (index === -1 || index === 0) {
        return;
      }
      const op2 = json1.moveOp([unitId, subUnitId, "order" /* order */, index], [unitId, subUnitId, "order" /* order */, index - 1]);
      ops.push(op2);
      if (!newIds.includes(orders[index - 1])) {
        newIds.push(orders[index - 1]);
      }
    });
    const op = ops.reduce(json1.type.compose, null);
    const invertOp = json1.type.invertWithDoc(op, this.drawingManagerData);
    return { undo: invertOp, redo: op, unitId, subUnitId, objects: { ...orderParams, drawingIds: newIds } };
  }
  getFrontDrawingsOp(orderParams) {
    const { unitId, subUnitId, drawingIds } = orderParams;
    const orderDrawingIds = this._getOrderFromSearchParams(unitId, subUnitId, drawingIds);
    const newIds = [...drawingIds];
    const orders = this.getDrawingOrder(unitId, subUnitId);
    const ops = [];
    orderDrawingIds.forEach((orderDrawingId) => {
      const { drawingId } = orderDrawingId;
      const index = this._getDrawingCount(unitId, subUnitId) - 1;
      const op2 = json1.moveOp([unitId, subUnitId, "order" /* order */, this._getDrawingOrder(unitId, subUnitId).indexOf(drawingId)], [unitId, subUnitId, "order" /* order */, index]);
      ops.push(op2);
      if (!newIds.includes(orders[index])) {
        newIds.push(orders[index]);
      }
    });
    const op = ops.reduce(json1.type.compose, null);
    const invertOp = json1.type.invertWithDoc(op, this.drawingManagerData);
    return { undo: invertOp, redo: op, unitId, subUnitId, objects: { ...orderParams, drawingIds: newIds } };
  }
  getBackDrawingsOp(orderParams) {
    const { unitId, subUnitId, drawingIds } = orderParams;
    const orderSearchParams = this._getOrderFromSearchParams(unitId, subUnitId, drawingIds, true);
    const newIds = [...drawingIds];
    const orders = this.getDrawingOrder(unitId, subUnitId);
    const ops = [];
    orderSearchParams.forEach((orderSearchParam) => {
      const { drawingId } = orderSearchParam;
      const op2 = json1.moveOp([unitId, subUnitId, "order" /* order */, this._getDrawingOrder(unitId, subUnitId).indexOf(drawingId)], [unitId, subUnitId, "order" /* order */, 0]);
      ops.push(op2);
      if (!newIds.includes(orders[0])) {
        newIds.push(orders[0]);
      }
    });
    const op = ops.reduce(json1.type.compose, null);
    const invertOp = json1.type.invertWithDoc(op, this.drawingManagerData);
    return { undo: invertOp, redo: op, unitId, subUnitId, objects: { ...orderParams, drawingIds: newIds } };
  }
  _getDrawingCount(unitId, subUnitId) {
    return this.getDrawingOrder(unitId, subUnitId).length || 0;
  }
  _getOrderFromSearchParams(unitId, subUnitId, drawingIds, isDesc = false) {
    return drawingIds.map((drawingId) => {
      const zIndex = this._hasDrawingOrder({ unitId, subUnitId, drawingId });
      return { drawingId, zIndex };
    }).sort(isDesc === false ? sortRules : sortRulesByDesc);
  }
  _hasDrawingOrder(searchParam) {
    if (searchParam == null) {
      return -1;
    }
    const { unitId, subUnitId, drawingId } = searchParam;
    this._establishDrawingMap(unitId, subUnitId);
    return this._getDrawingOrder(unitId, subUnitId).indexOf(drawingId);
  }
  _getCurrentBySearch(searchParam) {
    var _a;
    if (searchParam == null) {
      return;
    }
    const { unitId, subUnitId, drawingId } = searchParam;
    return getOwn((_a = getOwn(getOwn(this.drawingManagerData, unitId), subUnitId)) == null ? void 0 : _a.data, drawingId);
  }
  _getOldBySearch(searchParam) {
    var _a;
    if (searchParam == null) {
      return;
    }
    const { unitId, subUnitId, drawingId } = searchParam;
    return getOwn((_a = getOwn(getOwn(this._oldDrawingManagerData, unitId), subUnitId)) == null ? void 0 : _a.data, drawingId);
  }
  _establishDrawingMap(unitId, subUnitId, drawingId) {
    assertSafeDrawingKeys(unitId, subUnitId);
    if (drawingId != null) {
      assertSafeDrawingKeys(drawingId);
    }
    if (!getOwn(this.drawingManagerData, unitId)) {
      this.drawingManagerData[unitId] = {};
    }
    if (!getOwn(this.drawingManagerData[unitId], subUnitId)) {
      this.drawingManagerData[unitId][subUnitId] = {
        data: {},
        order: []
      };
    }
    if (drawingId == null) {
      return null;
    }
    return getOwn(this.drawingManagerData[unitId][subUnitId].data, drawingId);
  }
  _addByParam(insertParam) {
    const { unitId, subUnitId, drawingId } = insertParam;
    this._establishDrawingMap(unitId, subUnitId, drawingId);
    const op1 = json1.insertOp([unitId, subUnitId, "data" /* data */, drawingId], insertParam);
    const op2 = json1.insertOp([unitId, subUnitId, "order" /* order */, this._getDrawingOrder(unitId, subUnitId).length], drawingId);
    const op = [op1, op2].reduce(json1.type.compose, null);
    const invertOp = json1.type.invertWithDoc(op, this.drawingManagerData);
    return { op, invertOp };
  }
  _removeByParam(searchParam) {
    if (searchParam == null) {
      return { op: [], invertOp: [] };
    }
    const { unitId, subUnitId, drawingId } = searchParam;
    const object = this._establishDrawingMap(unitId, subUnitId, drawingId);
    if (object == null) {
      return { op: [], invertOp: [] };
    }
    const op1 = json1.removeOp([unitId, subUnitId, "data" /* data */, drawingId], true);
    const op2 = json1.removeOp([unitId, subUnitId, "order" /* order */, this._getDrawingOrder(unitId, subUnitId).indexOf(drawingId)], true);
    const op = [op1, op2].reduce(json1.type.compose, null);
    const invertOp = json1.type.invertWithDoc(op, this.drawingManagerData);
    return { op, invertOp };
  }
  _updateByParam(updateParam) {
    const { unitId, subUnitId, drawingId } = updateParam;
    const object = this._establishDrawingMap(unitId, subUnitId, drawingId);
    if (object == null) {
      return { op: [], invertOp: [] };
    }
    const ops = this._getUpdateParamCompareOp(updateParam, object);
    const op = ops.reduce(json1.type.compose, null);
    const invertOp = json1.type.invertWithDoc(op, this.drawingManagerData);
    return { op, invertOp };
  }
  // private _initializeDrawingData(updateParam: T, oldParam: T) {
  //     Object.keys(updateParam).forEach((key) => {
  //         if (!(key in oldParam)) {
  //             oldParam[key as keyof IDrawingParam] = null as unknown as never;
  //         }
  //     });
  // }
  _getUpdateParamCompareOp(newParam, oldParam) {
    const { unitId, subUnitId, drawingId } = newParam;
    const ops = [];
    Object.keys(newParam).forEach((key) => {
      const newVal = newParam[key];
      const oldVal = oldParam[key];
      if (oldVal === newVal) {
        return;
      }
      ops.push(
        json1.replaceOp([unitId, subUnitId, "data" /* data */, drawingId, key], oldVal, newVal)
      );
    });
    return ops;
  }
  _getDrawingData(unitId, subUnitId) {
    var _a;
    return ((_a = getOwn(getOwn(this.drawingManagerData, unitId), subUnitId)) == null ? void 0 : _a.data) || {};
  }
  _getDrawingOrder(unitId, subUnitId) {
    var _a;
    return ((_a = getOwn(getOwn(this.drawingManagerData, unitId), subUnitId)) == null ? void 0 : _a.order) || [];
  }
  getDrawingVisible() {
    return this._visible;
  }
  getDrawingEditable() {
    return this._editable;
  }
  setDrawingVisible(visible) {
    this._visible = visible;
  }
  setDrawingEditable(editable) {
    this._editable = editable;
  }
};
var DrawingManagerService = class extends UnitDrawingService {
};

// ../packages/drawing/src/services/image-io-impl.service.ts
var ImageIoService = class {
  constructor() {
    __publicField(this, "_waitCount", 0);
    __publicField(this, "_change$", new Subject());
    __publicField(this, "change$", this._change$);
    __publicField(this, "_imageSourceCache", /* @__PURE__ */ new Map());
  }
  setWaitCount(count) {
    this._waitCount = count;
    this._change$.next(count);
  }
  getImageSourceCache(source, imageSourceType) {
    if (imageSourceType === "BASE64" /* BASE64 */) {
      const image = new Image();
      image.src = source;
      return image;
    }
    return this._imageSourceCache.get(source);
  }
  addImageSourceCache(source, imageSourceType, imageSource) {
    if (imageSourceType === "BASE64" /* BASE64 */ || imageSource == null) {
      return;
    }
    this._imageSourceCache.set(source, imageSource);
  }
  async getImage(imageId) {
    return Promise.resolve(imageId);
  }
  async saveImage(imageFile) {
    return new Promise((resolve, reject) => {
      if (!DRAWING_IMAGE_ALLOW_IMAGE_LIST.includes(imageFile.type)) {
        reject(new Error("2" /* ERROR_IMAGE_TYPE */));
        this._decreaseWaiting();
        return;
      }
      if (imageFile.size > getDrawingImageAllowSize()) {
        reject(new Error("1" /* ERROR_EXCEED_SIZE */));
        this._decreaseWaiting();
        return;
      }
      const reader = new FileReader();
      reader.readAsDataURL(imageFile);
      reader.onload = (evt) => {
        var _a;
        const replaceSrc = (_a = evt.target) == null ? void 0 : _a.result;
        if (replaceSrc == null) {
          reject(new Error("4" /* ERROR_IMAGE */));
          this._decreaseWaiting();
          return;
        }
        const imageId = generateRandomId(6);
        resolve({
          imageId,
          imageSourceType: "BASE64" /* BASE64 */,
          source: replaceSrc,
          base64Cache: replaceSrc,
          status: "0" /* SUCCUSS */
        });
        this._decreaseWaiting();
      };
    });
  }
  _decreaseWaiting() {
    this._waitCount -= 1;
    this._change$.next(this._waitCount);
  }
};

// ../packages/drawing/src/services/url-image.service.ts
var URLImageService = class extends Disposable {
  constructor() {
    super(...arguments);
    __publicField(this, "_urlImageDownloader", null);
  }
  registerURLImageDownloader(downloader) {
    this._urlImageDownloader = downloader;
    return toDisposable(() => {
      this._urlImageDownloader = null;
    });
  }
  async getImage(url) {
    if (this._urlImageDownloader) {
      try {
        return await this._urlImageDownloader(url);
      } catch (error) {
        console.error(`Custom downloader failed for ${url}, falling back to default behavior:`, error);
      }
    }
    return url;
  }
  async downloadImage(url) {
    if (this._urlImageDownloader) {
      try {
        const base64 = await this._urlImageDownloader(url);
        const response2 = await fetch(base64);
        return await response2.blob();
      } catch (error) {
        console.error(`Custom downloader failed for ${url}, falling back to default fetch:`, error);
      }
    }
    const response = await fetch(url);
    return await response.blob();
  }
};

// ../packages/drawing/src/plugin.ts
var UniverDrawingPlugin = class extends Plugin {
  constructor(_config = defaultPluginConfig, _injector, _configService, _commandService) {
    super();
    __publicField(this, "_config", _config);
    __publicField(this, "_injector", _injector);
    __publicField(this, "_configService", _configService);
    __publicField(this, "_commandService", _commandService);
    const { ...rest } = merge_default(
      {},
      defaultPluginConfig,
      this._config
    );
    this._configService.setConfig(DRAWING_PLUGIN_CONFIG_KEY, rest);
    if (rest.allowImageSize !== void 0) {
      setDrawingImageAllowSize(rest.allowImageSize);
    }
  }
  onStarting() {
    this._initCommands();
    this._initDependencies();
  }
  _initDependencies() {
    var _a;
    const dependencies = [
      [IImageIoService, { useClass: ImageIoService }],
      [IURLImageService, { useClass: URLImageService }],
      [IDrawingManagerService, { useClass: DrawingManagerService }]
    ];
    const dependency = mergeOverrideWithDependencies(dependencies, (_a = this._config) == null ? void 0 : _a.override);
    dependency.forEach((d) => this._injector.add(d));
  }
  _initCommands() {
    [
      SetDrawingSelectedOperation
    ].forEach((command) => this.disposeWithMe(this._commandService.registerCommand(command)));
  }
};
__publicField(UniverDrawingPlugin, "pluginName", "UNIVER_DRAWING_PLUGIN");
__publicField(UniverDrawingPlugin, "packageName", package_default.name);
__publicField(UniverDrawingPlugin, "version", package_default.version);
UniverDrawingPlugin = __decorateClass([
  __decorateParam(1, Inject(Injector)),
  __decorateParam(2, IConfigService),
  __decorateParam(3, ICommandService)
], UniverDrawingPlugin);

// ../packages/drawing/src/utils/get-image-shape-key.ts
function getDrawingShapeKeyByDrawingSearch({ unitId, subUnitId, drawingId }, index) {
  return typeof index === "number" ? `${unitId}#-#${subUnitId}#-#${drawingId}#-#${index}` : `${unitId}#-#${subUnitId}#-#${drawingId}`;
}

// ../packages/drawing/src/utils/get-image-size.ts
var getImageSize = async (src) => {
  return new Promise((resolve, reject) => {
    const image = new Image();
    image.src = src;
    image.onload = () => {
      resolve({
        width: image.width,
        height: image.height,
        image
      });
    };
    image.onerror = (error) => {
      reject(error);
    };
  });
};

// ../packages/docs/package.json
var package_default2 = {
  name: "@univerjs/docs",
  version: "0.25.2",
  private: false,
  description: "Core document model and rich-text operations for Univer Docs.",
  author: "DreamNum Co., Ltd. <developer@univer.ai>",
  license: "Apache-2.0",
  funding: {
    type: "opencollective",
    url: "https://opencollective.com/univer"
  },
  homepage: "https://univer.ai",
  repository: {
    type: "git",
    url: "https://github.com/dream-num/univer"
  },
  bugs: {
    url: "https://github.com/dream-num/univer/issues"
  },
  keywords: [
    "univer",
    "docs",
    "document",
    "rich-text",
    "plugin"
  ],
  exports: {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  main: "./src/index.ts",
  types: "./lib/types/index.d.ts",
  publishConfig: {
    access: "public",
    main: "./lib/es/index.js",
    module: "./lib/es/index.js",
    exports: {
      ".": {
        import: "./lib/es/index.js",
        require: "./lib/cjs/index.js",
        types: "./lib/types/index.d.ts"
      },
      "./*": {
        import: "./lib/es/*",
        require: "./lib/cjs/*",
        types: "./lib/types/index.d.ts"
      },
      "./lib/*": "./lib/*"
    }
  },
  directories: {
    lib: "lib"
  },
  files: [
    "lib"
  ],
  scripts: {
    test: "vitest run",
    "test:watch": "vitest",
    coverage: "vitest run --coverage",
    typecheck: "tsc --noEmit",
    "build:bundle": "univer-cli build",
    "build:types": "tsc -p tsconfig.node.json",
    build: "pnpm run build:bundle && pnpm run build:types"
  },
  peerDependencies: {
    rxjs: ">=7.0.0"
  },
  dependencies: {
    "@univerjs/core": "workspace:*",
    "@univerjs/engine-render": "workspace:*"
  },
  devDependencies: {
    "@univerjs-infra/shared": "workspace:*",
    rxjs: "^7.8.2",
    typescript: "^6.0.3",
    vitest: "^4.1.7"
  }
};

// ../packages/docs/src/commands/operations/text-selection.operation.ts
var SetTextSelectionsOperation = {
  id: "doc.operation.set-selections",
  type: 1 /* OPERATION */,
  handler: () => {
    return true;
  }
};

// ../packages/docs/src/services/doc-selection-manager.service.ts
var DocSelectionManagerService = class extends RxDisposable {
  constructor(_commandService, _univerInstanceService) {
    super();
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_currentSelection", null);
    __publicField(this, "_textSelectionInfo", /* @__PURE__ */ new Map());
    __publicField(this, "_textSelection$", new Subject());
    __publicField(this, "textSelection$", this._textSelection$.asObservable());
    __publicField(this, "_refreshSelection$", new BehaviorSubject(null));
    __publicField(this, "refreshSelection$", this._refreshSelection$.asObservable());
    this._listenCurrentUnit();
  }
  _listenCurrentUnit() {
    this._univerInstanceService.getCurrentTypeOfUnit$(1 /* UNIVER_DOC */).pipe(takeUntil(this.dispose$)).subscribe((documentModel) => {
      if (documentModel == null) {
        return;
      }
      const unitId = documentModel.getUnitId();
      this._setCurrentSelectionNotRefresh({
        unitId,
        subUnitId: unitId
      });
    });
  }
  __getCurrentSelection() {
    return this._currentSelection;
  }
  getSelectionInfo(params = this._currentSelection) {
    return this._getTextRanges(params);
  }
  refreshSelection(params = this._currentSelection) {
    if (params == null) {
      return;
    }
    this._refresh(params);
  }
  // **Only used in test case** because this does not go through the render layer.
  __TEST_ONLY_setCurrentSelection(param) {
    this._currentSelection = param;
    this._refresh(param);
  }
  getTextRanges(params = this._currentSelection) {
    var _a;
    return (_a = this._getTextRanges(params)) == null ? void 0 : _a.textRanges;
  }
  getRectRanges(params = this._currentSelection) {
    var _a;
    return (_a = this._getTextRanges(params)) == null ? void 0 : _a.rectRanges;
  }
  getDocRanges(params = this._currentSelection) {
    var _a, _b;
    const textRanges = (_a = this.getTextRanges(params)) != null ? _a : [];
    const rectRanges = (_b = this.getRectRanges(params)) != null ? _b : [];
    const allRanges = [...textRanges, ...rectRanges].filter((range) => range.startOffset != null && range.endOffset != null).sort((a, b) => {
      if (a.startOffset > b.startOffset) {
        return 1;
      } else if (a.startOffset < b.startOffset) {
        return -1;
      } else {
        return 0;
      }
    });
    return allRanges;
  }
  getActiveTextRange() {
    const selectionInfo = this._getTextRanges(this._currentSelection);
    if (selectionInfo == null) {
      return;
    }
    const { textRanges } = selectionInfo;
    return textRanges.find((textRange) => textRange.isActive);
  }
  /**
   *
   * @deprecated
   */
  getActiveRectRange() {
    const selectionInfo = this._getTextRanges(this._currentSelection);
    if (selectionInfo == null) {
      return;
    }
    const { rectRanges } = selectionInfo;
    return rectRanges.find((rectRange) => rectRange.isActive);
  }
  // **Only used in test case** because this does not go through the render layer.
  __TEST_ONLY_add(textRanges, isEditing = true) {
    if (this._currentSelection == null) {
      return;
    }
    this._addByParam({
      ...this._currentSelection,
      textRanges,
      rectRanges: [],
      segmentId: "",
      segmentPage: -1,
      isEditing,
      style: NORMAL_TEXT_SELECTION_PLUGIN_STYLE
      // mock style.
    });
  }
  // Use to replace the current editor selection.
  /**
   * @deprecated pls use replaceDocRanges.
   */
  replaceTextRanges(docRanges, isEditing = true, options) {
    return this.replaceDocRanges(
      docRanges,
      this._currentSelection,
      isEditing,
      options
    );
  }
  replaceDocRanges(docRanges, params = this._currentSelection, isEditing = true, options) {
    if (params == null) {
      return;
    }
    const { unitId, subUnitId } = params;
    this._refreshSelection$.next({
      unitId,
      subUnitId,
      docRanges,
      isEditing,
      options
    });
  }
  // Only use in doc-selection-render.controller.ts
  __replaceTextRangesWithNoRefresh(textSelectionInfo, search) {
    if (this._currentSelection == null) {
      return;
    }
    const params = {
      ...textSelectionInfo,
      ...search
    };
    this._replaceByParam(params);
    this._textSelection$.next(params);
    const { unitId, subUnitId, segmentId, style, textRanges, rectRanges, isEditing } = params;
    const ranges = [...textRanges, ...rectRanges].filter((range) => range.startOffset != null && range.endOffset != null).sort((a, b) => {
      if (a.startOffset > b.startOffset) {
        return 1;
      } else if (a.startOffset < b.startOffset) {
        return -1;
      } else {
        return 0;
      }
    });
    this._commandService.executeCommand(SetTextSelectionsOperation.id, {
      unitId,
      subUnitId,
      segmentId,
      style,
      isEditing,
      ranges
    });
  }
  dispose() {
    this._textSelection$.complete();
    this._refreshSelection$.complete();
  }
  _setCurrentSelectionNotRefresh(param) {
    this._currentSelection = param;
  }
  _getTextRanges(param) {
    var _a;
    if (param == null) {
      return;
    }
    const { unitId, subUnitId = "" } = param;
    return (_a = this._textSelectionInfo.get(unitId)) == null ? void 0 : _a.get(subUnitId);
  }
  _refresh(param) {
    const allTextSelectionInfo = this._getTextRanges(param);
    if (allTextSelectionInfo == null) {
      return;
    }
    const { textRanges, rectRanges } = allTextSelectionInfo;
    const docRanges = [...textRanges, ...rectRanges];
    const { unitId, subUnitId } = param;
    this._refreshSelection$.next({
      unitId,
      subUnitId,
      docRanges,
      isEditing: false
    });
  }
  _replaceByParam(insertParam) {
    const { unitId, subUnitId, ...selectionInsertParam } = insertParam;
    if (!this._textSelectionInfo.has(unitId)) {
      this._textSelectionInfo.set(unitId, /* @__PURE__ */ new Map());
    }
    const unitTextRange = this._textSelectionInfo.get(unitId);
    unitTextRange.set(subUnitId, { ...selectionInsertParam });
  }
  _addByParam(insertParam) {
    const { unitId, subUnitId, ...selectionInsertParam } = insertParam;
    if (!this._textSelectionInfo.has(unitId)) {
      this._textSelectionInfo.set(unitId, /* @__PURE__ */ new Map());
    }
    const unitTextRange = this._textSelectionInfo.get(unitId);
    if (!unitTextRange.has(subUnitId)) {
      unitTextRange.set(subUnitId, { ...selectionInsertParam });
    } else {
      const OldTextRanges = unitTextRange.get(subUnitId);
      OldTextRanges.textRanges.push(...insertParam.textRanges);
    }
  }
};
DocSelectionManagerService = __decorateClass([
  __decorateParam(0, ICommandService),
  __decorateParam(1, IUniverInstanceService)
], DocSelectionManagerService);

// ../packages/docs/src/services/doc-skeleton-manager.service.ts
var DocSkeletonManagerService = class extends RxDisposable {
  constructor(_context, _localeService, _univerInstanceService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_localeService", _localeService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_skeleton");
    __publicField(this, "_docViewModel");
    __publicField(this, "_currentSkeleton$", new BehaviorSubject(null));
    __publicField(this, "currentSkeleton$", this._currentSkeleton$.asObservable());
    // CurrentSkeletonBefore for pre-triggered logic during registration
    __publicField(this, "_currentSkeletonBefore$", new BehaviorSubject(null));
    __publicField(this, "currentSkeletonBefore$", this._currentSkeletonBefore$.asObservable());
    __publicField(this, "_currentViewModel$", new BehaviorSubject(null));
    __publicField(this, "currentViewModel$", this._currentViewModel$.asObservable());
    this._init();
    this._univerInstanceService.getCurrentTypeOfUnit$(1 /* UNIVER_DOC */).pipe(takeUntil(this.dispose$)).subscribe((documentModel) => {
      if (documentModel && documentModel.getUnitId() === this._context.unitId) {
        this._update(documentModel);
      }
    });
  }
  dispose() {
    super.dispose();
    this._currentSkeletonBefore$.complete();
    this._currentSkeleton$.complete();
  }
  getSkeleton() {
    return this._skeleton;
  }
  getViewModel() {
    return this._docViewModel;
  }
  _init() {
    const documentDataModel = this._context.unit;
    this._update(documentDataModel);
  }
  _update(documentDataModel) {
    const unitId = this._context.unitId;
    if (documentDataModel.getBody() == null) {
      return;
    }
    if (this._docViewModel && isInternalEditorID(unitId)) {
      this._docViewModel.reset(documentDataModel);
      this._context.unit = documentDataModel;
    } else if (!this._docViewModel) {
      this._docViewModel = this._buildDocViewModel(documentDataModel);
    }
    if (!this._skeleton) {
      this._skeleton = this._buildSkeleton(this._docViewModel);
    }
    const skeleton = this._skeleton;
    skeleton.calculate();
    this._currentSkeletonBefore$.next(skeleton);
    this._currentSkeleton$.next(skeleton);
    this._currentViewModel$.next(this._docViewModel);
  }
  _buildSkeleton(documentViewModel) {
    return DocumentSkeleton.create(documentViewModel, this._localeService);
  }
  _buildDocViewModel(documentDataModel) {
    return new DocumentViewModel(documentDataModel);
  }
};
DocSkeletonManagerService = __decorateClass([
  __decorateParam(1, Inject(LocaleService)),
  __decorateParam(2, IUniverInstanceService)
], DocSkeletonManagerService);

// ../packages/docs/src/services/doc-state-emit.service.ts
var DocStateEmitService = class extends RxDisposable {
  constructor() {
    super();
    __publicField(this, "_docStateChangeParams$", new BehaviorSubject(null));
    __publicField(this, "docStateChangeParams$", this._docStateChangeParams$.asObservable());
  }
  emitStateChangeInfo(params) {
    this._docStateChangeParams$.next(params);
  }
  dispose() {
    super.dispose();
    this._docStateChangeParams$.complete();
  }
};

// ../packages/docs/src/commands/mutations/core-editing.mutation.ts
var RichTextEditingMutationId = "doc.mutation.rich-text-editing";
var RichTextEditingMutation = {
  id: RichTextEditingMutationId,
  type: 2 /* MUTATION */,
  // eslint-disable-next-line max-lines-per-function
  handler: (accessor, params) => {
    var _a, _b;
    const {
      unitId,
      segmentId = "",
      actions,
      textRanges,
      prevTextRanges,
      trigger,
      noHistory,
      isCompositionEnd,
      noNeedSetTextRange,
      debounce,
      isEditing = true,
      isSync,
      syncer
    } = params;
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const renderManagerService = accessor.get(IRenderManagerService);
    const docStateEmitService = accessor.get(DocStateEmitService);
    const documentDataModel = univerInstanceService.getUniverDocInstance(unitId);
    const documentViewModel = (_a = renderManagerService.getRenderById(unitId)) == null ? void 0 : _a.with(DocSkeletonManagerService).getViewModel();
    if (documentDataModel == null || documentViewModel == null) {
      throw new Error(`DocumentDataModel or documentViewModel not found for unitId: ${unitId}`);
    }
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const docRanges = (_b = docSelectionManagerService.getDocRanges()) != null ? _b : [];
    const disabled = !!documentDataModel.getSnapshot().disabled;
    if (JSONX.isNoop(actions) || actions && actions.length === 0 || disabled) {
      return {
        unitId,
        actions: [],
        textRanges: docRanges
      };
    }
    const undoActions = JSONX.invertWithDoc(actions, documentDataModel.getSnapshot());
    documentDataModel.apply(actions);
    documentViewModel.reset(documentDataModel);
    if (!noNeedSetTextRange && textRanges && trigger != null && !isSync) {
      queueMicrotask(() => {
        docSelectionManagerService.replaceDocRanges(textRanges, { unitId, subUnitId: unitId }, isEditing, params.options);
      });
    }
    const changeState = {
      commandId: RichTextEditingMutationId,
      unitId,
      segmentId,
      trigger,
      noHistory,
      debounce,
      redoState: {
        actions,
        textRanges
      },
      undoState: {
        actions: undoActions,
        textRanges: prevTextRanges != null ? prevTextRanges : docRanges
      },
      isCompositionEnd,
      isSync,
      syncer
    };
    docStateEmitService.emitStateChangeInfo(changeState);
    return {
      unitId,
      actions: undoActions,
      textRanges: docRanges
    };
  }
};

// ../packages/docs/src/commands/mutations/docs-rename.mutation.ts
var DocsRenameMutation = {
  id: "doc.mutation.rename-doc",
  type: 2 /* MUTATION */,
  handler: (accessor, params) => {
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const doc = univerInstanceService.getUnit(params.unitId, 1 /* UNIVER_DOC */);
    if (!doc) {
      return false;
    }
    doc.setName(params.name);
    return true;
  }
};

// ../packages/docs/src/config/config.ts
var DOCS_PLUGIN_CONFIG_KEY = "docs.config";
var configSymbol2 = Symbol(DOCS_PLUGIN_CONFIG_KEY);
var defaultPluginConfig2 = {};

// ../packages/docs/src/controllers/custom-range.controller.ts
var DocCustomRangeController = class extends Disposable {
  constructor(_commandService, _textSelectionManagerService, _univerInstanceService) {
    super();
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_textSelectionManagerService", _textSelectionManagerService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    this._initSelectionChange();
  }
  _transformCustomRange(doc, selection) {
    var _a;
    const { startOffset, endOffset, collapsed } = selection;
    const customRanges = (_a = doc.getCustomRanges()) == null ? void 0 : _a.filter((range) => {
      if (!range.wholeEntity) {
        return false;
      }
      if (startOffset <= range.startIndex && endOffset > range.endIndex) {
        return false;
      }
      if (collapsed) {
        return range.startIndex < startOffset && range.endIndex >= endOffset;
      }
      return BuildTextUtils.range.isIntersects(startOffset, endOffset - 1, range.startIndex, range.endIndex);
    });
    if (customRanges == null ? void 0 : customRanges.length) {
      let start = startOffset;
      let end = endOffset;
      customRanges.forEach((range) => {
        start = Math.min(range.startIndex, start);
        end = Math.max(range.endIndex + 1, end);
      });
      return {
        ...selection,
        startOffset: start,
        endOffset: end,
        collapsed: start === end
      };
    }
    return selection;
  }
  _initSelectionChange() {
    this.disposeWithMe(this._commandService.onCommandExecuted((commandInfo) => {
      if (commandInfo.id === SetTextSelectionsOperation.id) {
        const params = commandInfo.params;
        const { unitId, ranges, isEditing } = params;
        const doc = this._univerInstanceService.getUnit(unitId);
        if (!doc) {
          return;
        }
        const transformedRanges = ranges.map((range) => this._transformCustomRange(doc, range));
        if (transformedRanges.some((range, i) => ranges[i] !== range)) {
          this._textSelectionManagerService.replaceTextRanges(transformedRanges, isEditing);
        }
      }
    }));
  }
};
DocCustomRangeController = __decorateClass([
  __decorateParam(0, ICommandService),
  __decorateParam(1, Inject(DocSelectionManagerService)),
  __decorateParam(2, IUniverInstanceService)
], DocCustomRangeController);

// ../packages/docs/src/plugin.ts
var UniverDocsPlugin = class extends Plugin {
  // static override type = UniverInstanceType.UNIVER_DOC;
  constructor(_config = defaultPluginConfig2, _injector, _configService) {
    super();
    __publicField(this, "_config", _config);
    __publicField(this, "_injector", _injector);
    __publicField(this, "_configService", _configService);
    const { ...rest } = merge_default(
      {},
      defaultPluginConfig2,
      this._config
    );
    this._configService.setConfig(DOCS_PLUGIN_CONFIG_KEY, rest);
  }
  onStarting() {
    this._initializeDependencies();
    this._initializeCommands();
  }
  _initializeCommands() {
    [
      RichTextEditingMutation,
      DocsRenameMutation,
      SetTextSelectionsOperation
    ].forEach((command) => {
      this._injector.get(ICommandService).registerCommand(command);
    });
  }
  _initializeDependencies() {
    [
      [DocSelectionManagerService],
      [DocStateEmitService],
      [DocCustomRangeController]
    ].forEach((d) => this._injector.add(d));
  }
  onReady() {
    this._injector.get(DocCustomRangeController);
  }
};
__publicField(UniverDocsPlugin, "pluginName", "DOCS_PLUGIN");
__publicField(UniverDocsPlugin, "packageName", package_default2.name);
__publicField(UniverDocsPlugin, "version", package_default2.version);
UniverDocsPlugin = __decorateClass([
  __decorateParam(1, Inject(Injector)),
  __decorateParam(2, IConfigService)
], UniverDocsPlugin);

// ../packages/docs/src/services/doc-interceptor/interceptor-const.ts
var CUSTOM_RANGE = createInterceptorKey("CUSTOM_RANGE");
var CUSTOM_DECORATION = createInterceptorKey("CUSTOM_DECORATION");
var DOC_INTERCEPTOR_POINT = {
  CUSTOM_RANGE,
  CUSTOM_DECORATION
};

// ../packages/docs/src/services/doc-interceptor/doc-interceptor.service.ts
var DocInterceptorService = class extends Disposable {
  constructor(_context, _docSkeletonManagerService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_interceptorsByName", /* @__PURE__ */ new Map());
    const viewModel = this._docSkeletonManagerService.getViewModel();
    const unitId = viewModel.getDataModel().getUnitId();
    if (unitId === DOCS_NORMAL_EDITOR_UNIT_ID_KEY || unitId === DOCS_FORMULA_BAR_EDITOR_UNIT_ID_KEY) {
      return;
    }
    this.disposeWithMe(this.interceptDocumentViewModel(viewModel));
    this.disposeWithMe(this.intercept(DOC_INTERCEPTOR_POINT.CUSTOM_RANGE, {
      priority: -1,
      handler: (data, pos, next) => {
        return next(data);
      }
    }));
    let disposableCollection = new DisposableCollection();
    viewModel.segmentViewModels$.subscribe((segmentViewModels) => {
      disposableCollection.dispose();
      disposableCollection = new DisposableCollection();
      segmentViewModels.forEach((segmentViewModel) => {
        disposableCollection.add(this.interceptDocumentViewModel(segmentViewModel));
      });
    });
    this.disposeWithMe(disposableCollection);
  }
  intercept(name, interceptor) {
    const key = name;
    if (!this._interceptorsByName.has(key)) {
      this._interceptorsByName.set(key, []);
    }
    const interceptors = this._interceptorsByName.get(key);
    interceptors.push(interceptor);
    this._interceptorsByName.set(
      key,
      interceptors.sort((a, b) => {
        var _a, _b;
        return ((_a = b.priority) != null ? _a : 0) - ((_b = a.priority) != null ? _b : 0);
      })
    );
    return this.disposeWithMe(toDisposable(() => remove(this._interceptorsByName.get(key), interceptor)));
  }
  fetchThroughInterceptors(name) {
    const key = name;
    const interceptors = this._interceptorsByName.get(key);
    return composeInterceptors(interceptors || []);
  }
  interceptDocumentViewModel(viewModel) {
    const disposableCollection = new DisposableCollection();
    disposableCollection.add(viewModel.registerCustomRangeInterceptor({
      getCustomRange: (index) => {
        var _a;
        return this.fetchThroughInterceptors(DOC_INTERCEPTOR_POINT.CUSTOM_RANGE)(
          viewModel.getCustomRangeRaw(index),
          {
            index,
            unitId: viewModel.getDataModel().getUnitId(),
            customRanges: (_a = viewModel.getDataModel().getCustomRanges()) != null ? _a : []
          }
        );
      },
      getCustomDecoration: (index) => {
        var _a;
        return this.fetchThroughInterceptors(DOC_INTERCEPTOR_POINT.CUSTOM_DECORATION)(
          viewModel.getCustomDecorationRaw(index),
          {
            index,
            unitId: viewModel.getDataModel().getUnitId(),
            customDecorations: (_a = viewModel.getDataModel().getCustomDecorations()) != null ? _a : []
          }
        );
      }
    }));
    return disposableCollection;
  }
};
DocInterceptorService = __decorateClass([
  __decorateParam(1, Inject(DocSkeletonManagerService))
], DocInterceptorService);

// ../packages/docs/src/utils/custom-range-factory.ts
function getRichTextEditPath(docDataModel, segmentId = "") {
  if (!segmentId) {
    return ["body"];
  }
  const { headers, footers } = docDataModel.getSnapshot();
  if (headers == null && footers == null) {
    throw new Error("Document data model must have headers or footers when update by segment id");
  }
  if ((headers == null ? void 0 : headers[segmentId]) != null) {
    return ["headers", segmentId, "body"];
  } else if ((footers == null ? void 0 : footers[segmentId]) != null) {
    return ["footers", segmentId, "body"];
  } else {
    throw new Error("Segment id not found in headers or footers");
  }
}
function addCustomRangeBySelectionFactory(accessor, param) {
  var _a;
  const { rangeId, rangeType, wholeEntity, properties, unitId, selections: propSelection } = param;
  const docSelectionManagerService = accessor.get(DocSelectionManagerService);
  const univerInstanceService = accessor.get(IUniverInstanceService);
  const selections = propSelection != null ? propSelection : docSelectionManagerService.getTextRanges({ unitId, subUnitId: unitId });
  const segmentId = (_a = selections == null ? void 0 : selections[0]) == null ? void 0 : _a.segmentId;
  if (!(selections == null ? void 0 : selections.length)) {
    return false;
  }
  const documentDataModel = univerInstanceService.getUnit(unitId, 1 /* UNIVER_DOC */);
  if (!documentDataModel) {
    return false;
  }
  const body = documentDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
  if (!body) {
    return false;
  }
  const textX = BuildTextUtils.customRange.add({
    ranges: selections,
    rangeId,
    rangeType,
    segmentId,
    wholeEntity,
    properties,
    body
  });
  if (!textX) {
    return false;
  }
  const jsonX = JSONX.getInstance();
  const doMutation = {
    id: RichTextEditingMutation.id,
    params: {
      unitId,
      actions: [],
      textRanges: textX.selections,
      segmentId
    },
    textX
  };
  const path = getRichTextEditPath(documentDataModel, segmentId);
  doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
  return doMutation;
}
function deleteCustomRangeFactory(accessor, params) {
  const { unitId, segmentId, insert } = params;
  const univerInstanceService = accessor.get(IUniverInstanceService);
  const documentDataModel = univerInstanceService.getUnit(unitId);
  if (!documentDataModel) {
    return false;
  }
  const doMutation = {
    id: RichTextEditingMutation.id,
    params: {
      unitId: params.unitId,
      actions: [],
      textRanges: void 0,
      segmentId
    }
  };
  const jsonX = JSONX.getInstance();
  const textX = BuildTextUtils.customRange.delete({
    documentDataModel,
    rangeId: params.rangeId,
    insert,
    segmentId
  });
  if (!textX) {
    return false;
  }
  const path = getRichTextEditPath(documentDataModel, segmentId);
  doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
  doMutation.params.textRanges = textX.selections;
  return doMutation;
}

// ../packages/docs/src/utils/replace-selection-factory.ts
function replaceSelectionFactory(accessor, params) {
  var _a, _b, _c, _d;
  const { unitId, body: insertBody, doc } = params;
  let docDataModel = doc;
  if (!docDataModel) {
    const univerInstanceService = accessor.get(IUniverInstanceService);
    docDataModel = univerInstanceService.getUnit(unitId);
  }
  if (!docDataModel) {
    return false;
  }
  const segmentId = (_a = params.selection) == null ? void 0 : _a.segmentId;
  const body = (_b = docDataModel.getSelfOrHeaderFooterModel(segmentId)) == null ? void 0 : _b.getBody();
  if (!body) return false;
  const docSelectionManagerService = accessor.get(DocSelectionManagerService);
  const selection = (_c = params.selection) != null ? _c : docSelectionManagerService.getActiveTextRange();
  if (!selection || !body) {
    return false;
  }
  const textRanges = (_d = params.textRanges) != null ? _d : [{
    startOffset: selection.startOffset + insertBody.dataStream.length,
    endOffset: selection.startOffset + insertBody.dataStream.length,
    collapsed: true,
    segmentId
  }];
  const textX = BuildTextUtils.selection.replace({
    selection,
    body: insertBody,
    doc: docDataModel
  });
  if (!textX) {
    return false;
  }
  const doMutation = {
    id: RichTextEditingMutation.id,
    params: {
      unitId,
      actions: [],
      textRanges,
      debounce: true,
      segmentId
    },
    textX
  };
  const jsonX = JSONX.getInstance();
  doMutation.params.actions = jsonX.editOp(textX.serialize());
  return doMutation;
}

// ../packages/docs-ui/package.json
var package_default3 = {
  name: "@univerjs/docs-ui",
  version: "0.25.2",
  private: false,
  description: "Editor UI layer for Univer Docs.",
  author: "DreamNum Co., Ltd. <developer@univer.ai>",
  license: "Apache-2.0",
  funding: {
    type: "opencollective",
    url: "https://opencollective.com/univer"
  },
  homepage: "https://univer.ai",
  repository: {
    type: "git",
    url: "https://github.com/dream-num/univer"
  },
  bugs: {
    url: "https://github.com/dream-num/univer/issues"
  },
  keywords: [
    "univer",
    "docs",
    "document",
    "editor",
    "ui"
  ],
  exports: {
    ".": "./src/index.ts",
    "./*": "./src/*",
    "./locale/*": "./src/locale/*.ts",
    "./facade": "./src/facade/index.ts"
  },
  main: "./src/index.ts",
  types: "./lib/types/index.d.ts",
  publishConfig: {
    access: "public",
    main: "./lib/es/index.js",
    module: "./lib/es/index.js",
    exports: {
      ".": {
        import: "./lib/es/index.js",
        require: "./lib/cjs/index.js",
        types: "./lib/types/index.d.ts"
      },
      "./*": {
        import: "./lib/es/*",
        require: "./lib/cjs/*",
        types: "./lib/types/index.d.ts"
      },
      "./locale/*": {
        import: "./lib/es/locale/*.js",
        require: "./lib/cjs/locale/*.js",
        types: "./lib/types/locale/*.d.ts"
      },
      "./facade": {
        import: "./lib/es/facade.js",
        require: "./lib/cjs/facade.js",
        types: "./lib/types/facade/index.d.ts"
      },
      "./lib/facade": {
        import: "./lib/es/facade.js",
        require: "./lib/cjs/facade.js",
        types: "./lib/types/facade/index.d.ts"
      },
      "./lib/*": "./lib/*"
    }
  },
  directories: {
    lib: "lib"
  },
  files: [
    "lib"
  ],
  scripts: {
    test: "vitest run",
    "test:watch": "vitest",
    coverage: "vitest run --coverage",
    typecheck: "tsc --noEmit",
    "build:bundle": "univer-cli build",
    "build:types": "tsc -p tsconfig.node.json",
    build: "pnpm run build:bundle && pnpm run build:types"
  },
  peerDependencies: {
    react: "^16.9.0 || ^17.0.0 || ^18.0.0 || ^19.0.0 || ^19.0.0-rc",
    rxjs: ">=7.0.0"
  },
  dependencies: {
    "@univerjs/core": "workspace:*",
    "@univerjs/design": "workspace:*",
    "@univerjs/docs": "workspace:*",
    "@univerjs/drawing": "workspace:*",
    "@univerjs/engine-render": "workspace:*",
    "@univerjs/icons": "1.4.0",
    "@univerjs/ui": "workspace:*"
  },
  devDependencies: {
    "@univerjs-infra/shared": "workspace:*",
    "@univerjs/docs-drawing": "workspace:*",
    "jest-canvas-mock": "^2.5.2",
    jsdom: "^27.0.1",
    postcss: "^8.5.15",
    react: "18.3.1",
    rxjs: "^7.8.2",
    tailwindcss: "3.4.18",
    typescript: "^6.0.3",
    vitest: "^4.1.7"
  }
};

// ../packages/docs-ui/src/services/doc-auto-format.service.ts
var DocAutoFormatService = class extends Disposable {
  constructor(_univerInstanceService, _textSelectionManagerService) {
    super();
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_textSelectionManagerService", _textSelectionManagerService);
    __publicField(this, "_matches", /* @__PURE__ */ new Map());
  }
  registerAutoFormat(match) {
    const matchList = this._matches.get(match.id);
    if (matchList) {
      matchList.push(match);
      matchList.sort((a, b) => {
        var _a, _b;
        return ((_a = b.priority) != null ? _a : 0) - ((_b = a.priority) != null ? _b : 0);
      });
    } else {
      this._matches.set(match.id, [match]);
    }
    return toDisposable(() => {
      const matchList2 = this._matches.get(match.id);
      if (matchList2) {
        const index = matchList2.findIndex((i) => i === match);
        if (index >= 0) {
          matchList2.splice(index, 1);
        }
      }
    });
  }
  onAutoFormat(id, params) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i;
    const autoFormats = (_a = this._matches.get(id)) != null ? _a : [];
    const unit = this._univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const docRanges = this._textSelectionManagerService.getDocRanges();
    const selection = (_b = docRanges.find((range) => range.isActive)) != null ? _b : docRanges[0];
    if (unit && selection) {
      const doc = unit.getSelfOrHeaderFooterModel(selection.segmentId);
      const context = {
        unit: doc,
        selection,
        isBody: !selection.segmentId,
        paragraphs: BuildTextUtils.range.getParagraphsInRange(selection, (_d = (_c = doc.getBody()) == null ? void 0 : _c.paragraphs) != null ? _d : [], (_f = (_e = doc.getBody()) == null ? void 0 : _e.dataStream) != null ? _f : ""),
        customRanges: BuildTextUtils.customRange.getCustomRangesInterestsWithSelection(selection, (_h = (_g = doc.getBody()) == null ? void 0 : _g.customRanges) != null ? _h : []),
        commandId: id,
        commandParams: params
      };
      const matched = autoFormats.find((i) => i.match(context));
      return (_i = matched == null ? void 0 : matched.getMutations(context)) != null ? _i : [];
    }
    return [];
  }
};
DocAutoFormatService = __decorateClass([
  __decorateParam(0, IUniverInstanceService),
  __decorateParam(1, Inject(DocSelectionManagerService))
], DocAutoFormatService);

// ../packages/docs-ui/src/commands/commands/auto-format.command.ts
var TabCommandId = "doc.command.tab";
var TabCommand = {
  id: TabCommandId,
  type: 0 /* COMMAND */,
  async handler(accessor, params) {
    const autoFormatService = accessor.get(DocAutoFormatService);
    const mutations = autoFormatService.onAutoFormat(TabCommand.id, params);
    return (await sequenceExecuteAsync(mutations, accessor.get(ICommandService))).result;
  }
};
var AfterSpaceCommandId = "doc.command.after-space";
var AfterSpaceCommand = {
  id: AfterSpaceCommandId,
  type: 0 /* COMMAND */,
  async handler(accessor) {
    const autoFormatService = accessor.get(DocAutoFormatService);
    const mutations = autoFormatService.onAutoFormat(AfterSpaceCommand.id);
    return (await sequenceExecuteAsync(mutations, accessor.get(ICommandService))).result;
  }
};
var EnterCommand = {
  id: "doc.command.enter",
  type: 0 /* COMMAND */,
  async handler(accessor) {
    const autoFormatService = accessor.get(DocAutoFormatService);
    const mutations = autoFormatService.onAutoFormat(EnterCommand.id);
    return (await sequenceExecuteAsync(mutations, accessor.get(ICommandService))).result;
  }
};

// ../packages/docs-ui/src/basics/paragraph.ts
function getTextRunAtPosition(body, position, defaultStyle, cacheStyle, isCellEditor) {
  const { textRuns = [], dataStream } = body;
  const isFormula = isCellEditor && dataStream.startsWith("=");
  const retTextRun = {
    st: 0,
    ed: 0,
    ts: {}
  };
  if (isFormula) {
    return retTextRun;
  }
  for (let i = textRuns.length - 1; i >= 0; i--) {
    const textRun = textRuns[i];
    const { st, ed } = textRun;
    if (position > st && position <= ed) {
      retTextRun.st = st;
      retTextRun.ed = ed;
      retTextRun.ts = {
        ...retTextRun.ts,
        ...textRun.ts
      };
    }
  }
  if (position === 0) {
    const textRun = textRuns == null ? void 0 : textRuns[0];
    if (textRun && textRun.st === 0) {
      retTextRun.ts = {
        ...retTextRun.ts,
        ...textRun.ts
      };
    }
  }
  if (cacheStyle) {
    retTextRun.ts = {
      ...retTextRun.ts,
      ...cacheStyle
    };
  }
  return retTextRun;
}
function getCustomRangeAtPosition(customRanges, position, extendRange) {
  if (extendRange) {
    const range2 = customRanges.find((customRange) => position >= customRange.startIndex && position <= customRange.endIndex + 1);
    return (range2 == null ? void 0 : range2.wholeEntity) ? null : range2;
  }
  const range = customRanges.find((customRange) => position > customRange.startIndex && position <= customRange.endIndex);
  return (range == null ? void 0 : range.wholeEntity) ? null : range;
}
function getCustomDecorationAtPosition(customDecorations, position) {
  return customDecorations.filter((customDecoration) => position > customDecoration.startIndex && position <= customDecoration.endIndex);
}

// ../packages/docs-ui/src/services/doc-menu-style.service.ts
var BODY_DEFAULT_FONTSIZE = 11;
var HEADER_FOOTER_DEFAULT_FONTSIZE = 9;
var DEFAULT_TEXT_STYLE = {
  /**
   * fontFamily
   */
  ff: "Arial",
  /**
   * fontSize
   */
  fs: BODY_DEFAULT_FONTSIZE
};
var DocMenuStyleService = class extends Disposable {
  constructor(_textSelectionManagerService, _univerInstanceService, _renderManagerService) {
    super();
    __publicField(this, "_textSelectionManagerService", _textSelectionManagerService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_renderManagerService", _renderManagerService);
    __publicField(this, "_cacheStyle", null);
    this._init();
  }
  _init() {
    this._listenDocRangeChange();
  }
  _listenDocRangeChange() {
    this.disposeWithMe(
      this._textSelectionManagerService.textSelection$.subscribe(() => {
        this._clearStyleCache();
      })
    );
  }
  getStyleCache() {
    return this._cacheStyle;
  }
  getDefaultStyle() {
    var _a;
    const docDataModel = this._univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    if (docDataModel == null) {
      return {
        ...DEFAULT_TEXT_STYLE
      };
    }
    const unitId = docDataModel == null ? void 0 : docDataModel.getUnitId();
    const docSkeletonManagerService = (_a = this._renderManagerService.getRenderById(unitId)) == null ? void 0 : _a.with(DocSkeletonManagerService);
    const docViewModel = docSkeletonManagerService == null ? void 0 : docSkeletonManagerService.getViewModel();
    if (docViewModel == null) {
      return {
        ...DEFAULT_TEXT_STYLE
      };
    }
    const editArea = docViewModel.getEditArea();
    if (editArea === "BODY" /* BODY */) {
      return {
        ...DEFAULT_TEXT_STYLE
      };
    } else {
      return {
        ...DEFAULT_TEXT_STYLE,
        fs: HEADER_FOOTER_DEFAULT_FONTSIZE
      };
    }
  }
  setStyleCache(style) {
    this._cacheStyle = {
      ...this._cacheStyle,
      ...style
    };
  }
  _clearStyleCache() {
    this._cacheStyle = null;
  }
};
DocMenuStyleService = __decorateClass([
  __decorateParam(0, Inject(DocSelectionManagerService)),
  __decorateParam(1, IUniverInstanceService),
  __decorateParam(2, IRenderManagerService)
], DocMenuStyleService);

// ../packages/docs-ui/src/commands/util.ts
function getCommandSkeleton(accessor, unitId) {
  var _a;
  const renderManagerService = accessor.get(IRenderManagerService);
  return (_a = renderManagerService.getRenderById(unitId)) == null ? void 0 : _a.with(DocSkeletonManagerService);
}
function getRichTextEditPath2(docDataModel, segmentId = "") {
  if (!segmentId) {
    return ["body"];
  }
  const { headers, footers } = docDataModel.getSnapshot();
  if (headers == null && footers == null) {
    throw new Error("Document data model must have headers or footers when update by segment id");
  }
  if ((headers == null ? void 0 : headers[segmentId]) != null) {
    return ["headers", segmentId, "body"];
  } else if ((footers == null ? void 0 : footers[segmentId]) != null) {
    return ["footers", segmentId, "body"];
  } else {
    throw new Error("Segment id not found in headers or footers");
  }
}

// ../packages/docs-ui/src/commands/commands/break-line.command.ts
function generateParagraphs(dataStream, prevParagraph, borderBottom) {
  const paragraphs = [];
  for (let i = 0, len = dataStream.length; i < len; i++) {
    const char = dataStream[i];
    if (char !== "\r" /* PARAGRAPH */) {
      continue;
    }
    paragraphs.push({
      startIndex: i
    });
  }
  if (prevParagraph) {
    for (const paragraph of paragraphs) {
      if (prevParagraph.bullet) {
        paragraph.bullet = Tools.deepClone(prevParagraph.bullet);
      }
      if (prevParagraph.paragraphStyle) {
        paragraph.paragraphStyle = Tools.deepClone(prevParagraph.paragraphStyle);
        delete paragraph.paragraphStyle.borderBottom;
        if (prevParagraph.paragraphStyle.headingId) {
          paragraph.paragraphStyle.headingId = generateRandomId(6);
        }
      }
    }
  }
  if (borderBottom) {
    for (const paragraph of paragraphs) {
      if (!paragraph.paragraphStyle) {
        paragraph.paragraphStyle = {};
      }
      paragraph.paragraphStyle.borderBottom = borderBottom;
    }
  }
  return paragraphs;
}
var BreakLineCommand = {
  id: "doc.command.break-line",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function, complexity
  handler: (accessor, params) => {
    var _a, _b, _c, _d, _e;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const docMenuStyleService = accessor.get(DocMenuStyleService);
    const activeTextRange = (_a = params == null ? void 0 : params.textRange) != null ? _a : docSelectionManagerService.getActiveTextRange();
    const rectRanges = docSelectionManagerService.getRectRanges();
    if (activeTextRange == null) {
      return false;
    }
    if (rectRanges && rectRanges.length) {
      const { startOffset: startOffset2 } = activeTextRange;
      docSelectionManagerService.replaceDocRanges([{
        startOffset: startOffset2,
        endOffset: startOffset2
      }]);
      return true;
    }
    const { horizontalLine } = params != null ? params : {};
    const { segmentId } = activeTextRange;
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const originBody = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId != null ? segmentId : "").getBody();
    if (docDataModel == null || originBody == null) {
      return false;
    }
    const unitId = docDataModel.getUnitId();
    const { startOffset, endOffset } = activeTextRange;
    const paragraphs = (_b = originBody.paragraphs) != null ? _b : [];
    const prevParagraph = paragraphs.find((p) => p.startIndex >= startOffset);
    if (!prevParagraph) {
      return false;
    }
    const isAtParagraphEnd = startOffset === prevParagraph.startIndex;
    const prevParagraphIndex = prevParagraph.startIndex;
    const defaultTextStyle = docMenuStyleService.getDefaultStyle();
    const styleCache = docMenuStyleService.getStyleCache();
    const curTextRun = getTextRunAtPosition(originBody, endOffset, defaultTextStyle, styleCache);
    const insertBody = {
      dataStream: "\r" /* PARAGRAPH */,
      paragraphs: generateParagraphs(
        "\r" /* PARAGRAPH */,
        prevParagraph,
        horizontalLine
      ),
      textRuns: [{
        st: 0,
        ed: 1,
        ts: {
          ...curTextRun.ts
        }
      }]
    };
    if (docDataModel == null) {
      return false;
    }
    const activeRange = docSelectionManagerService.getActiveTextRange();
    if (originBody == null) {
      return false;
    }
    const { collapsed } = activeTextRange;
    const cursorMove = insertBody.dataStream.length;
    const textRanges = [
      {
        startOffset: startOffset + cursorMove,
        endOffset: startOffset + cursorMove,
        style: activeRange == null ? void 0 : activeRange.style,
        collapsed
      }
    ];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges,
        debounce: true
      }
    };
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    if (collapsed) {
      if (startOffset > 0) {
        textX.push({
          t: "r" /* RETAIN */,
          len: startOffset
        });
      }
      textX.push({
        t: "i" /* INSERT */,
        body: insertBody,
        len: insertBody.dataStream.length
      });
    } else {
      const dos = BuildTextUtils.selection.delete([activeTextRange], originBody, 0, insertBody);
      textX.push(...dos);
    }
    if (((_c = prevParagraph.bullet) == null ? void 0 : _c.listType) === "CHECK_LIST_CHECKED" /* CHECK_LIST_CHECKED */ || ((_d = prevParagraph.paragraphStyle) == null ? void 0 : _d.headingId)) {
      if (activeTextRange.endOffset < prevParagraphIndex) {
        textX.push({
          t: "r" /* RETAIN */,
          len: prevParagraphIndex - activeTextRange.endOffset
        });
      }
      textX.push({
        t: "r" /* RETAIN */,
        len: 1,
        body: {
          dataStream: "",
          paragraphs: [
            {
              ...prevParagraph,
              paragraphStyle: {
                ...prevParagraph.paragraphStyle,
                ...isAtParagraphEnd ? {
                  headingId: void 0,
                  namedStyleType: void 0
                } : null
              },
              startIndex: 0,
              bullet: ((_e = prevParagraph.paragraphStyle) == null ? void 0 : _e.headingId) ? void 0 : {
                ...prevParagraph.bullet,
                listType: "CHECK_LIST" /* CHECK_LIST */
              }
            }
          ]
        },
        coverType: 1 /* REPLACE */
      });
    }
    doMutation.params.textRanges = [{
      startOffset: startOffset + cursorMove,
      endOffset: startOffset + cursorMove,
      collapsed
    }];
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};

// ../packages/docs-ui/src/commands/commands/table/table.ts
function genEmptyTable(rowCount, colCount) {
  let dataStream = "" /* TABLE_START */;
  const paragraphs = [];
  const sectionBreaks = [];
  for (let i = 0; i < rowCount; i++) {
    dataStream += "\x1B" /* TABLE_ROW_START */;
    for (let j = 0; j < colCount; j++) {
      dataStream += `${"" /* TABLE_CELL_START */}\r
${"" /* TABLE_CELL_END */}`;
      paragraphs.push({
        startIndex: dataStream.length - 3,
        paragraphStyle: {
          spaceAbove: { v: 3 },
          lineSpacing: 2,
          spaceBelow: { v: 0 }
        }
      });
      sectionBreaks.push({
        startIndex: dataStream.length - 2
      });
    }
    dataStream += "" /* TABLE_ROW_END */;
  }
  dataStream += "" /* TABLE_END */;
  return {
    dataStream,
    paragraphs,
    sectionBreaks
  };
}
function getEmptyTableCell() {
  const tableCell = {
    margin: {
      start: {
        v: 10
      },
      end: {
        v: 10
      },
      top: {
        v: 5
      },
      bottom: {
        v: 5
      }
    }
  };
  return tableCell;
}
function getEmptyTableRow(col) {
  const tableCell = getEmptyTableCell();
  const tableRow = {
    tableCells: [...new Array(col).fill(null).map(() => Tools.deepClone(tableCell))],
    trHeight: {
      val: { v: 30 },
      hRule: 0 /* AUTO */
    }
  };
  return tableRow;
}
function getTableColumn(width) {
  const tableColumn = {
    size: {
      type: 1 /* SPECIFIED */,
      width: {
        v: width
      }
    }
  };
  return tableColumn;
}
function genTableSource(rowCount, colCount, pageContentWidth) {
  const tableColumn = getTableColumn(pageContentWidth / colCount);
  const tableRow = getEmptyTableRow(colCount);
  const tableRows = [...new Array(rowCount).fill(null).map(() => Tools.deepClone(tableRow))];
  const tableColumns = [...new Array(colCount).fill(null).map(() => Tools.deepClone(tableColumn))];
  const tableId = generateRandomId(6);
  const table = {
    tableRows,
    tableColumns,
    tableId,
    align: 0 /* START */,
    indent: {
      v: 0
    },
    textWrap: 0 /* NONE */,
    position: {
      positionH: {
        relativeFrom: 0 /* PAGE */,
        posOffset: 0
      },
      positionV: {
        relativeFrom: 0 /* PAGE */,
        posOffset: 0
      }
    },
    dist: {
      distB: 0,
      distL: 0,
      distR: 0,
      distT: 0
    },
    cellMargin: {
      start: {
        v: 10
      },
      end: {
        v: 10
      },
      top: {
        v: 5
      },
      bottom: {
        v: 5
      }
    },
    size: {
      type: 0 /* UNSPECIFIED */,
      width: {
        v: pageContentWidth
      }
    }
  };
  return table;
}
function getRangeInfoFromRanges(textRange, rectRanges) {
  if (!textRange && !rectRanges) {
    return null;
  }
  if (rectRanges && rectRanges.length > 0) {
    let startOffset = Number.POSITIVE_INFINITY;
    let endOffset = Number.NEGATIVE_INFINITY;
    const segmentId = "";
    for (const rectRange of rectRanges) {
      const { startOffset: st, endOffset: ed, segmentId: sid } = rectRange;
      if (st == null || ed == null || sid == null) {
        continue;
      }
      startOffset = Math.min(startOffset, st);
      endOffset = Math.max(endOffset, ed);
    }
    if (Number.isFinite(startOffset) && Number.isFinite(endOffset)) {
      return {
        startOffset,
        endOffset,
        segmentId
      };
    }
  } else if (textRange) {
    const { startOffset, endOffset, segmentId } = textRange;
    if (startOffset == null || endOffset == null || segmentId == null) {
      return null;
    }
    return {
      startOffset,
      endOffset,
      segmentId
    };
  }
}
function getInsertRowBody(col) {
  let dataStream = "\x1B" /* TABLE_ROW_START */;
  const paragraphs = [];
  const sectionBreaks = [];
  for (let i = 0; i < col; i++) {
    dataStream += `${"" /* TABLE_CELL_START */}\r
${"" /* TABLE_CELL_END */}`;
    paragraphs.push({
      startIndex: dataStream.length - 3,
      paragraphStyle: {
        spaceAbove: { v: 3 },
        lineSpacing: 2,
        spaceBelow: { v: 0 }
      }
    });
    sectionBreaks.push({
      startIndex: dataStream.length - 2
    });
  }
  dataStream += "" /* TABLE_ROW_END */;
  return {
    dataStream,
    paragraphs,
    sectionBreaks
  };
}
function getInsertColumnBody() {
  const dataStream = `${"" /* TABLE_CELL_START */}\r
${"" /* TABLE_CELL_END */}`;
  const paragraphs = [];
  const sectionBreaks = [];
  paragraphs.push({
    startIndex: 1,
    paragraphStyle: {
      spaceAbove: { v: 3 },
      lineSpacing: 2,
      spaceBelow: { v: 0 }
    }
  });
  sectionBreaks.push({
    startIndex: 2
  });
  return {
    dataStream,
    paragraphs,
    sectionBreaks
  };
}
function getInsertRowActionsParams(rangeInfo, position, viewModel) {
  var _a, _b, _c;
  const { startOffset, endOffset, segmentId } = rangeInfo;
  const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
  const index = position === 0 /* ABOVE */ ? startOffset : endOffset;
  let tableRow = null;
  const tableId = (_c = (_b = (_a = viewModel.getBody()) == null ? void 0 : _a.tables) == null ? void 0 : _b.find((t) => index >= t.startIndex && index <= t.endIndex)) == null ? void 0 : _c.tableId;
  let rowIndex = 0;
  for (const section of vm.getChildren()) {
    for (const paragraph of section.children) {
      const { children } = paragraph;
      const table = children[0];
      if (table) {
        for (const row of table.children) {
          if (row.startIndex <= index && index <= row.endIndex) {
            rowIndex = table.children.indexOf(row);
            tableRow = row;
            break;
          }
        }
      }
      if (tableRow) {
        break;
      }
    }
    if (tableRow) {
      break;
    }
  }
  if (tableRow == null || tableId == null) {
    return null;
  }
  return {
    offset: position === 0 /* ABOVE */ ? tableRow.startIndex : tableRow.endIndex + 1,
    colCount: tableRow.children.length,
    tableId,
    insertRowIndex: position === 0 /* ABOVE */ ? rowIndex : rowIndex + 1
  };
}
function getInsertColumnActionsParams(rangeInfo, position, viewModel) {
  var _a, _b, _c;
  const { startOffset, endOffset, segmentId } = rangeInfo;
  const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
  const index = position === 0 /* LEFT */ ? startOffset : endOffset;
  const tableId = (_c = (_b = (_a = viewModel.getBody()) == null ? void 0 : _a.tables) == null ? void 0 : _b.find((t) => index >= t.startIndex && index <= t.endIndex)) == null ? void 0 : _c.tableId;
  const offsets = [];
  let table = null;
  let columnIndex = -1;
  for (const section of vm.getChildren()) {
    for (const paragraph of section.children) {
      const { children } = paragraph;
      const tableNode = children[0];
      if (tableNode) {
        if (index < tableNode.startIndex || index > tableNode.endIndex) {
          continue;
        }
        table = tableNode;
        for (const row of tableNode.children) {
          for (const cell of row.children) {
            const cellIndex = row.children.indexOf(cell);
            if (index >= cell.startIndex && index <= cell.endIndex) {
              columnIndex = cellIndex;
              break;
            }
          }
          if (columnIndex !== -1) {
            break;
          }
        }
      }
      if (table) {
        break;
      }
    }
    if (table) {
      break;
    }
  }
  if (table == null || tableId == null || columnIndex === -1) {
    return null;
  }
  let cursor = 0;
  for (const row of table.children) {
    const cell = row.children[columnIndex];
    const insertIndex = position === 0 /* LEFT */ ? cell.startIndex : cell.endIndex + 1;
    offsets.push(insertIndex - cursor);
    cursor = insertIndex;
  }
  return {
    offsets,
    tableId,
    columnIndex,
    rowCount: table.children.length
  };
}
function getColumnWidths(pageWidth, tableColumns, insertColumnIndex) {
  const widths = [];
  let newColWidth = tableColumns[insertColumnIndex].size.width.v;
  let totalWidth = 0;
  for (let i = 0; i < tableColumns.length; i++) {
    totalWidth += tableColumns[i].size.width.v;
  }
  totalWidth += newColWidth;
  for (let i = 0; i < tableColumns.length; i++) {
    widths.push(tableColumns[i].size.width.v / totalWidth * pageWidth);
  }
  newColWidth = newColWidth / totalWidth * pageWidth;
  return {
    widths,
    newColWidth
  };
}
function getDeleteRowsActionsParams(rangeInfo, viewModel) {
  var _a, _b, _c;
  const { startOffset, endOffset, segmentId } = rangeInfo;
  const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
  const tableId = (_c = (_b = (_a = viewModel.getBody()) == null ? void 0 : _a.tables) == null ? void 0 : _b.find((t) => startOffset >= t.startIndex && endOffset <= t.endIndex)) == null ? void 0 : _c.tableId;
  const rowIndexes = [];
  let offset = -1;
  let len = 0;
  let cursor = -1;
  let selectWholeTable = false;
  for (const section of vm.getChildren()) {
    for (const paragraph of section.children) {
      const { children } = paragraph;
      const table = children[0];
      if (table) {
        if (startOffset < table.startIndex || endOffset > table.endIndex) {
          continue;
        }
        cursor = table.startIndex + 3;
        for (const row of table.children) {
          const rowIndex = table.children.indexOf(row);
          const { startIndex, endIndex } = row;
          if (startOffset >= startIndex && startOffset <= endIndex) {
            offset = startIndex;
            rowIndexes.push(rowIndex);
            len += endIndex - startIndex + 1;
          } else if (startIndex > startOffset && endIndex < endOffset) {
            rowIndexes.push(rowIndex);
            len += endIndex - startIndex + 1;
          } else if (endOffset >= startIndex && endOffset <= endIndex) {
            rowIndexes.push(rowIndex);
            len += endIndex - startIndex + 1;
          }
          if (rowIndexes.length === table.children.length) {
            selectWholeTable = true;
          }
        }
      }
      if (rowIndexes.length) {
        break;
      }
    }
    if (rowIndexes.length) {
      break;
    }
  }
  if (tableId == null || rowIndexes.length === 0) {
    return null;
  }
  return {
    tableId,
    rowIndexes,
    offset,
    len,
    cursor,
    selectWholeTable
  };
}
function getDeleteColumnsActionParams(rangeInfo, viewModel) {
  var _a, _b, _c;
  const { startOffset, endOffset, segmentId } = rangeInfo;
  const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
  const tableId = (_c = (_b = (_a = viewModel.getBody()) == null ? void 0 : _a.tables) == null ? void 0 : _b.find((t) => startOffset >= t.startIndex && endOffset <= t.endIndex)) == null ? void 0 : _c.tableId;
  const offsets = [];
  let table = null;
  const columnIndexes = [];
  let cursor = -1;
  let startColumnIndex = -1;
  let endColumnIndex = -1;
  for (const section of vm.getChildren()) {
    for (const paragraph of section.children) {
      const { children } = paragraph;
      const tableNode = children[0];
      if (tableNode) {
        if (startOffset < tableNode.startIndex || endOffset > tableNode.endIndex) {
          continue;
        }
        table = tableNode;
        for (const row of tableNode.children) {
          for (const cell of row.children) {
            const cellIndex = row.children.indexOf(cell);
            if (startOffset >= cell.startIndex && startOffset <= cell.endIndex) {
              startColumnIndex = cellIndex;
            }
            if (endOffset >= cell.startIndex && endOffset <= cell.endIndex) {
              endColumnIndex = cellIndex;
            }
          }
        }
      }
      if (table) {
        break;
      }
    }
    if (table) {
      break;
    }
  }
  if (table == null || tableId == null) {
    return null;
  }
  for (let i = startColumnIndex; i <= endColumnIndex; i++) {
    columnIndexes.push(i);
  }
  let delta = 0;
  for (const row of table.children) {
    const startCell = row.children[startColumnIndex];
    const endCell = row.children[endColumnIndex];
    offsets.push({
      retain: startCell.startIndex - delta,
      delete: endCell.endIndex - startCell.startIndex + 1
    });
    delta = endCell.endIndex + 1;
  }
  cursor = table.startIndex + 3;
  return {
    offsets,
    tableId,
    columnIndexes,
    cursor,
    selectWholeTable: columnIndexes.length === table.children[0].children.length,
    rowCount: table.children.length
  };
}
function getDeleteTableActionParams(rangeInfo, viewModel) {
  var _a, _b, _c;
  const { startOffset, endOffset, segmentId } = rangeInfo;
  const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
  const tableId = (_c = (_b = (_a = viewModel.getBody()) == null ? void 0 : _a.tables) == null ? void 0 : _b.find((t) => startOffset >= t.startIndex && endOffset <= t.endIndex)) == null ? void 0 : _c.tableId;
  let offset = -1;
  let len = 0;
  let cursor = -1;
  for (const section of vm.getChildren()) {
    for (const paragraph of section.children) {
      const { children } = paragraph;
      const table = children[0];
      if (table) {
        if (startOffset < table.startIndex || endOffset > table.endIndex) {
          continue;
        }
        offset = table.startIndex;
        len = table.endIndex - table.startIndex + 1;
        cursor = table.startIndex;
      }
      if (table) {
        break;
      }
    }
    if (len > 0) {
      break;
    }
  }
  if (tableId == null) {
    return null;
  }
  return {
    tableId,
    offset,
    len,
    cursor
  };
}
function getDeleteRowContentActionParams(rangeInfo, viewModel) {
  var _a, _b, _c;
  const { startOffset, endOffset, segmentId } = rangeInfo;
  const vm = viewModel.getSelfOrHeaderFooterViewModel(segmentId);
  const tableId = (_c = (_b = (_a = viewModel.getBody()) == null ? void 0 : _a.tables) == null ? void 0 : _b.find((t) => startOffset >= t.startIndex && endOffset <= t.endIndex)) == null ? void 0 : _c.tableId;
  const offsets = [];
  let table = null;
  let cursor = -1;
  let rowIndex = -1;
  let startColumnIndex = -1;
  let endColumnIndex = -1;
  for (const section of vm.getChildren()) {
    for (const paragraph of section.children) {
      const { children } = paragraph;
      const tableNode = children[0];
      if (tableNode) {
        if (startOffset < tableNode.startIndex || endOffset > tableNode.endIndex) {
          continue;
        }
        table = tableNode;
        for (const row2 of tableNode.children) {
          const rIndex = tableNode.children.indexOf(row2);
          for (const cell of row2.children) {
            const cellIndex = row2.children.indexOf(cell);
            if (startOffset >= cell.startIndex && startOffset <= cell.endIndex) {
              rowIndex = rIndex;
              startColumnIndex = cellIndex;
            }
            if (endOffset >= cell.startIndex && endOffset <= cell.endIndex) {
              endColumnIndex = cellIndex;
            }
          }
        }
      }
      if (table) {
        break;
      }
    }
    if (table) {
      break;
    }
  }
  if (table == null || tableId == null || rowIndex === -1) {
    return null;
  }
  const row = table.children[rowIndex];
  for (let i = startColumnIndex; i <= endColumnIndex; i++) {
    const cell = row.children[i];
    offsets.push({
      retain: cell.startIndex + 1,
      delete: cell.endIndex - cell.startIndex - 3
    });
  }
  cursor = table.startIndex + 3;
  return {
    offsets,
    tableId,
    cursor,
    rowCount: table.children.length
  };
}
function getCellOffsets(viewModel, range, position) {
  const { startOffset } = range;
  let targetTable = null;
  for (const section of viewModel.getChildren()) {
    for (const paragraph of section.children) {
      const table = paragraph.children[0];
      if (table) {
        if (startOffset > table.startIndex && startOffset < table.endIndex) {
          targetTable = table;
          break;
        }
      }
    }
    if (targetTable) {
      break;
    }
  }
  if (targetTable == null) {
    return null;
  }
  let cellIndex = -1;
  let rowIndex = -1;
  let targetRow = null;
  for (const row of targetTable.children) {
    for (const cell of row.children) {
      if (startOffset > cell.startIndex && startOffset < cell.endIndex) {
        cellIndex = row.children.indexOf(cell);
        rowIndex = targetTable.children.indexOf(row);
        targetRow = row;
        break;
      }
    }
    if (cellIndex > -1) {
      break;
    }
  }
  if (cellIndex === -1 || rowIndex === -1 || targetRow == null) {
    return null;
  }
  let newCell = null;
  if (position === 0 /* NEXT */) {
    newCell = targetRow.children[cellIndex + 1];
    if (!newCell) {
      const nextRow = targetTable.children[rowIndex + 1];
      if (nextRow) {
        newCell = nextRow.children[0];
      }
    }
  } else {
    newCell = targetRow.children[cellIndex - 1];
    if (!newCell) {
      const prevRow = targetTable.children[rowIndex - 1];
      if (prevRow) {
        newCell = prevRow.children[prevRow.children.length - 1];
      }
    }
  }
  if (newCell) {
    const { startIndex, endIndex } = newCell;
    return {
      startOffset: startIndex + 1,
      endOffset: endIndex - 2
    };
  }
}

// ../packages/docs-ui/src/commands/commands/clipboard.inner.command.ts
function getCustomBlockIdsInSelections(body, selections) {
  const customBlockIds = [];
  const { customBlocks = [] } = body;
  for (const selection of selections) {
    const { startOffset, endOffset } = selection;
    if (startOffset == null || endOffset == null) {
      continue;
    }
    for (const customBlock of customBlocks) {
      const { startIndex } = customBlock;
      if (startIndex >= startOffset && startIndex < endOffset) {
        customBlockIds.push(customBlock.blockId);
      }
    }
  }
  return customBlockIds;
}
function hasRangeInTable(ranges) {
  return ranges.some((range) => {
    const { startNodePosition } = range;
    return startNodePosition ? (startNodePosition == null ? void 0 : startNodePosition.path.indexOf("cells")) > -1 : false;
  });
}
var UNITS = SHEET_EDITOR_UNITS;
var InnerPasteCommand = {
  id: "doc.command.inner-paste",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function, complexity
  handler: async (accessor, params) => {
    var _a, _b, _c, _d, _e, _f;
    const { segmentId, textRanges, doc } = params;
    const commandService = accessor.get(ICommandService);
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const selections = docSelectionManagerService.getTextRanges();
    const rectRanges = docSelectionManagerService.getRectRanges();
    const { body, tableSource, drawings } = doc;
    if (!Array.isArray(selections) || selections.length === 0 || body == null) {
      return false;
    }
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    const originBody = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (docDataModel == null || originBody == null) {
      return false;
    }
    const unitId = docDataModel.getUnitId();
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges,
        segmentId
      }
    };
    const memoryCursor = new MemoryCursor();
    memoryCursor.reset();
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const rawActions = [];
    const hasTable = !!((_a = body.tables) == null ? void 0 : _a.length);
    const hasCustomBlock = !!((_b = body.customBlocks) == null ? void 0 : _b.length);
    if (hasTable && segmentId) {
      return false;
    }
    if (hasTable && hasRangeInTable(selections)) {
      return false;
    }
    if (selections.length && (rectRanges == null ? void 0 : rectRanges.length)) {
      return false;
    }
    for (let i = 0; i < selections.length; i++) {
      const selection = selections[i];
      const { startOffset, endOffset, collapsed } = selection;
      const len = startOffset - memoryCursor.cursor;
      const cloneBody = Tools.deepClone(body);
      if (hasTable) {
        for (const t of cloneBody.tables) {
          const { tableId: oldTableId } = t;
          const tableId = generateRandomId(6);
          t.tableId = tableId;
          const table = Tools.deepClone(tableSource[oldTableId]);
          table.tableId = tableId;
          const action = jsonX.insertOp(["tableSource", tableId], table);
          rawActions.push(action);
        }
      }
      if (hasCustomBlock && drawings) {
        const drawingLen = (_d = (_c = docDataModel.getSnapshot().drawingsOrder) == null ? void 0 : _c.length) != null ? _d : 0;
        for (const block of cloneBody.customBlocks) {
          const { blockId } = block;
          const drawingId = generateRandomId(6);
          block.blockId = drawingId;
          const drawing = Tools.deepClone(drawings[blockId]);
          drawing.drawingId = drawingId;
          const action = jsonX.insertOp(["drawings", drawingId], drawing);
          const orderAction = jsonX.insertOp(["drawingsOrder", drawingLen], drawingId);
          rawActions.push(action);
          rawActions.push(orderAction);
        }
      }
      const customRange = getCustomRangeAtPosition((_e = originBody.customRanges) != null ? _e : [], endOffset, UNITS.includes(unitId));
      const customDecorations = getCustomDecorationAtPosition((_f = originBody.customDecorations) != null ? _f : [], endOffset);
      if (customRange) {
        cloneBody.customRanges = [{
          ...customRange,
          startIndex: 0,
          endIndex: body.dataStream.length - 1
        }];
      }
      if (customDecorations.length) {
        cloneBody.customDecorations = customDecorations.map((customDecoration) => ({
          ...customDecoration,
          startIndex: 0,
          endIndex: body.dataStream.length - 1
        }));
      }
      if (collapsed) {
        textX.push({
          t: "r" /* RETAIN */,
          len
        });
        textX.push({
          t: "i" /* INSERT */,
          body: cloneBody,
          len: body.dataStream.length
        });
      } else {
        const dos = BuildTextUtils.selection.delete([selection], body, memoryCursor.cursor, cloneBody, selections.length === 1);
        textX.push(...dos);
      }
      memoryCursor.reset();
      memoryCursor.moveCursor(endOffset);
    }
    const path = getRichTextEditPath2(docDataModel, segmentId);
    rawActions.push(jsonX.editOp(textX.serialize(), path));
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
function adjustSelectionByTable(selection, tables) {
  const { startOffset, endOffset } = selection;
  const endsWithTable = tables.some((t) => t.startIndex === endOffset);
  const newEndOffset = Math.max(startOffset, endsWithTable ? endOffset - 1 : endOffset);
  return {
    ...selection,
    endOffset: newEndOffset,
    collapsed: startOffset === newEndOffset
  };
}
function getCutActionsFromTextRanges(selections, docDataModel, segmentId) {
  var _a, _b;
  const originBody = docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
  const textX = new TextX();
  const jsonX = JSONX.getInstance();
  const rawActions = [];
  if (originBody == null) {
    return rawActions;
  }
  const { tables = [] } = originBody;
  const memoryCursor = new MemoryCursor();
  memoryCursor.reset();
  for (let i = 0; i < selections.length; i++) {
    const selection = adjustSelectionByTable(selections[i], tables);
    const { startOffset, endOffset, collapsed } = selection;
    const len = startOffset - memoryCursor.cursor;
    if (collapsed) {
      textX.push({
        t: "r" /* RETAIN */,
        len
      });
    } else {
      textX.push(...BuildTextUtils.selection.delete([selection], originBody, memoryCursor.cursor, null, false));
    }
    memoryCursor.reset();
    memoryCursor.moveCursor(endOffset);
  }
  const path = getRichTextEditPath2(docDataModel, segmentId);
  rawActions.push(jsonX.editOp(textX.serialize(), path));
  const removedCustomBlockIds = getCustomBlockIdsInSelections(originBody, selections);
  const drawings = (_a = docDataModel.getDrawings()) != null ? _a : {};
  const drawingOrder = (_b = docDataModel.getDrawingsOrder()) != null ? _b : [];
  const sortedRemovedCustomBlockIds = removedCustomBlockIds.sort((a, b) => {
    if (drawingOrder.indexOf(a) > drawingOrder.indexOf(b)) {
      return -1;
    } else if (drawingOrder.indexOf(a) < drawingOrder.indexOf(b)) {
      return 1;
    }
    return 0;
  });
  if (sortedRemovedCustomBlockIds.length > 0) {
    for (const blockId of sortedRemovedCustomBlockIds) {
      const drawing = drawings[blockId];
      const drawingIndex = drawingOrder.indexOf(blockId);
      if (drawing == null || drawingIndex < 0) {
        continue;
      }
      const removeDrawingAction = jsonX.removeOp(["drawings", blockId], drawing);
      const removeDrawingOrderAction = jsonX.removeOp(["drawingsOrder", drawingIndex], blockId);
      rawActions.push(removeDrawingAction);
      rawActions.push(removeDrawingOrderAction);
    }
  }
  return rawActions.reduce((acc, cur) => {
    return JSONX.compose(acc, cur);
  }, null);
}
function getCutActionsFromRectRanges(ranges, docDataModel, viewModel, segmentId) {
  const rawActions = [];
  const segmentBody = docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
  if (segmentBody == null) {
    return rawActions;
  }
  const textX = new TextX();
  const jsonX = JSONX.getInstance();
  const memoryCursor = new MemoryCursor();
  memoryCursor.reset();
  for (const range of ranges) {
    const { startOffset, endOffset, spanEntireRow, spanEntireTable } = range;
    if (startOffset == null || endOffset == null) {
      continue;
    }
    if (spanEntireTable) {
      const actionParams = getDeleteTableActionParams({ startOffset, endOffset, segmentId }, viewModel);
      if (actionParams == null) {
        continue;
      }
      const { offset, len, tableId } = actionParams;
      if (offset - memoryCursor.cursor > 0) {
        textX.push({
          t: "r" /* RETAIN */,
          len: offset - memoryCursor.cursor
        });
      }
      textX.push({
        t: "d" /* DELETE */,
        len
      });
      const action = jsonX.removeOp(["tableSource", tableId]);
      rawActions.push(action);
      memoryCursor.moveCursorTo(offset + len);
    } else if (spanEntireRow) {
      const actionParams = getDeleteRowsActionsParams({ startOffset, endOffset, segmentId }, viewModel);
      if (actionParams == null) {
        continue;
      }
      const { offset, rowIndexes, len, tableId } = actionParams;
      if (offset - memoryCursor.cursor > 0) {
        textX.push({
          t: "r" /* RETAIN */,
          len: offset - memoryCursor.cursor
        });
      }
      textX.push({
        t: "d" /* DELETE */,
        len
      });
      for (const index of rowIndexes.reverse()) {
        const action = jsonX.removeOp(["tableSource", tableId, "tableRows", index]);
        rawActions.push(action);
      }
      memoryCursor.moveCursorTo(offset + len);
    } else {
      const actionParams = getDeleteRowContentActionParams({ startOffset, endOffset, segmentId }, viewModel);
      if (actionParams == null) {
        continue;
      }
      const { offsets } = actionParams;
      for (const offset of offsets) {
        const { retain, delete: delLen } = offset;
        if (retain - memoryCursor.cursor > 0) {
          textX.push({
            t: "r" /* RETAIN */,
            len: retain - memoryCursor.cursor
          });
        }
        textX.push({
          t: "d" /* DELETE */,
          len: delLen
        });
        memoryCursor.moveCursorTo(retain + delLen);
      }
    }
  }
  const path = getRichTextEditPath2(docDataModel, segmentId);
  rawActions.push(jsonX.editOp(textX.serialize(), path));
  return rawActions.reduce((acc, cur) => {
    return JSONX.compose(acc, cur);
  }, null);
}
function getCutActionsFromDocRanges(textRanges, rectRanges, docDataModel, viewModel, segmentId) {
  let rawActions = [];
  if (Array.isArray(textRanges) && (textRanges == null ? void 0 : textRanges.length) !== 0) {
    rawActions = getCutActionsFromTextRanges(textRanges, docDataModel, segmentId);
  }
  if (Array.isArray(rectRanges) && (rectRanges == null ? void 0 : rectRanges.length) !== 0) {
    const actions = getCutActionsFromRectRanges(rectRanges, docDataModel, viewModel, segmentId);
    if (rawActions == null || rawActions.length === 0) {
      rawActions = actions;
    } else {
      rawActions = JSONX.compose(
        rawActions,
        JSONX.transform(actions, rawActions, "right")
      );
    }
  }
  return rawActions;
}
var CutContentCommand = {
  id: "doc.command.inner-cut",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    var _a;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const commandService = accessor.get(ICommandService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const { segmentId, textRanges, selections = docSelectionManagerService.getTextRanges(), rectRanges = docSelectionManagerService.getRectRanges() } = params;
    if ((!Array.isArray(selections) || selections.length === 0) && (!Array.isArray(rectRanges) || rectRanges.length === 0)) {
      return false;
    }
    const unitId = (_a = univerInstanceService.getCurrentUniverDocInstance()) == null ? void 0 : _a.getUnitId();
    if (!unitId) {
      return false;
    }
    const docDataModel = univerInstanceService.getUniverDocInstance(unitId);
    if (docDataModel == null) {
      return false;
    }
    const docSkeletonManagerService = getCommandSkeleton(accessor, unitId);
    if (docSkeletonManagerService == null) {
      return false;
    }
    const viewModel = docSkeletonManagerService.getViewModel();
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges
      }
    };
    doMutation.params.actions = getCutActionsFromDocRanges(selections, rectRanges, docDataModel, viewModel, segmentId);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};

// ../packages/docs-ui/src/commands/commands/core-editing.command.ts
var EditorInsertTextCommandId = "doc.command.insert-text";
var InsertCommand = {
  id: EditorInsertTextCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    var _a;
    const commandService = accessor.get(ICommandService);
    const { range, segmentId, body, unitId, cursorOffset } = params;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const docDataModel = univerInstanceService.getUnit(unitId, 1 /* UNIVER_DOC */);
    if (docDataModel == null) {
      return false;
    }
    const activeRange = docSelectionManagerService.getActiveTextRange();
    const originBody = docDataModel.getSelfOrHeaderFooterModel((_a = activeRange == null ? void 0 : activeRange.segmentId) != null ? _a : "").getBody();
    if (originBody == null) {
      return false;
    }
    const { startOffset, collapsed } = range;
    const cursorMove = cursorOffset != null ? cursorOffset : body.dataStream.length;
    const textRanges = [
      {
        startOffset: startOffset + cursorMove,
        endOffset: startOffset + cursorMove,
        style: activeRange == null ? void 0 : activeRange.style,
        collapsed
      }
    ];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges,
        debounce: true
      }
    };
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    if (collapsed) {
      if (startOffset > 0) {
        textX.push({
          t: "r" /* RETAIN */,
          len: startOffset
        });
      }
      textX.push({
        t: "i" /* INSERT */,
        body,
        len: body.dataStream.length
      });
    } else {
      const dos = BuildTextUtils.selection.delete([range], originBody, 0, body);
      textX.push(...dos);
    }
    doMutation.params.textRanges = [{
      startOffset: startOffset + cursorMove,
      endOffset: startOffset + cursorMove,
      collapsed
    }];
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var DeleteCommand = {
  id: "doc.command.delete-text",
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    var _a;
    const commandService = accessor.get(ICommandService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const { range, segmentId, unitId, direction, len = 1 } = params;
    const docDataModel = univerInstanceService.getUnit(unitId, 1 /* UNIVER_DOC */);
    const body = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (docDataModel == null || body == null) {
      return false;
    }
    const { startOffset } = range;
    let start = direction === 0 /* LEFT */ ? startOffset - len : startOffset;
    let end = direction === 0 /* LEFT */ ? startOffset - 1 : startOffset + len - 1;
    const customRange = (_a = body.customRanges) == null ? void 0 : _a.find((customRange2) => customRange2.startIndex <= start && customRange2.endIndex >= end);
    if (customRange == null ? void 0 : customRange.wholeEntity) {
      start = customRange.startIndex;
      end = Math.max(end, customRange.endIndex);
    }
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges: [{
          startOffset: start,
          endOffset: start,
          collapsed: true
        }],
        debounce: true
      }
    };
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const cursor = 0;
    textX.push({
      t: "r" /* RETAIN */,
      len: start - cursor
    });
    textX.push({
      t: "d" /* DELETE */,
      len: end - start + 1
    });
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var UpdateCommand = {
  id: "doc.command.update-text",
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const { range, segmentId, updateBody, coverType, unitId, textRanges } = params;
    const commandService = accessor.get(ICommandService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    if (docDataModel == null) {
      return false;
    }
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges
      }
    };
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const { startOffset, endOffset } = range;
    textX.push({
      t: "r" /* RETAIN */,
      len: startOffset
    });
    textX.push({
      t: "r" /* RETAIN */,
      body: updateBody,
      len: endOffset - startOffset,
      coverType
    });
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};

// ../packages/docs-ui/src/commands/commands/util.ts
function getCurrentParagraph(accessor) {
  var _a, _b, _c, _d;
  const instanceService = accessor.get(IUniverInstanceService);
  const docSelectionManagerService = accessor.get(DocSelectionManagerService);
  const range = docSelectionManagerService.getActiveTextRange();
  const doc = instanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
  if (!range || !range.collapsed || !doc || range.segmentId) {
    return false;
  }
  const paragraph = BuildTextUtils.range.getParagraphsInRange(range, (_b = (_a = doc.getBody()) == null ? void 0 : _a.paragraphs) != null ? _b : [], (_d = (_c = doc.getBody()) == null ? void 0 : _c.dataStream) != null ? _d : "")[0];
  return paragraph;
}

// ../packages/docs-ui/src/commands/commands/doc-delete.command.ts
var DeleteCustomBlockCommand = {
  id: "doc.command.delete-custom-block",
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    var _a;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const activeRange = docSelectionManagerService.getActiveTextRange();
    const documentDataModel = univerInstanceService.getCurrentUniverDocInstance();
    if (activeRange == null || documentDataModel == null) {
      return false;
    }
    const { direction, range, unitId, drawingId } = params;
    const { startOffset, segmentId, style } = activeRange;
    const cursor = direction === 0 /* LEFT */ ? startOffset - 1 : startOffset;
    const textRanges = [
      {
        startOffset: cursor,
        endOffset: cursor,
        style
      }
    ];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges,
        prevTextRanges: [range]
      }
    };
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const rawActions = [];
    if (startOffset > 0) {
      textX.push({
        t: "r" /* RETAIN */,
        len: direction === 0 /* LEFT */ ? startOffset - 1 : startOffset
      });
    }
    textX.push({
      t: "d" /* DELETE */,
      len: 1
    });
    const path = getRichTextEditPath2(documentDataModel, segmentId);
    rawActions.push(jsonX.editOp(textX.serialize(), path));
    const drawing = ((_a = documentDataModel.getDrawings()) != null ? _a : {})[drawingId];
    const drawingOrder = documentDataModel.getDrawingsOrder();
    const drawingIndex = drawingOrder.indexOf(drawingId);
    const removeDrawingAction = jsonX.removeOp(["drawings", drawingId], drawing);
    const removeDrawingOrderAction = jsonX.removeOp(["drawingsOrder", drawingIndex], drawingId);
    rawActions.push(removeDrawingAction);
    rawActions.push(removeDrawingOrderAction);
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var MergeTwoParagraphCommand = {
  id: "doc.command.merge-two-paragraph",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler: async (accessor, params) => {
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const { direction, range } = params;
    const activeRange = docSelectionManagerService.getActiveTextRange();
    const ranges = docSelectionManagerService.getTextRanges();
    if (activeRange == null || ranges == null) {
      return false;
    }
    const { segmentId, style } = activeRange;
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const originBody = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (docDataModel == null || originBody == null) {
      return false;
    }
    const dataStream = originBody.dataStream;
    const actualRange = activeRange;
    const unitId = docDataModel.getUnitId();
    const { startOffset, collapsed } = actualRange;
    if (!collapsed) {
      return false;
    }
    const startIndex = direction === 0 /* LEFT */ ? startOffset : startOffset + 1;
    let curParagraph;
    let nextParagraph;
    for (const paragraph of originBody.paragraphs) {
      if (paragraph.startIndex >= startIndex) {
        nextParagraph = paragraph;
        break;
      }
      curParagraph = paragraph;
    }
    if (curParagraph == null || nextParagraph == null) {
      return false;
    }
    const cursor = direction === 0 /* LEFT */ ? startOffset - 1 : startOffset;
    const textRanges = [
      {
        startOffset: cursor,
        endOffset: cursor,
        style
      }
    ];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges,
        prevTextRanges: [range]
      }
    };
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    if (curParagraph.startIndex > 0) {
      textX.push({
        t: "r" /* RETAIN */,
        len: curParagraph.startIndex
      });
    }
    textX.push({
      t: "d" /* DELETE */,
      len: 1
    });
    if (nextParagraph.startIndex > curParagraph.startIndex + 1) {
      textX.push({
        t: "r" /* RETAIN */,
        len: nextParagraph.startIndex - curParagraph.startIndex - 1
      });
    }
    const tokens = Object.values(DataStreamTreeTokenType);
    const lastToken = dataStream[curParagraph.startIndex - 1];
    if (lastToken && !tokens.includes(lastToken) || lastToken === " ") {
      textX.push({
        t: "r" /* RETAIN */,
        len: 1,
        coverType: 1 /* REPLACE */,
        body: {
          dataStream: "",
          paragraphs: [
            {
              ...Tools.deepClone(curParagraph),
              startIndex: 0
            }
          ]
        }
      });
    }
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var RemoveHorizontalLineCommand = {
  id: "doc.command.remove-horizontal-line",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler: async (accessor) => {
    var _a;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const activeRange = docSelectionManagerService.getActiveTextRange();
    const ranges = docSelectionManagerService.getTextRanges();
    if (activeRange == null || ranges == null) {
      return false;
    }
    const { segmentId, style } = activeRange;
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const originBody = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (docDataModel == null || originBody == null) {
      return false;
    }
    const actualRange = activeRange;
    const unitId = docDataModel.getUnitId();
    const { startOffset, collapsed } = actualRange;
    if (!collapsed) {
      return false;
    }
    const paragraph = (_a = originBody.paragraphs) == null ? void 0 : _a.find((p) => p.startIndex === startOffset - 1);
    if (paragraph == null) {
      return false;
    }
    const textRanges = [
      {
        startOffset,
        endOffset: startOffset,
        style
      }
    ];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges
      }
    };
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    if (paragraph.startIndex > 0) {
      textX.push({
        t: "r" /* RETAIN */,
        len: paragraph.startIndex
      });
    }
    textX.push({
      t: "r" /* RETAIN */,
      len: 1,
      coverType: 1 /* REPLACE */,
      body: {
        dataStream: "",
        paragraphs: [
          {
            ...Tools.deepClone({
              ...paragraph,
              paragraphStyle: {
                ...paragraph.paragraphStyle,
                borderBottom: void 0
              }
            }),
            startIndex: 0
          }
        ]
      }
    });
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
function getCursorWhenDelete(textRanges, rectRanges) {
  let cursor = 0;
  if (textRanges == null || textRanges.length === 0) {
    if (typeof rectRanges[0].startOffset === "number") {
      const rectRange = rectRanges[0];
      const { spanEntireRow, spanEntireTable } = rectRange;
      if (spanEntireTable) {
        cursor = rectRange.startOffset - 3;
      } else if (spanEntireRow) {
        if (rectRange.startRow > 0) {
          cursor = rectRange.startOffset - 6;
        } else {
          cursor = rectRange.startOffset;
        }
      } else {
        cursor = rectRanges[0].startOffset;
      }
    }
  } else if (textRanges.length > 0 && rectRanges.length > 0) {
    const textRange = textRanges[0];
    const rectRange = rectRanges[0];
    if (textRange.startOffset != null && rectRange.startOffset != null) {
      if (textRange.startOffset < rectRange.startOffset) {
        cursor = textRange.startOffset;
      } else if (textRange.startOffset >= rectRange.startOffset) {
        const { spanEntireRow, spanEntireTable } = rectRange;
        if (spanEntireTable) {
          cursor = rectRange.startOffset - 3;
        } else if (spanEntireRow) {
          cursor = rectRange.startOffset - 6;
        }
      }
    }
  }
  return cursor;
}
function getBlockRangeEndTokenOffset(body, blockRange) {
  return body.dataStream[blockRange.endIndex] === "" /* BLOCK_END */ ? blockRange.endIndex : body.dataStream[blockRange.endIndex + 1] === "" /* BLOCK_END */ ? blockRange.endIndex + 1 : blockRange.endIndex;
}
function isDeleteOffsetInsideBlockRange(body, offset) {
  var _a, _b;
  return (_b = (_a = body.blockRanges) == null ? void 0 : _a.some((blockRange) => {
    const endTokenOffset = getBlockRangeEndTokenOffset(body, blockRange);
    return blockRange.startIndex < offset && offset < endTokenOffset;
  })) != null ? _b : false;
}
var DeleteLeftCommand = {
  id: "doc.command.delete-left",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function, complexity
  handler: async (accessor) => {
    var _a, _b, _c, _d, _e, _f;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    let result = true;
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    if (docDataModel == null) {
      return false;
    }
    const unitId = docDataModel.getUnitId();
    const docSkeletonManagerService = getCommandSkeleton(accessor, unitId);
    const activeRange = docSelectionManagerService.getActiveTextRange();
    const rectRanges = docSelectionManagerService.getRectRanges();
    const ranges = docSelectionManagerService.getTextRanges();
    const skeleton = docSkeletonManagerService == null ? void 0 : docSkeletonManagerService.getSkeleton();
    if (skeleton == null) {
      return false;
    }
    if (rectRanges == null ? void 0 : rectRanges.length) {
      const cursor2 = getCursorWhenDelete(ranges, rectRanges);
      const segmentId2 = rectRanges[0].segmentId;
      const textRanges = [
        {
          startOffset: cursor2,
          endOffset: cursor2
        }
      ];
      return commandService.executeCommand(CutContentCommand.id, {
        segmentId: segmentId2,
        textRanges
      });
    }
    if (activeRange == null || ranges == null) {
      return false;
    }
    const autoFormatResult = await executeDeleteAutoFormat(accessor, DeleteLeftCommand.id);
    if (autoFormatResult != null) {
      return autoFormatResult;
    }
    const { segmentId, style, segmentPage } = activeRange;
    const body = docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (body == null) {
      return false;
    }
    const actualRange = activeRange;
    const { startOffset, collapsed } = actualRange;
    if (collapsed) {
      const emptyCenteredParagraph = getEmptyCenteredParagraphAtOffset(body, startOffset);
      if (emptyCenteredParagraph != null) {
        return resetEmptyCenteredParagraphAlignment(commandService, docDataModel, segmentId != null ? segmentId : "", emptyCenteredParagraph, style);
      }
    }
    const curGlyph = skeleton.findNodeByCharIndex(startOffset, segmentId, segmentPage);
    const isBullet = hasListGlyph(curGlyph);
    const isIndent = isIndentByGlyph(curGlyph, body);
    let cursor = startOffset;
    const preGlyph = skeleton.findNodeByCharIndex(startOffset - 1, segmentId, segmentPage);
    const isInBlockRange = isDeleteOffsetInsideBlockRange(body, startOffset);
    const isUpdateParagraph = !isInBlockRange && isFirstGlyph(curGlyph) && preGlyph !== curGlyph && (isBullet === true || isIndent === true);
    if (isUpdateParagraph && collapsed) {
      const paragraph = getParagraphByGlyph(curGlyph, body);
      if (paragraph == null) {
        return false;
      }
      const paragraphIndex = paragraph == null ? void 0 : paragraph.startIndex;
      const updateParagraph = { startIndex: 0 };
      const paragraphStyle = paragraph.paragraphStyle;
      if (isBullet === true) {
        const paragraphStyle2 = paragraph.paragraphStyle;
        if (paragraphStyle2) {
          updateParagraph.paragraphStyle = paragraphStyle2;
          const { hanging } = paragraphStyle2;
          if (hanging) {
            updateParagraph.paragraphStyle.indentStart = hanging;
            updateParagraph.paragraphStyle.hanging = void 0;
          }
        }
      } else if (isIndent === true) {
        const bullet = paragraph.bullet;
        if (bullet) {
          updateParagraph.bullet = bullet;
        }
        if (paragraphStyle != null) {
          updateParagraph.paragraphStyle = { ...paragraphStyle };
          delete updateParagraph.paragraphStyle.hanging;
          delete updateParagraph.paragraphStyle.indentStart;
        }
      }
      const textRanges = [
        {
          startOffset: cursor,
          endOffset: cursor,
          style
        }
      ];
      result = await commandService.executeCommand(UpdateCommand.id, {
        unitId: docDataModel.getUnitId(),
        updateBody: {
          dataStream: "",
          paragraphs: [{ ...updateParagraph }]
        },
        range: {
          startOffset: paragraphIndex,
          endOffset: paragraphIndex + 1
        },
        textRanges,
        coverType: 1 /* REPLACE */,
        segmentId
      });
    } else {
      if (collapsed === true) {
        if (preGlyph == null) {
          return true;
        }
        if (preGlyph.content === "\r") {
          const paragraph = (_a = body.paragraphs) == null ? void 0 : _a.find((p) => p.startIndex === startOffset - 1);
          if ((_b = paragraph == null ? void 0 : paragraph.paragraphStyle) == null ? void 0 : _b.borderBottom) {
            result = await commandService.executeCommand(RemoveHorizontalLineCommand.id);
          } else {
            result = await commandService.executeCommand(MergeTwoParagraphCommand.id, {
              direction: 0 /* LEFT */,
              range: actualRange
            });
          }
        } else if (preGlyph.streamType === "\b") {
          const drawing = (_d = docDataModel.getSnapshot().drawings) == null ? void 0 : _d[(_c = preGlyph.drawingId) != null ? _c : ""];
          if (drawing == null) {
            return true;
          }
          const customBlock = (_f = (_e = docDataModel.getBody()) == null ? void 0 : _e.customBlocks) == null ? void 0 : _f.find((block) => block.blockId === preGlyph.drawingId);
          const isInlineDrawingOrCustom = drawing.layoutType === 0 /* INLINE */ || (customBlock == null ? void 0 : customBlock.blockType) === 1 /* CUSTOM */;
          if (isInlineDrawingOrCustom) {
            const unitId2 = docDataModel.getUnitId();
            result = await commandService.executeCommand(DeleteCustomBlockCommand.id, {
              direction: 0 /* LEFT */,
              range: activeRange,
              unitId: unitId2,
              drawingId: preGlyph.drawingId
            });
          } else {
            const prePreGlyph = skeleton.findNodeByCharIndex(startOffset - 2);
            if (prePreGlyph == null) {
              return true;
            }
            cursor -= preGlyph.count;
            cursor -= prePreGlyph.count;
            const textRanges = [
              {
                startOffset: cursor,
                endOffset: cursor,
                style
              }
            ];
            result = await commandService.executeCommand(DeleteCommand.id, {
              unitId: docDataModel.getUnitId(),
              range: {
                ...activeRange,
                startOffset: activeRange.startOffset - 1,
                endOffset: activeRange.endOffset - 1
              },
              segmentId,
              direction: 0 /* LEFT */,
              len: prePreGlyph.count,
              textRanges
            });
          }
        } else {
          cursor -= preGlyph.count;
          result = await commandService.executeCommand(DeleteCommand.id, {
            unitId: docDataModel.getUnitId(),
            range: actualRange,
            segmentId,
            direction: 0 /* LEFT */,
            len: preGlyph.count
          });
        }
      } else {
        const textRanges = getTextRangesWhenDelete(actualRange, [actualRange]);
        result = await commandService.executeCommand(CutContentCommand.id, {
          segmentId,
          textRanges,
          selections: [actualRange]
        });
      }
    }
    return result;
  }
};
var DeleteRightCommand = {
  id: "doc.command.delete-right",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function, complexity
  handler: async (accessor) => {
    var _a, _b;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    if (!docDataModel) {
      return false;
    }
    const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
    const commandService = accessor.get(ICommandService);
    const activeRange = docSelectionManagerService.getActiveTextRange();
    const rectRanges = docSelectionManagerService.getRectRanges();
    const ranges = docSelectionManagerService.getTextRanges();
    const skeleton = docSkeletonManagerService == null ? void 0 : docSkeletonManagerService.getSkeleton();
    if (rectRanges == null ? void 0 : rectRanges.length) {
      const cursor = getCursorWhenDelete(ranges, rectRanges);
      const segmentId2 = rectRanges[0].segmentId;
      const textRanges = [
        {
          startOffset: cursor,
          endOffset: cursor
        }
      ];
      return commandService.executeCommand(CutContentCommand.id, {
        segmentId: segmentId2,
        textRanges
      });
    }
    if (activeRange == null || skeleton == null || ranges == null) {
      return false;
    }
    const autoFormatResult = await executeDeleteAutoFormat(accessor, DeleteRightCommand.id);
    if (autoFormatResult != null) {
      return autoFormatResult;
    }
    const { segmentId, style, segmentPage } = activeRange;
    const body = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (!docDataModel || !body) {
      return false;
    }
    const actualRange = activeRange;
    const { startOffset, endOffset, collapsed } = actualRange;
    if (collapsed) {
      const emptyCenteredParagraph = getEmptyCenteredParagraphAtOffset(body, startOffset);
      if (emptyCenteredParagraph != null) {
        return resetEmptyCenteredParagraphAlignment(commandService, docDataModel, segmentId != null ? segmentId : "", emptyCenteredParagraph, style);
      }
    }
    if (startOffset === body.dataStream.length - 2 && collapsed) {
      return true;
    }
    let result = false;
    if (collapsed === true) {
      const needDeleteGlyph = skeleton.findNodeByCharIndex(startOffset, segmentId, segmentPage);
      const nextGlyph = skeleton.findNodeByCharIndex(startOffset + 1);
      if (needDeleteGlyph.streamType === "\r" /* PARAGRAPH */ && (nextGlyph == null ? void 0 : nextGlyph.streamType) === "\n" /* SECTION_BREAK */) {
        return false;
      }
      if (needDeleteGlyph.content === "\r") {
        result = await commandService.executeCommand(MergeTwoParagraphCommand.id, {
          direction: 1 /* RIGHT */,
          range: activeRange
        });
      } else if (needDeleteGlyph.streamType === "\b") {
        const drawing = (_b = docDataModel.getSnapshot().drawings) == null ? void 0 : _b[(_a = needDeleteGlyph.drawingId) != null ? _a : ""];
        if (drawing == null) {
          return true;
        }
        const isInlineDrawing = drawing.layoutType === 0 /* INLINE */;
        if (isInlineDrawing) {
          const unitId = docDataModel.getUnitId();
          result = await commandService.executeCommand(DeleteCustomBlockCommand.id, {
            direction: 1 /* RIGHT */,
            range: activeRange,
            unitId,
            drawingId: needDeleteGlyph.drawingId
          });
        } else {
          if (nextGlyph == null) {
            return true;
          }
          const textRanges = [
            {
              startOffset: startOffset + 1,
              endOffset: startOffset + 1,
              style
            }
          ];
          result = await commandService.executeCommand(DeleteCommand.id, {
            unitId: docDataModel.getUnitId(),
            range: {
              ...activeRange,
              startOffset: startOffset + 1,
              endOffset: endOffset + 1
            },
            segmentId,
            direction: 1 /* RIGHT */,
            textRanges,
            len: nextGlyph.count
          });
        }
      } else {
        const textRanges = [
          {
            startOffset,
            endOffset: startOffset,
            style
          }
        ];
        result = await commandService.executeCommand(DeleteCommand.id, {
          unitId: docDataModel.getUnitId(),
          range: actualRange,
          segmentId,
          direction: 1 /* RIGHT */,
          textRanges,
          len: needDeleteGlyph.count
        });
      }
    } else {
      const textRanges = getTextRangesWhenDelete(actualRange, [actualRange]);
      result = await commandService.executeCommand(CutContentCommand.id, {
        segmentId,
        textRanges,
        selections: [actualRange]
      });
    }
    return result;
  }
};
async function executeDeleteAutoFormat(accessor, commandId) {
  if (!accessor.has(DocAutoFormatService)) {
    return null;
  }
  const commandService = accessor.get(ICommandService);
  const mutations = accessor.get(DocAutoFormatService).onAutoFormat(commandId);
  if (!mutations.length) {
    return null;
  }
  return (await sequenceExecuteAsync(mutations, commandService)).result;
}
function getTextRangesWhenDelete(activeRange, ranges) {
  let cursor = activeRange.endOffset;
  for (const range of ranges) {
    const { startOffset, endOffset } = range;
    if (startOffset == null || endOffset == null) {
      continue;
    }
    if (endOffset <= activeRange.endOffset) {
      cursor -= endOffset - startOffset;
    }
  }
  const textRanges = [
    {
      startOffset: cursor,
      endOffset: cursor,
      style: activeRange.style
    }
  ];
  return textRanges;
}
function getEmptyCenteredParagraphAtOffset(body, offset) {
  var _a, _b;
  const paragraphs = (_a = body.paragraphs) != null ? _a : [];
  for (let i = 0; i < paragraphs.length; i++) {
    const paragraph = paragraphs[i];
    const paragraphTextStart = i === 0 ? 0 : paragraphs[i - 1].startIndex + 1;
    if (paragraph.startIndex === paragraphTextStart && paragraph.startIndex === offset && ((_b = paragraph.paragraphStyle) == null ? void 0 : _b.horizontalAlign) === 2 /* CENTER */) {
      return paragraph;
    }
  }
}
function resetEmptyCenteredParagraphAlignment(commandService, docDataModel, segmentId, paragraph, style) {
  return commandService.executeCommand(UpdateCommand.id, {
    unitId: docDataModel.getUnitId(),
    updateBody: {
      dataStream: "",
      paragraphs: [{
        ...paragraph,
        startIndex: 0,
        paragraphStyle: {
          ...paragraph.paragraphStyle,
          horizontalAlign: 1 /* LEFT */
        }
      }]
    },
    range: {
      startOffset: paragraph.startIndex,
      endOffset: paragraph.startIndex + 1
    },
    textRanges: [{
      startOffset: paragraph.startIndex,
      endOffset: paragraph.startIndex,
      collapsed: true,
      style
    }],
    coverType: 1 /* REPLACE */,
    segmentId
  });
}
var DeleteCurrentParagraphCommand = {
  id: "doc.command.delete-current-paragraph",
  type: 0 /* COMMAND */,
  handler: async (accessor) => {
    var _a, _b;
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    if (!docDataModel) {
      return false;
    }
    const dataStream = (_b = (_a = docDataModel.getBody()) == null ? void 0 : _a.dataStream) != null ? _b : "";
    const paragraph = getCurrentParagraph(accessor);
    if (!paragraph) {
      return false;
    }
    const actions = BuildTextUtils.selection.delete(
      [{
        startOffset: paragraph.paragraphStart,
        endOffset: dataStream[paragraph.paragraphEnd + 1] === "\n" ? paragraph.paragraphEnd : paragraph.paragraphEnd + 1,
        collapsed: false
      }],
      docDataModel.getBody(),
      0,
      void 0,
      true
    );
    const path = getRichTextEditPath2(docDataModel);
    const params = {
      unitId: docDataModel.getUnitId(),
      actions: JSONX.getInstance().editOp(actions, path),
      textRanges: [{
        startOffset: paragraph.paragraphStart,
        endOffset: paragraph.paragraphStart,
        collapsed: true
      }]
    };
    return commandService.syncExecuteCommand(RichTextEditingMutation.id, params);
  }
};

// ../packages/docs-ui/src/services/clipboard/copy-content-cache.ts
var COPY_CONTENT_CACHE_LIMIT = 10;
function extractId(html) {
  const match = html.match(/data-copy-id="([^\s]+)"/);
  if (match && match[1]) {
    return match[1];
  }
  return null;
}
var CopyContentCache = class {
  constructor() {
    __publicField(this, "_cache", new LRUMap(COPY_CONTENT_CACHE_LIMIT));
  }
  set(id, clipboardData) {
    this._cache.set(id, clipboardData);
  }
  get(id) {
    return this._cache.get(id);
  }
  clear() {
    this._cache.clear();
  }
};
var copyContentCache = new CopyContentCache();

// ../packages/docs-ui/src/services/clipboard/html-to-udm/parse-node-style.ts
function extractNodeStyle(node) {
  const styles = node.style;
  const docStyles = {};
  const tagName = node.tagName.toLowerCase();
  switch (tagName) {
    case "b":
    case "em":
    case "strong": {
      docStyles.bl = 1 /* TRUE */;
      break;
    }
    case "s": {
      docStyles.st = {
        s: 1 /* TRUE */
      };
      break;
    }
    case "u": {
      docStyles.ul = {
        s: 1 /* TRUE */
      };
      break;
    }
    case "i": {
      docStyles.it = 1 /* TRUE */;
      break;
    }
    case "sub":
    case "sup": {
      docStyles.va = tagName === "sup" ? 3 /* SUPERSCRIPT */ : 2 /* SUBSCRIPT */;
      break;
    }
  }
  for (let i = 0; i < styles.length; i++) {
    const cssRule = styles[i];
    const cssValue = styles.getPropertyValue(cssRule);
    switch (cssRule) {
      case "font-family": {
        docStyles.ff = cssValue.replace(/^"/g, "").replace(/"$/g, "");
        break;
      }
      case "font-size": {
        const fontSize = Number.parseInt(cssValue);
        if (!Number.isNaN(fontSize)) {
          if (cssValue.endsWith("pt")) {
            docStyles.fs = fontSize;
          } else if (cssValue.endsWith("px")) {
            docStyles.fs = pixelToPt(fontSize);
          }
        }
        break;
      }
      case "font-style": {
        if (cssValue === "italic") {
          docStyles.it = 1 /* TRUE */;
        }
        break;
      }
      case "font-weight": {
        const MIDDLE_FONT_WEIGHT = 400;
        if (Number(cssValue) > MIDDLE_FONT_WEIGHT || String(cssValue) === "bold") {
          docStyles.bl = 1 /* TRUE */;
        }
        break;
      }
      case "text-decoration": {
        if (/underline/.test(cssValue)) {
          docStyles.ul = {
            s: 1 /* TRUE */
          };
        } else if (/overline/.test(cssValue)) {
          docStyles.ol = {
            s: 1 /* TRUE */
          };
        } else if (/line-through/.test(cssValue)) {
          docStyles.st = {
            s: 1 /* TRUE */
          };
        }
        break;
      }
      case "color": {
        try {
          const color = new ColorKit(cssValue);
          if (color.isValid) {
            docStyles.cl = {
              rgb: color.toRgbString()
            };
          }
        } catch (_e) {
        }
        break;
      }
      case "background-color":
      case "background": {
        const color = new ColorKit(cssValue);
        if (color.isValid) {
          docStyles.bg = {
            rgb: color.toRgbString()
          };
        }
        break;
      }
      default: {
        break;
      }
    }
  }
  return docStyles;
}

// ../packages/docs-ui/src/services/clipboard/html-to-udm/parse-to-dom.ts
function parseToDom(rawHtml) {
  const parser = new DOMParser();
  const html = `<x-univer id="univer-root">${rawHtml}</x-univer>`;
  const doc = parser.parseFromString(html, "text/html");
  return doc.querySelector("#univer-root");
}

// ../packages/docs-ui/src/services/clipboard/html-to-udm/converter.ts
function matchFilter(node, filter2) {
  const tagName = node.tagName.toLowerCase();
  if (typeof filter2 === "string") {
    return tagName === filter2;
  }
  if (Array.isArray(filter2)) {
    return filter2.some((name) => name === tagName);
  }
  return filter2(node);
}
var DEFAULT_TABLE_WIDTH = 660;
var WORD_SPACE_PLACEHOLDER = "\uE000";
var _HtmlToUDMService = class _HtmlToUDMService {
  constructor() {
    __publicField(this, "_tableCache", []);
    __publicField(this, "_styleCache", /* @__PURE__ */ new Map());
    __publicField(this, "_styleRules", []);
    __publicField(this, "_afterProcessRules", []);
    __publicField(this, "_listStack", []);
    __publicField(this, "_lastParagraphIndex", -1);
  }
  static use(plugin) {
    if (this._pluginList.includes(plugin)) {
      throw new Error(`Univer paste plugin ${plugin.name} already added`);
    }
    this._pluginList.push(plugin);
  }
  convert(html, metaConfig = {}) {
    var _a, _b, _c;
    const pastePlugin = _HtmlToUDMService._pluginList.find((plugin) => plugin.checkPasteType(html));
    const normalizedHtml = (_b = (_a = pastePlugin == null ? void 0 : pastePlugin.preprocessHtml) == null ? void 0 : _a.call(pastePlugin, html)) != null ? _b : html;
    const dom = parseToDom(normalizedHtml);
    const body = {
      dataStream: "",
      paragraphs: [],
      sectionBreaks: [],
      tables: [],
      textRuns: [],
      customBlocks: []
    };
    const docData = {
      body,
      tableSource: {},
      id: (_c = metaConfig == null ? void 0 : metaConfig.unitId) != null ? _c : ""
    };
    if (pastePlugin) {
      this._styleRules = [...pastePlugin.stylesRules];
      this._afterProcessRules = [...pastePlugin.afterProcessRules];
    }
    this._tableCache = [];
    this._listStack = [];
    this._lastParagraphIndex = -1;
    this._styleCache.clear();
    this._process(null, dom.childNodes, docData);
    this._styleCache.clear();
    this._styleRules = [];
    this._afterProcessRules = [];
    return docData;
  }
  // eslint-disable-next-line max-lines-per-function, complexity
  _process(parent, nodes, doc) {
    var _a, _b, _c, _d, _e, _f, _g;
    const body = doc.body;
    for (const node of nodes) {
      if (node.nodeType === Node.TEXT_NODE) {
        const text = normalizeTextNode(node);
        if (!text) {
          continue;
        }
        let style;
        if (parent && this._styleCache.has(parent)) {
          style = this._styleCache.get(parent);
        }
        body.dataStream += text;
        if (style && Object.getOwnPropertyNames(style).length) {
          body.textRuns.push({
            st: body.dataStream.length - text.length,
            ed: body.dataStream.length,
            ts: style
          });
        }
      } else if (node.nodeName === "IMG") {
        const element = node;
        const imageSourceType = (_a = element.dataset.imageSourceType) != null ? _a : element.src ? inferImageSourceType(element.src) : void 0;
        const source = imageSourceType === "UUID" /* UUID */ ? element.dataset.source : element.src;
        if (source && imageSourceType) {
          const width = (_b = readImageSize(element, "width")) != null ? _b : 100;
          const height = (_c = readImageSize(element, "height")) != null ? _c : 100;
          const docTransformWidth = (_d = readCssSize(element.dataset.docTransformWidth || null, null, "width")) != null ? _d : width;
          const docTransformHeight = (_e = readCssSize(element.dataset.docTransformHeight || null, null, "height")) != null ? _e : height;
          const id = generateRandomId(6);
          (_g = (_f = doc.body) == null ? void 0 : _f.customBlocks) == null ? void 0 : _g.push({ startIndex: body.dataStream.length, blockId: id });
          body.dataStream += "\b";
          if (!doc.drawings) {
            doc.drawings = {};
          }
          doc.drawings[id] = {
            drawingId: id,
            title: "",
            description: "",
            imageSourceType,
            source,
            transform: { width, height, left: 0 },
            docTransform: {
              size: { width: docTransformWidth, height: docTransformHeight },
              angle: 0,
              positionH: {
                relativeFrom: 0 /* PAGE */,
                posOffset: 0
              },
              positionV: {
                relativeFrom: 1 /* PARAGRAPH */,
                posOffset: 0
              }
            },
            layoutType: 0 /* INLINE */,
            drawingType: 0 /* DRAWING_IMAGE */,
            unitId: doc.id || "",
            subUnitId: doc.id || ""
          };
        }
      } else if (skipParseTagNames.includes(node.nodeName.toLowerCase())) {
        continue;
      } else if (node.nodeType === Node.ELEMENT_NODE) {
        const element = node;
        if (element.tagName.toUpperCase() === "TABLE") {
          this._processHtmlTable(element, doc);
          continue;
        }
        if (this._processCodeBlock(element, doc)) {
          continue;
        }
        const linkStart = this._processBeforeLink(element, doc);
        const blockStart = this._processBeforeStructuredBlock(element, doc);
        const listContext = this._processBeforeList(element);
        const parentStyles = parent ? this._styleCache.get(parent) : {};
        const styleRule = this._styleRules.find(({ filter: filter2 }) => matchFilter(node, filter2));
        const nodeStyles = styleRule ? styleRule.getStyle(node) : extractNodeStyle(node);
        this._styleCache.set(node, { ...parentStyles, ...nodeStyles });
        const { childNodes } = node;
        this._processBeforeTable(node, doc);
        this._process(node, childNodes, doc);
        this._processAfterTable(node, doc);
        this._processAfterList(listContext);
        const afterProcessRule = this._afterProcessRules.find(
          ({ filter: filter2 }) => matchFilter(node, filter2)
        );
        const paragraphAddedByPlugin = Boolean(afterProcessRule);
        if (afterProcessRule) {
          afterProcessRule.handler(doc, node);
        }
        this._processAfterDefaultBlock(element, doc, paragraphAddedByPlugin);
        this._processAfterStructuredBlock(element, doc, blockStart);
        this._processAfterLink(element, doc, linkStart);
      }
    }
  }
  _processCodeBlock(node, doc) {
    var _a, _b;
    if (node.tagName.toUpperCase() !== "PRE") {
      return false;
    }
    const body = doc.body;
    const startIndex = body.dataStream.length;
    const text = (_a = node.textContent) != null ? _a : "";
    body.dataStream += text;
    this._appendParagraph(doc, node);
    (_b = body.blockRanges) != null ? _b : body.blockRanges = [];
    body.blockRanges.push({
      blockId: generateRandomId(6),
      blockType: "code",
      startIndex,
      endIndex: body.dataStream.length - 1
    });
    return true;
  }
  _processBeforeStructuredBlock(node, doc) {
    const blockType = getStructuredBlockType(node);
    if (!blockType) {
      return null;
    }
    return doc.body.dataStream.length;
  }
  _processAfterStructuredBlock(node, doc, startIndex) {
    var _a;
    const blockType = getStructuredBlockType(node);
    const body = doc.body;
    if (!blockType || startIndex == null || body.dataStream.length <= startIndex) {
      return;
    }
    (_a = body.blockRanges) != null ? _a : body.blockRanges = [];
    body.blockRanges.push({
      blockId: generateRandomId(6),
      blockType,
      startIndex,
      endIndex: body.dataStream.length - 1
    });
  }
  _processBeforeList(node) {
    const tagName = node.tagName.toUpperCase();
    if (tagName !== "OL" && tagName !== "UL") {
      return null;
    }
    const context = {
      listId: generateRandomId(6),
      listType: tagName === "OL" ? "ORDER_LIST" /* ORDER_LIST */ : "BULLET_LIST" /* BULLET_LIST */,
      nestingLevel: this._listStack.length
    };
    this._listStack.push(context);
    return context;
  }
  _processAfterList(context) {
    if (!context) {
      return;
    }
    this._listStack.pop();
  }
  _processAfterDefaultBlock(node, doc, paragraphAddedByPlugin) {
    if (paragraphAddedByPlugin) {
      this._applyListInfo(node, doc);
      this._lastParagraphIndex = doc.body.dataStream.length - 1;
      return;
    }
    if (!isDefaultParagraphElement(node)) {
      return;
    }
    this._appendParagraph(doc, node);
  }
  _appendParagraph(doc, node) {
    var _a, _b;
    const body = doc.body;
    if (body.dataStream[this._lastParagraphIndex] === "\r" && this._lastParagraphIndex === body.dataStream.length - 1) {
      return;
    }
    (_a = body.paragraphs) != null ? _a : body.paragraphs = [];
    const paragraph = {
      startIndex: body.dataStream.length
    };
    const heading = getHeadingNamedStyleType(node);
    if (heading != null) {
      paragraph.paragraphStyle = {
        ...paragraph.paragraphStyle,
        headingId: generateRandomId(6),
        namedStyleType: heading
      };
    }
    const listContext = (_b = this._listStack[this._listStack.length - 1]) != null ? _b : extractWordListInfo(node);
    if (listContext) {
      paragraph.bullet = {
        listId: listContext.listId,
        listType: listContext.listType,
        nestingLevel: listContext.nestingLevel
      };
      paragraph.startIndex -= stripListMarkerFromCurrentParagraph(body);
    }
    body.paragraphs.push(paragraph);
    body.dataStream += "\r";
    this._lastParagraphIndex = body.dataStream.length - 1;
  }
  _applyListInfo(node, doc) {
    var _a, _b;
    const htmlListContext = this._listStack[this._listStack.length - 1];
    const wordListContext = extractWordListInfo(node);
    const listContext = htmlListContext != null ? htmlListContext : wordListContext;
    const paragraph = (_b = (_a = doc.body) == null ? void 0 : _a.paragraphs) == null ? void 0 : _b[doc.body.paragraphs.length - 1];
    if (!listContext || !paragraph) {
      return;
    }
    paragraph.bullet = {
      listId: listContext.listId,
      listType: listContext.listType,
      nestingLevel: listContext.nestingLevel
    };
    if (wordListContext) {
      paragraph.startIndex -= stripListMarkerFromCurrentParagraph(doc.body);
    }
  }
  _processHtmlTable(node, doc) {
    var _a, _b, _c, _d;
    const body = doc.body;
    if (body.dataStream[body.dataStream.length - 1] !== "\r") {
      body.dataStream += "\r";
      (_a = body.paragraphs) != null ? _a : body.paragraphs = [];
      body.paragraphs.push({
        startIndex: body.dataStream.length - 1
      });
    }
    (_b = doc.tableSource) != null ? _b : doc.tableSource = {};
    (_c = body.tables) != null ? _c : body.tables = [];
    (_d = body.sectionBreaks) != null ? _d : body.sectionBreaks = [];
    const rows = collectHtmlTableRows(node);
    const grid = buildHtmlTableGrid(rows);
    const columnCount = Math.max(1, ...grid.map((row) => row.length));
    const table = genTableSource(0, columnCount, DEFAULT_TABLE_WIDTH);
    const tableWidth = readCssSize(node.getAttribute("width"), node.getAttribute("style"), "width");
    const columnWidths = resolveHtmlTableColumnWidths(node, grid, columnCount, tableWidth);
    table.tableRows = [];
    table.tableColumns = columnWidths.map((width) => getTableColumn(width));
    table.size = {
      type: 1 /* SPECIFIED */,
      width: { v: tableWidth != null ? tableWidth : table.tableColumns.reduce((sum, column) => sum + column.size.width.v, 0) }
    };
    const startIndex = body.dataStream.length;
    body.dataStream += "" /* TABLE_START */;
    grid.forEach((row, rowIndex) => {
      var _a2;
      const tableRow = getEmptyTableRow(0);
      const rowElement = rows[rowIndex];
      const height = rowElement ? readCssSize(rowElement.getAttribute("height"), rowElement.getAttribute("style"), "height") : void 0;
      if (height != null) {
        tableRow.trHeight = {
          val: { v: height },
          hRule: 2 /* EXACT */
        };
      }
      table.tableRows.push(tableRow);
      body.dataStream += "\x1B" /* TABLE_ROW_START */;
      for (let columnIndex = 0; columnIndex < columnCount; columnIndex++) {
        const parsedCell = (_a2 = row[columnIndex]) != null ? _a2 : { element: null, rowSpan: 1, colSpan: 1, covered: true };
        const tableCell = parsedCell.covered ? { ...getEmptyTableCell(), rowSpan: 0, columnSpan: 0 } : createTableCellFromHtml(parsedCell.element, parsedCell.rowSpan, parsedCell.colSpan);
        tableRow.tableCells.push(tableCell);
        this._appendHtmlTableCell(parsedCell, doc);
      }
      body.dataStream += "" /* TABLE_ROW_END */;
    });
    doc.tableSource[table.tableId] = table;
    body.dataStream += "" /* TABLE_END */;
    body.tables.push({
      startIndex,
      endIndex: body.dataStream.length,
      tableId: table.tableId
    });
  }
  _appendHtmlTableCell(cell, doc) {
    var _a, _b;
    const body = doc.body;
    body.dataStream += "" /* TABLE_CELL_START */;
    if (cell.element && !cell.covered) {
      this._process(cell.element, cell.element.childNodes, doc);
    }
    if (body.dataStream[body.dataStream.length - 1] !== "\r") {
      this._appendParagraph(doc, (_a = cell.element) != null ? _a : document.createElement("td"));
    }
    (_b = body.sectionBreaks) != null ? _b : body.sectionBreaks = [];
    body.sectionBreaks.push({
      startIndex: body.dataStream.length
    });
    body.dataStream += `
${"" /* TABLE_CELL_END */}`;
  }
  _processBeforeTable(node, doc) {
    var _a;
    const tagName = node.tagName.toUpperCase();
    const body = doc.body;
    switch (tagName) {
      case "TABLE": {
        if (body.dataStream[body.dataStream.length - 1] !== "\r") {
          body.dataStream += "\r";
          if (body.paragraphs == null) {
            body.paragraphs = [];
          }
          (_a = body.paragraphs) == null ? void 0 : _a.push({
            startIndex: body.dataStream.length - 1
          });
        }
        const table = genTableSource(0, 0, DEFAULT_TABLE_WIDTH);
        this._tableCache.push({
          table,
          startIndex: body.dataStream.length
        });
        body.dataStream += "" /* TABLE_START */;
        break;
      }
      case "TR": {
        const row = getEmptyTableRow(0);
        const lastTable = this._tableCache[this._tableCache.length - 1].table;
        lastTable.tableRows.push(row);
        body.dataStream += "\x1B" /* TABLE_ROW_START */;
        break;
      }
      case "TD": {
        const cell = getEmptyTableCell();
        const lastTable = this._tableCache[this._tableCache.length - 1].table;
        const lastRow = lastTable.tableRows[lastTable.tableRows.length - 1];
        lastRow.tableCells.push(cell);
        body.dataStream += "" /* TABLE_CELL_START */;
        break;
      }
    }
  }
  _processAfterTable(node, doc) {
    var _a, _b;
    const tagName = node.tagName.toUpperCase();
    const body = doc.body;
    if (doc.tableSource == null) {
      doc.tableSource = {};
    }
    if (body.tables == null) {
      body.tables = [];
    }
    if (body.sectionBreaks == null) {
      body.sectionBreaks = [];
    }
    const { tableSource } = doc;
    switch (tagName) {
      case "TABLE": {
        const tableCache = this._tableCache.pop();
        const { startIndex, table } = tableCache;
        const colCount = table.tableRows[0].tableCells.length;
        const tableColumn = getTableColumn(DEFAULT_TABLE_WIDTH / colCount);
        const tableColumns = [...new Array(colCount).fill(null).map(() => Tools.deepClone(tableColumn))];
        table.tableColumns = tableColumns;
        tableSource[table.tableId] = table;
        body.dataStream += "" /* TABLE_END */;
        body.tables.push({
          startIndex,
          endIndex: body.dataStream.length,
          tableId: table.tableId
        });
        break;
      }
      case "TR": {
        body.dataStream += "" /* TABLE_ROW_END */;
        break;
      }
      case "TD": {
        if (body.dataStream[body.dataStream.length - 1] !== "\r") {
          (_a = body.paragraphs) == null ? void 0 : _a.push({
            startIndex: body.dataStream.length
          });
          body.dataStream += "\r";
        }
        (_b = body.sectionBreaks) == null ? void 0 : _b.push({
          startIndex: body.dataStream.length
        });
        body.dataStream += `
${"" /* TABLE_CELL_END */}`;
        break;
      }
    }
  }
  _processBeforeLink(node, doc) {
    const body = doc.body;
    return body.dataStream.length;
  }
  _processAfterLink(node, doc, start) {
    var _a, _b;
    const body = doc.body;
    const element = node;
    if (element.tagName.toUpperCase() === "A") {
      const href = element.href;
      if (!isSafeUrl(href)) {
        return;
      }
      body.customRanges = (_a = body.customRanges) != null ? _a : [];
      body.customRanges.push({
        startIndex: start,
        endIndex: body.dataStream.length - 1,
        rangeId: (_b = element.dataset.rangeid) != null ? _b : generateRandomId(),
        rangeType: 0 /* HYPERLINK */,
        properties: { url: href }
      });
    }
  }
};
__publicField(_HtmlToUDMService, "_pluginList", []);
var HtmlToUDMService = _HtmlToUDMService;
function collectHtmlTableRows(table) {
  const rows = [];
  Array.from(table.children).forEach((child) => {
    const tagName = child.tagName.toUpperCase();
    if (tagName === "TR") {
      rows.push(child);
    } else if (tagName === "TBODY" || tagName === "THEAD" || tagName === "TFOOT") {
      rows.push(...Array.from(child.children).filter((row) => row.tagName.toUpperCase() === "TR"));
    }
  });
  return rows;
}
function normalizeTextNode(node) {
  var _a;
  const value = (_a = node.nodeValue) != null ? _a : "";
  if (value.includes(WORD_SPACE_PLACEHOLDER)) {
    return value.replace(/[\t\u00A0]+/g, " ").replace(/[ \r\n]+/g, " ").replace(/ *\uE000+ */g, (match) => " ".repeat(Array.from(match).filter((char) => char === WORD_SPACE_PLACEHOLDER).length));
  }
  const hasExplicitSpacing = /[\t\u00A0]/.test(value);
  const text = value.replace(/[\t\u00A0]+/g, " ").replace(/[ \r\n]+/g, " ");
  if (text.trim()) {
    return text;
  }
  if (!hasExplicitSpacing) {
    return "";
  }
  return hasTextSibling(node.previousSibling, "previous") && hasTextSibling(node.nextSibling, "next") ? " " : "";
}
function inferImageSourceType(src) {
  return src.startsWith("data:image/") ? "BASE64" /* BASE64 */ : "URL" /* URL */;
}
function hasTextSibling(node, direction) {
  var _a, _b;
  let current = node;
  while (current) {
    if (current.nodeType === Node.TEXT_NODE && ((_a = current.nodeValue) == null ? void 0 : _a.trim())) {
      return true;
    }
    if (current.nodeType === Node.ELEMENT_NODE && ((_b = current.textContent) != null ? _b : "").trim()) {
      return true;
    }
    current = direction === "previous" ? current.previousSibling : current.nextSibling;
  }
  return false;
}
function buildHtmlTableGrid(rows) {
  const grid = [];
  const occupied = /* @__PURE__ */ new Set();
  rows.forEach((row, rowIndex) => {
    const parsedRow = [];
    let columnIndex = 0;
    getHtmlTableCells(row).forEach((cellElement) => {
      var _a, _b;
      while (occupied.has(`${rowIndex}:${columnIndex}`)) {
        parsedRow[columnIndex] = { element: null, rowSpan: 1, colSpan: 1, covered: true };
        columnIndex++;
      }
      const rowSpan = Math.max(1, Number((_a = cellElement.getAttribute("rowspan")) != null ? _a : 1));
      const colSpan = Math.max(1, Number((_b = cellElement.getAttribute("colspan")) != null ? _b : 1));
      parsedRow[columnIndex] = { element: cellElement, rowSpan, colSpan };
      for (let rowOffset = 0; rowOffset < rowSpan; rowOffset++) {
        for (let columnOffset = 0; columnOffset < colSpan; columnOffset++) {
          if (rowOffset === 0 && columnOffset === 0) {
            continue;
          }
          occupied.add(`${rowIndex + rowOffset}:${columnIndex + columnOffset}`);
          if (rowOffset === 0) {
            parsedRow[columnIndex + columnOffset] = { element: null, rowSpan: 1, colSpan: 1, covered: true };
          }
        }
      }
      columnIndex += colSpan;
    });
    while (occupied.has(`${rowIndex}:${columnIndex}`)) {
      parsedRow[columnIndex] = { element: null, rowSpan: 1, colSpan: 1, covered: true };
      columnIndex++;
    }
    grid.push(parsedRow);
  });
  const columnCount = Math.max(0, ...grid.map((row) => row.length));
  grid.forEach((row) => {
    while (row.length < columnCount) {
      row.push({ element: null, rowSpan: 1, colSpan: 1, covered: true });
    }
  });
  return grid.length ? grid : [[]];
}
function getHtmlTableCells(row) {
  return Array.from(row.children).filter((element) => {
    const tagName = element.tagName.toUpperCase();
    return tagName === "TD" || tagName === "TH";
  });
}
function collectHtmlTableColumnWidths(table, grid, columnCount) {
  const widths = Array.from({ length: columnCount });
  Array.from(table.querySelectorAll("col")).forEach((col, index) => {
    if (index < widths.length) {
      widths[index] = readCssSize(col.getAttribute("width"), col.getAttribute("style"), "width");
    }
  });
  grid.forEach((row) => {
    row.forEach((cell, columnIndex) => {
      if (!cell.element || widths[columnIndex] != null) {
        return;
      }
      const width = readCssSize(cell.element.getAttribute("width"), cell.element.getAttribute("style"), "width");
      if (width != null && cell.colSpan <= 1) {
        widths[columnIndex] = width;
      }
    });
  });
  return widths;
}
function resolveHtmlTableColumnWidths(table, grid, columnCount, tableWidth) {
  const explicitWidths = collectHtmlTableColumnWidths(table, grid, columnCount);
  const fallbackTableWidth = tableWidth != null ? tableWidth : DEFAULT_TABLE_WIDTH;
  const explicitTotal = explicitWidths.reduce((sum, width) => sum + (width != null ? width : 0), 0);
  const missingCount = explicitWidths.filter((width) => width == null).length;
  const fallbackColumnWidth = missingCount ? Math.max((fallbackTableWidth - explicitTotal) / missingCount, 1) : fallbackTableWidth / columnCount;
  return explicitWidths.map((width) => width != null ? width : roundCssNumber(fallbackColumnWidth));
}
function createTableCellFromHtml(element, rowSpan, colSpan) {
  var _a, _b, _c;
  const cell = getEmptyTableCell();
  if (rowSpan > 1) {
    cell.rowSpan = rowSpan;
  }
  if (colSpan > 1) {
    cell.columnSpan = colSpan;
  }
  const style = (_a = element.getAttribute("style")) != null ? _a : "";
  const width = readCssSize(element.getAttribute("width"), style, "width");
  const backgroundColor = (_c = (_b = readCssColor(style, "background-color")) != null ? _b : readCssColor(style, "background")) != null ? _c : readHtmlAttributeColor(element, "bgcolor");
  const borderTop = createTableCellBorder(style, "top");
  const borderRight = createTableCellBorder(style, "right");
  const borderBottom = createTableCellBorder(style, "bottom");
  const borderLeft = createTableCellBorder(style, "left");
  if (width != null) {
    cell.size = {
      type: 1 /* SPECIFIED */,
      width: { v: width }
    };
  }
  if (backgroundColor) {
    cell.backgroundColor = { rgb: backgroundColor };
  }
  if (borderTop) {
    cell.borderTop = borderTop;
  }
  if (borderRight) {
    cell.borderRight = borderRight;
  }
  if (borderBottom) {
    cell.borderBottom = borderBottom;
  }
  if (borderLeft) {
    cell.borderLeft = borderLeft;
  }
  return cell;
}
function createTableCellBorder(style, side) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k;
  const sideStyle = readCssValue(style, `border-${side}`);
  const color = (_g = (_f = (_c = (_b = readCssColor(style, `border-${side}-color`)) != null ? _b : normalizeCssColor((_a = sideStyle == null ? void 0 : sideStyle.match(/(#[0-9a-f]{3,8}|rgb\([^)]+\))/i)) == null ? void 0 : _a[1])) != null ? _c : readCssColor(style, "border-color")) != null ? _f : normalizeCssColor((_e = (_d = readCssValue(style, "border")) == null ? void 0 : _d.match(/(#[0-9a-f]{3,8}|rgb\([^)]+\))/i)) == null ? void 0 : _e[1])) != null ? _g : readNamedBorderColor(style);
  const width = (_j = (_i = (_h = readCssLengthValue(readCssValue(style, `border-${side}-width`))) != null ? _h : readCssLengthValue(sideStyle)) != null ? _i : readCssLengthValue(readCssValue(style, "border-width"))) != null ? _j : readCssLengthValue(readCssValue(style, "border"));
  if (!color && width == null) {
    return void 0;
  }
  return {
    color: { rgb: (_k = normalizeCssColor(color)) != null ? _k : "#1f1f1f" },
    width: { v: width != null ? width : 1 }
  };
}
function readNamedBorderColor(style) {
  const match = style.match(/border(?:-[a-z]+)?\s*:\s*([^;]+)/i);
  if (!match) {
    return void 0;
  }
  const keyword = match[1].split(/\s+/).find((part) => {
    const normalized = part.toLowerCase();
    return normalized && !/^[0-9.]+/.test(normalized) && !["none", "hidden", "dotted", "dashed", "solid", "double", "groove", "ridge", "inset", "outset"].includes(normalized);
  });
  return normalizeCssColor(keyword);
}
function readHtmlAttributeColor(element, attributeName) {
  return normalizeCssColor(element.getAttribute(attributeName));
}
function readCssColor(style, property) {
  return normalizeCssColor(readCssValue(style, property));
}
function normalizeCssColor(value) {
  if (!value) {
    return void 0;
  }
  try {
    const color = new ColorKit(value.trim());
    return color.isValid ? color.toRgbString() : void 0;
  } catch {
    return void 0;
  }
}
function readCssValue(style, property) {
  const normalizedProperty = property.toLowerCase();
  const declaration = style.split(";").map((part) => part.split(":")).find(([name]) => (name == null ? void 0 : name.trim().toLowerCase()) === normalizedProperty);
  return (declaration == null ? void 0 : declaration.slice(1).join(":").trim()) || void 0;
}
function readCssSize(attributeValue, style, property) {
  var _a;
  const styleMatch = (_a = readCssValue(style != null ? style : "", property)) == null ? void 0 : _a.match(/^([0-9.]+)\s*(px|pt|in|cm|mm)?$/i);
  if (styleMatch) {
    return toPixels(Number(styleMatch[1]), styleMatch[2]);
  }
  const attrMatch = attributeValue == null ? void 0 : attributeValue.match(/([0-9.]+)\s*(px|pt|in|cm|mm)?/i);
  return attrMatch ? toPixels(Number(attrMatch[1]), attrMatch[2]) : void 0;
}
function readCssLengthValue(value) {
  var _a;
  const match = (_a = value == null ? void 0 : value.match(/(?:^|\s)([0-9.]+)\s*(px|pt|in|cm|mm)(?=\s|$)/i)) != null ? _a : value == null ? void 0 : value.match(/^\s*([0-9.]+)\s*$/);
  return match ? toPixels(Number(match[1]), match[2]) : void 0;
}
function readImageSize(element, property) {
  var _a;
  const datasetValue = property === "width" ? element.dataset.width : element.dataset.height;
  return (_a = readCssSize(datasetValue || null, null, property)) != null ? _a : readCssSize(element.getAttribute(property), element.getAttribute("style"), property);
}
function toPixels(value, unit) {
  switch (unit == null ? void 0 : unit.toLowerCase()) {
    case "pt":
      return roundCssNumber(value * 4 / 3);
    case "in":
      return roundCssNumber(value * 96);
    case "cm":
      return roundCssNumber(value * 96 / 2.54);
    case "mm":
      return roundCssNumber(value * 96 / 25.4);
    default:
      return roundCssNumber(value);
  }
}
function roundCssNumber(value) {
  return Math.round(value * 100) / 100;
}
function getStructuredBlockType(node) {
  const docType = node.dataset.docType;
  if (docType === "quote") {
    return "quote";
  }
  if (docType === "callout") {
    return "callout";
  }
  const tagName = node.tagName.toUpperCase();
  if (tagName === "BLOCKQUOTE") {
    return docType === "callout" ? "callout" : "quote";
  }
  if (tagName === "ASIDE" && node.getAttribute("role") === "note") {
    return "callout";
  }
  return null;
}
function isDefaultParagraphElement(node) {
  const tagName = node.tagName.toUpperCase();
  return ["P", "DIV", "H1", "H2", "H3", "H4", "H5", "H6", "LI"].includes(tagName);
}
function getHeadingNamedStyleType(node) {
  switch (node.tagName.toUpperCase()) {
    case "H1":
      return 4 /* HEADING_1 */;
    case "H2":
      return 5 /* HEADING_2 */;
    case "H3":
      return 6 /* HEADING_3 */;
    case "H4":
      return 7 /* HEADING_4 */;
    case "H5":
      return 8 /* HEADING_5 */;
    default:
      return null;
  }
}
function extractWordListInfo(node) {
  var _a, _b, _c, _d, _e;
  const style = (_a = node.getAttribute("style")) != null ? _a : "";
  const match = style.match(/mso-list:\s*([^ ;]+)\s+level(\d+)/i);
  if (!match && !/MsoListParagraph/i.test(node.className)) {
    return null;
  }
  {
    const marker = ((_b = node.textContent) != null ? _b : "").trim().match(/^([0-9]+[.)]|[a-zA-Z][.)]|[ivxlcdmIVXLCDM]+[.)]|[\u2022\u00B7\u25CF\u25CB\u25AA\u25AB-])/);
    const markerText = (_c = marker == null ? void 0 : marker[1]) != null ? _c : "";
    const ordered = Boolean(markerText && !/^[\u2022\u00B7\u25CF\u25CB\u25AA\u25AB-]$/.test(markerText));
    return {
      listId: (_d = match == null ? void 0 : match[1]) != null ? _d : "mso-list",
      listType: ordered ? "ORDER_LIST" /* ORDER_LIST */ : "BULLET_LIST" /* BULLET_LIST */,
      nestingLevel: Math.max(0, Number((_e = match == null ? void 0 : match[2]) != null ? _e : 1) - 1)
    };
  }
}
function stripListMarkerFromCurrentParagraph(body) {
  var _a, _b;
  const paragraphEnd = body.dataStream.endsWith("\r") ? body.dataStream.length - 1 : body.dataStream.length;
  const paragraphStart = Math.max(0, body.dataStream.lastIndexOf("\r", paragraphEnd - 1) + 1);
  const text = body.dataStream.slice(paragraphStart, paragraphEnd);
  {
    const marker2 = text.match(/^(\s*(?:[0-9]+[.)]|[a-zA-Z][.)]|[ivxlcdmIVXLCDM]+[.)]|[\u2022\u00B7\u25CF\u25CB\u25AA\u25AB-])\s*)/);
    if (marker2) {
      const length2 = marker2[1].length;
      body.dataStream = body.dataStream.slice(0, paragraphStart) + body.dataStream.slice(paragraphStart + length2);
      (_a = body.textRuns) == null ? void 0 : _a.forEach((textRun) => {
        if (textRun.st >= paragraphStart + length2) {
          textRun.st -= length2;
          textRun.ed -= length2;
        } else if (textRun.ed > paragraphStart) {
          textRun.ed = Math.max(textRun.st, textRun.ed - length2);
        }
      });
      return length2;
    }
  }
  const marker = text.match(/^(\s*(?:[0-9]+\.|[a-zA-Z]\.|[ivxlcdmIVXLCDM]+\.|[·•●○\-])\s*)/);
  if (!marker) {
    return 0;
  }
  const length = marker[1].length;
  body.dataStream = body.dataStream.slice(0, paragraphStart) + body.dataStream.slice(paragraphStart + length);
  (_b = body.textRuns) == null ? void 0 : _b.forEach((textRun) => {
    if (textRun.st >= paragraphStart + length) {
      textRun.st -= length;
      textRun.ed -= length;
    } else if (textRun.ed > paragraphStart) {
      textRun.ed = Math.max(textRun.st, textRun.ed - length);
    }
  });
  return length;
}

// ../packages/docs-ui/src/services/clipboard/html-to-udm/paste-plugins/plugin-lark.ts
var LarkPastePlugin = {
  name: "univer-doc-paste-plugin-lark",
  checkPasteType(html) {
    return /lark-record-clipboard/i.test(html);
  },
  // TODO: @JOCS, support inline code copy from lark.
  stylesRules: [
    {
      filter: ["s"],
      getStyle(node) {
        const inlineStyle = extractNodeStyle(node);
        return {
          st: {
            s: 1 /* TRUE */
          },
          ...inlineStyle
        };
      }
    }
  ],
  afterProcessRules: [
    {
      filter(el) {
        return el.tagName === "DIV" && /ace-line/i.test(el.className);
      },
      handler(doc) {
        const body = doc.body;
        if (body.paragraphs == null) {
          body.paragraphs = [];
        }
        body.paragraphs.push({
          startIndex: body.dataStream.length
        });
        body.dataStream += "\r";
      }
    }
  ]
};
var plugin_lark_default = LarkPastePlugin;

// ../packages/docs-ui/src/services/clipboard/html-to-udm/utils.ts
function getParagraphStyle(el) {
  const styles = el.style;
  const paragraphStyle = {};
  for (let i = 0; i < styles.length; i++) {
    const cssRule = styles[i];
    const cssValue = styles.getPropertyValue(cssRule);
    switch (cssRule) {
      case "margin-top": {
        const marginTopValue = Number.parseInt(cssValue);
        paragraphStyle.spaceAbove = { v: /pt/.test(cssValue) ? ptToPixel(marginTopValue) : marginTopValue };
        break;
      }
      case "margin-bottom": {
        const marginBottomValue = Number.parseInt(cssValue);
        paragraphStyle.spaceBelow = { v: /pt/.test(cssValue) ? ptToPixel(marginBottomValue) : marginBottomValue };
        break;
      }
      case "line-height": {
        let lineHeightValue = Number.parseFloat(cssValue);
        if (cssValue.endsWith("%")) {
          lineHeightValue /= 100;
        }
        paragraphStyle.lineSpacing = lineHeightValue;
        break;
      }
      case "text-align": {
        paragraphStyle.horizontalAlign = getHorizontalAlign(cssValue);
        break;
      }
      default: {
        break;
      }
    }
  }
  return Object.getOwnPropertyNames(paragraphStyle).length ? paragraphStyle : null;
}
function getHorizontalAlign(value) {
  switch (value.trim().toLowerCase()) {
    case "center":
      return 2 /* CENTER */;
    case "right":
    case "end":
      return 3 /* RIGHT */;
    case "justify":
      return 4 /* JUSTIFIED */;
    case "left":
    case "start":
      return 1 /* LEFT */;
    default:
      return 0 /* UNSPECIFIED */;
  }
}

// ../packages/docs-ui/src/services/clipboard/html-to-udm/paste-plugins/plugin-univer.ts
var UniverPastePlugin = {
  name: "univer-doc-paste-plugin-univer",
  checkPasteType(html) {
    return /UniverNormal/i.test(html);
  },
  stylesRules: [],
  afterProcessRules: [
    {
      filter(el) {
        return el.tagName === "P" && /UniverNormal/i.test(el.className);
      },
      handler(doc, el) {
        const body = doc.body;
        if (body.paragraphs == null) {
          body.paragraphs = [];
        }
        const paragraph = {
          startIndex: body.dataStream.length
        };
        const paragraphStyle = getParagraphStyle(el);
        if (paragraphStyle) {
          paragraph.paragraphStyle = paragraphStyle;
        }
        body.paragraphs.push(paragraph);
        body.dataStream += "\r";
      }
    }
  ]
};
var plugin_univer_default = UniverPastePlugin;

// ../packages/docs-ui/src/services/clipboard/html-to-udm/paste-plugins/plugin-word.ts
var WORD_SPACE_PLACEHOLDER_ENTITY = "&#57344;";
var WORD_TAB_WIDTH = 4;
var WordPastePlugin = {
  name: "univer-doc-paste-plugin-word",
  checkPasteType(html) {
    return /word|mso|wps|kingsoft/i.test(html);
  },
  preprocessHtml(html) {
    return html.replace(/<span\b([^>]*)>([\s\S]*?)<\/span>/gi, (match, attributes, content) => {
      const isWordSpacer = /mso-spacerun\s*:\s*yes/i.test(attributes);
      const tabCount = readMsoTabCount(attributes);
      if (!isWordSpacer && tabCount == null) {
        return match;
      }
      const contentSpaceCount = countWordSpaces(content);
      const fallbackSpaceCount = tabCount == null ? 1 : Math.max(1, Math.round(tabCount * WORD_TAB_WIDTH));
      const spaceCount = Math.max(contentSpaceCount, fallbackSpaceCount);
      return WORD_SPACE_PLACEHOLDER_ENTITY.repeat(spaceCount);
    });
  },
  stylesRules: [
    {
      filter: ["b"],
      getStyle(node) {
        const inlineStyle = extractNodeStyle(node);
        return { bl: 1 /* TRUE */, ...inlineStyle };
      }
    }
  ],
  afterProcessRules: [
    {
      filter(el) {
        return el.tagName === "P";
      },
      handler(doc, el) {
        const body = doc.body;
        if (body.paragraphs == null) {
          body.paragraphs = [];
        }
        const paragraph = {
          startIndex: body.dataStream.length
        };
        const paragraphStyle = getParagraphStyle(el);
        if (paragraphStyle) {
          paragraph.paragraphStyle = paragraphStyle;
        }
        body.paragraphs.push(paragraph);
        body.dataStream += "\r";
      }
    }
  ]
};
function readMsoTabCount(attributes) {
  const match = attributes.match(/mso-tab-count\s*:\s*([0-9.]+)/i);
  if (!match) {
    return void 0;
  }
  const count = Number(match[1]);
  return Number.isFinite(count) ? count : void 0;
}
function countWordSpaces(content) {
  var _a, _b, _c, _d, _e, _f;
  const entitySpaceCount = (_b = (_a = content.match(/&nbsp;|&#160;|&#x0*a0;/gi)) == null ? void 0 : _a.length) != null ? _b : 0;
  const text = content.replace(/&nbsp;|&#160;|&#x0*a0;/gi, "\xA0").replace(/<[^>]*>/g, "");
  const literalSpaceCount = (_d = (_c = text.match(/[\u00A0\t]/g)) == null ? void 0 : _c.reduce((count, char) => count + (char === "	" ? WORD_TAB_WIDTH : 1), 0)) != null ? _d : 0;
  if (entitySpaceCount || literalSpaceCount) {
    return literalSpaceCount;
  }
  return (_f = (_e = text.replace(/\r?\n/g, "").match(/ /g)) == null ? void 0 : _e.length) != null ? _f : 0;
}
var plugin_word_default = WordPastePlugin;

// ../packages/docs-ui/src/services/clipboard/internal-fragment.ts
var DOC_INTERNAL_FRAGMENT_MIME = "application/x-doc-fragment+json";
var DOC_INTERNAL_FRAGMENT_COMMENT_PREFIX = "univer-doc-fragment:";
function createInternalClipboardFragment(doc) {
  return JSON.stringify({
    version: 1,
    kind: "univer-doc-fragment",
    doc: Tools.deepClone(doc)
  });
}
function parseInternalClipboardFragment(value) {
  var _a;
  if (!value) {
    return null;
  }
  try {
    const parsed = JSON.parse(value);
    if ((parsed == null ? void 0 : parsed.version) === 1 && parsed.kind === "univer-doc-fragment" && ((_a = parsed.doc) == null ? void 0 : _a.body)) {
      return parsed.doc;
    }
  } catch {
    return null;
  }
  return null;
}
function createInternalClipboardDocData(doc) {
  var _a, _b, _c, _d;
  const body = Tools.deepClone(doc.body);
  const internalDocData = { body };
  if (((_a = body == null ? void 0 : body.tables) == null ? void 0 : _a.length) && doc.tableSource) {
    internalDocData.tableSource = {};
    body.tables.forEach((tableRange) => {
      var _a2;
      const table = (_a2 = doc.tableSource) == null ? void 0 : _a2[tableRange.tableId];
      if (table) {
        internalDocData.tableSource[tableRange.tableId] = Tools.deepClone(table);
      }
    });
  }
  const listTypes = new Set((_b = body == null ? void 0 : body.paragraphs) == null ? void 0 : _b.map((paragraph) => {
    var _a2;
    return (_a2 = paragraph.bullet) == null ? void 0 : _a2.listType;
  }).filter(Boolean));
  if (listTypes.size && doc.lists) {
    internalDocData.lists = {};
    listTypes.forEach((listType) => {
      var _a2;
      const list = (_a2 = doc.lists) == null ? void 0 : _a2[String(listType)];
      if (list) {
        internalDocData.lists[String(listType)] = Tools.deepClone(list);
      }
    });
  }
  if ((_c = body == null ? void 0 : body.customBlocks) == null ? void 0 : _c.length) {
    internalDocData.drawings = {};
    for (const block of body.customBlocks) {
      const { blockId } = block;
      const drawing = (_d = doc.drawings) == null ? void 0 : _d[blockId];
      if (drawing) {
        const id = generateRandomId(6);
        block.blockId = id;
        internalDocData.drawings[id] = {
          ...Tools.deepClone(drawing),
          drawingId: id
        };
      }
    }
  }
  return internalDocData;
}
function createInternalClipboardDocDataList(docs) {
  var _a, _b, _c, _d, _e;
  if (docs.length === 0) {
    return null;
  }
  if (docs.length === 1) {
    return createInternalClipboardDocData(docs[0]);
  }
  const merged = {
    body: {
      dataStream: ""
    }
  };
  for (const doc of docs) {
    const part = createInternalClipboardDocData(doc);
    const body = part.body;
    if (!body) {
      continue;
    }
    const offset = merged.body.dataStream.length;
    merged.body.dataStream += (_a = body.dataStream) != null ? _a : "";
    appendOffsetRanges(merged.body, body, offset);
    if (part.tableSource) {
      (_b = merged.tableSource) != null ? _b : merged.tableSource = {};
      Object.assign(merged.tableSource, part.tableSource);
    }
    if (part.lists) {
      (_c = merged.lists) != null ? _c : merged.lists = {};
      Object.assign(merged.lists, part.lists);
    }
    if (part.drawings) {
      (_d = merged.drawings) != null ? _d : merged.drawings = {};
      Object.assign(merged.drawings, part.drawings);
    }
  }
  return ((_e = merged.body) == null ? void 0 : _e.dataStream) ? merged : null;
}
function embedInternalClipboardFragment(html, fragmentJson) {
  return `<!--${DOC_INTERNAL_FRAGMENT_COMMENT_PREFIX}${encodeBase64(fragmentJson)}-->${html}`;
}
function extractInternalClipboardFragmentFromHtml(html) {
  if (!html) {
    return null;
  }
  const escapedPrefix = DOC_INTERNAL_FRAGMENT_COMMENT_PREFIX.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
  const match = html.match(new RegExp(`<!--\\s*${escapedPrefix}([A-Za-z0-9+/=]+)\\s*-->`));
  if (!match) {
    return null;
  }
  return parseInternalClipboardFragment(decodeBase64(match[1]));
}
var WORD_CLIPBOARD_HTML_HEAD = `<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
<meta name="ProgId" content="Word.Document">
<meta name="Generator" content="Microsoft Word 15">
<!--[if gte mso 9]><xml><w:WordDocument><w:View>Normal</w:View><w:Zoom>0</w:Zoom><w:TrackFormatting/><w:DoNotPromoteQF/></w:WordDocument></xml><![endif]-->
<style><!--
p.MsoNormal, li.MsoNormal, div.MsoNormal{margin:0cm;margin-bottom:8.0pt;line-height:115%;font-size:11.0pt;font-family:Arial,sans-serif;mso-pagination:widow-orphan;}
p.UniverNormal, li.UniverNormal, div.UniverNormal, p.univernormal, li.univernormal, div.univernormal{mso-style-name:univernormal;margin-top:0cm;margin-right:0cm;margin-bottom:8.0pt;margin-left:0cm;line-height:115%;mso-pagination:widow-orphan;font-size:11.0pt;font-family:Arial,sans-serif;mso-bidi-font-family:Arial;}
p.UniverHeading, li.UniverHeading, div.UniverHeading, p.univerheading, li.univerheading, div.univerheading{mso-style-name:univerheading;margin-top:6.0pt;margin-right:0cm;margin-bottom:4.0pt;margin-left:0cm;mso-pagination:widow-orphan;font-size:12.0pt;font-family:Arial,sans-serif;mso-bidi-font-family:Arial;font-weight:bold;}
table.MsoNormalTable, table.UniverTable{mso-style-name:"Normal Table";mso-tstyle-rowband-size:0;mso-tstyle-colband-size:0;mso-style-noshow:yes;mso-style-priority:99;mso-style-parent:"";mso-padding-alt:0cm 5.4pt 0cm 5.4pt;mso-para-margin:0cm;mso-pagination:widow-orphan;font-size:11.0pt;font-family:Arial,sans-serif;border-collapse:collapse;mso-table-lspace:0pt;mso-table-rspace:0pt;}
td.UniverTableCell, th.UniverTableCell{mso-padding-alt:0cm 5.4pt 0cm 5.4pt;}
--></style>
</head>`;
function wrapClipboardHtml(fragmentHtml) {
  return `<html xmlns:o="urn:schemas-microsoft-com:office:office" xmlns:w="urn:schemas-microsoft-com:office:word" xmlns="http://www.w3.org/TR/REC-html40">${WORD_CLIPBOARD_HTML_HEAD}<body lang="ZH-CN" style="tab-interval:21.0pt;word-wrap:break-word"><!--StartFragment-->${fragmentHtml}<!--EndFragment--></body></html>`;
}
function appendOffsetRanges(target, source, offset) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p;
  if ((_a = source.textRuns) == null ? void 0 : _a.length) {
    (_b = target.textRuns) != null ? _b : target.textRuns = [];
    target.textRuns.push(...source.textRuns.map((textRun) => ({
      ...textRun,
      st: textRun.st + offset,
      ed: textRun.ed + offset
    })));
  }
  if ((_c = source.paragraphs) == null ? void 0 : _c.length) {
    (_d = target.paragraphs) != null ? _d : target.paragraphs = [];
    target.paragraphs.push(...source.paragraphs.map((paragraph) => ({
      ...paragraph,
      startIndex: paragraph.startIndex + offset
    })));
  }
  if ((_e = source.sectionBreaks) == null ? void 0 : _e.length) {
    (_f = target.sectionBreaks) != null ? _f : target.sectionBreaks = [];
    target.sectionBreaks.push(...source.sectionBreaks.map((sectionBreak) => ({
      ...sectionBreak,
      startIndex: sectionBreak.startIndex + offset
    })));
  }
  if ((_g = source.tables) == null ? void 0 : _g.length) {
    (_h = target.tables) != null ? _h : target.tables = [];
    target.tables.push(...source.tables.map((table) => ({
      ...table,
      startIndex: table.startIndex + offset,
      endIndex: table.endIndex + offset
    })));
  }
  if ((_i = source.blockRanges) == null ? void 0 : _i.length) {
    (_j = target.blockRanges) != null ? _j : target.blockRanges = [];
    target.blockRanges.push(...source.blockRanges.map((blockRange) => ({
      ...blockRange,
      startIndex: blockRange.startIndex + offset,
      endIndex: blockRange.endIndex + offset
    })));
  }
  if ((_k = source.customRanges) == null ? void 0 : _k.length) {
    (_l = target.customRanges) != null ? _l : target.customRanges = [];
    target.customRanges.push(...source.customRanges.map((customRange) => ({
      ...customRange,
      startIndex: customRange.startIndex + offset,
      endIndex: customRange.endIndex + offset
    })));
  }
  if ((_m = source.customDecorations) == null ? void 0 : _m.length) {
    (_n = target.customDecorations) != null ? _n : target.customDecorations = [];
    target.customDecorations.push(...source.customDecorations.map((customDecoration) => ({
      ...customDecoration,
      startIndex: customDecoration.startIndex + offset,
      endIndex: customDecoration.endIndex + offset
    })));
  }
  if ((_o = source.customBlocks) == null ? void 0 : _o.length) {
    (_p = target.customBlocks) != null ? _p : target.customBlocks = [];
    target.customBlocks.push(...source.customBlocks.map((customBlock) => ({
      ...customBlock,
      startIndex: customBlock.startIndex + offset
    })));
  }
}
function encodeBase64(value) {
  return btoa(unescape(encodeURIComponent(value)));
}
function decodeBase64(value) {
  return decodeURIComponent(escape(atob(value)));
}

// ../packages/docs-ui/src/services/clipboard/udm-to-html/convertor.ts
var DEFAULT_CLIPBOARD_FONT_FAMILY = "Arial";
function covertImageToHtml(item) {
  var _a, _b;
  const transformObjectToString = (obj2) => {
    let result = "";
    Object.keys(obj2).forEach((key) => {
      if (obj2[key] !== void 0) {
        result += ` ${key}=${obj2[key]}`;
      }
    });
    return result;
  };
  const obj = {
    "data-doc-transform-height": item.docTransform.size.height,
    "data-doc-transform-width": item.docTransform.size.width,
    "data-width": (_a = item.transform) == null ? void 0 : _a.width,
    "data-height": (_b = item.transform) == null ? void 0 : _b.height,
    "data-image-source-type": item.imageSourceType,
    "data-source": item.imageSourceType === "UUID" /* UUID */ ? item.source : void 0,
    src: item.source
  };
  return `<img  ${transformObjectToString(obj)}></img>`;
}
function covertTextRunToHtml(dataStream, textRun) {
  const { st: start, ed, ts = {} } = textRun;
  const { ff, fs, it, bl, ul, st, ol, bg, cl, va } = ts;
  let html = escapeInlineText(dataStream.slice(start, ed));
  const style = [];
  if (it === 1 /* TRUE */) {
    html = `<i>${html}</i>`;
  }
  if (va === 3 /* SUPERSCRIPT */) {
    html = `<sup>${html}</sup>`;
  } else if (va === 2 /* SUBSCRIPT */) {
    html = `<sub>${html}</sub>`;
  }
  if ((ul == null ? void 0 : ul.s) === 1 /* TRUE */) {
    html = `<u>${html}</u>`;
  }
  if ((st == null ? void 0 : st.s) === 1 /* TRUE */) {
    html = `<s>${html}</s>`;
  }
  if (bl === 1 /* TRUE */) {
    html = `<strong>${html}</strong>`;
  }
  style.push(`font-family: ${ff != null ? ff : DEFAULT_CLIPBOARD_FONT_FAMILY}`);
  if (cl == null ? void 0 : cl.rgb) {
    style.push(`color: ${normalizeCssColor2(cl.rgb)}`);
  }
  if (fs) {
    style.push(`font-size: ${fs}pt`);
  }
  if (ol) {
    style.push("text-decoration: overline");
  }
  if (bg == null ? void 0 : bg.rgb) {
    style.push(`background: ${normalizeCssColor2(bg.rgb)}`);
  }
  return style.length ? `<span style="${style.join("; ")};">${html}</span>` : html;
}
function getBodyInlineSlice(body, startIndex, endIndex) {
  const { dataStream, textRuns = [] } = body;
  if (startIndex === endIndex) {
    return "";
  }
  let cursorIndex = startIndex;
  const spanList = [];
  for (const textRun of textRuns) {
    const { st, ed } = textRun;
    if (Tools.hasIntersectionBetweenTwoRanges(startIndex, endIndex, st, ed)) {
      if (st > cursorIndex) {
        spanList.push(escapeInlineText(dataStream.slice(cursorIndex, st)));
        spanList.push(covertTextRunToHtml(dataStream, {
          ...textRun,
          ed: Math.min(ed, endIndex)
        }));
      } else {
        spanList.push(covertTextRunToHtml(dataStream, {
          ...textRun,
          st: cursorIndex,
          ed: Math.min(ed, endIndex)
        }));
      }
    }
    cursorIndex = Math.max(startIndex, Math.min(ed, endIndex));
  }
  if (cursorIndex !== endIndex) {
    spanList.push(escapeInlineText(dataStream.slice(cursorIndex, endIndex)));
  }
  return spanList.join("");
}
function getBodySliceHtml(doc, startIndex, endIndex) {
  const body = doc.body;
  const drawings = doc.drawings || {};
  const { customRanges = [], customBlocks = [] } = body || {};
  const cloneCustomBlocks = [...customBlocks];
  const customRangesInRange = customRanges.filter((range) => range.startIndex >= startIndex && range.endIndex <= endIndex);
  let cursorIndex = startIndex;
  let html = "";
  const handleCustomBlock = (startIndex2, endIndex2) => {
    let sliceHtml = "";
    let customBlockLength = 0;
    let handleCustomBlockCursorIndex = startIndex2;
    let blockItemIndex = cloneCustomBlocks.findIndex((block) => startIndex2 <= block.startIndex && endIndex2 >= block.startIndex);
    if (blockItemIndex === -1) {
      sliceHtml = getBodyInlineSlice(body, startIndex2, endIndex2);
      return { sliceHtml, customBlockLength };
    }
    while (blockItemIndex !== -1) {
      const blockItem = cloneCustomBlocks[blockItemIndex];
      cloneCustomBlocks.splice(blockItemIndex, 1);
      sliceHtml += getBodyInlineSlice(body, handleCustomBlockCursorIndex, blockItem.startIndex);
      const drawingItem = drawings[blockItem.blockId];
      if (drawingItem) {
        switch (drawingItem.drawingType) {
          case 0 /* DRAWING_IMAGE */: {
            sliceHtml += covertImageToHtml(drawingItem);
            customBlockLength++;
            break;
          }
        }
      }
      handleCustomBlockCursorIndex = blockItem.startIndex + 1;
      blockItemIndex = cloneCustomBlocks.findIndex((block) => handleCustomBlockCursorIndex <= block.startIndex && endIndex2 >= block.startIndex);
    }
    sliceHtml = sliceHtml + getBodyInlineSlice(body, handleCustomBlockCursorIndex, endIndex2 + 1);
    return { sliceHtml, customBlockLength };
  };
  customRangesInRange.forEach((range) => {
    var _a, _b;
    const { startIndex: startIndex2, endIndex: endIndex2, rangeType, rangeId } = range;
    const preHtml = handleCustomBlock(cursorIndex, startIndex2);
    html += preHtml.sliceHtml;
    const sliceHtml = handleCustomBlock(startIndex2, endIndex2 + 1);
    switch (rangeType) {
      case 0 /* HYPERLINK */: {
        html += `<a data-rangeid="${rangeId}" href="${(_b = (_a = range.properties) == null ? void 0 : _a.url) != null ? _b : ""}">${sliceHtml.sliceHtml}</a>`;
        break;
      }
      default: {
        html += sliceHtml.sliceHtml;
        break;
      }
    }
    cursorIndex = endIndex2 + 1 + (preHtml.customBlockLength + sliceHtml.customBlockLength);
  });
  const endHtml = handleCustomBlock(cursorIndex, endIndex);
  html += endHtml.sliceHtml;
  return html;
}
function convertBodyToHtml(doc) {
  const body = doc.body || {};
  const { paragraphs = [], sectionBreaks = [] } = body;
  let { dataStream = "" } = body;
  if (!dataStream.endsWith("\r\n")) {
    dataStream += "\r\n";
    paragraphs.push({
      startIndex: dataStream.length - 2
    });
    sectionBreaks.push({
      startIndex: dataStream.length - 1
    });
    body.dataStream = dataStream;
    body.paragraphs = paragraphs;
    body.sectionBreaks = sectionBreaks;
  }
  const result = { html: "" };
  const nodeList = parseDataStreamToTree(dataStream).sectionList;
  for (const node of nodeList) {
    processNode(node, doc, result);
  }
  closeListStack(result);
  return result.html;
}
function processNode(node, doc, result) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o, _p, _q, _r, _s;
  switch (node.nodeType) {
    case "SECTION_BREAK" /* SECTION_BREAK */: {
      for (const n of node.children) {
        processNode(n, doc, result);
      }
      break;
    }
    case "PARAGRAPH" /* PARAGRAPH */: {
      const { children, startIndex, endIndex } = node;
      const paragraph = (_c = (_b = (_a = doc.body) == null ? void 0 : _a.paragraphs) == null ? void 0 : _b.find((p) => p.startIndex === endIndex)) != null ? _c : {};
      const { paragraphStyle = {} } = paragraph;
      const { horizontalAlign, spaceAbove, spaceBelow, lineSpacing } = paragraphStyle;
      const style = [];
      if (horizontalAlign != null) {
        const align = getTextAlign(horizontalAlign);
        if (align) {
          style.push(`text-align: ${align}`);
        }
      }
      if (spaceAbove != null) {
        if (typeof spaceAbove === "number") {
          style.push(`margin-top: ${spaceAbove}px`);
        } else {
          style.push(`margin-top: ${spaceAbove.v}px`);
        }
      }
      if (spaceBelow != null) {
        if (typeof spaceBelow === "number") {
          style.push(`margin-bottom: ${spaceBelow}px`);
        } else {
          style.push(`margin-bottom: ${spaceBelow.v}px`);
        }
      }
      if (lineSpacing != null) {
        style.push(`line-height: ${lineSpacing}`);
      }
      style.push(...serializeTextStyle(paragraphStyle.textStyle));
      const inlineHtml = applySemanticTextStyle(getBodySliceHtml(doc, startIndex, endIndex), paragraphStyle.textStyle);
      const childHtml = children.map((table) => {
        const childResult = { html: "" };
        processNode(table, doc, childResult);
        closeListStack(childResult);
        return childResult.html;
      }).join("");
      if (childHtml) {
        if (hasVisibleHtml(inlineHtml)) {
          renderParagraphNodeHtml(doc, paragraph, startIndex, endIndex, inlineHtml, style, result);
        } else {
          closeListStack(result);
        }
        result.html += childHtml;
        break;
      }
      renderParagraphNodeHtml(doc, paragraph, startIndex, endIndex, inlineHtml, style, result);
      break;
    }
    case "TABLE" /* TABLE */: {
      const { children } = node;
      closeListStack(result);
      (_d = result.tableIndex) != null ? _d : result.tableIndex = 0;
      (_e = result.tableStack) != null ? _e : result.tableStack = [];
      const tableRange = (_g = (_f = doc.body) == null ? void 0 : _f.tables) == null ? void 0 : _g[result.tableIndex++];
      const table = tableRange ? (_h = doc.tableSource) == null ? void 0 : _h[tableRange.tableId] : void 0;
      const width = (_i = table == null ? void 0 : table.tableColumns) == null ? void 0 : _i.reduce((sum, column) => {
        var _a2, _b2, _c2;
        return sum + ((_c2 = (_b2 = (_a2 = column.size) == null ? void 0 : _a2.width) == null ? void 0 : _b2.v) != null ? _c2 : 0);
      }, 0);
      const tableStyle = [
        "border-collapse: collapse",
        width ? `width: ${roundCssNumber2(width)}px` : "width: 100%"
      ].join("; ");
      result.tableStack.push({ table, rowIndex: -1, columnIndex: 0 });
      result.html += `<table class="MsoNormalTable UniverTable" style="${tableStyle};"><tbody>`;
      for (const row of children) {
        processNode(row, doc, result);
      }
      result.html += "</tbody></table>";
      result.tableStack.pop();
      break;
    }
    case "TABLE_ROW" /* TABLE_ROW */: {
      const { children } = node;
      const tableContext = (_j = result.tableStack) == null ? void 0 : _j[result.tableStack.length - 1];
      if (tableContext) {
        tableContext.rowIndex++;
        tableContext.columnIndex = 0;
      }
      const row = (_l = (_k = tableContext == null ? void 0 : tableContext.table) == null ? void 0 : _k.tableRows) == null ? void 0 : _l[tableContext.rowIndex];
      const rowStyle = ((_n = (_m = row == null ? void 0 : row.trHeight) == null ? void 0 : _m.val) == null ? void 0 : _n.v) != null ? ` style="height: ${roundCssNumber2(row.trHeight.val.v)}px"` : "";
      result.html += `<tr class="UniverTableRow"${rowStyle}>`;
      for (const cell of children) {
        processNode(cell, doc, result);
      }
      result.html += "</tr>";
      break;
    }
    case "TABLE_CELL" /* TABLE_CELL */: {
      const { children } = node;
      const tableContext = (_o = result.tableStack) == null ? void 0 : _o[result.tableStack.length - 1];
      const row = (_q = (_p = tableContext == null ? void 0 : tableContext.table) == null ? void 0 : _p.tableRows) == null ? void 0 : _q[tableContext.rowIndex];
      const cell = (_s = row == null ? void 0 : row.tableCells) == null ? void 0 : _s[(_r = tableContext == null ? void 0 : tableContext.columnIndex) != null ? _r : 0];
      if (tableContext) {
        tableContext.columnIndex++;
      }
      if ((cell == null ? void 0 : cell.rowSpan) === 0 || (cell == null ? void 0 : cell.columnSpan) === 0) {
        break;
      }
      result.html += `<td class="UniverTableCell"${serializeTableCellAttributes(cell)}>`;
      for (const n of children) {
        processNode(n, doc, result);
      }
      closeListStack(result);
      result.html += "</td>";
      break;
    }
    default: {
      throw new Error(`Unknown node type: ${node.nodeType}`);
    }
  }
}
function renderParagraphNodeHtml(doc, paragraph, startIndex, endIndex, innerHtml, style, result) {
  const listItemHtml = renderListParagraphItemHtml(doc, paragraph, innerHtml, style);
  if (listItemHtml) {
    appendListParagraphHtml(result, listItemHtml);
    return;
  }
  closeListStack(result);
  result.html += renderBlockParagraphHtml(doc, paragraph, startIndex, endIndex, innerHtml, style);
}
function renderBlockParagraphHtml(doc, paragraph, startIndex, endIndex, innerHtml, style) {
  var _a, _b, _c;
  const blockRange = (_b = (_a = doc.body) == null ? void 0 : _a.blockRanges) == null ? void 0 : _b.find((range) => range.startIndex <= startIndex && endIndex <= range.endIndex + 1);
  if (blockRange && !hasVisibleHtml(innerHtml)) {
    return "";
  }
  const paragraphHtml = (_c = renderHeadingParagraphHtml(paragraph, innerHtml, style)) != null ? _c : `<p class="UniverNormal" ${style.length ? `style="${style.join("; ")};"` : ""}>${innerHtml}</p>`;
  switch (blockRange == null ? void 0 : blockRange.blockType) {
    case "quote":
      return `<blockquote data-doc-type="quote" style="border-left: 4px solid #d0d7de; margin: 8px 0; padding: 4px 0 4px 12px; color: #57606a;">${paragraphHtml}</blockquote>`;
    case "code":
      return `<pre data-doc-type="code-block" style="font-family: Consolas, 'Courier New', monospace; background: #f6f8fa; padding: 12px; border-radius: 6px; white-space: pre-wrap;"><code>${innerHtml.replace(/<\/?[^>]+>/g, "")}</code></pre>`;
    case "callout":
      return `<aside data-doc-type="callout" role="note" style="background: #fff8c5; border-left: 4px solid #d4a72c; padding: 8px 12px; margin: 8px 0; border-radius: 4px;"><p><span data-callout-icon="true" style="margin-right: 6px;">\u{1F4A1}</span>${innerHtml}</p></aside>`;
    default:
      return paragraphHtml;
  }
}
function renderHeadingParagraphHtml(paragraph, innerHtml, style) {
  var _a;
  const headingLevel = getHeadingLevel((_a = paragraph.paragraphStyle) == null ? void 0 : _a.namedStyleType);
  if (!headingLevel) {
    return null;
  }
  return `<p class="UniverHeading" role="heading" aria-level="${headingLevel}" data-heading-level="${headingLevel}" ${style.length ? `style="${style.join("; ")};"` : ""}>${innerHtml}</p>`;
}
function renderListParagraphItemHtml(doc, paragraph, innerHtml, style) {
  var _a;
  const bullet = paragraph.bullet;
  if (!bullet) {
    return null;
  }
  const ordered = isOrderedListType(String(bullet.listType));
  const tag = ordered ? "ol" : "ul";
  const level = (_a = bullet.nestingLevel) != null ? _a : 0;
  const listStyle = getListStyleType(doc, String(bullet.listType), level);
  return {
    tag,
    level,
    listType: String(bullet.listType),
    listStyle,
    html: `<p class="UniverNormal" ${style.length ? `style="${style.join("; ")};"` : ""}>${innerHtml}</p>`
  };
}
function appendListParagraphHtml(result, listItem) {
  var _a;
  (_a = result.listStack) != null ? _a : result.listStack = [];
  const stack = result.listStack;
  while (stack.length > listItem.level + 1) {
    result.html += `</li></${stack.pop().tag}>`;
  }
  const current = stack[stack.length - 1];
  if (current && current.level === listItem.level) {
    if (current.tag === listItem.tag && current.listType === listItem.listType) {
      result.html += "</li><li>";
    } else {
      result.html += `</li></${stack.pop().tag}>`;
      openList(result, listItem);
    }
  } else {
    while (stack.length < listItem.level) {
      openList(result, {
        ...listItem,
        level: stack.length
      });
    }
    openList(result, listItem);
  }
  result.html += listItem.html;
}
function openList(result, listItem) {
  const listStyleHtml = listItem.listStyle ? ` list-style-type: ${listItem.listStyle};` : "";
  const marginLeft = listItem.level > 0 ? ` margin-left: ${listItem.level * 24}px;` : "";
  result.html += `<${listItem.tag} data-doc-type="${listItem.tag === "ol" ? "ordered-list" : "bullet-list"}" style="margin-top: 0; margin-bottom: 0;${marginLeft}${listStyleHtml}"><li>`;
  result.listStack.push({ tag: listItem.tag, level: listItem.level, listType: listItem.listType });
}
function closeListStack(result) {
  var _a;
  while ((_a = result.listStack) == null ? void 0 : _a.length) {
    result.html += `</li></${result.listStack.pop().tag}>`;
  }
}
function serializeTableCellAttributes(cell) {
  var _a, _b, _c, _d, _e;
  if (!cell) {
    return "";
  }
  const attributes = [];
  const styles = [];
  if (((_a = cell.rowSpan) != null ? _a : 1) > 1) {
    attributes.push(`rowspan="${cell.rowSpan}"`);
  }
  if (((_b = cell.columnSpan) != null ? _b : 1) > 1) {
    attributes.push(`colspan="${cell.columnSpan}"`);
  }
  if (((_d = (_c = cell.size) == null ? void 0 : _c.width) == null ? void 0 : _d.v) != null) {
    styles.push(`width: ${roundCssNumber2(cell.size.width.v)}px`);
  }
  if ((_e = cell.backgroundColor) == null ? void 0 : _e.rgb) {
    styles.push(`background-color: ${cell.backgroundColor.rgb}`);
  }
  [
    ["top", cell.borderTop],
    ["right", cell.borderRight],
    ["bottom", cell.borderBottom],
    ["left", cell.borderLeft]
  ].forEach(([side, border]) => {
    var _a2, _b2, _c2, _d2;
    if (!border) {
      return;
    }
    styles.push(`border-${side}: ${roundCssNumber2((_b2 = (_a2 = border.width) == null ? void 0 : _a2.v) != null ? _b2 : 1)}px solid ${(_d2 = (_c2 = border.color) == null ? void 0 : _c2.rgb) != null ? _d2 : "#1f1f1f"}`);
  });
  if (styles.length) {
    attributes.push(`style="${styles.join("; ")};"`);
  }
  return attributes.length ? ` ${attributes.join(" ")}` : "";
}
function serializeTextStyle(textStyle) {
  if (!textStyle) {
    return [];
  }
  const { ff, fs, it, bl, ul, st, ol, bg, cl } = textStyle;
  const style = [];
  style.push(`font-family: ${ff != null ? ff : DEFAULT_CLIPBOARD_FONT_FAMILY}`);
  if (fs != null) {
    style.push(`font-size: ${fs}pt`);
  }
  if (cl == null ? void 0 : cl.rgb) {
    style.push(`color: ${normalizeCssColor2(cl.rgb)}`);
  }
  if (bg == null ? void 0 : bg.rgb) {
    style.push(`background-color: ${normalizeCssColor2(bg.rgb)}`);
  }
  if (bl === 1 /* TRUE */) {
    style.push("font-weight: bold");
  }
  if (it === 1 /* TRUE */) {
    style.push("font-style: italic");
  }
  const textDecorations = [];
  if ((ul == null ? void 0 : ul.s) === 1 /* TRUE */) {
    textDecorations.push("underline");
  }
  if ((st == null ? void 0 : st.s) === 1 /* TRUE */) {
    textDecorations.push("line-through");
  }
  if ((ol == null ? void 0 : ol.s) === 1 /* TRUE */) {
    textDecorations.push("overline");
  }
  if (textDecorations.length) {
    style.push(`text-decoration: ${textDecorations.join(" ")}`);
  }
  return style;
}
function applySemanticTextStyle(html, textStyle) {
  var _a, _b;
  if (!textStyle || !hasVisibleHtml(html)) {
    return html;
  }
  let result = html;
  if (textStyle.it === 1 /* TRUE */) {
    result = `<i>${result}</i>`;
  }
  if (((_a = textStyle.ul) == null ? void 0 : _a.s) === 1 /* TRUE */) {
    result = `<u>${result}</u>`;
  }
  if (((_b = textStyle.st) == null ? void 0 : _b.s) === 1 /* TRUE */) {
    result = `<s>${result}</s>`;
  }
  if (textStyle.bl === 1 /* TRUE */) {
    result = `<strong>${result}</strong>`;
  }
  return result;
}
function normalizeCssColor2(color) {
  const rgb = color.match(/^rgba?\(\s*(\d{1,3})\s*,\s*(\d{1,3})\s*,\s*(\d{1,3})(?:\s*,\s*[\d.]+)?\s*\)$/i);
  if (!rgb) {
    return color;
  }
  return `#${rgb.slice(1, 4).map((part) => Math.max(0, Math.min(255, Number(part))).toString(16).padStart(2, "0")).join("")}`;
}
function roundCssNumber2(value) {
  return Math.round(value * 100) / 100;
}
function getHeadingLevel(namedStyleType) {
  switch (namedStyleType) {
    case 2 /* TITLE */:
    case 4 /* HEADING_1 */:
      return 1;
    case 5 /* HEADING_2 */:
      return 2;
    case 6 /* HEADING_3 */:
      return 3;
    case 7 /* HEADING_4 */:
      return 4;
    case 8 /* HEADING_5 */:
      return 5;
    default:
      return null;
  }
}
function getTextAlign(horizontalAlign) {
  switch (horizontalAlign) {
    case 1 /* LEFT */:
      return "left";
    case 2 /* CENTER */:
      return "center";
    case 3 /* RIGHT */:
      return "right";
    case 4 /* JUSTIFIED */:
    case 5 /* BOTH */:
      return "justify";
    default:
      return null;
  }
}
function isOrderedListType(listType) {
  return listType.includes("ORDER") || listType === "ORDER_LIST" /* ORDER_LIST */;
}
function getListStyleType(doc, listType, level) {
  var _a, _b, _c, _d;
  const glyphType = (_d = (_c = (_b = (_a = doc.lists) == null ? void 0 : _a[listType]) == null ? void 0 : _b.nestingLevel) == null ? void 0 : _c[level]) == null ? void 0 : _d.glyphType;
  switch (glyphType) {
    case 3:
      return "upper-alpha";
    case 4:
      return "lower-alpha";
    case 5:
      return "upper-roman";
    case 6:
      return "lower-roman";
    default:
      return isOrderedListType(listType) ? "decimal" : null;
  }
}
function escapeHtml(value) {
  return value.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
}
function escapeInlineText(value) {
  return escapeHtml(stripDataStreamControlChars(value)).replace(/\r\n|\r|\n/g, "<br>");
}
function stripDataStreamControlChars(value) {
  return value.replace(/[\x00-\x08\x0B\x0C\x0E-\x1F]/g, "");
}
function hasVisibleHtml(html) {
  return /<(?:img|br)\b/i.test(html) || html.replace(/<[^>]+>/g, "").replace(/&nbsp;/gi, " ").trim().length > 0;
}
var UDMToHtmlService = class {
  convert(docList) {
    if (docList.length === 0) {
      throw new Error("The bodyList length at least to be 1");
    }
    let html = "";
    for (const doc of Tools.deepClone(docList)) {
      html += convertBodyToHtml(doc);
    }
    return html;
  }
};

// ../packages/docs-ui/src/services/clipboard/clipboard.service.ts
HtmlToUDMService.use(plugin_lark_default);
HtmlToUDMService.use(plugin_univer_default);
HtmlToUDMService.use(plugin_word_default);
function getTableClipboardBodySlice(body, range) {
  var _a;
  const { startOffset, endOffset, spanEntireTable, tableId } = range;
  if (startOffset == null || endOffset == null) {
    return { dataStream: "" };
  }
  if (spanEntireTable) {
    const tableRange = (_a = body.tables) == null ? void 0 : _a.find((table) => table.tableId === tableId);
    if (tableRange) {
      return getBodySlice(body, tableRange.startIndex, tableRange.endIndex, false, 0 /* copy */);
    }
  }
  return getTableCellContentClipboardBodySlice(body, startOffset, endOffset);
}
function getTableCellContentClipboardBodySlice(body, start, end) {
  var _a, _b, _c;
  const bodySlice = getBodySlice(body, start, end + 2);
  const dataStream = "" /* TABLE_START */ + "\x1B" /* TABLE_ROW_START */ + "" /* TABLE_CELL_START */ + bodySlice.dataStream + "" /* TABLE_CELL_END */ + "" /* TABLE_ROW_END */ + "" /* TABLE_END */;
  bodySlice.dataStream = dataStream;
  (_a = bodySlice.textRuns) == null ? void 0 : _a.forEach((textRun) => {
    const { st, ed } = textRun;
    textRun.st = st + 3;
    textRun.ed = ed + 3;
  });
  (_b = bodySlice.tables) == null ? void 0 : _b.forEach((table) => {
    const { startIndex, endIndex } = table;
    table.startIndex = startIndex + 3;
    table.endIndex = endIndex + 3;
  });
  (_c = bodySlice.paragraphs) == null ? void 0 : _c.forEach((paragraph) => {
    const { startIndex } = paragraph;
    paragraph.startIndex = startIndex + 3;
  });
  return bodySlice;
}
var IDocClipboardService = createIdentifier("doc.clipboard-service");
var DocClipboardService = class extends Disposable {
  constructor(_univerInstanceService, _logService, _commandService, _clipboardInterfaceService, _docSelectionManagerService) {
    super();
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_logService", _logService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_clipboardInterfaceService", _clipboardInterfaceService);
    __publicField(this, "_docSelectionManagerService", _docSelectionManagerService);
    __publicField(this, "_clipboardHooks", []);
    __publicField(this, "_htmlToUDM", new HtmlToUDMService());
    __publicField(this, "_umdToHtml", new UDMToHtmlService());
  }
  async copy(sliceType = 0 /* copy */, ranges) {
    var _a, _b;
    const { newSnapshotList = [], needCache = false, snapshot, ranges: allRanges } = (_a = this._getDocumentBodyInRanges(sliceType, ranges)) != null ? _a : {};
    if (newSnapshotList.length === 0 || snapshot == null) {
      return false;
    }
    try {
      const isCopyInHeaderFooter = !!((_b = allRanges == null ? void 0 : allRanges[0]) == null ? void 0 : _b.segmentId);
      this._setClipboardData(newSnapshotList, !isCopyInHeaderFooter && needCache);
    } catch (e) {
      this._logService.error("[DocClipboardService] copy failed", e);
      return false;
    }
    return true;
  }
  async cut(ranges) {
    return this._cut(ranges);
  }
  async paste(items) {
    const partDocData = await this._genDocDataFromClipboardItems(items);
    return this._paste(partDocData);
  }
  async legacyPaste(options) {
    let { html, internalJson, text, files } = options;
    const currentDocInstance = this._univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const docUnitId = (currentDocInstance == null ? void 0 : currentDocInstance.getUnitId()) || "";
    if (!html && !text && files.length) {
      html = await this._createImagePasteHtml(files);
    } else if (html && files.length) {
      html += await this._createImagePasteHtml(files);
    }
    html = await this._uploadBase64ImagesInHtml(html);
    if (!html && !text) {
      this._logService.warn("[DocClipboardController] html and text cannot be both empty!");
      return false;
    }
    const partDocData = this._genDocDataFromHtmlAndText(html, text, docUnitId, internalJson);
    if (docUnitId === DOCS_NORMAL_EDITOR_UNIT_ID_KEY) {
      if (text) {
        const textDocData = BuildTextUtils.transform.fromPlainText(text);
        return this._paste({ body: textDocData });
      } else {
        partDocData.body.textRuns = [];
      }
    }
    return this._paste(partDocData);
  }
  async _cut(ranges) {
    var _a, _b, _c, _d, _e;
    const textRanges = (_b = (_a = ranges == null ? void 0 : ranges.filter((range) => range.rangeType === "TEXT" /* TEXT */)) != null ? _a : this._docSelectionManagerService.getTextRanges()) != null ? _b : [];
    const rectRanges = (_d = (_c = ranges == null ? void 0 : ranges.filter((range) => range.rangeType === "RECT" /* RECT */)) != null ? _c : this._docSelectionManagerService.getRectRanges()) != null ? _d : [];
    const {
      segmentId,
      endOffset: activeEndOffset,
      style
    } = (_e = textRanges[0]) != null ? _e : {};
    if (segmentId == null) {
      this._logService.error("[DocClipboardController] segmentId is not existed");
    }
    if (textRanges.length === 0 && rectRanges.length === 0) {
      return false;
    }
    this.copy(1 /* cut */, ranges);
    try {
      let cursor = 0;
      if (rectRanges.length > 0) {
        cursor = getCursorWhenDelete(textRanges, rectRanges);
      } else if (activeEndOffset != null) {
        cursor = activeEndOffset;
        for (const range of textRanges) {
          const { startOffset, endOffset } = range;
          if (startOffset == null || endOffset == null) {
            continue;
          }
          if (endOffset <= activeEndOffset) {
            cursor -= endOffset - startOffset;
          }
        }
      }
      const newTextRanges = [
        {
          startOffset: cursor,
          endOffset: cursor,
          style
        }
      ];
      return this._commandService.executeCommand(CutContentCommand.id, {
        segmentId,
        textRanges: newTextRanges,
        rectRanges,
        selections: textRanges
      });
    } catch (_e2) {
      this._logService.error("[DocClipboardController] cut content failed");
      return false;
    }
  }
  async _paste(docData) {
    var _a, _b, _c;
    const { body: _body } = docData;
    if (_body == null) {
      return false;
    }
    let body = normalizeBody(_body);
    const unitId = (_a = this._univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */)) == null ? void 0 : _a.getUnitId();
    if (!unitId) {
      return false;
    }
    this._clipboardHooks.forEach((hook) => {
      if (hook.onBeforePaste) {
        body = hook.onBeforePaste(body);
      }
    });
    body.customRanges = (_b = body.customRanges) == null ? void 0 : _b.map(BuildTextUtils.customRange.copyCustomRange);
    (_c = body.paragraphs) == null ? void 0 : _c.forEach((copy) => {
      var _a2;
      if ((_a2 = copy.paragraphStyle) == null ? void 0 : _a2.headingId) {
        copy.paragraphStyle.headingId = generateRandomId(6);
      }
    });
    const activeRange = this._docSelectionManagerService.getActiveTextRange();
    const { segmentId, endOffset: activeEndOffset, style } = activeRange || {};
    const ranges = this._docSelectionManagerService.getTextRanges();
    if (segmentId == null) {
      this._logService.error("[DocClipboardController] segmentId does not exist!");
    }
    if (activeEndOffset == null || ranges == null) {
      return false;
    }
    try {
      let cursor = activeEndOffset;
      for (const range of ranges) {
        const { startOffset, endOffset } = range;
        if (startOffset == null || endOffset == null) {
          continue;
        }
        if (endOffset <= activeEndOffset) {
          cursor += body.dataStream.length - (endOffset - startOffset);
        }
      }
      const textRanges = [
        {
          startOffset: cursor,
          endOffset: cursor,
          style
        }
      ];
      return this._commandService.executeCommand(InnerPasteCommand.id, {
        doc: {
          ...docData,
          body
        },
        segmentId,
        textRanges
      });
    } catch (_) {
      this._logService.error("[DocClipboardController]", "clipboard is empty.");
      return false;
    }
  }
  async _setClipboardData(documentList, needCache = true) {
    var _a;
    const copyId = generateRandomId(6);
    const text = (documentList.length > 1 ? documentList.map((doc) => {
      var _a2;
      return ((_a2 = doc.body) == null ? void 0 : _a2.dataStream) || "";
    }).join("\n") : ((_a = documentList[0].body) == null ? void 0 : _a.dataStream) || "").replaceAll("" /* TABLE_START */, "").replaceAll("" /* TABLE_END */, "").replaceAll("\x1B" /* TABLE_ROW_START */, "").replaceAll("" /* TABLE_ROW_END */, "").replaceAll("" /* TABLE_CELL_START */, "").replaceAll("" /* TABLE_CELL_END */, "").replaceAll("" /* BLOCK_START */, "").replaceAll("" /* BLOCK_END */, "").replaceAll("\r\n", " ").replace(/[\x00-\x08\x0B\x0C\x0E-\x1F]/g, "");
    let html = this._umdToHtml.convert(documentList);
    let internalJson = "";
    let internalDocData = null;
    if (documentList.length === 1 && needCache) {
      html = html.replace(/(<[a-z]+)/, (_p0, p1) => `${p1} data-copy-id="${copyId}"`);
      const doc = documentList[0];
      const cache = createInternalClipboardDocData(doc);
      copyContentCache.set(copyId, cache);
      internalDocData = cache;
    } else {
      internalDocData = createInternalClipboardDocDataList(documentList);
    }
    if (internalDocData) {
      internalJson = createInternalClipboardFragment(internalDocData);
      html = embedInternalClipboardFragment(html, internalJson);
    }
    html = wrapClipboardHtml(html);
    return this._clipboardInterfaceService.write(text, html, internalJson ? { [DOC_INTERNAL_FRAGMENT_MIME]: internalJson } : void 0);
  }
  addClipboardHook(hook) {
    this._clipboardHooks.push(hook);
    return toDisposable(() => {
      const index = this._clipboardHooks.indexOf(hook);
      if (index > -1) {
        this._clipboardHooks.splice(index, 1);
      }
    });
  }
  _getDocumentBodyInRanges(sliceType, ranges) {
    var _a;
    const docDataModel = this._univerInstanceService.getCurrentUniverDocInstance();
    const allRanges = ranges != null ? ranges : this._docSelectionManagerService.getDocRanges();
    const results = [];
    let needCache = true;
    if (docDataModel == null || allRanges.length === 0) {
      return;
    }
    const segmentId = allRanges[0].segmentId;
    const body = (_a = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId)) == null ? void 0 : _a.getBody();
    const snapshot = docDataModel.getSnapshot();
    if (body == null) {
      return;
    }
    for (const range of allRanges) {
      const { startOffset, endOffset, collapsed, rangeType } = range;
      if (collapsed || startOffset == null || endOffset == null) {
        continue;
      }
      if (rangeType === "RECT" /* RECT */) {
        needCache = false;
        const bodySlice = getTableClipboardBodySlice(body, range);
        results.push(bodySlice);
        continue;
      }
      const deleteRange = { startOffset, endOffset, collapsed };
      const docBody = docDataModel.getSelfOrHeaderFooterModel(segmentId).sliceBody(deleteRange.startOffset, deleteRange.endOffset, sliceType);
      if (docBody == null) {
        continue;
      }
      results.push(docBody);
    }
    return {
      newSnapshotList: results.map((e) => ({ ...snapshot, body: e })),
      needCache,
      snapshot,
      ranges: allRanges
    };
  }
  async _genDocDataFromClipboardItems(items) {
    var _a;
    try {
      let html = "";
      let text = "";
      let internalJson = "";
      const files = [];
      for (const clipboardItem of items) {
        for (const type2 of clipboardItem.types) {
          switch (type2) {
            case DOC_INTERNAL_FRAGMENT_MIME: {
              internalJson = await clipboardItem.getType(type2).then((blob) => blob && blob.text());
              break;
            }
            case PLAIN_TEXT_CLIPBOARD_MIME_TYPE: {
              text = await clipboardItem.getType(type2).then((blob) => blob && blob.text());
              break;
            }
            case HTML_CLIPBOARD_MIME_TYPE: {
              html = await clipboardItem.getType(type2).then((blob) => blob && blob.text());
              break;
            }
            case FILE__BMP_CLIPBOARD_MIME_TYPE:
            case FILE__JPEG_CLIPBOARD_MIME_TYPE:
            case FILE__WEBP_CLIPBOARD_MIME_TYPE:
            case FILE_PNG_CLIPBOARD_MIME_TYPE: {
              const blob = await clipboardItem.getType(type2);
              const file = new File([blob], `pasted_image.${type2.split("/")[1]}`, { type: type2 });
              files.push(file);
              break;
            }
          }
        }
      }
      if (!html && !text && files.length) {
        html = await this._createImagePasteHtml(files);
      }
      html = (_a = await this._uploadBase64ImagesInHtml(html)) != null ? _a : "";
      return this._genDocDataFromHtmlAndText(html, text, void 0, internalJson);
    } catch (e) {
      return Promise.reject(e);
    }
  }
  _genDocDataFromHtmlAndText(html, text, _unitId, internalJson) {
    var _a;
    const internalDocData = (_a = parseInternalClipboardFragment(internalJson)) != null ? _a : extractInternalClipboardFragmentFromHtml(html);
    if (internalDocData == null ? void 0 : internalDocData.body) {
      return internalDocData;
    }
    if (!html) {
      if (text) {
        const body = BuildTextUtils.transform.fromPlainText(text);
        return { body };
      } else {
        throw new Error("[DocClipboardService] html and text cannot be both empty!");
      }
    }
    const copyId = extractId(html);
    if (copyId) {
      const copyCache = copyContentCache.get(copyId);
      if (copyCache) {
        return copyCache;
      }
    }
    if (!_unitId) {
      const currentDocInstance = this._univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
      const docUnitId = (currentDocInstance == null ? void 0 : currentDocInstance.getUnitId()) || "";
      _unitId = docUnitId;
    }
    const doc = this._htmlToUDM.convert(html, { unitId: _unitId });
    if (copyId) {
      copyContentCache.set(copyId, doc);
    }
    return doc;
  }
  async _uploadBase64ImagesInHtml(html) {
    var _a;
    if (!html || !html.includes("data:image/")) {
      return html;
    }
    const onBeforePasteImage = (_a = this._clipboardHooks.find((e) => e.onBeforePasteImage)) == null ? void 0 : _a.onBeforePasteImage;
    if (!onBeforePasteImage) {
      return html;
    }
    const doc = new DOMParser().parseFromString(html, "text/html");
    const images = Array.from(doc.querySelectorAll('img[src^="data:image/"]'));
    if (images.length === 0) {
      return html;
    }
    await Promise.all(images.map(async (image, index) => {
      if (image.dataset.imageSourceType && image.dataset.imageSourceType !== "BASE64" /* BASE64 */) {
        return;
      }
      const file = dataUrlToFile(image.getAttribute("src") || image.src, `pasted_image_${index}`);
      const uploaded = await onBeforePasteImage(file);
      if (!uploaded) {
        return;
      }
      image.dataset.imageSourceType = uploaded.imageSourceType;
      if (uploaded.imageSourceType === "UUID" /* UUID */) {
        image.dataset.source = uploaded.source;
        image.setAttribute("src", uploaded.source);
      } else {
        image.removeAttribute("data-source");
        image.setAttribute("src", uploaded.source);
      }
    }));
    return doc.documentElement.outerHTML;
  }
  async _createImagePasteHtml(files) {
    var _a, _b;
    const doc = {
      id: "",
      documentStyle: {},
      body: {
        dataStream: "",
        customBlocks: []
      },
      drawings: {}
    };
    const fileToBase64 = async (file) => {
      const reader = new FileReader();
      return new Promise((res) => {
        reader.onloadend = function() {
          res({
            source: reader.result,
            imageSourceType: "BASE64" /* BASE64 */
          });
        };
        reader.readAsDataURL(file);
      });
    };
    const getImageSize2 = (base64) => {
      const img = new Image();
      const maxWidth = 500;
      return new Promise((resolve) => {
        img.src = typeof base64 === "string" ? base64 : URL.createObjectURL(base64);
        img.onload = () => {
          const width = Math.min(maxWidth, img.naturalWidth);
          const scale = img.naturalHeight / img.naturalWidth;
          resolve({ width, height: width * scale });
        };
      });
    };
    const onBeforePasteImage = (_b = (_a = this._clipboardHooks.find((e) => e.onBeforePasteImage)) == null ? void 0 : _a.onBeforePasteImage) != null ? _b : fileToBase64;
    await Promise.all(files.map(async (file, index) => {
      var _a2;
      const image = await onBeforePasteImage(file);
      if (!image) {
        return Promise.resolve();
      }
      const { width = 100, height = 100 } = await getImageSize2(file);
      const itemId = `paste_image_id_${index}`;
      const body = doc.body;
      const drawings = doc.drawings;
      body.dataStream += "\b";
      (_a2 = body.customBlocks) == null ? void 0 : _a2.push({ startIndex: index, blockId: itemId });
      drawings[itemId] = {
        drawingId: itemId,
        unitId: "",
        subUnitId: "",
        imageSourceType: image.imageSourceType,
        title: "",
        source: image.source,
        description: "",
        layoutType: 0 /* INLINE */,
        drawingType: 0 /* DRAWING_IMAGE */,
        transform: {
          width,
          height,
          angle: 0
        },
        docTransform: {
          angle: 0,
          size: { width, height },
          positionH: { relativeFrom: 2 /* CHARACTER */, posOffset: 0 },
          positionV: { relativeFrom: 2 /* LINE */, posOffset: 0 }
        }
      };
    }));
    const html = this._umdToHtml.convert([doc]);
    return html;
  }
};
DocClipboardService = __decorateClass([
  __decorateParam(0, IUniverInstanceService),
  __decorateParam(1, ILogService),
  __decorateParam(2, ICommandService),
  __decorateParam(3, IClipboardInterfaceService),
  __decorateParam(4, Inject(DocSelectionManagerService))
], DocClipboardService);
function dataUrlToFile(dataUrl, fallbackName) {
  const match = /^data:([^;,]+)(;base64)?,(.*)$/i.exec(dataUrl);
  if (!match) {
    throw new Error("[DocClipboardService] invalid image data url.");
  }
  const [, mimeType, base64Marker, payload] = match;
  const bytes = base64Marker ? Uint8Array.from(atob(payload), (char) => char.charCodeAt(0)) : new TextEncoder().encode(decodeURIComponent(payload));
  const extension = mimeType.split("/")[1] || "png";
  return new File([bytes], `${fallbackName}.${extension}`, { type: mimeType });
}

// ../packages/docs-ui/src/commands/commands/clipboard.command.ts
function whenDocOrEditor(contextService) {
  return contextService.getContextValue(FOCUSING_DOC) || contextService.getContextValue(EDITOR_ACTIVATED);
}
var DOC_CLIPBOARD_PRIORITY = 999;
var DocCopyCommand = {
  id: CopyCommand.id,
  name: "doc.command.copy",
  type: 0 /* COMMAND */,
  multi: true,
  priority: DOC_CLIPBOARD_PRIORITY,
  preconditions: whenDocOrEditor,
  handler: async (accessor) => {
    const docClipboardService = accessor.get(IDocClipboardService);
    return docClipboardService.copy();
  }
};
var DocCopyCurrentParagraphCommand = {
  id: "doc.command.copy-current-paragraph",
  type: 0 /* COMMAND */,
  handler: async (accessor) => {
    const docClipboardService = accessor.get(IDocClipboardService);
    const paragraph = getCurrentParagraph(accessor);
    if (!paragraph) {
      return false;
    }
    return docClipboardService.copy(
      0 /* copy */,
      [{
        startOffset: paragraph.paragraphStart,
        endOffset: paragraph.paragraphEnd + 1,
        collapsed: false
      }]
    );
  }
};
var DocCutCommand = {
  id: CutCommand.id,
  name: "doc.command.cut",
  type: 0 /* COMMAND */,
  multi: true,
  priority: DOC_CLIPBOARD_PRIORITY,
  preconditions: whenDocOrEditor,
  handler: async (accessor) => {
    const docClipboardService = accessor.get(IDocClipboardService);
    return docClipboardService.cut();
  }
};
var DocCutCurrentParagraphCommand = {
  id: "doc.command.cut-current-paragraph",
  type: 0 /* COMMAND */,
  handler: async (accessor) => {
    const docClipboardService = accessor.get(IDocClipboardService);
    const paragraph = getCurrentParagraph(accessor);
    if (!paragraph) {
      return false;
    }
    return docClipboardService.cut(
      [{
        startOffset: paragraph.paragraphStart,
        endOffset: paragraph.paragraphEnd + 1,
        collapsed: false,
        rangeType: "TEXT" /* TEXT */
      }]
    );
  }
};
var DocPasteCommand = {
  id: PasteCommand.id,
  name: "doc.command.paste",
  type: 0 /* COMMAND */,
  multi: true,
  priority: DOC_CLIPBOARD_PRIORITY,
  preconditions: whenDocOrEditor,
  handler: async (accessor) => {
    const docClipboardService = accessor.get(IDocClipboardService);
    const clipboardInterfaceService = accessor.get(IClipboardInterfaceService);
    const clipboardItems = await clipboardInterfaceService.read();
    if (clipboardItems.length === 0) {
      return false;
    }
    return docClipboardService.paste(clipboardItems);
  }
};

// ../packages/docs-ui/src/commands/commands/doc-block-move.command.ts
var MoveDocBlockCommand = {
  id: "doc.command.move-block",
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    if (!params) {
      return false;
    }
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const doc = params.unitId ? univerInstanceService.getUnit(params.unitId, 1 /* UNIVER_DOC */) : univerInstanceService.getCurrentUniverDocInstance();
    if (!doc) {
      return false;
    }
    const previousDocumentData = doc.getSnapshot();
    const { nextDocumentData, movedRange } = buildMoveDocBlockActions({
      documentData: previousDocumentData,
      sourceRange: params.sourceRange,
      targetOffset: params.targetOffset
    });
    const actions = buildReplaceDocumentBodyActions(previousDocumentData, nextDocumentData);
    if (!actions) {
      return false;
    }
    const textRanges = [{
      startOffset: movedRange.startOffset,
      endOffset: movedRange.endOffset,
      collapsed: false
    }];
    return Boolean(commandService.syncExecuteCommand(RichTextEditingMutation.id, {
      unitId: doc.getUnitId(),
      actions,
      textRanges
    }));
  }
};
function buildMoveDocBlockActions(params) {
  const nextDocumentData = Tools.deepClone(params.documentData);
  const body = nextDocumentData.body;
  if (!(body == null ? void 0 : body.dataStream)) {
    return {
      nextDocumentData,
      movedRange: params.sourceRange
    };
  }
  const dataStreamLength = body.dataStream.length;
  const startOffset = clamp(params.sourceRange.startOffset, 0, dataStreamLength);
  const endOffset = clamp(params.sourceRange.endOffset, startOffset, dataStreamLength);
  const targetOffset = clamp(params.targetOffset, 0, dataStreamLength);
  if (startOffset === endOffset || targetOffset >= startOffset && targetOffset <= endOffset) {
    return {
      nextDocumentData,
      movedRange: {
        startOffset,
        endOffset
      }
    };
  }
  const movingText = body.dataStream.slice(startOffset, endOffset);
  const moveLength = movingText.length;
  const insertOffset = targetOffset > endOffset ? targetOffset - moveLength : targetOffset;
  const withoutMovingText = body.dataStream.slice(0, startOffset) + body.dataStream.slice(endOffset);
  body.dataStream = withoutMovingText.slice(0, insertOffset) + movingText + withoutMovingText.slice(insertOffset);
  remapBodyIndexesAfterMove(body, startOffset, endOffset, targetOffset, insertOffset, moveLength);
  return {
    nextDocumentData,
    movedRange: {
      startOffset: insertOffset,
      endOffset: insertOffset + moveLength
    }
  };
}
function buildReplaceDocumentBodyActions(previousDocumentData, nextDocumentData) {
  const jsonX = JSONX.getInstance();
  const previousBody = previousDocumentData.body;
  const nextBody = nextDocumentData.body;
  if (!previousBody || !nextBody) {
    return null;
  }
  const rawActions = [
    jsonX.replaceOp(["body", "dataStream"], previousBody.dataStream, nextBody.dataStream),
    jsonX.replaceOp(["body", "paragraphs"], previousBody.paragraphs, nextBody.paragraphs),
    jsonX.replaceOp(["body", "sectionBreaks"], previousBody.sectionBreaks, nextBody.sectionBreaks),
    jsonX.replaceOp(["body", "tables"], previousBody.tables, nextBody.tables),
    jsonX.replaceOp(["body", "customBlocks"], previousBody.customBlocks, nextBody.customBlocks),
    jsonX.replaceOp(["body", "blockRanges"], previousBody.blockRanges, nextBody.blockRanges),
    jsonX.replaceOp(["body", "customRanges"], previousBody.customRanges, nextBody.customRanges),
    jsonX.replaceOp(["body", "customDecorations"], previousBody.customDecorations, nextBody.customDecorations),
    jsonX.replaceOp(["body", "textRuns"], previousBody.textRuns, nextBody.textRuns)
  ].filter(Boolean);
  return rawActions.reduce((acc, cur) => JSONX.compose(acc, cur), null);
}
function remapBodyIndexesAfterMove(body, startOffset, endOffset, targetOffset, insertOffset, moveLength) {
  var _a, _b, _c, _d, _e, _f, _g, _h;
  body.paragraphs = (_a = body.paragraphs) == null ? void 0 : _a.map((paragraph) => ({
    ...paragraph,
    startIndex: remapIndexAfterMove(paragraph.startIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength)
  })).sort((left, right) => left.startIndex - right.startIndex);
  body.sectionBreaks = (_b = body.sectionBreaks) == null ? void 0 : _b.map((sectionBreak) => ({
    ...sectionBreak,
    startIndex: remapIndexAfterMove(sectionBreak.startIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength)
  })).sort((left, right) => left.startIndex - right.startIndex);
  body.customBlocks = (_c = body.customBlocks) == null ? void 0 : _c.map((customBlock) => ({
    ...customBlock,
    startIndex: remapIndexAfterMove(customBlock.startIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength)
  })).sort((left, right) => left.startIndex - right.startIndex);
  body.tables = (_d = body.tables) == null ? void 0 : _d.map((table) => remapExclusiveRange(table, startOffset, endOffset, targetOffset, insertOffset, moveLength)).sort((left, right) => left.startIndex - right.startIndex);
  body.blockRanges = (_e = body.blockRanges) == null ? void 0 : _e.map((blockRange) => remapInclusiveRange(blockRange, startOffset, endOffset, targetOffset, insertOffset, moveLength)).sort((left, right) => left.startIndex - right.startIndex);
  body.customRanges = (_f = body.customRanges) == null ? void 0 : _f.map((customRange) => remapInclusiveRange(customRange, startOffset, endOffset, targetOffset, insertOffset, moveLength)).sort((left, right) => left.startIndex - right.startIndex);
  body.customDecorations = (_g = body.customDecorations) == null ? void 0 : _g.map((customDecoration) => remapInclusiveRange(customDecoration, startOffset, endOffset, targetOffset, insertOffset, moveLength)).sort((left, right) => left.startIndex - right.startIndex);
  body.textRuns = (_h = body.textRuns) == null ? void 0 : _h.map((textRun) => {
    const remapped = remapExclusiveRange({ startIndex: textRun.st, endIndex: textRun.ed }, startOffset, endOffset, targetOffset, insertOffset, moveLength);
    return {
      ...textRun,
      st: remapped.startIndex,
      ed: remapped.endIndex
    };
  }).sort((left, right) => left.st - right.st);
}
function remapExclusiveRange(range, startOffset, endOffset, targetOffset, insertOffset, moveLength) {
  if (range.startIndex >= startOffset && range.endIndex <= endOffset) {
    return {
      ...range,
      startIndex: insertOffset + range.startIndex - startOffset,
      endIndex: insertOffset + range.endIndex - startOffset
    };
  }
  return {
    ...range,
    startIndex: remapIndexAfterMove(range.startIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength),
    endIndex: remapIndexAfterMove(range.endIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength)
  };
}
function remapInclusiveRange(range, startOffset, endOffset, targetOffset, insertOffset, moveLength) {
  if (range.startIndex >= startOffset && range.endIndex < endOffset) {
    return {
      ...range,
      startIndex: insertOffset + range.startIndex - startOffset,
      endIndex: insertOffset + range.endIndex - startOffset
    };
  }
  return {
    ...range,
    startIndex: remapIndexAfterMove(range.startIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength),
    endIndex: remapIndexAfterMove(range.endIndex, startOffset, endOffset, targetOffset, insertOffset, moveLength)
  };
}
function remapIndexAfterMove(index, startOffset, endOffset, targetOffset, insertOffset, moveLength) {
  if (index >= startOffset && index < endOffset) {
    return insertOffset + index - startOffset;
  }
  if (targetOffset < startOffset && index >= targetOffset && index < startOffset) {
    return index + moveLength;
  }
  if (targetOffset > endOffset && index >= endOffset && index < targetOffset) {
    return index - moveLength;
  }
  return index;
}
function clamp(value, min, max) {
  return Math.min(Math.max(value, min), max);
}

// ../packages/docs-ui/src/basics/selection.ts
function findFirstCursorOffset(snapshot) {
  var _a;
  const { dataStream } = (_a = snapshot.body) != null ? _a : {};
  const EXCLUDE_TOKENS = [
    "" /* TABLE_START */,
    "" /* TABLE_CELL_END */,
    "" /* TABLE_CELL_START */,
    "" /* TABLE_END */,
    "" /* TABLE_ROW_END */,
    "\x1B" /* TABLE_ROW_START */,
    "\v" /* COLUMN_BREAK */,
    "\f" /* PAGE_BREAK */,
    "	" /* TAB */,
    "\0" /* DOCS_END */,
    "\b" /* CUSTOM_BLOCK */
  ];
  if (typeof dataStream === "string") {
    for (let i = 0; i < dataStream.length; i++) {
      const char = dataStream[i];
      if (!EXCLUDE_TOKENS.includes(char)) {
        return i;
      }
    }
  }
  return 0;
}

// ../packages/docs-ui/src/basics/docs-view-key.ts
var DOCS_COMPONENT_BACKGROUND_LAYER_INDEX = 0;
var DOCS_COMPONENT_MAIN_LAYER_INDEX = 2;
var DOCS_COMPONENT_HEADER_LAYER_INDEX = 4;
var DOCS_COMPONENT_DEFAULT_Z_INDEX = 10;

// ../packages/docs-ui/src/basics/component-tools.ts
function neoGetDocObject(renderContext) {
  const { mainComponent, scene, engine, components } = renderContext;
  const document2 = mainComponent;
  const docBackground = components.get("__Document_Render_Background__" /* BACKGROUND */);
  return {
    document: document2,
    docBackground,
    scene,
    engine
  };
}
function getDocObject(univerInstanceService, renderManagerService) {
  const documentModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
  if (!documentModel) {
    return null;
  }
  const unitId = documentModel.getUnitId();
  const currentRender = renderManagerService.getRenderById(unitId);
  if (currentRender == null) {
    return;
  }
  const { mainComponent, scene, engine, components } = currentRender;
  const document2 = mainComponent;
  const docBackground = components.get("__Document_Render_Background__" /* BACKGROUND */);
  return {
    document: document2,
    docBackground,
    scene,
    engine
  };
}

// ../packages/docs-ui/src/commands/commands/replace-content.command.ts
var ReplaceSnapshotCommand = {
  id: "doc.command-replace-snapshot",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function, complexity
  handler: (accessor, params) => {
    const { unitId, snapshot, textRanges, segmentId = "", options } = params;
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const docDataModel = univerInstanceService.getUnit(unitId, 1 /* UNIVER_DOC */);
    const prevSnapshot = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getSnapshot();
    if (docDataModel == null || prevSnapshot == null) {
      return false;
    }
    const { body, tableSource, footers, headers, lists, drawings, drawingsOrder, documentStyle } = Tools.deepClone(snapshot);
    const {
      body: prevBody,
      tableSource: prevTableSource,
      footers: prevFooters,
      headers: prevHeaders,
      lists: prevLists,
      drawings: prevDrawings,
      drawingsOrder: prevDrawingsOrder,
      documentStyle: prevDocumentStyle
    } = prevSnapshot;
    if (body == null || prevBody == null) {
      return false;
    }
    if (Tools.diffValue(body, prevBody) && textRanges) {
      docSelectionManagerService.replaceDocRanges(textRanges, {
        unitId,
        subUnitId: unitId
      }, false);
      return true;
    }
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges
      }
    };
    if (options) {
      doMutation.params.options = options;
    }
    const rawActions = [];
    const jsonX = JSONX.getInstance();
    if (!Tools.diffValue(prevDocumentStyle, documentStyle)) {
      const actions = jsonX.replaceOp(["documentStyle"], prevDocumentStyle, documentStyle);
      if (actions != null) {
        rawActions.push(actions);
      }
    }
    if (!Tools.diffValue(body, prevBody)) {
      const actions = jsonX.replaceOp(["body"], prevBody, body);
      if (actions != null) {
        rawActions.push(actions);
      }
    }
    if (!Tools.diffValue(tableSource, prevTableSource)) {
      const actions = jsonX.replaceOp(["tableSource"], prevTableSource, tableSource);
      if (actions != null) {
        rawActions.push(actions);
      }
    }
    if (!Tools.diffValue(footers, prevFooters)) {
      const actions = jsonX.replaceOp(["footers"], prevFooters, footers);
      if (actions != null) {
        rawActions.push(actions);
      }
    }
    if (!Tools.diffValue(headers, prevHeaders)) {
      const actions = jsonX.replaceOp(["headers"], prevHeaders, headers);
      if (actions != null) {
        rawActions.push(actions);
      }
    }
    if (!Tools.diffValue(lists, prevLists)) {
      const actions = jsonX.replaceOp(["lists"], prevLists, lists);
      if (actions != null) {
        rawActions.push(actions);
      }
    }
    if (!Tools.diffValue(drawings, prevDrawings)) {
      const actions = jsonX.replaceOp(["drawings"], prevDrawings, drawings);
      if (actions != null) {
        rawActions.push(actions);
      }
    }
    if (!Tools.diffValue(drawingsOrder, prevDrawingsOrder)) {
      const actions = jsonX.replaceOp(["drawingsOrder"], prevDrawingsOrder, drawingsOrder);
      if (actions != null) {
        rawActions.push(actions);
      }
    }
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var ReplaceContentCommand = {
  id: "doc.command-replace-content",
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const { unitId, body, textRanges, segmentId = "", options } = params;
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const docDataModel = univerInstanceService.getUnit(unitId, 1 /* UNIVER_DOC */);
    const prevBody = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getSnapshot().body;
    if (docDataModel == null || prevBody == null) {
      return false;
    }
    const doMutation = getMutationParams(unitId, segmentId, docDataModel, prevBody, body);
    doMutation.params.textRanges = textRanges;
    if (options) {
      doMutation.params.options = options;
    }
    if (doMutation.params.actions == null && textRanges) {
      docSelectionManagerService.replaceDocRanges(textRanges, {
        unitId,
        subUnitId: unitId
      }, false);
      return true;
    }
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var CoverContentCommand = {
  id: "doc.command-cover-content",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    const { unitId, body, segmentId = "", textRanges } = params;
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const undoRedoService = accessor.get(IUndoRedoService);
    const docDatModel = univerInstanceService.getUniverDocInstance(unitId);
    const prevBody = docDatModel == null ? void 0 : docDatModel.getSnapshot().body;
    if (docDatModel == null || prevBody == null) {
      return false;
    }
    const doMutation = getMutationParams(unitId, segmentId, docDatModel, prevBody, body);
    doMutation.params.noNeedSetTextRange = true;
    doMutation.params.noHistory = true;
    doMutation.params.textRanges = textRanges;
    commandService.syncExecuteCommand(
      doMutation.id,
      doMutation.params
    );
    undoRedoService.clearUndoRedo(unitId);
    return true;
  }
};
function getMutationActions(segmentId, docDatModel, prevBody, body) {
  const textX = new TextX();
  const jsonX = JSONX.getInstance();
  const deleteLen = (prevBody == null ? void 0 : prevBody.dataStream.length) - 2;
  if (deleteLen > 0) {
    textX.push({
      t: "d" /* DELETE */,
      len: deleteLen
    });
  }
  if (body.dataStream.length > 0) {
    textX.push({
      t: "i" /* INSERT */,
      body,
      len: body.dataStream.length
    });
  }
  const path = getRichTextEditPath2(docDatModel, segmentId);
  return jsonX.editOp(textX.serialize(), path);
}
function getMutationParams(unitId, segmentId, docDatModel, prevBody, body) {
  const doMutation = {
    id: RichTextEditingMutation.id,
    params: {
      unitId,
      actions: [],
      textRanges: []
    }
  };
  const actions = getMutationActions(segmentId, docDatModel, prevBody, body);
  doMutation.params.actions = actions;
  return doMutation;
}
var ReplaceSelectionCommand = {
  id: "doc.command.replace-selection",
  type: 0 /* COMMAND */,
  handler(accessor, params) {
    var _a;
    if (!params) {
      return false;
    }
    const commandService = accessor.get(ICommandService);
    const { unitId, body: insertBody, textRanges } = params;
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const docDataModel = univerInstanceService.getUnit(unitId);
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    if (!docDataModel) {
      return false;
    }
    const body = docDataModel.getBody();
    const selection = (_a = params.selection) != null ? _a : docSelectionManagerService.getActiveTextRange();
    if (!selection || !body) {
      return false;
    }
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges,
        debounce: true
      }
    };
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    textX.push(...BuildTextUtils.selection.delete([selection], body, 0, insertBody));
    doMutation.params.actions = jsonX.editOp(textX.serialize());
    return commandService.syncExecuteCommand(doMutation.id, doMutation.params);
  }
};
var ReplaceTextRunsCommand = {
  id: "doc.command.replace-text-runs",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    const { unitId, body, textRanges, segmentId = "", options } = params;
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const themeService = accessor.get(ThemeService);
    const docDataModel = univerInstanceService.getUnit(unitId, 1 /* UNIVER_DOC */);
    const prevBody = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getSnapshot().body;
    if (docDataModel == null || prevBody == null) {
      return false;
    }
    const textX = BuildTextUtils.selection.replaceTextRuns({
      doc: docDataModel,
      body,
      selection: {
        startOffset: 0,
        endOffset: prevBody.dataStream.length - 2,
        collapsed: false
      },
      themeService
    });
    if (!textX) {
      return false;
    }
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges,
        noHistory: true
      }
    };
    const jsonX = JSONX.getInstance();
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    doMutation.params.textRanges = textRanges;
    if (options) {
      doMutation.params.options = options;
    }
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};

// ../packages/docs-ui/src/services/selection/convert-text-range.ts
var NodePositionType = /* @__PURE__ */ ((NodePositionType2) => {
  NodePositionType2[NodePositionType2["page"] = 0] = "page";
  NodePositionType2[NodePositionType2["section"] = 1] = "section";
  NodePositionType2[NodePositionType2["column"] = 2] = "column";
  NodePositionType2[NodePositionType2["line"] = 3] = "line";
  NodePositionType2[NodePositionType2["divide"] = 4] = "divide";
  NodePositionType2[NodePositionType2["glyph"] = 5] = "glyph";
  return NodePositionType2;
})(NodePositionType || {});
var NodePositionMap = {
  page: 0,
  section: 1,
  column: 2,
  line: 3,
  divide: 4,
  glyph: 5
};
function compareNodePositionLogic(pos1, pos2) {
  if (pos1.page > pos2.page) {
    return false;
  }
  if (pos1.page < pos2.page) {
    return true;
  }
  if (pos1.section > pos2.section) {
    return false;
  }
  if (pos1.section < pos2.section) {
    return true;
  }
  if (pos1.column > pos2.column) {
    return false;
  }
  if (pos1.column < pos2.column) {
    return true;
  }
  if (pos1.line > pos2.line) {
    return false;
  }
  if (pos1.line < pos2.line) {
    return true;
  }
  if (pos1.divide > pos2.divide) {
    return false;
  }
  if (pos1.divide < pos2.divide) {
    return true;
  }
  if (pos1.glyph > pos2.glyph) {
    return false;
  }
  if (pos1.glyph < pos2.glyph) {
    return true;
  }
  return true;
}
function compareNodePosition(pos1, pos2) {
  const compare = compareNodePositionLogic(pos1, pos2);
  if (compare) {
    return {
      start: pos1,
      end: pos2
    };
  }
  return {
    start: pos2,
    end: pos1
  };
}
function getOneTextSelectionRange(rangeList) {
  const rangeCount = rangeList.length;
  if (rangeCount === 0) {
    return;
  }
  const firstCursor = rangeList[0];
  const lastCursor = rangeList[rangeCount - 1];
  const collapsed = rangeList.length === 1 && firstCursor.collapsed;
  return {
    startOffset: firstCursor.startOffset,
    endOffset: lastCursor.endOffset,
    collapsed
  };
}
function getOffsetInDivide(glyphGroup, startGlyphIndex, endGlyphIndex, st) {
  let startOffset = st;
  let endOffset = st;
  for (let i = 0; i < glyphGroup.length; i++) {
    const glyph = glyphGroup[i];
    const contentLength = glyph.count;
    if (i < startGlyphIndex) {
      startOffset += contentLength;
    }
    if (i < endGlyphIndex) {
      endOffset += contentLength;
    }
  }
  return {
    startOffset,
    endOffset
  };
}
function pushToPoints(position) {
  const { startX, startY, endX, endY } = position;
  const points = [];
  points.push({
    x: startX,
    y: startY
  });
  points.push({
    x: endX,
    y: startY
  });
  points.push({
    x: endX,
    y: endY
  });
  points.push({
    x: startX,
    y: endY
  });
  points.push({
    x: startX,
    y: startY
  });
  return points;
}
var NodePositionConvertToCursor = class {
  constructor(_documentOffsetConfig, _docSkeleton) {
    __publicField(this, "_documentOffsetConfig", _documentOffsetConfig);
    __publicField(this, "_docSkeleton", _docSkeleton);
    __publicField(this, "_liquid", new Liquid());
    __publicField(this, "_horizontalClip", null);
    __publicField(this, "_currentStartState", {
      page: 0 /* NORMAL */,
      section: 0 /* NORMAL */,
      column: 0 /* NORMAL */,
      line: 0 /* NORMAL */,
      divide: 0 /* NORMAL */,
      glyph: 0 /* NORMAL */
    });
    __publicField(this, "_currentEndState", {
      page: 0 /* NORMAL */,
      section: 0 /* NORMAL */,
      column: 0 /* NORMAL */,
      line: 0 /* NORMAL */,
      divide: 0 /* NORMAL */,
      glyph: 0 /* NORMAL */
    });
  }
  // eslint-disable-next-line max-lines-per-function
  getRangePointData(startOrigin, endOrigin) {
    const borderBoxPointGroup = [];
    const contentBoxPointGroup = [];
    const cursorList = [];
    if (startOrigin == null || endOrigin == null) {
      return {
        borderBoxPointGroup,
        contentBoxPointGroup,
        cursorList
      };
    }
    const isValid = this._isValidPosition(startOrigin, endOrigin);
    if (!isValid) {
      throw new Error(
        `
                Invalid positions in NodePositionConvertToCursor,
                they are not in the same segment page when in header or footer.`
      );
    }
    const { start, end } = compareNodePosition(startOrigin, endOrigin);
    this._selectionIterator(start, end, (start_sp, end_sp, isFirst, isLast, divide, line) => {
      const { lineHeight, asc, paddingTop, paddingBottom, contentHeight, marginTop, marginBottom } = line;
      const { glyphGroup, st } = divide;
      if (glyphGroup.length === 0) {
        return;
      }
      const { x: startX, y: startY } = this._liquid;
      let borderBoxPosition;
      let contentBoxPosition;
      const firstGlyph = glyphGroup[start_sp];
      const lastGlyph = glyphGroup[end_sp];
      const preGlyph = glyphGroup[start_sp - 1];
      const firstGlyphLeft = (firstGlyph == null ? void 0 : firstGlyph.left) || 0;
      const firstGlyphWidth = (firstGlyph == null ? void 0 : firstGlyph.width) || 0;
      const lastGlyphLeft = (lastGlyph == null ? void 0 : lastGlyph.left) || 0;
      const lastGlyphWidth = (lastGlyph == null ? void 0 : lastGlyph.width) || 0;
      const isCurrentList = (firstGlyph == null ? void 0 : firstGlyph.glyphType) === 2 /* LIST */;
      const { startOffset, endOffset } = getOffsetInDivide(glyphGroup, start_sp, end_sp, st);
      const isStartBack = start.glyph === start_sp && isFirst ? start.isBack : true;
      const isEndBack = end.glyph === end_sp && isLast ? end.isBack : false;
      const collapsed = start === end;
      const anchorGlyph = isStartBack ? preGlyph != null ? preGlyph : firstGlyph : firstGlyph;
      const borderBoxStartY = startY;
      const borderBoxEndY = contentHeight == null ? startY + lineHeight - marginTop - marginBottom : startY + paddingTop + contentHeight + paddingBottom;
      if (start_sp === 0 && end_sp === glyphGroup.length - 1) {
        borderBoxPosition = {
          startX: startX + firstGlyphLeft + (isCurrentList ? firstGlyphWidth : 0),
          startY: borderBoxStartY,
          endX: startX + lastGlyphLeft + (isEndBack ? 0 : lastGlyphWidth),
          endY: borderBoxEndY
        };
        contentBoxPosition = {
          startX: startX + firstGlyphLeft + (isCurrentList ? firstGlyphWidth : 0),
          startY: startY + paddingTop + asc - anchorGlyph.bBox.ba,
          endX: startX + lastGlyphLeft + (isEndBack ? 0 : lastGlyphWidth),
          endY: startY + paddingTop + asc + anchorGlyph.bBox.bd
        };
      } else {
        const isStartBackFin = isStartBack && !isCurrentList;
        borderBoxPosition = {
          startX: startX + firstGlyphLeft + (isStartBackFin ? 0 : firstGlyphWidth),
          startY: borderBoxStartY,
          endX: startX + lastGlyphLeft + (isEndBack ? 0 : lastGlyphWidth),
          endY: borderBoxEndY
        };
        contentBoxPosition = {
          startX: startX + firstGlyphLeft + (isStartBackFin ? 0 : firstGlyphWidth),
          startY: startY + paddingTop + asc - anchorGlyph.bBox.ba,
          endX: startX + lastGlyphLeft + (isEndBack ? 0 : lastGlyphWidth),
          endY: startY + paddingTop + asc + anchorGlyph.bBox.bd
        };
      }
      const clippedBorderBoxPosition = clipPositionToHorizontalRange(borderBoxPosition, this._horizontalClip);
      const clippedContentBoxPosition = clipPositionToHorizontalRange(contentBoxPosition, this._horizontalClip);
      if (clippedBorderBoxPosition) {
        borderBoxPointGroup.push(pushToPoints(clippedBorderBoxPosition));
      }
      if (clippedContentBoxPosition) {
        contentBoxPointGroup.push(pushToPoints(clippedContentBoxPosition));
      }
      cursorList.push({
        startOffset: isStartBack ? startOffset : startOffset + firstGlyph.count,
        endOffset: isEndBack ? endOffset : endOffset + lastGlyph.count,
        collapsed
      });
    });
    return {
      borderBoxPointGroup,
      contentBoxPointGroup,
      cursorList
    };
  }
  _isValidPosition(startOrigin, endOrigin) {
    const { segmentPage: startPage, pageType: startPageType } = startOrigin;
    const { segmentPage: endPage, pageType: endPageType } = endOrigin;
    if (startPageType !== endPageType) {
      return false;
    }
    if (startPageType === 1 /* HEADER */ || startPageType === 2 /* FOOTER */) {
      return startPage === endPage;
    }
    return true;
  }
  _resetCurrentNodePositionState() {
    this._currentStartState = {
      page: 0 /* NORMAL */,
      section: 0 /* NORMAL */,
      column: 0 /* NORMAL */,
      line: 0 /* NORMAL */,
      divide: 0 /* NORMAL */,
      glyph: 0 /* NORMAL */
    };
    this._currentEndState = {
      page: 0 /* NORMAL */,
      section: 0 /* NORMAL */,
      column: 0 /* NORMAL */,
      line: 0 /* NORMAL */,
      divide: 0 /* NORMAL */,
      glyph: 0 /* NORMAL */
    };
  }
  _setNodePositionState(type2 = 0 /* page */, start, end, current) {
    if (current === start) {
      this._currentStartState[type2] = 1 /* START */;
    } else {
      this._currentStartState[type2] = 0 /* NORMAL */;
    }
    if (current === end) {
      this._currentEndState[type2] = 2 /* END */;
    } else {
      this._currentEndState[type2] = 0 /* NORMAL */;
    }
  }
  _checkPreviousNodePositionState(typeIndex, isStart = true) {
    let index = typeIndex;
    let resultState;
    while (index >= 0) {
      const type2 = NodePositionType[index];
      let state;
      if (isStart) {
        state = this._currentStartState[type2];
      } else {
        state = this._currentEndState[type2];
      }
      if (state === void 0) {
        return;
      }
      if (resultState === void 0) {
        resultState = state;
      }
      if (state !== resultState) {
        return 0 /* NORMAL */;
      }
      index--;
    }
    return resultState;
  }
  _getSelectionRuler(typeIndex, startPosition, endPosition, nextLength, current) {
    let start_next = 0;
    let end_next = nextLength;
    const type2 = NodePositionType[typeIndex];
    const nextType = NodePositionType[typeIndex + 1];
    if (nextType === null || type2 === null) {
      return {
        start_next,
        end_next
      };
    }
    const start = startPosition[type2];
    const end = endPosition[type2];
    this._setNodePositionState(type2, start, end, current);
    const preStartNestType = this._checkPreviousNodePositionState(typeIndex);
    const preEndNestType = this._checkPreviousNodePositionState(typeIndex, false);
    if (preStartNestType === 1 /* START */) {
      start_next = startPosition[nextType];
    }
    if (preEndNestType === 2 /* END */) {
      end_next = endPosition[nextType];
    }
    return {
      start_next,
      end_next
    };
  }
  // eslint-disable-next-line max-lines-per-function, complexity
  _selectionIterator(startPosition, endPosition, func) {
    var _a, _b;
    const skeleton = this._docSkeleton;
    if (!skeleton) {
      return [];
    }
    const { pageType, path } = startPosition;
    this._liquid.reset();
    const skeletonData = skeleton.getSkeletonData();
    if (skeletonData == null) {
      return [];
    }
    const { pages, skeHeaders, skeFooters } = skeletonData;
    const { page: pageIndex, segmentPage } = startPosition;
    const { page: endPageIndex, segmentPage: endSegmentPage } = endPosition;
    this._resetCurrentNodePositionState();
    if (this._documentOffsetConfig == null) {
      return [];
    }
    const { pageLayoutType, pageMarginLeft, pageMarginTop } = this._documentOffsetConfig;
    const skipPageIndex = pageType === 0 /* BODY */ || pageType === 3 /* CELL */ ? pageIndex : segmentPage;
    for (let p = 0; p < skipPageIndex; p++) {
      const page = pages[p];
      this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
    }
    const endIndex = pageType === 0 /* BODY */ || pageType === 3 /* CELL */ ? endPageIndex : endSegmentPage;
    for (let p = skipPageIndex; p <= endIndex; p++) {
      const page = pages[p];
      const { headerId, footerId, pageWidth } = page;
      let segmentPage2 = page;
      if (pageType === 1 /* HEADER */) {
        segmentPage2 = (_a = skeHeaders.get(headerId)) == null ? void 0 : _a.get(pageWidth);
      } else if (pageType === 2 /* FOOTER */) {
        segmentPage2 = (_b = skeFooters.get(footerId)) == null ? void 0 : _b.get(pageWidth);
      } else if (pageType === 3 /* CELL */) {
        segmentPage2 = getPageFromPath(skeletonData, path);
      }
      if (segmentPage2 == null) {
        this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
        continue;
      }
      const sections = segmentPage2.sections;
      const { start_next: start_s, end_next: end_s } = this._getSelectionRuler(
        NodePositionMap.page,
        startPosition,
        endPosition,
        sections.length - 1,
        pageType === 0 /* BODY */ || pageType === 3 /* CELL */ ? p : 0
      );
      this._liquid.translateSave();
      const previousHorizontalClip = this._horizontalClip;
      this._horizontalClip = null;
      switch (pageType) {
        case 1 /* HEADER */:
          this._liquid.translatePagePadding({
            ...segmentPage2,
            marginLeft: page.marginLeft
            // Because header or footer margin Left is 0.
          });
          break;
        case 2 /* FOOTER */: {
          const footerTop = page.pageHeight - segmentPage2.height - segmentPage2.marginBottom;
          this._liquid.translate(page.marginLeft, footerTop);
          break;
        }
        case 3 /* CELL */: {
          this._liquid.translatePagePadding(page);
          const rowSke = segmentPage2.parent;
          const tableSke = rowSke.parent;
          const { left: cellLeft } = segmentPage2;
          const { top: tableTop, left: tableLeft } = tableSke;
          const { top: rowTop } = rowSke;
          const sourceTableId = getTableIdAndSliceIndex(tableSke.tableId).tableId;
          const viewport = getDocsTableRenderViewport(getDocumentUnitId(skeleton), sourceTableId);
          const hasHorizontalViewport = viewport && viewport.contentWidth > viewport.viewportWidth;
          const scrollLeft = hasHorizontalViewport ? viewport.scrollLeft : 0;
          if (hasHorizontalViewport) {
            const visibleLeft = this._liquid.x + tableLeft;
            this._horizontalClip = {
              left: visibleLeft,
              right: visibleLeft + viewport.viewportWidth
            };
          }
          this._liquid.translate(tableLeft + cellLeft - scrollLeft, tableTop + rowTop);
          this._liquid.translatePagePadding(segmentPage2);
          break;
        }
        default:
          this._liquid.translatePagePadding(page);
          break;
      }
      for (let s = start_s; s <= end_s; s++) {
        const section = sections[s];
        const columns = section.columns;
        const { start_next: start_c, end_next: end_c } = this._getSelectionRuler(
          NodePositionMap.section,
          startPosition,
          endPosition,
          columns.length - 1,
          s
        );
        this._liquid.translateSection(section);
        for (let c = start_c; c <= end_c; c++) {
          const column = columns[c];
          const lines = column.lines;
          const { start_next: start_l, end_next: end_l } = this._getSelectionRuler(
            NodePositionMap.column,
            startPosition,
            endPosition,
            lines.length - 1,
            c
          );
          this._liquid.translateColumn(column);
          for (let l = start_l; l <= end_l; l++) {
            const line = lines[l];
            const { divides } = line;
            const { start_next: start_d, end_next: end_d } = this._getSelectionRuler(
              NodePositionMap.line,
              startPosition,
              endPosition,
              divides.length - 1,
              l
            );
            this._liquid.translateSave();
            this._liquid.translateLine(line, true, false);
            for (let d = start_d; d <= end_d; d++) {
              const divide = divides[d];
              this._liquid.translateSave();
              this._liquid.translateDivide(divide);
              const { glyphGroup } = divide;
              const { start_next: start_sp, end_next: end_sp } = this._getSelectionRuler(
                NodePositionMap.divide,
                startPosition,
                endPosition,
                glyphGroup.length - 1,
                d
              );
              let isFirst = false;
              let isLast = false;
              if (p === skipPageIndex && s === start_s && c === start_c && l === start_l && d === start_d) {
                isFirst = true;
              }
              if (p === endIndex && s === end_s && c === end_c && l === end_l && d === end_d) {
                isLast = true;
              }
              func && func(start_sp, end_sp, isFirst, isLast, divide, line, column, section, segmentPage2);
              this._liquid.translateRestore();
            }
            this._liquid.translateRestore();
          }
        }
      }
      this._liquid.translateRestore();
      this._horizontalClip = previousHorizontalClip;
      this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
    }
  }
};
function clipPositionToHorizontalRange(position, clip) {
  if (!clip) {
    return position;
  }
  const startX = Math.max(position.startX, clip.left);
  const endX = Math.min(position.endX, clip.right);
  const collapsed = position.startX === position.endX;
  if (collapsed) {
    return position.startX >= clip.left && position.startX <= clip.right ? position : null;
  }
  if (endX <= startX) {
    return null;
  }
  return {
    ...position,
    startX,
    endX
  };
}
function getDocumentUnitId(docSkeleton) {
  var _a, _b, _c, _d;
  const viewModel = docSkeleton.getViewModel();
  return (_d = (_c = (_a = viewModel.getDataModel) == null ? void 0 : (_b = _a.call(viewModel)).getUnitId) == null ? void 0 : _c.call(_b)) != null ? _d : "";
}

// ../packages/docs-ui/src/services/selection/convert-rect-range.ts
function isValidRectRange(anchorNodePosition, focusNodePosition) {
  const { path: anchorPath } = anchorNodePosition;
  const { path: focusPath } = focusNodePosition;
  if (anchorPath.length !== focusPath.length) {
    return false;
  }
  if (anchorPath.indexOf("cells") === -1) {
    return false;
  }
  const tableIdIndex = anchorPath.indexOf("skeTables") + 1;
  const rowIndex = anchorPath.indexOf("rows") + 1;
  const cellIndex = anchorPath.indexOf("cells") + 1;
  const { tableId: anchorTableId, sliceIndex: anchorSliceIndex } = getTableIdAndSliceIndex(anchorPath[tableIdIndex]);
  const { tableId: focusTableId, sliceIndex: focusSliceIndex } = getTableIdAndSliceIndex(focusPath[tableIdIndex]);
  if (anchorTableId !== focusTableId) {
    return false;
  }
  const anchorRowIndex = anchorPath[rowIndex];
  const focusRowIndex = focusPath[rowIndex];
  const anchorCellIndex = anchorPath[cellIndex];
  const focusCellIndex = focusPath[cellIndex];
  if (anchorRowIndex === focusRowIndex && anchorCellIndex === focusCellIndex && anchorSliceIndex === focusSliceIndex) {
    return false;
  }
  return true;
}
function isInSameTableCell(anchorNodePosition, focusNodePosition) {
  const { path: anchorPath } = anchorNodePosition;
  const { path: focusPath } = focusNodePosition;
  if (anchorPath.indexOf("cells") === -1 || focusPath.indexOf("cells") === -1) {
    return false;
  }
  if (anchorPath.length !== focusPath.length) {
    return false;
  }
  return Tools.diffValue(anchorPath, focusPath);
}
function isInSameTableCellData(skeleton, anchorNodePosition, focusNodePosition) {
  var _a, _b, _c, _d, _e, _f, _g, _h;
  const { path: anchorPath } = anchorNodePosition;
  const { path: focusPath } = focusNodePosition;
  if (anchorPath.indexOf("cells") === -1 || focusPath.indexOf("cells") === -1) {
    return false;
  }
  const anchorGlyph = skeleton.findGlyphByPosition(anchorNodePosition);
  const focusGlyph = skeleton.findGlyphByPosition(focusNodePosition);
  const anchorCellPage = (_d = (_c = (_b = (_a = anchorGlyph == null ? void 0 : anchorGlyph.parent) == null ? void 0 : _a.parent) == null ? void 0 : _b.parent) == null ? void 0 : _c.parent) == null ? void 0 : _d.parent;
  const focusCellPage = (_h = (_g = (_f = (_e = focusGlyph == null ? void 0 : focusGlyph.parent) == null ? void 0 : _e.parent) == null ? void 0 : _f.parent) == null ? void 0 : _g.parent) == null ? void 0 : _h.parent;
  if (anchorCellPage == null || focusCellPage == null) {
    return false;
  }
  const anchorRow = anchorCellPage.parent;
  const focusRow = focusCellPage.parent;
  const anchorColIndex = anchorRow.cells.indexOf(anchorCellPage);
  const focusColIndex = focusRow.cells.indexOf(focusCellPage);
  return anchorColIndex === focusColIndex && anchorRow.index === focusRow.index;
}
function compareNodePositionInTable(a, b) {
  if (isInSameTableCell(a, b)) {
    return compareNodePositionLogic(a, b);
  }
  const { path: aPath } = a;
  const { path: bPath } = b;
  const aTableId = aPath[aPath.length - 5];
  const bTableId = bPath[bPath.length - 5];
  if (aTableId !== bTableId && typeof aTableId === "string" && typeof bTableId === "string") {
    const aSlideId = aTableId.split("#-#")[1];
    const bSlideId = bTableId.split("#-#")[1];
    return +aSlideId < +bSlideId;
  }
  const aRowCount = aPath[aPath.length - 3];
  const bRowCount = bPath[bPath.length - 3];
  const aCellCount = aPath[aPath.length - 1];
  const bCellCount = bPath[bPath.length - 1];
  if (aRowCount < bRowCount) {
    return true;
  }
  if (aRowCount > bRowCount) {
    return false;
  }
  return aCellCount <= bCellCount;
}
function isEmptyCellPage(cell) {
  return cell.sections[0].columns[0].lines.length === 0;
}
function findNonEmptyCellPages(cells, startCol, endCol) {
  let s = startCol;
  let e = endCol;
  let startCell = cells[s];
  let endCell = cells[e];
  while (s < e && (isEmptyCellPage(startCell) || isEmptyCellPage(endCell))) {
    if (isEmptyCellPage(startCell)) {
      s++;
      startCell = cells[s];
    } else if (isEmptyCellPage(endCell)) {
      e--;
      endCell = cells[e];
    }
  }
  if (!isEmptyCellPage(startCell) && !isEmptyCellPage(endCell)) {
    return [startCell, endCell];
  }
}
function getColumnBoundary(table, column) {
  var _a;
  const columns = (_a = table.tableSource) == null ? void 0 : _a.tableColumns;
  if (columns && column >= 0 && column <= columns.length) {
    return columns.slice(0, column).reduce((total, tableColumn) => {
      var _a2, _b, _c;
      return total + ((_c = (_b = (_a2 = tableColumn.size) == null ? void 0 : _a2.width) == null ? void 0 : _b.v) != null ? _c : 0);
    }, 0);
  }
  const row = table.rows[0];
  const cell = row == null ? void 0 : row.cells[column];
  if (cell) {
    return cell.left;
  }
  const previousCell = row == null ? void 0 : row.cells[column - 1];
  if (previousCell) {
    return previousCell.left + previousCell.pageWidth;
  }
  return null;
}
function getDocumentUnitId2(docSkeleton) {
  var _a, _b, _c, _d;
  const viewModel = docSkeleton.getViewModel();
  return (_d = (_c = (_a = viewModel.getDataModel) == null ? void 0 : (_b = _a.call(viewModel)).getUnitId) == null ? void 0 : _c.call(_b)) != null ? _d : "";
}
function pushViewportClippedPoints(pointGroup, position, viewport, tableLeft) {
  const scrollLeft = viewport && viewport.contentWidth > viewport.viewportWidth ? viewport.scrollLeft : 0;
  const viewportWidth = viewport && viewport.contentWidth > viewport.viewportWidth ? viewport.viewportWidth : null;
  const startX = position.startX - scrollLeft;
  const endX = position.endX - scrollLeft;
  const clippedStartX = viewportWidth == null ? startX : Math.max(startX, tableLeft);
  const clippedEndX = viewportWidth == null ? endX : Math.min(endX, tableLeft + viewportWidth);
  if (clippedEndX <= clippedStartX) {
    return;
  }
  pointGroup.push(pushToPoints({
    ...position,
    startX: clippedStartX,
    endX: clippedEndX
  }));
}
var NodePositionConvertToRectRange = class {
  constructor(_documentOffsetConfig, _docSkeleton) {
    __publicField(this, "_documentOffsetConfig", _documentOffsetConfig);
    __publicField(this, "_docSkeleton", _docSkeleton);
    __publicField(this, "_liquid", new Liquid());
  }
  // eslint-disable-next-line max-lines-per-function
  getRangePointData(startNodePosition, endNodePosition) {
    const pointGroup = [];
    const docSkeleton = this._docSkeleton;
    const skeletonData = docSkeleton.getSkeletonData();
    if (skeletonData == null) {
      return;
    }
    const { pages } = skeletonData;
    const { segmentPage: startSegmentPage, page: startPage, pageType } = startNodePosition;
    const rectInfo = this._getTableRectRangeInfo(startNodePosition, endNodePosition);
    if (rectInfo == null) {
      return;
    }
    const {
      tableId,
      startRowIndex: startRow,
      startColumnIndex: startColumn,
      endRowIndex: endRow,
      endColumnIndex: endColumn,
      intersectsMergedCell
    } = rectInfo;
    this._liquid.reset();
    const { pageLayoutType, pageMarginLeft, pageMarginTop } = this._documentOffsetConfig;
    const unitId = getDocumentUnitId2(docSkeleton);
    const sourceTableId = getTableIdAndSliceIndex(tableId).tableId;
    const skipPageIndex = pageType === 0 /* BODY */ || pageType === 3 /* CELL */ ? startPage : startSegmentPage;
    for (let p = 0; p < skipPageIndex; p++) {
      const page = pages[p];
      this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
    }
    for (let p = skipPageIndex; p < pages.length; p++) {
      const page = pages[p];
      this._liquid.translatePagePadding(page);
      const { skeTables } = page;
      let table = null;
      for (const [id, tableSke] of skeTables.entries()) {
        if (id.startsWith(tableId)) {
          table = tableSke;
        }
      }
      if (table == null) {
        this._liquid.restorePagePadding(page);
        this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
        continue;
      }
      this._liquid.translateSave();
      this._liquid.translate(0, table.top);
      const { x, y } = this._liquid;
      const { left: tableLeft } = table;
      const viewport = getDocsTableRenderViewport(unitId, sourceTableId);
      if (intersectsMergedCell) {
        const rows = table.rows.filter((row) => row.index >= startRow && row.index <= endRow);
        const firstRow = rows[0];
        const lastRow = rows[rows.length - 1];
        const startX = getColumnBoundary(table, startColumn);
        const endX = getColumnBoundary(table, endColumn + 1);
        if (firstRow && lastRow && startX != null && endX != null) {
          pushViewportClippedPoints(pointGroup, {
            startX: x + tableLeft + startX,
            startY: y + firstRow.top,
            endX: x + tableLeft + endX,
            endY: y + lastRow.top + lastRow.height
          }, viewport, x + tableLeft);
        }
        this._liquid.translateRestore();
        this._liquid.restorePagePadding(page);
        this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
        continue;
      }
      for (const row of table.rows) {
        if (row.index >= startRow && row.index <= endRow) {
          const cells = findNonEmptyCellPages(row.cells, startColumn, endColumn);
          if (cells == null) {
            continue;
          }
          const [rowStartCell, rowEndCell] = cells;
          const position = {
            startX: x + rowStartCell.left + tableLeft,
            startY: y + row.top,
            endX: x + rowEndCell.left + rowEndCell.pageWidth + tableLeft,
            endY: y + row.top + row.height
          };
          pushViewportClippedPoints(pointGroup, position, viewport, x + tableLeft);
        }
      }
      this._liquid.translateRestore();
      this._liquid.restorePagePadding(page);
      this._liquid.translatePage(page, pageLayoutType, pageMarginLeft, pageMarginTop);
    }
    return {
      pointGroup,
      startRow,
      startColumn,
      endRow,
      endColumn,
      tableId
    };
  }
  getNodePositionGroup(anchorNodePosition, focusNodePosition) {
    const nodePositionGroup = [];
    const anchorIndex = this._docSkeleton.findCharIndexByPosition(anchorNodePosition);
    const focusIndex = this._docSkeleton.findCharIndexByPosition(focusNodePosition);
    if (anchorIndex == null || focusIndex == null) {
      return;
    }
    const compare = anchorIndex < focusIndex;
    const rectInfo = this._getTableRectRangeInfo(anchorNodePosition, focusNodePosition);
    if (rectInfo == null) {
      return;
    }
    const { tableId, startRowIndex, startColumnIndex, endRowIndex, endColumnIndex, intersectsMergedCell } = rectInfo;
    if (intersectsMergedCell) {
      return [{
        anchor: anchorNodePosition,
        focus: focusNodePosition
      }];
    }
    const tableNode = this._docSkeleton.getViewModel().findTableNodeById(tableId);
    if (tableNode == null) {
      return;
    }
    const totalColumns = tableNode.children[0].children.length;
    const spanEntireRow = startColumnIndex === 0 && endColumnIndex === totalColumns - 1;
    if (spanEntireRow) {
      const startCellNode = tableNode.children[startRowIndex].children[startColumnIndex];
      const startNodePosition = this._docSkeleton.findNodePositionByCharIndex(startCellNode.startIndex + 1);
      const endCellNode = tableNode.children[endRowIndex].children[endColumnIndex];
      const endNodePosition = this._docSkeleton.findNodePositionByCharIndex(endCellNode.endIndex - 2);
      if (startNodePosition == null || endNodePosition == null) {
        return;
      }
      nodePositionGroup.push({
        anchor: compare ? startNodePosition : endNodePosition,
        focus: compare ? endNodePosition : startNodePosition
      });
    } else {
      for (let i = startRowIndex; i <= endRowIndex; i++) {
        const rowNode = tableNode.children[i];
        const startCellNode = rowNode.children[startColumnIndex];
        const endCellNode = rowNode.children[endColumnIndex];
        const startNodePosition = this._docSkeleton.findNodePositionByCharIndex(startCellNode.startIndex + 1);
        const endNodePosition = this._docSkeleton.findNodePositionByCharIndex(endCellNode.endIndex - 2);
        if (startNodePosition == null || endNodePosition == null) {
          return;
        }
        nodePositionGroup.push({
          anchor: compare ? startNodePosition : endNodePosition,
          focus: compare ? endNodePosition : startNodePosition
        });
      }
    }
    return nodePositionGroup;
  }
  _getTableRectRangeInfo(anchorPosition, focusPosition) {
    var _a;
    const docSkeleton = this._docSkeleton;
    const skeletonData = docSkeleton.getSkeletonData();
    if (skeletonData == null) {
      return;
    }
    const { pages } = skeletonData;
    const { path: anchorPath } = anchorPosition;
    const { path: focusPath } = focusPosition;
    const anchorCell = getPageFromPath(skeletonData, anchorPath);
    const focusCell = getPageFromPath(skeletonData, focusPath);
    if (anchorCell == null || focusCell == null) {
      return;
    }
    const tableId = anchorCell.segmentId;
    const anchorRow = anchorCell.parent.index;
    const anchorColumn = anchorCell.parent.cells.indexOf(anchorCell);
    const focusRow = (focusCell == null ? void 0 : focusCell.parent).index;
    const focusColumn = (focusCell == null ? void 0 : focusCell.parent).cells.indexOf(focusCell);
    const sourceTableId = getTableIdAndSliceIndex(tableId).tableId;
    const tableSource = (_a = docSkeleton.getViewModel().getSnapshot().tableSource) == null ? void 0 : _a[sourceTableId];
    const rawRange = {
      startRowIndex: Math.min(anchorRow, focusRow),
      endRowIndex: Math.max(anchorRow, focusRow),
      startColumnIndex: Math.min(anchorColumn, focusColumn),
      endColumnIndex: Math.max(anchorColumn, focusColumn)
    };
    const intersectsMergedCell = rangeIntersectsMergedCell(tableSource, rawRange);
    const range = expandRangeByMergedCells(tableSource, rawRange);
    return {
      pages,
      tableId,
      intersectsMergedCell,
      ...range
    };
  }
};
function expandRangeByMergedCells(table, range) {
  if (!table) {
    return range;
  }
  let expanded = normalizeRange(range);
  let changed = true;
  while (changed) {
    changed = false;
    table.tableRows.forEach((row, rowIndex) => {
      row.tableCells.forEach((cell, columnIndex) => {
        var _a, _b;
        const rowSpan = (_a = cell.rowSpan) != null ? _a : 1;
        const columnSpan = (_b = cell.columnSpan) != null ? _b : 1;
        if (rowSpan <= 0 || columnSpan <= 0 || rowSpan === 1 && columnSpan === 1) {
          return;
        }
        const mergedRange = {
          startRowIndex: rowIndex,
          endRowIndex: rowIndex + rowSpan - 1,
          startColumnIndex: columnIndex,
          endColumnIndex: columnIndex + columnSpan - 1
        };
        if (!rangesIntersect(expanded, mergedRange)) {
          return;
        }
        const next = {
          startRowIndex: Math.min(expanded.startRowIndex, mergedRange.startRowIndex),
          endRowIndex: Math.max(expanded.endRowIndex, mergedRange.endRowIndex),
          startColumnIndex: Math.min(expanded.startColumnIndex, mergedRange.startColumnIndex),
          endColumnIndex: Math.max(expanded.endColumnIndex, mergedRange.endColumnIndex)
        };
        if (!rangesEqual(expanded, next)) {
          expanded = next;
          changed = true;
        }
      });
    });
  }
  return expanded;
}
function rangeIntersectsMergedCell(table, range) {
  if (!table) {
    return false;
  }
  const normalized = normalizeRange(range);
  return table.tableRows.some((row, rowIndex) => row.tableCells.some((cell, columnIndex) => {
    var _a, _b;
    const rowSpan = (_a = cell.rowSpan) != null ? _a : 1;
    const columnSpan = (_b = cell.columnSpan) != null ? _b : 1;
    if (rowSpan <= 0 || columnSpan <= 0 || rowSpan === 1 && columnSpan === 1) {
      return false;
    }
    return rangesIntersect(normalized, {
      startRowIndex: rowIndex,
      endRowIndex: rowIndex + rowSpan - 1,
      startColumnIndex: columnIndex,
      endColumnIndex: columnIndex + columnSpan - 1
    });
  }));
}
function normalizeRange(range) {
  return {
    startRowIndex: Math.min(range.startRowIndex, range.endRowIndex),
    endRowIndex: Math.max(range.startRowIndex, range.endRowIndex),
    startColumnIndex: Math.min(range.startColumnIndex, range.endColumnIndex),
    endColumnIndex: Math.max(range.startColumnIndex, range.endColumnIndex)
  };
}
function rangesIntersect(left, right) {
  return left.startRowIndex <= right.endRowIndex && left.endRowIndex >= right.startRowIndex && left.startColumnIndex <= right.endColumnIndex && left.endColumnIndex >= right.startColumnIndex;
}
function rangesEqual(left, right) {
  return left.startRowIndex === right.startRowIndex && left.endRowIndex === right.endRowIndex && left.startColumnIndex === right.startColumnIndex && left.endColumnIndex === right.endColumnIndex;
}

// ../packages/docs-ui/src/services/selection/text-range.ts
var TEXT_RANGE_KEY_PREFIX = "__TestSelectionRange__";
var TEXT_ANCHOR_KEY_PREFIX = "__TestSelectionAnchor__";
var ID_LENGTH = 6;
var BLINK_ON = 500;
var BLINK_OFF = 500;
var TEXT_RANGE_LAYER_INDEX = 3;
function getAnchorBounding(pointsGroup) {
  const points = pointsGroup[0];
  const startPoint = points[0];
  const endPoint = points[2];
  const { x: startX, y: startY } = startPoint;
  const { x: endX, y: endY } = endPoint;
  return {
    left: startX,
    top: startY,
    width: endX - startX,
    height: endY - startY
  };
}
function getLineBounding(pointsGroup) {
  return pointsGroup.map((line) => {
    let xMin = Infinity;
    let xMax = -Infinity;
    let yMin = Infinity;
    let yMax = -Infinity;
    line.forEach((point) => {
      xMin = Math.min(point.x, xMin);
      xMax = Math.max(point.x, xMax);
      yMax = Math.max(point.y, yMax);
      yMin = Math.min(point.y, yMin);
    });
    return {
      left: xMin,
      right: xMax,
      top: yMin,
      bottom: yMax
    };
  });
}
var TextRange = class {
  constructor(_scene, _document, _docSkeleton, anchorNodePosition, focusNodePosition, style = NORMAL_TEXT_SELECTION_PLUGIN_STYLE, _segmentId = "", _segmentPage = -1) {
    __publicField(this, "_scene", _scene);
    __publicField(this, "_document", _document);
    __publicField(this, "_docSkeleton", _docSkeleton);
    __publicField(this, "anchorNodePosition", anchorNodePosition);
    __publicField(this, "focusNodePosition", focusNodePosition);
    __publicField(this, "style", style);
    __publicField(this, "_segmentId", _segmentId);
    __publicField(this, "_segmentPage", _segmentPage);
    __publicField(this, "rangeType", "TEXT" /* TEXT */);
    // Identifies whether the range is the current one, most of which is the last range.
    __publicField(this, "_current", false);
    // The rendered range graphic when collapsed is false
    __publicField(this, "_rangeShape");
    // The rendered range graphic when collapsed is true
    __publicField(this, "_anchorShape");
    __publicField(this, "_cursorList", []);
    __publicField(this, "_anchorBlinkTimer", null);
    this._anchorBlink();
    this.refresh();
  }
  _anchorBlink() {
    setTimeout(() => {
      if (this._anchorShape) {
        if (this._anchorShape.visible) {
          this.deactivateStatic();
        }
      }
    }, BLINK_ON);
    this._anchorBlinkTimer = setInterval(() => {
      if (this._anchorShape) {
        if (this._anchorShape.visible) {
          this.activeStatic();
          setTimeout(() => {
            this.deactivateStatic();
          }, BLINK_ON);
        }
      }
    }, BLINK_OFF + BLINK_ON);
  }
  // The start position of the range
  get startOffset() {
    var _a;
    const { startOffset } = (_a = getOneTextSelectionRange(this._cursorList)) != null ? _a : {};
    const body = this._docSkeleton.getViewModel().getDataModel().getSelfOrHeaderFooterModel(this._segmentId).getBody();
    if (startOffset == null || body == null) {
      return startOffset;
    }
    const maxLength = body.dataStream.length - 2;
    return Math.min(maxLength, startOffset);
  }
  // The end position of the range
  get endOffset() {
    var _a;
    const { endOffset } = (_a = getOneTextSelectionRange(this._cursorList)) != null ? _a : {};
    const body = this._docSkeleton.getViewModel().getDataModel().getSelfOrHeaderFooterModel(this._segmentId).getBody();
    if (endOffset == null || body == null) {
      return endOffset;
    }
    const maxLength = body.dataStream.length - 2;
    return Math.min(endOffset, maxLength);
  }
  get collapsed() {
    const { startOffset, endOffset } = this;
    return startOffset != null && startOffset === endOffset;
  }
  get startNodePosition() {
    if (this.anchorNodePosition == null) {
      return null;
    }
    if (this.focusNodePosition == null) {
      return this.anchorNodePosition;
    }
    const { start } = compareNodePosition(this.anchorNodePosition, this.focusNodePosition);
    return start;
  }
  get endNodePosition() {
    if (this.anchorNodePosition == null) {
      return this.focusNodePosition;
    }
    if (this.focusNodePosition == null) {
      return null;
    }
    const { end } = compareNodePosition(this.anchorNodePosition, this.focusNodePosition);
    return end;
  }
  get direction() {
    const { collapsed, anchorNodePosition, focusNodePosition } = this;
    if (collapsed || anchorNodePosition == null || focusNodePosition == null) {
      return "none" /* NONE */;
    }
    const compare = compareNodePositionLogic(anchorNodePosition, focusNodePosition);
    return compare ? "forward" /* FORWARD */ : "backward" /* BACKWARD */;
  }
  get segmentId() {
    return this._segmentId;
  }
  get segmentPage() {
    return this._segmentPage;
  }
  getAbsolutePosition() {
    const anchor = this.anchorNodePosition;
    const focus = this.focusNodePosition;
    if (this._isEmpty()) {
      return;
    }
    const documentOffsetConfig = this._document.getOffsetConfig();
    const { docsLeft, docsTop } = documentOffsetConfig;
    const convertor = new NodePositionConvertToCursor(documentOffsetConfig, this._docSkeleton);
    if (this._isCollapsed()) {
      const { contentBoxPointGroup, cursorList: cursorList2 } = convertor.getRangePointData(anchor, anchor);
      this._setCursorList(cursorList2);
      if (contentBoxPointGroup.length === 0) {
        return;
      }
      const pos2 = getAnchorBounding(contentBoxPointGroup);
      return {
        ...pos2,
        left: pos2.left + docsLeft,
        top: pos2.top + docsTop
      };
    }
    const { borderBoxPointGroup, cursorList } = convertor.getRangePointData(anchor, focus);
    this._setCursorList(cursorList);
    if (borderBoxPointGroup.length === 0) {
      return;
    }
    const pos = getAnchorBounding(borderBoxPointGroup);
    return {
      ...pos,
      left: pos.left + docsLeft,
      top: pos.top + docsTop
    };
  }
  getAnchor() {
    return this._anchorShape;
  }
  activeStatic() {
    var _a, _b;
    (_b = this._anchorShape) == null ? void 0 : _b.setProps({
      stroke: ((_a = this.style) == null ? void 0 : _a.strokeActive) || getColor(COLORS.black, 1)
    });
  }
  deactivateStatic() {
    var _a, _b;
    (_b = this._anchorShape) == null ? void 0 : _b.setProps({
      stroke: ((_a = this.style) == null ? void 0 : _a.stroke) || getColor(COLORS.black, 0)
    });
  }
  isActive() {
    return this._current === true;
  }
  activate() {
    this._current = true;
  }
  deactivate() {
    this._current = false;
  }
  dispose() {
    var _a, _b;
    (_a = this._rangeShape) == null ? void 0 : _a.dispose();
    this._rangeShape = null;
    (_b = this._anchorShape) == null ? void 0 : _b.dispose();
    this._anchorShape = null;
    if (this._anchorBlinkTimer) {
      clearInterval(this._anchorBlinkTimer);
      this._anchorBlinkTimer = null;
    }
  }
  isIntersection(compareRange) {
    const { startOffset: activeStart, endOffset: activeEnd } = this;
    const { startOffset: compareStart, endOffset: compareEnd } = compareRange;
    if (activeStart == null || activeEnd == null || compareStart == null || compareEnd == null) {
      return false;
    }
    return activeStart <= compareEnd && activeEnd >= compareStart;
  }
  // render cursor and selection.
  refresh() {
    var _a, _b;
    const { _document, _docSkeleton } = this;
    const anchor = this.anchorNodePosition;
    const focus = this.focusNodePosition;
    (_a = this._anchorShape) == null ? void 0 : _a.hide();
    (_b = this._rangeShape) == null ? void 0 : _b.hide();
    if (this._isEmpty()) {
      return;
    }
    const documentOffsetConfig = _document.getOffsetConfig();
    const { docsLeft, docsTop } = documentOffsetConfig;
    const convertor = new NodePositionConvertToCursor(documentOffsetConfig, _docSkeleton);
    if (this._isCollapsed()) {
      const { contentBoxPointGroup, cursorList: cursorList2 } = convertor.getRangePointData(anchor, anchor);
      this._setCursorList(cursorList2);
      if (contentBoxPointGroup.length > 0) {
        const glyphAtCursor = _docSkeleton.findGlyphByPosition(anchor);
        this._createOrUpdateAnchor(contentBoxPointGroup, docsLeft, docsTop, glyphAtCursor);
      }
      return;
    }
    const { borderBoxPointGroup, cursorList } = convertor.getRangePointData(anchor, focus);
    this._setCursorList(cursorList);
    if (borderBoxPointGroup.length > 0) {
      this._createOrUpdateRange(borderBoxPointGroup, docsLeft, docsTop);
    }
  }
  _isEmpty() {
    return this.anchorNodePosition == null && this.focusNodePosition == null;
  }
  _isCollapsed() {
    const anchor = this.anchorNodePosition;
    const focus = this.focusNodePosition;
    if (anchor != null && focus == null) {
      return true;
    }
    if (anchor == null || focus == null) {
      return false;
    }
    const keys = Object.keys(NodePositionMap);
    for (const key of keys) {
      const startNodeValue = anchor[key];
      const endNodeValue = focus[key];
      if (startNodeValue !== endNodeValue) {
        return false;
      }
    }
    if (anchor.isBack !== focus.isBack) {
      return false;
    }
    return true;
  }
  _createOrUpdateRange(pointsGroup, left, top) {
    var _a;
    if (this._rangeShape) {
      this._rangeShape.translate(left, top);
      this._rangeShape.updatePointGroup(pointsGroup);
      this._rangeShape.show();
      return;
    }
    const OPACITY = 0.3;
    const polygon = new RegularPolygon(TEXT_RANGE_KEY_PREFIX + generateRandomId(ID_LENGTH), {
      pointsGroup,
      fill: ((_a = this.style) == null ? void 0 : _a.fill) || getColor(COLORS.black, OPACITY),
      left,
      top,
      evented: false,
      debounceParentDirty: false
    });
    this._rangeShape = polygon;
    this._scene.addObject(polygon, TEXT_RANGE_LAYER_INDEX);
  }
  _createOrUpdateAnchor(pointsGroup, docsLeft, docsTop, glyph) {
    var _a, _b, _c;
    const bounding = getAnchorBounding(pointsGroup);
    const { left: boundingLeft } = bounding;
    let { top: boundingTop, height } = bounding;
    const MIN_CURSOR_HEIGHT = 14;
    if (height < MIN_CURSOR_HEIGHT) {
      boundingTop -= MIN_CURSOR_HEIGHT - height;
      height = MIN_CURSOR_HEIGHT;
    }
    const ITALIC_DEGREE = 12;
    let left = boundingLeft + docsLeft;
    const top = boundingTop + docsTop;
    const isItalic = ((_a = glyph == null ? void 0 : glyph.ts) == null ? void 0 : _a.it) === 1 /* TRUE */;
    if (isItalic) {
      left += height * Math.tan(ITALIC_DEGREE * Math.PI / 180) / 2;
    }
    if (this._anchorShape) {
      this._anchorShape.transformByState({ left, top, height });
      this._anchorShape.show();
      if (isItalic) {
        this._anchorShape.skew(-ITALIC_DEGREE, 0);
      } else {
        this._anchorShape.skew(0, 0);
      }
      return;
    }
    const anchor = new Rect(TEXT_ANCHOR_KEY_PREFIX + generateRandomId(ID_LENGTH), {
      left,
      top,
      height,
      strokeWidth: ((_b = this.style) == null ? void 0 : _b.strokeWidth) || 1,
      stroke: ((_c = this.style) == null ? void 0 : _c.strokeActive) || getColor(COLORS.black, 1),
      evented: false
    });
    if (isItalic) {
      anchor.skew(-ITALIC_DEGREE, 0);
    }
    this._anchorShape = anchor;
    this._scene.addObject(anchor, TEXT_RANGE_LAYER_INDEX);
    this.activeStatic();
  }
  _setCursorList(cursorList) {
    if (cursorList.length === 0) {
      return;
    }
    this._cursorList = cursorList;
  }
};

// ../packages/docs-ui/src/services/selection/rect-range.ts
var RECT_RANGE_KEY_PREFIX = "__DocTableRectRange__";
var ID_LENGTH2 = 6;
function convertPositionsToRectRanges(scene, document2, docSkeleton, anchorNodePosition, focusNodePosition, style = NORMAL_TEXT_SELECTION_PLUGIN_STYLE, segmentId = "", segmentPage = -1) {
  const documentOffsetConfig = document2.getOffsetConfig();
  const convertor = new NodePositionConvertToRectRange(documentOffsetConfig, docSkeleton);
  const nodePositionGroup = convertor.getNodePositionGroup(anchorNodePosition, focusNodePosition);
  return (nodePositionGroup != null ? nodePositionGroup : []).map((position) => new RectRange(
    scene,
    document2,
    docSkeleton,
    position.anchor,
    position.focus,
    style,
    segmentId,
    segmentPage
  ));
}
var RectRange = class {
  constructor(_scene, _document, _docSkeleton, anchorNodePosition, focusNodePosition, style = NORMAL_TEXT_SELECTION_PLUGIN_STYLE, _segmentId = "", _segmentPage = -1) {
    __publicField(this, "_scene", _scene);
    __publicField(this, "_document", _document);
    __publicField(this, "_docSkeleton", _docSkeleton);
    __publicField(this, "anchorNodePosition", anchorNodePosition);
    __publicField(this, "focusNodePosition", focusNodePosition);
    __publicField(this, "style", style);
    __publicField(this, "_segmentId", _segmentId);
    __publicField(this, "_segmentPage", _segmentPage);
    __publicField(this, "rangeType", "RECT" /* RECT */);
    // The rendered rect range
    __publicField(this, "_rangeShape");
    // Identifies whether the range is the current one, most of which is the last range.
    __publicField(this, "_current", false);
    // Rect Range start row.
    __publicField(this, "_startRow");
    // Rect Range start column.
    __publicField(this, "_startCol");
    // Rect Range end row.
    __publicField(this, "_endRow");
    // Rect Range end column.
    __publicField(this, "_endCol");
    // table id in view model.
    __publicField(this, "_tableId");
    this.refresh();
  }
  get startOffset() {
    const { startNodePosition } = this;
    return this._docSkeleton.findCharIndexByPosition(startNodePosition);
  }
  get endOffset() {
    const { endNodePosition } = this;
    return this._docSkeleton.findCharIndexByPosition(endNodePosition);
  }
  get collapsed() {
    return false;
  }
  get startRow() {
    return this._startRow;
  }
  get startColumn() {
    return this._startCol;
  }
  get endRow() {
    return this._endRow;
  }
  get endColumn() {
    return this._endCol;
  }
  get tableId() {
    return this._tableId;
  }
  get segmentId() {
    return this._segmentId;
  }
  get segmentPage() {
    return this._segmentPage;
  }
  get spanEntireRow() {
    var _a;
    const viewModel = this._docSkeleton.getViewModel();
    const table = (_a = viewModel.getSnapshot().tableSource) == null ? void 0 : _a[this._tableId];
    const { _startCol, _endCol } = this;
    if (table == null) {
      throw new Error("Table is not found.");
    }
    const { tableColumns } = table;
    return _startCol === 0 && _endCol === tableColumns.length - 1;
  }
  get spanEntireColumn() {
    var _a;
    const viewModel = this._docSkeleton.getViewModel();
    const table = (_a = viewModel.getSnapshot().tableSource) == null ? void 0 : _a[this._tableId];
    const { _startRow, _endRow } = this;
    if (table == null) {
      throw new Error("Table is not found.");
    }
    const { tableRows } = table;
    return _startRow === 0 && _endRow === tableRows.length - 1;
  }
  get spanEntireTable() {
    return this.spanEntireRow && this.spanEntireColumn;
  }
  get startNodePosition() {
    const { anchorNodePosition, focusNodePosition } = this;
    const compare = compareNodePositionInTable(anchorNodePosition, focusNodePosition);
    return compare ? anchorNodePosition : focusNodePosition;
  }
  get endNodePosition() {
    const { anchorNodePosition, focusNodePosition } = this;
    const compare = compareNodePositionInTable(anchorNodePosition, focusNodePosition);
    return compare ? focusNodePosition : anchorNodePosition;
  }
  get direction() {
    const { anchorNodePosition, focusNodePosition } = this;
    const compare = compareNodePositionInTable(anchorNodePosition, focusNodePosition);
    return compare ? "forward" /* FORWARD */ : "backward" /* BACKWARD */;
  }
  isActive() {
    return this._current === true;
  }
  activate() {
    this._current = true;
  }
  deactivate() {
    this._current = false;
  }
  dispose() {
    var _a;
    (_a = this._rangeShape) == null ? void 0 : _a.dispose();
    this._rangeShape = null;
  }
  isIntersection(compareRange) {
    const { startRow, startColumn, endRow, endColumn } = this;
    const { startRow: cStartRow, startColumn: cStartColumn, endRow: cEndRow, endColumn: cEndColumn } = compareRange;
    const rect1 = {
      left: startColumn,
      top: startRow,
      right: endColumn,
      bottom: endRow
    };
    const rect2 = {
      left: cStartColumn,
      top: cStartRow,
      right: cEndColumn,
      bottom: cEndRow
    };
    return Rectangle.hasIntersectionBetweenTwoRect(rect1, rect2);
  }
  refresh() {
    var _a;
    (_a = this._rangeShape) == null ? void 0 : _a.hide();
    const { startNodePosition, endNodePosition, _document, _docSkeleton } = this;
    const documentOffsetConfig = _document.getOffsetConfig();
    const { docsLeft, docsTop } = documentOffsetConfig;
    const convertor = new NodePositionConvertToRectRange(documentOffsetConfig, _docSkeleton);
    const rectInfo = convertor.getRangePointData(startNodePosition, endNodePosition);
    if (rectInfo == null) {
      return;
    }
    const { pointGroup = [], startRow, endRow, startColumn, endColumn, tableId } = rectInfo;
    if ((pointGroup == null ? void 0 : pointGroup.length) > 0) {
      this._createOrUpdateRange(pointGroup, docsLeft, docsTop);
    }
    this._updateTableInfo(startRow, endRow, startColumn, endColumn, tableId);
  }
  _updateTableInfo(startRow, endRow, startCol, endCol, tableId) {
    this._startRow = startRow;
    this._endRow = endRow;
    this._startCol = startCol;
    this._endCol = endCol;
    this._tableId = tableId;
  }
  _createOrUpdateRange(pointsGroup, left, top) {
    var _a;
    if (this._rangeShape) {
      this._rangeShape.translate(left, top);
      this._rangeShape.updatePointGroup(pointsGroup);
      this._rangeShape.show();
      return;
    }
    const OPACITY = 0.3;
    const polygon = new RegularPolygon(RECT_RANGE_KEY_PREFIX + generateRandomId(ID_LENGTH2), {
      pointsGroup,
      fill: ((_a = this.style) == null ? void 0 : _a.fill) || getColor(COLORS.black, OPACITY),
      left,
      top,
      evented: false,
      debounceParentDirty: false
    });
    this._rangeShape = polygon;
    this._scene.addObject(polygon, TEXT_RANGE_LAYER_INDEX);
  }
};

// ../packages/docs-ui/src/services/selection/selection-utils.ts
function getTextRangeFromCharIndex(startOffset, endOffset, scene, document2, skeleton, style, segmentId, segmentPage, startIsBack = true, endIsBack = true) {
  const startNodePosition = skeleton.findNodePositionByCharIndex(startOffset, startIsBack, segmentId, segmentPage);
  const endNodePosition = skeleton.findNodePositionByCharIndex(endOffset, endIsBack, segmentId, segmentPage);
  if (startNodePosition == null || endNodePosition == null) {
    return;
  }
  return new TextRange(scene, document2, skeleton, startNodePosition, endNodePosition, style, segmentId, segmentPage);
}
function getRectRangeFromCharIndex(startOffset, endOffset, scene, document2, skeleton, style, segmentId, segmentPage) {
  const startNodePosition = skeleton.findNodePositionByCharIndex(startOffset, true, segmentId, segmentPage);
  const endNodePosition = skeleton.findNodePositionByCharIndex(endOffset, true, segmentId, segmentPage);
  if (startNodePosition == null || endNodePosition == null) {
    return;
  }
  return new RectRange(scene, document2, skeleton, startNodePosition, endNodePosition, style, segmentId, segmentPage);
}
function getRangeListFromCharIndex(startOffset, endOffset, scene, document2, skeleton, style, segmentId, segmentPage) {
  const startNodePosition = skeleton.findNodePositionByCharIndex(startOffset, true, segmentId, segmentPage);
  const endNodePosition = skeleton.findNodePositionByCharIndex(endOffset, true, segmentId, segmentPage);
  if (startNodePosition == null || endNodePosition == null) {
    return;
  }
  return getRangeListFromSelection(
    startNodePosition,
    endNodePosition,
    scene,
    document2,
    skeleton,
    style,
    segmentId,
    segmentPage
  );
}
function getRangeListFromSelection(anchorPosition, focusPosition, scene, document2, skeleton, style, segmentId, segmentPage) {
  const textRanges = [];
  const rectRanges = [];
  const rangeParams = [scene, document2, skeleton, anchorPosition, focusPosition, style, segmentId, segmentPage];
  if (isInSameTableCellData(skeleton, anchorPosition, focusPosition)) {
    if (isInSameTableCell(anchorPosition, focusPosition)) {
      textRanges.push(new TextRange(...rangeParams));
      return {
        textRanges,
        rectRanges
      };
    } else {
      const ranges = convertPositionsToRectRanges(
        ...rangeParams
      );
      rectRanges.push(...ranges);
      return {
        textRanges,
        rectRanges
      };
    }
  }
  if (isValidRectRange(anchorPosition, focusPosition)) {
    const ranges = convertPositionsToRectRanges(
      ...rangeParams
    );
    rectRanges.push(...ranges);
    return {
      textRanges,
      rectRanges
    };
  }
  const viewModel = skeleton.getViewModel().getSelfOrHeaderFooterViewModel(segmentId);
  const anchorOffset = skeleton.findCharIndexByPosition(anchorPosition);
  const focusOffset = skeleton.findCharIndexByPosition(focusPosition);
  if (anchorOffset == null || focusOffset == null) {
    return;
  }
  const direction = anchorOffset <= focusOffset ? "forward" /* FORWARD */ : "backward" /* BACKWARD */;
  const startOffset = Math.min(anchorOffset, focusOffset);
  const endOffset = Math.max(anchorOffset, focusOffset);
  const originRange = compareNodePosition(anchorPosition, focusPosition);
  const findStartNodePositionByCharIndex = (charIndex, isBack = true) => {
    var _a;
    return (_a = skeleton.findNodePositionByCharIndex(charIndex, isBack, segmentId, segmentPage)) != null ? _a : charIndex === startOffset ? originRange.start : void 0;
  };
  const findEndNodePositionByCharIndex = (charIndex, isBack = true) => {
    var _a;
    return (_a = skeleton.findNodePositionByCharIndex(charIndex, isBack, segmentId, segmentPage)) != null ? _a : charIndex === endOffset ? originRange.end : void 0;
  };
  let start = startOffset;
  let end = endOffset;
  for (const section of viewModel.getChildren()) {
    for (const paragraph of section.children) {
      const { startIndex, endIndex, children } = paragraph;
      const paragraphIndex = section.children.indexOf(paragraph);
      const nextParagraph = section.children[paragraphIndex + 1];
      const table = children[0];
      let endInTable = false;
      if (table) {
        const { startIndex: tableStart, endIndex: tableEnd, children: children2 } = table;
        let tableStartPosition = null;
        let tableEndPosition = null;
        const startRow = children2.find((row) => row.startIndex <= startOffset && row.endIndex >= startOffset);
        const endRow = children2.find((row) => row.startIndex <= endOffset && row.endIndex >= endOffset);
        if (startOffset > tableStart && startOffset < tableEnd) {
          tableStartPosition = skeleton.findNodePositionByCharIndex(startRow.startIndex + 2, true, segmentId, segmentPage);
          tableEndPosition = skeleton.findNodePositionByCharIndex(tableEnd - 4, true, segmentId, segmentPage);
          start = tableEnd + 1;
        } else if (endOffset > tableStart && endOffset < tableEnd) {
          tableStartPosition = skeleton.findNodePositionByCharIndex(tableStart + 3, true, segmentId, segmentPage);
          tableEndPosition = skeleton.findNodePositionByCharIndex(endRow.endIndex - 3, true, segmentId, segmentPage);
          end = tableStart - 1;
          endInTable = true;
        } else if (tableStart > startOffset && tableEnd < endOffset) {
          tableStartPosition = skeleton.findNodePositionByCharIndex(tableStart + 3, true, segmentId, segmentPage);
          tableEndPosition = skeleton.findNodePositionByCharIndex(tableEnd - 4, true, segmentId, segmentPage);
          if (start <= tableStart - 1) {
            const sp = findStartNodePositionByCharIndex(start, true);
            const ep = findEndNodePositionByCharIndex(tableStart - 1, false);
            const ap = direction === "forward" /* FORWARD */ ? sp : ep;
            const fp = direction === "forward" /* FORWARD */ ? ep : sp;
            textRanges.push(new TextRange(scene, document2, skeleton, ap, fp, style, segmentId, segmentPage));
          }
          start = tableEnd + 1;
        }
        if (tableStartPosition && tableEndPosition) {
          const ap = direction === "forward" /* FORWARD */ ? tableStartPosition : tableEndPosition;
          const fp = direction === "forward" /* FORWARD */ ? tableEndPosition : tableStartPosition;
          rectRanges.push(...convertPositionsToRectRanges(
            scene,
            document2,
            skeleton,
            ap,
            fp,
            style,
            segmentId,
            segmentPage
          ));
        }
      }
      if (end === endIndex + 1 && !endInTable && nextParagraph && nextParagraph.children.length) {
        end = endIndex;
        endInTable = true;
      }
      if (end >= startIndex && end <= endIndex || endInTable) {
        const sp = findStartNodePositionByCharIndex(start, true);
        const ep = findEndNodePositionByCharIndex(end, !endInTable);
        const ap = direction === "forward" /* FORWARD */ ? sp : ep;
        const fp = direction === "forward" /* FORWARD */ ? ep : sp;
        if (rectRanges.length && Tools.diffValue(ap, fp)) {
          continue;
        }
        textRanges.push(new TextRange(scene, document2, skeleton, ap, fp, style, segmentId, segmentPage));
      }
    }
  }
  return {
    textRanges,
    rectRanges
  };
}
function getCanvasOffsetByEngine(engine) {
  const canvas = engine == null ? void 0 : engine.getCanvasElement();
  if (!canvas) {
    return {
      left: 0,
      top: 0
    };
  }
  const { top, left } = getOffsetRectForDom(canvas);
  return {
    left,
    top
  };
}
function getParagraphInfoByGlyph(node) {
  var _a;
  const line = (_a = node.parent) == null ? void 0 : _a.parent;
  const column = line == null ? void 0 : line.parent;
  if (line == null || column == null) {
    return;
  }
  const { paragraphIndex } = line;
  const lines = column.lines.filter((line2) => line2.paragraphIndex === paragraphIndex);
  let nodeIndex = -1;
  let content = "";
  let hasFound = false;
  for (const line2 of lines) {
    for (const divide of line2.divides) {
      for (const glyph of divide.glyphGroup) {
        if (!hasFound) {
          nodeIndex += glyph.count;
        }
        if (glyph === node) {
          hasFound = true;
        }
        content += glyph.count > 0 ? glyph.content : "";
      }
    }
  }
  return {
    st: lines[0].st,
    ed: paragraphIndex,
    content,
    nodeIndex
  };
}
function serializeTextRange(textRange) {
  const { startOffset, endOffset, collapsed, rangeType, startNodePosition, endNodePosition, direction, segmentId, segmentPage } = textRange;
  const serializedTextRange = {
    startOffset,
    endOffset,
    collapsed,
    rangeType,
    startNodePosition,
    endNodePosition,
    direction,
    segmentId,
    segmentPage,
    isActive: textRange.isActive()
  };
  return serializedTextRange;
}
function serializeRectRange(rectRange) {
  const serializedTextRange = serializeTextRange(rectRange);
  const {
    startRow,
    startColumn,
    endRow,
    endColumn,
    tableId,
    spanEntireRow,
    spanEntireColumn,
    spanEntireTable
  } = rectRange;
  return {
    ...serializedTextRange,
    startRow,
    startColumn,
    endRow,
    endColumn,
    tableId,
    spanEntireRow,
    spanEntireColumn,
    spanEntireTable
  };
}

// ../packages/docs-ui/src/services/selection/doc-selection-render.service.ts
var DocSelectionRenderService = class extends RxDisposable {
  constructor(_context, _layoutService, _logService, _univerInstanceService, _docSkeletonManagerService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_layoutService", _layoutService);
    __publicField(this, "_logService", _logService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_onInputBefore$", new Subject());
    __publicField(this, "onInputBefore$", this._onInputBefore$.asObservable());
    __publicField(this, "_onKeydown$", new Subject());
    __publicField(this, "onKeydown$", this._onKeydown$.asObservable());
    __publicField(this, "_onInput$", new Subject());
    __publicField(this, "onInput$", this._onInput$.asObservable());
    __publicField(this, "_onCompositionstart$", new BehaviorSubject(null));
    __publicField(this, "onCompositionstart$", this._onCompositionstart$.asObservable());
    __publicField(this, "_onCompositionupdate$", new BehaviorSubject(null));
    __publicField(this, "onCompositionupdate$", this._onCompositionupdate$.asObservable());
    __publicField(this, "_onCompositionend$", new BehaviorSubject(null));
    __publicField(this, "onCompositionend$", this._onCompositionend$.asObservable());
    __publicField(this, "_onSelectionStart$", new BehaviorSubject(null));
    __publicField(this, "onSelectionStart$", this._onSelectionStart$.asObservable());
    __publicField(this, "onChangeByEvent$", merge(
      this._onInput$,
      this._onKeydown$.pipe(filter((e) => e.event.keyCode === 8 /* BACKSPACE */)),
      this._onCompositionend$
    ));
    __publicField(this, "_onPaste$", new Subject());
    __publicField(this, "onPaste$", this._onPaste$.asObservable());
    __publicField(this, "_textSelectionInner$", new BehaviorSubject(null));
    __publicField(this, "textSelectionInner$", this._textSelectionInner$.asObservable());
    __publicField(this, "_onFocus$", new Subject());
    __publicField(this, "onFocus$", this._onFocus$.asObservable());
    __publicField(this, "_onBlur$", new Subject());
    __publicField(this, "onBlur$", this._onBlur$.asObservable());
    __publicField(this, "_onPointerDown$", new Subject());
    __publicField(this, "onPointerDown$", this._onPointerDown$.asObservable());
    __publicField(this, "_container");
    __publicField(this, "_inputParent");
    __publicField(this, "_input");
    __publicField(this, "_scrollTimers", []);
    __publicField(this, "_rangeList", []);
    // Use to cache range list in moving.
    __publicField(this, "_rangeListCache", []);
    // Rect range list.
    __publicField(this, "_rectRangeList", []);
    // Use to cache rect range list in moving.
    __publicField(this, "_rectRangeListCache", []);
    __publicField(this, "_anchorNodePosition", null);
    __publicField(this, "_focusNodePosition", null);
    __publicField(this, "_currentSegmentId", "");
    __publicField(this, "_currentSegmentPage", -1);
    __publicField(this, "_selectionStyle", NORMAL_TEXT_SELECTION_PLUGIN_STYLE);
    __publicField(this, "_onPointerEvent", false);
    __publicField(this, "_viewPortObserverMap", /* @__PURE__ */ new Map());
    __publicField(this, "_isIMEInputApply", false);
    __publicField(this, "_scenePointerMoveSubs", []);
    __publicField(this, "_scenePointerUpSubs", []);
    // When the user switches editors, whether to clear the doc ranges.
    __publicField(this, "_reserveRanges", false);
    this._initDOM();
    this._registerContainer();
    this._setSystemHighlightColorToStyle();
    this._listenCurrentUnitChange();
  }
  get isOnPointerEvent() {
    return this._onPointerEvent;
  }
  get isFocusing() {
    return this._input === document.activeElement;
  }
  get canFocusing() {
    return this.isFocusing || document.activeElement === document.body || document.activeElement === null;
  }
  _listenCurrentUnitChange() {
    this._univerInstanceService.getCurrentTypeOfUnit$(1 /* UNIVER_DOC */).pipe(takeUntil(this.dispose$)).subscribe((documentModel) => {
      if (documentModel == null) {
        return;
      }
      const unitId = documentModel.getUnitId();
      if (unitId !== this._context.unitId && !this._reserveRanges) {
        this.removeAllRanges();
      }
    });
  }
  get activeViewPort() {
    return this._context.scene.getViewports()[0];
  }
  setSegment(id) {
    this._currentSegmentId = id;
  }
  getSegment() {
    return this._currentSegmentId;
  }
  setSegmentPage(pageIndex) {
    this._currentSegmentPage = pageIndex;
  }
  getSegmentPage() {
    return this._currentSegmentPage;
  }
  setReserveRangesStatus(status) {
    this._reserveRanges = status;
  }
  _setRangeStyle(style = NORMAL_TEXT_SELECTION_PLUGIN_STYLE) {
    this._selectionStyle = style;
  }
  // eslint-disable-next-line max-lines-per-function
  addDocRanges(ranges, isEditing = true, options) {
    const {
      _currentSegmentId: segmentId,
      _currentSegmentPage: segmentPage,
      _selectionStyle: style
    } = this;
    const { scene, mainComponent } = this._context;
    const document2 = mainComponent;
    const docSkeleton = this._docSkeletonManagerService.getSkeleton();
    const generalAddRange = (startOffset, endOffset) => {
      const rangeList = getRangeListFromCharIndex(
        startOffset,
        endOffset,
        scene,
        document2,
        docSkeleton,
        style,
        segmentId,
        segmentPage
      );
      if (rangeList == null) {
        return;
      }
      const { textRanges, rectRanges } = rangeList;
      for (const textRange of textRanges) {
        this._addTextRange(textRange);
      }
      this._addRectRanges(rectRanges);
    };
    for (const range of ranges) {
      const { startOffset, endOffset, rangeType, startNodePosition, endNodePosition } = range;
      if (rangeType === "RECT" /* RECT */) {
        const rectRange = getRectRangeFromCharIndex(
          startOffset,
          endOffset,
          scene,
          document2,
          docSkeleton,
          style,
          segmentId,
          segmentPage
        );
        if (rectRange) {
          this._addRectRanges([rectRange]);
        }
      } else if (rangeType === "TEXT" /* TEXT */) {
        try {
          let textRange = null;
          if (startNodePosition && endNodePosition) {
            textRange = getTextRangeFromCharIndex(
              startNodePosition.isBack ? startOffset : startOffset - 1,
              endNodePosition.isBack ? endOffset : endOffset - 1,
              scene,
              document2,
              docSkeleton,
              style,
              segmentId,
              segmentPage,
              startNodePosition.isBack,
              endNodePosition.isBack
            );
          } else {
            textRange = getTextRangeFromCharIndex(
              startOffset,
              endOffset,
              scene,
              document2,
              docSkeleton,
              style,
              segmentId,
              segmentPage
            );
          }
          if (textRange) {
            this._addTextRange(textRange);
          }
        } catch (_e) {
          generalAddRange(startOffset, endOffset);
        }
      } else {
        generalAddRange(startOffset, endOffset);
      }
    }
    this._textSelectionInner$.next({
      textRanges: this._getAllTextRanges(),
      rectRanges: this._getAllRectRanges(),
      segmentId,
      segmentPage,
      style,
      isEditing,
      options
    });
    if (!ranges.length || (options == null ? void 0 : options.shouldFocus) === false) return;
    this._updateInputPosition(options == null ? void 0 : options.forceFocus);
  }
  setCursorManually(evtOffsetX, evtOffsetY) {
    const startNode = this._findNodeByCoord(evtOffsetX, evtOffsetY, {
      strict: true,
      segmentId: this._currentSegmentId,
      segmentPage: this._currentSegmentPage
    });
    const position = this._getNodePosition(startNode);
    if (position == null) {
      this._removeAllRanges();
      return;
    }
    if ((startNode == null ? void 0 : startNode.node.streamType) === "\r" /* PARAGRAPH */) {
      position.isBack = true;
    }
    this._createTextRangeByAnchorPosition(position);
    this._textSelectionInner$.next({
      textRanges: this._getAllTextRanges(),
      rectRanges: this._getAllRectRanges(),
      segmentId: this._currentSegmentId,
      segmentPage: this._currentSegmentPage,
      style: this._selectionStyle,
      isEditing: false
    });
  }
  // Sync canvas selection to dom selection.
  sync() {
    this._updateInputPosition();
  }
  /**
   * @deprecated
   */
  activate(x, y, force = false) {
    this._ensureHostContainer();
    let left = x;
    let top = y;
    const fixedContainer = this._container.parentElement;
    if (fixedContainer) {
      const rect = fixedContainer.getBoundingClientRect();
      left -= rect.left;
      top -= rect.top;
    }
    this._container.style.left = `${left}px`;
    this._container.style.top = `${top}px`;
    this._container.style.zIndex = "1000";
    if (this.canFocusing || force) {
      this.focus();
    }
  }
  hasFocus() {
    return document.activeElement === this._input;
  }
  focus() {
    this._input.focus();
  }
  blur() {
    this._input.blur();
  }
  // FIXME: for editor cell editor we don't need to blur the input element
  /**
   * @deprecated
   */
  deactivate() {
    this._container.style.left = "0px";
    this._container.style.top = "0px";
  }
  // Handler double click.
  __handleDblClick(evt) {
    const { offsetX: evtOffsetX, offsetY: evtOffsetY } = evt;
    const startNode = this._findNodeByCoord(evtOffsetX, evtOffsetY, {
      strict: false,
      segmentId: this._currentSegmentId,
      segmentPage: this._currentSegmentPage
    });
    if (startNode == null || startNode.node == null) {
      return;
    }
    const paragraphInfo = getParagraphInfoByGlyph(startNode.node);
    if (paragraphInfo == null) {
      return;
    }
    const { content, st, nodeIndex } = paragraphInfo;
    if (nodeIndex === -1) {
      return;
    }
    const segmenter = new Intl.Segmenter(void 0, { granularity: "word" });
    const segments = segmenter.segment(content);
    let startOffset = Number.NEGATIVE_INFINITY;
    let endOffset = Number.NEGATIVE_INFINITY;
    for (const { segment, index, isWordLike } of segments) {
      if (index <= nodeIndex && nodeIndex < index + segment.length && isWordLike) {
        startOffset = index + st;
        endOffset = index + st + segment.length;
        break;
      }
    }
    if (Number.isFinite(startOffset) && Number.isFinite(endOffset)) {
      this.removeAllRanges();
      const textRanges = [
        {
          startOffset,
          endOffset
        }
      ];
      this.addDocRanges(textRanges, false, { forceFocus: true });
    }
  }
  __handleTripleClick(evt) {
    const { offsetX: evtOffsetX, offsetY: evtOffsetY } = evt;
    const startNode = this._findNodeByCoord(evtOffsetX, evtOffsetY, {
      strict: false,
      segmentId: this._currentSegmentId,
      segmentPage: this._currentSegmentPage
    });
    if (startNode == null || startNode.node == null) {
      return;
    }
    const paragraphInfo = getParagraphInfoByGlyph(startNode.node);
    if (paragraphInfo == null) {
      return;
    }
    this.removeAllRanges();
    const { st, ed } = paragraphInfo;
    const textRanges = [
      {
        startOffset: st,
        endOffset: ed
      }
    ];
    this.addDocRanges(textRanges, false, { forceFocus: true });
  }
  // Handle pointer down.
  // eslint-disable-next-line max-lines-per-function, complexity
  __onPointDown(evt) {
    var _a, _b;
    const { scene, mainComponent } = this._context;
    const skeleton = this._docSkeletonManagerService.getSkeleton();
    const { offsetX: evtOffsetX, offsetY: evtOffsetY } = evt;
    const startNode = this._findNodeByCoord(evtOffsetX, evtOffsetY, {
      strict: false,
      segmentId: this._currentSegmentId,
      segmentPage: this._currentSegmentPage
    });
    const position = this._getNodePosition(startNode);
    if (position == null || startNode == null) {
      this._removeAllRanges();
      return;
    }
    if ((startNode == null ? void 0 : startNode.node.streamType) === "\r" /* PARAGRAPH */) {
      position.isBack = true;
    }
    const docSelection = this._textSelectionInner$.value;
    if (startNode && evt.button === 2 && docSelection) {
      const nodeCharIndex = skeleton.findCharIndexByPosition(position);
      if (typeof nodeCharIndex === "number" && docSelection.textRanges.some((textRange) => textRange.startOffset <= nodeCharIndex && textRange.endOffset > nodeCharIndex)) {
        return;
      }
      if (typeof nodeCharIndex === "number" && docSelection.rectRanges.some((rectRange) => rectRange.startOffset <= nodeCharIndex && rectRange.endOffset >= nodeCharIndex)) {
        return;
      }
    }
    const { segmentId, segmentPage } = startNode;
    if (segmentId && this._currentSegmentId && segmentId !== this._currentSegmentId) {
      this.setSegment(segmentId);
    }
    if (segmentId && segmentPage !== this._currentSegmentPage) {
      this.setSegmentPage(segmentPage);
    }
    this._anchorNodePosition = position;
    if (evt.shiftKey && this._getActiveRangeInstance()) {
      this._updateActiveRangePosition(position);
    } else if (evt.ctrlKey) {
      this._removeAllCollapsedTextRanges();
    } else if (!this._isEmpty()) {
      this._removeAllRanges();
    }
    scene.disableObjectsEvent();
    const scrollTimer = ScrollTimer.create(scene);
    this._scrollTimers.push(scrollTimer);
    scrollTimer.startScroll(evtOffsetX, evtOffsetY);
    this._onSelectionStart$.next((_a = this._getActiveRangeInstance()) == null ? void 0 : _a.startNodePosition);
    (_b = scene.getTransformer()) == null ? void 0 : _b.clearSelectedObjects();
    let preMoveOffsetX = evtOffsetX;
    let preMoveOffsetY = evtOffsetY;
    this._onPointerEvent = true;
    this._scenePointerMoveSubs.push(scene.onPointerMove$.subscribeEvent((moveEvt) => {
      const { offsetX: moveOffsetX, offsetY: moveOffsetY } = moveEvt;
      scene.setCursor("text" /* TEXT */);
      if (Math.sqrt((moveOffsetX - preMoveOffsetX) ** 2 + (moveOffsetY - preMoveOffsetY) ** 2) < 3) {
        return;
      }
      this._moving(moveOffsetX, moveOffsetY);
      scrollTimer.scrolling(moveOffsetX, moveOffsetY, () => {
        this._moving(moveOffsetX, moveOffsetY);
      });
      preMoveOffsetX = moveOffsetX;
      preMoveOffsetY = moveOffsetY;
    }));
    this._scenePointerUpSubs.push(scene.onPointerUp$.subscribeEvent(() => {
      [...this._scenePointerMoveSubs, ...this._scenePointerUpSubs].forEach((e) => {
        e.unsubscribe();
      });
      this._onPointerEvent = false;
      scene.enableObjectsEvent();
      if (this._anchorNodePosition && !this._focusNodePosition) {
        if (evt.ctrlKey) {
          this._disposeScrollTimers();
          return;
        }
        const textRange = new TextRange(scene, mainComponent, skeleton, this._anchorNodePosition, void 0, this._selectionStyle, this._currentSegmentId, this._currentSegmentPage);
        this._addTextRange(textRange);
      } else if (this._anchorNodePosition && this._focusNodePosition) {
        for (const textRange of this._rangeListCache) {
          if (evt.ctrlKey) {
            if (textRange.collapsed) {
              textRange.dispose();
            } else {
              this._addTextRange(textRange);
            }
          } else {
            this._addTextRange(textRange);
          }
        }
        this._addRectRanges(this._rectRangeListCache);
        this._rangeListCache = [];
        this._rectRangeListCache = [];
      }
      this._anchorNodePosition = null;
      this._focusNodePosition = null;
      const selectionInfo = {
        textRanges: this._getAllTextRanges(),
        rectRanges: this._getAllRectRanges(),
        segmentId: this._currentSegmentId,
        segmentPage: this._currentSegmentPage,
        style: this._selectionStyle,
        isEditing: false
      };
      this._textSelectionInner$.next(selectionInfo);
      this._disposeScrollTimers();
      this._updateInputPosition(true);
    }));
  }
  removeAllRanges() {
    this._removeAllRanges();
    this.deactivate();
  }
  getActiveTextRange() {
    return this._getActiveRangeInstance();
  }
  _disposeScrollTimers() {
    this._scrollTimers.forEach((timer) => {
      timer == null ? void 0 : timer.dispose();
    });
    this._scrollTimers = [];
  }
  _setSystemHighlightColorToStyle() {
    const { r, g, b, a } = getSystemHighlightColor();
    const style = {
      strokeWidth: 1,
      stroke: "rgba(0, 0, 0, 0)",
      strokeActive: "rgba(0, 0, 0, 1)",
      fill: `rgba(${r}, ${g}, ${b}, ${a != null ? a : 0.3})`
    };
    this._setRangeStyle(style);
  }
  _getAllTextRanges() {
    return this._rangeList.map(serializeTextRange);
  }
  _getAllRectRanges() {
    return this._rectRangeList.map(serializeRectRange);
  }
  getAllTextRanges() {
    return this._getAllTextRanges();
  }
  getAllRectRanges() {
    return this._getAllRectRanges();
  }
  _getActiveRange() {
    const activeRange = this._rangeList.find((range) => range.isActive());
    if (activeRange == null) {
      return null;
    }
    const { startOffset, endOffset } = activeRange;
    if (startOffset == null || endOffset == null) {
      return null;
    }
    return serializeTextRange(activeRange);
  }
  _getActiveRangeInstance() {
    return this._rangeList.find((range) => range.isActive());
  }
  dispose() {
    super.dispose();
    this._detachEvent();
    this._removeAllRanges();
    this._container.remove();
  }
  _initDOM() {
    const { unitId } = this._context;
    const container = document.createElement("div");
    container.style.position = "fixed";
    container.style.left = "0px";
    container.style.top = "0px";
    container.id = `univer-doc-selection-container-${unitId}`;
    const inputParent = document.createElement("div");
    const inputDom = document.createElement("div");
    inputParent.appendChild(inputDom);
    container.appendChild(inputParent);
    this._container = container;
    this._inputParent = inputParent;
    this._input = inputDom;
    this._initInput();
    this._initInputEvents();
    const host = this._layoutService.rootContainerElement;
    const hostElement = (host == null ? void 0 : host.isConnected) ? host : document.body;
    hostElement.appendChild(container);
  }
  _registerContainer() {
    this.disposeWithMe(
      // the content editable div should be regarded as part of the applications container
      this._layoutService.registerContainerElement(this._container)
    );
  }
  _initInput() {
    this._inputParent.style.cssText = `
            position:absolute;
            height:1px;
            width:1px;
            overflow: hidden;
        `;
    this._input.contentEditable = "true";
    this._input.dataset.uComp = "editor";
    this._input.id = `__editor_${this._context.unitId}`;
    this._input.style.cssText = `
            position: absolute;
            overflow: hidden;
            opacity: 1;
            background: #000;
            color: transparent;
            outline: none;
            z-index: -2;
            caret-color: transparent;
            white-space: pre-wrap;
            user-select: text;
        `;
  }
  _ensureHostContainer() {
    const host = this._layoutService.rootContainerElement;
    if (host == null ? void 0 : host.isConnected) {
      if (this._container.parentElement !== host) {
        host.appendChild(this._container);
      }
      return;
    }
    if (this._container.parentElement !== document.body) {
      document.body.appendChild(this._container);
    }
  }
  _getNodePosition(node) {
    if (node == null) {
      return;
    }
    const { node: glyph, ratioX, segmentPage } = node;
    const skeleton = this._docSkeletonManagerService.getSkeleton();
    const position = skeleton.findPositionByGlyph(glyph, segmentPage);
    if (position == null) {
      return;
    }
    const HALF = 0.5;
    let isBack = ratioX < HALF;
    if (glyph.glyphType === 2 /* LIST */) {
      isBack = true;
    }
    return {
      ...position,
      isBack
    };
  }
  _interactTextRanges(textRanges) {
    const newTextRanges = [];
    for (const range of this._rangeList) {
      if (textRanges.some((textRange) => textRange.isIntersection(range))) {
        range.dispose();
        continue;
      }
      newTextRanges.push(range);
    }
    this._rangeList = newTextRanges;
  }
  _interactRectRanges(rectRanges) {
    const newRanges = [];
    for (const range of this._rectRangeList) {
      if (rectRanges.some((rectRange) => rectRange.isIntersection(range))) {
        range.dispose();
        continue;
      }
      newRanges.push(range);
    }
    this._rectRangeList = newRanges;
  }
  _removeAllRanges() {
    this._removeAllTextRanges();
    this._removeAllRectRanges();
  }
  _removeAllCacheRanges() {
    this._rangeListCache.forEach((range) => {
      range.dispose();
    });
    this._rectRangeListCache.forEach((range) => {
      range.dispose();
    });
    this._rangeListCache = [];
    this._rectRangeListCache = [];
  }
  _removeAllTextRanges() {
    this._rangeList.forEach((range) => {
      range.dispose();
    });
    this._rangeList = [];
  }
  _removeAllRectRanges() {
    this._rectRangeList.forEach((range) => {
      range.dispose();
    });
    this._rectRangeList = [];
  }
  _removeAllCollapsedTextRanges() {
    const newRanges = [];
    for (const range of this._rangeList) {
      if (range.collapsed) {
        range.dispose();
        continue;
      }
      newRanges.push(range);
    }
    this._rangeList = newRanges;
  }
  _deactivateAllTextRanges() {
    this._rangeList.forEach((range) => {
      range.deactivate();
    });
  }
  _deactivateAllRectRanges() {
    this._rectRangeList.forEach((range) => {
      range.deactivate();
    });
  }
  _addTextRangesToCache(textRanges) {
    this._rangeListCache.push(...textRanges);
  }
  _addTextRange(textRange) {
    this._deactivateAllTextRanges();
    textRange.activate();
    this._rangeList.push(textRange);
  }
  _addRectRangesToCache(rectRanges) {
    this._rectRangeListCache.push(...rectRanges);
  }
  _addRectRanges(rectRanges) {
    if (rectRanges.length === 0) {
      return;
    }
    this._deactivateAllRectRanges();
    rectRanges[rectRanges.length - 1].activate();
    this._rectRangeList.push(...rectRanges);
  }
  _createTextRangeByAnchorPosition(position) {
    this._removeAllRanges();
    const { scene, mainComponent } = this._context;
    const skeleton = this._docSkeletonManagerService.getSkeleton();
    const lastRange = new TextRange(scene, mainComponent, skeleton, position, void 0, this._selectionStyle, this._currentSegmentId, this._currentSegmentPage);
    this._addTextRange(lastRange);
  }
  _updateActiveRangePosition(position) {
    const activeTextRange = this._getActiveRangeInstance();
    if (activeTextRange == null || activeTextRange.anchorNodePosition == null) {
      this._logService.error(
        "[DocSelectionRenderService] _updateActiveRangeFocusPosition: active range has no anchor"
      );
      return;
    }
    this._removeAllRanges();
    this._anchorNodePosition = activeTextRange.anchorNodePosition;
    this._focusNodePosition = position;
    const { scene, mainComponent } = this._context;
    const skeleton = this._docSkeletonManagerService.getSkeleton();
    const { _anchorNodePosition, _focusNodePosition, _selectionStyle, _currentSegmentId, _currentSegmentPage } = this;
    if (_anchorNodePosition == null || _focusNodePosition == null || mainComponent == null) {
      return;
    }
    const ranges = getRangeListFromSelection(
      _anchorNodePosition,
      _focusNodePosition,
      scene,
      mainComponent,
      skeleton,
      _selectionStyle,
      _currentSegmentId,
      _currentSegmentPage
    );
    if (ranges == null) {
      return;
    }
    const { textRanges, rectRanges } = ranges;
    this._addTextRangesToCache(textRanges);
    this._addRectRangesToCache(rectRanges);
    this.deactivate();
  }
  _isEmpty() {
    return this._rangeList.length === 0 && this._rectRangeList.length === 0;
  }
  _getCanvasOffset() {
    var _a;
    const engine = (_a = this._context.scene) == null ? void 0 : _a.getEngine();
    return getCanvasOffsetByEngine(engine);
  }
  _updateInputPosition(forceFocus = false) {
    const activeRangeInstance = this._getActiveRangeInstance();
    const anchor = activeRangeInstance == null ? void 0 : activeRangeInstance.getAnchor();
    if (!anchor || anchor && !anchor.visible || this.activeViewPort == null) {
      this.focus();
      return;
    }
    const { left, top } = anchor;
    const absoluteCoord = this.activeViewPort.getAbsoluteVector(Vector2.FromArray([left, top]));
    const { x, y } = absoluteCoord;
    let { left: canvasLeft, top: canvasTop } = this._getCanvasOffset();
    canvasLeft += x;
    canvasTop += y;
    this.activate(canvasLeft, canvasTop, forceFocus);
  }
  _moving(moveOffsetX, moveOffsetY) {
    var _a, _b;
    const { _currentSegmentId: segmentId, _currentSegmentPage: segmentPage } = this;
    const focusNode = this._findNodeByCoord(moveOffsetX, moveOffsetY, {
      strict: true,
      segmentId,
      segmentPage
    });
    const focusNodePosition = this._getNodePosition(focusNode);
    if (focusNodePosition == null || focusNode == null) {
      return;
    }
    const divide = focusNode == null ? void 0 : focusNode.node.parent;
    const nextGlyph = divide == null ? void 0 : divide.glyphGroup[divide.glyphGroup.indexOf(focusNode.node) + 1];
    if ((focusNode == null ? void 0 : focusNode.node.streamType) === "\r" /* PARAGRAPH */ && (nextGlyph == null ? void 0 : nextGlyph.streamType) === "\n" /* SECTION_BREAK */) {
      focusNodePosition.isBack = true;
    }
    if (this._shouldSnapBackwardFocusToGlyphStart(focusNode, focusNodePosition)) {
      focusNodePosition.isBack = true;
    }
    const { _anchorNodePosition, _selectionStyle } = this;
    const { scene, mainComponent } = this._context;
    const skeleton = this._docSkeletonManagerService.getSkeleton();
    if (_anchorNodePosition == null || mainComponent == null) {
      return;
    }
    const ranges = getRangeListFromSelection(
      _anchorNodePosition,
      focusNodePosition,
      scene,
      mainComponent,
      skeleton,
      _selectionStyle,
      segmentId,
      segmentPage
    );
    if (ranges == null) {
      return;
    }
    const { textRanges, rectRanges } = ranges;
    if (!this._hasVisibleSelectionRanges(textRanges, rectRanges)) {
      textRanges.forEach((range) => range.dispose());
      rectRanges.forEach((range) => range.dispose());
      return;
    }
    this._focusNodePosition = focusNodePosition;
    this._removeAllCacheRanges();
    if (this._rangeList.length > 0 && textRanges.length > 0) {
      this._interactTextRanges(textRanges);
    }
    if (this._rectRangeList.length > 0 && rectRanges.length > 0) {
      this._interactRectRanges(rectRanges);
    }
    this._addTextRangesToCache(textRanges);
    this._addRectRangesToCache(rectRanges);
    this.deactivate();
    (_b = (_a = this._context.scene) == null ? void 0 : _a.getEngine()) == null ? void 0 : _b.setCapture();
  }
  _hasVisibleSelectionRanges(textRanges, rectRanges) {
    return rectRanges.length > 0 || textRanges.some((range) => !range.collapsed);
  }
  _shouldSnapBackwardFocusToGlyphStart(focusNode, focusNodePosition) {
    const anchorNodePosition = this._anchorNodePosition;
    if (!anchorNodePosition || !this._isBeforeNodePosition(focusNodePosition, anchorNodePosition)) {
      return false;
    }
    return this._isFirstSelectableGlyphInDivide(focusNode);
  }
  _isBeforeNodePosition(left, right) {
    if (!compareNodePositionLogic(left, right)) {
      return false;
    }
    return left.page !== right.page || left.section !== right.section || left.column !== right.column || left.line !== right.line || left.divide !== right.divide || left.glyph !== right.glyph;
  }
  _isFirstSelectableGlyphInDivide(nodeInfo) {
    var _a;
    const glyphGroup = (_a = nodeInfo.node.parent) == null ? void 0 : _a.glyphGroup;
    if (!glyphGroup) {
      return false;
    }
    const firstSelectableGlyph = glyphGroup.find((glyph) => glyph.content && glyph.glyphType !== 2 /* LIST */);
    return firstSelectableGlyph === nodeInfo.node;
  }
  __attachScrollEvent() {
    const viewport = this.activeViewPort;
    if (!viewport) {
      return;
    }
    const { unitId } = this._context;
    if (this._viewPortObserverMap.has(unitId)) {
      return;
    }
    const scrollBefore = viewport.onScrollAfter$.subscribeEvent((param) => {
      const viewport2 = param.viewport;
      if (!viewport2) {
        return;
      }
      const activeRangeInstance = this._getActiveRangeInstance();
      activeRangeInstance == null ? void 0 : activeRangeInstance.activeStatic();
    });
    const scrollStop = viewport.onScrollEnd$.subscribeEvent((param) => {
      const viewport2 = param.viewport;
      if (!viewport2) {
        return;
      }
      const bounds = viewport2.getBounding();
      const activeRangeInstance = this._getActiveRangeInstance();
      const anchor = activeRangeInstance == null ? void 0 : activeRangeInstance.getAnchor();
      if (!anchor || anchor && !anchor.visible) {
        return;
      }
      if (bounds) {
        const { left, top, right, bottom } = bounds.viewBound;
        if (anchor.left < left || anchor.left > right || anchor.top < top || anchor.top > bottom) {
          activeRangeInstance == null ? void 0 : activeRangeInstance.deactivateStatic();
          return;
        }
      }
      this._updateInputPosition();
    });
    this._viewPortObserverMap.set(unitId, {
      scrollBefore,
      scrollStop
    });
  }
  // FIXME: listeners here are not correctly disposed
  // eslint-disable-next-line max-lines-per-function
  _initInputEvents() {
    this.disposeWithMe(
      fromEvent(this._input, "keydown").subscribe((e) => {
        if (this._isIMEInputApply) {
          return;
        }
        this._eventHandle(e, (config) => {
          this._onKeydown$.next(config);
        });
      })
    );
    this.disposeWithMe(
      fromEvent(this._input, "input").subscribe((e) => {
        if (e.inputType === "historyUndo" || e.inputType === "historyRedo") {
          return;
        }
        if (this._rectRangeList.length > 0) {
          e.stopPropagation();
          return e.preventDefault();
        }
        if (this._isIMEInputApply) {
          return;
        }
        this._eventHandle(e, (config) => {
          this._onInputBefore$.next(config);
          this._onInput$.next(config);
        });
      })
    );
    this.disposeWithMe(
      fromEvent(this._input, "compositionstart").subscribe((e) => {
        if (this._rectRangeList.length > 0) {
          e.stopPropagation();
          return e.preventDefault();
        }
        this._isIMEInputApply = true;
        this._eventHandle(e, (config) => {
          this._onCompositionstart$.next(config);
        });
      })
    );
    this.disposeWithMe(
      fromEvent(this._input, "compositionend").subscribe((e) => {
        this._isIMEInputApply = false;
        this._eventHandle(e, (config) => {
          this._onCompositionend$.next(config);
        });
      })
    );
    this.disposeWithMe(
      fromEvent(this._input, "compositionupdate").subscribe((e) => {
        this._eventHandle(e, (config) => {
          this._onInputBefore$.next(config);
          this._onCompositionupdate$.next(config);
        });
      })
    );
    this.disposeWithMe(
      fromEvent(this._input, "paste").subscribe((e) => {
        this._eventHandle(e, (config) => {
          this._onPaste$.next(config);
        });
      })
    );
    this.disposeWithMe(
      fromEvent(this._input, "focus").subscribe((e) => {
        this._eventHandle(e, (config) => {
          this._onFocus$.next(config);
        });
      })
    );
    this.disposeWithMe(
      fromEvent(this._input, "blur").subscribe((e) => {
        this._eventHandle(e, (config) => {
          this._onBlur$.next(config);
        });
      })
    );
  }
  _eventHandle(e, func) {
    const content = this._input.textContent || "";
    this._input.innerHTML = "";
    const activeRange = this._getActiveRange();
    const rangeList = this._getAllTextRanges();
    func({
      event: e,
      content,
      activeRange,
      rangeList
    });
  }
  _getTransformCoordForDocumentOffset(evtOffsetX, evtOffsetY) {
    const document2 = this._context.mainComponent;
    const { documentTransform } = document2.getOffsetConfig();
    if (this.activeViewPort == null || documentTransform == null) {
      return;
    }
    const originCoord = this.activeViewPort.transformVector2SceneCoord(Vector2.FromArray([evtOffsetX, evtOffsetY]));
    if (!originCoord) {
      return;
    }
    return documentTransform.clone().invert().applyPoint(originCoord);
  }
  _findNodeByCoord(evtOffsetX, evtOffsetY, restrictions) {
    const coord = this._getTransformCoordForDocumentOffset(evtOffsetX, evtOffsetY);
    if (coord == null) {
      return;
    }
    const document2 = this._context.mainComponent;
    const skeleton = this._docSkeletonManagerService.getSkeleton();
    const {
      pageLayoutType = 0 /* VERTICAL */,
      pageMarginLeft,
      pageMarginTop
    } = document2.getOffsetConfig();
    const nodeInfo = skeleton.findNodeByCoord(
      coord,
      pageLayoutType,
      pageMarginLeft,
      pageMarginTop,
      restrictions
    );
    return nodeInfo;
  }
  _detachEvent() {
    this._onInputBefore$.complete();
    this._onKeydown$.complete();
    this._onInput$.complete();
    this._onCompositionstart$.complete();
    this._onCompositionupdate$.complete();
    this._onCompositionend$.complete();
    this._onSelectionStart$.complete();
    this._textSelectionInner$.complete();
    this._onPaste$.complete();
    this._onFocus$.complete();
    this._onBlur$.complete();
    this._onPointerDown$.complete();
  }
};
DocSelectionRenderService = __decorateClass([
  __decorateParam(1, ILayoutService),
  __decorateParam(2, ILogService),
  __decorateParam(3, IUniverInstanceService),
  __decorateParam(4, Inject(DocSkeletonManagerService))
], DocSelectionRenderService);

// ../packages/docs-ui/src/services/editor/editor.ts
var Editor = class extends Disposable {
  constructor(_param, _univerInstanceService, _docSelectionManagerService, _commandService, _undoRedoService, _injector) {
    super();
    __publicField(this, "_param", _param);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_docSelectionManagerService", _docSelectionManagerService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_undoRedoService", _undoRedoService);
    __publicField(this, "_injector", _injector);
    // Emit change event when editor lose focus.
    __publicField(this, "_change$", new Subject());
    __publicField(this, "change$", this._change$.asObservable());
    // The input event fires when the value of a editor has been changed as a direct result of a user action.
    __publicField(this, "_input$", new Subject());
    __publicField(this, "input$", this._input$.asObservable());
    // paste event.
    __publicField(this, "_paste$", new Subject());
    __publicField(this, "paste$", this._paste$.asObservable());
    // Editor get focus.
    __publicField(this, "_focus$", new Subject());
    __publicField(this, "focus$", this._focus$.asObservable());
    // Editor lose focus.
    __publicField(this, "_blur$", new Subject());
    __publicField(this, "blur$", this._blur$.asObservable());
    // Emit when doc selection changed.
    __publicField(this, "_selectionChange$", new Subject());
    __publicField(this, "selectionChange$", this._selectionChange$.asObservable());
    this._listenSelection();
  }
  get docSelectionRenderService() {
    return this._param.render.with(DocSelectionRenderService);
  }
  _listenSelection() {
    const docSelectionRenderService = this._param.render.with(DocSelectionRenderService);
    this.disposeWithMe(
      docSelectionRenderService.onBlur$.subscribe((e) => {
        this._blur$.next(e);
        const data = this.getDocumentData();
        this._change$.next({
          target: this,
          data
        });
      })
    );
    this.disposeWithMe(
      docSelectionRenderService.onFocus$.subscribe((e) => {
        this._focus$.next(e);
      })
    );
    this.disposeWithMe(
      docSelectionRenderService.onPaste$.subscribe((e) => {
        this._paste$.next(e);
      })
    );
    this.disposeWithMe(
      merge(
        docSelectionRenderService.onInput$,
        docSelectionRenderService.onKeydown$.pipe(filter((e) => {
          const event = e.event;
          if (event.ctrlKey || event.metaKey) {
            return [88 /* X */, 86 /* V */].includes(event.keyCode);
          }
          return [8 /* BACKSPACE */].includes(event.keyCode);
        })),
        docSelectionRenderService.onCompositionupdate$,
        docSelectionRenderService.onCompositionend$,
        docSelectionRenderService.onPaste$
      ).subscribe((e) => {
        if (e == null) {
          return;
        }
        const { content = "" } = e;
        const data = this.getDocumentData();
        this._input$.next({
          target: this,
          content,
          data,
          isComposing: e.event.type === "compositionupdate"
        });
      })
    );
    this.disposeWithMe(
      this._docSelectionManagerService.textSelection$.subscribe((e) => {
        if (e == null) {
          return;
        }
        const { unitId, subUnitId, ...params } = e;
        const editorId = this.getEditorId();
        if (unitId === editorId) {
          this._selectionChange$.next(params);
        }
      })
    );
  }
  isFocus() {
    const docSelectionRenderService = this._param.render.with(DocSelectionRenderService);
    return docSelectionRenderService.isFocusing && Boolean(docSelectionRenderService.getActiveTextRange());
  }
  /**
   * @deprecated use `IEditorService.focus` as instead. this is for internal usage.
   */
  focus() {
    const curDoc = this._univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const editorUnitId = this.getEditorId();
    if (curDoc == null || curDoc.getUnitId() !== editorUnitId) {
      this._univerInstanceService.setCurrentUnitForType(editorUnitId);
    }
    const docSelectionRenderService = this._param.render.with(DocSelectionRenderService);
    docSelectionRenderService.focus();
  }
  /**
   * @deprecated use `IEditorService.blur` as instead. this is for internal usage.
   */
  blur() {
    const docSelectionRenderService = this._param.render.with(DocSelectionRenderService);
    docSelectionRenderService.blur();
  }
  // Selects the entire content of the editor.
  // Calling editor.select() will not necessarily focus the editor, so it is often used with Editor.focus
  select() {
    const documentData = this.getDocumentData();
    return this.setSelectionRanges([{
      startOffset: 0,
      endOffset: documentData.body ? documentData.body.dataStream.length - 2 : 0
    }]);
  }
  // Selects the specified range of characters within editor.
  setSelectionRanges(ranges, shouldFocus = true) {
    const editorUnitId = this.getEditorId();
    const params = {
      unitId: editorUnitId,
      subUnitId: editorUnitId
    };
    return this._docSelectionManagerService.replaceDocRanges(ranges, params, false, { shouldFocus });
  }
  // Get current doc ranges. include text range and rect range.
  getSelectionRanges() {
    const editorUnitId = this.getEditorId();
    const params = {
      unitId: editorUnitId,
      subUnitId: editorUnitId
    };
    return this._docSelectionManagerService.getDocRanges(params);
  }
  getCursorPosition() {
    var _a, _b;
    const selectionRanges = this.getSelectionRanges();
    return (_b = (_a = selectionRanges.find((range) => range.collapsed)) == null ? void 0 : _a.startOffset) != null ? _b : -1;
  }
  // get editor id.
  getEditorId() {
    return this._getEditorId();
  }
  // get document data.
  getDocumentData() {
    const docDataModel = this._getDocDataModel();
    return docDataModel.getSnapshot();
  }
  getDocumentDataModel() {
    return this._getDocDataModel();
  }
  // Set the new document data.
  setDocumentData(data, textRanges) {
    this._commandService.syncExecuteCommand(ReplaceSnapshotCommand.id, {
      unitId: this.getEditorId(),
      snapshot: data,
      textRanges
    });
  }
  replaceText(text, resetCursor = true) {
    const data = this.getDocumentData();
    this.setDocumentData(
      {
        ...data,
        body: {
          dataStream: `${text}\r
`,
          paragraphs: [{
            startIndex: 0
          }],
          customRanges: [],
          sectionBreaks: [],
          tables: [],
          textRuns: []
        }
      },
      typeof resetCursor === "object" ? resetCursor : resetCursor ? [{
        startOffset: text.length,
        endOffset: text.length,
        collapsed: true
      }] : null
    );
  }
  // Clear the undo redo history of this editor.
  clearUndoRedoHistory() {
    const editorUnitId = this.getEditorId();
    return this._undoRedoService.clearUndoRedo(editorUnitId);
  }
  dispose() {
    const docDataModel = this._getDocDataModel();
    docDataModel == null ? void 0 : docDataModel.dispose();
  }
  /**
   * @deprecated use getEditorId.
   */
  get editorUnitId() {
    return this._param.editorUnitId;
  }
  /**
   * @deprecated @TODO: @JOCS remove this in the future.
   */
  get params() {
    return this._param;
  }
  get cancelDefaultResizeListener() {
    return this._param.cancelDefaultResizeListener;
  }
  get render() {
    return this._param.render;
  }
  isReadOnly() {
    return this._param.readonly === true;
  }
  getBoundingClientRect() {
    return this._param.editorDom.getBoundingClientRect();
  }
  get editorDOM() {
    return this._param.editorDom;
  }
  isVisible() {
    return this._param.visible;
  }
  getSkeleton() {
    var _a;
    const skeleton = (_a = this._injector.get(IRenderManagerService).getRenderById(this._getEditorId())) == null ? void 0 : _a.with(DocSkeletonManagerService).getSkeleton();
    return skeleton;
  }
  isSheetEditor() {
    return isInternalEditorID(this._getEditorId());
  }
  /**
   * @deprecated use getDocumentData.
   */
  getValue() {
    var _a;
    const docDataModel = this._getDocDataModel();
    const value = ((_a = docDataModel.getBody()) == null ? void 0 : _a.dataStream) || "";
    return value.replace(/\r\n/g, "").replace(/\n/g, "").replace(/\n/g, "");
  }
  /**
   * @deprecated use getDocumentData.
   */
  getBody() {
    const docDataModel = this._getDocDataModel();
    return docDataModel.getBody();
  }
  /**
   * @deprecated.
   */
  update(param) {
    this._param = {
      ...this._param,
      ...param
    };
  }
  /**
   * @deprecated.
   */
  updateCanvasStyle() {
    var _a;
    const docDataModel = this._getDocDataModel();
    if (docDataModel == null) {
      return;
    }
    const documentStyle = {};
    if ((_a = this._param.canvasStyle) == null ? void 0 : _a.fontSize) {
      if (documentStyle.textStyle == null) {
        documentStyle.textStyle = {};
      }
      documentStyle.textStyle.fs = this._param.canvasStyle.fontSize;
    }
    docDataModel.updateDocumentStyle(documentStyle);
  }
  _getDocDataModel() {
    const editorUnitId = this._getEditorId();
    const docDataModel = this._univerInstanceService.getUnit(editorUnitId, 1 /* UNIVER_DOC */);
    return docDataModel;
  }
  _getEditorId() {
    var _a;
    return ((_a = this._param.initialSnapshot) == null ? void 0 : _a.id) || this._param.editorUnitId || "";
  }
};

// ../packages/docs-ui/src/services/editor/editor-manager.service.ts
var editorFocusInElements = [
  "editor",
  "render-canvas"
  // 'univer-range-selector',
  // 'univer-range-selector-editor',
  // 'univer-text-editor-container-placeholder',
];
var EditorService = class extends Disposable {
  constructor(_univerInstanceService, _renderManagerService, _docSelectionManagerService, _contextService, _commandService, _undoRedoService, _injector) {
    super();
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_renderManagerService", _renderManagerService);
    __publicField(this, "_docSelectionManagerService", _docSelectionManagerService);
    __publicField(this, "_contextService", _contextService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_undoRedoService", _undoRedoService);
    __publicField(this, "_injector", _injector);
    __publicField(this, "_editors", /* @__PURE__ */ new Map());
    __publicField(this, "_focusEditorUnitId");
    __publicField(this, "_blur$", new Subject());
    __publicField(this, "blur$", this._blur$.asObservable());
    __publicField(this, "_focus$", new Subject());
    __publicField(this, "focus$", this._focus$.asObservable());
    this._initUniverFocusListener();
  }
  // REFACTOR: @Gggpound The specific business processing should not be placed here,
  // I moved from the layout service. https://github.com/dream-num/univer-pro/issues/1708
  _initUniverFocusListener() {
    this.disposeWithMe(
      fromEvent(window, "focusin").subscribe((event) => {
        const target = event.target;
        this._blurSheetEditor(target);
      })
    );
  }
  _blurSheetEditor(target) {
    if (editorFocusInElements.some((item) => target.dataset.uComp === item)) {
      return;
    }
    const focusEditor = this.getFocusEditor();
    if (focusEditor && focusEditor.isSheetEditor() !== true) {
      this.blur();
    }
  }
  _setFocusId(id) {
    this._focusEditorUnitId = id;
  }
  getFocusId() {
    return this._focusEditorUnitId;
  }
  getFocusEditor() {
    if (this._focusEditorUnitId) {
      return this.getEditor(this._focusEditorUnitId);
    }
  }
  isEditor(editorUnitId) {
    return this._editors.has(editorUnitId);
  }
  isSheetEditor(editorUnitId) {
    const editor = this._editors.get(editorUnitId);
    return !!(editor && editor.isSheetEditor());
  }
  blur(force) {
    const focusingEditor = this.getFocusEditor();
    if (force) {
      focusingEditor == null ? void 0 : focusingEditor.setSelectionRanges([]);
    }
    focusingEditor == null ? void 0 : focusingEditor.blur();
    this._contextService.setContextValue(EDITOR_ACTIVATED, false);
    this._contextService.setContextValue(FOCUSING_EDITOR_STANDALONE, false);
    this._contextService.setContextValue(FOCUSING_COMMENT_EDITOR, false);
    this._setFocusId(null);
    this._blur$.next(null);
  }
  focus(editorUnitId) {
    if (editorUnitId === this._focusEditorUnitId) {
      return;
    }
    if (this._focusEditorUnitId) {
      this.blur();
    }
    if (editorUnitId == null) {
      return;
    }
    const editor = this.getEditor(editorUnitId);
    if (editor == null) {
      return;
    }
    this._univerInstanceService.setCurrentUnitForType(editorUnitId);
    const valueCount = editor.getValue().length;
    this._contextService.setContextValue(EDITOR_ACTIVATED, true);
    if (!isInternalEditorID(editorUnitId)) {
      this._contextService.setContextValue(FOCUSING_EDITOR_STANDALONE, true);
    }
    if (isCommentEditorID(editorUnitId)) {
      this._contextService.setContextValue(FOCUSING_COMMENT_EDITOR, true);
    }
    editor.focus();
    this._setFocusId(editorUnitId);
    this._focus$.next({
      startOffset: valueCount,
      endOffset: valueCount
    });
  }
  dispose() {
    this._editors.clear();
    super.dispose();
  }
  getEditor(id = this._getCurrentEditorUnitId()) {
    return this._editors.get(id);
  }
  getAllEditor() {
    return this._editors;
  }
  register(config, container) {
    var _a, _b, _c, _d;
    const { initialSnapshot, canvasStyle = {} } = config;
    const editorUnitId = initialSnapshot.id;
    const documentDataModel = this._univerInstanceService.getUnit(editorUnitId, 1 /* UNIVER_DOC */);
    if (documentDataModel == null) {
      this._univerInstanceService.createUnit(
        1 /* UNIVER_DOC */,
        initialSnapshot || this._getBlank(editorUnitId),
        { makeCurrent: false }
      );
    }
    let render = this._renderManagerService.getRenderById(editorUnitId);
    if (render == null) {
      this._renderManagerService.create(editorUnitId);
      render = this._renderManagerService.getRenderById(editorUnitId);
    }
    if (render) {
      render.engine.setContainer(container);
      const editor = new Editor(
        { ...config, render, editorDom: container, canvasStyle },
        this._univerInstanceService,
        this._docSelectionManagerService,
        this._commandService,
        this._undoRedoService,
        this._injector
      );
      this._editors.set(editorUnitId, editor);
      if (canvasStyle.backgroundColor != null) {
        render.engine.getCanvas().getCanvasEle().style.backgroundColor = canvasStyle.backgroundColor;
        const docBackground = render.components.get("__Document_Render_Background__" /* BACKGROUND */);
        docBackground == null ? void 0 : docBackground.setFillColors(
          canvasStyle.backgroundColor,
          canvasStyle.backgroundColor,
          canvasStyle.backgroundColor,
          canvasStyle.backgroundColor
        );
      }
      if (!config.scrollBar) {
        const viewport = (_c = (_b = (_a = render.mainComponent) == null ? void 0 : _a.getScene()) == null ? void 0 : _b.getViewports()) == null ? void 0 : _c[0];
        (_d = viewport == null ? void 0 : viewport.getScrollBar()) == null ? void 0 : _d.dispose();
        viewport == null ? void 0 : viewport.updateScrollVal({
          scrollX: 0,
          scrollY: 0,
          viewportScrollX: 0,
          viewportScrollY: 0
        });
      }
    }
    return toDisposable(() => {
      this._unRegister(editorUnitId);
    });
  }
  _unRegister(editorUnitId) {
    const editor = this._editors.get(editorUnitId);
    if (editor == null) {
      return;
    }
    this._renderManagerService.removeRender(editorUnitId);
    editor.dispose();
    this._editors.delete(editorUnitId);
    this._univerInstanceService.disposeUnit(editorUnitId);
  }
  _getCurrentEditorUnitId() {
    const current = this._univerInstanceService.getCurrentUniverDocInstance();
    return current.getUnitId();
  }
  _getBlank(id) {
    return {
      id,
      body: {
        dataStream: `${DEFAULT_EMPTY_DOCUMENT_VALUE}`,
        textRuns: [],
        paragraphs: [
          {
            startIndex: 0
          }
        ]
      },
      documentStyle: {
        renderConfig: {
          verticalAlign: 1 /* TOP */,
          horizontalAlign: 1 /* LEFT */
        },
        marginLeft: 3,
        marginTop: 0,
        marginRight: 3
      }
    };
  }
};
EditorService = __decorateClass([
  __decorateParam(0, IUniverInstanceService),
  __decorateParam(1, IRenderManagerService),
  __decorateParam(2, Inject(DocSelectionManagerService)),
  __decorateParam(3, IContextService),
  __decorateParam(4, ICommandService),
  __decorateParam(5, IUndoRedoService),
  __decorateParam(6, Inject(Injector))
], EditorService);
var IEditorService = createIdentifier("univer.editor.service");

// ../packages/docs-ui/src/views/header-footer/panel/component-name.ts
var COMPONENT_DOC_HEADER_FOOTER_PANEL = "COMPONENT_DOC_HEADER_FOOTER_PANEL";

// ../packages/docs-ui/src/views/header-footer/panel/DocHeaderFooterPanel.tsx
var import_react2 = __toESM(require_react());

// ../packages/docs-ui/src/views/header-footer/panel/DocHeaderFooterOptions.tsx
var import_react = __toESM(require_react());
var import_jsx_runtime = __toESM(require_jsx_runtime());
function getSegmentId(documentStyle, editArea, pageIndex) {
  const {
    useFirstPageHeaderFooter,
    evenAndOddHeaders,
    defaultHeaderId,
    defaultFooterId,
    firstPageHeaderId,
    firstPageFooterId,
    evenPageHeaderId,
    evenPageFooterId
  } = documentStyle;
  if (editArea === "HEADER" /* HEADER */) {
    if (useFirstPageHeaderFooter === 1 /* TRUE */) {
      if (pageIndex === 0) {
        return firstPageHeaderId;
      } else {
        return evenAndOddHeaders === 1 /* TRUE */ && pageIndex % 2 === 1 ? evenPageHeaderId : defaultHeaderId;
      }
    } else {
      return evenAndOddHeaders === 1 /* TRUE */ && pageIndex % 2 === 1 ? evenPageHeaderId : defaultHeaderId;
    }
  } else {
    if (useFirstPageHeaderFooter === 1 /* TRUE */) {
      if (pageIndex === 0) {
        return firstPageFooterId;
      } else {
        return evenAndOddHeaders === 1 /* TRUE */ && pageIndex % 2 === 1 ? evenPageFooterId : defaultFooterId;
      }
    } else {
      return evenAndOddHeaders === 1 /* TRUE */ && pageIndex % 2 === 1 ? evenPageFooterId : defaultFooterId;
    }
  }
}
var DocHeaderFooterOptions = (props) => {
  const localeService = useDependency(LocaleService);
  const univerInstanceService = useDependency(IUniverInstanceService);
  const renderManagerService = useDependency(IRenderManagerService);
  const commandService = useDependency(ICommandService);
  const layoutService = useDependency(ILayoutService);
  const { unitId } = props;
  const docSelectionRenderService = renderManagerService.getRenderById(unitId).with(DocSelectionRenderService);
  const [options, setOptions] = (0, import_react.useState)({});
  const handleCheckboxChange = (val, type2) => {
    var _a;
    setOptions((prev) => ({
      ...prev,
      [type2]: val ? 1 /* TRUE */ : 0 /* FALSE */
    }));
    const docDataModel = univerInstanceService.getUniverDocInstance(unitId);
    const documentStyle = docDataModel == null ? void 0 : docDataModel.getSnapshot().documentStyle;
    const docSkeletonManagerService = (_a = renderManagerService.getRenderById(unitId)) == null ? void 0 : _a.with(DocSkeletonManagerService);
    const viewModel = docSkeletonManagerService == null ? void 0 : docSkeletonManagerService.getViewModel();
    if (documentStyle == null || viewModel == null) {
      return;
    }
    const editArea = viewModel.getEditArea();
    let needCreateHeaderFooter = false;
    const segmentPage = docSelectionRenderService.getSegmentPage();
    let needChangeSegmentId = false;
    if (type2 === "useFirstPageHeaderFooter" && val === true) {
      if (editArea === "HEADER" /* HEADER */ && !documentStyle.firstPageHeaderId) {
        needCreateHeaderFooter = true;
      } else if (editArea === "FOOTER" /* FOOTER */ && !documentStyle.firstPageFooterId) {
        needCreateHeaderFooter = true;
      }
      if (needCreateHeaderFooter && segmentPage === 0) {
        needChangeSegmentId = true;
      }
    }
    if (type2 === "evenAndOddHeaders" && val === true) {
      if (editArea === "HEADER" /* HEADER */ && !documentStyle.evenPageHeaderId) {
        needCreateHeaderFooter = true;
      } else if (editArea === "FOOTER" /* FOOTER */ && !documentStyle.evenPageFooterId) {
        needCreateHeaderFooter = true;
      }
      if (needCreateHeaderFooter && segmentPage % 2 === 1) {
        needChangeSegmentId = true;
      }
    }
    if (needCreateHeaderFooter) {
      const SEGMENT_ID_LEN = 6;
      const segmentId = generateRandomId(SEGMENT_ID_LEN);
      if (needChangeSegmentId) {
        docSelectionRenderService.setSegment(segmentId);
      }
      commandService.executeCommand(CoreHeaderFooterCommandId, {
        unitId,
        segmentId,
        headerFooterProps: {
          [type2]: val ? 1 /* TRUE */ : 0 /* FALSE */
        }
      });
    } else {
      const segmentPageIndex = docSelectionRenderService.getSegmentPage();
      const prevSegmentId = docSelectionRenderService.getSegment();
      const needFocusSegmentId = getSegmentId(
        {
          ...documentStyle,
          [type2]: val ? 1 /* TRUE */ : 0 /* FALSE */
        },
        editArea,
        segmentPageIndex
      );
      if (needFocusSegmentId && needFocusSegmentId !== prevSegmentId) {
        docSelectionRenderService.setSegment(needFocusSegmentId);
      }
      commandService.executeCommand(CoreHeaderFooterCommandId, {
        unitId,
        headerFooterProps: {
          [type2]: val ? 1 /* TRUE */ : 0 /* FALSE */
        }
      });
    }
    layoutService.focus();
  };
  const handleMarginChange = async (val, type2) => {
    setOptions((prev) => ({
      ...prev,
      [type2]: val
    }));
    await commandService.executeCommand(CoreHeaderFooterCommandId, {
      unitId,
      headerFooterProps: {
        [type2]: val
      }
    });
    docSelectionRenderService.removeAllRanges();
    docSelectionRenderService.blur();
  };
  const closeHeaderFooter = () => {
    commandService.executeCommand(CloseHeaderFooterCommand.id, {
      unitId
    });
  };
  (0, import_react.useEffect)(() => {
    const docDataModel = univerInstanceService.getUniverDocInstance(unitId);
    const documentStyle = docDataModel == null ? void 0 : docDataModel.getSnapshot().documentStyle;
    if (documentStyle) {
      const {
        marginHeader = 0,
        marginFooter = 0,
        useFirstPageHeaderFooter = 0 /* FALSE */,
        evenAndOddHeaders = 0 /* FALSE */
      } = documentStyle;
      setOptions({
        marginHeader,
        marginFooter,
        useFirstPageHeaderFooter,
        evenAndOddHeaders
      });
    }
  }, [unitId]);
  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "univer-grid univer-gap-4", children: [
    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "univer-grid univer-gap-2", children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        Checkbox,
        {
          checked: options.useFirstPageHeaderFooter === 1 /* TRUE */,
          onChange: (val) => {
            handleCheckboxChange(val, "useFirstPageHeaderFooter");
          },
          children: localeService.t("docs-ui.headerFooter.firstPageCheckBox")
        }
      ) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
        Checkbox,
        {
          checked: options.evenAndOddHeaders === 1 /* TRUE */,
          onChange: (val) => {
            handleCheckboxChange(val, "evenAndOddHeaders");
          },
          children: localeService.t("docs-ui.headerFooter.oddEvenCheckBox")
        }
      ) })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "univer-mb-1 univer-flex", children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: localeService.t("docs-ui.headerFooter.headerTopMargin") }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          InputNumber,
          {
            className: "univer-mt-1.5 univer-w-4/5",
            min: 0,
            max: 200,
            precision: 1,
            value: options.marginHeader,
            onChange: (val) => {
              handleMarginChange(val, "marginHeader");
            }
          }
        )
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { children: localeService.t("docs-ui.headerFooter.footerBottomMargin") }),
        /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
          InputNumber,
          {
            className: "univer-mt-1.5 univer-w-4/5",
            min: 0,
            max: 200,
            precision: 1,
            value: options.marginFooter,
            onChange: (val) => {
              handleMarginChange(val, "marginFooter");
            }
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "univer-flex univer-justify-end", children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)(Button, { onClick: closeHeaderFooter, children: localeService.t("docs-ui.headerFooter.closeHeaderFooter") }) })
  ] });
};

// ../packages/docs-ui/src/views/header-footer/panel/DocHeaderFooterPanel.tsx
var import_jsx_runtime2 = __toESM(require_jsx_runtime());
var DocHeaderFooterPanel = () => {
  var _a;
  const localeService = useDependency(LocaleService);
  const renderManagerService = useDependency(IRenderManagerService);
  const univerInstanceService = useDependency(IUniverInstanceService);
  const documentDataModel = univerInstanceService.getCurrentUniverDocInstance();
  const unitId = documentDataModel.getUnitId();
  const docSkeletonManagerService = (_a = renderManagerService.getRenderById(unitId)) == null ? void 0 : _a.with(DocSkeletonManagerService);
  const viewModel = docSkeletonManagerService.getViewModel();
  const [isEditHeaderFooter, setIsEditHeaderFooter] = (0, import_react2.useState)(true);
  (0, import_react2.useEffect)(() => {
    const editArea = viewModel.getEditArea();
    setIsEditHeaderFooter(editArea !== "BODY" /* BODY */);
    const subscription = viewModel.editAreaChange$.subscribe((editArea2) => {
      if (editArea2 == null) {
        return;
      }
      setIsEditHeaderFooter(editArea2 !== "BODY" /* BODY */);
    });
    return () => {
      subscription.unsubscribe();
    };
  }, []);
  return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { className: "univer-text-sm", children: isEditHeaderFooter ? /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(DocHeaderFooterOptions, { unitId }) : /* @__PURE__ */ (0, import_jsx_runtime2.jsx)("div", { className: "univer-text-gray-400", children: localeService.t("docs-ui.headerFooter.disableText") }) });
};

// ../packages/docs-ui/src/views/header-footer/text-bubble.ts
var COLLAB_CURSOR_LABEL_HEIGHT = 18;
var COLLAB_CURSOR_LABEL_MAX_WIDTH = 200;
var COLLAB_CURSOR_LABEL_TEXT_PADDING_LR = 6;
var COLLAB_CURSOR_LABEL_TEXT_PADDING_TB = 4;
function drawBubble(ctx, props) {
  let { radius, width, height } = props;
  radius = radius != null ? radius : 0;
  width = width != null ? width : 30;
  height = height != null ? height : 30;
  let bottomRight = 0;
  bottomRight = Math.min(radius, width / 2, height / 2);
  ctx.beginPath();
  ctx.moveTo(0, 0);
  ctx.lineTo(width, 0);
  ctx.lineTo(width, height - bottomRight);
  ctx.arc(width - bottomRight, height - bottomRight, bottomRight, 0, Math.PI / 2, false);
  ctx.lineTo(0, height);
  ctx.lineTo(0, 0);
  ctx.closePath();
  if (props.fill) {
    ctx.save();
    ctx.fillStyle = props.fill;
    if (props.fillRule === "evenodd") {
      ctx.fill("evenodd");
    } else {
      ctx.fill();
    }
    ctx.restore();
  }
}
var TextBubbleShape = class _TextBubbleShape extends Shape {
  constructor(key, props) {
    super(key, props);
    __publicField(this, "color");
    __publicField(this, "text");
    this.color = props == null ? void 0 : props.color;
    this.text = props == null ? void 0 : props.text;
  }
  static drawWith(ctx, props) {
    const { text, color } = props;
    ctx.save();
    ctx.font = "13px Source Han Sans CN";
    const textWidth = ctx.measureText(text).width;
    const realInfoWidth = Math.min(
      textWidth + 2 * COLLAB_CURSOR_LABEL_TEXT_PADDING_LR,
      COLLAB_CURSOR_LABEL_MAX_WIDTH
    );
    drawBubble(ctx, {
      height: COLLAB_CURSOR_LABEL_HEIGHT,
      width: realInfoWidth,
      radius: 4,
      fill: color,
      evented: false
    });
    ctx.fillStyle = "rgba(58, 96, 247, 1)";
    const offsetX = COLLAB_CURSOR_LABEL_TEXT_PADDING_LR;
    const offsetY = COLLAB_CURSOR_LABEL_HEIGHT - COLLAB_CURSOR_LABEL_TEXT_PADDING_TB;
    const maxTextWidth = COLLAB_CURSOR_LABEL_MAX_WIDTH - 2 * COLLAB_CURSOR_LABEL_TEXT_PADDING_LR;
    if (textWidth > maxTextWidth) {
      let truncatedText = "";
      let currentWidth = 0;
      for (const element of text) {
        const charWidth = ctx.measureText(element).width;
        if (currentWidth + charWidth <= maxTextWidth - ctx.measureText("...").width) {
          truncatedText += element;
          currentWidth += charWidth;
        } else {
          truncatedText += "...";
          break;
        }
      }
      ctx.fillText(truncatedText, offsetX, offsetY);
    } else {
      ctx.fillText(text, offsetX, offsetY);
    }
    ctx.restore();
  }
  _draw(ctx) {
    _TextBubbleShape.drawWith(ctx, this);
  }
};

// ../packages/docs-ui/src/controllers/doc-header-footer.controller.ts
var HEADER_FOOTER_STROKE_COLOR = "rgba(58, 96, 247, 1)";
var HEADER_FOOTER_FILL_COLOR = "rgba(58, 96, 247, 0.08)";
function checkCreateHeaderFooterType(viewModel, editArea, segmentPage) {
  const { documentStyle } = viewModel.getDataModel().getSnapshot();
  const {
    defaultHeaderId,
    defaultFooterId,
    evenPageHeaderId,
    evenPageFooterId,
    firstPageHeaderId,
    firstPageFooterId,
    evenAndOddHeaders,
    useFirstPageHeaderFooter
  } = documentStyle;
  switch (editArea) {
    case "BODY" /* BODY */:
      return {
        createType: null,
        headerFooterId: null
      };
    case "HEADER" /* HEADER */: {
      if (useFirstPageHeaderFooter === 1 /* TRUE */ && !firstPageHeaderId) {
        return {
          createType: 0 /* FIRST_PAGE_HEADER */,
          headerFooterId: null
        };
      }
      if (evenAndOddHeaders === 1 /* TRUE */ && segmentPage % 2 === 0 && !evenPageHeaderId) {
        return {
          createType: 4 /* EVEN_PAGE_HEADER */,
          headerFooterId: null
        };
      }
      return defaultHeaderId ? {
        createType: null,
        headerFooterId: defaultHeaderId
      } : {
        createType: 2 /* DEFAULT_HEADER */,
        headerFooterId: null
      };
    }
    case "FOOTER" /* FOOTER */: {
      if (useFirstPageHeaderFooter === 1 /* TRUE */ && !firstPageFooterId) {
        return {
          createType: 1 /* FIRST_PAGE_FOOTER */,
          headerFooterId: null
        };
      }
      if (evenAndOddHeaders === 1 /* TRUE */ && segmentPage % 2 === 0 && !evenPageFooterId) {
        return {
          createType: 5 /* EVEN_PAGE_FOOTER */,
          headerFooterId: null
        };
      }
      return defaultFooterId ? {
        createType: null,
        headerFooterId: defaultFooterId
      } : {
        createType: 3 /* DEFAULT_FOOTER */,
        headerFooterId: null
      };
    }
    default:
      throw new Error(`Invalid editArea: ${editArea}`);
  }
}
var DocHeaderFooterController = class extends Disposable {
  constructor(_context, _commandService, _editorService, _instanceSrv, _renderManagerService, _docSkeletonManagerService, _docSelectionRenderService, _localeService, _componentManager) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_editorService", _editorService);
    __publicField(this, "_instanceSrv", _instanceSrv);
    __publicField(this, "_renderManagerService", _renderManagerService);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_docSelectionRenderService", _docSelectionRenderService);
    __publicField(this, "_localeService", _localeService);
    __publicField(this, "_componentManager", _componentManager);
    __publicField(this, "_loadedMap", /* @__PURE__ */ new WeakSet());
    this._initialize();
  }
  _initialize() {
    this._init();
    this._drawHeaderFooterLabel();
    this._initCustomComponents();
    this._listenSwitchMode();
  }
  dispose() {
    super.dispose();
  }
  // Close header footer panel when switch mode.
  _listenSwitchMode() {
    this.disposeWithMe(
      this._commandService.onCommandExecuted((command) => {
        if (RichTextEditingMutation.id === command.id) {
          const docDataModel = this._context.unit;
          const viewModel = this._docSkeletonManagerService.getViewModel();
          const editArea = viewModel.getEditArea();
          const documentFlavor = docDataModel.getSnapshot().documentStyle.documentFlavor;
          if (editArea !== "BODY" /* BODY */ && documentFlavor === 2 /* MODERN */) {
            this._commandService.executeCommand(CloseHeaderFooterCommand.id, {
              unitId: this._context.unitId
            });
          }
        }
      })
    );
  }
  _initCustomComponents() {
    if (!this._componentManager.get(COMPONENT_DOC_HEADER_FOOTER_PANEL)) {
      this.disposeWithMe(this._componentManager.register(COMPONENT_DOC_HEADER_FOOTER_PANEL, DocHeaderFooterPanel));
    }
  }
  _init() {
    const { unitId } = this._context;
    const docObject = neoGetDocObject(this._context);
    if (docObject == null || docObject.document == null) {
      return;
    }
    if (!this._loadedMap.has(docObject.document)) {
      this._initialMain(unitId);
      this._loadedMap.add(docObject.document);
    }
  }
  _initialMain(unitId) {
    const docObject = neoGetDocObject(this._context);
    const { document: document2 } = docObject;
    this.disposeWithMe(document2.onDblclick$.subscribeEvent(async (evt) => {
      if (this._isEditorReadOnly(unitId)) {
        return;
      }
      if (!this._isTraditionalMode()) {
        return;
      }
      const { offsetX, offsetY } = evt;
      const {
        pageLayoutType = 0 /* VERTICAL */,
        pageMarginLeft,
        pageMarginTop
      } = document2.getOffsetConfig();
      const coord = this._getTransformCoordForDocumentOffset(offsetX, offsetY);
      if (coord == null) {
        return;
      }
      const viewModel = this._docSkeletonManagerService.getViewModel();
      const skeleton = this._docSkeletonManagerService.getSkeleton();
      const preEditArea = viewModel.getEditArea();
      const { editArea, pageNumber } = skeleton.findEditAreaByCoord(
        coord,
        pageLayoutType,
        pageMarginLeft,
        pageMarginTop
      );
      if (preEditArea === editArea) {
        return;
      }
      viewModel.setEditArea(editArea);
      const { createType, headerFooterId } = checkCreateHeaderFooterType(viewModel, editArea, pageNumber);
      if (editArea === "BODY" /* BODY */) {
        this._docSelectionRenderService.setSegment("");
        this._docSelectionRenderService.setSegmentPage(-1);
        this._docSelectionRenderService.setCursorManually(offsetX, offsetY);
      } else {
        if (createType != null) {
          const SEGMENT_ID_LEN = 6;
          const segmentId = generateRandomId(SEGMENT_ID_LEN);
          this._docSelectionRenderService.setSegment(segmentId);
          this._docSelectionRenderService.setSegmentPage(pageNumber);
          await this._commandService.executeCommand(CoreHeaderFooterCommand.id, {
            unitId,
            createType,
            segmentId
          });
        } else if (headerFooterId != null) {
          this._docSelectionRenderService.setSegment(headerFooterId);
          this._docSelectionRenderService.setSegmentPage(pageNumber);
          this._docSelectionRenderService.setCursorManually(offsetX, offsetY);
        }
      }
    }));
  }
  _getTransformCoordForDocumentOffset(evtOffsetX, evtOffsetY) {
    const docObject = neoGetDocObject(this._context);
    const { document: document2, scene } = docObject;
    const { documentTransform } = document2.getOffsetConfig();
    const activeViewport = scene.getViewports()[0];
    if (activeViewport == null) {
      return;
    }
    const originCoord = activeViewport.transformVector2SceneCoord(Vector2.FromArray([evtOffsetX, evtOffsetY]));
    return documentTransform.clone().invert().applyPoint(originCoord);
  }
  // eslint-disable-next-line max-lines-per-function
  _drawHeaderFooterLabel() {
    const localeService = this._localeService;
    this.disposeWithMe(
      this._instanceSrv.getCurrentTypeOfUnit$(1 /* UNIVER_DOC */).subscribe((unit) => {
        if (unit == null) {
          return;
        }
        const unitId = unit.getUnitId();
        const currentRender = this._renderManagerService.getRenderById(unitId);
        if (this._editorService.isEditor(unitId) || this._instanceSrv.getUniverDocInstance(unitId) == null) {
          return;
        }
        if (currentRender == null) {
          return;
        }
        const { mainComponent } = currentRender;
        const docsComponent = mainComponent;
        this.disposeWithMe(
          toDisposable(
            // eslint-disable-next-line max-lines-per-function
            docsComponent.pageRender$.subscribe((config) => {
              if (this._editorService.isEditor(unitId)) {
                return;
              }
              if (!this._isTraditionalMode()) {
                return;
              }
              const viewModel = this._docSkeletonManagerService.getViewModel();
              const editArea = viewModel.getEditArea();
              const isEditBody = editArea === "BODY" /* BODY */;
              const { page, pageLeft, pageTop, ctx } = config;
              const { pageWidth, pageHeight, marginTop, marginBottom } = page;
              ctx.save();
              ctx.translate(pageLeft - 0.5, pageTop - 0.5);
              if (isEditBody) {
                Rect.drawWith(ctx, {
                  left: 0,
                  top: 0,
                  width: pageWidth,
                  height: marginTop,
                  fill: "rgba(255, 255, 255, 0.5)"
                });
                ctx.save();
                ctx.translate(0, pageHeight - marginBottom);
                Rect.drawWith(ctx, {
                  left: 0,
                  top: 0,
                  width: pageWidth,
                  height: marginBottom,
                  fill: "rgba(255, 255, 255, 0.5)"
                });
                ctx.restore();
              } else {
                ctx.save();
                ctx.translate(0, marginTop);
                Rect.drawWith(ctx, {
                  left: 0,
                  top: marginTop,
                  width: pageWidth,
                  height: pageHeight - marginTop - marginBottom,
                  fill: "rgba(255, 255, 255, 0.5)"
                });
                ctx.restore();
              }
              if (!isEditBody) {
                const headerPathConfigIPathProps = {
                  dataArray: [{
                    command: "M",
                    points: [0, marginTop]
                  }, {
                    command: "L",
                    points: [pageWidth, marginTop]
                  }],
                  strokeWidth: 1,
                  stroke: HEADER_FOOTER_STROKE_COLOR
                };
                const footerPathConfigIPathProps = {
                  dataArray: [{
                    command: "M",
                    points: [0, pageHeight - marginBottom]
                  }, {
                    command: "L",
                    points: [pageWidth, pageHeight - marginBottom]
                  }],
                  strokeWidth: 1,
                  stroke: HEADER_FOOTER_STROKE_COLOR
                };
                Path.drawWith(ctx, headerPathConfigIPathProps);
                Path.drawWith(ctx, footerPathConfigIPathProps);
                ctx.translate(0, marginTop + 1);
                TextBubbleShape.drawWith(ctx, {
                  text: localeService.t("docs-ui.headerFooter.header"),
                  color: HEADER_FOOTER_FILL_COLOR
                });
                ctx.translate(0, pageHeight - marginTop - marginBottom);
                TextBubbleShape.drawWith(ctx, {
                  text: localeService.t("docs-ui.headerFooter.footer"),
                  color: HEADER_FOOTER_FILL_COLOR
                });
              }
              ctx.restore();
            })
          )
        );
      })
    );
  }
  _isEditorReadOnly(unitId) {
    const editor = this._editorService.getEditor(unitId);
    if (!editor) {
      return false;
    }
    return editor.isReadOnly();
  }
  _isTraditionalMode() {
    const docDataModel = this._context.unit;
    const documentFlavor = docDataModel.getSnapshot().documentStyle.documentFlavor;
    return documentFlavor === 1 /* TRADITIONAL */;
  }
};
DocHeaderFooterController = __decorateClass([
  __decorateParam(1, ICommandService),
  __decorateParam(2, IEditorService),
  __decorateParam(3, IUniverInstanceService),
  __decorateParam(4, IRenderManagerService),
  __decorateParam(5, Inject(DocSkeletonManagerService)),
  __decorateParam(6, Inject(DocSelectionRenderService)),
  __decorateParam(7, Inject(LocaleService)),
  __decorateParam(8, Inject(ComponentManager))
], DocHeaderFooterController);

// ../packages/docs-ui/src/commands/operations/doc-header-footer-panel.operation.ts
var SidebarDocHeaderFooterPanelOperation = {
  id: "sidebar.operation.doc-header-footer-panel",
  type: 1 /* OPERATION */,
  handler: async (accessor, params) => {
    const sidebarService = accessor.get(ISidebarService);
    const localeService = accessor.get(LocaleService);
    switch (params.value) {
      case "open":
        sidebarService.open({
          header: { title: localeService.t("docs-ui.headerFooter.panel") },
          children: { label: COMPONENT_DOC_HEADER_FOOTER_PANEL },
          onClose: () => {
          },
          width: 400
        });
        break;
      case "close":
      default:
        sidebarService.close();
        break;
    }
    return true;
  }
};

// ../packages/docs-ui/src/commands/commands/doc-header-footer.command.ts
function getEmptyHeaderFooterBody() {
  return {
    dataStream: "\r\n",
    textRuns: [{
      st: 0,
      ed: 0,
      ts: {
        fs: 9
        // The default header footer text size.
      }
    }],
    customBlocks: [],
    paragraphs: [
      {
        startIndex: 0,
        paragraphStyle: {
          spaceAbove: { v: 0 },
          lineSpacing: 1.5,
          spaceBelow: { v: 0 }
        }
      }
    ],
    sectionBreaks: [
      {
        startIndex: 1
      }
    ]
  };
}
function createHeaderFooterAction(segmentId, createType, documentStyle, actions) {
  const jsonX = JSONX.getInstance();
  const ID_LEN = 6;
  const firstSegmentId = segmentId != null ? segmentId : generateRandomId(ID_LEN);
  const isHeader = createType === 2 /* DEFAULT_HEADER */ || createType === 0 /* FIRST_PAGE_HEADER */ || createType === 4 /* EVEN_PAGE_HEADER */;
  const insertAction = jsonX.insertOp([isHeader ? "headers" : "footers", firstSegmentId], {
    [isHeader ? "headerId" : "footerId"]: firstSegmentId,
    body: getEmptyHeaderFooterBody()
  });
  actions.push(insertAction);
  const secondSegmentId = generateRandomId(ID_LEN);
  const insertPairAction = jsonX.insertOp([isHeader ? "footers" : "headers", secondSegmentId], {
    [isHeader ? "footerId" : "headerId"]: secondSegmentId,
    body: getEmptyHeaderFooterBody()
  });
  actions.push(insertPairAction);
  let key = "defaultHeaderId";
  let pairKey = "defaultFooterId";
  switch (createType) {
    case 2 /* DEFAULT_HEADER */:
      key = "defaultHeaderId";
      pairKey = "defaultFooterId";
      break;
    case 3 /* DEFAULT_FOOTER */:
      key = "defaultFooterId";
      pairKey = "defaultHeaderId";
      break;
    case 0 /* FIRST_PAGE_HEADER */:
      key = "firstPageHeaderId";
      pairKey = "firstPageFooterId";
      break;
    case 1 /* FIRST_PAGE_FOOTER */:
      key = "firstPageFooterId";
      pairKey = "firstPageHeaderId";
      break;
    case 4 /* EVEN_PAGE_HEADER */:
      key = "evenPageHeaderId";
      pairKey = "evenPageFooterId";
      break;
    case 5 /* EVEN_PAGE_FOOTER */:
      key = "evenPageFooterId";
      pairKey = "evenPageHeaderId";
      break;
    default:
      throw new Error(`Unknown header footer type: ${createType}`);
  }
  for (const [k, id] of [[key, firstSegmentId], [pairKey, secondSegmentId]]) {
    if (documentStyle[k] != null) {
      const replaceAction = jsonX.replaceOp(["documentStyle", k], documentStyle[k], id);
      actions.push(replaceAction);
    } else {
      const insertAction2 = jsonX.insertOp(["documentStyle", k], id);
      actions.push(insertAction2);
    }
  }
  return actions;
}
var CoreHeaderFooterCommandId = "doc.command.core-header-footer";
var CoreHeaderFooterCommand = {
  id: CoreHeaderFooterCommandId,
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler: async (accessor, params) => {
    var _a;
    const commandService = accessor.get(ICommandService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const renderManagerService = accessor.get(IRenderManagerService);
    const { unitId, segmentId, createType, headerFooterProps } = params;
    const docSkeletonManagerService = (_a = renderManagerService.getRenderById(unitId)) == null ? void 0 : _a.with(DocSkeletonManagerService);
    const docDataModel = univerInstanceService.getUniverDocInstance(unitId);
    const docViewModel = docSkeletonManagerService == null ? void 0 : docSkeletonManagerService.getViewModel();
    if (docDataModel == null || docViewModel == null) {
      return false;
    }
    const editArea = docViewModel.getEditArea();
    const { documentStyle } = docDataModel.getSnapshot();
    const isUpdateMargin = (headerFooterProps == null ? void 0 : headerFooterProps.marginFooter) != null || (headerFooterProps == null ? void 0 : headerFooterProps.marginHeader) != null;
    const textRanges = [{
      startOffset: 0,
      endOffset: 0,
      collapsed: true
    }];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges,
        debounce: true
      }
    };
    if (isUpdateMargin) {
      doMutation.params.noNeedSetTextRange = true;
    }
    const jsonX = JSONX.getInstance();
    const rawActions = [];
    if (createType != null) {
      createHeaderFooterAction(segmentId, createType, documentStyle, rawActions);
    }
    if (headerFooterProps != null) {
      Object.keys(headerFooterProps).forEach((key) => {
        const value = headerFooterProps[key];
        const oldValue = documentStyle[key];
        if (value === oldValue) {
          return;
        }
        let action;
        if (oldValue === void 0) {
          action = jsonX.insertOp(["documentStyle", key], value);
        } else {
          action = jsonX.replaceOp(["documentStyle", key], oldValue, value);
        }
        rawActions.push(action);
        if (key === "useFirstPageHeaderFooter" && value === 1 /* TRUE */ && !documentStyle.firstPageHeaderId) {
          const headerFooterType = editArea === "HEADER" /* HEADER */ ? 0 /* FIRST_PAGE_HEADER */ : 1 /* FIRST_PAGE_FOOTER */;
          createHeaderFooterAction(segmentId, headerFooterType, documentStyle, rawActions);
        } else if (key === "evenAndOddHeaders" && value === 1 /* TRUE */ && !documentStyle.evenPageHeaderId) {
          const headerFooterType = editArea === "HEADER" /* HEADER */ ? 4 /* EVEN_PAGE_HEADER */ : 5 /* EVEN_PAGE_FOOTER */;
          createHeaderFooterAction(segmentId, headerFooterType, documentStyle, rawActions);
        }
      });
    }
    if (rawActions.length === 0) {
      return false;
    }
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var OpenHeaderFooterPanelCommand = {
  id: "doc.command.open-header-footer-panel",
  type: 0 /* COMMAND */,
  handler: async (accessor, _params) => {
    const commandService = accessor.get(ICommandService);
    return commandService.executeCommand(SidebarDocHeaderFooterPanelOperation.id, { value: "open" });
  }
};
var CloseHeaderFooterCommand = {
  id: "doc.command.close-header-footer",
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    var _a;
    const commandService = accessor.get(ICommandService);
    const renderManagerService = accessor.get(IRenderManagerService);
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const instanceService = accessor.get(IUniverInstanceService);
    const { unitId } = params;
    const renderObject = renderManagerService.getRenderById(unitId);
    if (renderObject == null) {
      return false;
    }
    const { scene } = renderObject;
    const transformer = scene.getTransformerByCreate();
    const docSkeletonManagerService = renderObject.with(DocSkeletonManagerService);
    const docSelectionRenderService = renderObject.with(DocSelectionRenderService);
    const skeleton = docSkeletonManagerService == null ? void 0 : docSkeletonManagerService.getSkeleton();
    const viewModel = docSkeletonManagerService == null ? void 0 : docSkeletonManagerService.getViewModel();
    if (viewModel == null || skeleton == null) {
      return false;
    }
    docSelectionManagerService.replaceDocRanges([]);
    transformer.clearSelectedObjects();
    docSelectionRenderService.setSegment("");
    docSelectionRenderService.setSegmentPage(-1);
    viewModel.setEditArea("BODY" /* BODY */);
    skeleton.calculate();
    (_a = renderObject.mainComponent) == null ? void 0 : _a.makeDirty(true);
    queueMicrotask(() => {
      const docDataModel = instanceService.getUnit(unitId);
      const snapshot = docDataModel == null ? void 0 : docDataModel.getSnapshot();
      if (snapshot == null) {
        return;
      }
      const offset = findFirstCursorOffset(snapshot);
      docSelectionManagerService.replaceDocRanges([
        {
          startOffset: offset,
          endOffset: offset
        }
      ]);
    });
    commandService.executeCommand(SidebarDocHeaderFooterPanelOperation.id, { value: "close" });
    return true;
  }
};

// ../packages/docs-ui/src/services/doc-content-insert.service.ts
var DocContentInsertService = class extends Disposable {
  constructor() {
    super(...arguments);
    __publicField(this, "_range", null);
  }
  setInsertRange(range) {
    this._range = range;
  }
  consumeInsertRange(unitId) {
    if (!this._range) {
      return null;
    }
    if (unitId && this._range.unitId !== unitId) {
      return null;
    }
    const range = this._range;
    this._range = null;
    return range;
  }
  clearInsertRange() {
    this._range = null;
  }
};

// ../packages/docs-ui/src/commands/commands/doc-horizontal-line.command.ts
var HorizontalLineCommand = {
  id: "doc.command.horizontal-line",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(BreakLineCommand.id, {
      horizontalLine: {
        padding: 5,
        color: {
          rgb: "#CDD0D8"
        },
        width: 1,
        dashStyle: 1 /* SOLID */
      },
      textRange: params == null ? void 0 : params.insertRange
    });
  }
};
var InsertHorizontalLineBellowCommand = {
  id: "doc.command.insert-horizontal-line-bellow",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    const doc = accessor.get(IUniverInstanceService).getCurrentUnitOfType(1 /* UNIVER_DOC */);
    let contentInsertRange = null;
    try {
      contentInsertRange = accessor.get(DocContentInsertService).consumeInsertRange(doc == null ? void 0 : doc.getUnitId());
    } catch {
      contentInsertRange = null;
    }
    if (contentInsertRange) {
      return commandService.syncExecuteCommand(HorizontalLineCommand.id, {
        insertRange: {
          startOffset: contentInsertRange.startOffset,
          endOffset: contentInsertRange.endOffset
        }
      });
    }
    const paragraph = getCurrentParagraph(accessor);
    if (!paragraph) {
      return false;
    }
    return commandService.syncExecuteCommand(HorizontalLineCommand.id, {
      insertRange: {
        startOffset: paragraph.startIndex + 1,
        endOffset: paragraph.startIndex + 1
      }
    });
  }
};

// ../packages/docs-ui/src/commands/commands/doc-page-setup.command.ts
var PAGE_FILL_TABLE_TOLERANCE = 2;
function getPageContentWidth(config) {
  var _a, _b, _c, _d;
  const pageWidth = (_b = (_a = config.pageSize) == null ? void 0 : _a.width) != null ? _b : 0;
  if (!pageWidth) {
    return 0;
  }
  if (config.documentFlavor === 2 /* MODERN */) {
    return Math.max(0, pageWidth - MODERN_DOCUMENT_DEFAULT_MARGIN * 2);
  }
  return Math.max(0, pageWidth - ((_c = config.marginLeft) != null ? _c : 0) - ((_d = config.marginRight) != null ? _d : 0));
}
function getTableColumnTotalWidth(table) {
  return table.tableColumns.reduce((total, column) => {
    var _a;
    return total + ((_a = column.size.width.v) != null ? _a : 0);
  }, 0);
}
function isPageFillTable(table, oldContentWidth) {
  if (oldContentWidth <= 0 || table.tableColumns.length === 0) {
    return false;
  }
  const columnTotalWidth = getTableColumnTotalWidth(table);
  const tableWidth = columnTotalWidth || table.size.width.v;
  return Math.abs(tableWidth - oldContentWidth) <= PAGE_FILL_TABLE_TOLERANCE;
}
function resizePageFillTables(jsonX, rawActions, tableSource, oldContentWidth, newContentWidth) {
  if (!tableSource || oldContentWidth <= 0 || newContentWidth <= 0 || Math.abs(oldContentWidth - newContentWidth) <= PAGE_FILL_TABLE_TOLERANCE) {
    return;
  }
  Object.entries(tableSource).forEach(([tableId, table]) => {
    if (!isPageFillTable(table, oldContentWidth)) {
      return;
    }
    const totalColumnWidth = getTableColumnTotalWidth(table);
    if (totalColumnWidth <= 0) {
      return;
    }
    const tableWidthAction = jsonX.replaceOp(["tableSource", tableId, "size", "width", "v"], table.size.width.v, newContentWidth);
    tableWidthAction && rawActions.push(tableWidthAction);
    table.tableColumns.forEach((column, index) => {
      const nextWidth = column.size.width.v / totalColumnWidth * newContentWidth;
      const action = jsonX.replaceOp(["tableSource", tableId, "tableColumns", index, "size", "width", "v"], column.size.width.v, nextWidth);
      action && rawActions.push(action);
    });
  });
}
var DocPageSetupCommand = {
  id: "docs.command.page-setup",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line complexity, max-lines-per-function
  handler: (accessor, params) => {
    if (!params) return false;
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    if (!docDataModel) return false;
    const { documentFlavor, marginLeft, marginRight, marginBottom, marginTop, pageOrient, pageSize } = params;
    const jsonX = JSONX.getInstance();
    const documentStyle = docDataModel.getDocumentStyle();
    const snapshot = docDataModel.getSnapshot();
    const newDocumentStyle = {
      ...documentStyle,
      documentFlavor: documentFlavor != null ? documentFlavor : documentStyle.documentFlavor,
      marginBottom,
      marginLeft,
      marginRight,
      marginTop,
      pageOrient,
      pageSize
    };
    const {
      marginBottom: oldMarginBottom,
      marginLeft: oldMarginLeft,
      marginRight: oldMarginRight,
      marginTop: oldMarginTop,
      pageOrient: oldPageOrient,
      pageSize: oldPageSize,
      documentFlavor: oldDocumentFlavor
    } = documentStyle;
    const rawActions = [];
    resizePageFillTables(
      jsonX,
      rawActions,
      snapshot.tableSource,
      getPageContentWidth(documentStyle),
      getPageContentWidth(newDocumentStyle)
    );
    if (documentFlavor !== void 0) {
      if (oldDocumentFlavor === void 0) {
        const action = jsonX.insertOp(["documentStyle", "documentFlavor"], documentFlavor);
        action && rawActions.push(action);
      } else {
        const action = jsonX.replaceOp(["documentStyle", "documentFlavor"], oldDocumentFlavor, documentFlavor);
        action && rawActions.push(action);
      }
    }
    if (oldMarginBottom === void 0) {
      const action = jsonX.insertOp(["documentStyle", "marginBottom"], marginBottom);
      action && rawActions.push(action);
    } else {
      const action = jsonX.replaceOp(["documentStyle", "marginBottom"], oldMarginBottom, marginBottom);
      action && rawActions.push(action);
    }
    if (oldMarginLeft === void 0) {
      const action = jsonX.insertOp(["documentStyle", "marginLeft"], marginLeft);
      action && rawActions.push(action);
    } else {
      const action = jsonX.replaceOp(["documentStyle", "marginLeft"], oldMarginLeft, marginLeft);
      action && rawActions.push(action);
    }
    if (oldMarginRight === void 0) {
      const action = jsonX.insertOp(["documentStyle", "marginRight"], marginRight);
      action && rawActions.push(action);
    } else {
      const action = jsonX.replaceOp(["documentStyle", "marginRight"], oldMarginRight, marginRight);
      action && rawActions.push(action);
    }
    if (oldMarginTop === void 0) {
      const action = jsonX.insertOp(["documentStyle", "marginTop"], marginTop);
      action && rawActions.push(action);
    } else {
      const action = jsonX.replaceOp(["documentStyle", "marginTop"], oldMarginTop, marginTop);
      action && rawActions.push(action);
    }
    if (oldPageSize === void 0) {
      const action = jsonX.insertOp(["documentStyle", "pageSize"], pageSize);
      action && rawActions.push(action);
    } else {
      const action = jsonX.replaceOp(["documentStyle", "pageSize"], oldPageSize, pageSize);
      action && rawActions.push(action);
    }
    if (oldPageOrient === void 0) {
      const action = jsonX.insertOp(["documentStyle", "pageOrient"], pageOrient);
      action && rawActions.push(action);
    } else {
      const action = jsonX.replaceOp(["documentStyle", "pageOrient"], oldPageOrient, pageOrient);
      action && rawActions.push(action);
    }
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId: docDataModel.getUnitId(),
        actions: [],
        textRanges: void 0
      }
    };
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};

// ../packages/docs-ui/src/commands/commands/doc-paragraph-setting.command.ts
var DocParagraphSettingCommand = {
  id: "doc-paragraph-setting.command",
  type: 0 /* COMMAND */,
  handler: async (accessor, config) => {
    var _a, _b, _c, _d, _e;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const docRanges = docSelectionManagerService.getDocRanges();
    if (!docDataModel || docRanges.length === 0 || !config) {
      return false;
    }
    const segmentId = docRanges[0].segmentId;
    const unitId = docDataModel.getUnitId();
    const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
    const allParagraphs = (_b = (_a = segment.getBody()) == null ? void 0 : _a.paragraphs) != null ? _b : [];
    const dataStream = (_d = (_c = segment.getBody()) == null ? void 0 : _c.dataStream) != null ? _d : "";
    const paragraphs = (_e = BuildTextUtils.range.getParagraphsInRanges(docRanges, allParagraphs, dataStream)) != null ? _e : [];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges: docRanges
      }
    };
    const memoryCursor = new MemoryCursor();
    memoryCursor.reset();
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    for (const paragraph of paragraphs) {
      const { startIndex } = paragraph;
      textX.push({
        t: "r" /* RETAIN */,
        len: startIndex - memoryCursor.cursor
      });
      const paragraphStyle = {
        ...paragraph.paragraphStyle,
        ...config.paragraph
      };
      textX.push({
        t: "r" /* RETAIN */,
        len: 1,
        body: {
          dataStream: "",
          paragraphs: [
            {
              ...paragraph,
              paragraphStyle,
              startIndex: 0
            }
          ]
        },
        coverType: 1 /* REPLACE */
      });
      memoryCursor.moveCursorTo(startIndex + 1);
    }
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return !!result;
  }
};

// ../packages/docs-ui/src/commands/commands/doc-select-all.command.ts
var DocSelectAllCommand = {
  id: "doc.command.select-all",
  type: 0 /* COMMAND */,
  handler: async (accessor) => {
    var _a;
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const docRanges = docSelectionManagerService.getDocRanges();
    const activeRange = (_a = docRanges.find((range) => range.isActive)) != null ? _a : docRanges[0];
    if (docDataModel == null || activeRange == null) {
      return false;
    }
    const { segmentId } = activeRange;
    const unitId = docDataModel.getUnitId();
    const body = docDataModel.getSelfOrHeaderFooterModel(segmentId).getSnapshot().body;
    if (body == null) {
      return false;
    }
    const { dataStream } = body;
    if (dataStream === "\r\n") {
      return true;
    }
    const wholeDocRanges = getWholeDocumentRanges(body);
    const scopedRange = getScopedSelectAllRange(body, activeRange);
    const textRanges = scopedRange && !isSameRanges(docRanges, scopedRange) ? scopedRange : wholeDocRanges;
    docSelectionManagerService.replaceDocRanges(textRanges, {
      unitId,
      subUnitId: unitId
    }, false);
    return true;
  }
};
function getWholeDocumentRanges(body) {
  var _a;
  const textRanges = [];
  let offset = 0;
  for (const table of (_a = body.tables) != null ? _a : []) {
    const { startIndex, endIndex } = table;
    if (offset !== startIndex) {
      textRanges.push(...getTextRangesByParagraphs(body, offset, startIndex - 1));
    }
    textRanges.push(getTableRectRange(table));
    offset = endIndex;
  }
  if (offset !== body.dataStream.length - 2) {
    textRanges.push(...getTextRangesByParagraphs(body, offset, body.dataStream.length - 2));
  }
  return textRanges;
}
function getTextRangesByParagraphs(body, startOffset, endOffset) {
  var _a;
  if (startOffset > endOffset) {
    return [];
  }
  const paragraphs = [...(_a = body.paragraphs) != null ? _a : []].sort((left, right) => left.startIndex - right.startIndex);
  const ranges = [];
  let offset = startOffset;
  for (const paragraph of paragraphs) {
    if (paragraph.startIndex < offset) {
      continue;
    }
    if (paragraph.startIndex > endOffset) {
      break;
    }
    ranges.push({
      startOffset: offset,
      endOffset: paragraph.startIndex,
      rangeType: "TEXT" /* TEXT */
    });
    offset = paragraph.startIndex + 1;
  }
  if (offset <= endOffset) {
    ranges.push({
      startOffset: offset,
      endOffset,
      rangeType: "TEXT" /* TEXT */
    });
  }
  return ranges;
}
function getScopedSelectAllRange(body, activeRange) {
  var _a, _b, _c, _d, _e;
  const startOffset = activeRange.startOffset;
  const endOffset = activeRange.endOffset;
  if (startOffset == null || endOffset == null) {
    return null;
  }
  const table = ((_a = body.tables) != null ? _a : []).find((item) => isRangeInside(startOffset, endOffset, item.startIndex, item.endIndex));
  if (table) {
    return [getTableRectRange(table)];
  }
  const customBlock = ((_b = body.customBlocks) != null ? _b : []).find((item) => item.startIndex >= startOffset && item.startIndex <= endOffset);
  if (customBlock) {
    return [{
      endOffset: customBlock.startIndex,
      rangeType: "TEXT" /* TEXT */,
      startOffset: customBlock.startIndex
    }];
  }
  const blockRange = ((_c = body.blockRanges) != null ? _c : []).find((item) => isRangeInside(startOffset, endOffset, item.startIndex, item.endIndex));
  if (blockRange) {
    return [{
      endOffset: Math.max(blockRange.startIndex + 1, blockRange.endIndex - 1),
      rangeType: "TEXT" /* TEXT */,
      startOffset: blockRange.startIndex + 1
    }];
  }
  const paragraphRange = clampParagraphRangeByTables(getParagraphRangeAtOffset((_d = body.paragraphs) != null ? _d : [], startOffset), (_e = body.tables) != null ? _e : [], startOffset);
  return paragraphRange ? [{
    ...paragraphRange,
    rangeType: "TEXT" /* TEXT */
  }] : null;
}
function clampParagraphRangeByTables(paragraphRange, tables, activeOffset) {
  if (!paragraphRange) {
    return null;
  }
  const nextRange = { ...paragraphRange };
  for (const table of tables) {
    if (activeOffset < table.startIndex && nextRange.startOffset < table.startIndex && table.startIndex <= nextRange.endOffset) {
      nextRange.endOffset = table.startIndex - 1;
    } else if (activeOffset >= table.endIndex && nextRange.startOffset <= table.endIndex && table.endIndex < nextRange.endOffset) {
      nextRange.startOffset = table.endIndex;
    }
  }
  return nextRange.startOffset <= nextRange.endOffset ? nextRange : null;
}
function getTableRectRange(table) {
  return {
    startOffset: table.startIndex + 3,
    endOffset: table.endIndex - 5,
    rangeType: "RECT" /* RECT */
  };
}
function getParagraphRangeAtOffset(paragraphs, offset) {
  const sortedParagraphs = [...paragraphs].sort((left, right) => left.startIndex - right.startIndex);
  for (let index = 0; index < sortedParagraphs.length; index++) {
    const paragraph = sortedParagraphs[index];
    const startOffset = index === 0 ? 0 : sortedParagraphs[index - 1].startIndex + 1;
    if (startOffset <= offset && offset <= paragraph.startIndex) {
      return {
        endOffset: paragraph.startIndex,
        startOffset
      };
    }
  }
  return null;
}
function isRangeInside(startOffset, endOffset, scopeStart, scopeEnd) {
  return startOffset >= scopeStart && endOffset <= scopeEnd;
}
function isSameRanges(currentRanges, nextRanges) {
  if (currentRanges.length !== nextRanges.length) {
    return isSameTextRangeCoverage(currentRanges, nextRanges);
  }
  return currentRanges.every((currentRange, index) => {
    const nextRange = nextRanges[index];
    return currentRange.startOffset === nextRange.startOffset && currentRange.endOffset === nextRange.endOffset && getRangeType(currentRange) === getRangeType(nextRange);
  });
}
function isSameTextRangeCoverage(currentRanges, nextRanges) {
  if (nextRanges.length !== 1 || getRangeType(nextRanges[0]) !== "TEXT" /* TEXT */) {
    return false;
  }
  const targetRange = nextRanges[0];
  if (targetRange.startOffset == null || targetRange.endOffset == null) {
    return false;
  }
  if (currentRanges.some((range) => getRangeType(range) !== "TEXT" /* TEXT */ || range.startOffset == null || range.endOffset == null)) {
    return false;
  }
  const startOffset = Math.min(...currentRanges.map((range) => range.startOffset));
  const endOffset = Math.max(...currentRanges.map((range) => range.endOffset));
  return startOffset === targetRange.startOffset && endOffset === targetRange.endOffset && currentRanges.every((range) => range.startOffset >= targetRange.startOffset && range.endOffset <= targetRange.endOffset);
}
function getRangeType(range) {
  var _a;
  return (_a = range.rangeType) != null ? _a : "TEXT" /* TEXT */;
}

// ../packages/docs-ui/src/services/doc-ime-input-manager.service.ts
var DocIMEInputManagerService = class extends RxDisposable {
  constructor(_context) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_previousActiveRange", null);
    __publicField(this, "_undoMutationParamsCache", []);
    __publicField(this, "_redoMutationParamsCache", []);
  }
  clearUndoRedoMutationParamsCache() {
    this._undoMutationParamsCache = [];
    this._redoMutationParamsCache = [];
  }
  getUndoRedoMutationParamsCache() {
    return {
      undoCache: this._undoMutationParamsCache,
      redoCache: this._redoMutationParamsCache
    };
  }
  setUndoRedoMutationParamsCache({ undoCache = [], redoCache = [] }) {
    this._undoMutationParamsCache = undoCache;
    this._redoMutationParamsCache = redoCache;
  }
  getActiveRange() {
    return this._previousActiveRange;
  }
  setActiveRange(range) {
    this._previousActiveRange = range;
  }
  pushUndoRedoMutationParams(undoParams, redoParams) {
    this._undoMutationParamsCache.push(undoParams);
    this._redoMutationParamsCache.push(redoParams);
  }
  fetchComposedUndoRedoMutationParams() {
    if (this._undoMutationParamsCache.length === 0 || this._previousActiveRange == null || this._redoMutationParamsCache.length === 0) {
      return null;
    }
    const { unitId } = this._undoMutationParamsCache[0];
    const undoMutationParams = {
      unitId,
      actions: this._undoMutationParamsCache.reverse().reduce((acc, cur) => {
        return JSONX.compose(acc, cur.actions);
      }, null),
      textRanges: []
      // Add empty array, will never use, just fix type error
    };
    const redoMutationParams = {
      unitId,
      actions: this._redoMutationParamsCache.reduce((acc, cur) => {
        return JSONX.compose(acc, cur.actions);
      }, null),
      textRanges: []
      // Add empty array, will never use, just fix type error
    };
    return { redoMutationParams, undoMutationParams, previousActiveRange: this._previousActiveRange };
  }
  dispose() {
    this._undoMutationParamsCache = [];
    this._redoMutationParamsCache = [];
    this._previousActiveRange = null;
  }
};

// ../packages/docs-ui/src/commands/commands/ime-input.command.ts
var IMEInputCommand = {
  id: "doc.command.ime-input",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler: async (accessor, params) => {
    var _a, _b, _c;
    const { unitId, newText, oldTextLen, isCompositionEnd, isCompositionStart } = params;
    const commandService = accessor.get(ICommandService);
    const renderManagerService = accessor.get(IRenderManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const docMenuStyleService = accessor.get(DocMenuStyleService);
    const imeInputManagerService = (_a = renderManagerService.getRenderById(unitId)) == null ? void 0 : _a.with(DocIMEInputManagerService);
    const docDataModel = univerInstanceService.getUnit(unitId, 1 /* UNIVER_DOC */);
    if (docDataModel == null || imeInputManagerService == null) {
      return false;
    }
    const previousActiveRange = imeInputManagerService.getActiveRange();
    if (previousActiveRange == null) {
      return false;
    }
    const { style, segmentId } = previousActiveRange;
    const body = docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (body == null) {
      return false;
    }
    const insertRange = previousActiveRange;
    Object.assign(previousActiveRange, insertRange);
    const { startOffset, endOffset } = previousActiveRange;
    const len = newText.length;
    const textRanges = [
      {
        startOffset: startOffset + len,
        endOffset: startOffset + len,
        collapsed: true,
        style
      }
    ];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges
      }
    };
    const defaultTextStyle = docMenuStyleService.getDefaultStyle();
    const styleCache = docMenuStyleService.getStyleCache();
    const curCustomRange = getCustomRangeAtPosition((_b = body.customRanges) != null ? _b : [], startOffset + oldTextLen, SHEET_EDITOR_UNITS.includes(unitId));
    const curTextRun = getTextRunAtPosition(
      body,
      isCompositionStart ? endOffset : startOffset + oldTextLen,
      defaultTextStyle,
      styleCache,
      SHEET_EDITOR_UNITS.includes(unitId)
    );
    const customDecorations = getCustomDecorationAtPosition((_c = body.customDecorations) != null ? _c : [], startOffset + oldTextLen);
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    if (!previousActiveRange.collapsed && isCompositionStart) {
      const dos = BuildTextUtils.selection.delete([previousActiveRange], body, 0, null, false);
      textX.push(...dos);
      doMutation.params.textRanges = [{
        startOffset: startOffset + len,
        endOffset: startOffset + len,
        collapsed: true
      }];
    } else {
      textX.push({
        t: "r" /* RETAIN */,
        len: startOffset
      });
    }
    if (oldTextLen > 0) {
      textX.push({
        t: "d" /* DELETE */,
        len: oldTextLen
      });
    }
    textX.push({
      t: "i" /* INSERT */,
      body: {
        dataStream: newText,
        textRuns: curTextRun ? [{
          ...curTextRun,
          st: 0,
          ed: newText.length
        }] : [],
        customRanges: curCustomRange ? [{
          ...curCustomRange,
          startIndex: 0,
          endIndex: newText.length - 1
        }] : [],
        customDecorations: customDecorations.map((customDecoration) => ({
          ...customDecoration,
          startIndex: 0,
          endIndex: newText.length - 1
        }))
      },
      len: newText.length
    });
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    doMutation.params.noHistory = !isCompositionEnd;
    doMutation.params.isCompositionEnd = isCompositionEnd;
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    imeInputManagerService.pushUndoRedoMutationParams(result, doMutation.params);
    return Boolean(result);
  }
};

// ../packages/docs-ui/src/commands/commands/inline-format.command.ts
function handleInlineFormat(preCommandId, params, commandService) {
  return commandService.executeCommand(SetInlineFormatCommand.id, {
    preCommandId,
    ...params != null ? params : {}
  });
}
var SetInlineFormatBoldCommandId = "doc.command.set-inline-format-bold";
var SetInlineFormatBoldCommand = {
  id: SetInlineFormatBoldCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return handleInlineFormat(
      SetInlineFormatBoldCommandId,
      params,
      commandService
    );
  }
};
var SetInlineFormatItalicCommandId = "doc.command.set-inline-format-italic";
var SetInlineFormatItalicCommand = {
  id: SetInlineFormatItalicCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return handleInlineFormat(
      SetInlineFormatItalicCommandId,
      params,
      commandService
    );
  }
};
var SetInlineFormatUnderlineCommandId = "doc.command.set-inline-format-underline";
var SetInlineFormatUnderlineCommand = {
  id: SetInlineFormatUnderlineCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return handleInlineFormat(
      SetInlineFormatUnderlineCommandId,
      params,
      commandService
    );
  }
};
var SetInlineFormatStrikethroughCommandId = "doc.command.set-inline-format-strikethrough";
var SetInlineFormatStrikethroughCommand = {
  id: SetInlineFormatStrikethroughCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return handleInlineFormat(
      SetInlineFormatStrikethroughCommandId,
      params,
      commandService
    );
  }
};
var SetInlineFormatSubscriptCommandId = "doc.command.set-inline-format-subscript";
var SetInlineFormatSubscriptCommand = {
  id: SetInlineFormatSubscriptCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return handleInlineFormat(
      SetInlineFormatSubscriptCommandId,
      params,
      commandService
    );
  }
};
var SetInlineFormatSuperscriptCommandId = "doc.command.set-inline-format-superscript";
var SetInlineFormatSuperscriptCommand = {
  id: SetInlineFormatSuperscriptCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return handleInlineFormat(
      SetInlineFormatSuperscriptCommandId,
      params,
      commandService
    );
  }
};
var SetInlineFormatFontSizeCommandId = "doc.command.set-inline-format-fontsize";
var SetInlineFormatFontSizeCommand = {
  id: SetInlineFormatFontSizeCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return handleInlineFormat(
      SetInlineFormatFontSizeCommandId,
      params,
      commandService
    );
  }
};
var SetInlineFormatFontFamilyCommandId = "doc.command.set-inline-format-font-family";
var SetInlineFormatFontFamilyCommand = {
  id: SetInlineFormatFontFamilyCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return handleInlineFormat(
      SetInlineFormatFontFamilyCommandId,
      params,
      commandService
    );
  }
};
var SetInlineFormatTextColorCommandId = "doc.command.set-inline-format-text-color";
var SetInlineFormatTextColorCommand = {
  id: SetInlineFormatTextColorCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return handleInlineFormat(
      SetInlineFormatTextColorCommandId,
      params,
      commandService
    );
  }
};
var SetInlineFormatTextBackgroundColorCommandId = "doc.command.set-inline-format-text-background-color";
var SetInlineFormatTextBackgroundColorCommand = {
  id: SetInlineFormatTextBackgroundColorCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return handleInlineFormat(
      SetInlineFormatTextBackgroundColorCommandId,
      params,
      commandService
    );
  }
};
var ResetInlineFormatTextBackgroundColorCommandId = "doc.command.reset-inline-format-text-background-color";
var ResetInlineFormatTextBackgroundColorCommand = {
  id: ResetInlineFormatTextBackgroundColorCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    return handleInlineFormat(
      ResetInlineFormatTextBackgroundColorCommandId,
      params,
      commandService
    );
  }
};
var COMMAND_ID_TO_FORMAT_KEY_MAP = {
  [SetInlineFormatBoldCommand.id]: "bl",
  [SetInlineFormatItalicCommand.id]: "it",
  [SetInlineFormatUnderlineCommand.id]: "ul",
  [SetInlineFormatStrikethroughCommand.id]: "st",
  [SetInlineFormatFontSizeCommand.id]: "fs",
  [SetInlineFormatFontFamilyCommand.id]: "ff",
  [SetInlineFormatTextColorCommand.id]: "cl",
  [SetInlineFormatTextBackgroundColorCommand.id]: "bg",
  [ResetInlineFormatTextBackgroundColorCommand.id]: "bg",
  [SetInlineFormatSubscriptCommand.id]: "va",
  [SetInlineFormatSuperscriptCommand.id]: "va"
};
var SetInlineFormatCommand = {
  id: "doc.command.set-inline-format",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function, complexity
  handler: async (accessor, params) => {
    var _a, _b;
    const { value, preCommandId } = params;
    const commandService = accessor.get(ICommandService);
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const docMenuStyleService = accessor.get(DocMenuStyleService);
    const textRanges = (_a = docSelectionManagerService.getTextRanges()) != null ? _a : [];
    const docRanges = textRanges.length > 0 ? textRanges.filter((range) => range.startOffset != null && range.endOffset != null) : docSelectionManagerService.getDocRanges();
    const activeRange = (_b = docRanges.find((r) => r.isActive)) != null ? _b : docRanges[0];
    if (docRanges.length === 0) {
      return false;
    }
    const { segmentId } = docRanges[0];
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    if (docDataModel == null) {
      return false;
    }
    const body = docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (body == null) {
      return false;
    }
    const unitId = docDataModel.getUnitId();
    let formatValue;
    switch (preCommandId) {
      case SetInlineFormatBoldCommand.id:
      // fallthrough
      case SetInlineFormatItalicCommand.id:
      // fallthrough
      case SetInlineFormatUnderlineCommand.id:
      // fallthrough
      case SetInlineFormatStrikethroughCommand.id:
      // fallthrough
      case SetInlineFormatSubscriptCommand.id:
      // fallthrough
      case SetInlineFormatSuperscriptCommand.id: {
        const defaultStyle = docMenuStyleService.getDefaultStyle();
        const curTextStyle = getStyleInTextRange(
          body,
          activeRange,
          defaultStyle
        );
        formatValue = getReverseFormatValueInSelection(
          curTextStyle,
          preCommandId
        );
        break;
      }
      case SetInlineFormatFontSizeCommand.id:
      case SetInlineFormatFontFamilyCommand.id: {
        formatValue = value;
        break;
      }
      case SetInlineFormatTextColorCommand.id:
      case SetInlineFormatTextBackgroundColorCommand.id: {
        formatValue = {
          rgb: value
        };
        break;
      }
      case ResetInlineFormatTextBackgroundColorCommand.id: {
        formatValue = {
          rgb: null
        };
        break;
      }
      default: {
        throw new Error(`Unknown command: ${preCommandId} in handleInlineFormat`);
      }
    }
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges: docRanges
      }
    };
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const memoryCursor = new MemoryCursor();
    memoryCursor.reset();
    for (const range of docRanges) {
      let { startOffset, endOffset, rangeType } = range;
      if (startOffset == null || endOffset == null) {
        continue;
      }
      if (rangeType === "RECT" /* RECT */) {
        startOffset = startOffset - 1;
      }
      if (startOffset === endOffset) {
        const cacheStyle = docMenuStyleService.getStyleCache();
        const key = COMMAND_ID_TO_FORMAT_KEY_MAP[preCommandId];
        docMenuStyleService.setStyleCache(
          {
            [key]: (cacheStyle == null ? void 0 : cacheStyle[key]) !== void 0 && isNeedReverseValue(key) ? getReverseFormatValue(
              cacheStyle,
              key,
              preCommandId
            ) : formatValue
          }
        );
        continue;
      }
      const body2 = {
        dataStream: "",
        textRuns: [
          {
            st: 0,
            ed: endOffset - startOffset,
            ts: {
              [COMMAND_ID_TO_FORMAT_KEY_MAP[preCommandId]]: formatValue
            }
          }
        ]
      };
      const len = startOffset - memoryCursor.cursor;
      if (len !== 0) {
        textX.push({
          t: "r" /* RETAIN */,
          len
        });
      }
      textX.push({
        t: "r" /* RETAIN */,
        body: body2,
        len: endOffset - startOffset
      });
      memoryCursor.reset();
      memoryCursor.moveCursor(endOffset);
    }
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
function isTextDecoration(value) {
  return value !== null && typeof value === "object";
}
function isNeedReverseValue(key) {
  return !/fs|ff|cl|bg/.test(key);
}
function getReverseFormatValue(ts, key, preCommandId) {
  if (/bl|it/.test(key)) {
    return (ts == null ? void 0 : ts[key]) === 1 /* TRUE */ ? 0 /* FALSE */ : 1 /* TRUE */;
  }
  if (/ul|st/.test(key)) {
    return isTextDecoration(ts == null ? void 0 : ts[key]) && (ts == null ? void 0 : ts[key]).s === 1 /* TRUE */ ? {
      s: 0 /* FALSE */
    } : {
      s: 1 /* TRUE */
    };
  }
  if (/va/.test(key)) {
    if (preCommandId === SetInlineFormatSubscriptCommand.id) {
      return (ts == null ? void 0 : ts[key]) === 2 /* SUBSCRIPT */ ? 1 /* NORMAL */ : 2 /* SUBSCRIPT */;
    } else {
      return (ts == null ? void 0 : ts[key]) === 3 /* SUPERSCRIPT */ ? 1 /* NORMAL */ : 3 /* SUPERSCRIPT */;
    }
  }
}
function getStyleInTextRange(body, textRange, defaultStyle) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
  const { startOffset, endOffset, collapsed } = textRange;
  if (collapsed) {
    const textRuns2 = (_a = body.textRuns) != null ? _a : [];
    let textRun = null;
    for (let i = textRuns2.length - 1; i >= 0; i--) {
      const curTextRun = textRuns2[i];
      if (curTextRun.st < startOffset && startOffset <= curTextRun.ed) {
        textRun = curTextRun;
        break;
      }
    }
    return (textRun == null ? void 0 : textRun.ts) ? { ...defaultStyle, ...textRun.ts } : defaultStyle;
  }
  const { textRuns = [] } = getBodySlice(body, startOffset, endOffset);
  const style = Tools.deepClone(defaultStyle);
  const fsArr = textRuns.map((t) => {
    var _a2;
    return (_a2 = t == null ? void 0 : t.ts) == null ? void 0 : _a2.fs;
  }).filter(Boolean);
  style.fs = style.fs ? Math.max(style.fs, ...fsArr) : fsArr.length ? Math.max(...fsArr) : void 0;
  style.fs = !style.fs || Number.isNaN(style.fs) ? void 0 : style.fs;
  style.ff = (_d = (_c = (_b = textRuns.find((t) => {
    var _a2;
    return ((_a2 = t.ts) == null ? void 0 : _a2.ff) != null;
  })) == null ? void 0 : _b.ts) == null ? void 0 : _c.ff) != null ? _d : style.ff;
  style.it = textRuns.length && textRuns.every((t) => {
    var _a2;
    return ((_a2 = t.ts) == null ? void 0 : _a2.it) === 1 /* TRUE */;
  }) ? 1 /* TRUE */ : 0 /* FALSE */;
  style.bl = textRuns.length && textRuns.every((t) => {
    var _a2;
    return ((_a2 = t.ts) == null ? void 0 : _a2.bl) === 1 /* TRUE */;
  }) ? 1 /* TRUE */ : 0 /* FALSE */;
  style.ul = textRuns.length && textRuns.every((t) => {
    var _a2, _b2;
    return ((_b2 = (_a2 = t.ts) == null ? void 0 : _a2.ul) == null ? void 0 : _b2.s) === 1 /* TRUE */;
  }) ? (_e = textRuns[0].ts) == null ? void 0 : _e.ul : style.ul;
  style.st = textRuns.length && textRuns.every((t) => {
    var _a2, _b2;
    return ((_b2 = (_a2 = t.ts) == null ? void 0 : _a2.st) == null ? void 0 : _b2.s) === 1 /* TRUE */;
  }) ? (_f = textRuns[0].ts) == null ? void 0 : _f.st : style.st;
  style.bg = (_i = (_h = (_g = textRuns.find((t) => {
    var _a2;
    return ((_a2 = t.ts) == null ? void 0 : _a2.bg) != null;
  })) == null ? void 0 : _g.ts) == null ? void 0 : _h.bg) != null ? _i : style.bg;
  style.cl = (_l = (_k = (_j = textRuns.find((t) => {
    var _a2;
    return ((_a2 = t.ts) == null ? void 0 : _a2.cl) != null;
  })) == null ? void 0 : _j.ts) == null ? void 0 : _k.cl) != null ? _l : style.cl;
  const vas = textRuns.filter((t) => {
    var _a2;
    return ((_a2 = t == null ? void 0 : t.ts) == null ? void 0 : _a2.va) != null;
  });
  if (vas.length > 0 && vas.length === textRuns.length) {
    const va = (_m = vas[0].ts) == null ? void 0 : _m.va;
    let isSame = true;
    for (let i = 1; i < vas.length; i++) {
      if (((_n = vas[i].ts) == null ? void 0 : _n.va) !== va) {
        isSame = false;
        break;
      }
    }
    if (isSame) {
      style.va = va;
    }
  }
  return style;
}
function getReverseFormatValueInSelection(textStyle, preCommandId) {
  const key = COMMAND_ID_TO_FORMAT_KEY_MAP[preCommandId];
  const reverseValue = getReverseFormatValue(textStyle, key, preCommandId);
  return reverseValue;
}

// ../packages/docs-ui/src/commands/commands/insert-custom-range.command.ts
var InsertCustomRangeCommand = {
  id: "doc.command.insert-custom-range",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    if (!params) return false;
    const commandService = accessor.get(ICommandService);
    const { unitId, rangeId = generateRandomId(), textRanges, properties, text, wholeEntity } = params;
    const replaceSelectionParams = {
      unitId,
      textRanges,
      body: {
        dataStream: text,
        customRanges: [{
          startIndex: 0,
          endIndex: text.length - 1,
          rangeId,
          rangeType: 5 /* CUSTOM */,
          properties,
          wholeEntity
        }]
      }
    };
    return commandService.syncExecuteCommand(ReplaceSelectionCommand.id, replaceSelectionParams);
  }
};

// ../packages/docs-ui/src/commands/commands/list.command.ts
var ListOperationCommand = {
  id: "doc.command.list-operation",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    var _a, _b, _c, _d, _e, _f;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const listType = params.listType;
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const docRanges = (_b = (_a = params.docRange) != null ? _a : docSelectionManagerService.getDocRanges()) != null ? _b : [];
    if (docDataModel == null || docRanges.length === 0) {
      return false;
    }
    const segmentId = docRanges[0].segmentId;
    const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
    const paragraphs = (_d = (_c = segment.getBody()) == null ? void 0 : _c.paragraphs) != null ? _d : [];
    const dataStream = (_f = (_e = segment.getBody()) == null ? void 0 : _e.dataStream) != null ? _f : "";
    if (!paragraphs.length) {
      return false;
    }
    const currentParagraphs = BuildTextUtils.range.getParagraphsInRanges(docRanges, paragraphs, dataStream);
    const unitId = docDataModel.getUnitId();
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges: docRanges,
        isEditing: false
      }
    };
    const memoryCursor = new MemoryCursor();
    memoryCursor.reset();
    const textX = BuildTextUtils.paragraph.bullet.switch({
      paragraphs: currentParagraphs,
      listType,
      document: docDataModel,
      segmentId
    });
    const jsonX = JSONX.getInstance();
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var ChangeListTypeCommand = {
  id: "doc.command.change-list-type",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    var _a, _b, _c, _d, _e;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const { listType } = params;
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const activeRanges = (_a = params.docRange) != null ? _a : docSelectionManagerService.getDocRanges();
    if (docDataModel == null || activeRanges == null || !activeRanges.length) {
      return false;
    }
    const { segmentId } = activeRanges[0];
    const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
    const paragraphs = (_c = (_b = segment.getBody()) == null ? void 0 : _b.paragraphs) != null ? _c : [];
    const dataStream = (_e = (_d = segment.getBody()) == null ? void 0 : _d.dataStream) != null ? _e : "";
    if (!paragraphs.length) {
      return false;
    }
    const currentParagraphs = BuildTextUtils.range.getParagraphsInRanges(activeRanges, paragraphs, dataStream);
    const unitId = docDataModel.getUnitId();
    const textX = BuildTextUtils.paragraph.bullet.set({
      paragraphs: currentParagraphs,
      listType,
      segmentId,
      document: docDataModel
    });
    if (!textX) {
      return false;
    }
    const jsonX = JSONX.getInstance();
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges: activeRanges,
        isEditing: false
      }
    };
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var ChangeListNestingLevelCommand = {
  id: "doc.command.change-list-nesting-level",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    var _a, _b, _c, _d, _e;
    if (!params) {
      return false;
    }
    const { type: type2 } = params;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const activeRange = docSelectionManagerService.getActiveTextRange();
    if (docDataModel == null || activeRange == null) {
      return false;
    }
    const { segmentId } = activeRange;
    const selections = (_a = docSelectionManagerService.getDocRanges()) != null ? _a : [];
    const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
    const paragraphs = (_c = (_b = segment.getBody()) == null ? void 0 : _b.paragraphs) != null ? _c : [];
    const dataStream = (_e = (_d = segment.getBody()) == null ? void 0 : _d.dataStream) != null ? _e : "";
    if (!paragraphs.length) {
      return false;
    }
    const currentParagraphs = BuildTextUtils.range.getParagraphsInRange(activeRange, paragraphs, dataStream);
    const unitId = docDataModel.getUnitId();
    const jsonX = JSONX.getInstance();
    const textX = BuildTextUtils.paragraph.bullet.changeNestLevel({
      paragraphs: currentParagraphs,
      type: type2,
      segmentId,
      document: docDataModel
    });
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges: selections,
        isEditing: false
      }
    };
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var BulletListCommand = {
  id: "doc.command.bullet-list",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    if (params == null ? void 0 : params.value) {
      return commandService.syncExecuteCommand(ChangeListTypeCommand.id, {
        listType: params.value,
        docRange: params.docRange
      });
    }
    return commandService.syncExecuteCommand(ListOperationCommand.id, {
      listType: "BULLET_LIST" /* BULLET_LIST */,
      docRange: params == null ? void 0 : params.docRange
    });
  }
};
var CheckListCommand = {
  id: "doc.command.check-list",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    if (params == null ? void 0 : params.value) {
      return commandService.syncExecuteCommand(ChangeListTypeCommand.id, {
        listType: params.value,
        docRange: params.docRange
      });
    }
    return commandService.syncExecuteCommand(ListOperationCommand.id, {
      listType: "CHECK_LIST" /* CHECK_LIST */,
      docRange: params == null ? void 0 : params.docRange
    });
  }
};
var ToggleCheckListCommand = {
  id: "doc.command.toggle-check-list",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    var _a;
    if (!params) {
      return false;
    }
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const { index, segmentId, textRanges } = params;
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    if (docDataModel == null) {
      return false;
    }
    const paragraphs = (_a = docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody()) == null ? void 0 : _a.paragraphs;
    if (paragraphs == null) {
      return false;
    }
    const unitId = docDataModel.getUnitId();
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges: textRanges != null ? textRanges : [],
        segmentId,
        isEditing: false
      }
    };
    const textX = BuildTextUtils.paragraph.bullet.toggleChecklist({
      document: docDataModel,
      paragraphIndex: index,
      segmentId
    });
    if (!textX) {
      return false;
    }
    const jsonX = JSONX.getInstance();
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var OrderListCommand = {
  id: "doc.command.order-list",
  type: 0 /* COMMAND */,
  handler: (accessor, params) => {
    const commandService = accessor.get(ICommandService);
    if (params == null ? void 0 : params.value) {
      return commandService.syncExecuteCommand(ChangeListTypeCommand.id, {
        listType: params.value,
        docRange: params.docRange
      });
    }
    return commandService.syncExecuteCommand(ListOperationCommand.id, {
      listType: "ORDER_LIST" /* ORDER_LIST */,
      docRange: params == null ? void 0 : params.docRange
    });
  }
};
var QuickListCommand = {
  id: "doc.command.quick-list",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler(accessor, params) {
    var _a, _b, _c;
    if (!params) {
      return false;
    }
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const activeRange = docSelectionManagerService.getActiveTextRange();
    if (docDataModel == null || activeRange == null) {
      return false;
    }
    const { segmentId, startOffset } = activeRange;
    const { listType, paragraph } = params;
    const { paragraphStart, paragraphEnd } = paragraph;
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const { paragraphStyle = {} } = paragraph;
    const paragraphProperties = PRESET_LIST_TYPE[listType].nestingLevel[0].paragraphProperties || {};
    const bulletParagraphTextStyle = paragraphProperties.textStyle;
    const ID_LENGTH3 = 6;
    let listId = generateRandomId(ID_LENGTH3);
    const paragraphs = (_b = (_a = docDataModel.getBody()) == null ? void 0 : _a.paragraphs) != null ? _b : [];
    const curIndex = paragraphs.findIndex((i) => i.startIndex === paragraph.startIndex);
    const prevParagraph = paragraphs[curIndex - 1];
    const nextParagraph = paragraphs[curIndex + 1];
    if (prevParagraph && prevParagraph.bullet && prevParagraph.bullet.listType.indexOf(listType) === 0) {
      listId = prevParagraph.bullet.listId;
    } else if (nextParagraph && nextParagraph.bullet && nextParagraph.bullet.listType.indexOf(listType) === 0) {
      listId = nextParagraph.bullet.listId;
    }
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId: docDataModel.getUnitId(),
        actions: [],
        textRanges: [{
          startOffset: paragraphStart,
          endOffset: paragraphStart,
          collapsed: true
        }],
        isEditing: false
      }
    };
    textX.push({
      t: "r" /* RETAIN */,
      len: paragraphStart
    });
    textX.push({
      t: "d" /* DELETE */,
      len: startOffset - paragraphStart
    });
    if (paragraphEnd > startOffset) {
      textX.push({
        t: "r" /* RETAIN */,
        len: paragraphEnd - startOffset
      });
    }
    textX.push({
      t: "r" /* RETAIN */,
      len: 1,
      body: {
        dataStream: "",
        paragraphs: [
          {
            startIndex: 0,
            paragraphStyle: {
              ...paragraphStyle,
              textStyle: {
                ...paragraphStyle.textStyle,
                ...bulletParagraphTextStyle
              },
              indentFirstLine: void 0
            },
            bullet: {
              ...(_c = paragraph.bullet) != null ? _c : {
                nestingLevel: 0,
                textStyle: {
                  fs: 20
                }
              },
              listType,
              listId
            }
          }
        ]
      }
    });
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
function insertList(accessor, listType) {
  var _a, _b, _c;
  const commandService = accessor.get(ICommandService);
  const docDataModel = accessor.get(IUniverInstanceService).getCurrentUnitOfType(1 /* UNIVER_DOC */);
  if (!docDataModel) {
    return false;
  }
  let contentInsertRange = null;
  try {
    contentInsertRange = accessor.get(DocContentInsertService).consumeInsertRange(docDataModel.getUnitId());
  } catch {
    contentInsertRange = null;
  }
  const paragraph = contentInsertRange ? null : getCurrentParagraph(accessor);
  if (!contentInsertRange && !paragraph) {
    return false;
  }
  const sourceParagraph = paragraph || void 0;
  const insertOffset = (_a = contentInsertRange == null ? void 0 : contentInsertRange.startOffset) != null ? _a : sourceParagraph.startIndex + 1;
  const sourceBullet = sourceParagraph == null ? void 0 : sourceParagraph.bullet;
  const textX = BuildTextUtils.selection.replace({
    doc: docDataModel,
    selection: {
      startOffset: insertOffset,
      endOffset: (_b = contentInsertRange == null ? void 0 : contentInsertRange.endOffset) != null ? _b : insertOffset,
      collapsed: true
    },
    body: {
      dataStream: "\r",
      paragraphs: [
        {
          startIndex: 0,
          paragraphStyle: {
            ...(_c = sourceParagraph == null ? void 0 : sourceParagraph.paragraphStyle) != null ? _c : {}
          },
          bullet: {
            listType,
            listId: (sourceBullet == null ? void 0 : sourceBullet.listType) === listType ? sourceBullet.listId : generateRandomId(6),
            nestingLevel: (sourceBullet == null ? void 0 : sourceBullet.listType) === listType ? sourceBullet.nestingLevel : 0
          }
        }
      ]
    }
  });
  if (!textX) {
    return false;
  }
  const doMutation = {
    id: RichTextEditingMutation.id,
    params: {
      unitId: docDataModel.getUnitId(),
      actions: [],
      textRanges: [{
        startOffset: insertOffset,
        endOffset: insertOffset,
        collapsed: true
      }],
      isEditing: false
    }
  };
  const jsonX = JSONX.getInstance();
  const path = getRichTextEditPath2(docDataModel);
  doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
  const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
  return Boolean(result);
}
var InsertBulletListBellowCommand = {
  id: "doc.command.insert-bullet-list-bellow",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    return insertList(accessor, "BULLET_LIST" /* BULLET_LIST */);
  }
};
var InsertOrderListBellowCommand = {
  id: "doc.command.insert-order-list-bellow",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    return insertList(accessor, "ORDER_LIST" /* ORDER_LIST */);
  }
};
var InsertCheckListBellowCommand = {
  id: "doc.command.insert-check-list-bellow",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    return insertList(accessor, "CHECK_LIST" /* CHECK_LIST */);
  }
};

// ../packages/docs-ui/src/commands/commands/paragraph-align.command.ts
var AlignOperationCommand = {
  id: "doc.command.align-action",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler: (accessor, params) => {
    var _a, _b, _c, _d;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const { alignType } = params;
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    if (docDataModel == null) {
      return false;
    }
    const allRanges = docSelectionManagerService.getDocRanges();
    if (allRanges.length === 0) {
      return false;
    }
    const segmentId = allRanges[0].segmentId;
    const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
    const paragraphs = (_b = (_a = segment.getBody()) == null ? void 0 : _a.paragraphs) != null ? _b : [];
    const dataStream = (_d = (_c = segment.getBody()) == null ? void 0 : _c.dataStream) != null ? _d : "";
    if (paragraphs == null) {
      return false;
    }
    const currentParagraphs = BuildTextUtils.range.getParagraphsInRanges(allRanges, paragraphs, dataStream);
    const unitId = docDataModel.getUnitId();
    const isAlreadyAligned = currentParagraphs.every((paragraph) => {
      var _a2;
      return ((_a2 = paragraph.paragraphStyle) == null ? void 0 : _a2.horizontalAlign) === alignType;
    });
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges: allRanges
      }
    };
    const memoryCursor = new MemoryCursor();
    memoryCursor.reset();
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    for (const paragraph of currentParagraphs) {
      const { startIndex } = paragraph;
      textX.push({
        t: "r" /* RETAIN */,
        len: startIndex - memoryCursor.cursor
      });
      const paragraphStyle = {
        ...paragraph.paragraphStyle,
        horizontalAlign: isAlreadyAligned ? 0 /* UNSPECIFIED */ : alignType
      };
      textX.push({
        t: "r" /* RETAIN */,
        len: 1,
        body: {
          dataStream: "",
          paragraphs: [
            {
              ...paragraph,
              paragraphStyle,
              startIndex: 0
            }
          ]
        },
        coverType: 1 /* REPLACE */
      });
      memoryCursor.moveCursorTo(startIndex + 1);
    }
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var AlignLeftCommand = {
  id: "doc.command.align-left",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(AlignOperationCommand.id, {
      alignType: 1 /* LEFT */
    });
  }
};
var AlignCenterCommand = {
  id: "doc.command.align-center",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(AlignOperationCommand.id, {
      alignType: 2 /* CENTER */
    });
  }
};
var AlignRightCommand = {
  id: "doc.command.align-right",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(AlignOperationCommand.id, {
      alignType: 3 /* RIGHT */
    });
  }
};
var AlignJustifyCommand = {
  id: "doc.command.align-justify",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(AlignOperationCommand.id, {
      alignType: 4 /* JUSTIFIED */
    });
  }
};

// ../packages/docs-ui/src/commands/operations/set-doc-zoom-ratio.operation.ts
var SetDocZoomRatioUndoMutationFactory = (accessor, params) => {
  const univerInstanceService = accessor.get(IUniverInstanceService);
  const documentModel = univerInstanceService.getUnit(params.unitId, 1 /* UNIVER_DOC */);
  documentModel == null ? void 0 : documentModel.setZoomRatio(params.zoomRatio);
  return { ...params };
};
var SetDocZoomRatioOperation = {
  id: "doc.operation.set-zoom-ratio",
  type: 1 /* OPERATION */,
  handler: (accessor, params) => {
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const documentModel = univerInstanceService.getUnit(params.unitId, 1 /* UNIVER_DOC */);
    if (!documentModel) {
      return false;
    }
    documentModel.setZoomRatio(params.zoomRatio);
    return true;
  }
};

// ../packages/docs-ui/src/commands/commands/set-doc-zoom-ratio.command.ts
var SetDocZoomRatioCommand = {
  type: 0 /* COMMAND */,
  id: "doc.command.set-zoom-ratio",
  handler: async (accessor, params) => {
    var _a, _b, _c;
    const commandService = accessor.get(ICommandService);
    const undoRedoService = accessor.get(IUndoRedoService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    let documentId = (_a = univerInstanceService.getCurrentUniverDocInstance()) == null ? void 0 : _a.getUnitId();
    if (!documentId) return false;
    let zoomRatio = 1;
    if (params) {
      documentId = (_b = params.documentId) != null ? _b : documentId;
      zoomRatio = (_c = params.zoomRatio) != null ? _c : zoomRatio;
    }
    const workbook = univerInstanceService.getUniverDocInstance(documentId);
    if (!workbook) return false;
    const setZoomRatioMutationParams = {
      zoomRatio,
      unitId: documentId
    };
    const undoMutationParams = SetDocZoomRatioUndoMutationFactory(accessor, setZoomRatioMutationParams);
    const result = commandService.syncExecuteCommand(SetDocZoomRatioOperation.id, setZoomRatioMutationParams);
    if (result) {
      undoRedoService.pushUndoRedo({
        unitID: documentId,
        undoMutations: [{ id: SetDocZoomRatioOperation.id, params: undoMutationParams }],
        redoMutations: [{ id: SetDocZoomRatioOperation.id, params: setZoomRatioMutationParams }]
      });
      return true;
    }
    return false;
  }
};

// ../packages/docs-ui/src/commands/commands/set-heading.command.ts
var SetParagraphNamedStyleCommand = {
  id: "doc.command.set-paragraph-named-style",
  type: 0 /* COMMAND */,
  handler(accessor, params) {
    var _a;
    if (!params) {
      return false;
    }
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const doc = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    if (!doc) {
      return false;
    }
    const unitId = doc.getUnitId();
    const selectionService = accessor.get(DocSelectionManagerService);
    const contentInsertRange = params.textRanges ? null : consumeContentInsertRange(accessor, unitId);
    if (contentInsertRange) {
      return insertNamedStyleParagraph(accessor, doc, params.value, contentInsertRange.startOffset, contentInsertRange.endOffset);
    }
    const selections = (_a = params.textRanges) != null ? _a : selectionService.getTextRanges({ unitId, subUnitId: unitId });
    if (!(selections == null ? void 0 : selections.length)) {
      return false;
    }
    const segmentId = selections[0].segmentId;
    const textX = BuildTextUtils.paragraph.style.set({
      document: doc.getSelfOrHeaderFooterModel(segmentId),
      textRanges: selections,
      style: {
        namedStyleType: params.value,
        headingId: !params.value || params.value === 1 /* NORMAL_TEXT */ ? void 0 : generateRandomId(6),
        spaceAbove: void 0,
        spaceBelow: void 0,
        lineSpacing: void 0
      },
      paragraphTextRun: {}
    });
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        actions: [],
        textRanges: selections,
        unitId,
        segmentId
      }
    };
    const jsonX = JSONX.getInstance();
    const path = getRichTextEditPath2(doc, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const commandService = accessor.get(ICommandService);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
function consumeContentInsertRange(accessor, unitId) {
  try {
    return accessor.get(DocContentInsertService).consumeInsertRange(unitId);
  } catch {
    return null;
  }
}
function insertNamedStyleParagraph(accessor, doc, namedStyleType, startOffset, endOffset) {
  const textX = BuildTextUtils.selection.replace({
    doc,
    selection: {
      startOffset,
      endOffset,
      collapsed: startOffset === endOffset
    },
    body: {
      dataStream: "\r",
      paragraphs: [{
        startIndex: 0,
        paragraphStyle: {
          namedStyleType,
          headingId: !namedStyleType || namedStyleType === 1 /* NORMAL_TEXT */ ? void 0 : generateRandomId(6)
        }
      }]
    }
  });
  if (!textX) {
    return false;
  }
  const jsonX = JSONX.getInstance();
  const commandService = accessor.get(ICommandService);
  return Boolean(commandService.syncExecuteCommand(RichTextEditingMutation.id, {
    actions: jsonX.editOp(textX.serialize(), getRichTextEditPath2(doc)),
    isEditing: false,
    textRanges: [{
      startOffset,
      endOffset: startOffset,
      collapsed: true
    }],
    unitId: doc.getUnitId()
  }));
}
var QuickHeadingCommand = {
  id: "doc.command.quick-heading",
  type: 0 /* COMMAND */,
  handler(accessor, params) {
    var _a, _b, _c, _d;
    if (!params) {
      return false;
    }
    const { value } = params;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const activeRange = docSelectionManagerService.getActiveTextRange();
    if (docDataModel == null || activeRange == null || !activeRange.collapsed) {
      return false;
    }
    const { segmentId, startOffset } = activeRange;
    const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
    const paragraphs = (_b = (_a = segment.getBody()) == null ? void 0 : _a.paragraphs) != null ? _b : [];
    const dataStream = (_d = (_c = segment.getBody()) == null ? void 0 : _c.dataStream) != null ? _d : "";
    const paragraph = BuildTextUtils.paragraph.util.getParagraphsInRange(activeRange, paragraphs, dataStream)[0];
    if (!paragraph) {
      return false;
    }
    const { paragraphStart } = paragraph;
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const { paragraphStyle = {} } = paragraph;
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId: docDataModel.getUnitId(),
        actions: [],
        textRanges: [{
          startOffset: paragraphStart,
          endOffset: paragraphStart,
          collapsed: true
        }],
        isEditing: false
      }
    };
    textX.push({
      t: "r" /* RETAIN */,
      len: paragraphStart
    });
    BuildTextUtils.paragraph.style.set({
      textRanges: [activeRange],
      document: docDataModel,
      style: {
        ...paragraphStyle,
        headingId: generateRandomId(6),
        namedStyleType: value
      },
      cursor: startOffset,
      deleteLen: startOffset - paragraphStart,
      textX,
      paragraphTextRun: {}
    });
    const path = getRichTextEditPath2(docDataModel, segmentId);
    doMutation.params.actions = jsonX.editOp(textX.serialize(), path);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var QUICK_HEADING_MAP = {
  "#": 4 /* HEADING_1 */,
  "##": 5 /* HEADING_2 */,
  "###": 6 /* HEADING_3 */,
  "####": 7 /* HEADING_4 */,
  "#####": 8 /* HEADING_5 */
};
var H1HeadingCommand = {
  id: "doc.command.h1-heading",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(SetParagraphNamedStyleCommand.id, {
      value: 4 /* HEADING_1 */
    });
  }
};
var H2HeadingCommand = {
  id: "doc.command.h2-heading",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(SetParagraphNamedStyleCommand.id, {
      value: 5 /* HEADING_2 */
    });
  }
};
var H3HeadingCommand = {
  id: "doc.command.h3-heading",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(SetParagraphNamedStyleCommand.id, {
      value: 6 /* HEADING_3 */
    });
  }
};
var H4HeadingCommand = {
  id: "doc.command.h4-heading",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(SetParagraphNamedStyleCommand.id, {
      value: 7 /* HEADING_4 */
    });
  }
};
var H5HeadingCommand = {
  id: "doc.command.h5-heading",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(SetParagraphNamedStyleCommand.id, {
      value: 8 /* HEADING_5 */
    });
  }
};
var NormalTextHeadingCommand = {
  id: "doc.command.normal-text-heading",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(SetParagraphNamedStyleCommand.id, {
      value: 1 /* NORMAL_TEXT */
    });
  }
};
var TitleHeadingCommand = {
  id: "doc.command.title",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(SetParagraphNamedStyleCommand.id, {
      value: 2 /* TITLE */
    });
  }
};
var SubtitleHeadingCommand = {
  id: "doc.command.subtitle-heading",
  type: 0 /* COMMAND */,
  handler: (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.syncExecuteCommand(SetParagraphNamedStyleCommand.id, {
      value: 3 /* SUBTITLE */
    });
  }
};

// ../packages/docs-ui/src/commands/commands/switch-doc-mode.command.ts
var SwitchDocModeCommand = {
  id: "doc.command.switch-mode",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function, complexity
  handler: (accessor) => {
    var _a, _b, _c, _d, _e;
    const commandService = accessor.get(ICommandService);
    const renderManagerService = accessor.get(IRenderManagerService);
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    if (docDataModel == null) {
      return false;
    }
    const unitId = docDataModel.getUnitId();
    const skeleton = (_a = renderManagerService.getRenderById(unitId)) == null ? void 0 : _a.with(DocSkeletonManagerService).getSkeleton();
    const docSelectionRenderService = (_b = renderManagerService.getRenderById(unitId)) == null ? void 0 : _b.with(DocSelectionRenderService);
    if (skeleton == null || docSelectionRenderService == null) {
      return false;
    }
    const segmentId = docSelectionRenderService == null ? void 0 : docSelectionRenderService.getSegment();
    const segmentPage = docSelectionRenderService == null ? void 0 : docSelectionRenderService.getSegmentPage();
    const oldDocumentFlavor = docDataModel.getSnapshot().documentStyle.documentFlavor;
    const docRanges = docSelectionManagerService.getDocRanges();
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges: oldDocumentFlavor === 1 /* TRADITIONAL */ && !!segmentId ? [] : docRanges
      }
    };
    const jsonX = JSONX.getInstance();
    const rawActions = [];
    let action;
    if (oldDocumentFlavor === void 0) {
      action = jsonX.insertOp(["documentStyle", "documentFlavor"], 2 /* MODERN */);
    } else {
      if (oldDocumentFlavor === 2 /* MODERN */) {
        action = jsonX.replaceOp(["documentStyle", "documentFlavor"], oldDocumentFlavor, 1 /* TRADITIONAL */);
      } else {
        action = jsonX.replaceOp(["documentStyle", "documentFlavor"], oldDocumentFlavor, 2 /* MODERN */);
      }
    }
    if (action) {
      rawActions.push(action);
    } else {
      return false;
    }
    if (oldDocumentFlavor !== 2 /* MODERN */) {
      const snapshot = docDataModel.getSnapshot();
      const { drawings = {}, body } = snapshot;
      const customBlocks = (_c = body == null ? void 0 : body.customBlocks) != null ? _c : [];
      for (const drawingId in drawings) {
        const drawing = drawings[drawingId];
        const customBlock = customBlocks.find((block) => block.blockId === drawingId);
        if (customBlock == null) {
          continue;
        }
        const drawingPositionV = drawing.docTransform.positionV;
        const { relativeFrom: prevRelativeFrom, posOffset: prevPosOffset } = drawingPositionV;
        if (prevRelativeFrom === 1 /* PARAGRAPH */) {
          continue;
        }
        const { startIndex } = customBlock;
        const glyph = skeleton.findNodeByCharIndex(startIndex, segmentId, segmentPage);
        const line = (_d = glyph == null ? void 0 : glyph.parent) == null ? void 0 : _d.parent;
        const column = line == null ? void 0 : line.parent;
        const paragraphStartLine = column == null ? void 0 : column.lines.find((l) => l.paragraphIndex === (line == null ? void 0 : line.paragraphIndex) && l.paragraphStart);
        const page = (_e = column == null ? void 0 : column.parent) == null ? void 0 : _e.parent;
        if (glyph == null || line == null || paragraphStartLine == null || column == null || page == null) {
          continue;
        }
        let delta = 0;
        if (prevRelativeFrom === 2 /* LINE */) {
          delta -= line.top;
        } else if (prevRelativeFrom === 0 /* PAGE */) {
          delta += page.marginTop;
        }
        delta += paragraphStartLine.top;
        const newPositionV = {
          ...drawingPositionV,
          relativeFrom: 1 /* PARAGRAPH */,
          posOffset: (prevPosOffset != null ? prevPosOffset : 0) - delta
        };
        const updateDrawingAction = jsonX.replaceOp(["drawings", drawingId, "docTransform", "positionV"], drawingPositionV, newPositionV);
        if (updateDrawingAction) {
          rawActions.push(updateDrawingAction);
        }
      }
    }
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};

// ../packages/docs-ui/src/commands/commands/table/doc-table-create.command.ts
var CreateDocTableCommandId = "doc.command.create-table";
var CreateDocTableCommand = {
  id: CreateDocTableCommandId,
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler: async (accessor, params) => {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const { rowCount, colCount } = params;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const docMenuStyleService = accessor.get(DocMenuStyleService);
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    if (docDataModel == null) {
      return false;
    }
    let contentInsertRange = null;
    try {
      contentInsertRange = accessor.get(DocContentInsertService).consumeInsertRange(docDataModel.getUnitId());
    } catch {
      contentInsertRange = null;
    }
    const activeRange = docSelectionManagerService.getActiveTextRange();
    if (activeRange == null && contentInsertRange == null) {
      return false;
    }
    const segmentId = (_b = (_a = contentInsertRange == null ? void 0 : contentInsertRange.segmentId) != null ? _a : activeRange == null ? void 0 : activeRange.segmentId) != null ? _b : "";
    const segmentPage = activeRange == null ? void 0 : activeRange.segmentPage;
    const body = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (body == null) {
      return false;
    }
    const unitId = docDataModel.getUnitId();
    const docSkeletonManagerService = getCommandSkeleton(accessor, unitId);
    const skeleton = docSkeletonManagerService == null ? void 0 : docSkeletonManagerService.getSkeleton();
    if (skeleton == null) {
      return false;
    }
    const startOffset = (_c = contentInsertRange == null ? void 0 : contentInsertRange.startOffset) != null ? _c : activeRange.startOffset;
    const paragraphs = (_d = body.paragraphs) != null ? _d : [];
    const prevParagraph = paragraphs.find((p) => p.startIndex >= startOffset);
    const curGlyph = skeleton.findNodeByCharIndex(startOffset, segmentId, segmentPage);
    if (curGlyph == null) {
      return false;
    }
    const needCreateParagraph = true;
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const rawActions = [];
    const cursor = startOffset + (needCreateParagraph ? 4 : 3);
    const textRanges = [{
      startOffset: cursor,
      endOffset: cursor,
      collapsed: true
    }];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges
      }
    };
    if (startOffset > 0) {
      textX.push({
        t: "r" /* RETAIN */,
        len: startOffset
      });
    }
    if (needCreateParagraph) {
      textX.push({
        t: "i" /* INSERT */,
        body: {
          dataStream: "\r" /* PARAGRAPH */,
          paragraphs: generateParagraphs("\r" /* PARAGRAPH */, prevParagraph)
        },
        len: 1
      });
    }
    const defaultTextStyle = docMenuStyleService.getDefaultStyle();
    const styleCache = docMenuStyleService.getStyleCache();
    const curTextRun = getTextRunAtPosition(
      body,
      startOffset,
      defaultTextStyle,
      styleCache
    );
    const { dataStream: tableDataStream, paragraphs: tableParagraphs, sectionBreaks } = genEmptyTable(rowCount, colCount);
    const page = (_h = (_g = (_f = (_e = curGlyph.parent) == null ? void 0 : _e.parent) == null ? void 0 : _f.parent) == null ? void 0 : _g.parent) == null ? void 0 : _h.parent;
    if (page == null) {
      return false;
    }
    const { pageWidth, marginLeft, marginRight } = page;
    const tableSource = genTableSource(rowCount, colCount, pageWidth - marginLeft - marginRight);
    textX.push({
      t: "i" /* INSERT */,
      body: {
        dataStream: tableDataStream,
        paragraphs: tableParagraphs,
        sectionBreaks,
        textRuns: [{
          ...curTextRun,
          st: 0,
          ed: tableDataStream.length
        }],
        tables: [
          {
            startIndex: 0,
            endIndex: tableDataStream.length,
            tableId: tableSource.tableId
          }
        ]
      },
      len: tableDataStream.length
    });
    const path = getRichTextEditPath2(docDataModel, segmentId);
    rawActions.push(jsonX.editOp(textX.serialize(), path));
    const insertTableSource = jsonX.insertOp(["tableSource", tableSource.tableId], tableSource);
    rawActions.push(insertTableSource);
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};

// ../packages/docs-ui/src/commands/commands/table/doc-table-delete.command.ts
var DocTableDeleteRowsCommand = {
  id: "doc.table.delete-rows",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler: async (accessor) => {
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const activeRectRanges = docSelectionManagerService.getRectRanges();
    const activeTextRange = docSelectionManagerService.getActiveTextRange();
    const rangeInfo = getRangeInfoFromRanges(activeTextRange, activeRectRanges);
    if (rangeInfo == null) {
      return false;
    }
    const { segmentId } = rangeInfo;
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    const body = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (docDataModel == null || body == null) {
      return false;
    }
    const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
    if (docSkeletonManagerService == null) {
      return false;
    }
    const viewModel = docSkeletonManagerService.getViewModel();
    const unitId = docDataModel == null ? void 0 : docDataModel.getUnitId();
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const actionParams = getDeleteRowsActionsParams(rangeInfo, viewModel);
    if (actionParams == null) {
      return false;
    }
    const { offset, rowIndexes, len, tableId, cursor, selectWholeTable } = actionParams;
    if (selectWholeTable) {
      return commandService.executeCommand(DocTableDeleteTableCommand.id);
    }
    const rawActions = [];
    const textRanges = [{
      startOffset: cursor,
      endOffset: cursor,
      collapsed: true
    }];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges
      }
    };
    if (offset > 0) {
      textX.push({
        t: "r" /* RETAIN */,
        len: offset
      });
    }
    textX.push({
      t: "d" /* DELETE */,
      len
    });
    const path = getRichTextEditPath2(docDataModel, segmentId);
    rawActions.push(jsonX.editOp(textX.serialize(), path));
    for (const index of rowIndexes.reverse()) {
      const action = jsonX.removeOp(["tableSource", tableId, "tableRows", index]);
      rawActions.push(action);
    }
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var DocTableDeleteColumnsCommand = {
  id: "doc.table.delete-columns",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler: async (accessor) => {
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const activeRectRanges = docSelectionManagerService.getRectRanges();
    const activeTextRange = docSelectionManagerService.getActiveTextRange();
    const rangeInfo = getRangeInfoFromRanges(activeTextRange, activeRectRanges);
    if (rangeInfo == null) {
      return false;
    }
    const { segmentId } = rangeInfo;
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    const body = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (docDataModel == null || body == null) {
      return false;
    }
    const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
    if (docSkeletonManagerService == null) {
      return false;
    }
    const viewModel = docSkeletonManagerService.getViewModel();
    const unitId = docDataModel == null ? void 0 : docDataModel.getUnitId();
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const actionParams = getDeleteColumnsActionParams(rangeInfo, viewModel);
    if (actionParams == null) {
      return false;
    }
    const { offsets, columnIndexes, tableId, cursor, rowCount, selectWholeTable } = actionParams;
    if (selectWholeTable) {
      return commandService.executeCommand(DocTableDeleteTableCommand.id);
    }
    const rawActions = [];
    const textRanges = [{
      startOffset: cursor,
      endOffset: cursor,
      collapsed: true
    }];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges
      }
    };
    for (const offset of offsets) {
      const { retain, delete: deleteLen } = offset;
      if (retain > 0) {
        textX.push({
          t: "r" /* RETAIN */,
          len: retain
        });
      }
      textX.push({
        t: "d" /* DELETE */,
        len: deleteLen
      });
    }
    const path = getRichTextEditPath2(docDataModel, segmentId);
    rawActions.push(jsonX.editOp(textX.serialize(), path));
    columnIndexes.reverse();
    for (let i = 0; i < rowCount; i++) {
      for (const index of columnIndexes) {
        const action = jsonX.removeOp(["tableSource", tableId, "tableRows", i, "tableCells", index]);
        rawActions.push(action);
      }
    }
    for (const index of columnIndexes) {
      const action = jsonX.removeOp(["tableSource", tableId, "tableColumns", index]);
      rawActions.push(action);
    }
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var DocTableDeleteTableCommand = {
  id: "doc.table.delete-table",
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler: async (accessor) => {
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const activeRectRanges = docSelectionManagerService.getRectRanges();
    const activeTextRange = docSelectionManagerService.getActiveTextRange();
    const rangeInfo = getRangeInfoFromRanges(activeTextRange, activeRectRanges);
    if (rangeInfo == null) {
      return false;
    }
    const { segmentId } = rangeInfo;
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    const body = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (docDataModel == null || body == null) {
      return false;
    }
    const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
    if (docSkeletonManagerService == null) {
      return false;
    }
    const viewModel = docSkeletonManagerService.getViewModel();
    const unitId = docDataModel == null ? void 0 : docDataModel.getUnitId();
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const actionParams = getDeleteTableActionParams(rangeInfo, viewModel);
    if (actionParams == null) {
      return false;
    }
    const { offset, len, tableId, cursor } = actionParams;
    const rawActions = [];
    const textRanges = [{
      startOffset: cursor,
      endOffset: cursor,
      collapsed: true
    }];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges
      }
    };
    if (offset > 0) {
      textX.push({
        t: "r" /* RETAIN */,
        len: offset
      });
    }
    textX.push({
      t: "d" /* DELETE */,
      len
    });
    const path = getRichTextEditPath2(docDataModel, segmentId);
    rawActions.push(jsonX.editOp(textX.serialize(), path));
    const action = jsonX.removeOp(["tableSource", tableId]);
    rawActions.push(action);
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};

// ../packages/docs-ui/src/commands/commands/table/doc-table-insert.command.ts
var DocTableInsertRowCommandId = "doc.command.table-insert-row";
var DocTableInsertColumnCommandId = "doc.command.table-insert-column";
var DocTableInsertRowAboveCommandId = "doc.command.table-insert-row-above";
var DocTableInsertRowBellowCommandId = "doc.command.table-insert-row-bellow";
var DocTableInsertColumnLeftCommandId = "doc.command.table-insert-column-left";
var DocTableInsertColumnRightCommandId = "doc.command.table-insert-column-right";
var DocTableInsertRowAboveCommand = {
  id: DocTableInsertRowAboveCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.executeCommand(DocTableInsertRowCommandId, { position: 0 /* ABOVE */ });
  }
};
var DocTableInsertRowBellowCommand = {
  id: DocTableInsertRowBellowCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.executeCommand(DocTableInsertRowCommandId, { position: 1 /* BELLOW */ });
  }
};
var DocTableInsertColumnLeftCommand = {
  id: DocTableInsertColumnLeftCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.executeCommand(DocTableInsertColumnCommandId, { position: 0 /* LEFT */ });
  }
};
var DocTableInsertColumnRightCommand = {
  id: DocTableInsertColumnRightCommandId,
  type: 0 /* COMMAND */,
  handler: async (accessor) => {
    const commandService = accessor.get(ICommandService);
    return commandService.executeCommand(DocTableInsertColumnCommandId, { position: 1 /* RIGHT */ });
  }
};
var DocTableInsertRowCommand = {
  id: DocTableInsertRowCommandId,
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler: async (accessor, params) => {
    const { position } = params;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const activeRectRanges = docSelectionManagerService.getRectRanges();
    const activeTextRange = docSelectionManagerService.getActiveTextRange();
    const rangeInfo = getRangeInfoFromRanges(activeTextRange, activeRectRanges);
    if (rangeInfo == null) {
      return false;
    }
    const { segmentId } = rangeInfo;
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const body = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (docDataModel == null || body == null) {
      return false;
    }
    const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
    if (docSkeletonManagerService == null) {
      return false;
    }
    const viewModel = docSkeletonManagerService.getViewModel();
    const unitId = docDataModel == null ? void 0 : docDataModel.getUnitId();
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const actionParams = getInsertRowActionsParams(rangeInfo, position, viewModel);
    if (actionParams == null) {
      return false;
    }
    const { offset, colCount, tableId, insertRowIndex } = actionParams;
    const rawActions = [];
    const cursor = offset + 2;
    const textRanges = [{
      startOffset: cursor,
      endOffset: cursor,
      collapsed: true
    }];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges
      }
    };
    if (offset > 0) {
      textX.push({
        t: "r" /* RETAIN */,
        len: offset
      });
    }
    const insertBody = getInsertRowBody(colCount);
    textX.push({
      t: "i" /* INSERT */,
      body: insertBody,
      len: insertBody.dataStream.length
    });
    const path = getRichTextEditPath2(docDataModel, segmentId);
    rawActions.push(jsonX.editOp(textX.serialize(), path));
    const insertRow = getEmptyTableRow(colCount);
    const insertTableSource = jsonX.insertOp(["tableSource", tableId, "tableRows", insertRowIndex], insertRow);
    rawActions.push(insertTableSource);
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};
var DocTableInsertColumnCommand = {
  id: DocTableInsertColumnCommandId,
  type: 0 /* COMMAND */,
  // eslint-disable-next-line max-lines-per-function
  handler: async (accessor, params) => {
    var _a, _b, _c, _d;
    const { position } = params;
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const commandService = accessor.get(ICommandService);
    const activeRectRanges = docSelectionManagerService.getRectRanges();
    const activeTextRange = docSelectionManagerService.getActiveTextRange();
    const rangeInfo = getRangeInfoFromRanges(activeTextRange, activeRectRanges);
    if (rangeInfo == null) {
      return false;
    }
    const { segmentId } = rangeInfo;
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    const body = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (docDataModel == null || body == null) {
      return false;
    }
    const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
    if (docSkeletonManagerService == null) {
      return false;
    }
    const viewModel = docSkeletonManagerService.getViewModel();
    const unitId = docDataModel == null ? void 0 : docDataModel.getUnitId();
    const textX = new TextX();
    const jsonX = JSONX.getInstance();
    const actionParams = getInsertColumnActionsParams(rangeInfo, position, viewModel);
    if (actionParams == null) {
      return false;
    }
    const { offsets, columnIndex, tableId, rowCount } = actionParams;
    const rawActions = [];
    const cursor = offsets[0] + 1;
    const textRanges = [{
      startOffset: cursor,
      endOffset: cursor,
      collapsed: true
    }];
    const doMutation = {
      id: RichTextEditingMutation.id,
      params: {
        unitId,
        actions: [],
        textRanges
      }
    };
    for (const offset of offsets) {
      textX.push({
        t: "r" /* RETAIN */,
        len: offset
      });
      const insertBody = getInsertColumnBody();
      textX.push({
        t: "i" /* INSERT */,
        body: insertBody,
        len: insertBody.dataStream.length
      });
    }
    const path = getRichTextEditPath2(docDataModel, segmentId);
    rawActions.push(jsonX.editOp(textX.serialize(), path));
    for (let i = 0; i < rowCount; i++) {
      const insertCell = getEmptyTableCell();
      const insertTableSource = jsonX.insertOp(["tableSource", tableId, "tableRows", i, "tableCells", columnIndex], insertCell);
      rawActions.push(insertTableSource);
    }
    const snapshot = docDataModel.getSnapshot();
    const documentStyle = snapshot.documentStyle;
    const { marginLeft = 0, marginRight = 0 } = documentStyle;
    const pageWidth = ((_b = (_a = documentStyle.pageSize) == null ? void 0 : _a.width) != null ? _b : 800) - marginLeft - marginRight;
    const tableColumns = (_d = (_c = snapshot == null ? void 0 : snapshot.tableSource) == null ? void 0 : _c[tableId]) == null ? void 0 : _d.tableColumns;
    if (!tableColumns) {
      return false;
    }
    const { newColWidth, widths } = getColumnWidths(pageWidth, tableColumns, columnIndex);
    for (let i = 0; i < widths.length; i++) {
      const action = jsonX.replaceOp(["tableSource", tableId, "tableColumns", i, "size", "width", "v"], tableColumns[i].size.width.v, widths[i]);
      rawActions.push(action);
    }
    const insertCol = getTableColumn(newColWidth);
    const insertTableColumn = jsonX.insertOp(["tableSource", tableId, "tableColumns", columnIndex], insertCol);
    rawActions.push(insertTableColumn);
    doMutation.params.actions = rawActions.reduce((acc, cur) => {
      return JSONX.compose(acc, cur);
    }, null);
    const result = commandService.syncExecuteCommand(doMutation.id, doMutation.params);
    return Boolean(result);
  }
};

// ../packages/docs-ui/src/commands/commands/table/doc-table-tab.command.ts
var DocTableTabCommand = {
  id: "doc.table.tab-in-table",
  type: 0 /* COMMAND */,
  handler: async (accessor, params) => {
    var _a;
    const { shift } = params;
    const textSelectionManager = accessor.get(DocSelectionManagerService);
    const docRanges = textSelectionManager.getDocRanges();
    const commandService = accessor.get(ICommandService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    if (!docDataModel) {
      return false;
    }
    const activeRange = (_a = docRanges.find((range) => range.isActive)) != null ? _a : docRanges[0];
    const unitId = docDataModel.getUnitId();
    const docSkeletonManagerService = getCommandSkeleton(accessor, unitId);
    const skeleton = docSkeletonManagerService == null ? void 0 : docSkeletonManagerService.getSkeleton();
    const viewModel = skeleton == null ? void 0 : skeleton.getViewModel().getSelfOrHeaderFooterViewModel(activeRange == null ? void 0 : activeRange.segmentId);
    if (viewModel == null) {
      return false;
    }
    if (activeRange == null) {
      return false;
    }
    let offsets = null;
    if (shift) {
      offsets = getCellOffsets(viewModel, activeRange, 1 /* PREV */);
    } else {
      offsets = getCellOffsets(viewModel, activeRange, 0 /* NEXT */);
    }
    if (offsets) {
      const { startOffset, endOffset } = offsets;
      const textRanges = [{
        startOffset,
        endOffset
      }];
      textSelectionManager.replaceDocRanges(textRanges);
      return true;
    }
    if (shift === false) {
      const result = await commandService.executeCommand(DocTableInsertRowCommand.id, {
        position: 1 /* BELLOW */
      });
      return result;
    }
    return true;
  }
};

// ../packages/docs-ui/src/commands/operations/doc-cursor.operation.ts
var MoveCursorOperation = {
  id: "doc.operation.move-cursor",
  type: 1 /* OPERATION */,
  handler: (accessor, params) => {
    if (!params) {
      return false;
    }
    return true;
  }
};
var MoveSelectionOperation = {
  id: "doc.operation.move-selection",
  type: 1 /* OPERATION */,
  handler: (accessor, params) => {
    if (!params) {
      return false;
    }
    return true;
  }
};

// ../packages/docs-ui/src/views/paragraph-setting/index.tsx
var import_react5 = __toESM(require_react());

// ../packages/docs-ui/src/views/paragraph-setting/Setting.tsx
var import_react4 = __toESM(require_react());

// ../packages/docs-ui/src/views/paragraph-setting/hook/utils.ts
var import_react3 = __toESM(require_react());
var useDocRanges = () => {
  const docSelectionManagerService = useDependency(DocSelectionManagerService);
  const docParagraphSettingController = useDependency(DocParagraphSettingController);
  const docRanges = (0, import_react3.useMemo)(() => docSelectionManagerService.getDocRanges(), []);
  (0, import_react3.useEffect)(() => {
    if (!docRanges.length) {
      docParagraphSettingController.closePanel();
    }
  }, [docRanges]);
  return docRanges;
};
var useCurrentParagraph = () => {
  var _a, _b, _c, _d, _e;
  const univerInstanceService = useDependency(IUniverInstanceService);
  const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
  const docRanges = useDocRanges();
  if (!docDataModel || docRanges.length === 0) {
    return [];
  }
  const segmentId = docRanges[0].segmentId;
  const segment = docDataModel.getSelfOrHeaderFooterModel(segmentId);
  const paragraphs = (_b = (_a = segment.getBody()) == null ? void 0 : _a.paragraphs) != null ? _b : [];
  const dataStream = (_d = (_c = segment.getBody()) == null ? void 0 : _c.dataStream) != null ? _d : "";
  const currentParagraphs = (_e = BuildTextUtils.range.getParagraphsInRanges(docRanges, paragraphs, dataStream)) != null ? _e : [];
  return currentParagraphs;
};
var useFirstParagraphHorizontalAlign = (paragraph, defaultValue) => {
  const commandService = useDependency(ICommandService);
  const [horizontalAlign, setHorizontalAlignSetInternal] = (0, import_react3.useState)(() => {
    var _a, _b;
    const firstParagraph = paragraph[0];
    if (!firstParagraph) {
      return defaultValue;
    }
    return String((_b = (_a = firstParagraph.paragraphStyle) == null ? void 0 : _a.horizontalAlign) != null ? _b : defaultValue);
  });
  const sethorizontalAlign = (v) => {
    setHorizontalAlignSetInternal(v);
    return commandService.executeCommand(DocParagraphSettingCommand.id, {
      paragraph: { horizontalAlign: Number(v) }
    });
  };
  return [horizontalAlign, sethorizontalAlign];
};
var useFirstParagraphIndentStart = (paragraph) => {
  const commandService = useDependency(ICommandService);
  const [indentStart, setIndentStartInternal] = (0, import_react3.useState)(() => {
    var _a;
    const firstParagraph = paragraph[0];
    if (!firstParagraph) {
      return 0;
    }
    return getNumberUnitValue((_a = firstParagraph.paragraphStyle) == null ? void 0 : _a.indentStart, 0);
  });
  const setIndentStart = (v) => {
    setIndentStartInternal(v);
    return commandService.executeCommand(DocParagraphSettingCommand.id, {
      paragraph: { indentStart: { v } }
    });
  };
  return [indentStart, setIndentStart];
};
var useFirstParagraphIndentEnd = (paragraph) => {
  const commandService = useDependency(ICommandService);
  const [indentEnd, setIndentEndInternal] = (0, import_react3.useState)(() => {
    var _a;
    const firstParagraph = paragraph[0];
    if (!firstParagraph) {
      return 0;
    }
    return getNumberUnitValue((_a = firstParagraph.paragraphStyle) == null ? void 0 : _a.indentEnd, 0);
  });
  const setIndentEnd = (v) => {
    setIndentEndInternal(v);
    return commandService.executeCommand(DocParagraphSettingCommand.id, {
      paragraph: { indentEnd: { v } }
    });
  };
  return [indentEnd, setIndentEnd];
};
var useFirstParagraphIndentFirstLine = (paragraph) => {
  const commandService = useDependency(ICommandService);
  const [indentFirstLine, setIndentFirstLineInternal] = (0, import_react3.useState)(() => {
    var _a;
    const firstParagraph = paragraph[0];
    if (!firstParagraph) {
      return 0;
    }
    return getNumberUnitValue((_a = firstParagraph.paragraphStyle) == null ? void 0 : _a.indentFirstLine, 0);
  });
  const setIndentFirstLine = (v) => {
    setIndentFirstLineInternal(v);
    return commandService.executeCommand(DocParagraphSettingCommand.id, {
      paragraph: { indentFirstLine: { v } }
    });
  };
  return [indentFirstLine, setIndentFirstLine];
};
var useFirstParagraphIndentHanging = (paragraph) => {
  const commandService = useDependency(ICommandService);
  const [hanging, setHangingInternal] = (0, import_react3.useState)(() => {
    var _a;
    const firstParagraph = paragraph[0];
    if (!firstParagraph) {
      return 0;
    }
    return getNumberUnitValue((_a = firstParagraph.paragraphStyle) == null ? void 0 : _a.hanging, 0);
  });
  const setHanging = (v) => {
    setHangingInternal(v);
    return commandService.executeCommand(DocParagraphSettingCommand.id, {
      paragraph: { hanging: { v } }
    });
  };
  return [hanging, setHanging];
};
var useFirstParagraphIndentSpaceAbove = (paragraph) => {
  const commandService = useDependency(ICommandService);
  const [spaceAbove, setSpaceAboveInternal] = (0, import_react3.useState)(() => {
    var _a;
    const firstParagraph = paragraph[0];
    if (!firstParagraph) {
      return 0;
    }
    return getNumberUnitValue((_a = firstParagraph.paragraphStyle) == null ? void 0 : _a.spaceAbove, DEFAULT_DOCUMENT_PARAGRAPH_SPACE_ABOVE);
  });
  const setSpaceAbove = (v) => {
    setSpaceAboveInternal(v);
    return commandService.executeCommand(DocParagraphSettingCommand.id, {
      paragraph: { spaceAbove: { v } }
    });
  };
  return [spaceAbove, setSpaceAbove];
};
var useFirstParagraphSpaceBelow = (paragraph) => {
  const commandService = useDependency(ICommandService);
  const [spaceBelow, setSpaceBelowInternal] = (0, import_react3.useState)(() => {
    var _a;
    const firstParagraph = paragraph[0];
    if (!firstParagraph) {
      return 0;
    }
    return getNumberUnitValue((_a = firstParagraph.paragraphStyle) == null ? void 0 : _a.spaceBelow, DEFAULT_DOCUMENT_PARAGRAPH_SPACE_BELOW);
  });
  const setSpaceBelow = (v) => {
    setSpaceBelowInternal(v);
    return commandService.executeCommand(DocParagraphSettingCommand.id, {
      paragraph: { spaceBelow: { v } }
    });
  };
  return [spaceBelow, setSpaceBelow];
};
var useFirstParagraphLineSpacing = (paragraph) => {
  const commandService = useDependency(ICommandService);
  const renderManagerService = useDependency(IRenderManagerService);
  const univerInstanceService = useDependency(IUniverInstanceService);
  const skeleton = (0, import_react3.useMemo)(() => {
    var _a;
    const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    if (!docDataModel) {
      return void 0;
    }
    return (_a = renderManagerService.getRenderById(docDataModel == null ? void 0 : docDataModel.getUnitId())) == null ? void 0 : _a.with(DocSkeletonManagerService).getSkeleton();
  }, []);
  const stateChange$ = (0, import_react3.useMemo)(() => new BehaviorSubject({}), []);
  const [lineSpacing, setLineSpacingInternal] = (0, import_react3.useState)(() => {
    var _a, _b;
    const firstParagraph = paragraph[0];
    if (!firstParagraph) {
      return 1.5;
    }
    return (_b = (_a = firstParagraph.paragraphStyle) == null ? void 0 : _a.lineSpacing) != null ? _b : DEFAULT_DOCUMENT_PARAGRAPH_LINE_SPACING;
  });
  const lineSpacingCache = (0, import_react3.useRef)(lineSpacing);
  const [spacingRule, setSpacingRuleInternal] = (0, import_react3.useState)(() => {
    var _a, _b;
    const firstParagraph = paragraph[0];
    if (!firstParagraph) {
      return 0 /* AUTO */;
    }
    return (_b = (_a = firstParagraph.paragraphStyle) == null ? void 0 : _a.spacingRule) != null ? _b : 0 /* AUTO */;
  });
  const setLineSpacing = async (v) => {
    setLineSpacingInternal(v);
    stateChange$.next({ lineSpacing: v, spacingRule });
  };
  const setSpacingRule = async (v) => {
    if (v !== spacingRule) {
      let cache = lineSpacingCache.current;
      if (v === 1 /* AT_LEAST */) {
        const glyphNode = skeleton == null ? void 0 : skeleton.findNodeByCharIndex(paragraph[0].startIndex);
        const divideNode = glyphNode == null ? void 0 : glyphNode.parent;
        const lineNode = divideNode == null ? void 0 : divideNode.parent;
        if ((lineNode == null ? void 0 : lineNode.contentHeight) !== void 0) {
          cache = Math.max(lineNode.contentHeight, cache);
        }
      } else {
        if (cache > 5) {
          cache = 2;
        }
      }
      lineSpacingCache.current = lineSpacing;
      setLineSpacing(cache);
      setSpacingRuleInternal(v);
      stateChange$.next({ spacingRule: v });
    }
  };
  (0, import_react3.useEffect)(() => {
    const dispose = stateChange$.pipe(
      filter((obj) => !!Object.keys(obj).length),
      bufferTime(16),
      filter((list) => !!list.length),
      map((list) => {
        return list.reduce((a, b) => {
          Object.keys(b).forEach((key) => {
            a[key] = b[key];
          });
          return a;
        }, {});
      })
    ).subscribe((v) => {
      return commandService.executeCommand(DocParagraphSettingCommand.id, {
        paragraph: { ...v }
      });
    });
    return () => dispose.unsubscribe();
  }, []);
  return {
    lineSpacing: [lineSpacing, setLineSpacing],
    spacingRule: [spacingRule, setSpacingRule]
  };
};

// ../packages/docs-ui/src/views/paragraph-setting/Setting.tsx
var import_jsx_runtime3 = __toESM(require_jsx_runtime());
var PARAGRAPH_SETTING_CONTROL_CLASS = "univer-w-full";
var ParagraphSettingSection = (props) => {
  const { title, children, first: first2 = false } = props;
  return /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("section", { className: clsx(!first2 && "univer-mt-5"), children: [
    /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("div", { className: "univer-text-sm univer-font-medium univer-leading-5", children: title }),
    /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("div", { className: "univer-mt-3", children })
  ] });
};
var ParagraphSettingRow = (props) => {
  const { label, children } = props;
  return /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)(
    "div",
    {
      className: "univer-grid univer-min-h-8 univer-items-center univer-gap-3",
      style: { gridTemplateColumns: "minmax(0, 1fr) minmax(160px, 180px)" },
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
          "div",
          {
            className: "\n                  univer-min-w-0 univer-text-xs univer-leading-5 univer-text-gray-900\n                  dark:!univer-text-gray-100\n                ",
            children: label
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)("div", { className: "univer-w-full univer-min-w-0", children })
      ]
    }
  );
};
var AutoFocusInputNumber = (props) => {
  const { value, onChange, className = "", min = 0, max = 100, step = 0.1, precision = 1 } = props;
  const ref = (0, import_react4.useRef)(null);
  return /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
    InputNumber,
    {
      step,
      precision,
      ref,
      min,
      max,
      value,
      onChange: (v) => {
        onChange(v != null ? v : 0).finally(() => {
          setTimeout(() => {
            var _a;
            (_a = ref.current) == null ? void 0 : _a.focus();
          }, 30);
        });
      },
      className: clsx(PARAGRAPH_SETTING_CONTROL_CLASS, className)
    }
  );
};
function ParagraphSetting() {
  const localeService = useDependency(LocaleService);
  const alignmentOptions = (0, import_react4.useMemo)(() => [
    { label: localeService.t("docs-ui.toolbar.alignLeft"), value: String(1 /* LEFT */), icon: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(LeftJustifyingIcon, {}) },
    { label: localeService.t("docs-ui.toolbar.alignCenter"), value: String(2 /* CENTER */), icon: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(HorizontallyIcon, {}) },
    { label: localeService.t("docs-ui.toolbar.alignRight"), value: String(3 /* RIGHT */), icon: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(RightJustifyingIcon, {}) },
    { label: localeService.t("docs-ui.toolbar.alignJustify"), value: String(4 /* JUSTIFIED */), icon: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(AlignTextBothIcon, {}) }
  ], []);
  const currentParagraph = useCurrentParagraph();
  const [horizontalAlignValue, setHorizontalAlign] = useFirstParagraphHorizontalAlign(currentParagraph, alignmentOptions[0].value);
  const [indentStart, setIndentStart] = useFirstParagraphIndentStart(currentParagraph);
  const [indentEnd, setIndentEnd] = useFirstParagraphIndentEnd(currentParagraph);
  const [indentFirstLine, setIndentFirstLine] = useFirstParagraphIndentFirstLine(currentParagraph);
  const [hanging, setHanging] = useFirstParagraphIndentHanging(currentParagraph);
  const [spaceAbove, setSpaceAbove] = useFirstParagraphIndentSpaceAbove(currentParagraph);
  const [spaceBelow, setSpaceBelow] = useFirstParagraphSpaceBelow(currentParagraph);
  const { lineSpacing: [lineSpacing, setLineSpacing], spacingRule: [spacingRule, setSpacingRule] } = useFirstParagraphLineSpacing(currentParagraph);
  const lineSpaceConfig = (0, import_react4.useMemo)(() => {
    if (spacingRule === 0 /* AUTO */) {
      return { min: 1, max: 5, step: 0.1 };
    }
    return { min: 1, max: 100 };
  }, [spacingRule]);
  return /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("div", { className: "univer-box-border univer-w-full", children: [
    /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(ParagraphSettingSection, { title: localeService.t("docs-ui.doc.paragraphSetting.alignment"), first: true, children: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
      "div",
      {
        className: clsx(`univer-grid univer-h-10 univer-grid-cols-4 univer-items-stretch univer-gap-1 univer-rounded-lg univer-bg-white univer-p-1 dark:!univer-bg-gray-900`, borderClassName),
        children: alignmentOptions.map((item) => {
          return /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(Tooltip, { title: item.label, placement: "bottom", children: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
            "button",
            {
              type: "button",
              className: clsx(`univer-flex univer-h-full univer-w-full univer-cursor-pointer univer-items-center univer-justify-center univer-rounded-md univer-border-none univer-bg-transparent univer-p-0 univer-text-gray-900 univer-transition-colors hover:univer-bg-gray-100 dark:!univer-text-gray-100 dark:hover:!univer-bg-gray-700`, {
                "univer-bg-gray-200 dark:!univer-bg-gray-700": horizontalAlignValue === item.value
              }),
              onClick: () => setHorizontalAlign(item.value),
              children: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
                "span",
                {
                  className: "\n                                          univer-flex univer-size-5 univer-items-center univer-justify-center\n                                          univer-text-lg\n                                        ",
                  children: item.icon
                }
              )
            }
          ) }, item.value);
        })
      }
    ) }),
    /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(ParagraphSettingSection, { title: localeService.t("docs-ui.doc.paragraphSetting.indentation"), children: /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("div", { className: "univer-grid univer-gap-3", children: [
      /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(ParagraphSettingRow, { label: `${localeService.t("docs-ui.doc.paragraphSetting.left")}(px)`, children: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(AutoFocusInputNumber, { value: indentStart, onChange: (v) => setIndentStart(v != null ? v : 0) }) }),
      /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(ParagraphSettingRow, { label: `${localeService.t("docs-ui.doc.paragraphSetting.right")}(px)`, children: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(AutoFocusInputNumber, { value: indentEnd, onChange: (v) => setIndentEnd(v != null ? v : 0) }) }),
      /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(ParagraphSettingRow, { label: `${localeService.t("docs-ui.doc.paragraphSetting.firstLine")}(px)`, children: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(AutoFocusInputNumber, { value: indentFirstLine, onChange: (v) => setIndentFirstLine(v != null ? v : 0) }) }),
      /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(ParagraphSettingRow, { label: `${localeService.t("docs-ui.doc.paragraphSetting.hanging")}(px)`, children: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(AutoFocusInputNumber, { value: hanging, onChange: (v) => setHanging(v != null ? v : 0) }) })
    ] }) }),
    /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(ParagraphSettingSection, { title: localeService.t("docs-ui.doc.paragraphSetting.spacing"), children: /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("div", { className: "univer-grid univer-gap-3", children: [
      /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(ParagraphSettingRow, { label: `${localeService.t("docs-ui.doc.paragraphSetting.before")}(px)`, children: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(AutoFocusInputNumber, { value: spaceAbove, onChange: (v) => setSpaceAbove(v != null ? v : 0) }) }),
      /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(ParagraphSettingRow, { label: `${localeService.t("docs-ui.doc.paragraphSetting.after")}(px)`, children: /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(AutoFocusInputNumber, { value: spaceBelow, onChange: (v) => setSpaceBelow(v != null ? v : 0) }) }),
      /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(ParagraphSettingRow, { label: localeService.t("docs-ui.doc.paragraphSetting.lineSpace"), children: /* @__PURE__ */ (0, import_jsx_runtime3.jsxs)("div", { className: "univer-flex univer-w-full univer-flex-col univer-gap-2", children: [
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
          Select,
          {
            className: PARAGRAPH_SETTING_CONTROL_CLASS,
            value: `${spacingRule}`,
            options: [
              { label: localeService.t("docs-ui.doc.paragraphSetting.multiSpace"), value: `${0 /* AUTO */}` },
              { label: localeService.t("docs-ui.doc.paragraphSetting.fixedValue"), value: `${1 /* AT_LEAST */}` }
            ],
            onChange: (v) => setSpacingRule(Number(v))
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(
          AutoFocusInputNumber,
          {
            ...lineSpaceConfig,
            value: lineSpacing,
            onChange: (v) => setLineSpacing(v != null ? v : 0)
          }
        )
      ] }) })
    ] }) })
  ] });
}

// ../packages/docs-ui/src/views/paragraph-setting/index.tsx
var import_jsx_runtime4 = __toESM(require_jsx_runtime());
var isRangesEqual2 = (oldRanges, ranges) => {
  return ranges.length === oldRanges.length && oldRanges.some((oldRange) => ranges.some((range) => range.startOffset === oldRange.startOffset && range.endOffset === oldRange.endOffset));
};
function ParagraphSettingIndex() {
  const commandService = useDependency(ICommandService);
  const localeService = useDependency(LocaleService);
  const currentLocale = useObservable(localeService.currentLocale$);
  const [key, setKey] = (0, import_react5.useState)("");
  const debounceReset = (0, import_react5.useMemo)(() => {
    return debounce_default(() => setKey(generateRandomId(4)), 300);
  }, []);
  const rangeRef = (0, import_react5.useRef)([]);
  (0, import_react5.useEffect)(() => {
    const dispose = commandService.onCommandExecuted((info) => {
      if (SetTextSelectionsOperation.id === info.id) {
        const ranges = info.params.ranges;
        if (!isRangesEqual2(ranges, rangeRef.current)) {
          setKey(generateRandomId(4));
        }
        rangeRef.current = ranges;
      }
      if (RichTextEditingMutation.id === info.id) {
        const params = info.params;
        const trigger = params.trigger;
        if (trigger !== DocParagraphSettingCommand.id) {
          debounceReset();
        }
      }
    });
    return () => dispose.dispose();
  }, [debounceReset]);
  (0, import_react5.useEffect)(() => {
    setKey(generateRandomId(4));
  }, [currentLocale]);
  (0, import_react5.useEffect)(() => () => debounceReset.cancel(), [debounceReset]);
  return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(ParagraphSetting, {}, key);
}

// ../packages/docs-ui/src/controllers/doc-paragraph-setting.controller.ts
var paragraphSettingIndexKey = "doc_ui_paragraph-setting-panel";
var DocParagraphSettingController = class extends Disposable {
  constructor(_commandService, _componentManager, _sidebarService) {
    super();
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_componentManager", _componentManager);
    __publicField(this, "_sidebarService", _sidebarService);
    __publicField(this, "_id");
    this._init();
  }
  _init() {
    this.disposeWithMe(this._componentManager.register(paragraphSettingIndexKey, ParagraphSettingIndex));
  }
  openPanel() {
    const props = {
      header: { title: "docs-ui.doc.slider.paragraphSetting" },
      id: this._id,
      children: {
        label: paragraphSettingIndexKey
      },
      width: 300
    };
    this._sidebarService.open(props);
  }
  closePanel() {
    this._sidebarService.close(this._id);
  }
};
DocParagraphSettingController = __decorateClass([
  __decorateParam(0, ICommandService),
  __decorateParam(1, Inject(ComponentManager)),
  __decorateParam(2, Inject(ISidebarService))
], DocParagraphSettingController);

// ../packages/docs-ui/src/commands/operations/doc-paragraph-setting-panel.operation.ts
var DocParagraphSettingPanelOperation = {
  id: "sidebar.operation.doc-paragraph-setting-panel",
  type: 1 /* OPERATION */,
  handler: (accessor) => {
    const docParagraphSettingController = accessor.get(DocParagraphSettingController);
    docParagraphSettingController.openPanel();
    return true;
  }
};

// ../packages/docs-ui/src/views/page-settings/index.tsx
var import_react6 = __toESM(require_react());
var import_jsx_runtime5 = __toESM(require_jsx_runtime());
var MODERN_WIDTH_OPTIONS = [
  "narrow" /* NARROW */,
  "medium" /* MEDIUM */,
  "wide" /* WIDE */
];
var getPaperSize = (size) => {
  const keys = Object.keys(PAGE_SIZE);
  const result = keys.find((key) => {
    const { width, height } = PAGE_SIZE[key];
    if (size.width === width && size.height === height) {
      return true;
    }
    return false;
  });
  return result != null ? result : "A4";
};
var getModernWidthMode = (size) => {
  var _a;
  const width = (_a = size == null ? void 0 : size.width) != null ? _a : MODERN_DOCUMENT_WIDTH["medium" /* MEDIUM */];
  return MODERN_WIDTH_OPTIONS.reduce((nearest, option) => {
    const nearestDistance = Math.abs(MODERN_DOCUMENT_WIDTH[nearest] - width);
    const currentDistance = Math.abs(MODERN_DOCUMENT_WIDTH[option] - width);
    return currentDistance < nearestDistance ? option : nearest;
  }, "medium" /* MEDIUM */);
};
function SettingsLabel(props) {
  const { children, muted } = props;
  return /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
    "label",
    {
      className: clsx("univer-text-sm univer-font-medium", muted ? "univer-text-gray-500" : `univer-text-gray-900 dark:!univer-text-white`),
      children
    }
  );
}
function PageSettings(props) {
  const { hooks } = props;
  const univerInstanceService = useDependency(IUniverInstanceService);
  const currentDoc = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
  const documentStyle = currentDoc.getDocumentStyle();
  const localeService = useDependency(LocaleService);
  const [settings, setSettings] = (0, import_react6.useState)(() => {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j;
    return {
      mode: documentStyle.documentFlavor === 1 /* TRADITIONAL */ ? 1 /* TRADITIONAL */ : 2 /* MODERN */,
      modernWidth: getModernWidthMode(documentStyle.pageSize),
      paperSize: getPaperSize((_a = documentStyle.pageSize) != null ? _a : PAGE_SIZE.A4),
      pageSize: {
        width: (_c = (_b = documentStyle.pageSize) == null ? void 0 : _b.width) != null ? _c : 0,
        height: (_e = (_d = documentStyle.pageSize) == null ? void 0 : _d.height) != null ? _e : 0
      },
      orientation: (_f = documentStyle.pageOrient) != null ? _f : 0 /* PORTRAIT */,
      margins: {
        top: (_g = documentStyle.marginTop) != null ? _g : 0,
        bottom: (_h = documentStyle.marginBottom) != null ? _h : 0,
        left: (_i = documentStyle.marginLeft) != null ? _i : 0,
        right: (_j = documentStyle.marginRight) != null ? _j : 0
      }
    };
  });
  (0, import_react6.useEffect)(() => {
    hooks.beforeClose = () => {
      return settings;
    };
    hooks.beforeConfirm = () => {
      return settings;
    };
  }, [settings]);
  const handlePaperSizeChange = (value) => {
    setSettings((prev) => ({ ...prev, paperSize: value, pageSize: PAGE_SIZE[value] }));
  };
  const handleMarginChange = (position, value) => {
    setSettings((prev) => ({
      ...prev,
      margins: {
        ...prev.margins,
        [position]: value || 0
      }
    }));
  };
  const handleModeChange = (mode) => {
    setSettings((prev) => ({ ...prev, mode }));
  };
  const handleModernWidthChange = (modernWidth) => {
    setSettings((prev) => ({ ...prev, modernWidth }));
  };
  return /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "univer-flex univer-flex-col univer-gap-4", children: [
    /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
      "div",
      {
        className: "\n                  univer-grid univer-grid-cols-2 univer-rounded-lg univer-bg-gray-100 univer-p-1\n                  dark:!univer-bg-gray-800\n                ",
        children: [2 /* MODERN */, 1 /* TRADITIONAL */].map((mode) => /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
          "button",
          {
            type: "button",
            className: clsx(`univer-h-8 univer-cursor-pointer univer-rounded-md univer-border-none univer-bg-transparent univer-text-sm univer-font-medium univer-text-gray-600 univer-transition-colors hover:univer-bg-white hover:univer-text-gray-900 dark:!univer-text-gray-200 dark:hover:!univer-bg-gray-700`, {
              "univer-bg-white univer-text-gray-900 univer-shadow-sm dark:!univer-bg-gray-700 dark:!univer-text-white": settings.mode === mode
            }),
            onClick: () => handleModeChange(mode),
            children: localeService.t(mode === 2 /* MODERN */ ? "docs-ui.page-settings.modern-mode" : "docs-ui.page-settings.classic-mode")
          },
          mode
        ))
      }
    ),
    settings.mode === 2 /* MODERN */ ? /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "univer-flex univer-flex-col univer-gap-2.5", children: [
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(SettingsLabel, { children: localeService.t("docs-ui.page-settings.modern-width") }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("div", { className: "univer-grid univer-grid-cols-3 univer-gap-2", children: MODERN_WIDTH_OPTIONS.map((option) => /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)(
        "button",
        {
          type: "button",
          className: clsx(`univer-flex univer-h-16 univer-cursor-pointer univer-flex-col univer-items-center univer-justify-center univer-gap-1 univer-rounded-lg univer-border univer-border-solid univer-border-gray-200 univer-bg-white univer-text-sm univer-font-medium univer-text-gray-700 univer-transition-colors hover:univer-border-primary-500 hover:univer-text-primary-600 dark:!univer-border-gray-600 dark:!univer-bg-gray-900 dark:!univer-text-gray-100`, {
            "!univer-border-primary-500 !univer-bg-primary-50 !univer-text-primary-600 dark:!univer-bg-primary-900": settings.modernWidth === option
          }),
          onClick: () => handleModernWidthChange(option),
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime5.jsx)("span", { children: localeService.t(`docs-ui.page-settings.modern-width-${option}`) }),
            /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("span", { className: "univer-text-xs univer-font-normal univer-text-gray-400", children: [
              MODERN_DOCUMENT_WIDTH[option],
              "px"
            ] })
          ]
        },
        option
      )) })
    ] }) : /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)(import_jsx_runtime5.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "univer-flex univer-flex-col univer-gap-2", children: [
        /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(SettingsLabel, { children: localeService.t("docs-ui.page-settings.paper-size") }),
        /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
          Select,
          {
            value: settings.paperSize,
            onChange: handlePaperSizeChange,
            options: PAPER_TYPES.map((p) => ({
              label: localeService.t(`docs-ui.page-settings.page-size.${p.toLocaleLowerCase()}`),
              value: p
            }))
          }
        )
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "univer-flex univer-flex-col univer-gap-2", children: [
        /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(SettingsLabel, { children: localeService.t("docs-ui.page-settings.custom-paper-size") }),
        /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "univer-flex univer-flex-col univer-gap-2.5", children: [
          /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "univer-flex univer-gap-2.5", children: [
            /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "univer-flex univer-flex-1 univer-flex-col univer-gap-2", children: [
              /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(SettingsLabel, { muted: true, children: localeService.t("docs-ui.page-settings.top") }),
              /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
                InputNumber,
                {
                  precision: 2,
                  min: 0,
                  max: settings.pageSize.height / 2,
                  value: settings.margins.top,
                  onChange: (e) => handleMarginChange("top", e)
                }
              )
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "univer-flex univer-flex-1 univer-flex-col univer-gap-2", children: [
              /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(SettingsLabel, { muted: true, children: localeService.t("docs-ui.page-settings.bottom") }),
              /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
                InputNumber,
                {
                  precision: 2,
                  min: 0,
                  max: settings.pageSize.height / 2,
                  value: settings.margins.bottom,
                  onChange: (e) => handleMarginChange("bottom", e)
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "univer-flex univer-gap-2.5", children: [
            /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "univer-flex univer-flex-1 univer-flex-col univer-gap-2", children: [
              /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(SettingsLabel, { muted: true, children: localeService.t("docs-ui.page-settings.left") }),
              /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
                InputNumber,
                {
                  precision: 2,
                  min: 0,
                  max: settings.pageSize.width / 2,
                  value: settings.margins.left,
                  onChange: (e) => handleMarginChange("left", e)
                }
              )
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime5.jsxs)("div", { className: "univer-flex univer-flex-1 univer-flex-col univer-gap-2", children: [
              /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(SettingsLabel, { muted: true, children: localeService.t("docs-ui.page-settings.right") }),
              /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(
                InputNumber,
                {
                  precision: 2,
                  min: 0,
                  max: settings.pageSize.width / 2,
                  value: settings.margins.right,
                  onChange: (e) => handleMarginChange("right", e)
                }
              )
            ] })
          ] })
        ] })
      ] })
    ] })
  ] });
}
var PAGE_SETTING_COMPONENT_ID = "docs.component.page-setting";

// ../packages/docs-ui/src/commands/operations/open-page-setting.operation.ts
var DocOpenPageSettingCommand = {
  id: "docs.operation.open-page-setting",
  type: 1 /* OPERATION */,
  handler: (accessor) => {
    const confirmService = accessor.get(IConfirmService);
    const commandService = accessor.get(ICommandService);
    const localeService = accessor.get(LocaleService);
    const disposable = confirmService.open({
      id: PAGE_SETTING_COMPONENT_ID,
      title: {
        label: localeService.t("docs-ui.page-settings.document-setting")
      },
      children: {
        label: PAGE_SETTING_COMPONENT_ID
      },
      width: 528,
      onClose: () => {
        disposable.dispose();
      },
      onConfirm: (result) => {
        disposable.dispose();
        if (!result) return;
        const paperSize = result.mode === 2 /* MODERN */ ? {
          width: MODERN_DOCUMENT_WIDTH[result.modernWidth],
          height: PAGE_SIZE.A4.height
        } : PAGE_SIZE[result.paperSize];
        commandService.executeCommand(DocPageSetupCommand.id, {
          documentFlavor: result.mode,
          pageOrient: result.orientation,
          marginTop: result.margins.top,
          marginBottom: result.margins.bottom,
          marginLeft: result.margins.left,
          marginRight: result.margins.right,
          pageSize: paperSize
        });
      },
      confirmText: localeService.t("docs-ui.page-settings.confirm"),
      cancelText: localeService.t("docs-ui.page-settings.cancel")
    });
    return true;
  }
};

// ../packages/docs-ui/src/config/config.ts
var DOCS_UI_PLUGIN_CONFIG_KEY = "docs-ui.config";
var configSymbol3 = Symbol(DOCS_UI_PLUGIN_CONFIG_KEY);
var defaultPluginConfig3 = {
  toc: false,
  footer: true
};

// ../packages/docs-ui/src/controllers/doc-auto-format.controller.ts
var DocAutoFormatController = class extends Disposable {
  constructor(_docAutoFormatService, _renderManagerService) {
    super();
    __publicField(this, "_docAutoFormatService", _docAutoFormatService);
    __publicField(this, "_renderManagerService", _renderManagerService);
    this._initListTabAutoFormat();
    this._initSpaceAutoFormat();
    this._initDefaultEnterFormat();
    this._initExitListAutoFormat();
  }
  _initListTabAutoFormat() {
    this.disposeWithMe(
      this._docAutoFormatService.registerAutoFormat({
        id: TabCommand.id,
        match: (context) => {
          var _a;
          const { selection, paragraphs, unit } = context;
          if (paragraphs.length === 1 && selection.startOffset === paragraphs[0].paragraphStart && paragraphs[0].bullet) {
            const allParagraphs = (_a = unit.getBody()) == null ? void 0 : _a.paragraphs;
            const bulletParagraphs = allParagraphs == null ? void 0 : allParagraphs.filter((p) => {
              var _a2;
              return ((_a2 = p.bullet) == null ? void 0 : _a2.listId) === paragraphs[0].bullet.listId;
            });
            if ((bulletParagraphs == null ? void 0 : bulletParagraphs.findIndex((p) => p.startIndex === paragraphs[0].startIndex)) === 0) {
              return false;
            }
            return true;
          } else if (paragraphs.length > 1 && paragraphs.some((p) => p.bullet)) {
            return true;
          }
          return false;
        },
        // traverse all paragraphs, set paragraph
        getMutations(context) {
          const params = context.commandParams;
          return [{
            id: ChangeListNestingLevelCommand.id,
            params: {
              type: (params == null ? void 0 : params.shift) ? -1 /* decrease */ : 1 /* increase */
            }
          }];
        },
        priority: 100
      })
    );
    this.disposeWithMe(
      this._docAutoFormatService.registerAutoFormat({
        id: TabCommand.id,
        match: (context) => {
          const { selection, unit } = context;
          const { startNodePosition, endNodePosition } = selection;
          const renderObject = this._renderManagerService.getRenderById(unit.getUnitId());
          const skeleton = renderObject == null ? void 0 : renderObject.with(DocSkeletonManagerService).getSkeleton();
          if (skeleton == null) {
            return false;
          }
          if (startNodePosition && endNodePosition && isInSameTableCellData(skeleton, startNodePosition, endNodePosition)) {
            return true;
          }
          if (startNodePosition && !endNodePosition && startNodePosition.path.indexOf("cells") > -1) {
            return true;
          }
          return false;
        },
        getMutations(context) {
          const params = context.commandParams;
          return [{
            id: DocTableTabCommand.id,
            params: {
              shift: !!(params == null ? void 0 : params.shift)
            }
          }];
        },
        priority: 99
      })
    );
  }
  _initSpaceAutoFormat() {
    this.disposeWithMe(
      this._docAutoFormatService.registerAutoFormat({
        id: AfterSpaceCommand.id,
        match: (context) => {
          var _a;
          const { selection, paragraphs, unit } = context;
          if (!selection.collapsed) {
            return false;
          }
          if (paragraphs.length !== 1) {
            return false;
          }
          if (!selection.collapsed) {
            return false;
          }
          const text = (_a = unit.getBody()) == null ? void 0 : _a.dataStream.slice(paragraphs[0].paragraphStart, selection.startOffset - 1);
          if (text && (Object.keys(QuickListTypeMap).includes(text) || Object.keys(QUICK_HEADING_MAP).includes(text))) {
            return true;
          }
          return false;
        },
        getMutations(context) {
          var _a;
          const { paragraphs, unit, selection } = context;
          const text = (_a = unit.getBody()) == null ? void 0 : _a.dataStream.slice(paragraphs[0].paragraphStart, selection.startOffset - 1);
          if (text && Object.keys(QuickListTypeMap).includes(text)) {
            const type2 = QuickListTypeMap[text];
            return [{
              id: QuickListCommand.id,
              params: {
                listType: type2,
                paragraph: paragraphs[0]
              }
            }];
          }
          if (text && Object.keys(QUICK_HEADING_MAP).includes(text)) {
            const type2 = QUICK_HEADING_MAP[text];
            return [{
              id: QuickHeadingCommand.id,
              params: {
                value: type2
              }
            }];
          }
          return [];
        }
      })
    );
  }
  _initExitListAutoFormat() {
    this.disposeWithMe(
      this._docAutoFormatService.registerAutoFormat({
        id: EnterCommand.id,
        match: (context) => {
          const { paragraphs } = context;
          if (paragraphs.length === 1 && paragraphs[0].bullet && paragraphs[0].paragraphStart === paragraphs[0].paragraphEnd) {
            return true;
          }
          return false;
        },
        getMutations: (context) => {
          const bullet = context.paragraphs[0].bullet;
          if (!bullet) {
            return [];
          }
          if (bullet.nestingLevel > 0) {
            return [{
              id: ChangeListNestingLevelCommand.id,
              params: {
                type: -1 /* decrease */
              }
            }];
          }
          return [{
            id: ListOperationCommand.id,
            params: {
              listType: context.paragraphs[0].bullet.listType
            }
          }];
        }
      })
    );
  }
  _initDefaultEnterFormat() {
    this.disposeWithMe(
      this._docAutoFormatService.registerAutoFormat({
        id: EnterCommand.id,
        match: () => {
          return true;
        },
        getMutations() {
          return [{
            id: BreakLineCommand.id
          }];
        },
        priority: -9999
      })
    );
  }
};
DocAutoFormatController = __decorateClass([
  __decorateParam(0, Inject(DocAutoFormatService)),
  __decorateParam(1, IRenderManagerService)
], DocAutoFormatController);

// ../packages/docs-ui/src/basics/table.ts
function firstLineInTable(table) {
  const firstRow = table.rows[0];
  const firstCell = firstRow.cells[0];
  const firstLine = firstCell.sections[0].columns[0].lines[0];
  return firstLine;
}
function lastLineInTable(table) {
  const lastRow = table.rows[table.rows.length - 1];
  const lastCell = lastRow.cells[lastRow.cells.length - 1];
  const lastLine = getLastLine(lastCell);
  return lastLine;
}
function findTableAfterLine(line, page) {
  const { ed } = line;
  const { skeTables } = page;
  let table = null;
  for (const t of skeTables.values()) {
    if (t.st === ed + 1) {
      table = t;
      break;
    }
  }
  return table;
}
function findLineBeforeAndAfterTable(table) {
  const tablePage = table == null ? void 0 : table.parent;
  let lineBeforeTable = null;
  let lineAfterTable = null;
  if (table == null || tablePage == null) {
    return {
      lineBeforeTable,
      lineAfterTable
    };
  }
  const { st, ed } = table;
  const pages = tablePage.type === 3 /* CELL */ ? [tablePage] : tablePage.parent.pages;
  lineIterator(pages, (l) => {
    if (l.st === ed + 1) {
      lineAfterTable = l;
    } else if (l.ed === st - 1) {
      lineBeforeTable = l;
    }
  });
  return {
    lineBeforeTable,
    lineAfterTable
  };
}
function isEmptyCellPage2(cell) {
  return cell.sections[0].columns[0].lines.length === 0;
}
function findBellowCell(cell) {
  var _a, _b;
  const row = cell.parent;
  const table = row == null ? void 0 : row.parent;
  const tableId = table == null ? void 0 : table.tableId;
  if (row == null || table == null) {
    return;
  }
  const col = row.cells.indexOf(cell);
  let bellowRow = table.rows[table.rows.indexOf(row) + 1];
  if (bellowRow == null) {
    if (tableId.indexOf("#-#")) {
      const [id, index] = tableId.split("#-#");
      const pages = (_b = (_a = table.parent) == null ? void 0 : _a.parent) == null ? void 0 : _b.pages;
      const nextTableId = `${id}#-#${Number.parseInt(index) + 1}`;
      if (pages) {
        for (const page of pages) {
          const { skeTables } = page;
          if (skeTables.has(nextTableId)) {
            const nextTable = skeTables.get(nextTableId);
            if (nextTable == null ? void 0 : nextTable.rows.length) {
              bellowRow = nextTable.rows.find((r) => !r.isRepeatRow);
              break;
            }
          }
        }
      }
    }
  }
  if (bellowRow != null) {
    const cell2 = bellowRow.cells[col];
    return isEmptyCellPage2(cell2) ? findBellowCell(cell2) : cell2;
  }
}
function findAboveCell(cell) {
  var _a, _b;
  const row = cell.parent;
  const table = row == null ? void 0 : row.parent;
  if (row == null || table == null) {
    return;
  }
  let aboveRow = table.rows[table.rows.indexOf(row) - 1];
  const col = row.cells.indexOf(cell);
  if (aboveRow == null || aboveRow.isRepeatRow) {
    if (table.tableId.indexOf("#-#")) {
      const [id, index] = table.tableId.split("#-#");
      const pages = (_b = (_a = table.parent) == null ? void 0 : _a.parent) == null ? void 0 : _b.pages;
      const nextTableId = `${id}#-#${Number.parseInt(index) - 1}`;
      if (pages) {
        for (const page of pages) {
          const { skeTables } = page;
          if (skeTables.has(nextTableId)) {
            const nextTable = skeTables.get(nextTableId);
            if (nextTable == null ? void 0 : nextTable.rows.length) {
              aboveRow = nextTable.rows[nextTable.rows.length - 1];
              break;
            }
          }
        }
      }
    }
  }
  if (aboveRow != null) {
    const cell2 = aboveRow.cells[col];
    return isEmptyCellPage2(cell2) ? findAboveCell(cell2) : cell2;
  }
}
function findTableBeforeLine(line, page) {
  const { st } = line;
  const { skeTables } = page;
  let table = null;
  for (const t of skeTables.values()) {
    if (t.ed === st - 1) {
      table = t;
      break;
    }
  }
  return table;
}
function firstLineInCell(cell) {
  const firstLine = cell.sections[0].columns[0].lines[0];
  return firstLine;
}
function lastLineInCell(cell) {
  const lastColumn = getLastColumn(cell);
  const lastLine = lastColumn.lines[lastColumn.lines.length - 1];
  return lastLine;
}

// ../packages/docs-ui/src/controllers/render-controllers/back-scroll.render-controller.ts
var ANCHOR_WIDTH = 1.5;
var DocBackScrollRenderController = class extends RxDisposable {
  constructor(_context, _textSelectionManagerService, _editorService, _docSkeletonManagerService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_textSelectionManagerService", _textSelectionManagerService);
    __publicField(this, "_editorService", _editorService);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    this._init();
  }
  _init() {
    this._textSelectionManagerService.textSelection$.pipe(takeUntil(this.dispose$)).subscribe((params) => {
      if (params == null) {
        return;
      }
      const { isEditing, unitId } = params;
      if (unitId !== this._context.unitId || !isEditing) {
        return;
      }
      if (this._context.unitId === DOCS_NORMAL_EDITOR_UNIT_ID_KEY) {
        return;
      }
      this._scrollToSelection();
    });
  }
  scrollToRange(range) {
    const skeleton = this._docSkeletonManagerService.getSkeleton();
    if (!skeleton) {
      return;
    }
    const { startOffset } = range;
    const anchorNodePosition = skeleton.findNodePositionByCharIndex(startOffset);
    anchorNodePosition && this.scrollToNode(anchorNodePosition);
  }
  scrollToNode(startNodePosition) {
    var _a;
    const { unitId, scene, mainComponent } = this._context;
    const skeleton = this._docSkeletonManagerService.getSkeleton();
    if (mainComponent == null || skeleton == null) {
      return;
    }
    const documentOffsetConfig = mainComponent.getOffsetConfig();
    const { docsLeft, docsTop } = documentOffsetConfig;
    const convertor = new NodePositionConvertToCursor(documentOffsetConfig, skeleton);
    const { contentBoxPointGroup } = convertor.getRangePointData(startNodePosition, startNodePosition);
    const { left: aLeft, top: aTop, height } = getAnchorBounding(contentBoxPointGroup);
    const left = aLeft + docsLeft;
    const top = aTop + docsTop;
    const viewportMain = scene.getViewport("viewMain" /* VIEW_MAIN */);
    const editor = this._editorService.getEditor(unitId);
    if (viewportMain == null) {
      return;
    }
    const {
      left: boundLeft,
      top: boundTop,
      right: boundRight,
      bottom: boundBottom
    } = viewportMain.getBounding().viewBound;
    let offsetY = 0;
    let offsetX = 0;
    const delta = editor ? (_a = editor.params.backScrollOffset) != null ? _a : 0 : 100;
    if (top < boundTop) {
      offsetY = top - boundTop - delta;
    } else if (top > boundBottom - height) {
      offsetY = top - boundBottom + height + delta;
    }
    if (left < boundLeft) {
      offsetX = left - boundLeft;
    } else if (left > boundRight - ANCHOR_WIDTH) {
      offsetX = left - boundRight + ANCHOR_WIDTH;
    }
    const config = viewportMain.transViewportScroll2ScrollValue(offsetX, offsetY);
    viewportMain.scrollByBarDeltaValue(config);
  }
  // Let the selection show on the current screen.
  _scrollToSelection() {
    const activeTextRange = this._textSelectionManagerService.getActiveTextRange();
    if (activeTextRange == null) {
      return;
    }
    const { collapsed, startNodePosition } = activeTextRange;
    if (!collapsed) {
      return;
    }
    this.scrollToNode(startNodePosition);
  }
};
DocBackScrollRenderController = __decorateClass([
  __decorateParam(1, Inject(DocSelectionManagerService)),
  __decorateParam(2, IEditorService),
  __decorateParam(3, Inject(DocSkeletonManagerService))
], DocBackScrollRenderController);

// ../packages/docs-ui/src/controllers/doc-move-cursor.controller.ts
var DocMoveCursorController = class extends Disposable {
  constructor(_univerInstanceService, _renderManagerService, _textSelectionManagerService, _commandService) {
    super();
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_renderManagerService", _renderManagerService);
    __publicField(this, "_textSelectionManagerService", _textSelectionManagerService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_onInputSubscription");
    this._commandExecutedListener();
  }
  dispose() {
    var _a;
    super.dispose();
    (_a = this._onInputSubscription) == null ? void 0 : _a.unsubscribe();
  }
  _commandExecutedListener() {
    const updateCommandList = [MoveCursorOperation.id, MoveSelectionOperation.id];
    this.disposeWithMe(
      this._commandService.onCommandExecuted((command) => {
        if (!updateCommandList.includes(command.id)) {
          return;
        }
        const param = command.params;
        switch (command.id) {
          case MoveCursorOperation.id: {
            return this._handleMoveCursor(param.direction);
          }
          case MoveSelectionOperation.id: {
            return this._handleShiftMoveSelection(param.direction);
          }
          default: {
            throw new Error("Unknown command");
          }
        }
      })
    );
  }
  // eslint-disable-next-line max-lines-per-function, complexity
  _handleShiftMoveSelection(direction) {
    var _a, _b, _c;
    const activeRange = this._textSelectionManagerService.getActiveTextRange();
    const allRanges = this._textSelectionManagerService.getTextRanges();
    const docDataModel = this._univerInstanceService.getCurrentUniverDocInstance();
    if (docDataModel == null) {
      return;
    }
    const skeleton = (_a = this._renderManagerService.getRenderById(docDataModel.getUnitId())) == null ? void 0 : _a.with(DocSkeletonManagerService).getSkeleton();
    const docObject = this._getDocObject();
    if (activeRange == null || skeleton == null || docObject == null) {
      return;
    }
    const {
      startOffset,
      endOffset,
      style,
      collapsed,
      direction: rangeDirection,
      segmentId,
      startNodePosition,
      endNodePosition,
      segmentPage
    } = activeRange;
    if (allRanges.length > 1) {
      let min = Number.POSITIVE_INFINITY;
      let max = Number.NEGATIVE_INFINITY;
      for (const range of allRanges) {
        min = Math.min(min, range.startOffset);
        max = Math.max(max, range.endOffset);
      }
      this._textSelectionManagerService.replaceTextRanges([
        {
          startOffset: direction === 3 /* LEFT */ || direction === 0 /* UP */ ? max : min,
          endOffset: direction === 3 /* LEFT */ || direction === 0 /* UP */ ? min : max,
          style
        }
      ], false);
      return;
    }
    const anchorOffset = collapsed ? startOffset : rangeDirection === "forward" /* FORWARD */ ? startOffset : endOffset;
    let focusOffset = collapsed ? endOffset : rangeDirection === "forward" /* FORWARD */ ? endOffset : startOffset;
    const dataStreamLength = (_b = docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody().dataStream.length) != null ? _b : Number.POSITIVE_INFINITY;
    if (direction === 3 /* LEFT */ || direction === 1 /* RIGHT */) {
      const preGlyph = skeleton.findNodeByCharIndex(focusOffset - 1, segmentId, segmentPage);
      const curGlyph = skeleton.findNodeByCharIndex(focusOffset, segmentId, segmentPage);
      focusOffset = direction === 1 /* RIGHT */ ? focusOffset + curGlyph.count : focusOffset - ((_c = preGlyph == null ? void 0 : preGlyph.count) != null ? _c : 0);
      focusOffset = Math.min(dataStreamLength - 2, Math.max(0, focusOffset));
      this._textSelectionManagerService.replaceTextRanges([
        {
          startOffset: anchorOffset,
          endOffset: focusOffset,
          style
        }
      ], false);
      this._scrollToFocusNodePosition(docDataModel.getUnitId(), focusOffset);
    } else {
      const focusGlyph = skeleton.findNodeByCharIndex(focusOffset, segmentId, segmentPage);
      const documentOffsetConfig = docObject.document.getOffsetConfig();
      const focusNodePosition = collapsed ? startNodePosition : rangeDirection === "forward" /* FORWARD */ ? endNodePosition : startNodePosition;
      const newPos = this._getTopOrBottomPosition(skeleton, focusGlyph, focusNodePosition, direction === 2 /* DOWN */, true);
      if (newPos == null) {
        const newFocusOffset = direction === 0 /* UP */ ? 0 : dataStreamLength - 2;
        if (newFocusOffset === focusOffset) {
          return;
        }
        this._textSelectionManagerService.replaceTextRanges([
          {
            startOffset: anchorOffset,
            endOffset: newFocusOffset,
            style
          }
        ], false);
        return;
      }
      const newActiveRange = new NodePositionConvertToCursor(documentOffsetConfig, skeleton).getRangePointData(
        newPos,
        newPos
      ).cursorList[0];
      this._textSelectionManagerService.replaceTextRanges([
        {
          startOffset: anchorOffset,
          endOffset: newActiveRange.endOffset,
          style
        }
      ], false);
      this._scrollToFocusNodePosition(docDataModel.getUnitId(), newActiveRange.endOffset);
    }
  }
  // eslint-disable-next-line max-lines-per-function, complexity
  _handleMoveCursor(direction) {
    var _a, _b, _c, _d;
    const activeRange = this._textSelectionManagerService.getActiveTextRange();
    const allRanges = this._textSelectionManagerService.getTextRanges();
    const docDataModel = this._univerInstanceService.getCurrentUniverDocInstance();
    if (docDataModel == null) {
      return false;
    }
    const skeleton = (_a = this._renderManagerService.getRenderById(docDataModel.getUnitId())) == null ? void 0 : _a.with(DocSkeletonManagerService).getSkeleton();
    const docObject = this._getDocObject();
    if (activeRange == null || skeleton == null || docObject == null || allRanges == null) {
      return;
    }
    const { startOffset, endOffset, style, collapsed, segmentId, startNodePosition, endNodePosition, segmentPage } = activeRange;
    const body = docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
    if (body == null) {
      return;
    }
    const dataStreamLength = (_b = body.dataStream.length) != null ? _b : Number.POSITIVE_INFINITY;
    const customRanges = (_c = docDataModel.getCustomRanges()) != null ? _c : [];
    if (direction === 3 /* LEFT */ || direction === 1 /* RIGHT */) {
      let cursor;
      if (!activeRange.collapsed || allRanges.length > 1) {
        let min = Number.POSITIVE_INFINITY;
        let max = Number.NEGATIVE_INFINITY;
        for (const range of allRanges) {
          min = Math.min(min, range.startOffset);
          max = Math.max(max, range.endOffset);
        }
        cursor = direction === 3 /* LEFT */ ? min : max;
      } else {
        const preSpan = skeleton.findNodeByCharIndex(startOffset - 1, segmentId, segmentPage);
        const curSpan = skeleton.findNodeByCharIndex(startOffset, segmentId, segmentPage);
        const nextGlyph = skeleton.findNodeByCharIndex(startOffset + 1, segmentId, segmentPage);
        if (direction === 3 /* LEFT */) {
          cursor = Math.max(0, startOffset - ((_d = preSpan == null ? void 0 : preSpan.count) != null ? _d : 1));
        } else {
          cursor = Math.min(dataStreamLength - 2, endOffset + curSpan.count + ((nextGlyph == null ? void 0 : nextGlyph.streamType) === "\n" /* SECTION_BREAK */ ? 1 : 0));
        }
      }
      const skipTokens = [
        "" /* TABLE_START */,
        "" /* TABLE_END */,
        "\x1B" /* TABLE_ROW_START */,
        "" /* TABLE_ROW_END */,
        "" /* TABLE_CELL_START */,
        "" /* TABLE_CELL_END */,
        "\n" /* SECTION_BREAK */
      ];
      if (direction === 3 /* LEFT */) {
        while (skipTokens.includes(body.dataStream[cursor])) {
          cursor--;
        }
      } else {
        while (skipTokens.includes(body.dataStream[cursor])) {
          cursor++;
        }
      }
      const relativeRanges = customRanges.filter((range) => range.wholeEntity && range.startIndex < cursor && range.endIndex >= cursor);
      relativeRanges.forEach((range) => {
        if (direction === 3 /* LEFT */) {
          cursor = Math.min(range.startIndex, cursor);
        } else {
          cursor = Math.max(range.endIndex + 1, cursor);
        }
      });
      this._textSelectionManagerService.replaceTextRanges([
        {
          startOffset: Math.max(0, cursor),
          endOffset: Math.max(0, cursor),
          style
        }
      ], false);
      this._scrollToFocusNodePosition(docDataModel.getUnitId(), cursor);
    } else {
      const startNode = skeleton.findNodeByCharIndex(startOffset, segmentId, segmentPage);
      const endNode = skeleton.findNodeByCharIndex(endOffset, segmentId, segmentPage);
      const documentOffsetConfig = docObject.document.getOffsetConfig();
      const newPos = this._getTopOrBottomPosition(
        skeleton,
        direction === 0 /* UP */ ? startNode : collapsed ? startNode : endNode,
        direction === 0 /* UP */ ? startNodePosition : collapsed ? startNodePosition : endNodePosition,
        direction === 2 /* DOWN */
      );
      if (newPos == null) {
        let cursor;
        if (collapsed) {
          cursor = direction === 0 /* UP */ ? 0 : dataStreamLength - 2;
        } else {
          cursor = direction === 0 /* UP */ ? startOffset : endOffset;
        }
        this._textSelectionManagerService.replaceTextRanges([
          {
            startOffset: Math.max(0, cursor),
            endOffset: Math.max(0, cursor),
            style
          }
        ], false);
        return;
      }
      const newActiveRange = new NodePositionConvertToCursor(documentOffsetConfig, skeleton).getRangePointData(
        newPos,
        newPos
      ).cursorList[0];
      this._textSelectionManagerService.replaceTextRanges([
        {
          ...newActiveRange,
          style
        }
      ], false);
      this._scrollToFocusNodePosition(docDataModel.getUnitId(), newActiveRange.endOffset);
    }
  }
  _getTopOrBottomPosition(docSkeleton, glyph, nodePosition, direction, skipCellContent = false) {
    if (glyph == null || nodePosition == null) {
      return;
    }
    const offsetLeft = this._getGlyphLeftOffsetInLine(glyph);
    const line = this._getNextOrPrevLine(glyph, direction, skipCellContent);
    if (line == null) {
      return;
    }
    const position = this._matchPositionByLeftOffset(docSkeleton, line, offsetLeft, nodePosition);
    if (position == null) {
      return;
    }
    return { ...position, isBack: true };
  }
  _getGlyphLeftOffsetInLine(glyph) {
    const divide = glyph.parent;
    if (divide == null) {
      return Number.NEGATIVE_INFINITY;
    }
    const divideLeft = divide.left;
    const { left } = glyph;
    const start = divideLeft + left;
    return start;
  }
  _matchPositionByLeftOffset(docSkeleton, line, offsetLeft, nodePosition) {
    const nearestNode = {
      distance: Number.POSITIVE_INFINITY
    };
    for (const divide of line.divides) {
      const divideLeft = divide.left;
      for (const glyph of divide.glyphGroup) {
        if (glyph.streamType === "\n" /* SECTION_BREAK */) {
          continue;
        }
        const { left } = glyph;
        const leftSide = divideLeft + left;
        const distance = Math.abs(offsetLeft - leftSide);
        if (distance < nearestNode.distance) {
          nearestNode.glyph = glyph;
          nearestNode.distance = distance;
        }
      }
    }
    if (nearestNode.glyph == null) {
      return;
    }
    const { segmentPage } = nodePosition;
    return docSkeleton.findPositionByGlyph(nearestNode.glyph, segmentPage);
  }
  // eslint-disable-next-line max-lines-per-function, complexity
  _getNextOrPrevLine(glyph, direction, skipCellContent = false) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l;
    const divide = glyph.parent;
    const line = divide == null ? void 0 : divide.parent;
    const column = line == null ? void 0 : line.parent;
    const section = column == null ? void 0 : column.parent;
    const page = section == null ? void 0 : section.parent;
    if (divide == null || line == null || column == null || section == null || page == null) {
      return;
    }
    const currentLineIndex = column.lines.indexOf(line);
    if (currentLineIndex === -1) {
      return;
    }
    let newLine;
    if (page.type === 3 /* CELL */ && skipCellContent) {
      const nLine = findAboveOrBellowCellLine(page, direction);
      if (nLine) {
        return nLine;
      }
    }
    if (direction === true) {
      newLine = column.lines[currentLineIndex + 1];
      const tableAfterLine = findTableAfterLine(line, page);
      if (tableAfterLine) {
        const firstLine = firstLineInTable(tableAfterLine);
        if (firstLine) {
          newLine = firstLine;
        }
      }
    } else {
      newLine = column.lines[currentLineIndex - 1];
      const tableBeforeLine = findTableBeforeLine(line, page);
      if (tableBeforeLine) {
        const lastLine = lastLineInTable(tableBeforeLine);
        if (lastLine) {
          newLine = lastLine;
        }
      }
    }
    if (newLine != null) {
      return newLine;
    }
    const currentColumnIndex = section.columns.indexOf(column);
    if (currentColumnIndex === -1) {
      return;
    }
    if (direction === true) {
      newLine = (_a = section.columns[currentColumnIndex + 1]) == null ? void 0 : _a.lines[0];
    } else {
      const prevColumnLines = (_c = (_b = section.columns) == null ? void 0 : _b[currentColumnIndex - 1]) == null ? void 0 : _c.lines;
      newLine = prevColumnLines == null ? void 0 : prevColumnLines[prevColumnLines.length - 1];
    }
    if (newLine != null) {
      return newLine;
    }
    const currentSectionIndex = page.sections.indexOf(section);
    if (currentSectionIndex === -1) {
      return;
    }
    if (direction === true) {
      newLine = (_e = (_d = page.sections[currentSectionIndex - 1]) == null ? void 0 : _d.columns[0]) == null ? void 0 : _e.lines[0];
    } else {
      const prevColumns = (_g = (_f = page.sections) == null ? void 0 : _f[currentSectionIndex - 1]) == null ? void 0 : _g.columns;
      const column2 = prevColumns == null ? void 0 : prevColumns[prevColumns.length - 1];
      const prevColumnLines = column2 == null ? void 0 : column2.lines;
      newLine = prevColumnLines == null ? void 0 : prevColumnLines[prevColumnLines.length - 1];
    }
    if (newLine != null) {
      return newLine;
    }
    if (page.type === 3 /* CELL */) {
      return findAboveOrBellowCellLine(page, direction);
    }
    const skeleton = page.parent;
    if (skeleton == null) {
      return;
    }
    const currentPageIndex = skeleton.pages.indexOf(page);
    if (currentPageIndex === -1) {
      return;
    }
    if (direction === true) {
      newLine = (_j = (_i = (_h = skeleton.pages[currentPageIndex + 1]) == null ? void 0 : _h.sections[0]) == null ? void 0 : _i.columns[0]) == null ? void 0 : _j.lines[0];
    } else {
      const prevSections = (_k = skeleton.pages[currentPageIndex - 1]) == null ? void 0 : _k.sections;
      if (prevSections == null) {
        return;
      }
      const prevColumns = (_l = prevSections[prevSections.length - 1]) == null ? void 0 : _l.columns;
      const column2 = prevColumns[prevColumns.length - 1];
      const prevColumnLines = column2 == null ? void 0 : column2.lines;
      newLine = prevColumnLines[prevColumnLines.length - 1];
    }
    if (newLine != null) {
      return newLine;
    }
  }
  _scrollToFocusNodePosition(unitId, offset) {
    var _a;
    const backScrollController = (_a = this._renderManagerService.getRenderById(unitId)) == null ? void 0 : _a.with(DocBackScrollRenderController);
    if (backScrollController == null) {
      return;
    }
    backScrollController.scrollToRange({
      startOffset: offset,
      endOffset: offset,
      collapsed: true
    });
  }
  _getDocObject() {
    return getDocObject(this._univerInstanceService, this._renderManagerService);
  }
};
DocMoveCursorController = __decorateClass([
  __decorateParam(0, IUniverInstanceService),
  __decorateParam(1, IRenderManagerService),
  __decorateParam(2, Inject(DocSelectionManagerService)),
  __decorateParam(3, ICommandService)
], DocMoveCursorController);
function findAboveOrBellowCellLine(page, direction) {
  var _a, _b;
  let newLine = null;
  if (direction === true) {
    const bellowCell = findBellowCell(page);
    if (bellowCell) {
      newLine = firstLineInCell(bellowCell);
    } else {
      const table = (_a = page.parent) == null ? void 0 : _a.parent;
      const { lineAfterTable } = findLineBeforeAndAfterTable(table);
      if (lineAfterTable) {
        newLine = lineAfterTable;
      }
    }
  } else {
    const aboveCell = findAboveCell(page);
    if (aboveCell) {
      newLine = lastLineInCell(aboveCell);
    } else {
      const table = (_b = page.parent) == null ? void 0 : _b.parent;
      const { lineBeforeTable } = findLineBeforeAndAfterTable(table);
      if (lineBeforeTable) {
        newLine = lineBeforeTable;
      }
    }
  }
  return newLine;
}

// ../packages/docs-ui/src/views/table/create/component-name.ts
var COMPONENT_DOC_CREATE_TABLE_CONFIRM = "COMPONENT_DOC_CREATE_TABLE_CONFIRM";

// ../packages/docs-ui/src/commands/operations/doc-create-table.operation.ts
var COMPONENT_DOC_CREATE_TABLE_CONFIRM_ID = "doc.component.create-table-confirm";
var DocCreateTableOperation = {
  id: "doc.operation.create-table",
  type: 0 /* COMMAND */,
  handler: async (accessor) => {
    const localeService = accessor.get(LocaleService);
    const confirmService = accessor.get(IConfirmService);
    const commandService = accessor.get(ICommandService);
    const tableCreateParams = {
      rowCount: 3,
      colCount: 5
    };
    const handleRowColChange = (rowCount, colCount) => {
      tableCreateParams.rowCount = rowCount;
      tableCreateParams.colCount = colCount;
    };
    await confirmService.open({
      id: COMPONENT_DOC_CREATE_TABLE_CONFIRM_ID,
      children: {
        label: {
          name: COMPONENT_DOC_CREATE_TABLE_CONFIRM,
          props: {
            handleRowColChange,
            tableCreateParams
          }
        }
      },
      width: "auto",
      title: { title: localeService.t("docs-ui.toolbar.table.insert") },
      onConfirm: () => {
        commandService.executeCommand(CreateDocTableCommand.id, tableCreateParams);
        confirmService.close(COMPONENT_DOC_CREATE_TABLE_CONFIRM_ID);
      },
      onClose: () => {
        confirmService.close(COMPONENT_DOC_CREATE_TABLE_CONFIRM_ID);
      }
    });
    return true;
  }
};

// ../packages/docs-ui/src/views/table/create/TableCreate.tsx
var import_react7 = __toESM(require_react());
var import_jsx_runtime6 = __toESM(require_jsx_runtime());
var DocCreateTableConfirm = ({
  handleRowColChange,
  tableCreateParams
}) => {
  const localeService = useDependency(LocaleService);
  const [rowCount, setRowCount] = (0, import_react7.useState)(3);
  const [colCount, setColCount] = (0, import_react7.useState)(5);
  function handleInputChange(rowCount2, colCount2) {
    setRowCount(rowCount2);
    setColCount(colCount2);
    handleRowColChange(rowCount2, colCount2);
  }
  (0, import_react7.useEffect)(() => {
    setRowCount(tableCreateParams.rowCount);
    setColCount(tableCreateParams.colCount);
  }, [tableCreateParams]);
  return /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("div", { className: "univer-flex univer-items-center univer-justify-between univer-gap-2", children: [
    /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("div", { className: "univer-flex univer-items-center univer-gap-2", children: [
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("span", { children: localeService.t("docs-ui.toolbar.table.rowCount") }),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
        InputNumber,
        {
          className: "univer-w-28",
          min: 1,
          max: 20,
          precision: 0,
          value: rowCount,
          onChange: (val) => {
            handleInputChange(val, colCount);
          }
        }
      )
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime6.jsxs)("div", { className: "univer-flex univer-items-center univer-gap-2", children: [
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)("span", { children: localeService.t("docs-ui.toolbar.table.colCount") }),
      /* @__PURE__ */ (0, import_jsx_runtime6.jsx)(
        InputNumber,
        {
          className: "univer-w-28",
          min: 1,
          max: 20,
          precision: 0,
          value: colCount,
          onChange: (val) => {
            handleInputChange(rowCount, val);
          }
        }
      )
    ] })
  ] });
};

// ../packages/docs-ui/src/controllers/doc-table.controller.ts
var DocTableController = class extends Disposable {
  constructor(_commandService, _componentManager) {
    super();
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_componentManager", _componentManager);
    this._initialize();
  }
  _initialize() {
    this._init();
    this._registerCommands();
    this._initCustomComponents();
  }
  _registerCommands() {
    [
      DocCreateTableOperation
    ].forEach((command) => this.disposeWithMe(this._commandService.registerCommand(command)));
  }
  _initCustomComponents() {
    const componentManager = this._componentManager;
    this.disposeWithMe(componentManager.register(COMPONENT_DOC_CREATE_TABLE_CONFIRM, DocCreateTableConfirm));
  }
  _init() {
  }
};
DocTableController = __decorateClass([
  __decorateParam(0, ICommandService),
  __decorateParam(1, Inject(ComponentManager))
], DocTableController);

// ../packages/docs-ui/src/components/const.ts
var COMPONENT_PREFIX = "UI_PLUGIN_DOCS";

// ../packages/docs-ui/src/components/list-type-picker/Picker.tsx
var import_jsx_runtime7 = __toESM(require_jsx_runtime());
var ListTypePicker = (props) => {
  const { value, onChange, options } = props;
  return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)("div", { className: "univer-grid univer-grid-cols-3 univer-gap-2 univer-p-1.5", children: options.map((item) => {
    return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
      "a",
      {
        className: clsx(`univer-block univer-h-20 univer-w-[72px] univer-cursor-pointer univer-overflow-hidden univer-rounded univer-transition-all hover:univer-border-primary-500`, borderClassName, {
          "univer-border-primary-500": value === item.value
        }),
        onClick: () => {
          onChange(item.value);
        },
        children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
          "img",
          {
            className: "univer-size-full",
            src: item.img,
            draggable: false
          }
        )
      },
      item.value
    );
  }) });
};
var orderListOptions = [
  {
    value: "ORDER_LIST" /* ORDER_LIST */,
    img: "data:image/jpeg;base64,"
  },
  {
    value: "ORDER_LIST_1" /* ORDER_LIST_1 */,
    img: "data:image/jpeg;base64,"
  },
  {
    value: "ORDER_LIST_2" /* ORDER_LIST_2 */,
    img: "data:image/jpeg;base64,"
  },
  {
    value: "ORDER_LIST_3" /* ORDER_LIST_3 */,
    img: "data:image/jpeg;base64,"
  },
  {
    value: "ORDER_LIST_4" /* ORDER_LIST_4 */,
    img: "data:image/jpeg;base64,"
  },
  {
    value: "ORDER_LIST_5" /* ORDER_LIST_5 */,
    img: "data:image/jpeg;base64,"
  }
];
var OrderListTypePicker = (props) => {
  return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
    ListTypePicker,
    {
      ...props,
      options: orderListOptions
    }
  );
};
var bulletOptions = [
  {
    value: "BULLET_LIST" /* BULLET_LIST */,
    img: "data:image/jpeg;base64,"
  },
  {
    value: "BULLET_LIST_1" /* BULLET_LIST_1 */,
    img: "data:image/jpeg;base64,"
  },
  {
    value: "BULLET_LIST_2" /* BULLET_LIST_2 */,
    img: "data:image/jpeg;base64,"
  },
  {
    value: "BULLET_LIST_3" /* BULLET_LIST_3 */,
    img: "data:image/jpeg;base64,"
  },
  {
    value: "BULLET_LIST_4" /* BULLET_LIST_4 */,
    img: "data:image/jpeg;base64,"
  },
  {
    value: "BULLET_LIST_5" /* BULLET_LIST_5 */,
    img: "data:image/jpeg;base64,"
  }
];
var BulletListTypePicker = (props) => {
  return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
    ListTypePicker,
    {
      ...props,
      options: bulletOptions
    }
  );
};

// ../packages/docs-ui/src/components/list-type-picker/index.ts
var ORDER_LIST_TYPE_COMPONENT = `${COMPONENT_PREFIX}_ORDER_LIST_TYPE_COMPONENT`;
var BULLET_LIST_TYPE_COMPONENT = `${COMPONENT_PREFIX}_BULLET_LIST_TYPE_COMPONENT`;

// ../packages/docs-ui/src/components/paragraph-menu/index.tsx
var import_react10 = __toESM(require_react());

// ../packages/docs-ui/src/menu/paragraph-menu.ts
var import_react8 = __toESM(require_react());

// ../packages/docs-ui/src/menu/menu.ts
function getInsertTableHiddenObservable(accessor) {
  const univerInstanceService = accessor.get(IUniverInstanceService);
  const renderManagerService = accessor.get(IRenderManagerService);
  return new Observable((subscriber) => {
    const subscription = univerInstanceService.focused$.subscribe((unitId) => {
      if (unitId == null) {
        return subscriber.next(true);
      }
      const univerType = univerInstanceService.getUnitType(unitId);
      if (univerType !== 1 /* UNIVER_DOC */) {
        return subscriber.next(true);
      }
      const currentRender = renderManagerService.getRenderById(unitId);
      if (currentRender == null) {
        return subscriber.next(true);
      }
      const viewModel = currentRender.with(DocSkeletonManagerService).getViewModel();
      viewModel.editAreaChange$.subscribe((editArea) => {
        subscriber.next(editArea === "HEADER" /* HEADER */ || editArea === "FOOTER" /* FOOTER */);
      });
    });
    return () => subscription.unsubscribe();
  });
}
function getHeaderFooterMenuHiddenObservable(accessor) {
  const univerInstanceService = accessor.get(IUniverInstanceService);
  const commandService = accessor.get(ICommandService);
  return new Observable((subscriber) => {
    const subscription0 = commandService.onCommandExecuted((command) => {
      if (command.id === RichTextEditingMutation.id) {
        const { unitId } = command.params;
        const docDataModel2 = univerInstanceService.getUnit(unitId);
        if (docDataModel2 == null) {
          subscriber.next(true);
          return;
        }
        const { documentStyle } = docDataModel2.getSnapshot();
        subscriber.next((documentStyle == null ? void 0 : documentStyle.documentFlavor) !== 1 /* TRADITIONAL */);
      }
    });
    const subscription = univerInstanceService.focused$.subscribe((unitId) => {
      if (unitId == null) {
        return subscriber.next(true);
      }
      const docDataModel2 = univerInstanceService.getUniverDocInstance(unitId);
      const documentFlavor2 = docDataModel2 == null ? void 0 : docDataModel2.getSnapshot().documentStyle.documentFlavor;
      subscriber.next(documentFlavor2 !== 1 /* TRADITIONAL */);
    });
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    if (docDataModel == null) {
      return subscriber.next(true);
    }
    const documentFlavor = docDataModel == null ? void 0 : docDataModel.getSnapshot().documentStyle.documentFlavor;
    subscriber.next(documentFlavor !== 1 /* TRADITIONAL */);
    return () => {
      subscription0.dispose();
      subscription.unsubscribe();
    };
  });
}
function getTableDisabledObservable(accessor) {
  const docSelectionManagerService = accessor.get(DocSelectionManagerService);
  const univerInstanceService = accessor.get(IUniverInstanceService);
  return new Observable((subscriber) => {
    const subscription = docSelectionManagerService.textSelection$.subscribe((selection) => {
      if (selection == null) {
        subscriber.next(true);
        return;
      }
      const { textRanges } = selection;
      if (textRanges.length !== 1) {
        subscriber.next(true);
        return;
      }
      const textRange = textRanges[0];
      const { collapsed, startNodePosition, startOffset } = textRange;
      if (!collapsed || startOffset == null) {
        subscriber.next(true);
        return;
      }
      const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
      if (docDataModel == null) {
        subscriber.next(true);
        return;
      }
      const docSkeletonManagerService = getCommandSkeleton(accessor, docDataModel.getUnitId());
      if (docSkeletonManagerService == null) {
        subscriber.next(true);
        return;
      }
      const viewModel = docSkeletonManagerService.getViewModel();
      const customRange = viewModel.getCustomRangeRaw(startOffset);
      if (customRange) {
        subscriber.next(true);
        return;
      }
      if (startNodePosition != null) {
        const { path } = startNodePosition;
        if (path.indexOf("cells") !== -1) {
          subscriber.next(true);
          return;
        }
      }
      subscriber.next(false);
    });
    return () => subscription.unsubscribe();
  });
}
function disableMenuWhenNoDocRange(accessor) {
  const docSelectionManagerService = accessor.get(DocSelectionManagerService);
  const univerInstanceService = accessor.get(IUniverInstanceService);
  return new Observable((subscriber) => {
    const subscription = docSelectionManagerService.textSelection$.subscribe((selection) => {
      var _a, _b, _c;
      if (selection == null) {
        subscriber.next(true);
        return;
      }
      const { textRanges, rectRanges } = selection;
      if (textRanges.length === 0 && rectRanges.length === 0) {
        subscriber.next(true);
        return;
      }
      const document2 = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
      const codeBlockRanges = (_c = (_b = (_a = document2 == null ? void 0 : document2.getBody()) == null ? void 0 : _a.blockRanges) == null ? void 0 : _b.filter((range) => range.blockType === "code")) != null ? _c : [];
      if (codeBlockRanges.some((blockRange) => textRanges.some((range) => Math.max(range.startOffset, blockRange.startIndex) <= Math.min(range.endOffset, blockRange.endIndex)))) {
        subscriber.next(true);
        return;
      }
      subscriber.next(false);
    });
    return () => subscription.unsubscribe();
  });
}
function BoldMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  return {
    id: SetInlineFormatBoldCommand.id,
    type: 0 /* BUTTON */,
    icon: "BoldIcon",
    title: "Set bold",
    tooltip: "docs-ui.toolbar.bold",
    activated$: new Observable((subscriber) => {
      const calc = () => {
        var _a;
        const textRun = getFontStyleAtCursor(accessor);
        if (textRun == null) {
          subscriber.next(false);
          return;
        }
        const bl = (_a = textRun.ts) == null ? void 0 : _a.bl;
        subscriber.next(bl === 1 /* TRUE */);
      };
      const disposable = commandService.onCommandExecuted((c) => {
        const id = c.id;
        if (id === SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) {
          calc();
        }
      });
      calc();
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function ItalicMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  return {
    id: SetInlineFormatItalicCommand.id,
    type: 0 /* BUTTON */,
    icon: "ItalicIcon",
    title: "Set italic",
    tooltip: "docs-ui.toolbar.italic",
    activated$: new Observable((subscriber) => {
      const calc = () => {
        var _a;
        const textRun = getFontStyleAtCursor(accessor);
        if (textRun == null) {
          subscriber.next(false);
          return;
        }
        const it = (_a = textRun.ts) == null ? void 0 : _a.it;
        subscriber.next(it === 1 /* TRUE */);
      };
      const disposable = commandService.onCommandExecuted((c) => {
        const id = c.id;
        if (id === SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) {
          calc();
        }
      });
      calc();
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function UnderlineMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  return {
    id: SetInlineFormatUnderlineCommand.id,
    type: 0 /* BUTTON */,
    icon: "UnderlineIcon",
    title: "Set underline",
    tooltip: "docs-ui.toolbar.underline",
    activated$: new Observable((subscriber) => {
      const calc = () => {
        var _a;
        const textRun = getFontStyleAtCursor(accessor);
        if (textRun == null) {
          subscriber.next(false);
          return;
        }
        const ul = (_a = textRun.ts) == null ? void 0 : _a.ul;
        subscriber.next((ul == null ? void 0 : ul.s) === 1 /* TRUE */);
      };
      const disposable = commandService.onCommandExecuted((c) => {
        const id = c.id;
        if (id === SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) {
          calc();
        }
      });
      calc();
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function StrikeThroughMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  return {
    id: SetInlineFormatStrikethroughCommand.id,
    type: 0 /* BUTTON */,
    icon: "StrikethroughIcon",
    title: "Set strike through",
    tooltip: "docs-ui.toolbar.strikethrough",
    activated$: new Observable((subscriber) => {
      const calc = () => {
        var _a;
        const textRun = getFontStyleAtCursor(accessor);
        if (textRun == null) {
          subscriber.next(false);
          return;
        }
        const st = (_a = textRun.ts) == null ? void 0 : _a.st;
        subscriber.next((st == null ? void 0 : st.s) === 1 /* TRUE */);
      };
      const disposable = commandService.onCommandExecuted((c) => {
        const id = c.id;
        if (id === SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) {
          calc();
        }
      });
      calc();
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function SubscriptMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  return {
    id: SetInlineFormatSubscriptCommand.id,
    type: 0 /* BUTTON */,
    icon: "SubscriptIcon",
    tooltip: "docs-ui.toolbar.subscript",
    activated$: new Observable((subscriber) => {
      const calc = () => {
        var _a;
        const textRun = getFontStyleAtCursor(accessor);
        if (textRun == null) {
          subscriber.next(false);
          return;
        }
        const va = (_a = textRun.ts) == null ? void 0 : _a.va;
        subscriber.next(va === 2 /* SUBSCRIPT */);
      };
      const disposable = commandService.onCommandExecuted((c) => {
        const id = c.id;
        if (id === SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) {
          calc();
        }
      });
      calc();
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function SuperscriptMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  return {
    id: SetInlineFormatSuperscriptCommand.id,
    type: 0 /* BUTTON */,
    icon: "SuperscriptIcon",
    tooltip: "docs-ui.toolbar.superscript",
    activated$: new Observable((subscriber) => {
      const calc = () => {
        var _a;
        const textRun = getFontStyleAtCursor(accessor);
        if (textRun == null) {
          subscriber.next(false);
          return;
        }
        const va = (_a = textRun.ts) == null ? void 0 : _a.va;
        subscriber.next(va === 3 /* SUPERSCRIPT */);
      };
      const disposable = commandService.onCommandExecuted((c) => {
        const id = c.id;
        if (id === SetTextSelectionsOperation.id || id === SetInlineFormatCommand.id) {
          calc();
        }
      });
      calc();
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function FontFamilySelectorMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  return {
    id: SetInlineFormatFontFamilyCommand.id,
    tooltip: "docs-ui.toolbar.font",
    type: 1 /* SELECTOR */,
    label: {
      name: FONT_FAMILY_COMPONENT,
      props: {
        id: SetInlineFormatFontFamilyCommand.id
      }
    },
    selections: [{
      label: {
        name: FONT_FAMILY_ITEM_COMPONENT,
        hoverable: false,
        selectable: false,
        props: {
          id: SetInlineFormatFontFamilyCommand.id
        }
      }
    }],
    // disabled$: getCurrentSheetDisabled$(accessor),
    value$: new Observable((subscriber) => {
      const defaultValue = DEFAULT_STYLES.ff;
      const calc = () => {
        var _a;
        const textRun = getFontStyleAtCursor(accessor);
        if (textRun == null) {
          subscriber.next(defaultValue);
          return;
        }
        const ff = (_a = textRun.ts) == null ? void 0 : _a.ff;
        subscriber.next(ff != null ? ff : defaultValue);
      };
      const disposable = commandService.onCommandExecuted((c) => {
        const id = c.id;
        if (id === SetTextSelectionsOperation.id || id === SetInlineFormatFontFamilyCommand.id) {
          calc();
        }
      });
      calc();
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function FontSizeSelectorMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  return {
    id: SetInlineFormatFontSizeCommand.id,
    type: 1 /* SELECTOR */,
    tooltip: "docs-ui.toolbar.fontSize",
    label: {
      name: FONT_SIZE_COMPONENT,
      props: {
        min: 1,
        max: 400
        // disabled$,
      }
    },
    selections: FONT_SIZE_LIST,
    // disabled$,
    value$: new Observable((subscriber) => {
      const DEFAULT_SIZE = DEFAULT_STYLES.fs;
      const calc = () => {
        var _a;
        const textRun = getFontStyleAtCursor(accessor);
        if (textRun == null) {
          subscriber.next(DEFAULT_SIZE);
          return;
        }
        const fs = (_a = textRun.ts) == null ? void 0 : _a.fs;
        subscriber.next(fs != null ? fs : DEFAULT_SIZE);
      };
      const disposable = commandService.onCommandExecuted((c) => {
        const id = c.id;
        if (id === SetTextSelectionsOperation.id || id === SetInlineFormatFontSizeCommand.id) {
          calc();
        }
      });
      calc();
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function HeadingSelectorMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  return {
    id: SetParagraphNamedStyleCommand.id,
    type: 1 /* SELECTOR */,
    tooltip: "ui.toolbar.heading.tooltip",
    label: {
      name: COMMON_LABEL_COMPONENT,
      props: {
        selections: HEADING_LIST
      }
    },
    selections: HEADING_LIST.map((item) => ({
      label: {
        name: HEADING_ITEM_COMPONENT,
        props: {
          value: item.value,
          text: item.label
        }
      },
      value: item.value
    })),
    value$: new Observable((subscriber) => {
      const DEFAULT_TYPE = 1 /* NORMAL_TEXT */;
      const calc = () => {
        var _a, _b;
        const paragraph = getParagraphStyleAtCursor(accessor);
        if (paragraph == null) {
          subscriber.next(DEFAULT_TYPE);
          return;
        }
        const namedStyleType = (_b = (_a = paragraph.paragraphStyle) == null ? void 0 : _a.namedStyleType) != null ? _b : DEFAULT_TYPE;
        subscriber.next(namedStyleType);
      };
      const disposable = commandService.onCommandExecuted((c) => {
        const id = c.id;
        if (id === SetTextSelectionsOperation.id || id === SetInlineFormatFontSizeCommand.id) {
          calc();
        }
      });
      calc();
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
var FLOAT_TEXT_STYLE_MENU_ID = "doc.menu.float-text-style";
var FLOAT_TOOLBAR_MENU_POSITION = "doc.menu.float-toolbar";
var FLOAT_TEXT_STYLE_OPTIONS = [
  {
    icon: "TextTypeIcon",
    label: "ui.toolbar.heading.normal",
    value: 1 /* NORMAL_TEXT */
  },
  {
    icon: "H1Icon",
    label: "ui.toolbar.heading.1",
    value: 4 /* HEADING_1 */
  },
  {
    icon: "H2Icon",
    label: "ui.toolbar.heading.2",
    value: 5 /* HEADING_2 */
  },
  {
    icon: "H3Icon",
    label: "ui.toolbar.heading.3",
    value: 6 /* HEADING_3 */
  },
  {
    icon: "H4Icon",
    label: "ui.toolbar.heading.4",
    value: 7 /* HEADING_4 */
  },
  {
    icon: "H5Icon",
    label: "ui.toolbar.heading.5",
    value: 8 /* HEADING_5 */
  },
  {
    id: OrderListCommand.id,
    icon: "OrderIcon",
    label: "docs-ui.toolbar.order",
    value: "ORDER_LIST" /* ORDER_LIST */
  },
  {
    id: BulletListCommand.id,
    icon: "UnorderIcon",
    label: "docs-ui.toolbar.unorder",
    value: "BULLET_LIST" /* BULLET_LIST */
  },
  {
    id: CheckListCommand.id,
    icon: "TodoListDoubleIcon",
    label: "docs-ui.toolbar.checklist",
    value: "CHECK_LIST" /* CHECK_LIST */
  }
];
function registerFloatingTextStyleIcons(componentManager) {
  [
    ["TextTypeIcon", TextTypeIcon],
    ["H1Icon", H1Icon],
    ["H2Icon", H2Icon],
    ["H3Icon", H3Icon],
    ["H4Icon", H4Icon],
    ["H5Icon", H5Icon]
  ].forEach(([key, component]) => {
    if (!componentManager.get(key)) {
      componentManager.register(key, component);
    }
  });
}
function normalizeFloatingTextStyleValue(paragraph) {
  var _a, _b, _c;
  const listType = (_a = paragraph == null ? void 0 : paragraph.bullet) == null ? void 0 : _a.listType;
  if (listType == null ? void 0 : listType.startsWith("ORDER_LIST" /* ORDER_LIST */)) {
    return "ORDER_LIST" /* ORDER_LIST */;
  }
  if (listType == null ? void 0 : listType.startsWith("BULLET_LIST" /* BULLET_LIST */)) {
    return "BULLET_LIST" /* BULLET_LIST */;
  }
  if (listType === "CHECK_LIST" /* CHECK_LIST */ || listType === "CHECK_LIST_CHECKED" /* CHECK_LIST_CHECKED */) {
    return "CHECK_LIST" /* CHECK_LIST */;
  }
  return (_c = (_b = paragraph == null ? void 0 : paragraph.paragraphStyle) == null ? void 0 : _b.namedStyleType) != null ? _c : 1 /* NORMAL_TEXT */;
}
function FloatTextStyleMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  const componentManager = accessor.get(ComponentManager);
  registerFloatingTextStyleIcons(componentManager);
  return {
    id: FLOAT_TEXT_STYLE_MENU_ID,
    commandId: SetParagraphNamedStyleCommand.id,
    type: 1 /* SELECTOR */,
    icon: "TextTypeIcon",
    tooltip: "ui.toolbar.heading.tooltip",
    selections: FLOAT_TEXT_STYLE_OPTIONS,
    value$: new Observable((subscriber) => {
      const calc = () => {
        subscriber.next(normalizeFloatingTextStyleValue(getParagraphStyleAtCursor(accessor)));
      };
      const disposable = commandService.onCommandExecuted((c) => {
        const id = c.id;
        if (id === SetTextSelectionsOperation.id || id === SetParagraphNamedStyleCommand.id || id === OrderListCommand.id || id === BulletListCommand.id || id === CheckListCommand.id) {
          calc();
        }
      });
      calc();
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function TextColorSelectorMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  const themeService = accessor.get(ThemeService);
  return {
    id: SetInlineFormatTextColorCommand.id,
    icon: "FontColorDoubleIcon",
    tooltip: "docs-ui.toolbar.textColor.main",
    type: 2 /* BUTTON_SELECTOR */,
    selections: [
      {
        label: {
          name: COLOR_PICKER_COMPONENT,
          hoverable: false,
          selectable: false
        },
        value$: new Observable((subscriber) => {
          const defaultValue = DEFAULT_STYLES.cl.rgb;
          const calc = () => {
            var _a, _b;
            const textRun = getFontStyleAtCursor(accessor);
            if (!textRun) {
              subscriber.next(defaultValue);
              return;
            }
            const color = (_b = (_a = textRun.ts) == null ? void 0 : _a.cl) == null ? void 0 : _b.rgb;
            subscriber.next(color != null ? color : defaultValue);
          };
          const disposable = commandService.onCommandExecuted((c) => {
            if (c.id === SetInlineFormatTextColorCommand.id) {
              calc();
            }
          });
          calc();
          return disposable.dispose;
        })
      }
    ],
    value$: new Observable((subscriber) => {
      const defaultColor = themeService.getColorFromTheme("gray.900");
      const disposable = commandService.onCommandExecuted((c) => {
        if (c.id === SetInlineFormatTextColorCommand.id) {
          const color = c.params.value;
          subscriber.next(color != null ? color : defaultColor);
        }
      });
      subscriber.next(defaultColor);
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
    // disabled$: getCurrentSheetDisabled$(accessor),
  };
}
function HeaderFooterMenuItemFactory(accessor) {
  return {
    id: OpenHeaderFooterPanelCommand.id,
    type: 0 /* BUTTON */,
    icon: "HeaderFooterIcon",
    tooltip: "docs-ui.toolbar.headerFooter",
    hidden$: combineLatest(getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */, void 0, DOCS_ZEN_EDITOR_UNIT_ID_KEY), getHeaderFooterMenuHiddenObservable(accessor), (one, two) => {
      return one || two;
    })
  };
}
var TableIcon = "GridIcon";
var TABLE_MENU_ID = "doc.menu.table";
function TableMenuFactory(accessor) {
  return {
    id: TABLE_MENU_ID,
    type: 3 /* SUBITEMS */,
    icon: TableIcon,
    tooltip: "docs-ui.toolbar.table.main",
    disabled$: getTableDisabledObservable(accessor),
    // Do not show header footer menu and insert table at zen mode.
    hidden$: combineLatest(getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */, void 0, DOCS_ZEN_EDITOR_UNIT_ID_KEY), getInsertTableHiddenObservable(accessor), (one, two) => {
      return one || two;
    })
  };
}
function InsertTableMenuFactory(_accessor) {
  return {
    id: DocCreateTableOperation.id,
    title: "docs-ui.toolbar.table.insert",
    type: 0 /* BUTTON */,
    icon: TableIcon,
    hidden$: getMenuHiddenObservable(_accessor, 1 /* UNIVER_DOC */)
  };
}
function InsertDefaultTableMenuFactory(_accessor) {
  return {
    id: DocCreateTableOperation.id,
    commandId: CreateDocTableCommand.id,
    params: {
      rowCount: 3,
      colCount: 5
    },
    title: "docs-ui.toolbar.table.insert",
    type: 0 /* BUTTON */,
    icon: TableIcon,
    hidden$: getMenuHiddenObservable(_accessor, 1 /* UNIVER_DOC */)
  };
}
var HORIZONTAL_ALIGN_OPTIONS = [
  {
    id: AlignLeftCommand.id,
    value: 1 /* LEFT */,
    label: "docs-ui.toolbar.alignLeft",
    icon: "LeftJustifyingIcon"
  },
  {
    id: AlignCenterCommand.id,
    value: 2 /* CENTER */,
    label: "docs-ui.toolbar.alignCenter",
    icon: "HorizontallyIcon"
  },
  {
    id: AlignRightCommand.id,
    value: 3 /* RIGHT */,
    label: "docs-ui.toolbar.alignRight",
    icon: "RightJustifyingIcon"
  },
  {
    id: AlignJustifyCommand.id,
    value: 4 /* JUSTIFIED */,
    label: "docs-ui.toolbar.alignJustify",
    icon: "AlignTextBothIcon"
  }
];
function AlignMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  const value$ = new Observable((subscriber) => {
    const calc = () => {
      var _a, _b;
      const paragraph = getParagraphStyleAtCursor(accessor);
      subscriber.next((_b = (_a = paragraph == null ? void 0 : paragraph.paragraphStyle) == null ? void 0 : _a.horizontalAlign) != null ? _b : 1 /* LEFT */);
    };
    const disposable = commandService.onCommandExecuted((c) => {
      if (c.id === SetTextSelectionsOperation.id || c.id === AlignOperationCommand.id) {
        calc();
      }
    });
    calc();
    return disposable.dispose;
  });
  return {
    id: AlignOperationCommand.id,
    type: 1 /* SELECTOR */,
    icon: value$.pipe(map((alignType) => {
      var _a, _b;
      return (_b = (_a = HORIZONTAL_ALIGN_OPTIONS.find((option) => option.value === alignType)) == null ? void 0 : _a.icon) != null ? _b : "LeftJustifyingIcon";
    })),
    tooltip: "docs-ui.toolbar.alignLeft",
    selections: HORIZONTAL_ALIGN_OPTIONS,
    value$,
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */, void 0, DOCS_ZEN_EDITOR_UNIT_ID_KEY)
  };
}
function HorizontalLineFactory(accessor) {
  return {
    id: HorizontalLineCommand.id,
    type: 0 /* BUTTON */,
    icon: "ReduceIcon",
    tooltip: "docs-ui.toolbar.horizontalLine",
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */, void 0, DOCS_ZEN_EDITOR_UNIT_ID_KEY)
  };
}
var listValueFactory$ = (accessor) => {
  return new Observable((subscriber) => {
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const docSelectionManagerService = accessor.get(DocSelectionManagerService);
    const subscription = univerInstanceService.focused$.subscribe((unitId) => {
      var _a, _b, _c, _d, _e;
      if (unitId == null) {
        return;
      }
      const docDataModel = univerInstanceService.getUniverDocInstance(unitId);
      if (docDataModel == null) {
        return;
      }
      const docRanges = docSelectionManagerService.getDocRanges();
      const range = (_a = docRanges.find((r) => r.isActive)) != null ? _a : docRanges[0];
      if (range) {
        const doc = docDataModel.getSelfOrHeaderFooterModel(range == null ? void 0 : range.segmentId);
        const paragraphs = BuildTextUtils.range.getParagraphsInRange(range, (_c = (_b = doc.getBody()) == null ? void 0 : _b.paragraphs) != null ? _c : [], (_e = (_d = doc.getBody()) == null ? void 0 : _d.dataStream) != null ? _e : "");
        let listType;
        if (paragraphs.every((p) => {
          var _a2;
          if (!listType) {
            listType = (_a2 = p.bullet) == null ? void 0 : _a2.listType;
          }
          return p.bullet && p.bullet.listType === listType;
        })) {
          subscriber.next(listType);
          return;
        }
      }
      subscriber.next(void 0);
    });
    return () => {
      subscription.unsubscribe();
    };
  });
};
function OrderListMenuItemFactory(accessor) {
  return {
    id: OrderListCommand.id,
    type: 2 /* BUTTON_SELECTOR */,
    slot: true,
    selections: [
      {
        label: {
          name: ORDER_LIST_TYPE_COMPONENT,
          hoverable: false,
          selectable: false
        },
        value$: listValueFactory$(accessor)
      }
    ],
    icon: "OrderIcon",
    tooltip: "docs-ui.toolbar.order",
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */),
    disabled$: disableMenuWhenNoDocRange(accessor),
    activated$: listValueFactory$(accessor).pipe(map((v) => Boolean(v && v.indexOf("ORDER_LIST") === 0)))
  };
}
function BulletListMenuItemFactory(accessor) {
  return {
    id: BulletListCommand.id,
    type: 2 /* BUTTON_SELECTOR */,
    slot: true,
    selections: [
      {
        label: {
          name: BULLET_LIST_TYPE_COMPONENT,
          hoverable: false,
          selectable: false
        },
        value$: listValueFactory$(accessor)
      }
    ],
    icon: "UnorderIcon",
    tooltip: "docs-ui.toolbar.unorder",
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */),
    activated$: listValueFactory$(accessor).pipe(map((v) => Boolean(v && v.indexOf("BULLET_LIST") === 0)))
  };
}
function CheckListMenuItemFactory(accessor) {
  return {
    id: CheckListCommand.id,
    type: 0 /* BUTTON */,
    icon: "TodoListDoubleIcon",
    tooltip: "docs-ui.toolbar.checklist",
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */),
    activated$: listValueFactory$(accessor).pipe(map((v) => Boolean(v && v.indexOf("CHECK_LIST") === 0)))
  };
}
function DocSwitchModeMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  const univerInstanceService = accessor.get(IUniverInstanceService);
  return {
    id: SwitchDocModeCommand.id,
    type: 0 /* BUTTON */,
    icon: "KeyboardIcon",
    tooltip: "docs-ui.toolbar.documentFlavor",
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */, void 0, DOCS_ZEN_EDITOR_UNIT_ID_KEY),
    activated$: new Observable((subscriber) => {
      var _a;
      const subscription = commandService.onCommandExecuted((c) => {
        var _a2;
        if (c.id === RichTextEditingMutation.id) {
          const instance2 = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
          subscriber.next(((_a2 = instance2 == null ? void 0 : instance2.getSnapshot()) == null ? void 0 : _a2.documentStyle.documentFlavor) === 2 /* MODERN */);
        }
      });
      const instance = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
      subscriber.next(((_a = instance == null ? void 0 : instance.getSnapshot()) == null ? void 0 : _a.documentStyle.documentFlavor) === 2 /* MODERN */);
      return () => subscription.dispose();
    })
  };
}
function ResetBackgroundColorMenuItemFactory(accessor) {
  return {
    id: ResetInlineFormatTextBackgroundColorCommand.id,
    type: 0 /* BUTTON */,
    title: "docs-ui.toolbar.resetColor",
    icon: "NoColorDoubleIcon"
  };
}
function BackgroundColorSelectorMenuItemFactory(accessor) {
  const commandService = accessor.get(ICommandService);
  const themeService = accessor.get(ThemeService);
  return {
    id: SetInlineFormatTextBackgroundColorCommand.id,
    tooltip: "docs-ui.toolbar.fillColor.main",
    type: 2 /* BUTTON_SELECTOR */,
    icon: "PaintBucketDoubleIcon",
    selections: [
      {
        label: {
          name: COLOR_PICKER_COMPONENT,
          hoverable: false,
          selectable: false
        },
        value$: new Observable((subscriber) => {
          const defaultValue = DEFAULT_STYLES.bg.rgb;
          const calc = () => {
            var _a, _b;
            const textRun = getFontStyleAtCursor(accessor);
            if (!textRun) {
              subscriber.next(defaultValue);
              return;
            }
            const color = (_b = (_a = textRun.ts) == null ? void 0 : _a.bg) == null ? void 0 : _b.rgb;
            subscriber.next(color != null ? color : defaultValue);
          };
          const disposable = commandService.onCommandExecuted((c) => {
            if (c.id === SetInlineFormatTextBackgroundColorCommand.id) {
              calc();
            }
          });
          calc();
          return disposable.dispose;
        })
      }
    ],
    value$: new Observable((subscriber) => {
      const defaultColor = themeService.getColorFromTheme("primary.600");
      const disposable = commandService.onCommandExecuted((c) => {
        if (c.id === SetInlineFormatTextBackgroundColorCommand.id) {
          const color = c.params.value;
          subscriber.next(color != null ? color : defaultColor);
        }
      });
      subscriber.next(defaultColor);
      return disposable.dispose;
    }),
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function getFontStyleAtCursor(accessor) {
  var _a, _b, _c, _d;
  const univerInstanceService = accessor.get(IUniverInstanceService);
  const textSelectionService = accessor.get(DocSelectionManagerService);
  const docMenuStyleService = accessor.get(DocMenuStyleService);
  const docDataModel = univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
  const docRanges = textSelectionService.getDocRanges();
  const activeRange = (_a = docRanges.find((r) => r.isActive)) != null ? _a : docRanges[0];
  const defaultTextStyle = docMenuStyleService.getDefaultStyle();
  const cacheStyle = (_b = docMenuStyleService.getStyleCache()) != null ? _b : {};
  const paragraph = getParagraphStyleAtCursor(accessor);
  const namedStyle = ((_c = paragraph == null ? void 0 : paragraph.paragraphStyle) == null ? void 0 : _c.namedStyleType) ? NAMED_STYLE_MAP[(_d = paragraph == null ? void 0 : paragraph.paragraphStyle) == null ? void 0 : _d.namedStyleType] : null;
  if (docDataModel == null || activeRange == null) {
    return {
      ts: {
        ...defaultTextStyle,
        ...namedStyle,
        ...cacheStyle
      }
    };
  }
  const { segmentId } = activeRange;
  const body = docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
  if (body == null) {
    return {
      ts: {
        ...defaultTextStyle,
        ...namedStyle,
        ...cacheStyle
      }
    };
  }
  const curTextStyle = getStyleInTextRange(body, activeRange, {});
  Tools.deleteNull(curTextStyle);
  return {
    ts: {
      ...defaultTextStyle,
      ...namedStyle,
      ...curTextStyle,
      ...cacheStyle
    }
  };
}
function getParagraphStyleAtCursor(accessor) {
  var _a, _b;
  const univerInstanceService = accessor.get(IUniverInstanceService);
  const textSelectionService = accessor.get(DocSelectionManagerService);
  const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
  const docRanges = textSelectionService.getDocRanges();
  const activeRange = (_a = docRanges.find((r) => r.isActive)) != null ? _a : docRanges[0];
  if (docDataModel == null || activeRange == null) {
    return;
  }
  const { startOffset, segmentId } = activeRange;
  const paragraphs = (_b = docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody()) == null ? void 0 : _b.paragraphs;
  if (paragraphs == null) {
    return;
  }
  let prevIndex = -1;
  for (const paragraph of paragraphs) {
    const { startIndex } = paragraph;
    if (startOffset > prevIndex && startOffset <= startIndex) {
      return paragraph;
    }
    prevIndex = startIndex;
  }
  return null;
}
function PageSettingMenuItemFactory(accessor) {
  return {
    id: DocOpenPageSettingCommand.id,
    type: 0 /* BUTTON */,
    icon: "DocumentSettingIcon",
    tooltip: "docs-ui.toolbar.pageSetup",
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}

// ../packages/docs-ui/src/menu/paragraph-menu.ts
var HEADING_MAP = {
  [4 /* HEADING_1 */]: H1HeadingCommand,
  [5 /* HEADING_2 */]: H2HeadingCommand,
  [6 /* HEADING_3 */]: H3HeadingCommand,
  [7 /* HEADING_4 */]: H4HeadingCommand,
  [8 /* HEADING_5 */]: H5HeadingCommand,
  [1 /* NORMAL_TEXT */]: NormalTextHeadingCommand,
  [2 /* TITLE */]: TitleHeadingCommand,
  [3 /* SUBTITLE */]: SubtitleHeadingCommand,
  [0 /* NAMED_STYLE_TYPE_UNSPECIFIED */]: NormalTextHeadingCommand
};
var HEADING_TITLE_MAP = {
  [4 /* HEADING_1 */]: "ui.toolbar.heading.1",
  [5 /* HEADING_2 */]: "ui.toolbar.heading.2",
  [6 /* HEADING_3 */]: "ui.toolbar.heading.3",
  [7 /* HEADING_4 */]: "ui.toolbar.heading.4",
  [8 /* HEADING_5 */]: "ui.toolbar.heading.5",
  [1 /* NORMAL_TEXT */]: "ui.toolbar.heading.normal",
  [2 /* TITLE */]: "ui.toolbar.heading.title",
  [3 /* SUBTITLE */]: "ui.toolbar.heading.subTitle"
};
function TitleTypeIcon({ className }) {
  return (0, import_react8.createElement)(
    "svg",
    {
      className,
      viewBox: "0 0 24 24",
      fill: "currentColor",
      "aria-hidden": true
    },
    (0, import_react8.createElement)("text", {
      x: 2,
      y: 19,
      fontFamily: "Arial, sans-serif",
      fontSize: 19,
      fontWeight: 500
    }, "T"),
    (0, import_react8.createElement)("text", {
      x: 15,
      y: 19,
      fontFamily: "Arial, sans-serif",
      fontSize: 13,
      fontWeight: 500
    }, "t")
  );
}
function SubtitleTypeIcon({ className }) {
  return (0, import_react8.createElement)(
    "svg",
    {
      className,
      viewBox: "0 0 24 24",
      fill: "currentColor",
      "aria-hidden": true
    },
    (0, import_react8.createElement)("text", {
      x: 5,
      y: 19,
      fontFamily: "Arial, sans-serif",
      fontSize: 19,
      fontWeight: 500
    }, "S")
  );
}
var HEADING_ICON_MAP = {
  [4 /* HEADING_1 */]: { key: "H1Icon", component: H1Icon },
  [5 /* HEADING_2 */]: { key: "H2Icon", component: H2Icon },
  [6 /* HEADING_3 */]: { key: "H3Icon", component: H3Icon },
  [7 /* HEADING_4 */]: { key: "H4Icon", component: H4Icon },
  [8 /* HEADING_5 */]: { key: "H5Icon", component: H5Icon },
  [1 /* NORMAL_TEXT */]: { key: "TextTypeIcon", component: TextTypeIcon },
  [2 /* TITLE */]: { key: "TitleTypeIcon", component: TitleTypeIcon },
  [3 /* SUBTITLE */]: { key: "SubtitleTypeIcon", component: SubtitleTypeIcon },
  [0 /* NAMED_STYLE_TYPE_UNSPECIFIED */]: { key: "TextTypeIcon", component: TextTypeIcon }
};
var createHeadingSelectorMenuItemFactory = (headingType) => (accessor) => {
  const commandService = accessor.get(ICommandService);
  const componentManager = accessor.get(ComponentManager);
  const icon = HEADING_ICON_MAP[headingType];
  if (!componentManager.get(icon.key)) {
    componentManager.register(icon.key, icon.component);
  }
  return {
    id: HEADING_MAP[headingType].id,
    type: 0 /* BUTTON */,
    icon: icon.key,
    title: HEADING_TITLE_MAP[headingType],
    tooltip: "ui.toolbar.heading.tooltip",
    disabled$: disableMenuWhenNoDocRange(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */),
    activated$: new Observable((subscriber) => {
      const DEFAULT_TYPE = 1 /* NORMAL_TEXT */;
      const calc = () => {
        var _a, _b;
        const paragraph = getParagraphStyleAtCursor(accessor);
        if (paragraph == null) {
          subscriber.next(DEFAULT_TYPE === headingType);
          return;
        }
        const namedStyleType = (_b = (_a = paragraph.paragraphStyle) == null ? void 0 : _a.namedStyleType) != null ? _b : DEFAULT_TYPE;
        subscriber.next(namedStyleType === headingType);
      };
      const disposable = commandService.onCommandExecuted((c) => {
        const id = c.id;
        if (id === SetTextSelectionsOperation.id || id === SetInlineFormatFontSizeCommand.id) {
          calc();
        }
      });
      calc();
      return disposable.dispose;
    })
  };
};
var H1HeadingMenuItemFactory = createHeadingSelectorMenuItemFactory(4 /* HEADING_1 */);
var H2HeadingMenuItemFactory = createHeadingSelectorMenuItemFactory(5 /* HEADING_2 */);
var H3HeadingMenuItemFactory = createHeadingSelectorMenuItemFactory(6 /* HEADING_3 */);
var H4HeadingMenuItemFactory = createHeadingSelectorMenuItemFactory(7 /* HEADING_4 */);
var H5HeadingMenuItemFactory = createHeadingSelectorMenuItemFactory(8 /* HEADING_5 */);
var NormalTextHeadingMenuItemFactory = createHeadingSelectorMenuItemFactory(1 /* NORMAL_TEXT */);
var TitleHeadingMenuItemFactory = createHeadingSelectorMenuItemFactory(2 /* TITLE */);
var SubtitleHeadingMenuItemFactory = createHeadingSelectorMenuItemFactory(3 /* SUBTITLE */);
var createEmptyParagraphButtonFactory = (command, icon, title) => (accessor) => {
  const componentManager = accessor.get(ComponentManager);
  const headingIcon = Object.values(HEADING_ICON_MAP).find((item) => item.key === icon);
  if (headingIcon && !componentManager.get(headingIcon.key)) {
    componentManager.register(headingIcon.key, headingIcon.component);
  }
  return {
    id: command.id,
    type: 0 /* BUTTON */,
    icon,
    title
  };
};
var EMPTY_PARAGRAPH_MENU_ID = "doc.menu.empty-paragraph";
var EmptyParagraphH1MenuItemFactory = createEmptyParagraphButtonFactory(H1HeadingCommand, "H1Icon", "ui.toolbar.heading.1");
var EmptyParagraphH2MenuItemFactory = createEmptyParagraphButtonFactory(H2HeadingCommand, "H2Icon", "ui.toolbar.heading.2");
var EmptyParagraphH3MenuItemFactory = createEmptyParagraphButtonFactory(H3HeadingCommand, "H3Icon", "ui.toolbar.heading.3");
var EmptyParagraphH4MenuItemFactory = createEmptyParagraphButtonFactory(H4HeadingCommand, "H4Icon", "ui.toolbar.heading.4");
var EmptyParagraphH5MenuItemFactory = createEmptyParagraphButtonFactory(H5HeadingCommand, "H5Icon", "ui.toolbar.heading.5");
var EmptyParagraphNormalTextMenuItemFactory = createEmptyParagraphButtonFactory(NormalTextHeadingCommand, "TextTypeIcon", "ui.toolbar.heading.normal");
var EmptyParagraphOrderListMenuItemFactory = createEmptyParagraphButtonFactory(OrderListCommand, "OrderIcon", "docs-ui.rightClick.orderList");
var EmptyParagraphBulletListMenuItemFactory = createEmptyParagraphButtonFactory(BulletListCommand, "UnorderIcon", "docs-ui.rightClick.bulletList");
var EmptyParagraphCheckListMenuItemFactory = createEmptyParagraphButtonFactory(CheckListCommand, "TodoListDoubleIcon", "docs-ui.rightClick.checkList");
var EmptyParagraphHorizontalLineMenuItemFactory = createEmptyParagraphButtonFactory(HorizontalLineCommand, "ReduceIcon", "docs-ui.toolbar.horizontalLine");
var CopyCurrentParagraphMenuItemFactory = (accessor) => {
  return {
    id: DocCopyCurrentParagraphCommand.id,
    type: 0 /* BUTTON */,
    icon: "CopyDoubleIcon",
    title: "docs-ui.rightClick.copy"
  };
};
var CutCurrentParagraphMenuItemFactory = (accessor) => {
  return {
    id: DocCutCurrentParagraphCommand.id,
    type: 0 /* BUTTON */,
    icon: "CutIcon",
    title: "docs-ui.rightClick.cut"
  };
};
var DeleteCurrentParagraphMenuItemFactory = (accessor) => {
  return {
    id: DeleteCurrentParagraphCommand.id,
    type: 0 /* BUTTON */,
    icon: "DeleteIcon",
    title: "docs-ui.rightClick.delete"
  };
};
var InsertBulletListBellowMenuItemFactory = (accessor) => {
  return {
    id: InsertBulletListBellowCommand.id,
    type: 0 /* BUTTON */,
    icon: "UnorderIcon",
    title: "docs-ui.rightClick.bulletList"
  };
};
var InsertOrderListBellowMenuItemFactory = (accessor) => {
  return {
    id: InsertOrderListBellowCommand.id,
    type: 0 /* BUTTON */,
    icon: "OrderIcon",
    title: "docs-ui.rightClick.orderList"
  };
};
var InsertCheckListBellowMenuItemFactory = (accessor) => {
  return {
    id: InsertCheckListBellowCommand.id,
    type: 0 /* BUTTON */,
    icon: "TodoListDoubleIcon",
    title: "docs-ui.rightClick.checkList"
  };
};
var InsertHorizontalLineBellowMenuItemFactory = (accessor) => {
  return {
    id: InsertHorizontalLineBellowCommand.id,
    type: 0 /* BUTTON */,
    icon: "ReduceIcon",
    title: "docs-ui.toolbar.horizontalLine"
  };
};
var INSERT_BELLOW_MENU_ID = "doc.menu.insert-bellow";
var DOC_CONTENT_INSERT_MENU_ID = "doc.menu.content-insert";
var DOC_TABLE_BLOCK_MENU_ID = "doc.menu.table-block";
var TableBlockCopyMenuItemFactory = (accessor) => {
  return {
    id: DocCopyCommand.name,
    commandId: DocCopyCommand.id,
    type: 0 /* BUTTON */,
    icon: "CopyDoubleIcon",
    title: "docs-ui.rightClick.copy"
  };
};
var TableBlockPasteMenuItemFactory = (accessor) => {
  return {
    id: DocPasteCommand.id,
    type: 0 /* BUTTON */,
    icon: "PasteSpecialDoubleIcon",
    title: "docs-ui.rightClick.paste"
  };
};
var TableBlockDeleteMenuItemFactory = (accessor) => {
  return {
    id: DocTableDeleteTableCommand.id,
    type: 0 /* BUTTON */,
    icon: "DeleteIcon",
    title: "docs-ui.rightClick.delete"
  };
};
function DocInsertBellowMenuItemFactory(accessor) {
  return {
    id: INSERT_BELLOW_MENU_ID,
    type: 3 /* SUBITEMS */,
    title: "docs-ui.rightClick.insertBellow"
  };
}

// ../packages/docs-ui/src/types/const/padding.ts
var DOC_VERTICAL_PADDING = 4;

// ../packages/docs-ui/src/services/doc-popup-manager.service.ts
function transformBound2OffsetBound(originBound, scene) {
  const topLeft = transformPosition2Offset(originBound.left, originBound.top, scene);
  const bottomRight = transformPosition2Offset(originBound.right, originBound.bottom, scene);
  return {
    left: topLeft.x,
    top: topLeft.y,
    right: bottomRight.x,
    bottom: bottomRight.y
  };
}
function transformPosition2Offset(x, y, scene) {
  const { scaleX, scaleY } = scene.getAncestorScale();
  const viewMain = scene.getViewport("viewMain" /* VIEW_MAIN */);
  if (!viewMain) {
    return {
      x,
      y
    };
  }
  const { viewportScrollX: actualScrollX, viewportScrollY: actualScrollY } = viewMain;
  const offsetX = (x - actualScrollX) * scaleX;
  const offsetY = (y - actualScrollY) * scaleY;
  return {
    x: offsetX,
    y: offsetY
  };
}
function transformOffset2Bound(offsetX, offsetY, scene) {
  const { scaleX, scaleY } = scene.getAncestorScale();
  const viewMain = scene.getViewport("viewMain" /* VIEW_MAIN */);
  if (!viewMain) {
    return {
      x: offsetX,
      y: offsetY
    };
  }
  const { viewportScrollX: actualScrollX, viewportScrollY: actualScrollY } = viewMain;
  const x = offsetX / scaleX + actualScrollX;
  const y = offsetY / scaleY + actualScrollY;
  return {
    x,
    y
  };
}
var calcDocRangePositions = (range, currentRender) => {
  const { scene, mainComponent, engine } = currentRender;
  const skeleton = currentRender.with(DocSkeletonManagerService).getSkeleton();
  const startPosition = skeleton.findNodePositionByCharIndex(range.startOffset, true, range.segmentId, range.segmentPage);
  const endIndex = range.collapsed ? range.startOffset : range.endOffset - 1;
  const endPosition = skeleton.findNodePositionByCharIndex(endIndex, true, range.segmentId, range.segmentPage);
  const document2 = mainComponent;
  if (!endPosition || !startPosition) {
    return;
  }
  const documentOffsetConfig = document2.getOffsetConfig();
  const { docsLeft, docsTop } = documentOffsetConfig;
  const canvasElement = engine.getCanvasElement();
  const canvasClientRect = canvasElement.getBoundingClientRect();
  const widthOfCanvas = pxToNum(canvasElement.style.width);
  const { top, left, width } = canvasClientRect;
  const scaleAdjust = width / widthOfCanvas;
  const { scaleX, scaleY } = scene.getAncestorScale();
  const convertor = new NodePositionConvertToCursor(documentOffsetConfig, skeleton);
  const { borderBoxPointGroup } = convertor.getRangePointData(startPosition, endPosition);
  const bounds = getLineBounding(borderBoxPointGroup);
  const res = bounds.map((bound) => transformBound2OffsetBound(bound, scene)).map((i) => ({
    left: (i.left + docsLeft * scaleX) * scaleAdjust + left,
    right: (i.right + docsLeft * scaleX) * scaleAdjust + left,
    top: (i.top + docsTop * scaleY) * scaleAdjust + top,
    bottom: (i.bottom + docsTop * scaleY) * scaleAdjust + top
  }));
  return res;
};
var DocCanvasPopManagerService = class extends Disposable {
  constructor(_globalPopupManagerService, _renderManagerService, _univerInstanceService, _commandService) {
    super();
    __publicField(this, "_globalPopupManagerService", _globalPopupManagerService);
    __publicField(this, "_renderManagerService", _renderManagerService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_commandService", _commandService);
  }
  _createRectPositionObserver(rect, currentRender) {
    const calc = () => {
      const { scene, engine } = currentRender;
      const bound = typeof rect === "function" ? rect() : rect;
      const canvasElement = engine.getCanvasElement();
      const canvasClientRect = canvasElement.getBoundingClientRect();
      const widthOfCanvas = pxToNum(canvasElement.style.width);
      const offsetBound = transformBound2OffsetBound(bound, scene);
      const { top: topOffset, left: leftOffset, width: domWidth } = canvasClientRect;
      const scaleAdjust = domWidth / widthOfCanvas;
      const position2 = {
        left: offsetBound.left * scaleAdjust + leftOffset,
        right: offsetBound.right * scaleAdjust + leftOffset,
        top: offsetBound.top * scaleAdjust + topOffset,
        bottom: offsetBound.bottom * scaleAdjust + topOffset
      };
      return position2;
    };
    const position = calc();
    const position$ = new BehaviorSubject(position);
    const disposable = new DisposableCollection();
    disposable.add(this._commandService.onCommandExecuted((commandInfo) => {
      if (commandInfo.id === SetDocZoomRatioOperation.id || commandInfo.id === RichTextEditingMutation.id) {
        const newPosition = calc();
        if (newPosition) {
          position$.next(newPosition);
        }
      }
    }));
    const viewMain = currentRender.scene.getViewport("viewMain" /* VIEW_MAIN */);
    if (viewMain) {
      disposable.add(viewMain.onScrollAfter$.subscribeEvent(() => {
        position$.next(calc());
      }));
    }
    disposable.add(currentRender.scene.onTransformChange$.subscribeEvent(() => {
      position$.next(calc());
    }));
    return {
      position,
      position$,
      disposable
    };
  }
  _createObjectPositionObserver(targetObject, currentRender) {
    const getBound = () => {
      const { left, top, width, height } = targetObject;
      const bound = {
        left,
        right: left + width,
        top,
        bottom: top + height
      };
      return bound;
    };
    return this._createRectPositionObserver(getBound, currentRender);
  }
  _createRangePositionObserver(range, currentRender) {
    var _a;
    const positions = (_a = calcDocRangePositions(range, currentRender)) != null ? _a : [];
    const positions$ = new BehaviorSubject(positions);
    const disposable = new DisposableCollection();
    disposable.add(this._commandService.onCommandExecuted((commandInfo) => {
      if (commandInfo.id === SetDocZoomRatioOperation.id || commandInfo.id === RichTextEditingMutation.id) {
        const params = commandInfo.params;
        if (params.unitId === currentRender.unitId) {
          const position = calcDocRangePositions(range, currentRender);
          if (position) {
            positions$.next(position);
          }
        }
      }
    }));
    const viewMain = currentRender.scene.getViewport("viewMain" /* VIEW_MAIN */);
    if (viewMain) {
      disposable.add(viewMain.onScrollAfter$.subscribeEvent(() => {
        const position = calcDocRangePositions(range, currentRender);
        if (position) {
          positions$.next(position);
        }
      }));
    }
    disposable.add(currentRender.scene.onTransformChange$.subscribeEvent(() => {
      const position = calcDocRangePositions(range, currentRender);
      if (position) {
        positions$.next(position);
      }
    }));
    return {
      positions,
      positions$,
      disposable
    };
  }
  attachPopupToRect(rect, popup, unitId) {
    const currentRender = this._renderManagerService.getRenderById(unitId);
    if (!currentRender) {
      throw new Error(`Current render not found, unitId: ${unitId}`);
    }
    const { position, position$, disposable } = this._createRectPositionObserver(rect, currentRender);
    const id = this._globalPopupManagerService.addPopup({
      ...popup,
      unitId,
      subUnitId: "default",
      anchorRect: position,
      anchorRect$: position$,
      canvasElement: currentRender.engine.getCanvasElement()
    });
    return {
      dispose: () => {
        this._globalPopupManagerService.removePopup(id);
        position$.complete();
        disposable.dispose();
      },
      canDispose: () => this._globalPopupManagerService.activePopupId !== id
    };
  }
  // #region attach to object
  /**
   * attach a popup to canvas object
   * @param targetObject target canvas object
   * @param popup popup item
   * @returns disposable
   */
  attachPopupToObject(targetObject, popup, unitId) {
    const currentRender = this._renderManagerService.getRenderById(unitId);
    if (!currentRender) {
      throw new Error(`Current render not found, unitId: ${unitId}`);
    }
    const { position, position$, disposable } = this._createObjectPositionObserver(targetObject, currentRender);
    const id = this._globalPopupManagerService.addPopup({
      ...popup,
      unitId,
      subUnitId: "default",
      anchorRect: position,
      anchorRect$: position$,
      canvasElement: currentRender.engine.getCanvasElement()
    });
    return {
      dispose: () => {
        this._globalPopupManagerService.removePopup(id);
        position$.complete();
        disposable.dispose();
      },
      canDispose: () => this._globalPopupManagerService.activePopupId !== id
    };
  }
  // #endregion
  // #region attach to range
  /**
   * attach a popup to doc range
   * @param range doc range
   * @param popup popup item
   * @param unitId unit id
   * @returns disposable
   */
  attachPopupToRange(range, popup, unitId) {
    const doc = this._univerInstanceService.getUnit(unitId);
    if (!doc) {
      throw new Error(`Document not found, unitId: ${unitId}`);
    }
    const { direction = "top", multipleDirection } = popup;
    const currentRender = this._renderManagerService.getRenderById(unitId);
    if (!currentRender) {
      throw new Error(`Current render not found, unitId: ${unitId}`);
    }
    const { positions: bounds, positions$: bounds$, disposable } = this._createRangePositionObserver(range, currentRender);
    const position$ = bounds$.pipe(map((bounds2) => direction.includes("top") ? bounds2[0] : bounds2[bounds2.length - 1]));
    const id = this._globalPopupManagerService.addPopup({
      ...popup,
      unitId,
      subUnitId: "default",
      anchorRect: direction.includes("top") ? bounds[0] : bounds[bounds.length - 1],
      anchorRect$: position$,
      excludeRects: bounds,
      excludeRects$: bounds$,
      direction: ["top", "bottom", "horizontal"].some((i) => direction.includes(i)) ? bounds.length > 1 ? multipleDirection != null ? multipleDirection : direction : direction : direction,
      canvasElement: currentRender.engine.getCanvasElement()
    });
    return {
      dispose: () => {
        this._globalPopupManagerService.removePopup(id);
        bounds$.complete();
        disposable.dispose();
      },
      canDispose: () => this._globalPopupManagerService.activePopupId !== id
    };
  }
  // #endregion
};
DocCanvasPopManagerService = __decorateClass([
  __decorateParam(0, Inject(ICanvasPopupService)),
  __decorateParam(1, IRenderManagerService),
  __decorateParam(2, IUniverInstanceService),
  __decorateParam(3, ICommandService)
], DocCanvasPopManagerService);

// ../packages/docs-ui/src/services/doc-event-manager.service.ts
var calcDocRangePositions2 = (range, documents, skeleton, pageIndex) => {
  var _a, _b, _c, _d, _e;
  const startPosition = skeleton.findNodePositionByCharIndex(range.startOffset, true, range.segmentId, pageIndex);
  const skeletonData = skeleton.getSkeletonData();
  let end = range.collapsed ? range.startOffset : range.endOffset - 1;
  if (range.segmentId) {
    const root = (_e = Array.from((_b = (_a = skeletonData == null ? void 0 : skeletonData.skeFooters.get(range.segmentId)) == null ? void 0 : _a.values()) != null ? _b : [])[0]) != null ? _e : Array.from((_d = (_c = skeletonData == null ? void 0 : skeletonData.skeHeaders.get(range.segmentId)) == null ? void 0 : _c.values()) != null ? _d : [])[0];
    if (root) {
      end = Math.min(root.ed, end);
    }
  }
  const endPosition = skeleton.findNodePositionByCharIndex(end, true, range.segmentId, pageIndex);
  if (!endPosition || !startPosition) {
    return;
  }
  const documentOffsetConfig = documents.getOffsetConfig();
  const convertor = new NodePositionConvertToCursor(documentOffsetConfig, skeleton);
  const { borderBoxPointGroup } = convertor.getRangePointData(startPosition, endPosition);
  const bounds = getLineBounding(borderBoxPointGroup);
  return bounds.map((rect) => ({
    top: rect.top + documentOffsetConfig.docsTop - DOC_VERTICAL_PADDING,
    bottom: rect.bottom + documentOffsetConfig.docsTop + DOC_VERTICAL_PADDING,
    left: rect.left + documentOffsetConfig.docsLeft,
    right: rect.right + documentOffsetConfig.docsLeft
  }));
};
var calcDocParagraphPositions = (sections, top, left, pageWidth) => {
  const paragraphBounds = [];
  for (const section of sections) {
    const sectionTop = section.top;
    for (const column of section.columns) {
      const columnLeft = column.left;
      const width = section.colCount === 1 ? pageWidth : column.width;
      let currentParagraph = null;
      for (const line of column.lines) {
        const startIndex = line.paragraphIndex;
        if (line.paragraphStart) {
          if (currentParagraph) {
            paragraphBounds.push(currentParagraph);
            currentParagraph = null;
          }
          const lineRect = {
            top: top + sectionTop + line.top,
            left: left + columnLeft,
            right: left + columnLeft + width,
            bottom: top + sectionTop + line.top + line.lineHeight
          };
          currentParagraph = {
            paragraphStart: line.st,
            paragraphEnd: startIndex,
            startIndex,
            rect: lineRect,
            fisrtLine: {
              top: top + sectionTop + line.top + line.marginTop + line.paddingTop,
              left: left + columnLeft,
              right: left + columnLeft + width,
              bottom: top + sectionTop + line.top + line.marginTop + line.paddingTop + line.contentHeight
            }
          };
        } else {
          if (currentParagraph && currentParagraph.startIndex === startIndex) {
            currentParagraph.rect.bottom = top + sectionTop + line.top + line.lineHeight;
          }
        }
      }
      if (currentParagraph) {
        paragraphBounds.push(currentParagraph);
      }
    }
  }
  return paragraphBounds;
};
var calcDocGlyphPosition = (glyph, documents, skeleton, pageIndex = -1) => {
  const start = skeleton.findPositionByGlyph(glyph, pageIndex);
  if (!start) {
    return;
  }
  const documentOffsetConfig = documents.getOffsetConfig();
  const startPosition = { ...start, isBack: true };
  const convertor = new NodePositionConvertToCursor(documentOffsetConfig, skeleton);
  const { borderBoxPointGroup } = convertor.getRangePointData(startPosition, startPosition);
  const bounds = getLineBounding(borderBoxPointGroup);
  const rect = bounds[0];
  if (!rect) {
    return;
  }
  return {
    top: rect.top + documentOffsetConfig.docsTop,
    bottom: rect.bottom + documentOffsetConfig.docsTop,
    left: rect.left + documentOffsetConfig.docsLeft,
    right: rect.left + documentOffsetConfig.docsLeft + glyph.width
  };
};
var TABLE_BLOCK_MENU_HOVER_GUTTER_LEFT = 72;
var TABLE_BLOCK_MENU_HOVER_GUTTER_TOP = 42;
function isPointInRect(x, y, rect) {
  const { left, right, top, bottom } = rect;
  if (x >= left && x <= right && y >= top && y <= bottom) {
    return true;
  }
  return false;
}
function getTableBlockMenuHoverRect(tableRect) {
  return {
    bottom: tableRect.bottom,
    left: tableRect.left - TABLE_BLOCK_MENU_HOVER_GUTTER_LEFT,
    right: tableRect.right,
    top: tableRect.top - TABLE_BLOCK_MENU_HOVER_GUTTER_TOP
  };
}
function getTableHorizontalViewportGeometry(tableLeft, tableWidth, viewport) {
  const hasHorizontalViewport = viewport != null && viewport.contentWidth > viewport.viewportWidth;
  return {
    scrollLeft: hasHorizontalViewport ? viewport.scrollLeft : 0,
    visibleLeft: tableLeft,
    visibleRight: tableLeft + (hasHorizontalViewport ? viewport.viewportWidth : tableWidth)
  };
}
var DocEventManagerService = class extends Disposable {
  constructor(_context, _docSkeletonManagerService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_hoverCustomRanges$", new BehaviorSubject([]));
    __publicField(this, "hoverCustomRanges$", this._hoverCustomRanges$.pipe(distinctUntilChanged((pre, aft) => pre.length === aft.length && pre.every((item, i) => aft[i].range.rangeId === item.range.rangeId && aft[i].segmentId === item.segmentId && aft[i].segmentPageIndex === item.segmentPageIndex && aft[i].range.startIndex === item.range.startIndex))));
    __publicField(this, "_clickCustomRanges$", new Subject());
    __publicField(this, "clickCustomRanges$", this._clickCustomRanges$.asObservable());
    __publicField(this, "_hoverBullet$", new Subject());
    __publicField(this, "hoverBullet$", this._hoverBullet$.pipe(distinctUntilChanged((pre, aft) => (pre == null ? void 0 : pre.paragraph.startIndex) === (aft == null ? void 0 : aft.paragraph.startIndex) && (pre == null ? void 0 : pre.segmentId) === (aft == null ? void 0 : aft.segmentId) && (pre == null ? void 0 : pre.segmentPageIndex) === (aft == null ? void 0 : aft.segmentPageIndex))));
    __publicField(this, "_clickBullet$", new Subject());
    __publicField(this, "clickBullets$", this._clickBullet$.asObservable());
    __publicField(this, "_contextMenuBullet$", new Subject());
    __publicField(this, "contextMenuBullets$", this._contextMenuBullet$.asObservable());
    __publicField(this, "_hoverParagraph$", new BehaviorSubject(null));
    __publicField(this, "hoverParagraph$", this._hoverParagraph$.pipe(distinctUntilChanged((pre, aft) => (pre == null ? void 0 : pre.startIndex) === (aft == null ? void 0 : aft.startIndex) && (pre == null ? void 0 : pre.segmentId) === (aft == null ? void 0 : aft.segmentId) && (pre == null ? void 0 : pre.pageIndex) === (aft == null ? void 0 : aft.pageIndex))));
    __publicField(this, "hoverParagraphRealTime$", this._hoverParagraph$.asObservable());
    __publicField(this, "_hoverParagraphLeft$", new BehaviorSubject(null));
    __publicField(this, "hoverParagraphLeft$", this._hoverParagraphLeft$.pipe(distinctUntilChanged((pre, aft) => (pre == null ? void 0 : pre.startIndex) === (aft == null ? void 0 : aft.startIndex) && (pre == null ? void 0 : pre.segmentId) === (aft == null ? void 0 : aft.segmentId) && (pre == null ? void 0 : pre.pageIndex) === (aft == null ? void 0 : aft.pageIndex))));
    __publicField(this, "hoverParagraphLeftRealTime$", this._hoverParagraphLeft$.asObservable());
    __publicField(this, "_hoverTableCell$", new Subject());
    __publicField(this, "hoverTableCell$", this._hoverTableCell$.pipe(distinctUntilChanged((pre, aft) => (pre == null ? void 0 : pre.rowIndex) === (aft == null ? void 0 : aft.rowIndex) && (pre == null ? void 0 : pre.colIndex) === (aft == null ? void 0 : aft.colIndex) && (pre == null ? void 0 : pre.tableId) === (aft == null ? void 0 : aft.tableId) && (pre == null ? void 0 : pre.pageIndex) === (aft == null ? void 0 : aft.pageIndex))));
    __publicField(this, "hoverTableCellRealTime$", this._hoverTableCell$.asObservable());
    __publicField(this, "_hoverTable$", new Subject());
    __publicField(this, "hoverTable$", this._hoverTable$.pipe(distinctUntilChanged((pre, aft) => (pre == null ? void 0 : pre.tableId) === (aft == null ? void 0 : aft.tableId) && (pre == null ? void 0 : pre.pageIndex) === (aft == null ? void 0 : aft.pageIndex))));
    __publicField(this, "hoverTableRealTime$", this._hoverTable$.asObservable());
    __publicField(this, "_customRangeDirty", true);
    __publicField(this, "_bulletDirty", true);
    __publicField(this, "_paragraphDirty", true);
    /**
     * cache the bounding of custom ranges,
     * it will be updated when the doc-skeleton is recalculated
     */
    __publicField(this, "_customRangeBounds", []);
    /**
     * cache the bounding of bullets,
     * it will be updated when the doc-skeleton is recalculated
     */
    __publicField(this, "_bulletBounds", []);
    /**
     * cache the bounding of paragraphs,
     * it will be updated when the doc-skeleton is recalculated
     */
    __publicField(this, "_paragraphBounds", /* @__PURE__ */ new Map());
    __publicField(this, "_paragraphLeftBounds", []);
    __publicField(this, "_tableParagraphBounds", /* @__PURE__ */ new Map());
    __publicField(this, "_segmentParagraphBounds", /* @__PURE__ */ new Map());
    __publicField(this, "_tableViewportSignature", "");
    __publicField(this, "_tableCellBounds", /* @__PURE__ */ new Map());
    __publicField(this, "_tableBounds", /* @__PURE__ */ new Map());
    this._initResetDirty();
    this._initEvents();
    this._initPointer();
  }
  get hoverParagraph() {
    return this._hoverParagraph$.value;
  }
  get hoverParagraphLeft() {
    return this._hoverParagraphLeft$.value;
  }
  get _skeleton() {
    return this._docSkeletonManagerService.getSkeleton();
  }
  get _documents() {
    return this._context.mainComponent;
  }
  dispose() {
    this._hoverCustomRanges$.complete();
    this._clickCustomRanges$.complete();
    super.dispose();
  }
  _initPointer() {
    let preCursor = "text" /* TEXT */;
    this.disposeWithMe(this.hoverCustomRanges$.subscribe((ranges) => {
      if (ranges.length) {
        preCursor = this._context.scene.getCursor();
        this._context.scene.setCursor("pointer" /* POINTER */);
      } else {
        this._context.scene.setCursor(preCursor);
      }
    }));
  }
  _initResetDirty() {
    this.disposeWithMe(this._skeleton.dirty$.subscribe(() => {
      this._customRangeDirty = true;
      this._bulletDirty = true;
      this._paragraphDirty = true;
    }));
    this.disposeWithMe(
      fromEventSubject(this._context.engine.onTransformChange$).pipe(
        filter((evt) => evt.type === 1 /* resize */)
      ).subscribe(() => {
        this._customRangeDirty = true;
        this._bulletDirty = true;
        this._paragraphDirty = true;
      })
    );
    this.disposeWithMe(
      fromEventSubject(this._context.scene.onTransformChange$).pipe(
        filter((evt) => evt.type === 2 /* scale */)
      ).subscribe(() => {
        this._customRangeDirty = true;
        this._bulletDirty = true;
        this._paragraphDirty = true;
      })
    );
  }
  _initEvents() {
    this.disposeWithMe(fromEventSubject(this._context.scene.onPointerMove$).pipe(throttleTime(30)).subscribe((evt) => {
      if (evt.buttons > 0) {
        this._hoverBullet$.next(null);
        this._hoverCustomRanges$.next([]);
        this._hoverParagraph$.next(null);
        this._hoverParagraphLeft$.next(null);
        this._hoverTableCell$.next(null);
        this._hoverTable$.next(null);
        return;
      }
      const { x, y } = transformOffset2Bound(evt.offsetX, evt.offsetY, this._context.scene);
      this._hoverCustomRanges$.next(
        this._calcActiveRanges({ x, y })
      );
      this._hoverParagraph$.next(
        this._calcActiveParagraph({ x, y })
      );
      this._hoverParagraphLeft$.next(
        this._calcActiveParagraphLeft({ x, y })
      );
      this._hoverBullet$.next(
        this._calcActiveBullet({ x, y })
      );
    }));
    this.disposeWithMe(this._context.scene.onPointerEnter$.subscribeEvent(() => {
      this._hoverBullet$.next(null);
      this._hoverCustomRanges$.next([]);
    }));
    const onPointerDown$ = fromEventSubject(this._context.mainComponent.onPointerDown$);
    const onPointerUp$ = fromEventSubject(this._context.scene.onPointerUp$);
    this.disposeWithMe(onPointerDown$.pipe(
      switchMap((down) => onPointerUp$.pipe(take(1), map((up) => ({ down, up })))),
      filter(({ down, up }) => down.target === up.target && up.timeStamp - down.timeStamp < 300)
    ).subscribe(({ down }) => {
      const point = transformOffset2Bound(down.offsetX, down.offsetY, this._context.scene);
      if (down.button === 2) {
        const bullet2 = this._calcActiveBullet(point);
        if (bullet2) {
          this._contextMenuBullet$.next({
            ...bullet2,
            x: down.offsetX,
            y: down.offsetY
          });
        }
        return;
      }
      const ranges = this._calcActiveRanges(point);
      if (ranges.length) {
        this._clickCustomRanges$.next(ranges.pop());
      }
      const bullet = this._calcActiveBullet(point);
      if (bullet) {
        this._clickBullet$.next(bullet);
      }
    }));
  }
  isPointerOnBullet(offsetX, offsetY) {
    return Boolean(this._calcActiveBullet(transformOffset2Bound(offsetX, offsetY, this._context.scene)));
  }
  getBulletBounds() {
    this._buildBulletBounds();
    return [...this._bulletBounds];
  }
  _buildCustomRangeBoundsBySegment(segmentId, segmentPage = -1) {
    var _a, _b, _c;
    const customRanges = (_c = (_b = (_a = this._context.unit.getSelfOrHeaderFooterModel(segmentId)) == null ? void 0 : _a.getBody()) == null ? void 0 : _b.customRanges) != null ? _c : [];
    const layouts = [];
    customRanges.forEach((range) => {
      const textRange = {
        startOffset: range.startIndex,
        endOffset: range.endIndex + 1,
        collapsed: false,
        segmentId
      };
      const rects = calcDocRangePositions2(textRange, this._documents, this._skeleton, segmentPage);
      if (!rects) {
        return null;
      }
      layouts.push({
        customRange: range,
        rects,
        segmentId,
        segmentPageIndex: segmentPage
      });
    });
    return layouts;
  }
  _buildCustomRangeBounds() {
    var _a;
    if (!this._customRangeDirty) {
      return;
    }
    this._customRangeDirty = false;
    const customRangeBounds = [];
    customRangeBounds.push(...this._buildCustomRangeBoundsBySegment());
    (_a = this._skeleton.getSkeletonData()) == null ? void 0 : _a.pages.forEach((page, pageIndex) => {
      if (page.headerId) {
        customRangeBounds.push(...this._buildCustomRangeBoundsBySegment(page.headerId, pageIndex));
      }
      if (page.footerId) {
        customRangeBounds.push(...this._buildCustomRangeBoundsBySegment(page.footerId, pageIndex));
      }
    });
    this._customRangeBounds = customRangeBounds;
  }
  _calcActiveRanges(evt) {
    this._buildCustomRangeBounds();
    const { x, y } = evt;
    const matchedRanges = this._customRangeBounds.filter((layout) => {
      return layout.rects.some((rect) => isPointInRect(x, y, rect));
    });
    return matchedRanges.map(
      (range) => ({
        segmentId: range.segmentId,
        range: range.customRange,
        segmentPageIndex: range.segmentPageIndex,
        rects: range.rects
      })
    );
  }
  _buildBulletBoundsBySegment(segmentId, segmentPage = -1) {
    var _a, _b, _c, _d, _e;
    const body = (_a = this._context.unit.getSelfOrHeaderFooterModel(segmentId)) == null ? void 0 : _a.getBody();
    const paragraphs = ((_b = body == null ? void 0 : body.paragraphs) != null ? _b : []).filter((p) => p.bullet);
    const bounds = [];
    const skeletonData = this._skeleton.getSkeletonData();
    if (!skeletonData) {
      return bounds;
    }
    const calc = (pages) => {
      var _a2, _b2, _c2;
      for (const page of pages) {
        const sections = [...page.sections];
        if (page.skeTables) {
          const tables = Array.from(page.skeTables.values());
          sections.push(...tables.map((i) => i.rows.map((i2) => i2.cells.map((i3) => i3.sections))).flat(4));
        }
        for (const selection of sections) {
          for (const column of selection.columns) {
            for (const line of column.lines) {
              if (line.paragraphStart) {
                const paragraph = paragraphs.find((p) => p.startIndex === line.paragraphIndex);
                if (paragraph) {
                  const targetLine = line;
                  const bulletNode = (_c2 = (_b2 = (_a2 = targetLine == null ? void 0 : targetLine.divides) == null ? void 0 : _a2[0]) == null ? void 0 : _b2.glyphGroup) == null ? void 0 : _c2[0];
                  if (!bulletNode) {
                    continue;
                  }
                  const rect = calcDocGlyphPosition(bulletNode, this._documents, this._skeleton, segmentPage);
                  if (!rect) {
                    continue;
                  }
                  bounds.push({
                    rect,
                    segmentId,
                    segmentPageIndex: segmentPage,
                    paragraph
                  });
                }
              }
            }
          }
        }
      }
      return bounds;
    };
    if (segmentId) {
      const page = (_e = (_c = skeletonData.skeFooters.get(segmentId)) == null ? void 0 : _c.values()) != null ? _e : (_d = skeletonData.skeHeaders.get(segmentId)) == null ? void 0 : _d.values();
      if (!page) {
        return bounds;
      }
      return calc(Array.from(page));
    }
    return calc(skeletonData.pages);
  }
  _buildBulletBounds() {
    var _a;
    if (!this._bulletDirty) {
      return;
    }
    this._bulletDirty = false;
    this._bulletBounds = [];
    this._bulletBounds.push(...this._buildBulletBoundsBySegment());
    (_a = this._skeleton.getSkeletonData()) == null ? void 0 : _a.pages.forEach((page, pageIndex) => {
      if (page.headerId) {
        this._bulletBounds.push(...this._buildBulletBoundsBySegment(page.headerId, pageIndex));
      }
      if (page.footerId) {
        this._bulletBounds.push(...this._buildBulletBoundsBySegment(page.footerId, pageIndex));
      }
    });
  }
  _calcActiveBullet(evt) {
    this._buildBulletBounds();
    const { x, y } = evt;
    const bullet = this._bulletBounds.find((layout) => isPointInRect(x, y, layout.rect));
    return bullet;
  }
  // eslint-disable-next-line max-lines-per-function
  _buildParagraphBoundsBySegment(segmentId) {
    var _a, _b, _c;
    const skeletonData = this._skeleton.getSkeletonData();
    const documents = this._documents;
    const documentOffsetConfig = documents.getOffsetConfig();
    if (!skeletonData) {
      return null;
    }
    const calc = (pages) => {
      const paragraphMap = /* @__PURE__ */ new Map();
      const handlePage = (page, pageIndex, top, left) => {
        const bounds = calcDocParagraphPositions(page.sections, top, left, page.pageWidth - page.marginLeft - page.marginRight);
        bounds.forEach((bound) => {
          if (!paragraphMap.has(bound.startIndex)) {
            paragraphMap.set(bound.startIndex, {
              rect: bound.rect,
              paragraphStart: bound.paragraphStart,
              paragraphEnd: bound.paragraphEnd,
              startIndex: bound.startIndex,
              rects: [bound.rect],
              pageIndex,
              segmentId,
              firstLine: bound.fisrtLine
            });
          } else {
            const current = paragraphMap.get(bound.startIndex);
            if (current) {
              current.rect.bottom = bound.rect.bottom;
              current.rects.push(bound.rect);
            }
          }
        });
      };
      for (let i = 0; i < pages.length; i++) {
        const page = pages[i];
        const top = ((page.pageHeight === Infinity ? 0 : page.pageHeight) + documentOffsetConfig.pageMarginTop) * i + page.marginTop + documentOffsetConfig.docsTop;
        const left = page.marginLeft + documentOffsetConfig.docsLeft;
        if (page.skeTables) {
          Array.from(page.skeTables.values()).forEach((table) => {
            const tableLeft = table.left + left;
            const tableTop = table.top + top;
            const sourceTableId = getTableIdAndSliceIndex(table.tableId).tableId;
            const tableViewport = getTableHorizontalViewportGeometry(
              tableLeft,
              table.width,
              getDocsTableRenderViewport(this._context.unitId, sourceTableId)
            );
            const tableRight = tableViewport.visibleRight;
            const tableBottom = tableTop + table.height;
            const tableId = table.tableId;
            this._tableBounds.set(tableId, {
              rect: {
                left: tableLeft,
                top: tableTop,
                right: tableRight,
                bottom: tableBottom
              },
              pageIndex: i,
              tableId
            });
            table.rows.forEach((row, rowIndex) => {
              row.cells.forEach((cell, colIndex) => {
                const top2 = ((page.pageHeight === Infinity ? 0 : page.pageHeight) + documentOffsetConfig.pageMarginTop) * i + table.top + documentOffsetConfig.docsTop + page.marginTop + row.top + cell.marginTop;
                const cellLeft = tableLeft + cell.left - tableViewport.scrollLeft + cell.marginLeft;
                const cellContentWidth = cell.pageWidth - cell.marginLeft - cell.marginRight;
                const cellRight = cellLeft + cellContentWidth;
                const visibleCellLeft = Math.max(cellLeft, tableViewport.visibleLeft);
                const visibleCellRight = Math.min(cellRight, tableViewport.visibleRight);
                if (visibleCellRight <= visibleCellLeft) {
                  return;
                }
                const bounds = calcDocParagraphPositions(cell.sections, top2, cellLeft, cellContentWidth).map((bound) => clipParagraphBoundHorizontally(bound, tableViewport.visibleLeft, tableViewport.visibleRight)).filter((bound) => bound != null);
                let arr = this._tableParagraphBounds.get(tableId);
                if (!arr) {
                  arr = [];
                  this._tableParagraphBounds.set(tableId, arr);
                }
                arr.push(
                  ...bounds.map((bound) => ({
                    rect: bound.rect,
                    paragraphStart: bound.paragraphStart,
                    paragraphEnd: bound.paragraphEnd,
                    startIndex: bound.startIndex,
                    pageIndex: i,
                    segmentId,
                    rowIndex,
                    colIndex,
                    firstLine: bound.fisrtLine,
                    tableId
                  }))
                );
                let cellBounds = this._tableCellBounds.get(tableId);
                if (!cellBounds) {
                  cellBounds = [];
                  this._tableCellBounds.set(tableId, cellBounds);
                }
                cellBounds.push({
                  rect: {
                    top: top2,
                    left: visibleCellLeft,
                    right: visibleCellRight,
                    bottom: top2 + cell.pageHeight - cell.marginBottom - cell.marginTop
                  },
                  pageIndex: i,
                  rowIndex,
                  colIndex,
                  tableId
                });
              });
            });
          });
        }
        handlePage(page, i, top, left);
      }
      return paragraphMap;
    };
    if (segmentId) {
      const page = (_c = (_a = skeletonData.skeFooters.get(segmentId)) == null ? void 0 : _a.values()) != null ? _c : (_b = skeletonData.skeHeaders.get(segmentId)) == null ? void 0 : _b.values();
      if (!page) {
        return null;
      }
      return calc(Array.from(page));
    }
    return calc(skeletonData.pages);
  }
  _buildParagraphBounds() {
    var _a, _b;
    const tableViewportSignature = this._getTableViewportSignature();
    if (!this._paragraphDirty && this._tableViewportSignature === tableViewportSignature) {
      return;
    }
    this._paragraphDirty = false;
    this._tableViewportSignature = tableViewportSignature;
    this._tableParagraphBounds = /* @__PURE__ */ new Map();
    this._tableCellBounds = /* @__PURE__ */ new Map();
    this._tableBounds = /* @__PURE__ */ new Map();
    this._paragraphBounds = (_a = this._buildParagraphBoundsBySegment()) != null ? _a : /* @__PURE__ */ new Map();
    this._paragraphLeftBounds = Array.from(this._paragraphBounds.values()).map((bound) => ({
      ...bound,
      rect: {
        left: bound.rect.left - 60,
        right: bound.rect.left,
        top: bound.rect.top,
        bottom: bound.rect.bottom
      }
    }));
    const handleSegment = (segmentId) => {
      var _a2;
      this._segmentParagraphBounds.set(segmentId, (_a2 = this._buildParagraphBoundsBySegment(segmentId)) != null ? _a2 : /* @__PURE__ */ new Map());
    };
    (_b = this._skeleton.getSkeletonData()) == null ? void 0 : _b.pages.forEach((page) => {
      if (page.headerId) {
        handleSegment(page.headerId);
      }
      if (page.footerId) {
        handleSegment(page.footerId);
      }
    });
  }
  _calcActiveParagraph(evt) {
    var _a, _b;
    this._buildParagraphBounds();
    const { x, y } = evt;
    const table = Array.from(this._tableBounds.values()).find((bound) => isPointInRect(x, y, getTableBlockMenuHoverRect(bound.rect)));
    this._hoverTable$.next(table);
    if (table) {
      const tableCell = (_a = this._tableCellBounds.get(table.tableId)) == null ? void 0 : _a.find((bound) => isPointInRect(x, y, bound.rect));
      this._hoverTableCell$.next(tableCell);
      if (!tableCell) {
        return null;
      }
      const paragraphs = (_b = this._tableParagraphBounds.get(tableCell.tableId)) == null ? void 0 : _b.filter((bound) => bound.colIndex === tableCell.colIndex && bound.rowIndex === tableCell.rowIndex);
      const paragraph2 = paragraphs == null ? void 0 : paragraphs.find((bound) => isPointInRect(x, y, bound.rect));
      return paragraph2 && {
        ...paragraph2,
        rects: [paragraph2.rect]
      };
    }
    let paragraph;
    for (const bounds of this._paragraphBounds) {
      const bound = bounds[1];
      if (bound.rects.some((rect) => isPointInRect(x, y, rect))) {
        paragraph = bound;
        break;
      }
    }
    return paragraph;
  }
  _calcActiveParagraphLeft(evt) {
    this._buildParagraphBounds();
    const { x, y } = evt;
    const paragraph = this._paragraphLeftBounds.find((bound) => isPointInRect(x, y, bound.rect));
    return paragraph;
  }
  get paragraphBounds() {
    this._buildParagraphBounds();
    return this._paragraphBounds;
  }
  get tableBounds() {
    this._buildParagraphBounds();
    return this._tableBounds;
  }
  findParagraphBoundByIndex(index) {
    this._buildParagraphBounds();
    const tableParagraph = Array.from(this._tableParagraphBounds.values()).flat().find((bound) => bound.startIndex === index);
    if (tableParagraph) {
      return tableParagraph;
    }
    const paragraph = this._paragraphBounds.get(index);
    if (paragraph) {
      return paragraph;
    }
  }
  findParagraphBoundsInRange(startIndex, endIndex) {
    this._buildParagraphBounds();
    const bodyBounds = [...this._paragraphBounds.values()];
    const tableBounds = Array.from(this._tableParagraphBounds.values()).flat().map((bound) => ({
      rect: bound.rect,
      paragraphStart: bound.paragraphStart,
      paragraphEnd: bound.paragraphEnd,
      startIndex: bound.startIndex,
      rects: [bound.rect],
      pageIndex: bound.pageIndex,
      segmentId: bound.segmentId,
      firstLine: bound.firstLine
    }));
    return getPreferredParagraphBoundsInRange(bodyBounds, tableBounds, startIndex, endIndex);
  }
  _getTableViewportSignature() {
    var _a, _b;
    const pages = (_b = (_a = this._skeleton.getSkeletonData()) == null ? void 0 : _a.pages) != null ? _b : [];
    const signatures = [];
    pages.forEach((page) => {
      var _a2;
      (_a2 = page.skeTables) == null ? void 0 : _a2.forEach((table) => {
        const tableId = getTableIdAndSliceIndex(table.tableId).tableId;
        const viewport = getDocsTableRenderViewport(this._context.unitId, tableId);
        if (!viewport) {
          signatures.push(`${tableId}:none`);
          return;
        }
        signatures.push(`${tableId}:${viewport.contentWidth}:${viewport.viewportWidth}:${viewport.scrollLeft}`);
      });
    });
    return signatures.join("|");
  }
};
DocEventManagerService = __decorateClass([
  __decorateParam(1, Inject(DocSkeletonManagerService))
], DocEventManagerService);
function getPreferredParagraphBoundsInRange(bodyBounds, tableBounds, startIndex, endIndex) {
  const matchedTableBounds = tableBounds.filter((bound) => isRangeOverlappingParagraph(bound, startIndex, endIndex));
  if (matchedTableBounds.length) {
    return matchedTableBounds;
  }
  return bodyBounds.filter((bound) => isRangeOverlappingParagraph(bound, startIndex, endIndex));
}
function isRangeOverlappingParagraph(bound, startIndex, endIndex) {
  return Math.max(bound.paragraphStart, startIndex) <= Math.min(bound.paragraphEnd, endIndex);
}
function clipParagraphBoundHorizontally(bound, visibleLeft, visibleRight) {
  var _a;
  const rect = clipRectHorizontally(bound.rect, visibleLeft, visibleRight);
  if (!rect) {
    return null;
  }
  return {
    ...bound,
    rect,
    fisrtLine: (_a = clipRectHorizontally(bound.fisrtLine, visibleLeft, visibleRight)) != null ? _a : rect
  };
}
function clipRectHorizontally(rect, visibleLeft, visibleRight) {
  const left = Math.max(rect.left, visibleLeft);
  const right = Math.min(rect.right, visibleRight);
  if (right <= left) {
    return null;
  }
  return {
    ...rect,
    left,
    right
  };
}

// ../packages/docs-ui/src/components/float-toolbar/FloatToolbar.tsx
var import_react9 = __toESM(require_react());
var import_jsx_runtime8 = __toESM(require_jsx_runtime());
var DEFAULT_AVALIABLE_MENUS = [
  FLOAT_TEXT_STYLE_MENU_ID,
  SetInlineFormatFontSizeCommand.id,
  SetInlineFormatBoldCommand.id,
  SetInlineFormatItalicCommand.id,
  SetInlineFormatUnderlineCommand.id,
  SetInlineFormatStrikethroughCommand.id,
  SetInlineFormatSubscriptCommand.id,
  SetInlineFormatSuperscriptCommand.id,
  SetInlineFormatTextColorCommand.id,
  SetInlineFormatTextBackgroundColorCommand.id
];
function FloatToolbar(props) {
  const { avaliableMenus = DEFAULT_AVALIABLE_MENUS } = props;
  const menuManagerService = useDependency(IMenuManagerService);
  const [menus, setMenus] = (0, import_react9.useState)([]);
  (0, import_react9.useEffect)(() => {
    function getRibbon() {
      const flatMenus = [
        ...menuManagerService.getFlatMenuByPositionKey(FLOAT_TOOLBAR_MENU_POSITION),
        ...menuManagerService.getFlatMenuByPositionKey("ribbon" /* RIBBON */)
      ];
      const menus2 = [];
      for (const key of avaliableMenus) {
        const item = flatMenus.find((item2) => item2.key === key);
        if (item) {
          menus2.push(item);
        }
      }
      setMenus(menus2);
    }
    getRibbon();
    const subscription = menuManagerService.menuChanged$.subscribe(getRibbon);
    return () => {
      subscription.unsubscribe();
    };
  }, [menuManagerService]);
  return /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(
    "div",
    {
      className: clsx(`univer-box-border univer-flex univer-rounded univer-bg-white univer-py-1.5 univer-shadow-sm dark:!univer-border-gray-700 dark:!univer-bg-gray-900`, borderClassName),
      children: menus.map((groupItem) => groupItem.item && /* @__PURE__ */ (0, import_jsx_runtime8.jsx)("div", { className: "univer-flex univer-flex-nowrap univer-gap-2 univer-px-2", children: /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(ToolbarItem, { ...groupItem.item }, groupItem.key) }, groupItem.key))
    }
  );
}

// ../packages/docs-ui/src/services/float-menu.service.ts
var FLOAT_MENU_COMPONENT_KEY = "univer.doc.float-menu";
function isInSameLine(startNodePosition, endNodePosition) {
  if (startNodePosition == null || endNodePosition == null) {
    return false;
  }
  const { glyph: _startGlyph, ...startRest } = startNodePosition;
  const { glyph: _endGlyph, ...endRest } = endNodePosition;
  return deepCompare(startRest, endRest);
}
var SKIP_SYMBOLS = [
  "\b" /* CUSTOM_BLOCK */,
  "\r" /* PARAGRAPH */
];
var DocFloatMenuService = class extends Disposable {
  constructor(_context, _docSelectionManagerService, _docCanvasPopManagerService, _componentManager, _univerInstanceService, _docSelectionRenderService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_docSelectionManagerService", _docSelectionManagerService);
    __publicField(this, "_docCanvasPopManagerService", _docCanvasPopManagerService);
    __publicField(this, "_componentManager", _componentManager);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_docSelectionRenderService", _docSelectionRenderService);
    __publicField(this, "_floatMenu", null);
    if (isInternalEditorID(this._context.unitId)) {
      return;
    }
    this._registerFloatMenu();
    this._initSelectionChange();
    this.disposeWithMe(() => {
      this._hideFloatMenu();
    });
  }
  get floatMenu() {
    return this._floatMenu;
  }
  _registerFloatMenu() {
    this.disposeWithMe(this._componentManager.register(FLOAT_MENU_COMPONENT_KEY, FloatToolbar));
  }
  _initSelectionChange() {
    this.disposeWithMe(this._docSelectionRenderService.onSelectionStart$.subscribe(() => {
      this._hideFloatMenu();
    }));
    this.disposeWithMe(this._docSelectionManagerService.textSelection$.subscribe((selection) => {
      var _a, _b;
      const { unitId, textRanges } = selection;
      if (unitId !== this._context.unitId) {
        return;
      }
      const range = textRanges.length > 0 && textRanges.find((range2) => !range2.collapsed);
      if (range) {
        if (range.startOffset === ((_a = this._floatMenu) == null ? void 0 : _a.start) && range.endOffset === ((_b = this._floatMenu) == null ? void 0 : _b.end)) {
          return;
        }
        this._hideFloatMenu();
        this._showFloatMenu(unitId, range);
        return;
      }
      this._hideFloatMenu();
    }));
  }
  _hideFloatMenu() {
    var _a;
    (_a = this._floatMenu) == null ? void 0 : _a.disposable.dispose();
    this._floatMenu = null;
  }
  _showFloatMenu(unitId, range) {
    var _a, _b, _c;
    const documentDataModel = this._univerInstanceService.getUnit(unitId, 1 /* UNIVER_DOC */);
    if (!documentDataModel || documentDataModel.getDisabled()) {
      return;
    }
    if (isRangeInCodeBlock(documentDataModel, range)) {
      return;
    }
    const token = (_a = documentDataModel.getBody()) == null ? void 0 : _a.dataStream[range.startOffset];
    if (range.endOffset - range.startOffset === 1 && token && SKIP_SYMBOLS.includes(token)) {
      return;
    }
    const wholeCustomRanges = (_c = (_b = documentDataModel.getBody()) == null ? void 0 : _b.customRanges) == null ? void 0 : _c.filter((range2) => range2.wholeEntity);
    if (wholeCustomRanges == null ? void 0 : wholeCustomRanges.some((customRange) => customRange.startIndex === range.startOffset && customRange.endIndex === range.endOffset - 1)) {
      return;
    }
    this._floatMenu = {
      disposable: this._docCanvasPopManagerService.attachPopupToRange(
        range,
        {
          componentKey: FLOAT_MENU_COMPONENT_KEY,
          direction: range.direction === "backward" || isInSameLine(range.startNodePosition, range.endNodePosition) ? "top-center" : "bottom-center",
          offset: [0, 4]
        },
        unitId
      ),
      start: range.startOffset,
      end: range.endOffset
    };
    return toDisposable(() => this._hideFloatMenu());
  }
};
DocFloatMenuService = __decorateClass([
  __decorateParam(1, Inject(DocSelectionManagerService)),
  __decorateParam(2, Inject(DocCanvasPopManagerService)),
  __decorateParam(3, Inject(ComponentManager)),
  __decorateParam(4, Inject(IUniverInstanceService)),
  __decorateParam(5, Inject(DocSelectionRenderService))
], DocFloatMenuService);
function isRangeInCodeBlock(documentDataModel, range) {
  var _a, _b;
  const blockRanges = (_b = (_a = documentDataModel.getBody()) == null ? void 0 : _a.blockRanges) != null ? _b : [];
  return blockRanges.some((blockRange) => blockRange.blockType === "code" && Math.max(range.startOffset, blockRange.startIndex) <= Math.min(range.endOffset, blockRange.endIndex));
}

// ../packages/docs-ui/src/services/doc-paragraph-menu.service.ts
var BLOCK_RANGE_ICON_MAP = {
  code: "DocsCodeBlockIcon",
  quote: "DocsQuoteBlockIcon",
  callout: "DocsCalloutBlockIcon"
};
var LIST_ICON_MAP = {
  ["ORDER_LIST" /* ORDER_LIST */]: "OrderIcon",
  ["ORDER_LIST_1" /* ORDER_LIST_1 */]: "OrderIcon",
  ["ORDER_LIST_2" /* ORDER_LIST_2 */]: "OrderIcon",
  ["ORDER_LIST_3" /* ORDER_LIST_3 */]: "OrderIcon",
  ["ORDER_LIST_4" /* ORDER_LIST_4 */]: "OrderIcon",
  ["ORDER_LIST_5" /* ORDER_LIST_5 */]: "OrderIcon",
  ["ORDER_LIST_QUICK_2" /* ORDER_LIST_QUICK_2 */]: "OrderIcon",
  ["ORDER_LIST_QUICK_3" /* ORDER_LIST_QUICK_3 */]: "OrderIcon",
  ["ORDER_LIST_QUICK_4" /* ORDER_LIST_QUICK_4 */]: "OrderIcon",
  ["ORDER_LIST_QUICK_5" /* ORDER_LIST_QUICK_5 */]: "OrderIcon",
  ["ORDER_LIST_QUICK_6" /* ORDER_LIST_QUICK_6 */]: "OrderIcon",
  ["BULLET_LIST" /* BULLET_LIST */]: "UnorderIcon",
  ["BULLET_LIST_1" /* BULLET_LIST_1 */]: "UnorderIcon",
  ["BULLET_LIST_2" /* BULLET_LIST_2 */]: "UnorderIcon",
  ["BULLET_LIST_3" /* BULLET_LIST_3 */]: "UnorderIcon",
  ["BULLET_LIST_4" /* BULLET_LIST_4 */]: "UnorderIcon",
  ["BULLET_LIST_5" /* BULLET_LIST_5 */]: "UnorderIcon",
  ["CHECK_LIST" /* CHECK_LIST */]: "TodoListDoubleIcon",
  ["CHECK_LIST_CHECKED" /* CHECK_LIST_CHECKED */]: "TodoListDoubleIcon"
};
var DocParagraphMenuService = class extends Disposable {
  constructor(_context, _docSelectionManagerService, _docEventManagerService, _docPopupManagerService, _docSkeletonManagerService, _floatMenuService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_docSelectionManagerService", _docSelectionManagerService);
    __publicField(this, "_docEventManagerService", _docEventManagerService);
    __publicField(this, "_docPopupManagerService", _docPopupManagerService);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_floatMenuService", _floatMenuService);
    __publicField(this, "_paragrahMenu", null);
    __publicField(this, "_activeTarget$", new BehaviorSubject(null));
    __publicField(this, "activeTarget$", this._activeTarget$.asObservable());
    __publicField(this, "_isBlockMenuDragging", false);
    if (isInternalEditorID(this._context.unitId)) {
      return;
    }
    this._init();
  }
  get activeParagraph() {
    var _a;
    return (_a = this._paragrahMenu) == null ? void 0 : _a.paragraph;
  }
  get activeTarget() {
    var _a, _b;
    return (_b = (_a = this._paragrahMenu) == null ? void 0 : _a.target) != null ? _b : null;
  }
  setBlockMenuDragging(isDragging) {
    this._isBlockMenuDragging = isDragging;
  }
  get isBlockMenuDragging() {
    return this._isBlockMenuDragging;
  }
  _isCursorInActiveParagraph() {
    if (!this._paragrahMenu) {
      return false;
    }
    const activeTextRange = this._docSelectionManagerService.getActiveTextRange();
    if (!(activeTextRange == null ? void 0 : activeTextRange.collapsed)) {
      return false;
    }
    if (!(activeTextRange == null ? void 0 : activeTextRange.collapsed) || activeTextRange.startOffset < this._paragrahMenu.paragraph.paragraphStart || activeTextRange.startOffset > this._paragrahMenu.paragraph.paragraphEnd) {
      return false;
    }
    return true;
  }
  setParagraphMenuActive(active) {
    if (this._paragrahMenu) {
      this._paragrahMenu.active = active;
      if (!this._isCursorInActiveParagraph()) {
        this._docSelectionManagerService.replaceDocRanges([{
          startOffset: this._paragrahMenu.paragraph.paragraphStart,
          endOffset: this._paragrahMenu.paragraph.paragraphStart
        }]);
      }
    }
  }
  _init() {
    var _a;
    const handleHoverTarget = (paragraph, tableBound) => {
      var _a2;
      if (this._isBlockMenuDragging) {
        return;
      }
      const viewModel = this._docSkeletonManagerService.getViewModel();
      if (viewModel.getEditArea() === "BODY" /* BODY */ && !this._floatMenuService.floatMenu && !this._context.unit.getDisabled()) {
        if ((_a2 = this._paragrahMenu) == null ? void 0 : _a2.active) {
          return;
        }
        if (paragraph) {
          this.showParagraphMenu(paragraph);
          return;
        }
        if (tableBound) {
          this.showTableMenu(tableBound);
          return;
        }
      }
      this.hideParagraphMenu(true);
    };
    this.disposeWithMe(
      combineLatest([
        this._docEventManagerService.hoverParagraphRealTime$,
        this._docEventManagerService.hoverParagraphLeft$,
        (_a = this._docEventManagerService.hoverTableRealTime$) != null ? _a : new BehaviorSubject(null)
      ]).pipe(throttleTime(16)).subscribe(([p, left, table]) => {
        const paragraph = p != null ? p : left;
        handleHoverTarget(paragraph, table);
      })
    );
    let lastScrollY = 0;
    this.disposeWithMe(this._context.scene.getViewport("viewMain" /* VIEW_MAIN */).onScrollAfter$.subscribeEvent((e) => {
      var _a2;
      if (lastScrollY === e.scrollY) {
        return;
      }
      lastScrollY = e.scrollY;
      if ((_a2 = this._paragrahMenu) == null ? void 0 : _a2.blockRange) {
        return;
      }
      if (this._isBlockMenuDragging) {
        return;
      }
      this.hideParagraphMenu(true);
    }));
    this.disposeWithMe(this._docEventManagerService.clickCustomRanges$.subscribe(() => {
      if (this._isBlockMenuDragging) {
        return;
      }
      this.hideParagraphMenu(true);
    }));
  }
  showParagraphMenu(paragraph) {
    var _a, _b;
    const target = this._buildParagraphMenuTarget(paragraph);
    if (!target) {
      this.hideParagraphMenu(true);
      return;
    }
    if (((_a = this._paragrahMenu) == null ? void 0 : _a.target.key) === target.key) {
      return;
    }
    this.hideParagraphMenu(true);
    const targetParagraph = (_b = target.paragraph) != null ? _b : paragraph;
    const blockRange = target.blockRange;
    const getFirstLine = () => {
      var _a2, _b2;
      return blockRange ? getBlockRangeAnchorRect(this._docEventManagerService, blockRange, paragraph) : (_b2 = (_a2 = this._docEventManagerService.findParagraphBoundByIndex(paragraph.startIndex)) == null ? void 0 : _a2.firstLine) != null ? _b2 : paragraph.firstLine;
    };
    const disposable = this._docPopupManagerService.attachPopupToRect(
      getFirstLine,
      {
        componentKey: "doc.paragraph.menu",
        direction: "left-center",
        onClickOutside: () => {
          this._docSelectionManagerService.textSelection$.pipe(first()).subscribe(() => {
            if (!this._isCursorInActiveParagraph()) {
              this.hideParagraphMenu(true);
            }
          });
        },
        zIndex: 101
      },
      this._context.unitId
    );
    this._paragrahMenu = {
      paragraph: targetParagraph,
      target,
      blockRange,
      disposable,
      active: false
    };
    this._activeTarget$.next(target);
  }
  showTableMenu(tableBound) {
    var _a, _b;
    const body = this._context.unit.getBody();
    const table = (_a = body == null ? void 0 : body.tables) == null ? void 0 : _a.find((item) => item.tableId === tableBound.tableId);
    if (!table) {
      return;
    }
    const target = {
      kind: "table",
      key: `table:${table.tableId}`,
      table,
      icon: "GridIcon",
      menuRange: {
        startOffset: table.startIndex,
        endOffset: table.endIndex,
        collapsed: false
      },
      moveRange: {
        startOffset: table.startIndex,
        endOffset: table.endIndex
      },
      emptyMode: false,
      draggable: true
    };
    if (((_b = this._paragrahMenu) == null ? void 0 : _b.target.key) === target.key) {
      return;
    }
    const anchorRect = getTableAnchorRect(tableBound);
    const paragraph = {
      rect: tableBound.rect,
      rects: [tableBound.rect],
      paragraphStart: table.startIndex,
      paragraphEnd: table.endIndex,
      startIndex: table.startIndex,
      pageIndex: tableBound.pageIndex,
      firstLine: anchorRect
    };
    this.hideParagraphMenu(true);
    const getFirstLine = () => {
      var _a2;
      return getTableAnchorRect((_a2 = this._docEventManagerService.tableBounds.get(table.tableId)) != null ? _a2 : tableBound);
    };
    const disposable = this._docPopupManagerService.attachPopupToRect(
      getFirstLine,
      {
        componentKey: "doc.paragraph.menu",
        direction: "top-right",
        onClickOutside: () => {
          this.hideParagraphMenu(true);
        },
        zIndex: 101
      },
      this._context.unitId
    );
    this._paragrahMenu = {
      paragraph,
      target,
      disposable,
      active: false
    };
    this._activeTarget$.next(target);
  }
  getDropTargetFromClientPoint(clientX, clientY, sourceRange) {
    const canvasRect = this._context.engine.getCanvasElement().getBoundingClientRect();
    const point = transformOffset2Bound(clientX - canvasRect.left, clientY - canvasRect.top, this._context.scene);
    const body = this._context.unit.getBody();
    const sourceCellRange = (body == null ? void 0 : body.dataStream) ? getContainingTableCellRange(body.dataStream, sourceRange.startOffset) : null;
    const blocks = (sourceCellRange ? this._getCellBlockTargets(sourceCellRange) : this._getBodyBlockTargets()).filter((block) => block.moveRange.startOffset !== sourceRange.startOffset || block.moveRange.endOffset !== sourceRange.endOffset).filter((block) => block.moveRange.endOffset <= sourceRange.startOffset || block.moveRange.startOffset >= sourceRange.endOffset).sort((left, right) => left.rect.top - right.rect.top || left.rect.left - right.rect.left);
    if (!blocks.length) {
      return null;
    }
    let nearest = blocks[0];
    let nearestDistance = Number.POSITIVE_INFINITY;
    for (const block of blocks) {
      const middle = (block.rect.top + block.rect.bottom) / 2;
      const distance = Math.abs(point.y - middle);
      if (distance < nearestDistance) {
        nearestDistance = distance;
        nearest = block;
      }
    }
    const before = point.y < (nearest.rect.top + nearest.rect.bottom) / 2;
    const targetOffset = before ? nearest.moveRange.startOffset : nearest.moveRange.endOffset;
    const docRect = {
      left: nearest.rect.left,
      right: nearest.rect.right,
      top: before ? nearest.rect.top : nearest.rect.bottom,
      bottom: before ? nearest.rect.top : nearest.rect.bottom
    };
    const screenRect = transformBound2OffsetBound(docRect, this._context.scene);
    return {
      targetOffset,
      rect: {
        left: canvasRect.left + screenRect.left,
        right: canvasRect.left + screenRect.right,
        top: canvasRect.top + screenRect.top,
        bottom: canvasRect.top + screenRect.bottom
      }
    };
  }
  hideParagraphMenu(force = false) {
    if (this._isBlockMenuDragging) {
      return;
    }
    if (this._paragrahMenu && (this._paragrahMenu.disposable.canDispose() || force)) {
      this._paragrahMenu.disposable.dispose();
      this._paragrahMenu = null;
      this._activeTarget$.next(null);
    }
  }
  _buildParagraphMenuTarget(paragraph) {
    var _a, _b, _c, _d;
    const body = this._context.unit.getBody();
    const dataStream = (_a = body == null ? void 0 : body.dataStream) != null ? _a : "";
    const cellRange = getContainingTableCellRange(dataStream, paragraph.paragraphStart);
    const inTable = cellRange != null || isParagraphInTable(this._context.unit, paragraph.paragraphStart);
    const blockRange = getParagraphBlockRange(this._context.unit, paragraph);
    if (blockRange) {
      const targetParagraph = {
        ...paragraph,
        paragraphStart: blockRange.startIndex,
        paragraphEnd: blockRange.endIndex + 1,
        startIndex: blockRange.startIndex,
        blockRange
      };
      return {
        kind: "blockRange",
        key: `blockRange:${blockRange.blockId}`,
        paragraph: targetParagraph,
        blockRange,
        icon: BLOCK_RANGE_ICON_MAP[blockRange.blockType],
        cellRange: cellRange != null ? cellRange : void 0,
        menuRange: {
          startOffset: blockRange.startIndex,
          endOffset: blockRange.endIndex + 1,
          collapsed: false
        },
        moveRange: {
          startOffset: blockRange.startIndex,
          endOffset: blockRange.endIndex + 1
        },
        emptyMode: false,
        draggable: cellRange != null || !inTable
      };
    }
    const paragraphDataStream = dataStream.slice(paragraph.paragraphStart, paragraph.paragraphEnd);
    const paragraphModel = (_b = body == null ? void 0 : body.paragraphs) == null ? void 0 : _b.find((item) => item.startIndex === paragraph.startIndex);
    const listIcon = getListIcon((_c = paragraphModel == null ? void 0 : paragraphModel.bullet) == null ? void 0 : _c.listType);
    const customBlock = (_d = body == null ? void 0 : body.customBlocks) == null ? void 0 : _d.find((item) => item.startIndex >= paragraph.paragraphStart && item.startIndex <= paragraph.paragraphEnd);
    const isCustomBlockOnly = (customBlock == null ? void 0 : customBlock.blockType) === 1 /* CUSTOM */ && paragraphDataStream.replace(/[\b\r\n]/g, "") === "";
    if (customBlock && customBlock.blockType === 1 /* CUSTOM */ && isCustomBlockOnly) {
      return {
        kind: "customBlock",
        key: `customBlock:${customBlock.blockId}`,
        paragraph,
        customBlock,
        icon: "TextTypeIcon",
        cellRange: cellRange != null ? cellRange : void 0,
        menuRange: {
          startOffset: customBlock.startIndex,
          endOffset: customBlock.startIndex,
          collapsed: true
        },
        moveRange: getParagraphMoveRange(this._context.unit, paragraph, cellRange),
        emptyMode: false,
        draggable: cellRange != null || !inTable
      };
    }
    if (paragraphDataStream === "\b") {
      return null;
    }
    return {
      kind: "paragraph",
      key: `paragraph:${paragraph.startIndex}`,
      paragraph,
      icon: listIcon,
      cellRange: cellRange != null ? cellRange : void 0,
      menuRange: {
        startOffset: paragraph.paragraphStart,
        endOffset: paragraph.paragraphStart,
        collapsed: true
      },
      moveRange: getParagraphMoveRange(this._context.unit, paragraph, cellRange),
      emptyMode: paragraphDataStream.replace(/[\r\n]/g, "") === "",
      draggable: cellRange != null || !inTable
    };
  }
  _getBodyBlockTargets() {
    var _a, _b;
    const body = this._context.unit.getBody();
    if (!body) {
      return [];
    }
    const tableRanges = (_a = body.tables) != null ? _a : [];
    const blockRanges = (_b = body.blockRanges) != null ? _b : [];
    const paragraphBounds = [...this._docEventManagerService.paragraphBounds.values()];
    const targets = [];
    for (const table of tableRanges) {
      const tableBound = this._docEventManagerService.tableBounds.get(table.tableId);
      if (!tableBound) {
        continue;
      }
      targets.push({
        kind: "table",
        key: `table:${table.tableId}`,
        table,
        icon: "GridIcon",
        menuRange: { startOffset: table.startIndex, endOffset: table.endIndex, collapsed: false },
        moveRange: { startOffset: table.startIndex, endOffset: table.endIndex },
        emptyMode: false,
        draggable: true,
        rect: tableBound.rect
      });
    }
    for (const blockRange of blockRanges) {
      const bounds = paragraphBounds.filter((bound) => Math.max(bound.paragraphStart, blockRange.startIndex) <= Math.min(bound.paragraphEnd, blockRange.endIndex));
      const rect = unionRects(bounds.map((bound) => bound.rect));
      if (!rect) {
        continue;
      }
      targets.push({
        kind: "blockRange",
        key: `blockRange:${blockRange.blockId}`,
        blockRange,
        icon: BLOCK_RANGE_ICON_MAP[blockRange.blockType],
        menuRange: { startOffset: blockRange.startIndex, endOffset: blockRange.endIndex + 1, collapsed: false },
        moveRange: { startOffset: blockRange.startIndex, endOffset: blockRange.endIndex + 1 },
        emptyMode: false,
        draggable: true,
        rect
      });
    }
    for (const paragraph of paragraphBounds) {
      if (tableRanges.some((table) => paragraph.paragraphStart > table.startIndex && paragraph.paragraphStart < table.endIndex)) {
        continue;
      }
      if (blockRanges.some((range) => Math.max(paragraph.paragraphStart, range.startIndex) <= Math.min(paragraph.paragraphEnd, range.endIndex))) {
        continue;
      }
      const target = this._buildParagraphMenuTarget(paragraph);
      if (!target) {
        continue;
      }
      targets.push({
        ...target,
        rect: paragraph.rect
      });
    }
    return targets;
  }
  _getCellBlockTargets(cellRange) {
    var _a;
    const body = this._context.unit.getBody();
    if (!body) {
      return [];
    }
    const blockRanges = (_a = body.blockRanges) != null ? _a : [];
    const paragraphBounds = [...this._docEventManagerService.paragraphBounds.values()].filter((paragraph) => paragraph.paragraphStart > cellRange.startOffset && paragraph.paragraphStart < cellRange.endOffset);
    const targets = [];
    for (const blockRange of blockRanges) {
      if (blockRange.startIndex <= cellRange.startOffset || blockRange.endIndex >= cellRange.endOffset) {
        continue;
      }
      const bounds = paragraphBounds.filter((bound) => Math.max(bound.paragraphStart, blockRange.startIndex) <= Math.min(bound.paragraphEnd, blockRange.endIndex));
      const rect = unionRects(bounds.map((bound) => bound.rect));
      if (!rect) {
        continue;
      }
      targets.push({
        kind: "blockRange",
        key: `blockRange:${blockRange.blockId}`,
        blockRange,
        icon: BLOCK_RANGE_ICON_MAP[blockRange.blockType],
        cellRange,
        menuRange: { startOffset: blockRange.startIndex, endOffset: blockRange.endIndex + 1, collapsed: false },
        moveRange: { startOffset: blockRange.startIndex, endOffset: blockRange.endIndex + 1 },
        emptyMode: false,
        draggable: true,
        rect
      });
    }
    for (const paragraph of paragraphBounds) {
      if (blockRanges.some((range) => Math.max(paragraph.paragraphStart, range.startIndex) <= Math.min(paragraph.paragraphEnd, range.endIndex))) {
        continue;
      }
      const target = this._buildParagraphMenuTarget(paragraph);
      if (!target) {
        continue;
      }
      targets.push({
        ...target,
        rect: paragraph.rect
      });
    }
    return targets;
  }
};
DocParagraphMenuService = __decorateClass([
  __decorateParam(1, Inject(DocSelectionManagerService)),
  __decorateParam(2, Inject(DocEventManagerService)),
  __decorateParam(3, Inject(DocCanvasPopManagerService)),
  __decorateParam(4, Inject(DocSkeletonManagerService)),
  __decorateParam(5, Inject(DocFloatMenuService))
], DocParagraphMenuService);
function isParagraphInTable(documentDataModel, paragraphStart) {
  var _a, _b;
  const tables = (_b = (_a = documentDataModel.getBody()) == null ? void 0 : _a.tables) != null ? _b : [];
  return tables.some((table) => paragraphStart > table.startIndex && paragraphStart < table.endIndex);
}
function getParagraphMoveRange(documentDataModel, paragraph, cellRange) {
  var _a, _b;
  const paragraphs = [...(_b = (_a = documentDataModel.getBody()) == null ? void 0 : _a.paragraphs) != null ? _b : []].sort((left, right) => left.startIndex - right.startIndex);
  const index = paragraphs.findIndex((item) => item.startIndex === paragraph.startIndex);
  const previousParagraph = index > 0 ? paragraphs.slice(0, index).reverse().find((item) => {
    if (!cellRange) {
      return true;
    }
    return item.startIndex > cellRange.startOffset && item.startIndex < cellRange.endOffset;
  }) : null;
  return {
    startOffset: previousParagraph ? previousParagraph.startIndex + 1 : cellRange ? cellRange.startOffset + 1 : 0,
    endOffset: paragraph.startIndex + 1
  };
}
function getListIcon(listType) {
  if (!listType) {
    return void 0;
  }
  return LIST_ICON_MAP[listType];
}
function getContainingTableCellRange(dataStream, offset) {
  const startOffset = dataStream.lastIndexOf("" /* TABLE_CELL_START */, Math.max(0, offset));
  if (startOffset < 0) {
    return null;
  }
  const previousCellEnd = dataStream.lastIndexOf("" /* TABLE_CELL_END */, Math.max(0, offset));
  if (previousCellEnd > startOffset) {
    return null;
  }
  const endOffset = dataStream.indexOf("" /* TABLE_CELL_END */, startOffset + 1);
  if (endOffset < 0 || offset > endOffset) {
    return null;
  }
  return { startOffset, endOffset };
}
function getTableAnchorRect(tableBound) {
  const gap = 4;
  const left = tableBound.rect.left - gap;
  const top = tableBound.rect.top - gap;
  return {
    left,
    right: left,
    top,
    bottom: top
  };
}
function unionRects(rects) {
  if (!rects.length) {
    return null;
  }
  return rects.reduce((acc, rect) => ({
    left: Math.min(acc.left, rect.left),
    right: Math.max(acc.right, rect.right),
    top: Math.min(acc.top, rect.top),
    bottom: Math.max(acc.bottom, rect.bottom)
  }));
}
function getParagraphBlockRange(documentDataModel, paragraph) {
  var _a, _b;
  const blockRanges = (_b = (_a = documentDataModel.getBody()) == null ? void 0 : _a.blockRanges) != null ? _b : [];
  return blockRanges.find((range) => Math.max(paragraph.paragraphStart, range.startIndex) <= Math.min(paragraph.paragraphEnd, range.endIndex));
}
function getBlockRangeAnchorRect(docEventManagerService, blockRange, fallbackParagraph) {
  var _a, _b;
  const bounds = docEventManagerService.findParagraphBoundsInRange(blockRange.startIndex, blockRange.endIndex).sort((left2, right) => left2.firstLine.top - right.firstLine.top || left2.firstLine.left - right.firstLine.left);
  const anchor = (_b = (_a = bounds[0]) == null ? void 0 : _a.firstLine) != null ? _b : fallbackParagraph.firstLine;
  const left = Math.min(...(bounds.length ? bounds : [fallbackParagraph]).map((bound) => bound.firstLine.left));
  const top = anchor.top;
  return {
    ...anchor,
    left,
    right: left,
    top,
    bottom: top
  };
}

// ../packages/docs-ui/src/components/paragraph-menu/index.tsx
var import_jsx_runtime9 = __toESM(require_jsx_runtime());
function getParagraphMenuIconSizeClass(iconKey) {
  return iconKey === "TextTypeIcon" ? "univer-size-3" : "univer-size-4";
}
function getParagraphMenuPopupDirection(anchorLeft, menuWidth = 212, viewportPadding = 8) {
  return anchorLeft - menuWidth < viewportPadding ? "right" : "left";
}
var PARAGRAPH_MENU_HOVER_OPEN_DELAY = 260;
function createParagraphMenuHoverOpenScheduler(openMenu, delay = PARAGRAPH_MENU_HOVER_OPEN_DELAY) {
  let openTimer = null;
  const cancel = () => {
    if (openTimer != null) {
      window.clearTimeout(openTimer);
      openTimer = null;
    }
  };
  return {
    schedule() {
      cancel();
      openTimer = window.setTimeout(() => {
        openTimer = null;
        openMenu();
      }, delay);
    },
    cancel,
    openNow() {
      cancel();
      openMenu();
    }
  };
}
function isEmptyParagraphMenuTarget(dataStream, paragraph) {
  if (!paragraph) {
    return false;
  }
  return dataStream.slice(paragraph.paragraphStart, paragraph.paragraphEnd).replace(/[\r\n]/g, "") === "";
}
function getParagraphMenuTargetRange(paragraph) {
  if (!paragraph) {
    return null;
  }
  const blockRange = paragraph.blockRange;
  if (blockRange) {
    return {
      startOffset: blockRange.startIndex,
      endOffset: blockRange.endIndex + 1,
      collapsed: false,
      segmentId: paragraph.segmentId
    };
  }
  return {
    startOffset: paragraph.paragraphStart,
    endOffset: paragraph.paragraphStart,
    collapsed: true,
    segmentId: paragraph.segmentId
  };
}
var HEADING_COMMAND_VALUES = {
  [H1HeadingCommand.id]: 4 /* HEADING_1 */,
  [H2HeadingCommand.id]: 5 /* HEADING_2 */,
  [H3HeadingCommand.id]: 6 /* HEADING_3 */,
  [H4HeadingCommand.id]: 7 /* HEADING_4 */,
  [H5HeadingCommand.id]: 8 /* HEADING_5 */,
  [NormalTextHeadingCommand.id]: 1 /* NORMAL_TEXT */,
  [TitleHeadingCommand.id]: 2 /* TITLE */,
  [SubtitleHeadingCommand.id]: 3 /* SUBTITLE */
};
var NAMED_STYLE_HEADING_COMMAND_IDS = {
  [4 /* HEADING_1 */]: H1HeadingCommand.id,
  [5 /* HEADING_2 */]: H2HeadingCommand.id,
  [6 /* HEADING_3 */]: H3HeadingCommand.id,
  [7 /* HEADING_4 */]: H4HeadingCommand.id,
  [8 /* HEADING_5 */]: H5HeadingCommand.id,
  [1 /* NORMAL_TEXT */]: NormalTextHeadingCommand.id,
  [2 /* TITLE */]: TitleHeadingCommand.id,
  [3 /* SUBTITLE */]: SubtitleHeadingCommand.id
};
function getParagraphMenuActiveHeadingCommandId(namedStyleType) {
  var _a;
  return (_a = NAMED_STYLE_HEADING_COMMAND_IDS[namedStyleType != null ? namedStyleType : 1 /* NORMAL_TEXT */]) != null ? _a : NormalTextHeadingCommand.id;
}
function getParagraphMenuHiddenHeadingCommandIds(namedStyleType) {
  if (namedStyleType === 2 /* TITLE */) {
    return [H5HeadingCommand.id, SubtitleHeadingCommand.id];
  }
  if (namedStyleType === 3 /* SUBTITLE */) {
    return [H5HeadingCommand.id, TitleHeadingCommand.id];
  }
  return [TitleHeadingCommand.id, SubtitleHeadingCommand.id];
}
function getParagraphMenuCommand(params, targetRange) {
  var _a, _b;
  const commandId = (_b = (_a = params.commandId) != null ? _a : params.id) != null ? _b : typeof params.label === "string" ? params.label : void 0;
  if (commandId && targetRange && commandId in HEADING_COMMAND_VALUES) {
    return {
      commandId: SetParagraphNamedStyleCommand.id,
      params: {
        value: HEADING_COMMAND_VALUES[commandId],
        textRanges: [targetRange]
      }
    };
  }
  if (commandId && targetRange && (commandId === BulletListCommand.id || commandId === OrderListCommand.id || commandId === CheckListCommand.id)) {
    return {
      commandId,
      params: {
        docRange: [targetRange]
      }
    };
  }
  if (commandId === HorizontalLineCommand.id && targetRange) {
    return {
      commandId,
      params: {
        insertRange: targetRange
      }
    };
  }
  const fallbackParams = typeof params.params === "function" ? params.params() : params.params;
  const commandParams = typeof params.value === "undefined" ? fallbackParams : { value: params.value };
  return {
    commandId,
    params: commandParams && typeof commandParams === "object" ? commandParams : void 0
  };
}
function getParagraphMenuType(target, emptyMode) {
  if ((target == null ? void 0 : target.kind) === "table") {
    return DOC_TABLE_BLOCK_MENU_ID;
  }
  return emptyMode ? EMPTY_PARAGRAPH_MENU_ID : "contextMenu.paragraph" /* PARAGRAPH */;
}
function shouldShowParagraphSettingMenu(target) {
  return !target || target.kind === "paragraph";
}
var ParagraphMenu = ({ popup }) => {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i;
  const [visible, setVisible] = (0, import_react10.useState)(false);
  const [emptyMode, setEmptyMode] = (0, import_react10.useState)(false);
  const [dropRect, setDropRect] = (0, import_react10.useState)(null);
  const [menuDirection, setMenuDirection] = (0, import_react10.useState)("left");
  const contentRef = (0, import_react10.useRef)(null);
  const targetRangeRef = (0, import_react10.useRef)(null);
  const dragTargetOffsetRef = (0, import_react10.useRef)(null);
  const dragRangeRef = (0, import_react10.useRef)(null);
  const isDraggingRef = (0, import_react10.useRef)(false);
  const openMenuRef = (0, import_react10.useRef)(() => void 0);
  const hoverOpenSchedulerRef = (0, import_react10.useRef)(createParagraphMenuHoverOpenScheduler(() => openMenuRef.current()));
  const commandService = useDependency(ICommandService);
  const docSelectionManagerService = useDependency(DocSelectionManagerService);
  const docClipboardService = useDependency(IDocClipboardService);
  const docContentInsertService = useDependency(DocContentInsertService);
  const clipboardInterfaceService = useDependency(IClipboardInterfaceService);
  const layoutService = useDependency(ILayoutService);
  const componentManager = useDependency(ComponentManager);
  const anchorRef = (0, import_react10.useRef)(null);
  const isMouseOver = (0, import_react10.useRef)(false);
  const hideTimerRef = (0, import_react10.useRef)(null);
  const renderManagerService = useDependency(IRenderManagerService);
  const univerInstanceService = useDependency(IUniverInstanceService);
  const renderUnit = renderManagerService.getRenderById(popup.unitId);
  const doc = univerInstanceService.getUnit(popup.unitId, 1 /* UNIVER_DOC */);
  const docParagraphMenuService = renderUnit == null ? void 0 : renderUnit.with(DocParagraphMenuService);
  const docEventManagerService = renderUnit == null ? void 0 : renderUnit.with(DocEventManagerService);
  const activeTarget = useObservable(docParagraphMenuService == null ? void 0 : docParagraphMenuService.activeTarget$);
  const paragraph = useObservable(docEventManagerService == null ? void 0 : docEventManagerService.hoverParagraph$);
  const paragraphLeft = useObservable(docEventManagerService == null ? void 0 : docEventManagerService.hoverParagraphLeft$);
  const currentActiveTarget = activeTarget != null ? activeTarget : docParagraphMenuService == null ? void 0 : docParagraphMenuService.activeTarget;
  const activeParagraphBound = (_c = (_b = (_a = currentActiveTarget == null ? void 0 : currentActiveTarget.paragraph) != null ? _a : docParagraphMenuService == null ? void 0 : docParagraphMenuService.activeParagraph) != null ? _b : paragraph) != null ? _c : paragraphLeft;
  const startIndex = activeParagraphBound == null ? void 0 : activeParagraphBound.startIndex;
  const dataStream = (_e = (_d = doc == null ? void 0 : doc.getBody()) == null ? void 0 : _d.dataStream) != null ? _e : "";
  const paragraphObj = (0, import_react10.useMemo)(() => {
    var _a2, _b2;
    return (_b2 = (_a2 = doc == null ? void 0 : doc.getBody()) == null ? void 0 : _a2.paragraphs) == null ? void 0 : _b2.find((p) => p.startIndex === startIndex);
  }, [doc, startIndex]);
  const isEmptyParagraph = (_f = currentActiveTarget == null ? void 0 : currentActiveTarget.emptyMode) != null ? _f : isEmptyParagraphMenuTarget(dataStream, activeParagraphBound);
  const namedStyleType = (_g = paragraphObj == null ? void 0 : paragraphObj.paragraphStyle) == null ? void 0 : _g.namedStyleType;
  const activeHeadingCommandId = getParagraphMenuActiveHeadingCommandId(namedStyleType);
  const hiddenHeadingCommandIds = (0, import_react10.useMemo)(() => getParagraphMenuHiddenHeadingCommandIds(namedStyleType), [namedStyleType]);
  const hiddenItemIds = (0, import_react10.useMemo)(() => {
    if (!shouldShowParagraphSettingMenu(currentActiveTarget)) {
      return [...hiddenHeadingCommandIds, DocParagraphSettingPanelOperation.id];
    }
    return hiddenHeadingCommandIds;
  }, [currentActiveTarget, hiddenHeadingCommandIds]);
  const icon = HEADING_ICON_MAP[namedStyleType != null ? namedStyleType : 1 /* NORMAL_TEXT */];
  const targetIconKey = (_h = currentActiveTarget == null ? void 0 : currentActiveTarget.icon) != null ? _h : icon.key;
  const TargetIcon = (_i = componentManager.get(targetIconKey)) != null ? _i : icon.component;
  const anchorRect$ = (0, import_react10.useMemo)(() => new BehaviorSubject({
    left: 0,
    right: 0,
    top: 0,
    bottom: 0
  }), []);
  const updateAnchorRect = () => {
    var _a2, _b2, _c2, _d2, _e2;
    const boundingRect = (_a2 = anchorRef.current) == null ? void 0 : _a2.getBoundingClientRect();
    const left = ((_b2 = boundingRect == null ? void 0 : boundingRect.left) != null ? _b2 : 0) - 4;
    setMenuDirection(getParagraphMenuPopupDirection(left));
    anchorRect$.next({
      left,
      right: (_c2 = boundingRect == null ? void 0 : boundingRect.right) != null ? _c2 : 0,
      top: (_d2 = boundingRect == null ? void 0 : boundingRect.top) != null ? _d2 : 0,
      bottom: (_e2 = boundingRect == null ? void 0 : boundingRect.bottom) != null ? _e2 : 0
    });
  };
  const handleHideMenu = () => {
    setVisible(false);
    targetRangeRef.current = null;
  };
  const clearHideTimer = () => {
    if (hideTimerRef.current != null) {
      window.clearTimeout(hideTimerRef.current);
      hideTimerRef.current = null;
    }
  };
  const scheduleHideMenu = () => {
    clearHideTimer();
    hideTimerRef.current = window.setTimeout(() => {
      if (!isMouseOver.current && !isDraggingRef.current) {
        handleHideMenu();
      }
    }, 180);
  };
  const handleOpenMenu = () => {
    var _a2;
    clearHideTimer();
    const latestTarget = (_a2 = docParagraphMenuService == null ? void 0 : docParagraphMenuService.activeTarget) != null ? _a2 : activeTarget;
    const targetRange = latestTarget ? {
      ...latestTarget.menuRange,
      segmentId: activeParagraphBound == null ? void 0 : activeParagraphBound.segmentId
    } : getParagraphMenuTargetRange(activeParagraphBound);
    targetRangeRef.current = targetRange;
    updateAnchorRect();
    setEmptyMode(isEmptyParagraph);
    setVisible(true);
  };
  openMenuRef.current = handleOpenMenu;
  const scheduleOpenMenu = () => {
    clearHideTimer();
    hoverOpenSchedulerRef.current.schedule();
  };
  const cancelOpenMenu = () => {
    hoverOpenSchedulerRef.current.cancel();
  };
  (0, import_react10.useEffect)(() => () => {
    if (hideTimerRef.current != null) {
      window.clearTimeout(hideTimerRef.current);
      hideTimerRef.current = null;
    }
    hoverOpenSchedulerRef.current.cancel();
  }, []);
  return /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(import_jsx_runtime9.Fragment, { children: [
    /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(
      "div",
      {
        "data-u-comp": "paragraph-menu",
        ref: anchorRef,
        className: clsx(`univer-mr-1 univer-inline-flex univer-h-8 univer-cursor-pointer univer-items-center univer-gap-1.5 univer-rounded-lg univer-border univer-border-gray-200 univer-bg-white univer-px-2.5 univer-py-0 univer-shadow-sm univer-transition-colors hover:univer-bg-gray-50 hover:univer-shadow-md dark:!univer-border-gray-700 dark:!univer-bg-gray-900 dark:hover:!univer-bg-gray-800`, {
          "univer-bg-gray-100 univer-shadow-md dark:!univer-bg-gray-800": visible
        }),
        onMouseEnter: (e) => {
          var _a2;
          (_a2 = popup.onPointerEnter) == null ? void 0 : _a2.call(popup, e);
          isMouseOver.current = true;
          scheduleOpenMenu();
        },
        onMouseLeave: () => {
          isMouseOver.current = false;
          cancelOpenMenu();
          scheduleHideMenu();
        },
        onClick: (event) => {
          event.preventDefault();
          event.stopPropagation();
          isMouseOver.current = true;
          hoverOpenSchedulerRef.current.openNow();
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
            TargetIcon,
            {
              className: clsx(
                getParagraphMenuIconSizeClass(targetIconKey),
                `univer-text-gray-700 dark:!univer-text-white`
              )
            }
          ),
          (currentActiveTarget == null ? void 0 : currentActiveTarget.draggable) && /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
            "button",
            {
              type: "button",
              className: `univer-group univer-flex univer-h-4 univer-w-2.5 univer-cursor-grab univer-items-center univer-justify-center univer-border-none univer-bg-transparent univer-p-0 active:univer-cursor-grabbing`,
              "aria-label": "Drag block",
              title: "Drag block",
              onPointerDown: (event) => {
                var _a2, _b2, _c2;
                const latestTarget = (_a2 = docParagraphMenuService == null ? void 0 : docParagraphMenuService.activeTarget) != null ? _a2 : activeTarget;
                const moveRange = latestTarget == null ? void 0 : latestTarget.moveRange;
                if (!moveRange || !(latestTarget == null ? void 0 : latestTarget.draggable)) {
                  return;
                }
                event.preventDefault();
                event.stopPropagation();
                (_c2 = (_b2 = event.currentTarget).setPointerCapture) == null ? void 0 : _c2.call(_b2, event.pointerId);
                clearHideTimer();
                isMouseOver.current = true;
                isDraggingRef.current = true;
                docParagraphMenuService == null ? void 0 : docParagraphMenuService.setBlockMenuDragging(true);
                dragRangeRef.current = moveRange;
                dragTargetOffsetRef.current = null;
                setDropRect(null);
                const pointerId = event.pointerId;
                const handlePointerMove = (moveEvent) => {
                  var _a3, _b3;
                  if (moveEvent.pointerId !== pointerId) {
                    return;
                  }
                  moveEvent.preventDefault();
                  const range = dragRangeRef.current;
                  if (!range) {
                    return;
                  }
                  const target = docParagraphMenuService == null ? void 0 : docParagraphMenuService.getDropTargetFromClientPoint(moveEvent.clientX, moveEvent.clientY, range);
                  dragTargetOffsetRef.current = (_a3 = target == null ? void 0 : target.targetOffset) != null ? _a3 : null;
                  setDropRect((_b3 = target == null ? void 0 : target.rect) != null ? _b3 : null);
                };
                const finishDrag = (shouldDrop) => {
                  window.removeEventListener("pointermove", handlePointerMove);
                  window.removeEventListener("pointerup", handlePointerUp);
                  window.removeEventListener("pointercancel", handlePointerCancel);
                  window.removeEventListener("blur", handleWindowBlur);
                  const range = dragRangeRef.current;
                  const targetOffset = dragTargetOffsetRef.current;
                  dragRangeRef.current = null;
                  dragTargetOffsetRef.current = null;
                  isDraggingRef.current = false;
                  docParagraphMenuService == null ? void 0 : docParagraphMenuService.setBlockMenuDragging(false);
                  setDropRect(null);
                  if (shouldDrop && range && targetOffset != null) {
                    commandService.executeCommand(MoveDocBlockCommand.id, {
                      unitId: popup.unitId,
                      sourceRange: range,
                      targetOffset
                    });
                  }
                };
                const handlePointerUp = (upEvent) => {
                  if (upEvent.pointerId !== pointerId) {
                    return;
                  }
                  upEvent.preventDefault();
                  finishDrag(true);
                };
                const handlePointerCancel = (cancelEvent) => {
                  if (cancelEvent.pointerId !== pointerId) {
                    return;
                  }
                  finishDrag(false);
                };
                const handleWindowBlur = () => {
                  finishDrag(false);
                };
                window.addEventListener("pointermove", handlePointerMove);
                window.addEventListener("pointerup", handlePointerUp);
                window.addEventListener("pointercancel", handlePointerCancel);
                window.addEventListener("blur", handleWindowBlur, { once: true });
              },
              children: /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(DragHandleDotsIcon, {})
            }
          )
        ]
      }
    ),
    visible && /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
      RectPopup,
      {
        portal: true,
        anchorRect$,
        direction: menuDirection,
        children: /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
          "section",
          {
            ref: contentRef,
            onMouseEnter: (e) => {
              var _a2;
              (_a2 = popup.onPointerEnter) == null ? void 0 : _a2.call(popup, e);
              isMouseOver.current = true;
              clearHideTimer();
            },
            onMouseLeave: () => {
              isMouseOver.current = false;
              scheduleHideMenu();
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
              ContextMenuPanel,
              {
                className: "univer-w-[212px]",
                menuType: getParagraphMenuType(currentActiveTarget, emptyMode),
                activeItemIds: [activeHeadingCommandId],
                hiddenItemIds,
                onOptionSelect: async (params) => {
                  var _a2, _b2, _c2, _d2, _e2;
                  const targetRange = (_a2 = targetRangeRef.current) != null ? _a2 : getParagraphMenuTargetRange(activeParagraphBound);
                  const { commandId, params: commandParams } = getParagraphMenuCommand(params, targetRange);
                  const latestTarget = (_b2 = docParagraphMenuService == null ? void 0 : docParagraphMenuService.activeTarget) != null ? _b2 : activeTarget;
                  if (commandId && shouldUseInsertBelowRange(commandId, params) && (latestTarget == null ? void 0 : latestTarget.moveRange)) {
                    docContentInsertService.setInsertRange({
                      unitId: popup.unitId,
                      startOffset: latestTarget.moveRange.endOffset,
                      endOffset: latestTarget.moveRange.endOffset,
                      segmentId: (_c2 = targetRange == null ? void 0 : targetRange.segmentId) != null ? _c2 : ""
                    });
                  }
                  if ((latestTarget == null ? void 0 : latestTarget.kind) === "table" && commandId && targetRange) {
                    const tableRange = {
                      ...targetRange,
                      segmentId: (_d2 = targetRange.segmentId) != null ? _d2 : "",
                      collapsed: false
                    };
                    const afterTableRange = {
                      startOffset: latestTarget.moveRange.endOffset,
                      endOffset: latestTarget.moveRange.endOffset,
                      collapsed: true,
                      segmentId: (_e2 = targetRange.segmentId) != null ? _e2 : ""
                    };
                    if (commandId === DocCopyCommand.id || commandId === DocCopyCommand.name) {
                      await docClipboardService.copy(0 /* copy */, [tableRange]);
                      layoutService.focus();
                      handleHideMenu();
                      return;
                    }
                    if (commandId === DocPasteCommand.id) {
                      docSelectionManagerService.replaceTextRanges([afterTableRange], false);
                      const clipboardItems = await clipboardInterfaceService.read();
                      await docClipboardService.paste(clipboardItems);
                      layoutService.focus();
                      handleHideMenu();
                      return;
                    }
                    if (commandId === DocTableDeleteTableCommand.id) {
                      docSelectionManagerService.replaceTextRanges([tableRange], false);
                    } else if (params.id === INSERT_BELLOW_MENU_ID || commandId !== INSERT_BELLOW_MENU_ID) {
                      docSelectionManagerService.replaceTextRanges([afterTableRange], false);
                    }
                  }
                  if (commandService && commandId) {
                    const blockRangeParams = (latestTarget == null ? void 0 : latestTarget.kind) === "blockRange" && latestTarget.blockRange && commandParams && typeof commandParams === "object" ? {
                      ...commandParams,
                      unitId: popup.unitId,
                      blockId: latestTarget.blockRange.blockId
                    } : commandParams;
                    commandService.executeCommand(commandId, blockRangeParams);
                  }
                  layoutService.focus();
                  handleHideMenu();
                }
              }
            )
          }
        )
      }
    ),
    dropRect && /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
      "div",
      {
        className: "\n                      univer-pointer-events-none univer-fixed univer-z-[10000] univer-h-0.5 univer-rounded-full\n                      univer-bg-primary-600\n                    ",
        style: {
          left: dropRect.left,
          top: dropRect.top,
          width: Math.max(60, dropRect.right - dropRect.left)
        }
      }
    )
  ] });
};
function shouldUseInsertBelowRange(commandId, params) {
  if (params.id === INSERT_BELLOW_MENU_ID) {
    return true;
  }
  const normalized = commandId.toLowerCase();
  if (normalized.includes("insert") && (normalized.includes("below") || normalized.includes("bellow"))) {
    return true;
  }
  if (normalized.includes("insert") && normalized.includes("image")) {
    return true;
  }
  return normalized === "doc.command.create-table" || normalized === "doc.operation.create-table";
}
function DragHandleDotsIcon() {
  return /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
    "span",
    {
      className: `univer-grid univer-h-3.5 univer-w-2 univer-grid-cols-2 univer-place-items-center univer-gap-x-0.5 univer-gap-y-px`,
      "aria-hidden": "true",
      children: Array.from({ length: 6 }).map((_, index) => /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
        "span",
        {
          className: `univer-size-0.5 univer-rounded-full univer-bg-gray-400 univer-transition-colors group-hover:univer-bg-gray-500 dark:!univer-bg-gray-500 dark:group-hover:!univer-bg-gray-300`
        },
        index
      ))
    }
  );
}

// ../packages/docs-ui/src/menu/context-menu.ts
var getDisableOnCollapsedObservable = (accessor) => {
  const docSelectionManagerService = accessor.get(DocSelectionManagerService);
  return new Observable((subscriber) => {
    const observable = docSelectionManagerService.textSelection$.subscribe(() => {
      const ranges = docSelectionManagerService.getDocRanges();
      const legal = ranges.some((range) => range.collapsed === false || range.rangeType === "RECT" /* RECT */);
      if (legal) {
        subscriber.next(false);
      } else {
        subscriber.next(true);
      }
    });
    return () => observable.unsubscribe();
  });
};
function inSameTable(rectRanges) {
  if (rectRanges.length < 2) {
    return true;
  }
  const tableIds = rectRanges.map((rectRange) => rectRange.tableId);
  return tableIds.every((tableId) => tableId === tableIds[0]);
}
function notInTableSubscriber(subscriber, docSelectionManagerService, univerInstanceService) {
  var _a;
  const rectRanges = docSelectionManagerService.getRectRanges();
  const activeRange = docSelectionManagerService.getActiveTextRange();
  if (rectRanges && rectRanges.length && inSameTable(rectRanges) && activeRange == null) {
    subscriber.next(false);
    return;
  }
  if (activeRange && (rectRanges == null || rectRanges.length === 0)) {
    const { segmentId, startOffset, endOffset } = activeRange;
    const docDataModel = univerInstanceService.getCurrentUniverDocInstance();
    const tables = (_a = docDataModel == null ? void 0 : docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody()) == null ? void 0 : _a.tables;
    if (tables && tables.length) {
      if (tables.some((table) => {
        const { startIndex, endIndex } = table;
        return startOffset > startIndex && startOffset < endIndex || endOffset > startIndex && endOffset < endIndex;
      })) {
        subscriber.next(false);
        return;
      }
    }
  }
  subscriber.next(true);
}
var getDisableWhenSelectionNotInTableObservable = (accessor) => {
  const docSelectionManagerService = accessor.get(DocSelectionManagerService);
  const univerInstanceService = accessor.get(IUniverInstanceService);
  return new Observable((subscriber) => {
    const observable = docSelectionManagerService.textSelection$.subscribe(() => {
      notInTableSubscriber(subscriber, docSelectionManagerService, univerInstanceService);
    });
    notInTableSubscriber(subscriber, docSelectionManagerService, univerInstanceService);
    return () => observable.unsubscribe();
  });
};
var CopyMenuFactory = (accessor) => {
  return {
    id: DocCopyCommand.name,
    commandId: DocCopyCommand.id,
    type: 0 /* BUTTON */,
    icon: "CopyDoubleIcon",
    title: "docs-ui.rightClick.copy",
    disabled$: getDisableOnCollapsedObservable(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
};
var ParagraphSettingMenuFactory = (accessor) => {
  return {
    id: DocParagraphSettingPanelOperation.id,
    type: 0 /* BUTTON */,
    icon: "MenuIcon",
    title: "docs-ui.doc.menu.paragraphSetting",
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
};
var CutMenuFactory = (accessor) => {
  return {
    id: DocCutCommand.id,
    type: 0 /* BUTTON */,
    icon: "CopyDoubleIcon",
    title: "docs-ui.rightClick.cut",
    disabled$: getDisableOnCollapsedObservable(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
};
var PasteMenuFactory = (accessor) => {
  return {
    id: DocPasteCommand.id,
    type: 0 /* BUTTON */,
    icon: "PasteSpecialDoubleIcon",
    title: "docs-ui.rightClick.paste",
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
};
var DeleteMenuFactory = (accessor) => {
  return {
    id: DeleteLeftCommand.id,
    type: 0 /* BUTTON */,
    icon: "PasteSpecialDoubleIcon",
    title: "docs-ui.rightClick.delete",
    disabled$: getDisableOnCollapsedObservable(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
};
var TABLE_INSERT_MENU_ID = "doc.menu.table-insert";
function TableInsertMenuItemFactory(accessor) {
  return {
    id: TABLE_INSERT_MENU_ID,
    type: 3 /* SUBITEMS */,
    title: "docs-ui.table.insert",
    icon: "InsertDoubleIcon",
    hidden$: combineLatest(getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */), getDisableWhenSelectionNotInTableObservable(accessor), (one, two) => {
      return one || two;
    })
  };
}
function InsertRowBeforeMenuItemFactory(accessor) {
  return {
    id: DocTableInsertRowAboveCommand.id,
    type: 0 /* BUTTON */,
    title: "docs-ui.table.insertRowAbove",
    icon: "InsertRowAboveDoubleIcon",
    disabled$: getDisableWhenSelectionNotInTableObservable(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function InsertRowAfterMenuItemFactory(accessor) {
  return {
    id: DocTableInsertRowBellowCommand.id,
    type: 0 /* BUTTON */,
    title: "docs-ui.table.insertRowBelow",
    icon: "InsertRowBelowDoubleIcon",
    disabled$: getDisableWhenSelectionNotInTableObservable(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function InsertColumnLeftMenuItemFactory(accessor) {
  return {
    id: DocTableInsertColumnLeftCommand.id,
    type: 0 /* BUTTON */,
    title: "docs-ui.table.insertColumnLeft",
    icon: "LeftInsertColumnDoubleIcon",
    disabled$: getDisableWhenSelectionNotInTableObservable(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function InsertColumnRightMenuItemFactory(accessor) {
  return {
    id: DocTableInsertColumnRightCommand.id,
    type: 0 /* BUTTON */,
    title: "docs-ui.table.insertColumnRight",
    icon: "RightInsertColumnDoubleIcon",
    disabled$: getDisableWhenSelectionNotInTableObservable(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
var TABLE_DELETE_MENU_ID = "doc.menu.table-delete";
function TableDeleteMenuItemFactory(accessor) {
  return {
    id: TABLE_DELETE_MENU_ID,
    type: 3 /* SUBITEMS */,
    title: "docs-ui.table.delete",
    icon: "ReduceDoubleIcon",
    hidden$: combineLatest(getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */), getDisableWhenSelectionNotInTableObservable(accessor), (one, two) => {
      return one || two;
    })
  };
}
function DeleteRowsMenuItemFactory(accessor) {
  return {
    id: DocTableDeleteRowsCommand.id,
    type: 0 /* BUTTON */,
    title: "docs-ui.table.deleteRows",
    icon: "DeleteRowDoubleIcon",
    disabled$: getDisableWhenSelectionNotInTableObservable(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function DeleteColumnsMenuItemFactory(accessor) {
  return {
    id: DocTableDeleteColumnsCommand.id,
    type: 0 /* BUTTON */,
    title: "docs-ui.table.deleteColumns",
    icon: "DeleteColumnDoubleIcon",
    disabled$: getDisableWhenSelectionNotInTableObservable(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}
function DeleteTableMenuItemFactory(accessor) {
  return {
    id: DocTableDeleteTableCommand.id,
    type: 0 /* BUTTON */,
    title: "docs-ui.table.deleteTable",
    icon: "GridIcon",
    disabled$: getDisableWhenSelectionNotInTableObservable(accessor),
    hidden$: getMenuHiddenObservable(accessor, 1 /* UNIVER_DOC */)
  };
}

// ../packages/docs-ui/src/menu/schema.ts
var floatToolbarMenuSchema = {
  [FLOAT_TOOLBAR_MENU_POSITION]: {
    [FLOAT_TEXT_STYLE_MENU_ID]: {
      order: 0,
      menuItemFactory: FloatTextStyleMenuItemFactory
    }
  }
};
var menuSchema = {
  ...floatToolbarMenuSchema,
  ["ribbon.start.format" /* FORMAT */]: {
    [SetInlineFormatBoldCommand.id]: {
      order: 0,
      menuItemFactory: BoldMenuItemFactory
    },
    [SetInlineFormatItalicCommand.id]: {
      order: 1,
      menuItemFactory: ItalicMenuItemFactory
    },
    [SetInlineFormatUnderlineCommand.id]: {
      order: 2,
      menuItemFactory: UnderlineMenuItemFactory
    },
    [SetInlineFormatStrikethroughCommand.id]: {
      order: 3,
      menuItemFactory: StrikeThroughMenuItemFactory
    },
    [SetInlineFormatSubscriptCommand.id]: {
      order: 4,
      menuItemFactory: SubscriptMenuItemFactory
    },
    [SetInlineFormatSuperscriptCommand.id]: {
      order: 5,
      menuItemFactory: SuperscriptMenuItemFactory
    },
    [SetParagraphNamedStyleCommand.id]: {
      order: 5.5,
      menuItemFactory: HeadingSelectorMenuItemFactory
    },
    [SetInlineFormatFontSizeCommand.id]: {
      order: 6,
      menuItemFactory: FontSizeSelectorMenuItemFactory
    },
    [SetInlineFormatFontFamilyCommand.id]: {
      order: 7,
      menuItemFactory: FontFamilySelectorMenuItemFactory
    },
    [SetInlineFormatTextColorCommand.id]: {
      order: 8,
      menuItemFactory: TextColorSelectorMenuItemFactory
    },
    [SetInlineFormatTextBackgroundColorCommand.id]: {
      order: 9,
      menuItemFactory: BackgroundColorSelectorMenuItemFactory,
      [ResetInlineFormatTextBackgroundColorCommand.id]: {
        order: 0,
        menuItemFactory: ResetBackgroundColorMenuItemFactory
      }
    }
  },
  ["ribbon.start.layout" /* LAYOUT */]: {
    [AlignOperationCommand.id]: {
      order: 2,
      menuItemFactory: AlignMenuItemFactory
    },
    [OrderListCommand.id]: {
      order: 7,
      menuItemFactory: OrderListMenuItemFactory
    },
    [BulletListCommand.id]: {
      order: 8,
      menuItemFactory: BulletListMenuItemFactory
    },
    [OpenHeaderFooterPanelCommand.id]: {
      order: 10,
      menuItemFactory: HeaderFooterMenuItemFactory
    },
    [SwitchDocModeCommand.id]: {
      order: 11,
      menuItemFactory: DocSwitchModeMenuItemFactory
    },
    [DocOpenPageSettingCommand.id]: {
      order: 12,
      menuItemFactory: PageSettingMenuItemFactory
    }
  },
  ["ribbon.insert.media" /* MEDIA */]: {
    [TABLE_MENU_ID]: {
      order: 2,
      menuItemFactory: TableMenuFactory,
      [DocCreateTableOperation.id]: {
        order: 0,
        menuItemFactory: InsertTableMenuFactory
      }
    },
    [HorizontalLineCommand.id]: {
      order: 3,
      menuItemFactory: HorizontalLineFactory
    },
    [CheckListCommand.id]: {
      order: 4,
      menuItemFactory: CheckListMenuItemFactory
    }
  },
  ["contextMenu.mainArea" /* MAIN_AREA */]: {
    ["contextMenu.quick" /* QUICK */]: {
      quickLayout: "tile",
      [DocCopyCommand.name]: {
        order: 0,
        menuItemFactory: CopyMenuFactory
      },
      [DocCutCommand.id]: {
        order: 1,
        menuItemFactory: CutMenuFactory
      },
      [DocPasteCommand.id]: {
        order: 2,
        menuItemFactory: PasteMenuFactory
      }
    },
    ["contextMenu.format" /* FORMAT */]: {
      [DeleteLeftCommand.id]: {
        order: 0,
        menuItemFactory: DeleteMenuFactory
      }
    },
    ["contextMenu.layout" /* LAYOUT */]: {
      [DocParagraphSettingPanelOperation.id]: {
        order: 0,
        menuItemFactory: ParagraphSettingMenuFactory
      },
      [TABLE_INSERT_MENU_ID]: {
        order: 1,
        menuItemFactory: TableInsertMenuItemFactory,
        [DocTableInsertRowAboveCommand.id]: {
          order: 1,
          menuItemFactory: InsertRowBeforeMenuItemFactory
        },
        [DocTableInsertRowBellowCommand.id]: {
          order: 2,
          menuItemFactory: InsertRowAfterMenuItemFactory
        },
        [DocTableInsertColumnLeftCommand.id]: {
          order: 3,
          menuItemFactory: InsertColumnLeftMenuItemFactory
        },
        [DocTableInsertColumnRightCommand.id]: {
          order: 4,
          menuItemFactory: InsertColumnRightMenuItemFactory
        }
      },
      [TABLE_DELETE_MENU_ID]: {
        order: 2,
        menuItemFactory: TableDeleteMenuItemFactory,
        [DocTableDeleteRowsCommand.id]: {
          order: 1,
          menuItemFactory: DeleteRowsMenuItemFactory
        },
        [DocTableDeleteColumnsCommand.id]: {
          order: 2,
          menuItemFactory: DeleteColumnsMenuItemFactory
        },
        [DocTableDeleteTableCommand.id]: {
          order: 3,
          menuItemFactory: DeleteTableMenuItemFactory
        }
      }
    }
  },
  ["contextMenu.paragraph" /* PARAGRAPH */]: {
    ["contextMenu.quick" /* QUICK */]: {
      [H1HeadingCommand.id]: {
        order: 0,
        menuItemFactory: H1HeadingMenuItemFactory
      },
      [H2HeadingCommand.id]: {
        order: 1,
        menuItemFactory: H2HeadingMenuItemFactory
      },
      [H3HeadingCommand.id]: {
        order: 2,
        menuItemFactory: H3HeadingMenuItemFactory
      },
      [H4HeadingCommand.id]: {
        order: 3,
        menuItemFactory: H4HeadingMenuItemFactory
      },
      [H5HeadingCommand.id]: {
        order: 4,
        menuItemFactory: H5HeadingMenuItemFactory
      },
      [TitleHeadingCommand.id]: {
        order: -1,
        menuItemFactory: TitleHeadingMenuItemFactory
      },
      [SubtitleHeadingCommand.id]: {
        order: -1,
        menuItemFactory: SubtitleHeadingMenuItemFactory
      },
      [NormalTextHeadingCommand.id]: {
        order: 5,
        menuItemFactory: NormalTextHeadingMenuItemFactory
      }
    },
    ["contextMenu.format" /* FORMAT */]: {
      [DocCopyCurrentParagraphCommand.id]: {
        order: 0,
        menuItemFactory: CopyCurrentParagraphMenuItemFactory
      },
      [DocCutCurrentParagraphCommand.id]: {
        order: 1,
        menuItemFactory: CutCurrentParagraphMenuItemFactory
      },
      [DeleteCurrentParagraphCommand.id]: {
        order: 2,
        menuItemFactory: DeleteCurrentParagraphMenuItemFactory
      }
    },
    ["contextMenu.layout" /* LAYOUT */]: {
      [DocParagraphSettingPanelOperation.id]: {
        order: 0,
        menuItemFactory: ParagraphSettingMenuFactory
      },
      [INSERT_BELLOW_MENU_ID]: {
        order: 1,
        menuItemFactory: DocInsertBellowMenuItemFactory,
        [InsertBulletListBellowCommand.id]: {
          order: 0,
          menuItemFactory: InsertBulletListBellowMenuItemFactory
        },
        [InsertOrderListBellowCommand.id]: {
          order: 1,
          menuItemFactory: InsertOrderListBellowMenuItemFactory
        },
        [InsertCheckListBellowCommand.id]: {
          order: 2,
          menuItemFactory: InsertCheckListBellowMenuItemFactory
        },
        [InsertHorizontalLineBellowCommand.id]: {
          order: 3,
          menuItemFactory: InsertHorizontalLineBellowMenuItemFactory
        },
        [DocCreateTableOperation.id]: {
          order: 4,
          menuItemFactory: InsertDefaultTableMenuFactory
        }
      }
    },
    [DOC_CONTENT_INSERT_MENU_ID]: {
      ["contextMenu.quick" /* QUICK */]: {
        order: -2,
        quickLayout: "icon",
        [H1HeadingCommand.id]: {
          order: 0,
          menuItemFactory: H1HeadingMenuItemFactory
        },
        [H2HeadingCommand.id]: {
          order: 1,
          menuItemFactory: H2HeadingMenuItemFactory
        },
        [H3HeadingCommand.id]: {
          order: 2,
          menuItemFactory: H3HeadingMenuItemFactory
        },
        [H4HeadingCommand.id]: {
          order: 3,
          menuItemFactory: H4HeadingMenuItemFactory
        },
        [H5HeadingCommand.id]: {
          order: 4,
          menuItemFactory: H5HeadingMenuItemFactory
        },
        [NormalTextHeadingCommand.id]: {
          order: 5,
          menuItemFactory: NormalTextHeadingMenuItemFactory
        }
      },
      ["contextMenu.layout" /* LAYOUT */]: {
        order: 1,
        [InsertBulletListBellowCommand.id]: {
          order: 0,
          menuItemFactory: InsertBulletListBellowMenuItemFactory
        },
        [InsertOrderListBellowCommand.id]: {
          order: 1,
          menuItemFactory: InsertOrderListBellowMenuItemFactory
        },
        [InsertCheckListBellowCommand.id]: {
          order: 2,
          menuItemFactory: InsertCheckListBellowMenuItemFactory
        },
        [InsertHorizontalLineBellowCommand.id]: {
          order: 3,
          menuItemFactory: InsertHorizontalLineBellowMenuItemFactory
        },
        [DocCreateTableOperation.id]: {
          order: 4,
          menuItemFactory: InsertDefaultTableMenuFactory
        }
      }
    },
    [EMPTY_PARAGRAPH_MENU_ID]: {
      ["contextMenu.quick" /* QUICK */]: {
        order: -1,
        quickLayout: "icon",
        [H1HeadingCommand.id]: {
          order: 0,
          menuItemFactory: EmptyParagraphH1MenuItemFactory
        },
        [H2HeadingCommand.id]: {
          order: 1,
          menuItemFactory: EmptyParagraphH2MenuItemFactory
        },
        [H3HeadingCommand.id]: {
          order: 2,
          menuItemFactory: EmptyParagraphH3MenuItemFactory
        },
        [H4HeadingCommand.id]: {
          order: 3,
          menuItemFactory: EmptyParagraphH4MenuItemFactory
        },
        [H5HeadingCommand.id]: {
          order: 4,
          menuItemFactory: EmptyParagraphH5MenuItemFactory
        },
        [NormalTextHeadingCommand.id]: {
          order: 5,
          menuItemFactory: EmptyParagraphNormalTextMenuItemFactory
        }
      },
      ["contextMenu.layout" /* LAYOUT */]: {
        order: 1,
        [DocParagraphSettingPanelOperation.id]: {
          order: 0,
          menuItemFactory: ParagraphSettingMenuFactory
        },
        [BulletListCommand.id]: {
          order: 1,
          menuItemFactory: EmptyParagraphBulletListMenuItemFactory
        },
        [OrderListCommand.id]: {
          order: 2,
          menuItemFactory: EmptyParagraphOrderListMenuItemFactory
        },
        [CheckListCommand.id]: {
          order: 3,
          menuItemFactory: EmptyParagraphCheckListMenuItemFactory
        },
        [HorizontalLineCommand.id]: {
          order: 4,
          menuItemFactory: EmptyParagraphHorizontalLineMenuItemFactory
        },
        [DocCreateTableOperation.id]: {
          order: 5,
          menuItemFactory: InsertDefaultTableMenuFactory
        }
      }
    },
    [DOC_TABLE_BLOCK_MENU_ID]: {
      ["contextMenu.format" /* FORMAT */]: {
        [DocCopyCommand.name]: {
          order: 0,
          menuItemFactory: TableBlockCopyMenuItemFactory
        },
        [DocPasteCommand.id]: {
          order: 1,
          menuItemFactory: TableBlockPasteMenuItemFactory
        },
        [DocTableDeleteTableCommand.id]: {
          order: 2,
          menuItemFactory: TableBlockDeleteMenuItemFactory
        }
      },
      ["contextMenu.layout" /* LAYOUT */]: {
        [INSERT_BELLOW_MENU_ID]: {
          order: 0,
          menuItemFactory: DocInsertBellowMenuItemFactory,
          [InsertBulletListBellowCommand.id]: {
            order: 0,
            menuItemFactory: InsertBulletListBellowMenuItemFactory
          },
          [InsertOrderListBellowCommand.id]: {
            order: 1,
            menuItemFactory: InsertOrderListBellowMenuItemFactory
          },
          [InsertCheckListBellowCommand.id]: {
            order: 2,
            menuItemFactory: InsertCheckListBellowMenuItemFactory
          },
          [InsertHorizontalLineBellowCommand.id]: {
            order: 3,
            menuItemFactory: InsertHorizontalLineBellowMenuItemFactory
          },
          [DocCreateTableOperation.id]: {
            order: 4,
            menuItemFactory: InsertDefaultTableMenuFactory
          }
        }
      }
    }
  }
};

// ../packages/docs-ui/src/shortcuts/utils.ts
function whenDocAndEditorFocused(contextService) {
  return contextService.getContextValue(FOCUSING_DOC) && contextService.getContextValue(FOCUSING_UNIVER_EDITOR) && !contextService.getContextValue(FOCUSING_COMMON_DRAWINGS);
}
function whenDocAndEditorFocusedWithBreakLine(contextService) {
  return contextService.getContextValue(FOCUSING_DOC) && contextService.getContextValue(FOCUSING_UNIVER_EDITOR) && !contextService.getContextValue(FOCUSING_COMMON_DRAWINGS);
}

// ../packages/docs-ui/src/shortcuts/format.shortcut.ts
var TabShortCut = {
  id: TabCommand.id,
  binding: 9 /* TAB */,
  preconditions: whenDocAndEditorFocused
};
var ShiftTabShortCut = {
  id: TabCommand.id,
  binding: 9 /* TAB */ | 1024 /* SHIFT */,
  preconditions: whenDocAndEditorFocused,
  staticParameters: {
    shift: true
  }
};

// ../packages/docs-ui/src/shortcuts/toolbar.shortcut.ts
var BoldShortCut = {
  id: SetInlineFormatBoldCommand.id,
  binding: 66 /* B */ | 4096 /* CTRL_COMMAND */,
  preconditions: whenDocAndEditorFocused
};
var ItalicShortCut = {
  id: SetInlineFormatItalicCommand.id,
  binding: 73 /* I */ | 4096 /* CTRL_COMMAND */,
  preconditions: whenDocAndEditorFocused
};
var UnderlineShortCut = {
  id: SetInlineFormatUnderlineCommand.id,
  binding: 85 /* U */ | 4096 /* CTRL_COMMAND */,
  preconditions: whenDocAndEditorFocused
};
var StrikeThroughShortCut = {
  id: SetInlineFormatStrikethroughCommand.id,
  binding: 88 /* X */ | 1024 /* SHIFT */ | 4096 /* CTRL_COMMAND */,
  preconditions: whenDocAndEditorFocused
};
var SubscriptShortCut = {
  id: SetInlineFormatSubscriptCommand.id,
  binding: 188 /* COMMA */ | 4096 /* CTRL_COMMAND */,
  preconditions: whenDocAndEditorFocused
};
var SuperscriptShortCut = {
  id: SetInlineFormatSuperscriptCommand.id,
  binding: 190 /* PERIOD */ | 4096 /* CTRL_COMMAND */,
  preconditions: whenDocAndEditorFocused
};
var AlignLeftShortCut = {
  id: AlignLeftCommand.id,
  binding: 76 /* L */ | 4096 /* CTRL_COMMAND */ | 1024 /* SHIFT */,
  preconditions: whenDocAndEditorFocused
};
var AlignRightShortCut = {
  id: AlignRightCommand.id,
  binding: 82 /* R */ | 4096 /* CTRL_COMMAND */ | 1024 /* SHIFT */,
  preconditions: whenDocAndEditorFocused
};
var AlignCenterShortCut = {
  id: AlignCenterCommand.id,
  binding: 69 /* E */ | 4096 /* CTRL_COMMAND */ | 1024 /* SHIFT */,
  preconditions: whenDocAndEditorFocused
};
var AlignJustifyShortCut = {
  id: AlignJustifyCommand.id,
  binding: 74 /* J */ | 4096 /* CTRL_COMMAND */ | 1024 /* SHIFT */,
  preconditions: whenDocAndEditorFocused
};
var OrderListShortCut = {
  id: OrderListCommand.id,
  binding: 55 /* Digit7 */ | 4096 /* CTRL_COMMAND */ | 1024 /* SHIFT */,
  preconditions: whenDocAndEditorFocused
};
var BulletListShortCut = {
  id: BulletListCommand.id,
  binding: 56 /* Digit8 */ | 4096 /* CTRL_COMMAND */ | 1024 /* SHIFT */,
  preconditions: whenDocAndEditorFocused
};

// ../packages/docs-ui/src/views/count-bar/ZoomSlider.tsx
var import_react11 = __toESM(require_react());
var import_jsx_runtime10 = __toESM(require_jsx_runtime());
var ZOOM_MAP = [50, 80, 100, 130, 150, 170, 200, 400];
var DOC_ZOOM_RANGE = [10, 400];
function ZoomSlider() {
  const commandService = useDependency(ICommandService);
  const univerInstanceService = useDependency(IUniverInstanceService);
  const [documentDataModel, setDocumentDataModel] = (0, import_react11.useState)(null);
  const [zoom, setZoom] = (0, import_react11.useState)(100);
  const getCurrentZoom = (0, import_react11.useCallback)(() => {
    var _a, _b;
    if (!documentDataModel) return 100;
    const currentZoom = ((_b = (_a = documentDataModel.getSettings()) == null ? void 0 : _a.zoomRatio) != null ? _b : 1) * 100;
    return Math.round(currentZoom);
  }, [documentDataModel]);
  (0, import_react11.useEffect)(() => {
    const currentDoc$ = univerInstanceService.getCurrentTypeOfUnit$(1 /* UNIVER_DOC */);
    const subscription = currentDoc$.subscribe((doc) => {
      if (doc) {
        setDocumentDataModel(doc);
        setZoom(getCurrentZoom());
      }
    });
    return () => {
      subscription.unsubscribe();
    };
  }, [univerInstanceService, getCurrentZoom]);
  (0, import_react11.useEffect)(() => {
    const disposable = commandService.onCommandExecuted((commandInfo) => {
      if (commandInfo.id === SetDocZoomRatioOperation.id) {
        const currentZoom = getCurrentZoom();
        setZoom(currentZoom);
      }
    });
    return disposable.dispose;
  }, [commandService, getCurrentZoom]);
  function handleChange(value) {
    setZoom(value);
    if (documentDataModel == null) {
      return;
    }
    const zoomRatio = value / 100;
    commandService.executeCommand(SetDocZoomRatioOperation.id, {
      unitId: documentDataModel.getUnitId(),
      zoomRatio
    });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(
    Slider,
    {
      min: DOC_ZOOM_RANGE[0],
      value: zoom,
      shortcuts: ZOOM_MAP,
      onChange: handleChange
    }
  );
}

// ../packages/docs-ui/src/views/count-bar/CountBar.tsx
var import_jsx_runtime11 = __toESM(require_jsx_runtime());
function CountBar(_props) {
  return /* @__PURE__ */ (0, import_jsx_runtime11.jsx)("section", { className: "univer-flex univer-flex-shrink-0 univer-justify-end", children: /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(ZoomSlider, {}) });
}

// ../packages/docs-ui/src/views/doc-footer/DocFooter.tsx
var import_jsx_runtime12 = __toESM(require_jsx_runtime());
function DocFooterContent() {
  var _a;
  const config = useConfigValue(DOCS_UI_PLUGIN_CONFIG_KEY);
  const isShow = (_a = config == null ? void 0 : config.footer) != null ? _a : true;
  return isShow && /* @__PURE__ */ (0, import_jsx_runtime12.jsxs)(
    "div",
    {
      className: `univer-box-border univer-flex univer-items-center univer-justify-between univer-px-5 univer-py-1.5`,
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime12.jsx)("div", {}),
        /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(CountBar, {})
      ]
    }
  );
}
function DocFooter() {
  const univerInstanceService = useDependency(IUniverInstanceService);
  const workbook = useObservable(() => univerInstanceService.getCurrentTypeOfUnit$(2 /* UNIVER_SHEET */), void 0, void 0, []);
  const slide = useObservable(() => univerInstanceService.getCurrentTypeOfUnit$(3 /* UNIVER_SLIDE */), void 0, void 0, []);
  if (workbook || slide) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(DocFooterContent, {});
}

// ../packages/docs-ui/src/views/side-menu/index.tsx
var import_react13 = __toESM(require_react());

// ../packages/docs-ui/src/components/side-menu/index.tsx
var import_react12 = __toESM(require_react());
var import_jsx_runtime13 = __toESM(require_jsx_runtime());
var commonClass = "univer-font-[500] univer-truncate univer-h-[24px] univer-mb-2 univer-leading-[24px] univer-cursor-pointer univer-pr-1 ";
var titleClass = "univer-text-base univer-font-semibold";
var h1Class = "univer-text-sm univer-font-semibold";
var textClass = "univer-text-sm";
var SideMenu = (0, import_react12.forwardRef)((props, ref) => {
  const { menus, onClick, className, style, mode, maxHeight, activeId, open, onOpenChange, maxWidth, wrapperClass, wrapperStyle, iconClass, iconStyle } = props;
  const isSideBar = mode === "side-bar";
  const containerRef = (0, import_react12.useRef)(null);
  const menusRef = (0, import_react12.useRef)(menus);
  menusRef.current = menus;
  const instance = (0, import_react12.useMemo)(() => ({
    scrollTo: (id) => {
      var _a;
      if (!menusRef.current) return;
      const index = menusRef.current.findIndex((menu) => menu.id === id);
      if (index === -1 || !containerRef.current) return;
      const targetElement = document.getElementById(`univer-side-menu-${id}`);
      if (!targetElement) return;
      const targetTop = targetElement.offsetTop;
      const containerHeight = containerRef.current.clientHeight;
      const maxScrollTop = containerRef.current.scrollHeight - containerHeight;
      const scrollPosition = Math.max(0, Math.min(targetTop - containerHeight / 2 + targetElement.clientHeight / 2, maxScrollTop));
      (_a = containerRef.current) == null ? void 0 : _a.scrollTo({
        behavior: "smooth",
        top: scrollPosition
      });
    }
  }), []);
  (0, import_react12.useImperativeHandle)(ref, () => instance);
  (0, import_react12.useEffect)(() => {
    if (activeId) {
      instance.scrollTo(activeId);
    }
  }, [activeId, instance]);
  return /* @__PURE__ */ (0, import_jsx_runtime13.jsxs)("div", { className: clsx("univer-relative", wrapperClass), style: wrapperStyle, children: [
    /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
      "div",
      {
        onClick: () => onOpenChange == null ? void 0 : onOpenChange(!open),
        className: clsx(`univer-absolute univer-left-5 univer-top-4 univer-z-[100] univer-flex univer-size-8 univer-cursor-pointer univer-items-center univer-justify-center univer-rounded-full univer-bg-white univer-text-gray-800 univer-shadow-sm dark:!univer-bg-gray-600 dark:!univer-text-gray-200`, iconClass),
        style: iconStyle,
        children: open ? /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(LeftIcon, {}) : /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(CatalogueIcon, {})
      }
    ),
    /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
      "div",
      {
        className: clsx(
          className,
          `univer-absolute univer-left-0 univer-top-0 univer-box-border univer-flex univer-min-w-[180px] univer-flex-col univer-px-4 univer-pb-4 univer-pt-14 univer-transition-all univer-duration-300`,
          {
            "univer-rounded-r-2xl univer-bg-white univer-shadow univer-backdrop-blur-[10px] dark:!univer-bg-gray-900": isSideBar
          }
        ),
        style: {
          ...style,
          transform: open ? "translateX(0)" : "translateX(-100%)",
          maxHeight,
          opacity: open ? 1 : 0,
          maxWidth: maxWidth != null ? maxWidth : mode === "side-bar" ? 320 : 180,
          paddingRight: mode === "float" ? void 0 : 0
        },
        children: /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
          "div",
          {
            ref: containerRef,
            className: clsx("univer-flex-1 univer-overflow-y-auto univer-overflow-x-hidden", scrollbarClassName),
            children: menus == null ? void 0 : menus.map((menu) => /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(
              "div",
              {
                id: `univer-side-menu-${menu.id}`,
                className: clsx(
                  commonClass,
                  {
                    [titleClass]: menu.isTitle,
                    [h1Class]: menu.level === 1,
                    [textClass]: menu.level > 1,
                    "univer-text-gray-500 dark:!univer-text-gray-400": menu.id !== activeId,
                    "univer-text-gray-800 dark:!univer-text-gray-200": menu.id === activeId
                  }
                ),
                style: {
                  paddingLeft: (menu.level - 1) * 12
                },
                onClick: () => {
                  onClick == null ? void 0 : onClick(menu);
                },
                children: menu.text
              },
              menu.id
            ))
          }
        )
      }
    )
  ] });
});

// ../packages/docs-ui/src/views/side-menu/index.tsx
var import_jsx_runtime14 = __toESM(require_jsx_runtime());
var transformNamedStyleTypeToLevel = (type2) => {
  switch (type2) {
    case 4 /* HEADING_1 */:
      return 1;
    case 5 /* HEADING_2 */:
      return 2;
    case 6 /* HEADING_3 */:
      return 3;
    case 7 /* HEADING_4 */:
      return 4;
    case 8 /* HEADING_5 */:
      return 5;
    case 2 /* TITLE */:
      return 1;
    default:
      return 1;
  }
};
function findActiveHeading(boundMap, paragraphMap, scrollTop, bottom) {
  var _a, _b;
  if (!boundMap) {
    return void 0;
  }
  const paragraphBounds = Array.from(boundMap.values());
  const paragraphIndex = paragraphBounds.findIndex((p) => p.paragraphStart !== p.paragraphEnd && p.rect.top < bottom && p.rect.bottom > scrollTop);
  if (paragraphIndex === -1) return void 0;
  const lastParagraphIndex = paragraphBounds == null ? void 0 : paragraphBounds.findLastIndex((p) => p.paragraphStart !== p.paragraphEnd && p.rect.top < bottom && p.rect.bottom > scrollTop);
  for (let i = paragraphIndex; i <= lastParagraphIndex; i++) {
    const bound = paragraphBounds[i];
    const paragraph = paragraphMap.get(bound.startIndex);
    if ((_a = paragraph == null ? void 0 : paragraph.paragraphStyle) == null ? void 0 : _a.headingId) {
      return paragraph.paragraphStyle.headingId;
    }
  }
  for (let i = paragraphIndex; i >= 0; i--) {
    const bound = paragraphBounds[i];
    const paragraph = paragraphMap.get(bound.startIndex);
    if ((_b = paragraph == null ? void 0 : paragraph.paragraphStyle) == null ? void 0 : _b.headingId) {
      return paragraph.paragraphStyle.headingId;
    }
  }
  return void 0;
}
var TITLE_ID = "__title";
function DocSideMenu() {
  const config = useConfigValue(DOCS_UI_PLUGIN_CONFIG_KEY);
  if (config == null ? void 0 : config.toc) {
    return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(DocSideMenuContent, {});
  }
  return null;
}
function DocSideMenuContent() {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i;
  const commandService = useDependency(ICommandService);
  const instanceService = useDependency(IUniverInstanceService);
  const currentDoc = useObservable((0, import_react13.useMemo)(() => instanceService.getCurrentTypeOfUnit$(1 /* UNIVER_DOC */), []));
  const renderManagerService = useDependency(IRenderManagerService);
  const fullDataStream = (_b = (_a = currentDoc == null ? void 0 : currentDoc.getBody()) == null ? void 0 : _a.dataStream) != null ? _b : "";
  const [_updateKey, setUpdateKey] = (0, import_react13.useState)(0);
  const [activeId, setActiveId] = (0, import_react13.useState)(void 0);
  const unitId = (_c = currentDoc == null ? void 0 : currentDoc.getUnitId()) != null ? _c : "";
  const renderer = renderManagerService.getRenderById(unitId);
  const title = currentDoc == null ? void 0 : currentDoc.getTitle();
  const docEventManagerService = renderer == null ? void 0 : renderer.with(DocEventManagerService);
  const paragraphBounds = docEventManagerService == null ? void 0 : docEventManagerService.paragraphBounds;
  const left = (_e = (_d = renderer == null ? void 0 : renderer.mainComponent) == null ? void 0 : _d.left) != null ? _e : 0;
  const canvasHeight = (_f = renderer == null ? void 0 : renderer.engine.height) != null ? _f : 0;
  const scaleY = (_g = renderer == null ? void 0 : renderer.scene.scaleY) != null ? _g : 1;
  const paragraphs = (_i = (_h = currentDoc == null ? void 0 : currentDoc.getBody()) == null ? void 0 : _h.paragraphs) != null ? _i : [];
  const paragraphMap = (0, import_react13.useMemo)(() => {
    const map2 = /* @__PURE__ */ new Map();
    paragraphs.forEach((p) => {
      map2.set(p.startIndex, p);
    });
    return map2;
  }, [paragraphs]);
  useObservable((0, import_react13.useMemo)(() => (renderer == null ? void 0 : renderer.engine.onTransformChange$) ? fromEventSubject(renderer == null ? void 0 : renderer.engine.onTransformChange$).pipe(throttleTime(33)) : of(null), [renderer == null ? void 0 : renderer.engine.onTransformChange$]));
  const mode = left < 180 ? "float" : "side-bar";
  let minLevel = Infinity;
  const paragraphMenus = paragraphs == null ? void 0 : paragraphs.filter(
    (p) => {
      var _a2;
      return ((_a2 = p.paragraphStyle) == null ? void 0 : _a2.namedStyleType) !== void 0 && p.paragraphStyle.namedStyleType !== 3 /* SUBTITLE */ && p.paragraphStyle.namedStyleType !== 1 /* NORMAL_TEXT */;
    }
  ).map((p) => {
    var _a2;
    const level = transformNamedStyleTypeToLevel(p.paragraphStyle.namedStyleType);
    minLevel = Math.min(minLevel, level);
    const bound = paragraphBounds == null ? void 0 : paragraphBounds.get(p.startIndex);
    if (!bound) return null;
    const { paragraphStart, paragraphEnd } = bound;
    return {
      id: p.paragraphStyle.headingId,
      text: getPlainText(fullDataStream.slice(paragraphStart, paragraphEnd)),
      level,
      isTitle: ((_a2 = p.paragraphStyle) == null ? void 0 : _a2.namedStyleType) === 2 /* TITLE */
    };
  }).filter((item) => item == null ? void 0 : item.text);
  const handleScroll = useEvent((params) => {
    const scrollTop = params.viewportScrollY;
    const bottom = scrollTop + canvasHeight / scaleY;
    const activeId2 = findActiveHeading(paragraphBounds, paragraphMap, scrollTop, bottom);
    if (activeId2) {
      setActiveId(activeId2);
    }
  });
  const menus = (paragraphMenus == null ? void 0 : paragraphMenus.find((p) => p.isTitle)) ? paragraphMenus : [
    ...title ? [{
      id: TITLE_ID,
      text: title,
      level: 1,
      isTitle: true
    }] : [],
    ...paragraphMenus != null ? paragraphMenus : []
  ].filter(Boolean);
  const [open, setOpen] = (0, import_react13.useState)(true);
  (0, import_react13.useEffect)(() => {
    const debounceUpdater = debounce_default(setUpdateKey, 100);
    const sub = commandService.onCommandExecuted((commandInfo) => {
      if (commandInfo.id === RichTextEditingMutation.id) {
        const params = commandInfo.params;
        if (params.unitId === (currentDoc == null ? void 0 : currentDoc.getUnitId())) {
          debounceUpdater((prev) => prev + 1);
        }
      }
    });
    return () => {
      sub.dispose();
    };
  }, [commandService, currentDoc]);
  (0, import_react13.useEffect)(() => {
    const viewport = renderer == null ? void 0 : renderer.scene.getViewport("viewMain" /* VIEW_MAIN */);
    if (!viewport) {
      return;
    }
    const sub = fromEventSubject(viewport.onScrollAfter$).pipe(throttleTime(33)).subscribe(handleScroll);
    return () => {
      sub.unsubscribe();
    };
  }, [renderer]);
  const handleClick = useEvent((menu) => {
    const viewport = renderer == null ? void 0 : renderer.scene.getViewport("viewMain" /* VIEW_MAIN */);
    if (!viewport) {
      return;
    }
    if (menu.id === TITLE_ID) {
      viewport.scrollToViewportPos({ viewportScrollY: 0 });
      return;
    }
    const paragraph = paragraphs.find((p) => {
      var _a2;
      return ((_a2 = p.paragraphStyle) == null ? void 0 : _a2.headingId) === menu.id;
    });
    if (!paragraph) {
      return;
    }
    const bound = paragraphBounds == null ? void 0 : paragraphBounds.get(paragraph.startIndex);
    if (!bound) {
      return;
    }
    viewport.scrollToViewportPos({ viewportScrollY: bound.rect.top });
    setActiveId(menu.id);
  });
  if (!currentDoc || isInternalEditorID(unitId) || !(menus == null ? void 0 : menus.length)) {
    return null;
  }
  return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
    "div",
    {
      className: "univer-absolute univer-bottom-0 univer-left-0 univer-top-0 univer-z-[100] univer-w-[0px]",
      children: /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(
        SideMenu,
        {
          menus,
          open,
          onOpenChange: setOpen,
          mode,
          maxWidth: mode === "float" ? void 0 : Math.floor(left) - 10,
          wrapperClass: "univer-mt-12",
          activeId,
          onClick: handleClick,
          maxHeight: canvasHeight - 48
        }
      )
    }
  );
}

// ../packages/docs-ui/src/controllers/doc-ui.controller.ts
var DocUIController = class extends Disposable {
  constructor(_injector, _componentManager, _commandService, _layoutService, _menuManagerService, _uiPartsService, _univerInstanceService, _shortcutService, _configService) {
    super();
    __publicField(this, "_injector", _injector);
    __publicField(this, "_componentManager", _componentManager);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_layoutService", _layoutService);
    __publicField(this, "_menuManagerService", _menuManagerService);
    __publicField(this, "_uiPartsService", _uiPartsService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_shortcutService", _shortcutService);
    __publicField(this, "_configService", _configService);
    this._init();
  }
  _initCustomComponents() {
    [
      [BULLET_LIST_TYPE_COMPONENT, BulletListTypePicker],
      [ORDER_LIST_TYPE_COMPONENT, OrderListTypePicker],
      ["TodoListDoubleIcon", TodoListDoubleIcon],
      ["doc.paragraph.menu", ParagraphMenu],
      ["DeleteIcon", DeleteIcon],
      [PAGE_SETTING_COMPONENT_ID, PageSettings],
      ["DocumentSettingIcon", DocSettingIcon]
    ].forEach(([key, comp]) => {
      this.disposeWithMe(
        this._componentManager.register(key, comp)
      );
    });
  }
  _initUiParts() {
    this.disposeWithMe(this._uiPartsService.registerComponent("footer" /* FOOTER */, () => connectInjector(DocFooter, this._injector)));
    this.disposeWithMe(this._uiPartsService.registerComponent("content" /* CONTENT */, () => connectInjector(DocSideMenu, this._injector)));
  }
  _initMenus() {
    this._menuManagerService.appendRootMenu(floatToolbarMenuSchema);
    this._menuManagerService.mergeMenu(menuSchema);
  }
  _initShortCut() {
    [
      BoldShortCut,
      ItalicShortCut,
      UnderlineShortCut,
      StrikeThroughShortCut,
      SubscriptShortCut,
      SuperscriptShortCut,
      AlignCenterShortCut,
      AlignJustifyShortCut,
      AlignRightShortCut,
      AlignLeftShortCut,
      OrderListShortCut,
      BulletListShortCut,
      TabShortCut
    ].forEach((shortcut) => {
      this.disposeWithMe(this._shortcutService.registerShortcut(shortcut));
    });
  }
  _init() {
    this._initCustomComponents();
    this._initMenus();
    this._initFocusHandler();
    this._initCommands();
    this._initUiParts();
    this._initShortCut();
  }
  _initCommands() {
    [
      CoreHeaderFooterCommand,
      OpenHeaderFooterPanelCommand,
      SidebarDocHeaderFooterPanelOperation
    ].forEach((command) => this.disposeWithMe(this._commandService.registerCommand(command)));
  }
  _initFocusHandler() {
    this.disposeWithMe(
      this._layoutService.registerFocusHandler(1 /* UNIVER_DOC */, (unitId) => {
        const renderManagerService = this._injector.get(IRenderManagerService);
        const docSelectionRenderService = renderManagerService.getRenderById(unitId).with(DocSelectionRenderService);
        docSelectionRenderService.focus();
      })
    );
  }
};
DocUIController = __decorateClass([
  __decorateParam(0, Inject(Injector)),
  __decorateParam(1, Inject(ComponentManager)),
  __decorateParam(2, ICommandService),
  __decorateParam(3, ILayoutService),
  __decorateParam(4, IMenuManagerService),
  __decorateParam(5, IUIPartsService),
  __decorateParam(6, IUniverInstanceService),
  __decorateParam(7, IShortcutService),
  __decorateParam(8, IConfigService)
], DocUIController);

// ../packages/docs-ui/src/controllers/render-controllers/doc-checklist.render-controller.ts
var DocChecklistRenderController = class extends Disposable {
  constructor(_context, _docSkeletonManagerService, _commandService, _docEventManagerService, _textSelectionManagerService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_docEventManagerService", _docEventManagerService);
    __publicField(this, "_textSelectionManagerService", _textSelectionManagerService);
    this._initPointerDownObserver();
    this._initHoverCursor();
  }
  _initPointerDownObserver() {
    this._docEventManagerService.clickBullets$.subscribe((paragraph) => {
      const textRanges = this._textSelectionManagerService.getTextRanges();
      this._commandService.executeCommand(ToggleCheckListCommand.id, {
        index: paragraph.paragraph.startIndex,
        segmentId: paragraph.segmentId,
        textRanges
      });
    });
  }
  _initHoverCursor() {
    this.disposeWithMe(
      this._docEventManagerService.hoverBullet$.subscribe((paragraph) => {
        if (paragraph) {
          this._context.mainComponent.setCursor("pointer" /* POINTER */);
        } else {
          this._context.mainComponent.setCursor("text" /* TEXT */);
        }
      })
    );
  }
  _getTransformCoordForDocumentOffset(document2, viewport, evtOffsetX, evtOffsetY) {
    const { documentTransform } = document2.getOffsetConfig();
    const originCoord = viewport.transformVector2SceneCoord(Vector2.FromArray([evtOffsetX, evtOffsetY]));
    if (!originCoord) {
      return;
    }
    return documentTransform.clone().invert().applyPoint(originCoord);
  }
};
DocChecklistRenderController = __decorateClass([
  __decorateParam(1, Inject(DocSkeletonManagerService)),
  __decorateParam(2, ICommandService),
  __decorateParam(3, Inject(DocEventManagerService)),
  __decorateParam(4, Inject(DocSelectionManagerService))
], DocChecklistRenderController);

// ../packages/docs-ui/src/controllers/render-controllers/doc-clipboard.controller.ts
var DocClipboardController = class extends RxDisposable {
  constructor(_context, _commandService, _docClipboardService, _docSelectionRenderService, _contextService, _editorService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_docClipboardService", _docClipboardService);
    __publicField(this, "_docSelectionRenderService", _docSelectionRenderService);
    __publicField(this, "_contextService", _contextService);
    __publicField(this, "_editorService", _editorService);
    this._init();
  }
  _init() {
    this._initLegacyPasteCommand();
  }
  _initLegacyPasteCommand() {
    var _a;
    (_a = this._docSelectionRenderService) == null ? void 0 : _a.onPaste$.pipe(takeUntil(this.dispose$)).subscribe((config) => {
      var _a2, _b, _c, _d;
      if (!whenDocOrEditor(this._contextService)) {
        return;
      }
      config.event.preventDefault();
      const clipboardEvent = config.event;
      let htmlContent = (_a2 = clipboardEvent.clipboardData) == null ? void 0 : _a2.getData(HTML_CLIPBOARD_MIME_TYPE);
      const internalJson = (_b = clipboardEvent.clipboardData) == null ? void 0 : _b.getData(DOC_INTERNAL_FRAGMENT_MIME);
      const textContent = (_c = clipboardEvent.clipboardData) == null ? void 0 : _c.getData(PLAIN_TEXT_CLIPBOARD_MIME_TYPE);
      const files = [...((_d = clipboardEvent.clipboardData) == null ? void 0 : _d.items) || []].filter((item) => imageMimeTypeSet.has(item.type)).map((item) => item.getAsFile()).filter((e) => !!e);
      const editor = this._editorService.getEditor(this._context.unitId);
      if (!!editor && (htmlContent != null ? htmlContent : "").indexOf("</table>") > -1) {
        htmlContent = "";
      }
      this._docClipboardService.legacyPaste({ html: htmlContent, internalJson, text: textContent, files });
    });
  }
};
DocClipboardController = __decorateClass([
  __decorateParam(1, ICommandService),
  __decorateParam(2, IDocClipboardService),
  __decorateParam(3, Inject(DocSelectionRenderService)),
  __decorateParam(4, IContextService),
  __decorateParam(5, IEditorService)
], DocClipboardController);

// ../packages/docs-ui/src/controllers/render-controllers/doc-contextmenu.render-controller.ts
var SKIP_UNIT_IDS = [
  DEFAULT_EMPTY_DOCUMENT_VALUE,
  DOCS_FORMULA_BAR_EDITOR_UNIT_ID_KEY,
  DOCS_NORMAL_EDITOR_UNIT_ID_KEY,
  DOCS_ZEN_EDITOR_UNIT_ID_KEY
];
var DocContextMenuRenderController = class extends Disposable {
  constructor(_context, _contextMenuService, _commandService, _docEventManagerService, _docSelectionManagerService, _univerInstanceService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_contextMenuService", _contextMenuService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_docEventManagerService", _docEventManagerService);
    __publicField(this, "_docSelectionManagerService", _docSelectionManagerService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    if (!SKIP_UNIT_IDS.includes(this._context.unitId)) {
      this._initPointerDown();
      this._initEditChange();
    }
  }
  _initPointerDown() {
    var _a, _b;
    const documentsPointerDownObserver = (_b = (_a = this._context) == null ? void 0 : _a.mainComponent) == null ? void 0 : _b.onPointerDown$;
    const documentsSubscription = documentsPointerDownObserver.subscribeEvent((event) => {
      if (event.button === 2) {
        if (this._docEventManagerService.isPointerOnBullet(event.offsetX, event.offsetY)) {
          return;
        }
        if (this._isSelectionInCodeBlock()) {
          return;
        }
        this._contextMenuService.triggerContextMenu(event, "contextMenu.mainArea" /* MAIN_AREA */);
      }
    });
    this.disposeWithMe(documentsSubscription);
  }
  _initEditChange() {
    this.disposeWithMe(
      this._commandService.onCommandExecuted((command) => {
        if (command.id === RichTextEditingMutation.id) {
          if (this._contextMenuService.visible) {
            this._contextMenuService.hideContextMenu();
          }
        }
      })
    );
  }
  _isSelectionInCodeBlock() {
    var _a, _b, _c, _d;
    const documentDataModel = this._univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
    const blockRanges = (_c = (_b = (_a = documentDataModel == null ? void 0 : documentDataModel.getBody()) == null ? void 0 : _a.blockRanges) == null ? void 0 : _b.filter((range) => range.blockType === "code")) != null ? _c : [];
    const textRanges = (_d = this._docSelectionManagerService.getTextRanges()) != null ? _d : [];
    if (!blockRanges.length || !textRanges.length) {
      return false;
    }
    return blockRanges.some((blockRange) => textRanges.some((range) => isRangeIntersectingBlock(range, blockRange.startIndex, blockRange.endIndex)));
  }
};
DocContextMenuRenderController = __decorateClass([
  __decorateParam(1, IContextMenuService),
  __decorateParam(2, ICommandService),
  __decorateParam(3, Inject(DocEventManagerService)),
  __decorateParam(4, Inject(DocSelectionManagerService)),
  __decorateParam(5, Inject(IUniverInstanceService))
], DocContextMenuRenderController);
function isRangeIntersectingBlock(range, startIndex, endIndex) {
  return Math.max(range.startOffset, startIndex) <= Math.min(range.endOffset, endIndex);
}

// ../packages/docs-ui/src/controllers/render-controllers/doc-editor-bridge.controller.ts
var DocEditorBridgeController = class extends Disposable {
  constructor(_context, _univerInstanceService, _editorService, _commandService, _docSelectionRenderService, _docSkeletonManagerService, _renderManagerService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_editorService", _editorService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_docSelectionRenderService", _docSelectionRenderService);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_renderManagerService", _renderManagerService);
    __publicField(this, "_initialEditors", /* @__PURE__ */ new Set());
    this._initialize();
  }
  _initialize() {
    this._editorService.getAllEditor().forEach((editor) => {
      const unitId = editor.getEditorId();
      if (unitId !== this._context.unitId) {
        return;
      }
      if (this._initialEditors.has(unitId)) {
        return;
      }
      this._initialEditors.add(unitId);
      this._resize(unitId);
    });
    this._commandExecutedListener();
    this._initialBlur();
    this._initialFocus();
  }
  _resize(unitId) {
    if (unitId == null) {
      return;
    }
    const editor = this._editorService.getEditor(unitId);
    if ((editor == null ? void 0 : editor.cancelDefaultResizeListener) === true) {
      return;
    }
    const editorDataModel = this._univerInstanceService.getUniverDocInstance(unitId);
    if (!editorDataModel) {
      return;
    }
    const skeleton = this._docSkeletonManagerService.getSkeleton();
    if (editor == null || editor.render == null || skeleton == null || editorDataModel == null) {
      return;
    }
    skeleton.calculate();
    const { marginTop = 0, marginBottom = 0, marginLeft = 0, marginRight = 0 } = editorDataModel.getSnapshot().documentStyle;
    const { scene, mainComponent } = editor.render;
    let { actualHeight, actualWidth } = skeleton.getActualSize();
    actualHeight += marginTop + marginBottom;
    actualWidth += marginLeft + marginRight;
    const { width, height } = editor.getBoundingClientRect();
    const contentWidth = Math.max(actualWidth, width);
    const contentHeight = Math.max(actualHeight, height);
    scene.transformByState({
      width: contentWidth,
      height: contentHeight
    });
    mainComponent == null ? void 0 : mainComponent.resize(contentWidth, contentHeight);
  }
  _initialBlur() {
    this.disposeWithMe(
      this._editorService.blur$.subscribe(() => {
        this._docSelectionRenderService.blur();
      })
    );
    this.disposeWithMe(
      this._docSelectionRenderService.onBlur$.subscribe(() => {
        const { unitId } = this._context;
        const editor = this._editorService.getEditor(unitId);
        const focusEditor = this._editorService.getFocusEditor();
        if (editor == null || editor.isSheetEditor() || focusEditor && focusEditor.getEditorId() === unitId) {
          return;
        }
        if (unitId.includes("range_selector") || unitId.includes("embedding_formula_editor")) {
          return;
        }
        this._editorService.blur();
      })
    );
  }
  _initialFocus() {
    const focusExcepts = [
      "univer-formula-search",
      "univer-formula-help",
      "formula-help-decorator",
      "univer-formula-help-param"
    ];
    this.disposeWithMe(
      fromEvent(window, "mousedown").subscribe((event) => {
        const target = event.target;
        const hasSearch = target.classList[0] || "";
        if (checkForSubstrings(hasSearch, focusExcepts)) {
          event.stopPropagation();
        }
      })
    );
    const disposableCollection = new DisposableCollection();
    this.disposeWithMe(
      this._univerInstanceService.getCurrentTypeOfUnit$(2 /* UNIVER_SHEET */).subscribe((unit) => {
        disposableCollection.dispose();
        if (!unit) {
          return;
        }
        const unitId = unit.getUnitId();
        const render = this._renderManagerService.getRenderById(unitId);
        const canvasEle = render == null ? void 0 : render.engine.getCanvas().getCanvasEle();
        if (canvasEle == null) {
          return;
        }
        const disposable = fromEvent(canvasEle, "mousedown").subscribe((evt) => {
          evt.stopPropagation();
        });
        disposableCollection.add(disposable);
      })
    );
    this.disposeWithMe(() => disposableCollection.dispose());
  }
  /**
   * Listen to document edits to refresh the size of the formula editor.
   */
  _commandExecutedListener() {
    const updateCommandList = [RichTextEditingMutation.id];
    this.disposeWithMe(
      this._commandService.onCommandExecuted((command) => {
        if (updateCommandList.includes(command.id)) {
          const params = command.params;
          const { unitId } = params;
          if (this._editorService.isSheetEditor(unitId) || unitId !== this._context.unitId) {
            return;
          }
          const editor = this._editorService.getEditor(unitId);
          if (editor && !editor.params.scrollBar) {
            this._resize(unitId);
          }
        }
      })
    );
  }
};
DocEditorBridgeController = __decorateClass([
  __decorateParam(1, IUniverInstanceService),
  __decorateParam(2, IEditorService),
  __decorateParam(3, ICommandService),
  __decorateParam(4, Inject(DocSelectionRenderService)),
  __decorateParam(5, Inject(DocSkeletonManagerService)),
  __decorateParam(6, IRenderManagerService)
], DocEditorBridgeController);

// ../packages/docs-ui/src/controllers/render-controllers/doc-ime-input.controller.ts
var DocIMEInputController = class extends Disposable {
  constructor(_context, _docSelectionRenderService, _docImeInputManagerService, _docSkeletonManagerService, _docStateEmitService, _commandService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_docSelectionRenderService", _docSelectionRenderService);
    __publicField(this, "_docImeInputManagerService", _docImeInputManagerService);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_docStateEmitService", _docStateEmitService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_previousIMEContent", "");
    __publicField(this, "_isCompositionStart", true);
    __publicField(this, "_onStartSubscription");
    __publicField(this, "_onUpdateSubscription");
    __publicField(this, "_onEndSubscription");
    this._initialize();
  }
  dispose() {
    var _a, _b, _c;
    (_a = this._onStartSubscription) == null ? void 0 : _a.unsubscribe();
    (_b = this._onUpdateSubscription) == null ? void 0 : _b.unsubscribe();
    (_c = this._onEndSubscription) == null ? void 0 : _c.unsubscribe();
  }
  _initialize() {
    this._initialOnCompositionstart();
    this._initialOnCompositionUpdate();
    this._initialOnCompositionend();
  }
  _initialOnCompositionstart() {
    this._onStartSubscription = this._docSelectionRenderService.onCompositionstart$.subscribe((config) => {
      if (config == null) {
        return;
      }
      this._resetIME();
      const { activeRange } = config;
      if (activeRange == null) {
        return;
      }
      this._docImeInputManagerService.setActiveRange(Tools.deepClone(activeRange));
    });
  }
  _initialOnCompositionUpdate() {
    this._onUpdateSubscription = this._docSelectionRenderService.onCompositionupdate$.subscribe((config) => {
      this._updateContent(config, true);
    });
  }
  _initialOnCompositionend() {
    this._onEndSubscription = this._docSelectionRenderService.onCompositionend$.subscribe((config) => {
      this._updateContent(config, false);
    });
  }
  async _updateContent(config, isUpdate) {
    if (config == null) {
      return;
    }
    const unitId = this._context.unitId;
    const skeleton = this._docSkeletonManagerService.getSkeleton();
    const { event, activeRange } = config;
    if (skeleton == null || activeRange == null) {
      return;
    }
    const e = event;
    const content = e.data;
    if (content === this._previousIMEContent && isUpdate) {
      return;
    }
    if (!isUpdate && content === this._previousIMEContent) {
      this._finalizeComposition();
      this._resetIME();
      return;
    }
    await this._commandService.executeCommand(IMEInputCommand.id, {
      unitId,
      newText: content,
      oldTextLen: this._previousIMEContent.length,
      isCompositionStart: this._isCompositionStart,
      isCompositionEnd: !isUpdate
    });
    if (isUpdate) {
      if (this._isCompositionStart) {
        this._isCompositionStart = false;
      }
      this._previousIMEContent = content;
    } else {
      this._resetIME();
    }
  }
  // When the composition is canceled, we need to emit the state change info to update the selection and other states.
  _finalizeComposition() {
    const previousActiveRange = this._docImeInputManagerService.getActiveRange();
    if (previousActiveRange == null) {
      return;
    }
    this._docStateEmitService.emitStateChangeInfo({
      commandId: RichTextEditingMutation.id,
      unitId: this._context.unitId,
      segmentId: previousActiveRange.segmentId,
      trigger: IMEInputCommand.id,
      redoState: {
        actions: [],
        textRanges: [previousActiveRange]
      },
      undoState: {
        actions: [],
        textRanges: [previousActiveRange]
      },
      isCompositionEnd: true
    });
  }
  _resetIME() {
    this._previousIMEContent = "";
    this._isCompositionStart = true;
    this._docImeInputManagerService.clearUndoRedoMutationParamsCache();
    this._docImeInputManagerService.setActiveRange(null);
  }
};
DocIMEInputController = __decorateClass([
  __decorateParam(1, Inject(DocSelectionRenderService)),
  __decorateParam(2, Inject(DocIMEInputManagerService)),
  __decorateParam(3, Inject(DocSkeletonManagerService)),
  __decorateParam(4, Inject(DocStateEmitService)),
  __decorateParam(5, ICommandService)
], DocIMEInputController);

// ../packages/docs-ui/src/controllers/render-controllers/doc-input.controller.ts
var DocInputController = class extends Disposable {
  constructor(_context, _docSelectionRenderService, _docSkeletonManagerService, _commandService, _docMenuStyleService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_docSelectionRenderService", _docSelectionRenderService);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_docMenuStyleService", _docMenuStyleService);
    __publicField(this, "_onInputSubscription");
    this._init();
  }
  dispose() {
    var _a;
    super.dispose();
    (_a = this._onInputSubscription) == null ? void 0 : _a.unsubscribe();
  }
  _init() {
    this._initialNormalInput();
  }
  _initialNormalInput() {
    this._onInputSubscription = this._docSelectionRenderService.onInput$.subscribe(async (config) => {
      var _a, _b;
      if (config == null) {
        return;
      }
      const { unitId } = this._context;
      const { event, content = "", activeRange } = config;
      const e = event;
      const skeleton = this._docSkeletonManagerService.getSkeleton();
      if (e.data == null || skeleton == null || activeRange == null) {
        return;
      }
      const { segmentId } = activeRange;
      const docDataModel = this._context.unit;
      const originBody = docDataModel.getSelfOrHeaderFooterModel(segmentId).getBody();
      const defaultTextStyle = this._docMenuStyleService.getDefaultStyle();
      const cacheStyle = this._docMenuStyleService.getStyleCache();
      const curCustomRange = getCustomRangeAtPosition((_a = originBody == null ? void 0 : originBody.customRanges) != null ? _a : [], activeRange.endOffset, SHEET_EDITOR_UNITS.includes(unitId));
      const curTextRun = getTextRunAtPosition(originBody, activeRange.endOffset, defaultTextStyle, cacheStyle, SHEET_EDITOR_UNITS.includes(unitId));
      const curCustomDecorations = getCustomDecorationAtPosition((_b = originBody == null ? void 0 : originBody.customDecorations) != null ? _b : [], activeRange.endOffset);
      await this._commandService.executeCommand(InsertCommand.id, {
        unitId,
        body: {
          dataStream: content,
          textRuns: curTextRun ? [
            {
              ...curTextRun,
              st: 0,
              ed: content.length
            }
          ] : [],
          customRanges: curCustomRange ? [{
            ...curCustomRange,
            startIndex: 0,
            endIndex: content.length - 1
          }] : [],
          customDecorations: curCustomDecorations.map((customDecoration) => ({
            ...customDecoration,
            startIndex: 0,
            endIndex: content.length - 1
          }))
        },
        range: activeRange,
        segmentId
      });
      if (content === " ") {
        await this._commandService.executeCommand(AfterSpaceCommand.id);
      }
    });
  }
};
DocInputController = __decorateClass([
  __decorateParam(1, Inject(DocSelectionRenderService)),
  __decorateParam(2, Inject(DocSkeletonManagerService)),
  __decorateParam(3, ICommandService),
  __decorateParam(4, Inject(DocMenuStyleService))
], DocInputController);

// ../packages/docs-ui/src/services/doc-page-layout.service.ts
var DocPageLayoutService = class extends Disposable {
  constructor(_context) {
    super();
    __publicField(this, "_context", _context);
  }
  calculatePagePosition() {
    var _a, _b;
    if (this._disposed) return;
    const docObject = neoGetDocObject(this._context);
    const docDataModel = this._context.unit;
    const zoomRatio = (_b = (_a = docDataModel.getSettings()) == null ? void 0 : _a.zoomRatio) != null ? _b : 1;
    const { document: docsComponent, scene, docBackground } = docObject;
    const parent = scene == null ? void 0 : scene.getParent();
    const { width: docsWidth, height: docsHeight, pageMarginLeft, pageMarginTop } = docsComponent;
    if (parent == null || docsWidth === Number.POSITIVE_INFINITY || docsHeight === Number.POSITIVE_INFINITY) {
      return;
    }
    const { width: engineWidth, height: engineHeight } = parent;
    let docsLeft = 0;
    let docsTop = pageMarginTop;
    let sceneWidth = 0;
    let sceneHeight = 0;
    let scrollToX = Number.POSITIVE_INFINITY;
    if (engineWidth > (docsWidth + pageMarginLeft * 2) * zoomRatio) {
      docsLeft = engineWidth / 2 - docsWidth * zoomRatio / 2;
      docsLeft /= zoomRatio;
      sceneWidth = (engineWidth - pageMarginLeft * 2) / zoomRatio;
      scrollToX = 0;
    } else {
      docsLeft = pageMarginLeft;
      sceneWidth = docsWidth + pageMarginLeft * 2;
      scrollToX = (sceneWidth - engineWidth / zoomRatio) / 2;
    }
    if (engineHeight > docsHeight) {
      sceneHeight = (engineHeight - pageMarginTop * 2) / zoomRatio;
    } else {
      sceneHeight = docsHeight + pageMarginTop * 2;
    }
    scene.resize(sceneWidth, sceneHeight);
    if (engineWidth <= 1) {
      docsLeft = -1e4;
      docsTop = -1e4;
    }
    docsComponent.translate(docsLeft, docsTop);
    docBackground.translate(docsLeft, docsTop);
    const viewport = scene.getViewport("viewMain" /* VIEW_MAIN */);
    if (scrollToX !== Number.POSITIVE_INFINITY && viewport != null) {
      viewport.scrollToViewportPos({
        viewportScrollX: scrollToX
      });
    }
    return this;
  }
};

// ../packages/docs-ui/src/controllers/render-controllers/doc-resize.render-controller.ts
var DocResizeRenderController = class extends Disposable {
  constructor(_context, _docPageLayoutService, _textSelectionManagerService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_docPageLayoutService", _docPageLayoutService);
    __publicField(this, "_textSelectionManagerService", _textSelectionManagerService);
    const unitId = this._context.unitId;
    if (isInternalEditorID(unitId) && unitId !== DOCS_ZEN_EDITOR_UNIT_ID_KEY) return this;
    this._initResize();
  }
  _initResize() {
    this.disposeWithMe(
      fromEventSubject(this._context.engine.onTransformChange$).pipe(
        filter((evt) => evt.type === 1 /* resize */),
        throttleTime(0, animationFrameScheduler)
      ).subscribe(() => {
        if (this._disposed) {
          return;
        }
        this._docPageLayoutService.calculatePagePosition();
        this._textSelectionManagerService.refreshSelection();
      })
    );
  }
};
DocResizeRenderController = __decorateClass([
  __decorateParam(1, Inject(DocPageLayoutService)),
  __decorateParam(2, Inject(DocSelectionManagerService))
], DocResizeRenderController);

// ../packages/docs-ui/src/controllers/render-controllers/doc-selection-render.controller.ts
var DocSelectionRenderController = class extends Disposable {
  constructor(_context, _commandService, _editorService, _instanceSrv, _docSelectionRenderService, _docSkeletonManagerService, _docSelectionManagerService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_editorService", _editorService);
    __publicField(this, "_instanceSrv", _instanceSrv);
    __publicField(this, "_docSelectionRenderService", _docSelectionRenderService);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_docSelectionManagerService", _docSelectionManagerService);
    __publicField(this, "_loadedMap", /* @__PURE__ */ new WeakSet());
    this._initialize();
  }
  _initialize() {
    this._init();
    this._skeletonListener();
    this._commandExecutedListener();
    this._refreshListener();
    this._syncSelection();
  }
  _init() {
    const { unitId } = this._context;
    const docObject = neoGetDocObject(this._context);
    if (docObject == null || docObject.document == null) {
      return;
    }
    if (!this._loadedMap.has(docObject.document)) {
      this._initialMain(unitId);
      this._loadedMap.add(docObject.document);
    }
  }
  _refreshListener() {
    this.disposeWithMe(
      this._docSelectionManagerService.refreshSelection$.subscribe((params) => {
        if (params == null) {
          return;
        }
        const { unitId, docRanges, isEditing, options } = params;
        if (unitId !== this._context.unitId) {
          return;
        }
        this._docSelectionRenderService.removeAllRanges();
        this._docSelectionRenderService.addDocRanges(docRanges, isEditing, options);
      })
    );
  }
  _syncSelection() {
    this.disposeWithMe(
      this._docSelectionRenderService.textSelectionInner$.subscribe((params) => {
        if (params == null) {
          return;
        }
        this._docSelectionManagerService.__replaceTextRangesWithNoRefresh(params, {
          unitId: this._context.unitId,
          subUnitId: this._context.unitId
        });
      })
    );
  }
  // eslint-disable-next-line max-lines-per-function
  _initialMain(unitId) {
    const docObject = neoGetDocObject(this._context);
    const { document: document2, scene } = docObject;
    this.disposeWithMe(document2.onPointerEnter$.subscribeEvent(() => {
      if (this._isEditorReadOnly(unitId)) {
        return;
      }
      document2.cursor = "text" /* TEXT */;
    }));
    this.disposeWithMe(document2.onPointerLeave$.subscribeEvent(() => {
      document2.cursor = "default" /* DEFAULT */;
      scene.resetCursor();
    }));
    this.disposeWithMe(document2.onPointerDown$.subscribeEvent((evt, state) => {
      if (this._isEditorReadOnly(unitId)) {
        return;
      }
      const docDataModel = this._instanceSrv.getCurrentUnitOfType(1 /* UNIVER_DOC */);
      if ((docDataModel == null ? void 0 : docDataModel.getUnitId()) !== unitId) {
        this._instanceSrv.setCurrentUnitForType(unitId);
      }
      const skeleton = this._docSkeletonManagerService.getSkeleton();
      const { offsetX, offsetY } = evt;
      const coord = this._getTransformCoordForDocumentOffset(offsetX, offsetY);
      if (coord != null) {
        const {
          pageLayoutType = 0 /* VERTICAL */,
          pageMarginLeft,
          pageMarginTop
        } = document2.getOffsetConfig();
        const { editArea } = skeleton.findEditAreaByCoord(
          coord,
          pageLayoutType,
          pageMarginLeft,
          pageMarginTop
        );
        const viewModel = this._docSkeletonManagerService.getViewModel();
        const preEditArea = viewModel.getEditArea();
        if (preEditArea !== "BODY" /* BODY */ && editArea !== "BODY" /* BODY */ && editArea !== preEditArea) {
          viewModel.setEditArea(editArea);
        }
      }
      this._docSelectionRenderService.__onPointDown(evt);
      if (this._editorService.getEditor(unitId)) {
        this._setEditorFocus(unitId);
        const { offsetX: offsetX2, offsetY: offsetY2 } = evt;
        setTimeout(() => {
          if (unitId === this._editorService.getFocusId() || this._docSelectionRenderService.isOnPointerEvent) {
            return;
          }
          this._setEditorFocus(unitId);
          this._docSelectionRenderService.setCursorManually(offsetX2, offsetY2);
        }, 0);
      }
      if (evt.button !== 2) {
        state.stopPropagation();
      }
    }));
    this.disposeWithMe(document2.onDblclick$.subscribeEvent((evt) => {
      if (this._isEditorReadOnly(unitId)) {
        return;
      }
      this._docSelectionRenderService.__handleDblClick(evt);
    }));
    this.disposeWithMe(document2.onTripleClick$.subscribeEvent((evt) => {
      if (this._isEditorReadOnly(unitId)) {
        return;
      }
      this._docSelectionRenderService.__handleTripleClick(evt);
    }));
  }
  _getTransformCoordForDocumentOffset(evtOffsetX, evtOffsetY) {
    const docObject = neoGetDocObject(this._context);
    const { document: document2, scene } = docObject;
    const { documentTransform } = document2.getOffsetConfig();
    const activeViewport = scene.getViewports()[0];
    if (activeViewport == null) {
      return;
    }
    const originCoord = activeViewport.transformVector2SceneCoord(Vector2.FromArray([evtOffsetX, evtOffsetY]));
    return documentTransform.clone().invert().applyPoint(originCoord);
  }
  _isEditorReadOnly(unitId) {
    const editor = this._editorService.getEditor(unitId);
    if (!editor) {
      return false;
    }
    return editor.isReadOnly();
  }
  _setEditorFocus(unitId) {
    this._editorService.focus(unitId);
  }
  _commandExecutedListener() {
    const updateCommandList = [SetDocZoomRatioOperation.id];
    this.disposeWithMe(
      this._commandService.onCommandExecuted((command) => {
        var _a;
        if (updateCommandList.includes(command.id)) {
          const params = command.params;
          const { unitId: documentId } = params;
          const unitId = (_a = this._docSelectionManagerService.__getCurrentSelection()) == null ? void 0 : _a.unitId;
          if (documentId !== unitId) {
            return;
          }
          this._docSelectionManagerService.refreshSelection();
        }
      })
    );
  }
  _skeletonListener() {
    this.disposeWithMe(this._docSkeletonManagerService.currentSkeleton$.subscribe((skeleton) => {
      if (!skeleton) return;
      const { unitId } = this._context;
      const isInternalEditor = isInternalEditorID(unitId);
      if (!isInternalEditor) {
        this._docSelectionRenderService.focus();
        const docDataModel = this._context.unit;
        const snapshot = docDataModel.getSnapshot();
        const offset = findFirstCursorOffset(snapshot);
        this._docSelectionManagerService.replaceDocRanges([
          {
            startOffset: offset,
            endOffset: offset
          }
        ], {
          unitId,
          subUnitId: unitId
        }, false);
      }
    }));
  }
};
DocSelectionRenderController = __decorateClass([
  __decorateParam(1, ICommandService),
  __decorateParam(2, IEditorService),
  __decorateParam(3, IUniverInstanceService),
  __decorateParam(4, Inject(DocSelectionRenderService)),
  __decorateParam(5, Inject(DocSkeletonManagerService)),
  __decorateParam(6, Inject(DocSelectionManagerService))
], DocSelectionRenderController);

// ../packages/docs-ui/src/services/docs-render.service.ts
var DOC_MAIN_CANVAS_ID = "univer-doc-main-canvas";
var DOC_TRADITIONAL_WORKSPACE_BACKGROUND_COLOR = "#fafafa";
var DOC_MODERN_WORKSPACE_BACKGROUND_COLOR = "#fff";
function getDocsCanvasBackgroundColor(documentFlavor) {
  return documentFlavor === 2 /* MODERN */ ? DOC_MODERN_WORKSPACE_BACKGROUND_COLOR : DOC_TRADITIONAL_WORKSPACE_BACKGROUND_COLOR;
}
var DocsRenderService = class extends RxDisposable {
  constructor(_instanceSrv, _renderManagerService) {
    super();
    __publicField(this, "_instanceSrv", _instanceSrv);
    __publicField(this, "_renderManagerService", _renderManagerService);
    this._init();
  }
  _init() {
    this._renderManagerService.createRender$.pipe(takeUntil(this.dispose$)).subscribe((unitId) => this._createRenderWithId(unitId));
    this._instanceSrv.getAllUnitsForType(1 /* UNIVER_DOC */).forEach((documentModel) => this._createRenderer(documentModel));
    this._instanceSrv.getTypeOfUnitAdded$(1 /* UNIVER_DOC */).pipe(takeUntil(this.dispose$)).subscribe((event) => this._createRenderer(event.unit));
    this._instanceSrv.getTypeOfUnitDisposed$(1 /* UNIVER_DOC */).pipe(takeUntil(this.dispose$)).subscribe((doc) => this._disposeRenderer(doc));
  }
  _createRenderer(doc) {
    const unitId = doc.getUnitId();
    this._renderManagerService.created$.subscribe((renderer) => {
      if (renderer.unitId === unitId) {
        const documentFlavor = doc.getSnapshot().documentStyle.documentFlavor;
        const canvas = renderer.engine.getCanvas();
        canvas.setId(DOC_MAIN_CANVAS_ID);
        canvas.getContext().setId(DOC_MAIN_CANVAS_ID);
        canvas.getCanvasEle().style.backgroundColor = getDocsCanvasBackgroundColor(documentFlavor);
      }
    });
    if (!this._renderManagerService.has(unitId)) {
      this._createRenderWithId(unitId);
    }
  }
  _createRenderWithId(unitId) {
    this._renderManagerService.createRender(unitId);
  }
  _disposeRenderer(doc) {
    const unitId = doc.getUnitId();
    this._renderManagerService.removeRender(unitId);
  }
};
DocsRenderService = __decorateClass([
  __decorateParam(0, IUniverInstanceService),
  __decorateParam(1, IRenderManagerService)
], DocsRenderService);

// ../packages/docs-ui/src/controllers/render-controllers/doc.render-controller.ts
var DocRenderController = class extends RxDisposable {
  constructor(_context, _commandService, _docSelectionRenderService, _docSkeletonManagerService, _editorService, _renderManagerService, _univerInstanceService, _docPageLayoutService, _textSelectionManagerService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_docSelectionRenderService", _docSelectionRenderService);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_editorService", _editorService);
    __publicField(this, "_renderManagerService", _renderManagerService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_docPageLayoutService", _docPageLayoutService);
    __publicField(this, "_textSelectionManagerService", _textSelectionManagerService);
    this._addNewRender();
    this._initRenderRefresh();
    this._initCommandListener();
  }
  reRender(unitId) {
    var _a, _b;
    const docSkeletonManagerService = (_a = this._renderManagerService.getRenderById(unitId)) == null ? void 0 : _a.with(DocSkeletonManagerService);
    const skeleton = docSkeletonManagerService == null ? void 0 : docSkeletonManagerService.getSkeleton();
    if (!skeleton) {
      return;
    }
    const disabled = !!skeleton.getViewModel().getDataModel().getSnapshot().disabled;
    if (disabled) {
      return;
    }
    skeleton.calculate();
    const editor = this._editorService.getEditor(unitId);
    if (this._editorService.isEditor(unitId) && !(editor == null ? void 0 : editor.params.scrollBar)) {
      (_b = this._context.mainComponent) == null ? void 0 : _b.makeDirty();
      return;
    }
    this._recalculateSizeBySkeleton(skeleton);
    this._refreshPagePositionAndSelection();
  }
  _addNewRender() {
    const { scene, engine } = this._context;
    const viewMain = new Viewport("viewMain" /* VIEW_MAIN */, scene, {
      left: 0,
      top: 0,
      bottom: 0,
      right: 0,
      isWheelPreventDefaultX: true
    });
    scene.attachControl();
    scene.onMouseWheel$.subscribeEvent((evt, state) => {
      const currentDocUnit = this._univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
      if ((currentDocUnit == null ? void 0 : currentDocUnit.getUnitId()) !== this._context.unitId) {
        return;
      }
      const e = evt;
      if (e.ctrlKey) {
        const deltaFactor = Math.abs(e.deltaX);
        let scrollNum = deltaFactor < 40 ? 0.2 : deltaFactor < 80 ? 0.4 : 0.2;
        scrollNum *= e.deltaY > 0 ? -1 : 1;
        if (scene.scaleX < 1) {
          scrollNum /= 2;
        }
        if (scene.scaleX + scrollNum > 4) {
          scene.scale(4, 4);
        } else if (scene.scaleX + scrollNum < 0.1) {
          scene.scale(0.1, 0.1);
        } else {
          e.preventDefault();
        }
      } else {
        viewMain.onMouseWheel(e, state);
      }
    });
    new ScrollBar(viewMain);
    scene.addLayer(
      new Layer(scene, [], DOCS_COMPONENT_MAIN_LAYER_INDEX),
      new Layer(scene, [], DOCS_COMPONENT_HEADER_LAYER_INDEX)
    );
    this._addComponent();
    const frameFn = () => scene.render();
    this.disposeWithMe(this._context.activated$.subscribe((activated) => {
      if (activated) {
        engine.runRenderLoop(frameFn);
      } else {
        engine.stopRenderLoop(frameFn);
      }
    }));
    this._docSelectionRenderService.__attachScrollEvent();
  }
  _addComponent() {
    const { scene, unit: documentModel, components } = this._context;
    const DEFAULT_PAGE_MARGIN_LEFT = 20;
    const DEFAULT_PAGE_MARGIN_TOP = 20;
    const config = {
      pageMarginLeft: DEFAULT_PAGE_MARGIN_LEFT,
      pageMarginTop: DEFAULT_PAGE_MARGIN_TOP,
      ...this._getEditorBackgroundConfig()
    };
    const documents = new Documents("__Document_Render_Main__" /* MAIN */, void 0, config);
    documents.zIndex = DOCS_COMPONENT_DEFAULT_Z_INDEX;
    const docBackground = new DocBackground("__Document_Render_Background__" /* BACKGROUND */, void 0, config);
    docBackground.zIndex = DOCS_COMPONENT_DEFAULT_Z_INDEX;
    this._context.mainComponent = documents;
    components.set("__Document_Render_Main__" /* MAIN */, documents);
    components.set("__Document_Render_Background__" /* BACKGROUND */, docBackground);
    scene.addObjects([documents], DOCS_COMPONENT_MAIN_LAYER_INDEX);
    scene.addObjects([docBackground], DOCS_COMPONENT_BACKGROUND_LAYER_INDEX);
    if (this._editorService.getEditor(documentModel.getUnitId()) == null) {
      scene.enableLayerCache(DOCS_COMPONENT_MAIN_LAYER_INDEX);
    }
  }
  _initRenderRefresh() {
    this._docSkeletonManagerService.currentSkeletonBefore$.pipe(takeUntil(this.dispose$)).subscribe((param) => {
      this._create(param);
    });
  }
  _create(skeleton) {
    var _a;
    if (!skeleton) {
      return;
    }
    const { mainComponent, components } = this._context;
    const docsComponent = mainComponent;
    const docBackground = components.get("__Document_Render_Background__" /* BACKGROUND */);
    docsComponent.changeSkeleton(skeleton);
    docBackground.changeSkeleton(skeleton);
    this._syncCanvasBackground();
    const { unitId } = this._context;
    const editor = this._editorService.getEditor(unitId);
    if (this._editorService.isEditor(unitId) && !(editor == null ? void 0 : editor.params.scrollBar)) {
      (_a = this._context.mainComponent) == null ? void 0 : _a.makeDirty();
      return;
    }
    this._recalculateSizeBySkeleton(skeleton);
    this._refreshPagePositionAndSelection();
  }
  _initCommandListener() {
    const updateCommandList = [RichTextEditingMutation.id];
    this.disposeWithMe(this._commandService.onCommandExecuted((command) => {
      if (updateCommandList.includes(command.id)) {
        const params = command.params;
        const { unitId } = params;
        this.reRender(unitId);
      }
    }));
  }
  _refreshPagePositionAndSelection() {
    if (this._editorService.isEditor(this._context.unitId)) {
      return;
    }
    this._docPageLayoutService.calculatePagePosition();
    this._textSelectionManagerService.refreshSelection();
  }
  _recalculateSizeBySkeleton(skeleton) {
    var _a;
    const { mainComponent, scene, unitId, components } = this._context;
    const docsComponent = mainComponent;
    const docBackground = components.get("__Document_Render_Background__" /* BACKGROUND */);
    const pages = (_a = skeleton.getSkeletonData()) == null ? void 0 : _a.pages;
    if (pages == null) {
      return;
    }
    let width = 0;
    let height = 0;
    const docDataModel = this._context.unit;
    const documentFlavor = docDataModel.getSnapshot().documentStyle.documentFlavor;
    this._syncCanvasBackground(documentFlavor);
    for (let i = 0, len = pages.length; i < len; i++) {
      const page = pages[i];
      let { pageWidth, pageHeight } = page;
      if (documentFlavor === 2 /* MODERN */) {
        const modernPageSize = getPageSizeInModernMode(page);
        pageWidth = modernPageSize.pageWidth;
        pageHeight = modernPageSize.pageHeight;
      }
      if (docsComponent.pageLayoutType === 0 /* VERTICAL */) {
        height += pageHeight;
        height += docsComponent.pageMarginTop;
        if (i === len - 1) {
          height += docsComponent.pageMarginTop;
        }
        width = Math.max(width, pageWidth);
      } else if (docsComponent.pageLayoutType === 1 /* HORIZONTAL */) {
        width += pageWidth;
        if (i !== len - 1) {
          width += docsComponent.pageMarginLeft;
        }
        height = Math.max(height, pageHeight);
      }
    }
    docsComponent.resize(width, height);
    docBackground.resize(width, height);
    const editor = this._editorService.getEditor(unitId);
    if (!this._editorService.isEditor(unitId) || (editor == null ? void 0 : editor.params.scrollBar)) {
      scene.resize(width, height);
    }
  }
  _syncCanvasBackground(documentFlavor = this._context.unit.getSnapshot().documentStyle.documentFlavor) {
    var _a, _b;
    const editor = this._editorService.getEditor(this._context.unitId);
    const editorBackgroundColor = (_a = editor == null ? void 0 : editor.params.canvasStyle) == null ? void 0 : _a.backgroundColor;
    this._context.engine.getCanvas().getCanvasEle().style.backgroundColor = editorBackgroundColor != null ? editorBackgroundColor : getDocsCanvasBackgroundColor(documentFlavor);
    const docBackground = this._context.components.get("__Document_Render_Background__" /* BACKGROUND */);
    (_b = docBackground == null ? void 0 : docBackground.setFillColors) == null ? void 0 : _b.call(docBackground, editorBackgroundColor, editorBackgroundColor, editorBackgroundColor, editorBackgroundColor);
  }
  _getEditorBackgroundConfig() {
    var _a, _b;
    const editorBackgroundColor = (_b = (_a = this._editorService.getEditor(this._context.unitId)) == null ? void 0 : _a.params.canvasStyle) == null ? void 0 : _b.backgroundColor;
    return editorBackgroundColor == null ? {} : {
      backgroundFillColor: editorBackgroundColor,
      pageFillColor: editorBackgroundColor,
      pageStrokeColor: editorBackgroundColor,
      marginStrokeColor: editorBackgroundColor
    };
  }
};
DocRenderController = __decorateClass([
  __decorateParam(1, ICommandService),
  __decorateParam(2, Inject(DocSelectionRenderService)),
  __decorateParam(3, Inject(DocSkeletonManagerService)),
  __decorateParam(4, IEditorService),
  __decorateParam(5, IRenderManagerService),
  __decorateParam(6, IUniverInstanceService),
  __decorateParam(7, Inject(DocPageLayoutService)),
  __decorateParam(8, Inject(DocSelectionManagerService))
], DocRenderController);
function getPageSizeInModernMode(page) {
  let { pageWidth, pageHeight } = page;
  const { marginLeft, marginRight, marginTop, marginBottom, skeDrawings, skeTables } = page;
  if (pageWidth === Number.POSITIVE_INFINITY) {
    pageWidth = page.width + marginLeft + marginRight;
  }
  if (pageHeight === Number.POSITIVE_INFINITY) {
    pageHeight = page.height + marginTop + marginBottom;
  }
  for (const drawing of skeDrawings.values()) {
    pageWidth = Math.max(pageWidth, drawing.aLeft + drawing.width + marginLeft + marginRight);
    pageHeight = Math.max(pageHeight, drawing.aTop + drawing.height + marginTop + marginBottom);
  }
  for (const table of skeTables.values()) {
    pageWidth = Math.max(pageWidth, table.left + table.width + marginLeft + marginRight);
    pageHeight = Math.max(pageHeight, table.top + table.height + marginTop + marginBottom);
  }
  return { pageWidth, pageHeight };
}

// ../packages/docs-ui/src/controllers/render-controllers/zoom.render-controller.ts
function shouldHandleDocWheelZoom(event, focusingDoc, _documentFlavor) {
  return focusingDoc && (event.ctrlKey || event.metaKey);
}
var DocZoomRenderController = class extends Disposable {
  constructor(_context, _contextService, _docSkeletonManagerService, _univerInstanceService, _commandService, _textSelectionManagerService, _editorService, _docPageLayoutService, _renderManagerService) {
    super();
    __publicField(this, "_context", _context);
    __publicField(this, "_contextService", _contextService);
    __publicField(this, "_docSkeletonManagerService", _docSkeletonManagerService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_textSelectionManagerService", _textSelectionManagerService);
    __publicField(this, "_editorService", _editorService);
    __publicField(this, "_docPageLayoutService", _docPageLayoutService);
    __publicField(this, "_renderManagerService", _renderManagerService);
    __publicField(this, "_isSheetEditor", false);
    __publicField(this, "_initTimer");
    __publicField(this, "_updateTimer");
    this._initSkeletonListener();
    this._initCommandExecutedListener();
    this._isSheetEditor = this._context.unitId === DOCS_NORMAL_EDITOR_UNIT_ID_KEY;
    const currentSheet = this._univerInstanceService.getCurrentUnitOfType(2 /* UNIVER_SHEET */);
    const sheetRenderer = currentSheet && this._renderManagerService.getRenderById(currentSheet.getUnitId());
    this._initTimer = window.setTimeout(() => this.updateViewZoom(sheetRenderer && this._isSheetEditor ? sheetRenderer.scene.scaleX : 1, true), 20);
    if (!isInternalEditorID(this._context.unitId)) {
      this._initZoomEventListener();
    }
  }
  dispose() {
    window.clearTimeout(this._initTimer);
    window.clearTimeout(this._updateTimer);
  }
  _initSkeletonListener() {
    this.disposeWithMe(this._docSkeletonManagerService.currentSkeleton$.subscribe((param) => {
      if (param == null) {
        return;
      }
      const documentModel = this._univerInstanceService.getCurrentUniverDocInstance();
      if (!documentModel) return;
      this._updateTimer = window.setTimeout(() => {
        const currentSheet = this._univerInstanceService.getCurrentUnitOfType(2 /* UNIVER_SHEET */);
        const sheetRenderer = currentSheet && this._renderManagerService.getRenderById(currentSheet.getUnitId());
        const zoomRatio = !this._isSheetEditor ? documentModel.zoomRatio : (sheetRenderer == null ? void 0 : sheetRenderer.scene.scaleX) || 1;
        this.updateViewZoom(zoomRatio, false);
      });
    }));
  }
  _initCommandExecutedListener() {
    const updateCommandList = [SetDocZoomRatioOperation.id];
    this.disposeWithMe(this._commandService.onCommandExecuted((command) => {
      if (updateCommandList.includes(command.id) && command.params.unitId === this._context.unitId) {
        const documentModel = this._context.unit;
        const zoomRatio = documentModel.zoomRatio || 1;
        this.updateViewZoom(zoomRatio);
      }
    }));
    this.disposeWithMe(
      this._commandService.beforeCommandExecuted((command) => {
        var _a;
        const shouldResetZoom = command.id === SwitchDocModeCommand.id || command.id === DocPageSetupCommand.id && ((_a = command.params) == null ? void 0 : _a.documentFlavor) === 2 /* MODERN */;
        if (shouldResetZoom) {
          this._commandService.executeCommand(SetDocZoomRatioCommand.id, {
            zoomRatio: 1,
            unitId: this._context.unitId
          });
        }
      })
    );
  }
  updateViewZoom(zoomRatio, needRefreshSelection = true) {
    var _a;
    const docObject = neoGetDocObject(this._context);
    docObject.scene.scale(zoomRatio, zoomRatio);
    if (!this._editorService.isEditor(this._context.unitId)) {
      this._docPageLayoutService.calculatePagePosition();
    }
    if (needRefreshSelection && !this._editorService.isEditor(this._context.unitId)) {
      this._textSelectionManagerService.refreshSelection();
    }
    if (isInternalEditorID(this._context.unitId)) {
      return;
    }
    (_a = docObject.scene.getTransformer()) == null ? void 0 : _a.clearSelectedObjects();
  }
  _initZoomEventListener() {
    const scene = this._context.scene;
    this.disposeWithMe(
      // hold ctrl & mousewheel ---> zoom
      scene.onMouseWheel$.subscribeEvent((e) => {
        const documentModel = this._univerInstanceService.getCurrentUniverDocInstance();
        if (!documentModel) {
          return;
        }
        const { documentFlavor } = documentModel.getSnapshot().documentStyle;
        if (!shouldHandleDocWheelZoom(e, Boolean(this._contextService.getContextValue(FOCUSING_DOC)), documentFlavor)) {
          return;
        }
        const currentRatio = documentModel.zoomRatio || 1;
        const nextRatio = getNextWheelZoomRatio(currentRatio, e);
        this._commandService.executeCommand(SetDocZoomRatioCommand.id, {
          zoomRatio: nextRatio,
          documentId: documentModel.getUnitId()
        });
        e.preventDefault();
      })
    );
  }
};
DocZoomRenderController = __decorateClass([
  __decorateParam(1, IContextService),
  __decorateParam(2, Inject(DocSkeletonManagerService)),
  __decorateParam(3, IUniverInstanceService),
  __decorateParam(4, ICommandService),
  __decorateParam(5, Inject(DocSelectionManagerService)),
  __decorateParam(6, IEditorService),
  __decorateParam(7, Inject(DocPageLayoutService)),
  __decorateParam(8, IRenderManagerService)
], DocZoomRenderController);

// ../packages/docs-ui/src/services/doc-print-interceptor.service.ts
var PRINTING_COMPONENT_COLLECT = createInterceptorKey("PRINTING_COMPONENT_COLLECT");
var PRINTING_DOM_COLLECT = createInterceptorKey("PRINTING_DOM_COLLECT");
var DocPrintInterceptorService = class extends Disposable {
  constructor() {
    super();
    __publicField(this, "_printComponentMap", /* @__PURE__ */ new Map());
    __publicField(this, "interceptor", new InterceptorManager({
      PRINTING_COMPONENT_COLLECT,
      PRINTING_DOM_COLLECT
    }));
    this.disposeWithMe(this.interceptor.intercept(this.interceptor.getInterceptPoints().PRINTING_COMPONENT_COLLECT, {
      priority: -1,
      handler: (_value) => _value
    }));
    this.disposeWithMe(this.interceptor.intercept(this.interceptor.getInterceptPoints().PRINTING_DOM_COLLECT, {
      priority: -1,
      handler: (_value) => _value
    }));
  }
  registerPrintComponent(componentKey, printingComponentKey) {
    this._printComponentMap.set(componentKey, printingComponentKey);
  }
  getPrintComponent(componentKey) {
    return this._printComponentMap.get(componentKey);
  }
};

// ../packages/docs-ui/src/services/doc-state-change-manager.service.ts
var DEBOUNCE_DELAY = 300;
var DocStateChangeManagerService = class extends RxDisposable {
  constructor(_undoRedoService, _commandService, _univerInstanceService, _docStateEmitService, _renderManagerService) {
    super();
    __publicField(this, "_undoRedoService", _undoRedoService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_docStateEmitService", _docStateEmitService);
    __publicField(this, "_renderManagerService", _renderManagerService);
    __publicField(this, "_docStateChange$", new BehaviorSubject(null));
    __publicField(this, "docStateChange$", this._docStateChange$.asObservable());
    // This cache used for history compose.
    __publicField(this, "_historyStateCache", /* @__PURE__ */ new Map());
    // This cache used for collaboration state compose.
    __publicField(this, "_changeStateCache", /* @__PURE__ */ new Map());
    __publicField(this, "_historyTimer", null);
    __publicField(this, "_changeStateCacheTimer", null);
    this._initialize();
    this._listenDocStateChange();
  }
  getStateCache(unitId) {
    var _a, _b;
    return {
      history: (_a = this._historyStateCache.get(unitId)) != null ? _a : [],
      collaboration: (_b = this._changeStateCache.get(unitId)) != null ? _b : []
    };
  }
  setStateCache(unitId, cache) {
    this._historyStateCache.set(unitId, cache.history);
    this._changeStateCache.set(unitId, cache.collaboration);
  }
  _setChangeState(changeState) {
    this._cacheChangeState(changeState, "history");
    this._cacheChangeState(changeState, "collaboration");
  }
  _initialize() {
    this.disposeWithMe(
      this._commandService.beforeCommandExecuted((command) => {
        if (command.id === UndoCommandId || command.id === RedoCommandId) {
          const univerDoc = this._univerInstanceService.getCurrentUniverDocInstance();
          if (univerDoc == null) {
            return;
          }
          const unitId = univerDoc.getUnitId();
          this._pushHistory(unitId);
          this._emitChangeState(unitId);
        }
      })
    );
  }
  _listenDocStateChange() {
    this._docStateEmitService.docStateChangeParams$.pipe(takeUntil(this.dispose$)).subscribe((changeStateInfo) => {
      var _a;
      if (changeStateInfo == null) {
        return;
      }
      const { isCompositionEnd, isSync, syncer, ...changeState } = changeStateInfo;
      const imeInputManagerService = (_a = this._renderManagerService.getRenderById(isSync ? syncer : changeStateInfo.unitId)) == null ? void 0 : _a.with(DocIMEInputManagerService);
      if (imeInputManagerService == null) {
        return;
      }
      if (isCompositionEnd) {
        const historyParams = imeInputManagerService.fetchComposedUndoRedoMutationParams();
        if (historyParams == null) {
          throw new Error("historyParams is null in RichTextEditingMutation");
        }
        const { undoMutationParams, redoMutationParams, previousActiveRange } = historyParams;
        changeState.redoState.actions = redoMutationParams.actions;
        changeState.undoState.actions = undoMutationParams.actions;
        changeState.undoState.textRanges = [previousActiveRange];
      }
      this._setChangeState(changeState);
    });
  }
  _cacheChangeState(changeState, type2 = "history") {
    const { trigger, unitId, noHistory, debounce = false } = changeState;
    if (noHistory || trigger == null) {
      return;
    }
    if (type2 === "history" && (trigger === RedoCommandId || trigger === UndoCommandId)) {
      return;
    }
    const stateCache = type2 === "history" ? this._historyStateCache : this._changeStateCache;
    const cb = type2 === "history" ? this._pushHistory.bind(this) : this._emitChangeState.bind(this);
    if (stateCache.has(unitId)) {
      const cacheStates = stateCache.get(unitId);
      cacheStates == null ? void 0 : cacheStates.push(changeState);
    } else {
      stateCache.set(unitId, [changeState]);
    }
    if (debounce) {
      if (type2 === "history") {
        if (this._historyTimer) {
          clearTimeout(this._historyTimer);
        }
        this._historyTimer = setTimeout(() => {
          cb(unitId);
        }, DEBOUNCE_DELAY);
      } else {
        if (this._changeStateCacheTimer) {
          clearTimeout(this._changeStateCacheTimer);
        }
        this._changeStateCacheTimer = setTimeout(() => {
          cb(unitId);
        }, DEBOUNCE_DELAY);
      }
    } else {
      cb(unitId);
    }
  }
  _pushHistory(unitId) {
    const undoRedoService = this._undoRedoService;
    const cacheStates = this._historyStateCache.get(unitId);
    if (!Array.isArray(cacheStates) || cacheStates.length === 0) {
      return;
    }
    const len = cacheStates.length;
    const commandId = cacheStates[0].commandId;
    const firstState = cacheStates[0];
    const lastState = cacheStates[len - 1];
    const redoParams = {
      unitId,
      actions: cacheStates.reduce((acc, cur) => JSONX.compose(acc, cur.redoState.actions), null),
      textRanges: lastState.redoState.textRanges
    };
    const undoParams = {
      unitId,
      // Always need to put undoParams after redoParams, because `reverse` will change the `cacheStates` order.
      actions: cacheStates.reverse().reduce((acc, cur) => JSONX.compose(acc, cur.undoState.actions), null),
      textRanges: firstState.undoState.textRanges
    };
    undoRedoService.pushUndoRedo({
      unitID: unitId,
      undoMutations: [{ id: commandId, params: undoParams }],
      redoMutations: [{ id: commandId, params: redoParams }]
    });
    cacheStates.length = 0;
  }
  _emitChangeState(unitId) {
    const cacheStates = this._changeStateCache.get(unitId);
    if (!Array.isArray(cacheStates) || cacheStates.length === 0) {
      return;
    }
    const len = cacheStates.length;
    const { commandId, trigger, segmentId, noHistory, debounce } = cacheStates[0];
    const firstState = cacheStates[0];
    const lastState = cacheStates[len - 1];
    const redoState = {
      unitId,
      actions: cacheStates.reduce((acc, cur) => JSONX.compose(acc, cur.redoState.actions), null),
      textRanges: lastState.redoState.textRanges
    };
    const undoState = {
      unitId,
      // Always need to put undoParams after redoParams, because `reverse` will change the `cacheStates` order.
      actions: cacheStates.reverse().reduce((acc, cur) => JSONX.compose(acc, cur.undoState.actions), null),
      textRanges: firstState.undoState.textRanges
    };
    const changeState = {
      commandId,
      unitId,
      trigger,
      redoState,
      undoState,
      segmentId,
      noHistory,
      debounce
    };
    cacheStates.length = 0;
    this._docStateChange$.next(changeState);
  }
};
DocStateChangeManagerService = __decorateClass([
  __decorateParam(0, Inject(IUndoRedoService)),
  __decorateParam(1, ICommandService),
  __decorateParam(2, IUniverInstanceService),
  __decorateParam(3, Inject(DocStateEmitService)),
  __decorateParam(4, IRenderManagerService)
], DocStateChangeManagerService);

// ../packages/docs-ui/src/shortcuts/core-editing.shortcut.ts
var BreakLineShortcut = {
  id: EnterCommand.id,
  preconditions: whenDocAndEditorFocusedWithBreakLine,
  binding: 13 /* ENTER */
};
var DeleteLeftShortcut = {
  id: DeleteLeftCommand.id,
  preconditions: whenDocAndEditorFocused,
  binding: 8 /* BACKSPACE */
};
var DeleteRightShortcut = {
  id: DeleteRightCommand.id,
  preconditions: whenDocAndEditorFocused,
  binding: 46 /* DELETE */
};

// ../packages/docs-ui/src/shortcuts/cursor.shortcut.ts
var MoveCursorUpShortcut = {
  id: MoveCursorOperation.id,
  binding: 38 /* ARROW_UP */,
  preconditions: whenDocAndEditorFocused,
  staticParameters: {
    direction: 0 /* UP */
  }
};
var MoveCursorDownShortcut = {
  id: MoveCursorOperation.id,
  binding: 40 /* ARROW_DOWN */,
  preconditions: whenDocAndEditorFocused,
  staticParameters: {
    direction: 2 /* DOWN */
  }
};
var MoveCursorLeftShortcut = {
  id: MoveCursorOperation.id,
  binding: 37 /* ARROW_LEFT */,
  preconditions: whenDocAndEditorFocused,
  staticParameters: {
    direction: 3 /* LEFT */
  }
};
var MoveCursorRightShortcut = {
  id: MoveCursorOperation.id,
  binding: 39 /* ARROW_RIGHT */,
  preconditions: whenDocAndEditorFocused,
  staticParameters: {
    direction: 1 /* RIGHT */
  }
};
var MoveSelectionUpShortcut = {
  id: MoveSelectionOperation.id,
  binding: 38 /* ARROW_UP */ | 1024 /* SHIFT */,
  preconditions: whenDocAndEditorFocused,
  staticParameters: {
    direction: 0 /* UP */
  }
};
var MoveSelectionDownShortcut = {
  id: MoveSelectionOperation.id,
  binding: 40 /* ARROW_DOWN */ | 1024 /* SHIFT */,
  preconditions: whenDocAndEditorFocused,
  staticParameters: {
    direction: 2 /* DOWN */
  }
};
var MoveSelectionLeftShortcut = {
  id: MoveSelectionOperation.id,
  binding: 37 /* ARROW_LEFT */ | 1024 /* SHIFT */,
  preconditions: whenDocAndEditorFocused,
  staticParameters: {
    direction: 3 /* LEFT */
  }
};
var MoveSelectionRightShortcut = {
  id: MoveSelectionOperation.id,
  binding: 39 /* ARROW_RIGHT */ | 1024 /* SHIFT */,
  preconditions: whenDocAndEditorFocused,
  staticParameters: {
    direction: 1 /* RIGHT */
  }
};
var SelectAllShortcut = {
  id: DocSelectAllCommand.id,
  binding: 65 /* A */ | 4096 /* CTRL_COMMAND */,
  preconditions: (contextService) => contextService.getContextValue(FOCUSING_UNIVER_EDITOR) && (contextService.getContextValue(FOCUSING_DOC) || contextService.getContextValue(EDITOR_ACTIVATED))
};

// ../packages/docs-ui/src/plugin.ts
var UniverDocsUIPlugin = class extends Plugin {
  // static override type = UniverInstanceType.UNIVER_DOC;
  constructor(_config = defaultPluginConfig3, _injector, _renderManagerSrv, _commandService, _logService, _configService) {
    super();
    __publicField(this, "_config", _config);
    __publicField(this, "_injector", _injector);
    __publicField(this, "_renderManagerSrv", _renderManagerSrv);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_logService", _logService);
    __publicField(this, "_configService", _configService);
    const { menu, ...rest } = merge_default(
      {},
      defaultPluginConfig3,
      this._config
    );
    if (menu) {
      this._configService.setConfig("menu", menu, { merge: true });
    }
    this._configService.setConfig(DOCS_UI_PLUGIN_CONFIG_KEY, rest);
    this._initDependencies(_injector);
    this._initializeShortcut();
    this._initCommand();
  }
  onReady() {
    this._initRenderBasics();
    this._markDocAsFocused();
    touchDependencies(this._injector, [
      [DocStateChangeManagerService],
      [DocsRenderService]
    ]);
  }
  onRendered() {
    this._initRenderModules();
    touchDependencies(this._injector, [
      [DocAutoFormatController],
      [DocMoveCursorController],
      [DocParagraphSettingController],
      [DocTableController],
      // FIXME: LifecycleStages.Rendered must be used, otherwise the menu cannot be added to the DOM, but the sheet ui
      // plugin can be added in LifecycleStages.Ready
      [DocUIController]
    ]);
  }
  // eslint-disable-next-line max-lines-per-function
  _initCommand() {
    [
      DeleteLeftCommand,
      DeleteRightCommand,
      SetInlineFormatBoldCommand,
      SetInlineFormatItalicCommand,
      SetInlineFormatUnderlineCommand,
      SetInlineFormatStrikethroughCommand,
      SetInlineFormatSubscriptCommand,
      SetInlineFormatSuperscriptCommand,
      SetInlineFormatFontSizeCommand,
      SetInlineFormatFontFamilyCommand,
      SetInlineFormatTextColorCommand,
      ResetInlineFormatTextBackgroundColorCommand,
      SetInlineFormatTextBackgroundColorCommand,
      SetInlineFormatCommand,
      BreakLineCommand,
      InsertCommand,
      DeleteCommand,
      DeleteCustomBlockCommand,
      MoveDocBlockCommand,
      UpdateCommand,
      MergeTwoParagraphCommand,
      RemoveHorizontalLineCommand,
      SetDocZoomRatioOperation,
      OrderListCommand,
      BulletListCommand,
      ListOperationCommand,
      AlignLeftCommand,
      AlignCenterCommand,
      AlignRightCommand,
      AlignOperationCommand,
      AlignJustifyCommand,
      HorizontalLineCommand,
      CreateDocTableCommand,
      DocTableInsertRowCommand,
      DocTableInsertRowAboveCommand,
      DocTableInsertRowBellowCommand,
      DocTableInsertColumnCommand,
      DocTableInsertColumnLeftCommand,
      DocTableInsertColumnRightCommand,
      DocTableDeleteRowsCommand,
      DocTableDeleteColumnsCommand,
      DocTableDeleteTableCommand,
      CloseHeaderFooterCommand,
      DocTableTabCommand,
      TabCommand,
      AfterSpaceCommand,
      EnterCommand,
      ChangeListNestingLevelCommand,
      ChangeListTypeCommand,
      CheckListCommand,
      ToggleCheckListCommand,
      QuickListCommand,
      IMEInputCommand,
      SwitchDocModeCommand,
      DocParagraphSettingCommand,
      InnerPasteCommand,
      CutContentCommand,
      ReplaceContentCommand,
      ReplaceSnapshotCommand,
      CoverContentCommand,
      SetDocZoomRatioCommand,
      DocSelectAllCommand,
      DocParagraphSettingPanelOperation,
      MoveCursorOperation,
      MoveSelectionOperation,
      ReplaceTextRunsCommand,
      ReplaceSelectionCommand,
      InsertCustomRangeCommand,
      SetParagraphNamedStyleCommand,
      QuickHeadingCommand,
      DeleteCurrentParagraphCommand,
      DocCopyCurrentParagraphCommand,
      DocCutCurrentParagraphCommand,
      H1HeadingCommand,
      H2HeadingCommand,
      H3HeadingCommand,
      H4HeadingCommand,
      H5HeadingCommand,
      NormalTextHeadingCommand,
      TitleHeadingCommand,
      SubtitleHeadingCommand,
      InsertBulletListBellowCommand,
      InsertOrderListBellowCommand,
      InsertCheckListBellowCommand,
      InsertHorizontalLineBellowCommand,
      DocPageSetupCommand,
      DocOpenPageSettingCommand
    ].forEach((e) => {
      this.disposeWithMe(this._commandService.registerCommand(e));
    });
    [DocCopyCommand, DocCutCommand, DocPasteCommand].forEach((command) => this.disposeWithMe(this._commandService.registerMultipleCommand(command)));
  }
  _initializeShortcut() {
    [
      MoveCursorUpShortcut,
      MoveCursorDownShortcut,
      MoveCursorRightShortcut,
      MoveCursorLeftShortcut,
      MoveSelectionUpShortcut,
      MoveSelectionDownShortcut,
      MoveSelectionLeftShortcut,
      MoveSelectionRightShortcut,
      SelectAllShortcut,
      DeleteLeftShortcut,
      DeleteRightShortcut,
      BreakLineShortcut,
      ShiftTabShortCut
    ].forEach((shortcut) => {
      this._injector.get(IShortcutService).registerShortcut(shortcut);
    });
  }
  _initDependencies(injector) {
    const dependencies = mergeOverrideWithDependencies([
      [DocPrintInterceptorService],
      [DocClipboardController],
      [DocEditorBridgeController],
      [DocUIController],
      [DocAutoFormatController],
      [DocTableController],
      [DocMoveCursorController],
      [DocParagraphSettingController],
      [IEditorService, { useClass: EditorService }],
      [IDocClipboardService, { useClass: DocClipboardService }],
      [DocCanvasPopManagerService],
      [DocsRenderService],
      [DocStateChangeManagerService],
      [DocAutoFormatService],
      [DocContentInsertService],
      [DocMenuStyleService]
    ], this._config.override);
    dependencies.forEach((d) => injector.add(d));
  }
  _markDocAsFocused() {
    const currentService = this._injector.get(IUniverInstanceService);
    const editorService = this._injector.get(IEditorService);
    try {
      const doc = currentService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
      if (!doc) return;
      const id = doc.getUnitId();
      if (!editorService.isEditor(id)) {
        currentService.focusUnit(doc.getUnitId());
      }
    } catch (err) {
      this._logService.warn(err);
    }
  }
  _initRenderBasics() {
    [
      [DocSkeletonManagerService],
      [DocSelectionRenderService],
      [DocInterceptorService],
      [DocPageLayoutService],
      [DocIMEInputManagerService],
      [DocRenderController],
      [DocZoomRenderController]
    ].forEach((m) => {
      this._renderManagerSrv.registerRenderModule(1 /* UNIVER_DOC */, m);
    });
  }
  _initRenderModules() {
    [
      [DocEventManagerService],
      [DocFloatMenuService],
      [DocParagraphMenuService],
      [DocBackScrollRenderController],
      [DocSelectionRenderController],
      [DocHeaderFooterController],
      [DocResizeRenderController],
      [DocContextMenuRenderController],
      [DocChecklistRenderController],
      [DocClipboardController],
      [DocInputController],
      [DocIMEInputController],
      [DocEditorBridgeController]
    ].forEach((m) => {
      this._renderManagerSrv.registerRenderModule(1 /* UNIVER_DOC */, m);
    });
  }
};
__publicField(UniverDocsUIPlugin, "pluginName", "DOC_UI_PLUGIN");
__publicField(UniverDocsUIPlugin, "packageName", package_default3.name);
__publicField(UniverDocsUIPlugin, "version", package_default3.version);
UniverDocsUIPlugin = __decorateClass([
  DependentOn(UniverRenderEnginePlugin),
  __decorateParam(1, Inject(Injector)),
  __decorateParam(2, IRenderManagerService),
  __decorateParam(3, ICommandService),
  __decorateParam(4, ILogService),
  __decorateParam(5, IConfigService)
], UniverDocsUIPlugin);

// ../packages/docs-ui/src/basics/custom-decoration-factory.ts
function addCustomDecorationFactory(param) {
  const { unitId, ranges, id, type: type2, segmentId } = param;
  const doMutation = {
    id: RichTextEditingMutation.id,
    params: {
      unitId,
      actions: [],
      textRanges: void 0,
      // noHistory: true,
      segmentId
    }
  };
  const jsonX = JSONX.getInstance();
  const textX = BuildTextUtils.customDecoration.add({ ranges, id, type: type2 });
  doMutation.params.actions = jsonX.editOp(textX.serialize());
  return doMutation;
}
function addCustomDecorationBySelectionFactory(accessor, param) {
  const { segmentId, id, type: type2, unitId: propUnitId } = param;
  const docSelectionManagerService = accessor.get(DocSelectionManagerService);
  const univerInstanceService = accessor.get(IUniverInstanceService);
  const documentDataModel = propUnitId ? univerInstanceService.getUnit(propUnitId, 1 /* UNIVER_DOC */) : univerInstanceService.getCurrentUnitOfType(1 /* UNIVER_DOC */);
  if (!documentDataModel) {
    return false;
  }
  const unitId = documentDataModel.getUnitId();
  const selections = docSelectionManagerService.getTextRanges({ unitId, subUnitId: unitId });
  if (!selections) {
    return false;
  }
  const body = documentDataModel.getBody();
  if (!body) {
    return false;
  }
  const doMutation = addCustomDecorationFactory(
    {
      unitId,
      ranges: selections,
      id,
      type: type2,
      segmentId
    }
  );
  return doMutation;
}
function deleteCustomDecorationFactory(accessor, params) {
  const { unitId, id, segmentId } = params;
  const univerInstanceService = accessor.get(IUniverInstanceService);
  const documentDataModel = univerInstanceService.getUnit(unitId);
  if (!documentDataModel) {
    return false;
  }
  const doMutation = {
    id: RichTextEditingMutation.id,
    params: {
      unitId,
      actions: [],
      textRanges: void 0,
      // noHistory: true,
      segmentId
    }
  };
  const textX = BuildTextUtils.customDecoration.delete({ id, segmentId, documentDataModel });
  if (!textX) {
    return false;
  }
  const jsonX = JSONX.getInstance();
  doMutation.params.actions = jsonX.editOp(textX.serialize());
  return doMutation;
}

// ../packages/docs-ui/src/basics/transform-position.ts
function docDrawingPositionToTransform(position) {
  return {
    left: position.positionH.posOffset,
    top: position.positionV.posOffset,
    width: position.size.width,
    height: position.size.height
  };
}

// ../packages/docs-ui/src/views/rich-text-editor/index.tsx
var import_react20 = __toESM(require_react());

// ../packages/docs-ui/src/views/rich-text-editor/hooks/use-editor.ts
var import_react14 = __toESM(require_react());
function useEditor(opts) {
  const { editorId, initialValue, container, autoFocus: _autoFocus, isSingle, canvasStyle } = opts;
  const autoFocus = (0, import_react14.useMemo)(() => _autoFocus != null ? _autoFocus : false, []);
  const [editor, setEditor] = (0, import_react14.useState)();
  const editorService = useDependency(IEditorService);
  (0, import_react14.useLayoutEffect)(() => {
    var _a, _b;
    if (container.current) {
      const initialDoc = typeof initialValue === "string" ? void 0 : Tools.deepClone(initialValue);
      const snapshot = {
        body: {
          dataStream: typeof initialValue === "string" ? `${initialValue}\r
` : "\r\n",
          textRuns: [],
          customBlocks: [],
          customDecorations: [],
          customRanges: [],
          paragraphs: [{
            startIndex: 0
          }]
        },
        ...initialDoc,
        documentStyle: {
          ...initialDoc == null ? void 0 : initialDoc.documentStyle,
          pageSize: {
            width: !isSingle ? container.current.clientWidth : Infinity,
            height: Infinity
          }
        },
        id: editorId
      };
      const dispose = editorService.register(
        {
          autofocus: true,
          canvasStyle,
          editorUnitId: editorId,
          initialSnapshot: snapshot
        },
        container.current
      );
      const editor2 = editorService.getEditor(editorId);
      setEditor(editor2);
      if (autoFocus) {
        editorService.focus(editorId);
        const end = ((_b = (_a = snapshot.body) == null ? void 0 : _a.dataStream.length) != null ? _b : 2) - 2;
        editor2.setSelectionRanges([{ startOffset: end, endOffset: end }]);
      }
      return () => {
        dispose == null ? void 0 : dispose.dispose();
      };
    }
  }, []);
  return editor;
}

// ../packages/docs-ui/src/views/rich-text-editor/hooks/use-editor-click-outside.ts
var import_react15 = __toESM(require_react());
var useEditorClickOutside = (editorId, containerRef, onClickOutside) => {
  const editorService = useDependency(IEditorService);
  (0, import_react15.useEffect)(() => {
    const handleClickOutside = (event) => {
      var _a, _b;
      if (editorService.getFocusId() !== editorId) return;
      const id = (_b = (_a = event.target) == null ? void 0 : _a.dataset) == null ? void 0 : _b.editorid;
      if (id === editorId) return;
      if (containerRef.current && !containerRef.current.contains(event.target)) {
        onClickOutside == null ? void 0 : onClickOutside();
      }
    };
    const timer = setTimeout(() => {
      document.addEventListener("click", handleClickOutside);
    }, 100);
    return () => {
      document.removeEventListener("click", handleClickOutside);
      clearTimeout(timer);
    };
  }, [editorId, editorService, onClickOutside, containerRef]);
};

// ../packages/docs-ui/src/views/rich-text-editor/hooks/use-is-focusing.ts
function useIsFocusing(editorId) {
  var _a;
  const renderManagerService = useDependency(IRenderManagerService);
  const renderer = renderManagerService.getRenderById(editorId);
  const docSelectionRenderService = renderer == null ? void 0 : renderer.with(DocSelectionRenderService);
  const selections = useObservable(docSelectionRenderService == null ? void 0 : docSelectionRenderService.textSelectionInner$);
  return Boolean(((_a = docSelectionRenderService == null ? void 0 : docSelectionRenderService.isFocusing) != null ? _a : false) && (selections == null ? void 0 : selections.textRanges.some((r) => r.collapsed)));
}

// ../packages/docs-ui/src/views/rich-text-editor/hooks/use-keyboard-event.ts
var import_react16 = __toESM(require_react());
function useKeyboardEvent(isNeed, config, editor) {
  const commandService = useDependency(ICommandService);
  const shortcutService = useDependency(IShortcutService);
  const key = (0, import_react16.useMemo)(() => generateRandomId(4), []);
  (0, import_react16.useEffect)(() => {
    if (!editor || !isNeed || !config) {
      return;
    }
    const editorId = editor.getEditorId();
    const operationId = `sheet.operation.editor-${editorId}-keyboard-${key}`;
    const d = new DisposableCollection();
    d.add(commandService.registerCommand({
      id: operationId,
      type: 1 /* OPERATION */,
      handler(_event, params) {
        const { keyCode, metaKey } = params;
        config.handler(keyCode, metaKey);
      }
    }));
    config.keyCodes.map((keyCode) => {
      return {
        id: operationId,
        binding: keyCode.metaKey ? keyCode.keyCode | keyCode.metaKey : keyCode.keyCode,
        preconditions: () => true,
        priority: 901,
        staticParameters: {
          eventType: 4 /* Keyboard */,
          keyCode: keyCode.keyCode,
          metaKey: keyCode.metaKey
        }
      };
    }).forEach((item) => {
      d.add(shortcutService.registerShortcut(item));
    });
    return () => {
      d.dispose();
    };
  }, [commandService, config, editor, isNeed, key, shortcutService]);
}

// ../packages/docs-ui/src/views/rich-text-editor/hooks/use-left-and-right-arrow.ts
var import_react17 = __toESM(require_react());
var useLeftAndRightArrow = (isNeed, selectingMode, editor, onMoveInEditor) => {
  const commandService = useDependency(ICommandService);
  const shortcutService = useDependency(IShortcutService);
  const selectingModeRef = (0, import_react17.useRef)(selectingMode);
  selectingModeRef.current = selectingMode;
  const onMoveInEditorRef = (0, import_react17.useRef)(onMoveInEditor);
  onMoveInEditorRef.current = onMoveInEditor;
  (0, import_react17.useEffect)(() => {
    if (!editor || !isNeed) {
      return;
    }
    const editorId = editor.getEditorId();
    const operationId = `doc.rich-text-editor.${editorId}`;
    const d = new DisposableCollection();
    const handleMoveInEditor = (keycode, metaKey) => {
      if (onMoveInEditorRef.current) {
        onMoveInEditorRef.current(keycode, metaKey);
        return;
      }
      let direction = 3 /* LEFT */;
      if (keycode === 40 /* ARROW_DOWN */) {
        direction = 2 /* DOWN */;
      } else if (keycode === 38 /* ARROW_UP */) {
        direction = 0 /* UP */;
      } else if (keycode === 39 /* ARROW_RIGHT */) {
        direction = 1 /* RIGHT */;
      }
      if (metaKey === 1024 /* SHIFT */) {
        commandService.executeCommand(MoveSelectionOperation.id, {
          direction
        });
      } else {
        commandService.executeCommand(MoveCursorOperation.id, {
          direction
        });
      }
    };
    d.add(commandService.registerCommand({
      id: operationId,
      type: 1 /* OPERATION */,
      handler(_event, params) {
        const { keyCode } = params;
        handleMoveInEditor(keyCode);
      }
    }));
    const keyCodes = [
      { keyCode: 40 /* ARROW_DOWN */ },
      { keyCode: 37 /* ARROW_LEFT */ },
      { keyCode: 39 /* ARROW_RIGHT */ },
      { keyCode: 38 /* ARROW_UP */ },
      { keyCode: 40 /* ARROW_DOWN */, metaKey: 1024 /* SHIFT */ },
      { keyCode: 37 /* ARROW_LEFT */, metaKey: 1024 /* SHIFT */ },
      { keyCode: 39 /* ARROW_RIGHT */, metaKey: 1024 /* SHIFT */ },
      { keyCode: 38 /* ARROW_UP */, metaKey: 1024 /* SHIFT */ },
      { keyCode: 40 /* ARROW_DOWN */, metaKey: 4096 /* CTRL_COMMAND */ },
      { keyCode: 37 /* ARROW_LEFT */, metaKey: 4096 /* CTRL_COMMAND */ },
      { keyCode: 39 /* ARROW_RIGHT */, metaKey: 4096 /* CTRL_COMMAND */ },
      { keyCode: 38 /* ARROW_UP */, metaKey: 4096 /* CTRL_COMMAND */ },
      { keyCode: 40 /* ARROW_DOWN */, metaKey: 4096 /* CTRL_COMMAND */ | 1024 /* SHIFT */ },
      { keyCode: 37 /* ARROW_LEFT */, metaKey: 4096 /* CTRL_COMMAND */ | 1024 /* SHIFT */ },
      { keyCode: 39 /* ARROW_RIGHT */, metaKey: 4096 /* CTRL_COMMAND */ | 1024 /* SHIFT */ },
      { keyCode: 38 /* ARROW_UP */, metaKey: 4096 /* CTRL_COMMAND */ | 1024 /* SHIFT */ }
    ];
    keyCodes.map(({ keyCode, metaKey }) => {
      return {
        id: operationId,
        binding: metaKey ? keyCode | metaKey : keyCode,
        preconditions: () => true,
        priority: 900,
        staticParameters: {
          eventType: 4 /* Keyboard */,
          keyCode
        }
      };
    }).forEach((item) => {
      d.add(shortcutService.registerShortcut(item));
    });
    return () => {
      d.dispose();
    };
  }, [commandService, editor, isNeed, shortcutService]);
};

// ../packages/docs-ui/src/views/rich-text-editor/hooks/use-on-change.ts
var import_react18 = __toESM(require_react());
function useOnChange(editor, onChange) {
  const commandService = useDependency(ICommandService);
  (0, import_react18.useEffect)(() => {
    if (!editor) return;
    const dispose = commandService.onCommandExecuted((command) => {
      var _a, _b;
      if (command.id === RichTextEditingMutation.id) {
        const params = command.params;
        if (params.unitId !== editor.getEditorId()) return;
        const data = editor.getDocumentData();
        onChange(data, getPlainText((_b = (_a = data.body) == null ? void 0 : _a.dataStream) != null ? _b : ""));
      }
    });
    return () => {
      dispose.dispose();
    };
  }, [editor, onChange, commandService]);
}

// ../packages/docs-ui/src/views/rich-text-editor/hooks/use-resize.ts
var import_react19 = __toESM(require_react());
var useResize = (editor, isSingle = true, autoScrollbar, autoScroll) => {
  const resize = (0, import_react19.useCallback)(() => {
    var _a;
    if (editor) {
      const { scene, mainComponent } = editor.render;
      const docSkeletonManagerService = editor.render.with(DocSkeletonManagerService);
      const { width, height } = editor.getBoundingClientRect();
      docSkeletonManagerService.getViewModel().getDataModel().updateDocumentDataPageSize(isSingle ? Infinity : width, Infinity);
      scene.transformByState({
        width,
        height
      });
      mainComponent == null ? void 0 : mainComponent.resize(width, height);
      mainComponent == null ? void 0 : mainComponent.translate(0, 0);
      (_a = editor.render.components.get("__Document_Render_Background__" /* BACKGROUND */)) == null ? void 0 : _a.translate(0, 0);
      const selectionRanges = editor.getSelectionRanges();
      if (selectionRanges.length > 0) {
        editor.setSelectionRanges(selectionRanges, false);
      }
    }
  }, [editor, isSingle]);
  const checkScrollBar = (0, import_react19.useMemo)(() => {
    return debounce_default(() => {
      var _a, _b;
      if (!autoScrollbar) return;
      if (!editor || !autoScrollbar || editor.render.isDisposed()) {
        return;
      }
      const docSkeletonManagerService = editor.render.with(DocSkeletonManagerService);
      const skeleton = docSkeletonManagerService.getSkeleton();
      const { scene, mainComponent } = editor.render;
      const viewportMain = scene.getViewport("viewMain" /* VIEW_MAIN */);
      const { actualWidth, actualHeight } = skeleton.getActualSize();
      const { width, height } = editor.getBoundingClientRect();
      let scrollBar = viewportMain == null ? void 0 : viewportMain.getScrollBar();
      const contentWidth = Math.max(actualWidth, width);
      const contentHeight = Math.max(actualHeight, height);
      scene.transformByState({
        width: contentWidth,
        height: contentHeight
      });
      mainComponent == null ? void 0 : mainComponent.resize(contentWidth, contentHeight);
      if (!isSingle) {
        if (actualHeight > height) {
          if (scrollBar == null) {
            if (viewportMain) {
              scrollBar = new ScrollBar(viewportMain, {
                enableHorizontal: false,
                enableVertical: true,
                barSize: 8,
                minThumbSizeV: 8
              });
            }
          } else {
            viewportMain == null ? void 0 : viewportMain.resetCanvasSizeAndUpdateScroll();
          }
          autoScroll && (viewportMain == null ? void 0 : viewportMain.scrollToBarPos({ x: 0, y: Infinity }));
        } else {
          scrollBar = null;
          viewportMain == null ? void 0 : viewportMain.scrollToBarPos({ x: 0, y: 0 });
          (_a = viewportMain == null ? void 0 : viewportMain.getScrollBar()) == null ? void 0 : _a.dispose();
        }
      } else {
        if (actualWidth > width) {
          if (scrollBar == null) {
            viewportMain && new ScrollBar(viewportMain, {
              barSize: 8,
              enableVertical: false,
              enableHorizontal: true,
              minThumbSizeV: 8
            });
          } else {
            viewportMain == null ? void 0 : viewportMain.resetCanvasSizeAndUpdateScroll();
          }
          autoScroll && (viewportMain == null ? void 0 : viewportMain.scrollToBarPos({ x: Infinity, y: 0 }));
        } else {
          scrollBar = null;
          viewportMain == null ? void 0 : viewportMain.scrollToBarPos({ x: 0, y: 0 });
          (_b = viewportMain == null ? void 0 : viewportMain.getScrollBar()) == null ? void 0 : _b.dispose();
        }
      }
    }, 30);
  }, [editor, autoScrollbar, isSingle, autoScroll]);
  (0, import_react19.useEffect)(() => {
    if (!autoScrollbar) return;
    if (editor) {
      const time = setTimeout(() => {
        resize();
        checkScrollBar();
      }, 500);
      return () => {
        clearTimeout(time);
      };
    }
  }, [editor, autoScrollbar, resize, checkScrollBar]);
  (0, import_react19.useEffect)(() => {
    if (!autoScrollbar) return;
    if (editor) {
      const d = editor.input$.subscribe(() => {
        checkScrollBar();
      });
      return () => {
        d.unsubscribe();
      };
    }
  }, [editor, autoScrollbar, checkScrollBar]);
  return { resize, checkScrollBar };
};

// ../packages/docs-ui/src/views/rich-text-editor/index.tsx
var import_jsx_runtime15 = __toESM(require_jsx_runtime());
var RichTextEditor = (props) => {
  const {
    className,
    autoFocus,
    onFocusChange: _onFocusChange,
    initialValue,
    onClickOutside: _onClickOutside,
    keyboardEventConfig,
    moveCursor = true,
    style,
    isSingle,
    editorId: propsEditorId,
    onHeightChange,
    onChange: _onChange,
    defaultHeight = 32,
    maxHeight = 32,
    icon,
    editorRef,
    placeholder,
    noStyle
  } = props;
  const editorService = useDependency(IEditorService);
  const onFocusChange = useEvent(_onFocusChange);
  const onClickOutside = useEvent(_onClickOutside);
  const [height, setHeight] = (0, import_react20.useState)(defaultHeight);
  const formulaEditorContainerRef = (0, import_react20.useRef)(null);
  const editorId = (0, import_react20.useMemo)(() => propsEditorId != null ? propsEditorId : createInternalEditorID(`RICH_TEXT_EDITOR-${generateRandomId(4)}`), [propsEditorId]);
  const editor = useEditor({
    editorId,
    initialValue,
    container: formulaEditorContainerRef,
    autoFocus,
    isSingle
  });
  const renderManagerService = useDependency(IRenderManagerService);
  const renderer = renderManagerService.getRenderById(editorId);
  const isFocusing = useIsFocusing(editorId);
  const sheetEmbeddingRef = (0, import_react20.useRef)(null);
  const [showPlaceholder, setShowPlaceholder] = (0, import_react20.useState)(() => {
    var _a, _b;
    return !BuildTextUtils.transform.getPlainText((_b = (_a = editor == null ? void 0 : editor.getDocumentData().body) == null ? void 0 : _a.dataStream) != null ? _b : "");
  });
  const { checkScrollBar } = useResize(editor, isSingle, true, true);
  (0, import_react20.useLayoutEffect)(() => {
    if (!editorRef || !editor) return;
    if (typeof editorRef === "function") {
      editorRef(editor);
      return;
    }
    editorRef.current = editor;
  }, [editor]);
  const onChange = useEvent((data) => {
    var _a, _b;
    const docSkeleton = renderer == null ? void 0 : renderer.with(DocSkeletonManagerService);
    const size = docSkeleton == null ? void 0 : docSkeleton.getSkeleton().getActualSize();
    if (size) {
      onHeightChange == null ? void 0 : onHeightChange(size.actualHeight);
      setHeight(Math.max(defaultHeight, Math.min(size.actualHeight + 10, maxHeight)));
    }
    _onChange == null ? void 0 : _onChange(data, getPlainText((_b = (_a = data.body) == null ? void 0 : _a.dataStream) != null ? _b : ""));
    checkScrollBar();
  });
  (0, import_react20.useEffect)(() => {
    var _a, _b;
    setShowPlaceholder(!BuildTextUtils.transform.getPlainText((_b = (_a = editor == null ? void 0 : editor.getDocumentData().body) == null ? void 0 : _a.dataStream) != null ? _b : ""));
    const sub = editor == null ? void 0 : editor.selectionChange$.subscribe(() => {
      var _a2, _b2;
      setShowPlaceholder(!BuildTextUtils.transform.getPlainText((_b2 = (_a2 = editor == null ? void 0 : editor.getDocumentData().body) == null ? void 0 : _a2.dataStream) != null ? _b2 : ""));
    });
    return () => sub == null ? void 0 : sub.unsubscribe();
  }, [editor]);
  useObservable(editor == null ? void 0 : editor.blur$);
  useObservable(editor == null ? void 0 : editor.focus$);
  (0, import_react20.useEffect)(() => {
    var _a, _b;
    const data = editor == null ? void 0 : editor.getDocumentData();
    onFocusChange == null ? void 0 : onFocusChange(isFocusing, getPlainText((_b = (_a = data == null ? void 0 : data.body) == null ? void 0 : _a.dataStream) != null ? _b : ""));
  }, [isFocusing, onFocusChange]);
  useEditorClickOutside(editorId, sheetEmbeddingRef, onClickOutside);
  useLeftAndRightArrow(isFocusing && moveCursor, false, editor);
  useKeyboardEvent(isFocusing, keyboardEventConfig, editor);
  useOnChange(editor, onChange);
  return /* @__PURE__ */ (0, import_jsx_runtime15.jsx)("div", { className, style, children: /* @__PURE__ */ (0, import_jsx_runtime15.jsxs)(
    "div",
    {
      className: clsx(
        {
          "univer-border-primary-500": isFocusing && !noStyle,
          [`univer-relative univer-box-border univer-flex univer-h-8 univer-w-full univer-items-center univer-justify-around univer-gap-2 univer-rounded-md univer-pb-0.5 univer-pl-1.5 univer-pr-2 univer-pt-1.5`]: !noStyle,
          "univer-size-full": noStyle,
          [borderClassName]: !noStyle
        }
      ),
      style: { height },
      ref: sheetEmbeddingRef,
      children: [
        /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
          "div",
          {
            ref: formulaEditorContainerRef,
            className: "univer-relative univer-size-full",
            onMouseUp: () => editor == null ? void 0 : editor.focus()
          }
        ),
        icon,
        !showPlaceholder ? null : /* @__PURE__ */ (0, import_jsx_runtime15.jsx)(
          "div",
          {
            className: `univer-absolute univer-left-[5px] univer-top-[5px] univer-text-sm univer-text-gray-500`,
            children: placeholder
          }
        )
      ]
    }
  ) });
};

// ../packages/data-validation/src/common/util.ts
function getRuleSetting(rule) {
  return {
    type: rule.type,
    operator: rule.operator,
    formula1: rule.formula1,
    formula2: rule.formula2,
    allowBlank: rule.allowBlank
  };
}
function getRuleOptions(rule) {
  return {
    error: rule.error,
    errorStyle: rule.errorStyle,
    errorTitle: rule.errorTitle,
    imeMode: rule.imeMode,
    prompt: rule.prompt,
    promptTitle: rule.promptTitle,
    showDropDown: rule.showDropDown,
    showErrorMessage: rule.showErrorMessage,
    showInputMessage: rule.showInputMessage,
    renderMode: rule.renderMode,
    bizInfo: rule.bizInfo
  };
}

// ../packages/data-validation/src/models/data-validation-model.ts
var DataValidationModel = class extends Disposable {
  constructor(_logService) {
    super();
    __publicField(this, "_logService", _logService);
    __publicField(this, "_model", /* @__PURE__ */ new Map());
    __publicField(this, "_ruleChange$", new Subject());
    __publicField(this, "ruleChange$", this._ruleChange$.asObservable());
    __publicField(this, "ruleChangeDebounce$", this.ruleChange$.pipe(debounceTime(20)));
    this.disposeWithMe({
      dispose: () => {
        this._ruleChange$.complete();
      }
    });
  }
  _ensureMap(unitId, subUnitId) {
    if (!this._model.has(unitId)) {
      this._model.set(unitId, /* @__PURE__ */ new Map());
    }
    const unitMap = this._model.get(unitId);
    if (unitMap.has(subUnitId)) {
      return unitMap.get(subUnitId);
    }
    const map2 = { map: /* @__PURE__ */ new Map(), list: [] };
    unitMap.set(subUnitId, map2);
    return map2;
  }
  _addSubUnitRule(subUnit, rule, index) {
    const { map: dataValidationMap, list: dataValidations } = subUnit;
    const _rules = Array.isArray(rule) ? rule : [rule];
    const rules = _rules.filter((item) => !dataValidationMap.has(item.uid));
    if (typeof index === "number" && index < dataValidations.length) {
      dataValidations.splice(index, 0, ...rules);
    } else {
      dataValidations.push(...rules);
    }
    rules.forEach((item) => {
      dataValidationMap.set(item.uid, item);
    });
  }
  _removeSubUnitRule(subUnit, ruleId) {
    const { map: dataValidationMap, list: dataValidations } = subUnit;
    const index = dataValidations.findIndex((item) => item.uid === ruleId);
    if (index > -1) {
      dataValidations.splice(index, 1);
      dataValidationMap.delete(ruleId);
    }
  }
  _updateSubUnitRule(subUnit, ruleId, payload) {
    const { map: dataValidationMap, list: dataValidations } = subUnit;
    const oldRule = dataValidationMap.get(ruleId);
    const index = dataValidations.findIndex((rule2) => ruleId === rule2.uid);
    if (!oldRule) {
      throw new Error(`Data validation rule is not found, ruleId: ${ruleId}.`);
    }
    const rule = { ...oldRule };
    switch (payload.type) {
      case 1 /* RANGE */: {
        rule.ranges = payload.payload;
        break;
      }
      case 0 /* SETTING */: {
        Object.assign(rule, getRuleSetting(payload.payload));
        break;
      }
      case 2 /* OPTIONS */: {
        Object.assign(rule, getRuleOptions(payload.payload));
        break;
      }
      case 3 /* ALL */: {
        Object.assign(rule, payload.payload);
        break;
      }
      default:
        break;
    }
    dataValidations[index] = rule;
    dataValidationMap.set(ruleId, rule);
    return rule;
  }
  _addRuleSideEffect(unitId, subUnitId, rule, source) {
    const subUnitMap = this._ensureMap(unitId, subUnitId);
    const oldRule = subUnitMap.map.get(rule.uid);
    if (oldRule) {
      return;
    }
    return {
      rule,
      type: "add",
      unitId,
      subUnitId,
      source
    };
  }
  addRule(unitId, subUnitId, rule, source, index) {
    try {
      const subUnitMap = this._ensureMap(unitId, subUnitId);
      const rules = Array.isArray(rule) ? rule : [rule];
      const effects = rules.map((item) => this._addRuleSideEffect(unitId, subUnitId, item, source));
      this._addSubUnitRule(subUnitMap, rule, index);
      effects.forEach((effect) => {
        if (effect) {
          this._ruleChange$.next(effect);
        }
      });
    } catch (error) {
      this._logService.error(error);
    }
  }
  updateRule(unitId, subUnitId, ruleId, payload, source) {
    try {
      const subUnitMap = this._ensureMap(unitId, subUnitId);
      const oldRule = Tools.deepClone(subUnitMap.map.get(ruleId));
      if (!oldRule) {
        throw new Error(`Data validation rule is not found, ruleId: ${ruleId}.`);
      }
      const rule = this._updateSubUnitRule(subUnitMap, ruleId, payload);
      this._ruleChange$.next({
        rule,
        type: "update",
        unitId,
        subUnitId,
        source,
        updatePayload: payload,
        oldRule
      });
    } catch (error) {
      this._logService.error(error);
    }
  }
  removeRule(unitId, subUnitId, ruleId, source) {
    try {
      const map2 = this._ensureMap(unitId, subUnitId);
      const oldRule = map2.map.get(ruleId);
      if (oldRule) {
        this._removeSubUnitRule(map2, ruleId);
        this._ruleChange$.next({
          rule: oldRule,
          type: "remove",
          unitId,
          subUnitId,
          source
        });
      }
    } catch (error) {
      this._logService.error(error);
    }
  }
  getRuleById(unitId, subUnitId, ruleId) {
    const map2 = this._ensureMap(unitId, subUnitId);
    return map2.map.get(ruleId);
  }
  getRuleIndex(unitId, subUnitId, ruleId) {
    const map2 = this._ensureMap(unitId, subUnitId);
    return map2.list.findIndex((rule) => rule.uid === ruleId);
  }
  getRules(unitId, subUnitId) {
    const manager = this._ensureMap(unitId, subUnitId);
    return [...manager.list];
  }
  getUnitRules(unitId) {
    const unitMap = this._model.get(unitId);
    if (!unitMap) {
      return [];
    }
    const res = [];
    unitMap.forEach((manager, subUnitId) => {
      res.push([subUnitId, manager.list]);
    });
    return res;
  }
  deleteUnitRules(unitId) {
    this._model.delete(unitId);
  }
  getSubUnitIds(unitId) {
    var _a, _b;
    return Array.from((_b = (_a = this._model.get(unitId)) == null ? void 0 : _a.keys()) != null ? _b : []);
  }
  getAll() {
    return Array.from(this._model.keys()).map((unitId) => [unitId, this.getUnitRules(unitId)]);
  }
};
DataValidationModel = __decorateClass([
  __decorateParam(0, ILogService)
], DataValidationModel);

// ../packages/data-validation/src/commands/mutations/data-validation.mutation.ts
var AddDataValidationMutation = {
  type: 2 /* MUTATION */,
  id: "data-validation.mutation.addRule",
  handler(accessor, params) {
    if (!params) {
      return false;
    }
    const { unitId, subUnitId, rule, index, source = "command" } = params;
    const dataValidationModel = accessor.get(DataValidationModel);
    dataValidationModel.addRule(unitId, subUnitId, rule, source, index);
    return true;
  }
};
var RemoveDataValidationMutation = {
  type: 2 /* MUTATION */,
  id: "data-validation.mutation.removeRule",
  handler(accessor, params) {
    if (!params) {
      return false;
    }
    const { unitId, subUnitId, ruleId, source = "command" } = params;
    const dataValidationModel = accessor.get(DataValidationModel);
    if (Array.isArray(ruleId)) {
      ruleId.forEach((item) => {
        dataValidationModel.removeRule(unitId, subUnitId, item, source);
      });
    } else {
      dataValidationModel.removeRule(unitId, subUnitId, ruleId, source);
    }
    return true;
  }
};
var UpdateDataValidationMutation = {
  type: 2 /* MUTATION */,
  id: "data-validation.mutation.updateRule",
  handler(accessor, params) {
    if (!params) {
      return false;
    }
    const { unitId, subUnitId, ruleId, payload, source = "command" } = params;
    const dataValidationModel = accessor.get(DataValidationModel);
    dataValidationModel.updateRule(unitId, subUnitId, ruleId, payload, source);
    return true;
  }
};

// ../packages/data-validation/src/controllers/dv-resource.controller.ts
var DATA_VALIDATION_PLUGIN_NAME = "SHEET_DATA_VALIDATION_PLUGIN";
var DataValidationResourceController = class extends Disposable {
  constructor(_resourceManagerService, _univerInstanceService, _dataValidationModel) {
    super();
    __publicField(this, "_resourceManagerService", _resourceManagerService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_dataValidationModel", _dataValidationModel);
    this._initSnapshot();
  }
  _initSnapshot() {
    const toJson = (unitID) => {
      const map2 = this._dataValidationModel.getUnitRules(unitID);
      const resultMap = {};
      if (map2) {
        map2.forEach(([key, v]) => {
          resultMap[key] = v;
        });
        return JSON.stringify(resultMap);
      }
      return "";
    };
    const parseJson = (json) => {
      if (!json) {
        return {};
      }
      try {
        return JSON.parse(json);
      } catch (err) {
        return {};
      }
    };
    this.disposeWithMe(
      this._resourceManagerService.registerPluginResource({
        pluginName: DATA_VALIDATION_PLUGIN_NAME,
        businesses: [2 /* UNIVER_SHEET */],
        toJson: (unitID) => toJson(unitID),
        parseJson: (json) => parseJson(json),
        onUnLoad: (unitID) => {
          this._dataValidationModel.deleteUnitRules(unitID);
        },
        onLoad: (unitID, value) => {
          Object.keys(value).forEach((subunitId) => {
            const ruleList = value[subunitId];
            ruleList.forEach((rule) => {
              this._dataValidationModel.addRule(unitID, subunitId, rule, "patched");
            });
          });
        }
      })
    );
  }
};
DataValidationResourceController = __decorateClass([
  __decorateParam(0, IResourceManagerService),
  __decorateParam(1, IUniverInstanceService),
  __decorateParam(2, Inject(DataValidationModel))
], DataValidationResourceController);

// ../packages/data-validation/package.json
var package_default4 = {
  name: "@univerjs/data-validation",
  version: "0.25.2",
  private: false,
  description: "Shared data validation models and rule services for Univer.",
  author: "DreamNum Co., Ltd. <developer@univer.ai>",
  license: "Apache-2.0",
  funding: {
    type: "opencollective",
    url: "https://opencollective.com/univer"
  },
  homepage: "https://univer.ai",
  repository: {
    type: "git",
    url: "https://github.com/dream-num/univer"
  },
  bugs: {
    url: "https://github.com/dream-num/univer/issues"
  },
  keywords: [
    "univer",
    "data-validation",
    "validation",
    "rules",
    "plugin"
  ],
  exports: {
    ".": "./src/index.ts",
    "./*": "./src/*",
    "./locale/*": "./src/locale/*.ts"
  },
  main: "./src/index.ts",
  types: "./lib/types/index.d.ts",
  publishConfig: {
    access: "public",
    main: "./lib/es/index.js",
    module: "./lib/es/index.js",
    exports: {
      ".": {
        import: "./lib/es/index.js",
        require: "./lib/cjs/index.js",
        types: "./lib/types/index.d.ts"
      },
      "./*": {
        import: "./lib/es/*",
        require: "./lib/cjs/*",
        types: "./lib/types/index.d.ts"
      },
      "./locale/*": {
        import: "./lib/es/locale/*.js",
        require: "./lib/cjs/locale/*.js",
        types: "./lib/types/locale/*.d.ts"
      },
      "./lib/*": "./lib/*"
    }
  },
  directories: {
    lib: "lib"
  },
  files: [
    "lib"
  ],
  scripts: {
    dev: "vite",
    test: "vitest run",
    "test:watch": "vitest",
    coverage: "vitest run --coverage",
    "build:bundle": "univer-cli build",
    "build:types": "tsc -p tsconfig.node.json",
    build: "pnpm run build:bundle && pnpm run build:types"
  },
  peerDependencies: {
    rxjs: ">=7.0.0"
  },
  dependencies: {
    "@univerjs/core": "workspace:*"
  },
  devDependencies: {
    "@univerjs-infra/shared": "workspace:*",
    "@univerjs/protocol": "workspace:*",
    "@univerjs/sheets": "workspace:*",
    rxjs: "^7.8.2",
    typescript: "^6.0.3",
    vitest: "^4.1.7"
  }
};

// ../packages/data-validation/src/services/data-validator-registry.service.ts
var DataValidatorRegistryService = class {
  constructor() {
    __publicField(this, "_validatorByScopes", /* @__PURE__ */ new Map());
    __publicField(this, "_validatorMap", /* @__PURE__ */ new Map());
    __publicField(this, "_validatorsChange$", new BehaviorSubject(void 0));
    __publicField(this, "validatorsChange$", this._validatorsChange$.asObservable());
  }
  _addValidatorToScope(validator, scope) {
    if (!this._validatorByScopes.has(scope)) {
      this._validatorByScopes.set(scope, []);
    }
    const validators = this._validatorByScopes.get(scope);
    if (validators.findIndex((m) => m.id === validator.id) > -1) {
      throw new Error(`Validator item with the same id ${validator.id} has already been added!`);
    }
    validators.push(validator);
  }
  _removeValidatorFromScope(validator, scope) {
    const validators = this._validatorByScopes.get(scope);
    if (!validators) {
      return;
    }
    const index = validators.findIndex((v) => v.id === validator.id);
    if (index > -1) {
      validators.splice(index, 1);
    }
  }
  register(validator) {
    this._validatorMap.set(validator.id, validator);
    if (Array.isArray(validator.scopes)) {
      validator.scopes.forEach((scope) => {
        this._addValidatorToScope(validator, scope);
      });
    } else {
      this._addValidatorToScope(validator, validator.scopes);
    }
    this._validatorsChange$.next();
    return toDisposable(() => {
      this._validatorMap.delete(validator.id);
      if (Array.isArray(validator.scopes)) {
        validator.scopes.forEach((scope) => {
          this._removeValidatorFromScope(validator, scope);
        });
      } else {
        this._removeValidatorFromScope(validator, validator.scopes);
      }
      this._validatorsChange$.next();
    });
  }
  getValidatorItem(id) {
    return this._validatorMap.get(id);
  }
  getValidatorsByScope(scope) {
    return this._validatorByScopes.get(scope);
  }
};

// ../packages/data-validation/src/commands/commands/data-validation.command.ts
var AddDataValidationCommand = {
  type: 0 /* COMMAND */,
  id: "data-validation.command.addRule",
  async handler(accessor, params) {
    const logService = accessor.get(ILogService);
    logService.error("[Deprecated]: `AddDataValidationCommand` is deprecated, please use `AddSheetDataValidationCommand` in `@univerjs/sheets-data-validation` instead!");
    if (!params) {
      return false;
    }
    const { rule, unitId, subUnitId } = params;
    const commandService = accessor.get(ICommandService);
    const undoRedoService = accessor.get(IUndoRedoService);
    const mutationParams = {
      ...params,
      rule: {
        ...params.rule,
        ranges: [params.rule.range]
      }
    };
    const redoMutations = [{
      id: AddDataValidationMutation.id,
      params: mutationParams
    }];
    const undoMutations = [{
      id: RemoveDataValidationMutation.id,
      params: {
        unitId,
        subUnitId,
        ruleId: rule.uid
      }
    }];
    undoRedoService.pushUndoRedo({
      unitID: unitId,
      redoMutations,
      undoMutations
    });
    await commandService.executeCommand(AddDataValidationMutation.id, mutationParams);
    return true;
  }
};
var RemoveDataValidationCommand = {
  type: 0 /* COMMAND */,
  id: "data-validation.command.removeRule",
  handler(accessor, params) {
    const logService = accessor.get(ILogService);
    logService.error("[Deprecated]: `RemoveDataValidationCommand` is deprecated, please use `RemoveSheetDataValidationCommand` in `@univerjs/sheets-data-validation` instead!");
    if (!params) {
      return false;
    }
    const { unitId, subUnitId, ruleId } = params;
    const commandService = accessor.get(ICommandService);
    const undoRedoService = accessor.get(IUndoRedoService);
    const dataValidationModel = accessor.get(DataValidationModel);
    const redoMutations = [{
      id: RemoveDataValidationMutation.id,
      params
    }];
    const undoMutations = [{
      id: AddDataValidationMutation.id,
      params: {
        unitId,
        subUnitId,
        rule: {
          ...dataValidationModel.getRuleById(unitId, subUnitId, ruleId)
        },
        index: dataValidationModel.getRuleIndex(unitId, subUnitId, ruleId)
      }
    }];
    undoRedoService.pushUndoRedo({
      undoMutations,
      redoMutations,
      unitID: params.unitId
    });
    commandService.executeCommand(RemoveDataValidationMutation.id, params);
    return true;
  }
};
var UpdateDataValidationOptionsCommand = {
  type: 0 /* COMMAND */,
  id: "data-validation.command.updateDataValidationSetting",
  handler(accessor, params) {
    const logService = accessor.get(ILogService);
    logService.warn("[Deprecated]: `UpdateDataValidationOptionsCommand` is deprecated, please use `UpdateSheetDataValidationOptionsCommand` in `@univerjs/sheets-data-validation` instead!");
    if (!params) {
      return false;
    }
    const commandService = accessor.get(ICommandService);
    const redoUndoService = accessor.get(IUndoRedoService);
    const dataValidationModel = accessor.get(DataValidationModel);
    const { unitId, subUnitId, ruleId, options } = params;
    const rule = dataValidationModel.getRuleById(unitId, subUnitId, ruleId);
    if (!rule) {
      return false;
    }
    const mutationParams = {
      unitId,
      subUnitId,
      ruleId,
      payload: {
        type: 2 /* OPTIONS */,
        payload: options
      }
    };
    const redoMutations = [{
      id: UpdateDataValidationMutation.id,
      params: mutationParams
    }];
    const undoMutationParams = {
      unitId,
      subUnitId,
      ruleId,
      payload: {
        type: 2 /* OPTIONS */,
        payload: getRuleOptions(rule)
      }
    };
    const undoMutations = [{
      id: UpdateDataValidationMutation.id,
      params: undoMutationParams
    }];
    redoUndoService.pushUndoRedo({
      unitID: unitId,
      redoMutations,
      undoMutations
    });
    commandService.executeCommand(UpdateDataValidationMutation.id, mutationParams);
    return true;
  }
};
var UpdateDataValidationSettingCommand = {
  type: 0 /* COMMAND */,
  id: "data-validation.command.updateDataValidationOptions",
  handler(accessor, params) {
    const logService = accessor.get(ILogService);
    logService.error("[Deprecated]: `UpdateDataValidationSettingCommand` is deprecated, please use `UpdateSheetDataValidationSettingCommand` in `@univerjs/sheets-data-validation` instead!");
    if (!params) {
      return false;
    }
    const commandService = accessor.get(ICommandService);
    const redoUndoService = accessor.get(IUndoRedoService);
    const dataValidationModel = accessor.get(DataValidationModel);
    const dataValidatorRegistryService = accessor.get(DataValidatorRegistryService);
    const { unitId, subUnitId, ruleId, setting } = params;
    const validator = dataValidatorRegistryService.getValidatorItem(setting.type);
    if (!validator) {
      return false;
    }
    const rule = dataValidationModel.getRuleById(unitId, subUnitId, ruleId);
    if (!rule) {
      return false;
    }
    const newRule = { ...rule, ...setting };
    if (!validator.validatorFormula(newRule, unitId, subUnitId).success) {
      return false;
    }
    const mutationParams = {
      unitId,
      subUnitId,
      ruleId,
      payload: {
        type: 0 /* SETTING */,
        payload: {
          ...setting,
          ...validator.normalizeFormula(newRule, unitId, subUnitId)
        }
      }
    };
    const redoMutations = [{
      id: UpdateDataValidationMutation.id,
      params: mutationParams
    }];
    const undoMutationParams = {
      unitId,
      subUnitId,
      ruleId,
      payload: {
        type: 0 /* SETTING */,
        payload: getRuleSetting(rule)
      }
    };
    const undoMutations = [{
      id: UpdateDataValidationMutation.id,
      params: undoMutationParams
    }];
    redoUndoService.pushUndoRedo({
      unitID: unitId,
      redoMutations,
      undoMutations
    });
    commandService.executeCommand(UpdateDataValidationMutation.id, mutationParams);
    return true;
  }
};
var RemoveAllDataValidationCommand = {
  type: 0 /* COMMAND */,
  id: "data-validation.command.removeAll",
  handler(accessor, params) {
    const logService = accessor.get(ILogService);
    logService.error("[Deprecated]: `RemoveAllDataValidationCommand` is deprecated, please use `RemoveSheetAllDataValidationCommand` in `@univerjs/sheets-data-validation` instead!");
    if (!params) {
      return false;
    }
    const { unitId, subUnitId } = params;
    const commandService = accessor.get(ICommandService);
    const dataValidationModel = accessor.get(DataValidationModel);
    const undoRedoService = accessor.get(IUndoRedoService);
    const currentRules = [...dataValidationModel.getRules(unitId, subUnitId)];
    const redoParams = {
      unitId,
      subUnitId,
      ruleId: currentRules.map((rule) => rule.uid)
    };
    const redoMutations = [{
      id: RemoveDataValidationMutation.id,
      params: redoParams
    }];
    const undoMutations = [{
      id: AddDataValidationMutation.id,
      params: {
        unitId,
        subUnitId,
        rule: currentRules
      }
    }];
    undoRedoService.pushUndoRedo({
      redoMutations,
      undoMutations,
      unitID: unitId
    });
    commandService.executeCommand(RemoveDataValidationMutation.id, redoParams);
    return true;
  }
};

// ../packages/data-validation/src/config/config.ts
var DATA_VALIDATION_PLUGIN_CONFIG_KEY = "data-validation.config";
var configSymbol4 = Symbol(DATA_VALIDATION_PLUGIN_CONFIG_KEY);
var defaultPluginConfig4 = {};

// ../packages/data-validation/src/plugin.ts
var UniverDataValidationPlugin = class extends Plugin {
  constructor(_config = defaultPluginConfig4, _injector, _commandService, _configService) {
    super();
    __publicField(this, "_config", _config);
    __publicField(this, "_injector", _injector);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_configService", _configService);
    const { ...rest } = merge_default(
      {},
      defaultPluginConfig4,
      this._config
    );
    this._configService.setConfig(DATA_VALIDATION_PLUGIN_CONFIG_KEY, rest);
  }
  onStarting() {
    [
      [DataValidationModel],
      [DataValidatorRegistryService],
      [DataValidationResourceController]
    ].forEach((d) => this._injector.add(d));
    [
      // command
      AddDataValidationCommand,
      RemoveAllDataValidationCommand,
      UpdateDataValidationOptionsCommand,
      UpdateDataValidationSettingCommand,
      RemoveDataValidationCommand,
      // mutation
      AddDataValidationMutation,
      UpdateDataValidationMutation,
      RemoveDataValidationMutation
    ].forEach((command) => {
      this._commandService.registerCommand(command);
    });
  }
  onReady() {
    this._injector.get(DataValidationResourceController);
  }
};
__publicField(UniverDataValidationPlugin, "pluginName", "UNIVER_DATA_VALIDATION_PLUGIN");
__publicField(UniverDataValidationPlugin, "packageName", package_default4.name);
__publicField(UniverDataValidationPlugin, "version", package_default4.version);
__publicField(UniverDataValidationPlugin, "type", 2 /* UNIVER_SHEET */);
UniverDataValidationPlugin = __decorateClass([
  __decorateParam(1, Inject(Injector)),
  __decorateParam(2, ICommandService),
  __decorateParam(3, IConfigService)
], UniverDataValidationPlugin);

// ../packages/data-validation/src/types/const/two-formula-operators.ts
var TWO_FORMULA_OPERATOR_COUNT = [
  "between" /* BETWEEN */,
  "notBetween" /* NOT_BETWEEN */
];

// ../packages/data-validation/src/types/const/operator-text-map.ts
var OperatorTextMap = {
  ["between" /* BETWEEN */]: "data-validation.operators.between",
  ["equal" /* EQUAL */]: "data-validation.operators.equal",
  ["greaterThan" /* GREATER_THAN */]: "data-validation.operators.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "data-validation.operators.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "data-validation.operators.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "data-validation.operators.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "data-validation.operators.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "data-validation.operators.notEqual"
};
var OperatorTitleMap = {
  ["between" /* BETWEEN */]: "data-validation.ruleName.between",
  ["equal" /* EQUAL */]: "data-validation.ruleName.equal",
  ["greaterThan" /* GREATER_THAN */]: "data-validation.ruleName.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "data-validation.ruleName.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "data-validation.ruleName.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "data-validation.ruleName.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "data-validation.ruleName.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "data-validation.ruleName.notEqual",
  NONE: "data-validation.ruleName.legal"
};
var OperatorErrorTitleMap = {
  ["between" /* BETWEEN */]: "data-validation.errorMsg.between",
  ["equal" /* EQUAL */]: "data-validation.errorMsg.equal",
  ["greaterThan" /* GREATER_THAN */]: "data-validation.errorMsg.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "data-validation.errorMsg.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "data-validation.errorMsg.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "data-validation.errorMsg.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "data-validation.errorMsg.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "data-validation.errorMsg.notEqual",
  NONE: "data-validation.errorMsg.legal"
};

// ../packages/data-validation/src/validators/base-data-validator.ts
var FORMULA1 = "{FORMULA1}";
var FORMULA2 = "{FORMULA2}";
var TYPE = "{TYPE}";
var operatorNameMap = {
  ["between" /* BETWEEN */]: "data-validation.operators.between",
  ["equal" /* EQUAL */]: "data-validation.operators.equal",
  ["greaterThan" /* GREATER_THAN */]: "data-validation.operators.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "data-validation.operators.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "data-validation.operators.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "data-validation.operators.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "data-validation.operators.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "data-validation.operators.notEqual"
};
var BaseDataValidator = class {
  // #endregion
  constructor(localeService, injector) {
    __publicField(this, "localeService", localeService);
    __publicField(this, "injector", injector);
    __publicField(this, "offsetFormulaByRange", true);
    // #region UI related
    __publicField(this, "formulaInput");
    __publicField(this, "canvasRender", null);
    __publicField(this, "dropdownType");
    __publicField(this, "optionsInput");
    __publicField(this, "skipDefaultFontRender");
  }
  get operatorNames() {
    return this.operators.map((operator) => this.localeService.t(operatorNameMap[operator]));
  }
  get titleStr() {
    return this.localeService.t(this.title);
  }
  generateRuleName(rule) {
    var _a, _b;
    if (!rule.operator) {
      return this.localeService.t(OperatorTitleMap.NONE).replace(TYPE, this.titleStr);
    }
    const ruleName = this.localeService.t(OperatorTitleMap[rule.operator]).replace(FORMULA1, (_a = rule.formula1) != null ? _a : "").replace(FORMULA2, (_b = rule.formula2) != null ? _b : "");
    return `${this.titleStr} ${ruleName}`;
  }
  generateRuleErrorMessage(rule, position) {
    var _a, _b;
    if (!rule.operator) {
      return this.localeService.t(OperatorErrorTitleMap.NONE).replace(TYPE, this.titleStr);
      ;
    }
    const errorMsg = this.localeService.t(OperatorErrorTitleMap[rule.operator]).replace(FORMULA1, (_a = rule.formula1) != null ? _a : "").replace(FORMULA2, (_b = rule.formula2) != null ? _b : "");
    return `${errorMsg}`;
  }
  getExtraStyle(rule, value, ctx, row, column) {
  }
  getRuleFinalError(rule, position) {
    if (rule.showErrorMessage && rule.error) {
      return rule.error;
    }
    return this.generateRuleErrorMessage(rule, position);
  }
  isEmptyCellValue(cellValue) {
    if (cellValue === "" || cellValue === void 0 || cellValue === null) {
      return true;
    }
    return false;
  }
  normalizeFormula(rule, unitId, subUnitId) {
    return {
      formula1: rule.formula1,
      formula2: rule.formula2
    };
  }
  async isValidType(cellInfo, formula, rule) {
    return true;
  }
  transform(cellInfo, formula, rule) {
    return cellInfo;
  }
  async validatorIsEqual(cellInfo, formula, rule) {
    const { formula1 } = formula;
    const { value: cellValue } = cellInfo;
    if (Number.isNaN(formula1)) {
      return true;
    }
    return cellValue === formula1;
  }
  async validatorIsNotEqual(cellInfo, formula, _rule) {
    const { formula1 } = formula;
    if (Number.isNaN(formula1)) {
      return true;
    }
    return cellInfo.value !== formula1;
  }
  async validatorIsBetween(cellInfo, formula, _rule) {
    const { formula1, formula2 } = formula;
    if (Number.isNaN(formula1) || Number.isNaN(formula2)) {
      return true;
    }
    const start = Math.min(formula1, formula2);
    const end = Math.max(formula1, formula2);
    return cellInfo.value >= start && cellInfo.value <= end;
  }
  async validatorIsNotBetween(cellInfo, formula, _rule) {
    const { formula1, formula2 } = formula;
    if (Number.isNaN(formula1) || Number.isNaN(formula2)) {
      return true;
    }
    const start = Math.min(formula1, formula2);
    const end = Math.max(formula1, formula2);
    return cellInfo.value < start || cellInfo.value > end;
  }
  async validatorIsGreaterThan(cellInfo, formula, _rule) {
    const { formula1 } = formula;
    if (Number.isNaN(formula1)) {
      return true;
    }
    return cellInfo.value > formula1;
  }
  async validatorIsGreaterThanOrEqual(cellInfo, formula, _rule) {
    const { formula1 } = formula;
    if (Number.isNaN(formula1)) {
      return true;
    }
    return cellInfo.value >= formula1;
  }
  async validatorIsLessThan(cellInfo, formula, _rule) {
    const { formula1 } = formula;
    if (Number.isNaN(formula1)) {
      return true;
    }
    return cellInfo.value < formula1;
  }
  async validatorIsLessThanOrEqual(cellInfo, formula, _rule) {
    const { formula1 } = formula;
    if (Number.isNaN(formula1)) {
      return true;
    }
    return cellInfo.value <= formula1;
  }
  async validator(cellInfo, rule) {
    const { value: cellValue, unitId, subUnitId } = cellInfo;
    const isEmpty = this.isEmptyCellValue(cellValue);
    const { allowBlank = true, operator } = rule;
    if (isEmpty) {
      return allowBlank;
    }
    const formulaInfo = await this.parseFormula(rule, unitId, subUnitId, cellInfo.row, cellInfo.column);
    if (!formulaInfo.isFormulaValid) {
      return false;
    }
    if (!await this.isValidType(cellInfo, formulaInfo, rule)) {
      return false;
    }
    if (!operator) {
      return true;
    }
    const transformedCell = this.transform(cellInfo, formulaInfo, rule);
    switch (operator) {
      case "between" /* BETWEEN */:
        return this.validatorIsBetween(transformedCell, formulaInfo, rule);
      case "equal" /* EQUAL */:
        return this.validatorIsEqual(transformedCell, formulaInfo, rule);
      case "greaterThan" /* GREATER_THAN */:
        return this.validatorIsGreaterThan(transformedCell, formulaInfo, rule);
      case "greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */:
        return this.validatorIsGreaterThanOrEqual(transformedCell, formulaInfo, rule);
      case "lessThan" /* LESS_THAN */:
        return this.validatorIsLessThan(transformedCell, formulaInfo, rule);
      case "lessThanOrEqual" /* LESS_THAN_OR_EQUAL */:
        return this.validatorIsLessThanOrEqual(transformedCell, formulaInfo, rule);
      case "notBetween" /* NOT_BETWEEN */:
        return this.validatorIsNotBetween(transformedCell, formulaInfo, rule);
      case "notEqual" /* NOT_EQUAL */:
        return this.validatorIsNotEqual(transformedCell, formulaInfo, rule);
      default:
        throw new Error("Unknown operator.");
    }
  }
};
BaseDataValidator = __decorateClass([
  __decorateParam(0, Inject(LocaleService)),
  __decorateParam(1, Inject(Injector))
], BaseDataValidator);

// ../packages/sheets-data-validation/package.json
var package_default5 = {
  name: "@univerjs/sheets-data-validation",
  version: "0.25.2",
  private: false,
  description: "Sheet-specific data validation commands, services, and Facade APIs for Univer Sheets.",
  author: "DreamNum Co., Ltd. <developer@univer.ai>",
  license: "Apache-2.0",
  funding: {
    type: "opencollective",
    url: "https://opencollective.com/univer"
  },
  homepage: "https://univer.ai",
  repository: {
    type: "git",
    url: "https://github.com/dream-num/univer"
  },
  bugs: {
    url: "https://github.com/dream-num/univer/issues"
  },
  keywords: [
    "univer",
    "sheets",
    "data-validation",
    "validation",
    "plugin"
  ],
  exports: {
    ".": "./src/index.ts",
    "./*": "./src/*",
    "./facade": "./src/facade/index.ts",
    "./locale/*": "./src/locale/*.ts"
  },
  main: "./src/index.ts",
  types: "./lib/types/index.d.ts",
  publishConfig: {
    access: "public",
    main: "./lib/es/index.js",
    module: "./lib/es/index.js",
    exports: {
      ".": {
        import: "./lib/es/index.js",
        require: "./lib/cjs/index.js",
        types: "./lib/types/index.d.ts"
      },
      "./*": {
        import: "./lib/es/*",
        require: "./lib/cjs/*",
        types: "./lib/types/index.d.ts"
      },
      "./locale/*": {
        import: "./lib/es/locale/*.js",
        require: "./lib/cjs/locale/*.js",
        types: "./lib/types/locale/*.d.ts"
      },
      "./facade": {
        import: "./lib/es/facade.js",
        require: "./lib/cjs/facade.js",
        types: "./lib/types/facade/index.d.ts"
      },
      "./lib/facade": {
        import: "./lib/es/facade.js",
        require: "./lib/cjs/facade.js",
        types: "./lib/types/facade/index.d.ts"
      },
      "./lib/*": "./lib/*"
    }
  },
  directories: {
    lib: "lib"
  },
  files: [
    "lib"
  ],
  scripts: {
    dev: "vite",
    test: "vitest run",
    "test:watch": "vitest",
    coverage: "vitest run --coverage",
    "build:bundle": "univer-cli build",
    "build:types": "tsc -p tsconfig.node.json",
    build: "pnpm run build:bundle && pnpm run build:types",
    typecheck: "tsc --noEmit"
  },
  peerDependencies: {
    rxjs: ">=7.0.0"
  },
  dependencies: {
    "@univerjs/core": "workspace:*",
    "@univerjs/data-validation": "workspace:*",
    "@univerjs/engine-formula": "workspace:*",
    "@univerjs/protocol": "workspace:*",
    "@univerjs/sheets": "workspace:*",
    "@univerjs/sheets-formula": "workspace:*"
  },
  devDependencies: {
    "@univerjs-infra/shared": "workspace:*",
    rxjs: "^7.8.2",
    typescript: "^6.0.3",
    vitest: "^4.1.7"
  }
};

// ../packages/sheets-data-validation/src/services/dv-cache.service.ts
var DataValidationCacheService = class extends Disposable {
  constructor(_commandService, _univerInstanceService, _sheetDataValidationModel) {
    super();
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_sheetDataValidationModel", _sheetDataValidationModel);
    __publicField(this, "_cacheMatrix", /* @__PURE__ */ new Map());
    __publicField(this, "_dirtyRanges$", new Subject());
    __publicField(this, "dirtyRanges$", this._dirtyRanges$.asObservable());
    this._initDirtyRanges();
    this._initSheetRemove();
  }
  _initDirtyRanges() {
    this.disposeWithMe(this._commandService.onCommandExecuted((commandInfo, options) => {
      if (commandInfo.id === SetRangeValuesMutation.id && !(options == null ? void 0 : options.onlyLocal)) {
        const { cellValue, unitId, subUnitId } = commandInfo.params;
        if (cellValue) {
          const range = new ObjectMatrix(cellValue).getDataRange();
          if (range.endRow === -1) return;
          const rules = this._sheetDataValidationModel.getRules(unitId, subUnitId);
          const ranges = [];
          for (const rule of rules) {
            ranges.push(...rule.ranges);
          }
          const intersectsRanges = [];
          for (const ruleRange of ranges) {
            const intersect = getIntersectRange(ruleRange, range);
            if (intersect) {
              intersectsRanges.push(intersect);
            }
          }
          if (intersectsRanges.length) {
            this.markRangeDirty(unitId, subUnitId, intersectsRanges, true);
          }
        }
      }
    }));
  }
  _initSheetRemove() {
    this.disposeWithMe(this._commandService.onCommandExecuted((commandInfo) => {
      var _a;
      if (commandInfo.id === RemoveSheetMutation.id) {
        const { unitId, subUnitId } = commandInfo.params;
        (_a = this._cacheMatrix.get(unitId)) == null ? void 0 : _a.delete(subUnitId);
      }
    }));
    this.disposeWithMe(this._univerInstanceService.unitDisposed$.subscribe((univerInstance) => {
      if (univerInstance.type === 2 /* UNIVER_SHEET */) {
        this._cacheMatrix.delete(univerInstance.getUnitId());
      }
    }));
  }
  _ensureCache(unitId, subUnitId) {
    let unitMap = this._cacheMatrix.get(unitId);
    if (!unitMap) {
      unitMap = /* @__PURE__ */ new Map();
      this._cacheMatrix.set(unitId, unitMap);
    }
    let cacheMatrix = unitMap.get(subUnitId);
    if (!cacheMatrix) {
      cacheMatrix = new ObjectMatrix();
      unitMap.set(subUnitId, cacheMatrix);
    }
    return cacheMatrix;
  }
  ensureCache(unitId, subUnitId) {
    return this._ensureCache(unitId, subUnitId);
  }
  addRule(unitId, subUnitId, rule) {
    this.markRangeDirty(unitId, subUnitId, rule.ranges);
  }
  removeRule(unitId, subUnitId, rule) {
    this._deleteRange(unitId, subUnitId, rule.ranges);
  }
  markRangeDirty(unitId, subUnitId, ranges, isSetRange) {
    const cache = this._ensureCache(unitId, subUnitId);
    ranges.forEach((range) => {
      Range.foreach(range, (row, col) => {
        if (cache.getValue(row, col) !== void 0) {
          cache.setValue(row, col, void 0);
        }
      });
    });
    this._dirtyRanges$.next({ unitId, subUnitId, ranges, isSetRange });
  }
  _deleteRange(unitId, subUnitId, ranges) {
    const cache = this._ensureCache(unitId, subUnitId);
    ranges.forEach((range) => {
      Range.foreach(range, (row, col) => {
        cache.realDeleteValue(row, col);
      });
    });
    this._dirtyRanges$.next({ unitId, subUnitId, ranges });
  }
  getValue(unitId, subUnitId, row, col) {
    const cache = this._ensureCache(unitId, subUnitId);
    return cache.getValue(row, col);
  }
};
DataValidationCacheService = __decorateClass([
  __decorateParam(0, Inject(ICommandService)),
  __decorateParam(1, Inject(IUniverInstanceService)),
  __decorateParam(2, Inject(DataValidationModel))
], DataValidationCacheService);

// ../packages/sheets-data-validation/src/utils/formula.ts
function getFormulaResult(result) {
  var _a, _b;
  return (_b = (_a = result == null ? void 0 : result[0]) == null ? void 0 : _a[0]) == null ? void 0 : _b.v;
}
function getFormulaCellData(result) {
  var _a;
  return (_a = result == null ? void 0 : result[0]) == null ? void 0 : _a[0];
}
function isLegalFormulaResult(res) {
  return !ERROR_TYPE_SET.has(res);
}
function shouldOffsetFormulaByRange(type2, validatorRegistryService) {
  var _a;
  const validator = validatorRegistryService.getValidatorItem(type2);
  return (_a = validator == null ? void 0 : validator.offsetFormulaByRange) != null ? _a : false;
}

// ../packages/sheets-data-validation/src/services/dv-custom-formula.service.ts
var DataValidationCustomFormulaService = class extends Disposable {
  constructor(_instanceSrv, _registerOtherFormulaService, _dataValidationModel, _dataValidationCacheService, _validatorRegistryService) {
    super();
    __publicField(this, "_instanceSrv", _instanceSrv);
    __publicField(this, "_registerOtherFormulaService", _registerOtherFormulaService);
    __publicField(this, "_dataValidationModel", _dataValidationModel);
    __publicField(this, "_dataValidationCacheService", _dataValidationCacheService);
    __publicField(this, "_validatorRegistryService", _validatorRegistryService);
    /**
     * Map of origin formula of rule
     */
    __publicField(this, "_ruleFormulaMap", /* @__PURE__ */ new Map());
    __publicField(this, "_ruleFormulaMap2", /* @__PURE__ */ new Map());
    this._initFormulaResultHandler();
    this._initDirtyRanges();
  }
  dispose() {
    super.dispose();
    this._ruleFormulaMap.clear();
    this._ruleFormulaMap2.clear();
  }
  _initFormulaResultHandler() {
    this.disposeWithMe(this._registerOtherFormulaService.formulaResult$.subscribe((resultMap) => {
      for (const unitId in resultMap) {
        const unitMap = resultMap[unitId];
        const type2 = this._instanceSrv.getUnitType(unitId);
        if (type2 !== 2 /* UNIVER_SHEET */) continue;
        for (const subUnitId in unitMap) {
          const results = unitMap[subUnitId];
          const { ruleFormulaMap } = this._ensureMaps(unitId, subUnitId);
          results.forEach((result) => {
            var _a, _b;
            const ruleInfo = ruleFormulaMap.get((_a = result.extra) == null ? void 0 : _a.ruleId);
            const rule = this._dataValidationModel.getRuleById(unitId, subUnitId, (_b = result.extra) == null ? void 0 : _b.ruleId);
            if (rule && ruleInfo) {
              this._dataValidationCacheService.markRangeDirty(unitId, subUnitId, rule.ranges);
            }
          });
        }
      }
    }));
  }
  _ensureMaps(unitId, subUnitId) {
    let ruleFormulaUnitMap = this._ruleFormulaMap.get(unitId);
    let ruleFormulaUnitMap2 = this._ruleFormulaMap2.get(unitId);
    if (!ruleFormulaUnitMap) {
      ruleFormulaUnitMap = /* @__PURE__ */ new Map();
      this._ruleFormulaMap.set(unitId, ruleFormulaUnitMap);
    }
    if (!ruleFormulaUnitMap2) {
      ruleFormulaUnitMap2 = /* @__PURE__ */ new Map();
      this._ruleFormulaMap2.set(unitId, ruleFormulaUnitMap2);
    }
    let ruleFormulaMap = ruleFormulaUnitMap.get(subUnitId);
    if (!ruleFormulaMap) {
      ruleFormulaMap = /* @__PURE__ */ new Map();
      ruleFormulaUnitMap.set(subUnitId, ruleFormulaMap);
    }
    let ruleFormulaMap2 = ruleFormulaUnitMap2.get(subUnitId);
    if (!ruleFormulaMap2) {
      ruleFormulaMap2 = /* @__PURE__ */ new Map();
      ruleFormulaUnitMap2.set(subUnitId, ruleFormulaMap2);
    }
    return { ruleFormulaMap, ruleFormulaMap2 };
  }
  _registerFormula(unitId, subUnitId, ruleId, formulaString, ranges) {
    return this._registerOtherFormulaService.registerFormulaWithRange(unitId, subUnitId, formulaString, ranges, { ruleId }, "dv-custom" /* DATA_VALIDATION_CUSTOM */, ruleId);
  }
  _handleDirtyRanges(unitId, subUnitId, ranges) {
    const rules = this._dataValidationModel.getRules(unitId, subUnitId);
    rules.forEach((rule) => {
      const ruleRanges = rule.ranges;
      const hasOverLap = Rectangle.doAnyRangesIntersect(ruleRanges, ranges);
      if (hasOverLap) {
        this.makeRuleDirty(unitId, subUnitId, rule.uid);
      }
    });
  }
  _initDirtyRanges() {
    this.disposeWithMe(this._dataValidationCacheService.dirtyRanges$.subscribe((data) => {
      if (data.isSetRange) {
        this._handleDirtyRanges(data.unitId, data.subUnitId, data.ranges);
      }
    }));
  }
  deleteByRuleId(unitId, subUnitId, ruleId) {
    const { ruleFormulaMap, ruleFormulaMap2 } = this._ensureMaps(unitId, subUnitId);
    const rule = this._dataValidationModel.getRuleById(unitId, subUnitId, ruleId);
    const formulaInfo = ruleFormulaMap.get(ruleId);
    if (!rule || !formulaInfo) {
      return;
    }
    const current = ruleFormulaMap.get(ruleId);
    if (current) {
      ruleFormulaMap.delete(ruleId);
      this._registerOtherFormulaService.deleteFormula(unitId, subUnitId, [current.formulaId]);
    }
    const current2 = ruleFormulaMap2.get(ruleId);
    if (current2) {
      ruleFormulaMap2.delete(ruleId);
      this._registerOtherFormulaService.deleteFormula(unitId, subUnitId, [current2.formulaId]);
    }
  }
  _addFormulaByRange(unitId, subUnitId, ruleId, formula, formula2, ranges) {
    const { ruleFormulaMap, ruleFormulaMap2 } = this._ensureMaps(unitId, subUnitId);
    const originRow = ranges[0].startRow;
    const originCol = ranges[0].startColumn;
    if (formula && isFormulaString(formula)) {
      const formulaId = this._registerFormula(unitId, subUnitId, ruleId, formula, ranges);
      ruleFormulaMap.set(ruleId, {
        formula,
        originCol,
        originRow,
        formulaId
      });
    }
    if (formula2 && isFormulaString(formula2)) {
      const formulaId2 = this._registerFormula(unitId, subUnitId, ruleId, formula2, ranges);
      ruleFormulaMap2.set(ruleId, {
        formula: formula2,
        originCol,
        originRow,
        formulaId: formulaId2
      });
    }
  }
  addRule(unitId, subUnitId, rule) {
    if (shouldOffsetFormulaByRange(rule.type, this._validatorRegistryService)) {
      const { ranges, formula1, formula2, uid: ruleId } = rule;
      this._addFormulaByRange(unitId, subUnitId, ruleId, formula1, formula2, ranges);
    }
  }
  async getCellFormulaValue(unitId, subUnitId, ruleId, row, column) {
    var _a, _b;
    const { ruleFormulaMap } = this._ensureMaps(unitId, subUnitId);
    const current = ruleFormulaMap.get(ruleId);
    if (!current) {
      return Promise.resolve(void 0);
    }
    const result = await this._registerOtherFormulaService.getFormulaValue(unitId, subUnitId, current.formulaId);
    const { originRow, originCol } = current;
    const offsetRow = row - originRow;
    const offsetCol = column - originCol;
    return getFormulaCellData((_b = (_a = result == null ? void 0 : result.result) == null ? void 0 : _a[offsetRow]) == null ? void 0 : _b[offsetCol]);
  }
  async getCellFormula2Value(unitId, subUnitId, ruleId, row, column) {
    var _a, _b;
    const { ruleFormulaMap2 } = this._ensureMaps(unitId, subUnitId);
    const current = ruleFormulaMap2.get(ruleId);
    if (!current) {
      return Promise.resolve(void 0);
    }
    const result = await this._registerOtherFormulaService.getFormulaValue(unitId, subUnitId, current.formulaId);
    const { originRow, originCol } = current;
    const offsetRow = row - originRow;
    const offsetCol = column - originCol;
    return getFormulaCellData((_b = (_a = result == null ? void 0 : result.result) == null ? void 0 : _a[offsetRow]) == null ? void 0 : _b[offsetCol]);
  }
  getCellFormulaValueSync(unitId, subUnitId, ruleId, row, column) {
    var _a, _b;
    const { ruleFormulaMap } = this._ensureMaps(unitId, subUnitId);
    const current = ruleFormulaMap.get(ruleId);
    if (!current) {
      return void 0;
    }
    const result = this._registerOtherFormulaService.getFormulaValueSync(unitId, subUnitId, current.formulaId);
    const { originRow, originCol } = current;
    const offsetRow = row - originRow;
    const offsetCol = column - originCol;
    return getFormulaCellData((_b = (_a = result == null ? void 0 : result.result) == null ? void 0 : _a[offsetRow]) == null ? void 0 : _b[offsetCol]);
  }
  getCellFormula2ValueSync(unitId, subUnitId, ruleId, row, column) {
    var _a, _b;
    const { ruleFormulaMap2 } = this._ensureMaps(unitId, subUnitId);
    const current = ruleFormulaMap2.get(ruleId);
    if (!current) {
      return void 0;
    }
    const result = this._registerOtherFormulaService.getFormulaValueSync(unitId, subUnitId, current.formulaId);
    const { originRow, originCol } = current;
    const offsetRow = row - originRow;
    const offsetCol = column - originCol;
    return getFormulaCellData((_b = (_a = result == null ? void 0 : result.result) == null ? void 0 : _a[offsetRow]) == null ? void 0 : _b[offsetCol]);
  }
  getRuleFormulaInfo(unitId, subUnitId, ruleId) {
    const { ruleFormulaMap } = this._ensureMaps(unitId, subUnitId);
    return ruleFormulaMap.get(ruleId);
  }
  makeRuleDirty(unitId, subUnitId, ruleId) {
    var _a, _b, _c, _d;
    const formula1 = (_b = (_a = this._ruleFormulaMap.get(unitId)) == null ? void 0 : _a.get(subUnitId)) == null ? void 0 : _b.get(ruleId);
    const formula2 = (_d = (_c = this._ruleFormulaMap2.get(unitId)) == null ? void 0 : _c.get(subUnitId)) == null ? void 0 : _d.get(ruleId);
    if (formula1) {
      this._registerOtherFormulaService.markFormulaDirty(unitId, subUnitId, formula1.formulaId);
    }
    if (formula2) {
      this._registerOtherFormulaService.markFormulaDirty(unitId, subUnitId, formula2.formulaId);
    }
  }
};
DataValidationCustomFormulaService = __decorateClass([
  __decorateParam(0, IUniverInstanceService),
  __decorateParam(1, Inject(RegisterOtherFormulaService)),
  __decorateParam(2, Inject(DataValidationModel)),
  __decorateParam(3, Inject(DataValidationCacheService)),
  __decorateParam(4, Inject(DataValidatorRegistryService))
], DataValidationCustomFormulaService);

// ../packages/sheets-data-validation/src/utils/get-cell-data-origin.ts
function getCellValueOrigin(cell) {
  return getOriginCellValue(cell);
}
function getStringCellValue(cell) {
  var _a;
  return String((_a = getCellValueOrigin(cell)) != null ? _a : "");
}

// ../packages/sheets-data-validation/src/services/dv-list-cache.service.ts
var DataValidationListCacheService = class extends Disposable {
  constructor(_injector, _dataValidationModel) {
    super();
    __publicField(this, "_injector", _injector);
    __publicField(this, "_dataValidationModel", _dataValidationModel);
    // Cache structure: unitId -> subUnitId -> ruleId -> cache item
    __publicField(this, "_cache", /* @__PURE__ */ new Map());
    this._initRuleChangeListener();
  }
  _initRuleChangeListener() {
    this.disposeWithMe(
      this._dataValidationModel.ruleChange$.subscribe((change) => {
        if (change.type === "remove" || change.type === "update") {
          this.markRuleDirty(change.unitId, change.subUnitId, change.rule.uid);
        }
      })
    );
  }
  /**
   * Get cached list data or compute and cache it if not exists.
   */
  getOrCompute(unitId, subUnitId, rule) {
    const cached = this.getCache(unitId, subUnitId, rule.uid);
    if (cached) {
      return cached;
    }
    const formulaService = this._injector.get(DataValidationFormulaService);
    const results = formulaService.getRuleFormulaResultSync(unitId, subUnitId, rule.uid);
    return this.computeAndCache(unitId, subUnitId, rule, results);
  }
  _ensureCache(unitId, subUnitId) {
    let unitMap = this._cache.get(unitId);
    if (!unitMap) {
      unitMap = /* @__PURE__ */ new Map();
      this._cache.set(unitId, unitMap);
    }
    let subUnitMap = unitMap.get(subUnitId);
    if (!subUnitMap) {
      subUnitMap = /* @__PURE__ */ new Map();
      unitMap.set(subUnitId, subUnitMap);
    }
    return subUnitMap;
  }
  /**
   * Get cached list data for a rule. Returns undefined if not cached.
   */
  getCache(unitId, subUnitId, ruleId) {
    var _a, _b;
    return (_b = (_a = this._cache.get(unitId)) == null ? void 0 : _a.get(subUnitId)) == null ? void 0 : _b.get(ruleId);
  }
  /**
   * Set cache for a rule.
   */
  setCache(unitId, subUnitId, ruleId, item) {
    const cache = this._ensureCache(unitId, subUnitId);
    cache.set(ruleId, item);
  }
  /**
   * Mark a rule's cache as dirty (invalidate it).
   * Called when formula results change.
   */
  markRuleDirty(unitId, subUnitId, ruleId) {
    var _a, _b;
    (_b = (_a = this._cache.get(unitId)) == null ? void 0 : _a.get(subUnitId)) == null ? void 0 : _b.delete(ruleId);
  }
  /**
   * Clear all caches.
   */
  clear() {
    this._cache.clear();
  }
  /**
   * Compute list data from formula result and cache it.
   */
  computeAndCache(unitId, subUnitId, rule, formulaResult) {
    var _a, _b, _c;
    const { formula1 = "", formula2 = "" } = rule;
    const list = isFormulaString(formula1) ? this._getRuleFormulaResultSet((_c = (_b = (_a = formulaResult == null ? void 0 : formulaResult[0]) == null ? void 0 : _a.result) == null ? void 0 : _b[0]) == null ? void 0 : _c[0]) : deserializeListOptions(formula1);
    const colorList = formula2.split(",");
    const listWithColor = list.map((label, i) => ({ label, color: colorList[i] || "" }));
    const colorMap = {};
    for (const item of listWithColor) {
      if (item.color) {
        colorMap[item.label] = item.color;
      }
    }
    const set = new Set(list);
    const cacheItem = { list, listWithColor, colorMap, set };
    this.setCache(unitId, subUnitId, rule.uid, cacheItem);
    return cacheItem;
  }
  /**
   * Extract string list from formula result cells.
   */
  _getRuleFormulaResultSet(result) {
    var _a, _b;
    if (!result) {
      return [];
    }
    const resultSet = /* @__PURE__ */ new Set();
    for (let i = 0, rowLen = result.length; i < rowLen; i++) {
      const row = result[i];
      if (!row) continue;
      for (let j = 0, colLen = row.length; j < colLen; j++) {
        const cell = row[j];
        const value = getCellValueOrigin(cell);
        if (value !== null && value !== void 0) {
          if (typeof value !== "string" && typeof (cell == null ? void 0 : cell.s) === "object" && ((_b = (_a = cell.s) == null ? void 0 : _a.n) == null ? void 0 : _b.pattern)) {
            resultSet.add(lib_exports.format(cell.s.n.pattern, value, { throws: false }));
            continue;
          }
          const valueStr = typeof value === "string" ? value : String(value);
          if (isLegalFormulaResult(valueStr)) {
            resultSet.add(valueStr);
          }
        }
      }
    }
    return [...resultSet];
  }
};
DataValidationListCacheService = __decorateClass([
  __decorateParam(0, Inject(Injector)),
  __decorateParam(1, Inject(DataValidationModel))
], DataValidationListCacheService);

// ../packages/sheets-data-validation/src/services/dv-formula.service.ts
var DataValidationFormulaService = class extends Disposable {
  constructor(_instanceService, _registerOtherFormulaService, _dataValidationCacheService, _dataValidationModel, _validatorRegistryService, _listCacheService) {
    super();
    __publicField(this, "_instanceService", _instanceService);
    __publicField(this, "_registerOtherFormulaService", _registerOtherFormulaService);
    __publicField(this, "_dataValidationCacheService", _dataValidationCacheService);
    __publicField(this, "_dataValidationModel", _dataValidationModel);
    __publicField(this, "_validatorRegistryService", _validatorRegistryService);
    __publicField(this, "_listCacheService", _listCacheService);
    __publicField(this, "_formulaRuleMap", /* @__PURE__ */ new Map());
    this._initFormulaResultHandler();
  }
  _initFormulaResultHandler() {
    this.disposeWithMe(this._registerOtherFormulaService.formulaResult$.subscribe((resultMap) => {
      for (const unitId in resultMap) {
        const unitMap = resultMap[unitId];
        const type2 = this._instanceService.getUnitType(unitId);
        if (type2 !== 2 /* UNIVER_SHEET */) continue;
        for (const subUnitId in unitMap) {
          const results = unitMap[subUnitId];
          const formulaMap = this._ensureRuleFormulaMap(unitId, subUnitId);
          results.forEach((result) => {
            var _a;
            const ruleId = (_a = result.extra) == null ? void 0 : _a.ruleId;
            if (ruleId && formulaMap.get(ruleId)) {
              const rule = this._dataValidationModel.getRuleById(unitId, subUnitId, ruleId);
              if (rule) {
                this._listCacheService.markRuleDirty(unitId, subUnitId, ruleId);
                this._dataValidationCacheService.markRangeDirty(unitId, subUnitId, rule.ranges);
              }
            }
          });
        }
      }
    }));
  }
  _ensureRuleFormulaMap(unitId, subUnitId) {
    let unitMap = this._formulaRuleMap.get(unitId);
    if (!unitMap) {
      unitMap = /* @__PURE__ */ new Map();
      this._formulaRuleMap.set(unitId, unitMap);
    }
    let subUnitMap = unitMap.get(subUnitId);
    if (!subUnitMap) {
      subUnitMap = /* @__PURE__ */ new Map();
      unitMap.set(subUnitId, subUnitMap);
    }
    return subUnitMap;
  }
  _registerSingleFormula(unitId, subUnitId, formula, ruleId) {
    const ranges = [{ startColumn: 0, endColumn: 0, startRow: 0, endRow: 0 }];
    return this._registerOtherFormulaService.registerFormulaWithRange(unitId, subUnitId, formula, ranges, { ruleId }, "dv" /* DATA_VALIDATION */, ruleId);
  }
  addRule(unitId, subUnitId, rule) {
    if (!shouldOffsetFormulaByRange(rule.type, this._validatorRegistryService) && rule.type !== "checkbox" /* CHECKBOX */) {
      const { formula1, formula2, uid: ruleId } = rule;
      const isFormula1Legal = isFormulaString(formula1);
      const isFormula2Legal = isFormulaString(formula2);
      if (!isFormula1Legal && !isFormula2Legal) {
        return;
      }
      const formulaRuleMap = this._ensureRuleFormulaMap(unitId, subUnitId);
      const item = [void 0, void 0];
      if (isFormula1Legal) {
        const id = this._registerSingleFormula(unitId, subUnitId, formula1, ruleId);
        item[0] = { id, text: formula1 };
      }
      if (isFormula2Legal) {
        const id = this._registerSingleFormula(unitId, subUnitId, formula2, ruleId);
        item[1] = { id, text: formula2 };
      }
      formulaRuleMap.set(ruleId, item);
    }
  }
  removeRule(unitId, subUnitId, ruleId) {
    const formulaRuleMap = this._ensureRuleFormulaMap(unitId, subUnitId);
    const item = formulaRuleMap.get(ruleId);
    if (!item) {
      return;
    }
    const [formula1, formula2] = item;
    const idList = [formula1 == null ? void 0 : formula1.id, formula2 == null ? void 0 : formula2.id].filter(Boolean);
    idList.length && this._registerOtherFormulaService.deleteFormula(unitId, subUnitId, idList);
  }
  getRuleFormulaResult(unitId, subUnitId, ruleId) {
    const ruleFormulaMap = this._ensureRuleFormulaMap(unitId, subUnitId);
    const formulaInfo = ruleFormulaMap.get(ruleId);
    if (!formulaInfo) {
      return Promise.resolve(null);
    }
    const getResult = async (info) => info && this._registerOtherFormulaService.getFormulaValue(unitId, subUnitId, info.id);
    return Promise.all([
      getResult(formulaInfo[0]),
      getResult(formulaInfo[1])
    ]);
  }
  getRuleFormulaResultSync(unitId, subUnitId, ruleId) {
    const ruleFormulaMap = this._ensureRuleFormulaMap(unitId, subUnitId);
    const formulaInfo = ruleFormulaMap.get(ruleId);
    if (!formulaInfo) {
      return void 0;
    }
    return formulaInfo.map((i) => {
      if (i) {
        return this._registerOtherFormulaService.getFormulaValueSync(unitId, subUnitId, i.id);
      }
      return void 0;
    });
  }
  getRuleFormulaInfo(unitId, subUnitId, ruleId) {
    const ruleFormulaMap = this._ensureRuleFormulaMap(unitId, subUnitId);
    return ruleFormulaMap.get(ruleId);
  }
};
DataValidationFormulaService = __decorateClass([
  __decorateParam(0, IUniverInstanceService),
  __decorateParam(1, Inject(RegisterOtherFormulaService)),
  __decorateParam(2, Inject(DataValidationCacheService)),
  __decorateParam(3, Inject(DataValidationModel)),
  __decorateParam(4, Inject(DataValidatorRegistryService)),
  __decorateParam(5, Inject(DataValidationListCacheService))
], DataValidationFormulaService);

// ../packages/sheets-data-validation/src/models/rule-matrix.ts
var RuleMatrix = class _RuleMatrix {
  constructor(value, _unitId, _subUnitId, _univerInstanceService, _disableTree = false) {
    __publicField(this, "_unitId", _unitId);
    __publicField(this, "_subUnitId", _subUnitId);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_disableTree", _disableTree);
    __publicField(this, "_map");
    __publicField(this, "_tree", new RBush());
    __publicField(this, "_dirty", true);
    __publicField(this, "_buildTree", () => {
      if (!this._dirty || this._disableTree) {
        return;
      }
      this._tree.clear();
      const items = [];
      this._map.forEach((ranges, ruleId) => {
        ranges.forEach((range) => {
          items.push({
            minX: range.startRow,
            maxX: range.endRow,
            minY: range.startColumn,
            maxY: range.endColumn,
            ruleId
          });
        });
      });
      this._tree.load(items);
      this._dirty = false;
    });
    __publicField(this, "_debonceBuildTree", debounce_default(this._buildTree, 0));
    this._map = value;
    this._buildTree();
  }
  get _worksheet() {
    var _a;
    return (_a = this._univerInstanceService.getUnit(this._unitId, 2 /* UNIVER_SHEET */)) == null ? void 0 : _a.getSheetBySheetId(this._subUnitId);
  }
  _addRule(ruleId, _ranges) {
    if (!this._worksheet) {
      return;
    }
    const ranges = Rectangle.mergeRanges(_ranges.map((range) => Range.transformRange(range, this._worksheet)));
    this._map.forEach((value, key) => {
      const newRanges = Rectangle.subtractMulti(value, ranges);
      if (newRanges.length === 0) {
        this._map.delete(key);
      } else {
        this._map.set(key, newRanges);
      }
    });
    this._dirty = true;
    this._map.set(ruleId, ranges);
    this._debonceBuildTree();
  }
  addRule(rule) {
    this._addRule(rule.uid, rule.ranges);
  }
  removeRange(_ranges) {
    if (!this._worksheet) {
      return;
    }
    const ranges = _ranges.map((range) => Range.transformRange(range, this._worksheet));
    this._map.forEach((value, key) => {
      const newRanges = Rectangle.subtractMulti(value, ranges);
      if (newRanges.length === 0) {
        this._map.delete(key);
      } else {
        this._map.set(key, newRanges);
      }
    });
    this._dirty = true;
    this._debonceBuildTree();
  }
  _removeRule(ruleId) {
    this._map.delete(ruleId);
    this._dirty = true;
    this._debonceBuildTree();
  }
  removeRule(rule) {
    this._removeRule(rule.uid);
  }
  updateRange(ruleId, _newRanges) {
    this._removeRule(ruleId);
    this._addRule(ruleId, _newRanges);
  }
  addRangeRules(rules) {
    rules.forEach(({ id: ruleId, ranges }) => {
      if (!ranges.length) {
        return;
      }
      let current = this._map.get(ruleId);
      if (!current) {
        current = ranges;
        this._map.set(ruleId, current);
      } else {
        this._map.set(ruleId, Rectangle.mergeRanges([...current, ...ranges]));
        current = this._map.get(ruleId);
      }
      this._map.forEach((value, key) => {
        if (key === ruleId) {
          return;
        }
        const newRanges = Rectangle.subtractMulti(value, ranges);
        if (newRanges.length === 0) {
          this._map.delete(key);
        } else {
          this._map.set(key, newRanges);
        }
      });
    });
    this._dirty = true;
    this._debonceBuildTree();
  }
  diff(rules) {
    const mutations = [];
    let deleteIndex = 0;
    rules.forEach((rule, index) => {
      var _a;
      const newRanges = (_a = this._map.get(rule.uid)) != null ? _a : [];
      const oldRanges = rule.ranges;
      if (newRanges.length !== 0 && (newRanges.length !== oldRanges.length || newRanges.some((range, i) => !Rectangle.equals(range, oldRanges[i])))) {
        mutations.push({
          type: "update",
          ruleId: rule.uid,
          oldRanges,
          newRanges: Rectangle.sort(newRanges),
          rule
        });
      }
      if (newRanges.length === 0) {
        mutations.push({
          type: "delete",
          rule,
          index: index - deleteIndex
        });
        deleteIndex++;
      }
    });
    return mutations;
  }
  diffWithAddition(rules, additionRules) {
    const mutations = [];
    let deleteIndex = 0;
    rules.forEach((rule, index) => {
      var _a;
      const newRanges = (_a = this._map.get(rule.uid)) != null ? _a : [];
      const oldRanges = rule.ranges;
      if (newRanges.length !== 0 && (newRanges.length !== oldRanges.length || newRanges.some((range, i) => !Rectangle.equals(range, oldRanges[i])))) {
        mutations.push({
          type: "update",
          ruleId: rule.uid,
          oldRanges,
          newRanges: Rectangle.sort(newRanges),
          rule
        });
      }
      if (newRanges.length === 0) {
        mutations.push({
          type: "delete",
          rule,
          index: index - deleteIndex
        });
        deleteIndex++;
      }
    });
    Array.from(additionRules).forEach((rule) => {
      var _a;
      const newRanges = (_a = this._map.get(rule.uid)) != null ? _a : [];
      mutations.push({
        type: "add",
        rule: {
          ...rule,
          ranges: Rectangle.sort(newRanges)
        }
      });
    });
    return mutations;
  }
  clone() {
    return new _RuleMatrix(
      new Map(Tools.deepClone(Array.from(this._map.entries()))),
      this._unitId,
      this._subUnitId,
      this._univerInstanceService,
      // disable tree on cloned matrix, cause there is no need to search
      true
    );
  }
  getValue(row, col) {
    if (this._dirty) {
      this._buildTree();
    }
    const result = this._tree.search({
      minX: row,
      maxX: row,
      minY: col,
      maxY: col
    });
    return result.length > 0 ? result[0].ruleId : void 0;
  }
};

// ../packages/sheets-data-validation/src/models/sheet-data-validation-model.ts
var SheetDataValidationModel = class extends Disposable {
  constructor(_dataValidationModel, _univerInstanceService, _dataValidatorRegistryService, _dataValidationCacheService, _dataValidationFormulaService, _dataValidationCustomFormulaService, _commandService) {
    super();
    __publicField(this, "_dataValidationModel", _dataValidationModel);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_dataValidatorRegistryService", _dataValidatorRegistryService);
    __publicField(this, "_dataValidationCacheService", _dataValidationCacheService);
    __publicField(this, "_dataValidationFormulaService", _dataValidationFormulaService);
    __publicField(this, "_dataValidationCustomFormulaService", _dataValidationCustomFormulaService);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_ruleMatrixMap", /* @__PURE__ */ new Map());
    __publicField(this, "_validStatusChange$", new Subject());
    __publicField(this, "_ruleChange$", new Subject());
    __publicField(this, "ruleChange$", this._ruleChange$.asObservable());
    __publicField(this, "validStatusChange$", this._validStatusChange$.asObservable());
    this._initRuleUpdateListener();
    this.disposeWithMe(() => {
      this._ruleChange$.complete();
      this._validStatusChange$.complete();
    });
    this._initUniverInstanceListener();
  }
  _initUniverInstanceListener() {
    this.disposeWithMe(
      this._univerInstanceService.unitDisposed$.subscribe((unit) => {
        this._ruleMatrixMap.delete(unit.getUnitId());
      })
    );
    this.disposeWithMe(
      this._commandService.onCommandExecuted((command) => {
        if (command.id === RemoveSheetMutation.id) {
          const { unitId, subUnitId } = command.params;
          const subUnitMap = this._ruleMatrixMap.get(unitId);
          if (subUnitMap) {
            subUnitMap.delete(subUnitId);
          }
        }
      })
    );
  }
  _initRuleUpdateListener() {
    const allRules = this._dataValidationModel.getAll();
    for (const [unitId, subUnitMap] of allRules) {
      for (const [subUnitId, rules] of subUnitMap) {
        for (const rule of rules) {
          this._addRule(unitId, subUnitId, rule);
          this._ruleChange$.next({
            type: "add",
            unitId,
            subUnitId,
            rule,
            source: "patched"
          });
        }
      }
    }
    this.disposeWithMe(
      this._dataValidationModel.ruleChange$.subscribe((ruleChange) => {
        switch (ruleChange.type) {
          case "add":
            this._addRule(ruleChange.unitId, ruleChange.subUnitId, ruleChange.rule);
            break;
          case "update":
            this._updateRule(ruleChange.unitId, ruleChange.subUnitId, ruleChange.rule.uid, ruleChange.oldRule, ruleChange.updatePayload);
            break;
          case "remove":
            this._removeRule(ruleChange.unitId, ruleChange.subUnitId, ruleChange.rule);
            break;
        }
        this._ruleChange$.next(ruleChange);
      })
    );
  }
  _ensureRuleMatrix(unitId, subUnitId) {
    let unitMap = this._ruleMatrixMap.get(unitId);
    if (!unitMap) {
      unitMap = /* @__PURE__ */ new Map();
      this._ruleMatrixMap.set(unitId, unitMap);
    }
    let matrix = unitMap.get(subUnitId);
    if (!matrix) {
      matrix = new RuleMatrix(/* @__PURE__ */ new Map(), unitId, subUnitId, this._univerInstanceService);
      unitMap.set(subUnitId, matrix);
    }
    return matrix;
  }
  _addRuleSideEffect(unitId, subUnitId, rule) {
    const ruleMatrix = this._ensureRuleMatrix(unitId, subUnitId);
    ruleMatrix.addRule(rule);
    this._dataValidationCacheService.addRule(unitId, subUnitId, rule);
    this._dataValidationFormulaService.addRule(unitId, subUnitId, rule);
    this._dataValidationCustomFormulaService.addRule(unitId, subUnitId, rule);
  }
  _addRule(unitId, subUnitId, rule) {
    const rules = Array.isArray(rule) ? rule : [rule];
    rules.forEach((item) => {
      this._addRuleSideEffect(unitId, subUnitId, item);
    });
  }
  _updateRule(unitId, subUnitId, ruleId, oldRule, payload) {
    const ruleMatrix = this._ensureRuleMatrix(unitId, subUnitId);
    const newRule = {
      ...oldRule,
      ...payload.payload
    };
    if (payload.type === 1 /* RANGE */) {
      ruleMatrix.updateRange(ruleId, payload.payload);
    } else if (payload.type === 3 /* ALL */) {
      ruleMatrix.updateRange(ruleId, payload.payload.ranges);
    }
    this._dataValidationCacheService.removeRule(unitId, subUnitId, oldRule);
    this._dataValidationCacheService.addRule(unitId, subUnitId, newRule);
    this._dataValidationFormulaService.removeRule(unitId, subUnitId, oldRule.uid);
    this._dataValidationFormulaService.addRule(unitId, subUnitId, newRule);
    this._dataValidationCustomFormulaService.deleteByRuleId(unitId, subUnitId, ruleId);
    this._dataValidationCustomFormulaService.addRule(unitId, subUnitId, newRule);
  }
  _removeRule(unitId, subUnitId, oldRule) {
    const ruleMatrix = this._ensureRuleMatrix(unitId, subUnitId);
    ruleMatrix.removeRule(oldRule);
    this._dataValidationCacheService.removeRule(unitId, subUnitId, oldRule);
    this._dataValidationCustomFormulaService.deleteByRuleId(unitId, subUnitId, oldRule.uid);
  }
  getValidator(type2) {
    return this._dataValidatorRegistryService.getValidatorItem(type2);
  }
  getRuleIdByLocation(unitId, subUnitId, row, col) {
    const ruleMatrix = this._ensureRuleMatrix(unitId, subUnitId);
    return ruleMatrix.getValue(row, col);
  }
  getRuleByLocation(unitId, subUnitId, row, col) {
    const ruleId = this.getRuleIdByLocation(unitId, subUnitId, row, col);
    if (!ruleId) {
      return void 0;
    }
    return this._dataValidationModel.getRuleById(unitId, subUnitId, ruleId);
  }
  validator(rule, pos, _onCompete) {
    const { col, row, unitId, subUnitId, worksheet } = pos;
    const onCompete = (status, changed) => {
      if (_onCompete) {
        _onCompete(status, changed);
      }
      if (changed) {
        this._validStatusChange$.next({
          unitId,
          subUnitId,
          ruleId: rule.uid,
          status,
          row,
          col
        });
      }
    };
    const cell = worksheet.getCellValueOnly(row, col);
    const validator = this.getValidator(rule.type);
    const cellRaw = worksheet.getCellRaw(row, col);
    const cellValue = getCellValueOrigin(cellRaw);
    if (validator) {
      const cache = this._dataValidationCacheService.ensureCache(unitId, subUnitId);
      const current = cache.getValue(row, col);
      if (current === null || current === void 0) {
        cache.setValue(row, col, "validating" /* VALIDATING */);
        validator.validator(
          {
            value: cellValue,
            unitId,
            subUnitId,
            row,
            column: col,
            worksheet: pos.worksheet,
            workbook: pos.workbook,
            interceptValue: getCellValueOrigin(cell),
            t: cellRaw == null ? void 0 : cellRaw.t
          },
          rule
        ).then((status) => {
          const realStatus = status ? "valid" /* VALID */ : "invalid" /* INVALID */;
          const now = cache.getValue(row, col);
          if (realStatus === "valid" /* VALID */) {
            cache.realDeleteValue(row, col);
          } else {
            cache.setValue(row, col, realStatus);
          }
          onCompete(realStatus, current !== now);
        });
        return "validating" /* VALIDATING */;
      }
      onCompete(current != null ? current : "valid" /* VALID */, false);
      return current != null ? current : "valid" /* VALID */;
    } else {
      onCompete("valid" /* VALID */, false);
      return "valid" /* VALID */;
    }
  }
  getRuleObjectMatrix(unitId, subUnitId) {
    return this._ensureRuleMatrix(unitId, subUnitId);
  }
  getRuleById(unitId, subUnitId, ruleId) {
    return this._dataValidationModel.getRuleById(unitId, subUnitId, ruleId);
  }
  getRuleIndex(unitId, subUnitId, ruleId) {
    return this._dataValidationModel.getRuleIndex(unitId, subUnitId, ruleId);
  }
  getRules(unitId, subUnitId) {
    return [...this._dataValidationModel.getRules(unitId, subUnitId)];
  }
  getUnitRules(unitId) {
    return this._dataValidationModel.getUnitRules(unitId);
  }
  deleteUnitRules(unitId) {
    return this._dataValidationModel.deleteUnitRules(unitId);
  }
  getSubUnitIds(unitId) {
    return this._dataValidationModel.getSubUnitIds(unitId);
  }
  getAll() {
    return this._dataValidationModel.getAll();
  }
};
SheetDataValidationModel = __decorateClass([
  __decorateParam(0, Inject(DataValidationModel)),
  __decorateParam(1, IUniverInstanceService),
  __decorateParam(2, Inject(DataValidatorRegistryService)),
  __decorateParam(3, Inject(DataValidationCacheService)),
  __decorateParam(4, Inject(DataValidationFormulaService)),
  __decorateParam(5, Inject(DataValidationCustomFormulaService)),
  __decorateParam(6, ICommandService)
], SheetDataValidationModel);

// ../packages/sheets-data-validation/src/types/const/operator-text-map.ts
var OperatorTextMap2 = {
  ["between" /* BETWEEN */]: "sheets-data-validation.operators.between",
  ["equal" /* EQUAL */]: "sheets-data-validation.operators.equal",
  ["greaterThan" /* GREATER_THAN */]: "sheets-data-validation.operators.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "sheets-data-validation.operators.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "sheets-data-validation.operators.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "sheets-data-validation.operators.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "sheets-data-validation.operators.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "sheets-data-validation.operators.notEqual"
};
var OperatorTitleMap2 = {
  ["between" /* BETWEEN */]: "sheets-data-validation.ruleName.between",
  ["equal" /* EQUAL */]: "sheets-data-validation.ruleName.equal",
  ["greaterThan" /* GREATER_THAN */]: "sheets-data-validation.ruleName.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "sheets-data-validation.ruleName.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "sheets-data-validation.ruleName.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "sheets-data-validation.ruleName.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "sheets-data-validation.ruleName.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "sheets-data-validation.ruleName.notEqual",
  NONE: "sheets-data-validation.ruleName.legal"
};
var OperatorErrorTitleMap2 = {
  ["between" /* BETWEEN */]: "sheets-data-validation.errorMsg.between",
  ["equal" /* EQUAL */]: "sheets-data-validation.errorMsg.equal",
  ["greaterThan" /* GREATER_THAN */]: "sheets-data-validation.errorMsg.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "sheets-data-validation.errorMsg.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "sheets-data-validation.errorMsg.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "sheets-data-validation.errorMsg.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "sheets-data-validation.errorMsg.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "sheets-data-validation.errorMsg.notEqual",
  NONE: "sheets-data-validation.errorMsg.legal"
};
var TextLengthErrorTitleMap = {
  ["between" /* BETWEEN */]: "sheets-data-validation.textLength.errorMsg.between",
  ["equal" /* EQUAL */]: "sheets-data-validation.textLength.errorMsg.equal",
  ["greaterThan" /* GREATER_THAN */]: "sheets-data-validation.textLength.errorMsg.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "sheets-data-validation.textLength.errorMsg.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "sheets-data-validation.textLength.errorMsg.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "sheets-data-validation.textLength.errorMsg.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "sheets-data-validation.textLength.errorMsg.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "sheets-data-validation.textLength.errorMsg.notEqual"
};

// ../packages/sheets-data-validation/src/validators/base-sheet-validator.ts
var FORMULA12 = "{FORMULA1}";
var FORMULA22 = "{FORMULA2}";
var TYPE2 = "{TYPE}";
var BaseSheetValidator = class extends BaseDataValidator {
  get operatorNames() {
    return this.operators.map((operator) => this.localeService.t(OperatorTextMap2[operator]));
  }
  generateRuleName(rule) {
    var _a, _b;
    if (!rule.operator) {
      return this.localeService.t(OperatorTitleMap2.NONE).replace(TYPE2, this.titleStr);
    }
    const ruleName = this.localeService.t(OperatorTitleMap2[rule.operator]).replace(FORMULA12, (_a = rule.formula1) != null ? _a : "").replace(FORMULA22, (_b = rule.formula2) != null ? _b : "");
    return `${this.titleStr} ${ruleName}`;
  }
  generateRuleErrorMessage(rule, _position) {
    var _a, _b;
    if (!rule.operator) {
      return this.localeService.t(OperatorErrorTitleMap2.NONE).replace(TYPE2, this.titleStr);
    }
    const errorMsg = this.localeService.t(OperatorErrorTitleMap2[rule.operator]).replace(FORMULA12, (_a = rule.formula1) != null ? _a : "").replace(FORMULA22, (_b = rule.formula2) != null ? _b : "");
    return `${errorMsg}`;
  }
};

// ../packages/sheets-data-validation/src/validators/checkbox-validator.ts
var CHECKBOX_FORMULA_1 = 1;
var CHECKBOX_FORMULA_2 = 0;
function getFailMessage(formula, localeService) {
  if (Tools.isBlank(formula)) {
    return localeService.t("sheets-data-validation.validFail.value");
  }
  if (isFormulaString(formula)) {
    return localeService.t("sheets-data-validation.validFail.primitive");
  }
  return "";
}
var transformCheckboxValue = (value) => Tools.isDefine(value) && String(value).toLowerCase() === "true" ? "1" : String(value).toLowerCase() === "false" ? "0" : value;
var CheckboxValidator = class extends BaseSheetValidator {
  constructor() {
    super(...arguments);
    __publicField(this, "id", "checkbox" /* CHECKBOX */);
    __publicField(this, "title", "sheets-data-validation.checkbox.title");
    __publicField(this, "operators", []);
    __publicField(this, "scopes", ["sheet"]);
    __publicField(this, "order", 41);
    __publicField(this, "offsetFormulaByRange", false);
    __publicField(this, "_formulaService", this.injector.get(DataValidationFormulaService));
    __publicField(this, "skipDefaultFontRender", (rule, cellValue, pos) => {
      const { unitId, subUnitId } = pos;
      const { formula1, formula2 } = this.parseFormulaSync(rule, unitId, subUnitId);
      const valueStr = `${cellValue != null ? cellValue : ""}`;
      const res = !valueStr || (valueStr === `${formula1}` || valueStr === `${formula2}`);
      return res;
    });
  }
  validatorFormula(rule, unitId, subUnitId) {
    const { formula1, formula2 } = rule;
    const isEqual = formula1 === formula2;
    if (Tools.isBlank(formula1) && Tools.isBlank(formula2)) {
      return {
        success: true
      };
    }
    if (isEqual) {
      return {
        success: false,
        formula1: this.localeService.t("sheets-data-validation.validFail.checkboxEqual"),
        formula2: this.localeService.t("sheets-data-validation.validFail.checkboxEqual")
      };
    }
    const error1 = getFailMessage(formula1, this.localeService);
    const error2 = getFailMessage(formula2, this.localeService);
    return {
      success: !error1 && !error2,
      formula1: error1,
      formula2: error2
    };
  }
  async parseFormula(rule, unitId, subUnitId) {
    var _a, _b, _c, _d;
    const { formula1 = CHECKBOX_FORMULA_1, formula2 = CHECKBOX_FORMULA_2 } = rule;
    const results = await this._formulaService.getRuleFormulaResult(unitId, subUnitId, rule.uid);
    const originFormula1 = isFormulaString(formula1) ? getFormulaResult((_b = (_a = results == null ? void 0 : results[0]) == null ? void 0 : _a.result) == null ? void 0 : _b[0][0]) : formula1;
    const originFormula2 = isFormulaString(formula2) ? getFormulaResult((_d = (_c = results == null ? void 0 : results[1]) == null ? void 0 : _c.result) == null ? void 0 : _d[0][0]) : formula2;
    const isFormulaValid = isLegalFormulaResult(String(originFormula1)) && isLegalFormulaResult(String(originFormula2));
    return {
      formula1: transformCheckboxValue(originFormula1),
      formula2: transformCheckboxValue(originFormula2),
      originFormula1,
      originFormula2,
      isFormulaValid
    };
  }
  getExtraStyle(rule, value) {
    return {
      tb: 2 /* CLIP */
    };
  }
  parseFormulaSync(rule, unitId, subUnitId) {
    var _a, _b, _c, _d;
    const { formula1 = CHECKBOX_FORMULA_1, formula2 = CHECKBOX_FORMULA_2 } = rule;
    const results = this._formulaService.getRuleFormulaResultSync(unitId, subUnitId, rule.uid);
    const originFormula1 = isFormulaString(formula1) ? getFormulaResult((_b = (_a = results == null ? void 0 : results[0]) == null ? void 0 : _a.result) == null ? void 0 : _b[0][0]) : formula1;
    const originFormula2 = isFormulaString(formula2) ? getFormulaResult((_d = (_c = results == null ? void 0 : results[1]) == null ? void 0 : _c.result) == null ? void 0 : _d[0][0]) : formula2;
    const isFormulaValid = isLegalFormulaResult(String(originFormula1)) && isLegalFormulaResult(String(originFormula2));
    return {
      formula1: transformCheckboxValue(originFormula1),
      formula2: transformCheckboxValue(originFormula2),
      originFormula1,
      originFormula2,
      isFormulaValid
    };
  }
  async isValidType(cellInfo, formula, rule) {
    const { value, unitId, subUnitId } = cellInfo;
    const { formula1, formula2, originFormula1, originFormula2 } = await this.parseFormula(rule, unitId, subUnitId);
    if (!Tools.isDefine(formula1) || !Tools.isDefine(formula2)) {
      return true;
    }
    return Tools.isDefine(value) && (String(value) === String(formula1) || String(value) === String(formula2) || String(value) === String(originFormula1 != null ? originFormula1 : "") || String(value) === String(originFormula2 != null ? originFormula2 : ""));
  }
  generateRuleErrorMessage(rule) {
    return this.localeService.t("sheets-data-validation.checkbox.error");
  }
  generateRuleName(rule) {
    return this.titleStr;
  }
};

// ../packages/sheets-data-validation/src/common/date-text-map.ts
var DateOperatorNameMap = {
  ["between" /* BETWEEN */]: "sheets-data-validation.date.operators.between",
  ["equal" /* EQUAL */]: "sheets-data-validation.date.operators.equal",
  ["greaterThan" /* GREATER_THAN */]: "sheets-data-validation.date.operators.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "sheets-data-validation.date.operators.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "sheets-data-validation.date.operators.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "sheets-data-validation.date.operators.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "sheets-data-validation.date.operators.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "sheets-data-validation.date.operators.notEqual"
};
var DateOperatorTextMap = {
  ["between" /* BETWEEN */]: "sheets-data-validation.date.operators.between",
  ["equal" /* EQUAL */]: "sheets-data-validation.date.operators.equal",
  ["greaterThan" /* GREATER_THAN */]: "sheets-data-validation.date.operators.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "sheets-data-validation.date.operators.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "sheets-data-validation.date.operators.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "sheets-data-validation.date.operators.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "sheets-data-validation.date.operators.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "sheets-data-validation.date.operators.notEqual"
};
var DateOperatorTitleMap = {
  ["between" /* BETWEEN */]: "sheets-data-validation.date.ruleName.between",
  ["equal" /* EQUAL */]: "sheets-data-validation.date.ruleName.equal",
  ["greaterThan" /* GREATER_THAN */]: "sheets-data-validation.date.ruleName.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "sheets-data-validation.date.ruleName.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "sheets-data-validation.date.ruleName.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "sheets-data-validation.date.ruleName.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "sheets-data-validation.date.ruleName.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "sheets-data-validation.date.ruleName.notEqual",
  NONE: "sheets-data-validation.date.ruleName.legal"
};
var DateOperatorErrorTitleMap = {
  ["between" /* BETWEEN */]: "sheets-data-validation.date.errorMsg.between",
  ["equal" /* EQUAL */]: "sheets-data-validation.date.errorMsg.equal",
  ["greaterThan" /* GREATER_THAN */]: "sheets-data-validation.date.errorMsg.greaterThan",
  ["greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */]: "sheets-data-validation.date.errorMsg.greaterThanOrEqual",
  ["lessThan" /* LESS_THAN */]: "sheets-data-validation.date.errorMsg.lessThan",
  ["lessThanOrEqual" /* LESS_THAN_OR_EQUAL */]: "sheets-data-validation.date.errorMsg.lessThanOrEqual",
  ["notBetween" /* NOT_BETWEEN */]: "sheets-data-validation.date.errorMsg.notBetween",
  ["notEqual" /* NOT_EQUAL */]: "sheets-data-validation.date.errorMsg.notEqual",
  NONE: "sheets-data-validation.date.errorMsg.legal"
};

// ../packages/sheets-data-validation/src/types/const/two-formula-operators.ts
var TWO_FORMULA_OPERATOR_COUNT2 = [
  "between" /* BETWEEN */,
  "notBetween" /* NOT_BETWEEN */
];

// ../packages/sheets-data-validation/src/validators/const.ts
var FORMULA13 = "{FORMULA1}";
var FORMULA23 = "{FORMULA2}";

// ../packages/sheets-data-validation/src/validators/util.ts
function getDataValidationCellValue(cellData) {
  const cellValue = getCellValueOrigin(cellData);
  if (cellValue === void 0 || cellValue === null) {
    return "";
  }
  return cellValue.toString();
}
function getTransformedFormula(lexerTreeBuilder, rule, position) {
  const { formula1, formula2 } = rule;
  const originStartRow = rule.ranges[0].startRow;
  const originStartColumn = rule.ranges[0].startColumn;
  const offsetRow = position.row - originStartRow;
  const offsetColumn = position.col - originStartColumn;
  const transformedFormula1 = isFormulaString(formula1) ? lexerTreeBuilder.moveFormulaRefOffset(formula1, offsetColumn, offsetRow, true) : formula1;
  const transformedFormula2 = isFormulaString(formula2) ? lexerTreeBuilder.moveFormulaRefOffset(formula2, offsetColumn, offsetRow, true) : formula2;
  return {
    transformedFormula1,
    transformedFormula2
  };
}

// ../packages/sheets-data-validation/src/validators/date-validator.ts
var transformDate2SerialNumber = (value) => {
  var _a, _b;
  if (value === void 0 || value === null || typeof value === "boolean") {
    return void 0;
  }
  if (typeof value === "number" || !Number.isNaN(+value)) {
    return +value;
  }
  const v = (_a = lib_exports.parseDate(value)) == null ? void 0 : _a.v;
  if (Tools.isDefine(v)) {
    return v;
  }
  return (_b = lib_exports.parseDate(dateKit(value).format("YYYY-MM-DD HH:mm:ss"))) == null ? void 0 : _b.v;
};
var DateValidator = class extends BaseSheetValidator {
  constructor() {
    super(...arguments);
    __publicField(this, "id", "date" /* DATE */);
    __publicField(this, "title", "sheets-data-validation.date.title");
    __publicField(this, "order", 40);
    __publicField(this, "operators", [
      "between" /* BETWEEN */,
      "equal" /* EQUAL */,
      "greaterThan" /* GREATER_THAN */,
      "greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */,
      "lessThan" /* LESS_THAN */,
      "lessThanOrEqual" /* LESS_THAN_OR_EQUAL */,
      "notBetween" /* NOT_BETWEEN */,
      "notEqual" /* NOT_EQUAL */
    ]);
    __publicField(this, "scopes", ["sheet"]);
    __publicField(this, "_customFormulaService", this.injector.get(DataValidationCustomFormulaService));
    __publicField(this, "_lexerTreeBuilder", this.injector.get(LexerTreeBuilder));
  }
  async parseFormula(rule, unitId, subUnitId, row, column) {
    const formulaResult1 = await this._customFormulaService.getCellFormulaValue(unitId, subUnitId, rule.uid, row, column);
    const formulaResult2 = await this._customFormulaService.getCellFormula2Value(unitId, subUnitId, rule.uid, row, column);
    const { formula1, formula2 } = rule;
    const isFormulaValid = isLegalFormulaResult(String(formulaResult1 == null ? void 0 : formulaResult1.v)) && isLegalFormulaResult(String(formulaResult2 == null ? void 0 : formulaResult2.v));
    return {
      formula1: transformDate2SerialNumber(isFormulaString(formula1) ? formulaResult1 == null ? void 0 : formulaResult1.v : formula1),
      formula2: transformDate2SerialNumber(isFormulaString(formula2) ? formulaResult2 == null ? void 0 : formulaResult2.v : formula2),
      isFormulaValid
    };
  }
  async isValidType(info) {
    const { interceptValue, value } = info;
    if (typeof value === "number" && typeof interceptValue === "string") {
      return Boolean(lib_exports.parseDate(interceptValue));
    }
    if (typeof interceptValue === "string") {
      return Boolean(lib_exports.parseDate(interceptValue));
    }
    return false;
  }
  _validatorSingleFormula(formula) {
    return !Tools.isBlank(formula) && (isFormulaString(formula) || !Number.isNaN(+formula) || Boolean(formula && lib_exports.parseDate(formula)));
  }
  validatorFormula(rule, unitId, subUnitId) {
    const operator = rule.operator;
    if (!operator) {
      return {
        success: true
      };
    }
    const formula1Success = this._validatorSingleFormula(rule.formula1);
    const errorMsg = this.localeService.t("sheets-data-validation.validFail.date");
    const isTwoFormula = TWO_FORMULA_OPERATOR_COUNT2.includes(operator);
    if (isTwoFormula) {
      const formula2Success = this._validatorSingleFormula(rule.formula2);
      return {
        success: formula1Success && formula2Success,
        formula1: formula1Success ? void 0 : errorMsg,
        formula2: formula2Success ? void 0 : errorMsg
      };
    }
    return {
      success: formula1Success,
      formula1: formula1Success ? void 0 : errorMsg
    };
  }
  normalizeFormula(rule, _unitId, _subUnitId) {
    const { formula1, formula2, bizInfo } = rule;
    const normlizeSingleFormula = (formula) => {
      var _a;
      if (!formula) {
        return formula;
      }
      let date;
      if (!Number.isNaN(+formula)) {
        date = lib_exports.dateFromSerial(+formula);
      } else {
        const res = (_a = lib_exports.parseDate(formula)) == null ? void 0 : _a.v;
        if (res === void 0 || res === null) {
          return "";
        }
        date = lib_exports.dateFromSerial(res);
      }
      return dateKit(`${date[0]}/${date[1]}/${date[2]} ${date[3]}:${date[4]}:${date[5]}`).format((bizInfo == null ? void 0 : bizInfo.showTime) ? "YYYY-MM-DD HH:mm:ss" : "YYYY-MM-DD");
    };
    return {
      formula1: isFormulaString(formula1) ? formula1 : normlizeSingleFormula(`${formula1}`),
      formula2: isFormulaString(formula2) ? formula2 : normlizeSingleFormula(`${formula2}`)
    };
  }
  transform(cellInfo, _formula, _rule) {
    const { value } = cellInfo;
    return {
      ...cellInfo,
      value: transformDate2SerialNumber(value)
    };
  }
  get operatorNames() {
    return this.operators.map((operator) => this.localeService.t(DateOperatorNameMap[operator]));
  }
  generateRuleName(rule) {
    var _a, _b;
    if (!rule.operator) {
      return this.localeService.t(DateOperatorTitleMap.NONE);
    }
    const ruleName = this.localeService.t(DateOperatorTitleMap[rule.operator]).replace(FORMULA13, (_a = rule.formula1) != null ? _a : "").replace(FORMULA23, (_b = rule.formula2) != null ? _b : "");
    return `${this.titleStr} ${ruleName}`;
  }
  generateRuleErrorMessage(rule, pos) {
    if (!rule.operator) {
      return this.titleStr;
    }
    const { transformedFormula1, transformedFormula2 } = getTransformedFormula(this._lexerTreeBuilder, rule, pos);
    const errorMsg = this.localeService.t(DateOperatorErrorTitleMap[rule.operator]).replace(FORMULA13, transformedFormula1 != null ? transformedFormula1 : "").replace(FORMULA23, transformedFormula2 != null ? transformedFormula2 : "");
    return `${errorMsg}`;
  }
};

// ../packages/sheets-data-validation/src/validators/decimal-validator.ts
function getCellValueNumber(cellValue) {
  return +cellValue;
}
var DecimalValidator = class extends BaseSheetValidator {
  constructor() {
    super(...arguments);
    __publicField(this, "_customFormulaService", this.injector.get(DataValidationCustomFormulaService));
    __publicField(this, "id", "decimal" /* DECIMAL */);
    __publicField(this, "_lexerTreeBuilder", this.injector.get(LexerTreeBuilder));
    __publicField(this, "title", "sheets-data-validation.decimal.title");
    __publicField(this, "order", 20);
    __publicField(this, "operators", [
      "between" /* BETWEEN */,
      "equal" /* EQUAL */,
      "greaterThan" /* GREATER_THAN */,
      "greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */,
      "lessThan" /* LESS_THAN */,
      "lessThanOrEqual" /* LESS_THAN_OR_EQUAL */,
      "notBetween" /* NOT_BETWEEN */,
      "notEqual" /* NOT_EQUAL */
    ]);
    __publicField(this, "scopes", ["sheet"]);
  }
  _isFormulaOrNumber(formula) {
    return !Tools.isBlank(formula) && (isFormulaString(formula) || !Number.isNaN(+formula));
  }
  async isValidType(cellInfo, formula, rule) {
    const { value: cellValue } = cellInfo;
    return !Number.isNaN(getCellValueNumber(cellValue));
  }
  transform(cellInfo, formula, rule) {
    const { value: cellValue } = cellInfo;
    return {
      ...cellInfo,
      value: getCellValueNumber(cellValue)
    };
  }
  _parseNumber(formula) {
    if (formula === void 0 || formula === null) {
      return Number.NaN;
    }
    return +formula;
  }
  async parseFormula(rule, unitId, subUnitId, row, column) {
    const formulaResult1 = await this._customFormulaService.getCellFormulaValue(unitId, subUnitId, rule.uid, row, column);
    const formulaResult2 = await this._customFormulaService.getCellFormula2Value(unitId, subUnitId, rule.uid, row, column);
    const { formula1, formula2 } = rule;
    const isFormulaValid = isLegalFormulaResult(String(formulaResult1 == null ? void 0 : formulaResult1.v)) && isLegalFormulaResult(String(formulaResult2 == null ? void 0 : formulaResult2.v));
    const info = {
      formula1: this._parseNumber(isFormulaString(formula1) ? formulaResult1 == null ? void 0 : formulaResult1.v : formula1),
      formula2: this._parseNumber(isFormulaString(formula2) ? formulaResult2 == null ? void 0 : formulaResult2.v : formula2),
      isFormulaValid
    };
    return info;
  }
  validatorFormula(rule, unitId, subUnitId) {
    const operator = rule.operator;
    if (!operator) {
      return {
        success: true
      };
    }
    const formula1Success = Tools.isDefine(rule.formula1) && this._isFormulaOrNumber(rule.formula1);
    const formula2Success = Tools.isDefine(rule.formula2) && this._isFormulaOrNumber(rule.formula2);
    const isTwoFormula = TWO_FORMULA_OPERATOR_COUNT2.includes(operator);
    const errorMsg = this.localeService.t("sheets-data-validation.validFail.number");
    if (isTwoFormula) {
      return {
        success: formula1Success && formula2Success,
        formula1: formula1Success ? void 0 : errorMsg,
        formula2: formula2Success ? void 0 : errorMsg
      };
    }
    return {
      success: formula1Success,
      formula1: formula1Success ? "" : errorMsg
    };
  }
  generateRuleErrorMessage(rule, position) {
    if (!rule.operator) {
      return this.localeService.t(OperatorErrorTitleMap2.NONE).replace("{TYPE}", this.titleStr);
    }
    const { transformedFormula1, transformedFormula2 } = getTransformedFormula(this._lexerTreeBuilder, rule, position);
    const errorMsg = this.localeService.t(OperatorErrorTitleMap2[rule.operator]).replace(FORMULA13, transformedFormula1 != null ? transformedFormula1 : "").replace(FORMULA23, transformedFormula2 != null ? transformedFormula2 : "");
    return `${errorMsg}`;
  }
};

// ../packages/sheets-data-validation/src/validators/list-validator.ts
function getRuleFormulaResultSet(result) {
  var _a, _b;
  if (!result) {
    return [];
  }
  const resultSet = /* @__PURE__ */ new Set();
  for (let i = 0, rowLen = result.length; i < rowLen; i++) {
    const row = result[i];
    if (!row) continue;
    for (let j = 0, colLen = row.length; j < colLen; j++) {
      const cell = row[j];
      const value = getCellValueOrigin(cell);
      if (value !== null && value !== void 0) {
        if (typeof value !== "string" && typeof (cell == null ? void 0 : cell.s) === "object" && ((_b = (_a = cell.s) == null ? void 0 : _a.n) == null ? void 0 : _b.pattern)) {
          resultSet.add(lib_exports.format(cell.s.n.pattern, value, { throws: false }));
          continue;
        }
        const valueStr = typeof value === "string" ? value : String(value);
        if (isLegalFormulaResult(valueStr)) {
          resultSet.add(valueStr);
        }
      }
    }
  }
  return [...resultSet];
}
var supportedFormula = [
  "if",
  "indirect",
  "choose",
  "offset"
];
function isValidListFormula(formula, lexer) {
  if (!isFormulaString(formula)) {
    return true;
  }
  const isRefString = isReferenceString(formula.slice(1));
  if (isRefString) {
    return true;
  }
  const nodes = lexer.sequenceNodesBuilder(formula);
  return nodes && nodes.some((node) => typeof node === "object" && node.nodeType === 3 /* FUNCTION */ && supportedFormula.indexOf(node.token.toLowerCase()) > -1);
}
function isRuleIntersects(rule, sheetName) {
  const { formula1 = "", ranges } = rule;
  const isRefString = isReferenceString(formula1.slice(1));
  if (isRefString) {
    const refRange = deserializeRangeWithSheet(formula1.slice(1));
    if ((!refRange.sheetName || refRange.sheetName === sheetName) && ranges.some((range) => Rectangle.intersects(range, refRange.range))) {
      return true;
    }
  }
  return false;
}
var ListValidator = class extends BaseSheetValidator {
  constructor() {
    super(...arguments);
    __publicField(this, "formulaService", this.injector.get(DataValidationFormulaService));
    __publicField(this, "_lexer", this.injector.get(LexerTreeBuilder));
    __publicField(this, "_univerInstanceService", this.injector.get(IUniverInstanceService));
    __publicField(this, "_listCacheService", this.injector.get(DataValidationListCacheService));
    __publicField(this, "order", 50);
    __publicField(this, "offsetFormulaByRange", false);
    __publicField(this, "id", "list" /* LIST */);
    __publicField(this, "title", "sheets-data-validation.list.title");
    __publicField(this, "operators", []);
    __publicField(this, "scopes", ["sheet"]);
    __publicField(this, "skipDefaultFontRender", (rule) => {
      return rule.renderMode !== 0 /* TEXT */;
    });
  }
  validatorFormula(rule, unitId, subUnitId) {
    var _a, _b, _c;
    const success = !Tools.isBlank(rule.formula1);
    const valid = isValidListFormula((_a = rule.formula1) != null ? _a : "", this._lexer);
    const sheetName = (_c = (_b = this._univerInstanceService.getUnit(unitId, 2 /* UNIVER_SHEET */)) == null ? void 0 : _b.getSheetBySheetId(subUnitId)) == null ? void 0 : _c.getName();
    const isIntersects = isRuleIntersects(rule, sheetName != null ? sheetName : "");
    return {
      success: Boolean(success && valid && !isIntersects),
      formula1: success ? valid ? !isIntersects ? void 0 : this.localeService.t("sheets-data-validation.validFail.listIntersects") : this.localeService.t("sheets-data-validation.validFail.listInvalid") : this.localeService.t("sheets-data-validation.validFail.list")
    };
  }
  getExtraStyle(rule, value, { style: defaultStyle }) {
    var _a;
    const tb = (_a = defaultStyle.tb !== 1 /* OVERFLOW */ ? defaultStyle.tb : 2 /* CLIP */) != null ? _a : 3 /* WRAP */;
    if (rule.type === "list" /* LIST */ && (rule.renderMode === 1 /* ARROW */ || rule.renderMode === 0 /* TEXT */)) {
      const colorMap = this.getListWithColorMap(rule);
      const valueStr = `${value != null ? value : ""}`;
      const color = colorMap[valueStr];
      if (color) {
        return {
          bg: {
            rgb: color
          },
          tb
        };
      }
    }
    return {
      tb
    };
  }
  parseCellValue(cellValue) {
    const cellString = cellValue.toString();
    return deserializeListOptions(cellString);
  }
  async parseFormula(rule, unitId, subUnitId) {
    var _a, _b;
    const results = await this.formulaService.getRuleFormulaResult(unitId, subUnitId, rule.uid);
    const formulaResult1 = getFormulaResult((_b = (_a = results == null ? void 0 : results[0]) == null ? void 0 : _a.result) == null ? void 0 : _b[0][0]);
    const isFormulaValid = isLegalFormulaResult(String(formulaResult1));
    return {
      formula1: void 0,
      formula2: void 0,
      isFormulaValid
    };
  }
  async isValidType(cellInfo, formula, rule) {
    const { value, unitId, subUnitId } = cellInfo;
    const { formula1 = "" } = rule;
    const formula1Result = isFormulaString(formula1) ? this._listCacheService.getOrCompute(
      unitId,
      subUnitId,
      rule
    ).list : deserializeListOptions(formula1);
    const selected = rule.type === "list" /* LIST */ ? value === void 0 || value === null || value === "" ? [] : [`${value}`] : this.parseCellValue(value);
    return selected.every((i) => formula1Result.includes(i));
  }
  generateRuleName() {
    return this.localeService.t("sheets-data-validation.list.name");
  }
  generateRuleErrorMessage() {
    return this.localeService.t("sheets-data-validation.list.error");
  }
  _getUnitAndSubUnit(currentUnitId, currentSubUnitId) {
    var _a, _b;
    const workbook = (_a = currentUnitId ? this._univerInstanceService.getUniverSheetInstance(currentUnitId) : void 0) != null ? _a : this._univerInstanceService.getCurrentUnitOfType(2 /* UNIVER_SHEET */);
    if (!workbook) return null;
    const worksheet = (_b = currentSubUnitId ? workbook.getSheetBySheetId(currentSubUnitId) : void 0) != null ? _b : workbook.getActiveSheet();
    if (!worksheet) return null;
    return {
      unitId: workbook.getUnitId(),
      subUnitId: worksheet.getSheetId()
    };
  }
  getList(rule, currentUnitId, currentSubUnitId) {
    const location = this._getUnitAndSubUnit(currentUnitId, currentSubUnitId);
    if (!location) return [];
    const { unitId, subUnitId } = location;
    return this._listCacheService.getOrCompute(
      unitId,
      subUnitId,
      rule
    ).list;
  }
  async getListAsync(rule, currentUnitId, currentSubUnitId) {
    var _a, _b;
    const { formula1 = "" } = rule;
    const location = this._getUnitAndSubUnit(currentUnitId, currentSubUnitId);
    if (!location) return [];
    const { unitId, subUnitId } = location;
    const results = await this.formulaService.getRuleFormulaResult(unitId, subUnitId, rule.uid);
    return isFormulaString(formula1) ? getRuleFormulaResultSet((_b = (_a = results == null ? void 0 : results[0]) == null ? void 0 : _a.result) == null ? void 0 : _b[0][0]) : deserializeListOptions(formula1);
  }
  getListWithColor(rule, currentUnitId, currentSubUnitId) {
    const location = this._getUnitAndSubUnit(currentUnitId, currentSubUnitId);
    if (!location) return [];
    const { unitId, subUnitId } = location;
    return this._listCacheService.getOrCompute(
      unitId,
      subUnitId,
      rule
    ).listWithColor;
  }
  getListWithColorMap(rule, currentUnitId, currentSubUnitId) {
    const location = this._getUnitAndSubUnit(currentUnitId, currentSubUnitId);
    if (!location) return {};
    const { unitId, subUnitId } = location;
    return this._listCacheService.getOrCompute(
      unitId,
      subUnitId,
      rule
    ).colorMap;
  }
};

// ../packages/sheets-data-validation/src/validators/text-length-validator.ts
var TextLengthValidator = class extends BaseSheetValidator {
  constructor() {
    super(...arguments);
    __publicField(this, "id", "textLength" /* TEXT_LENGTH */);
    __publicField(this, "title", "sheets-data-validation.textLength.title");
    __publicField(this, "_lexerTreeBuilder", this.injector.get(LexerTreeBuilder));
    __publicField(this, "order", 30);
    __publicField(this, "operators", [
      "between" /* BETWEEN */,
      "equal" /* EQUAL */,
      "greaterThan" /* GREATER_THAN */,
      "greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */,
      "lessThan" /* LESS_THAN */,
      "lessThanOrEqual" /* LESS_THAN_OR_EQUAL */,
      "notBetween" /* NOT_BETWEEN */,
      "notEqual" /* NOT_EQUAL */
    ]);
    __publicField(this, "scopes", ["sheet"]);
    __publicField(this, "_customFormulaService", this.injector.get(DataValidationCustomFormulaService));
  }
  _isFormulaOrInt(formula) {
    return !Tools.isBlank(formula) && (isFormulaString(formula) || !Number.isNaN(+formula) && Number.isInteger(+formula));
  }
  validatorFormula(rule, _unitId, _subUnitId) {
    const operator = rule.operator;
    if (!operator) {
      return {
        success: false
      };
    }
    const formula1Success = Tools.isDefine(rule.formula1) && this._isFormulaOrInt(rule.formula1);
    const formula2Success = Tools.isDefine(rule.formula2) && this._isFormulaOrInt(rule.formula2);
    const isTwoFormula = TWO_FORMULA_OPERATOR_COUNT2.includes(operator);
    const errorMsg = this.localeService.t("sheets-data-validation.validFail.number");
    if (isTwoFormula) {
      return {
        success: formula1Success && formula2Success,
        formula1: formula1Success ? void 0 : errorMsg,
        formula2: formula2Success ? void 0 : errorMsg
      };
    }
    return {
      success: formula1Success,
      formula1: errorMsg
    };
  }
  _parseNumber(formula) {
    if (formula === void 0 || formula === null) {
      return Number.NaN;
    }
    return +formula;
  }
  async parseFormula(rule, unitId, subUnitId, row, column) {
    const formulaResult1 = await this._customFormulaService.getCellFormulaValue(unitId, subUnitId, rule.uid, row, column);
    const formulaResult2 = await this._customFormulaService.getCellFormula2Value(unitId, subUnitId, rule.uid, row, column);
    const { formula1, formula2 } = rule;
    const isFormulaValid = isLegalFormulaResult(String(formulaResult1 == null ? void 0 : formulaResult1.v)) && isLegalFormulaResult(String(formulaResult2 == null ? void 0 : formulaResult2.v));
    return {
      formula1: this._parseNumber(isFormulaString(formula1) ? formulaResult1 == null ? void 0 : formulaResult1.v : formula1),
      formula2: this._parseNumber(isFormulaString(formula2) ? formulaResult2 == null ? void 0 : formulaResult2.v : formula2),
      isFormulaValid
    };
  }
  transform(cellInfo, _formula, _rule) {
    return {
      ...cellInfo,
      value: cellInfo.value.toString().length
    };
  }
  async isValidType(cellInfo, _formula, _rule) {
    const { value: cellValue } = cellInfo;
    return typeof cellValue === "string" || typeof cellValue === "number";
  }
  generateRuleErrorMessage(rule, pos) {
    if (!rule.operator) {
      return this.titleStr;
    }
    const { transformedFormula1, transformedFormula2 } = getTransformedFormula(this._lexerTreeBuilder, rule, pos);
    const errorMsg = this.localeService.t(TextLengthErrorTitleMap[rule.operator]).replace(FORMULA13, transformedFormula1 != null ? transformedFormula1 : "").replace(FORMULA23, transformedFormula2 != null ? transformedFormula2 : "");
    return `${errorMsg}`;
  }
};

// ../packages/sheets-data-validation/src/commands/commands/data-validation.command.ts
function isBlankCell(cellData) {
  var _a, _b;
  if (!cellData) {
    return true;
  }
  if (!cellData.p) {
    return Tools.isBlank(cellData.v);
  }
  const dataStream = ((_b = (_a = cellData.p.body) == null ? void 0 : _a.dataStream) != null ? _b : "").slice(0, -2).trim();
  return !dataStream;
}
function getDataValidationDiffMutations(unitId, subUnitId, diffs, accessor, source = "command", fillDefaultValue = true) {
  const lexerTreeBuilder = accessor.get(LexerTreeBuilder);
  const validatorRegistryService = accessor.get(DataValidatorRegistryService);
  const redoMutations = [];
  const undoMutations = [];
  const sheetDataValidationModel = accessor.get(SheetDataValidationModel);
  const univerInstanceService = accessor.get(IUniverInstanceService);
  const target = getSheetCommandTarget(univerInstanceService, { unitId, subUnitId });
  if (!target) {
    return {
      redoMutations,
      undoMutations
    };
  }
  const { worksheet } = target;
  const redoMatrix = new ObjectMatrix();
  let setRangeValue = false;
  function setRangesDefaultValue(ranges, defaultValue) {
    if (!fillDefaultValue) {
      return;
    }
    ranges.forEach((range) => {
      Range.foreach(range, (row, column) => {
        const cellData = worksheet.getCellRaw(row, column);
        const value = getStringCellValue(cellData);
        if ((isBlankCell(cellData) || value === defaultValue) && !(cellData == null ? void 0 : cellData.p)) {
          setRangeValue = true;
          redoMatrix.setValue(row, column, {
            v: defaultValue,
            p: null
          });
        }
      });
    });
  }
  diffs.forEach((diff) => {
    switch (diff.type) {
      case "delete":
        redoMutations.push({
          id: RemoveDataValidationMutation.id,
          params: {
            unitId,
            subUnitId,
            ruleId: diff.rule.uid,
            source
          }
        });
        undoMutations.unshift({
          id: AddDataValidationMutation.id,
          params: {
            unitId,
            subUnitId,
            rule: diff.rule,
            index: diff.index,
            source
          }
        });
        break;
      case "update": {
        if (shouldOffsetFormulaByRange(diff.rule.type, validatorRegistryService)) {
          const originRow = diff.oldRanges[0].startRow;
          const originColumn = diff.oldRanges[0].startColumn;
          const newRow = diff.newRanges[0].startRow;
          const newColumn = diff.newRanges[0].startColumn;
          const rowDiff = newRow - originRow;
          const columnDiff = newColumn - originColumn;
          const newFormula = isFormulaString(diff.rule.formula1) ? lexerTreeBuilder.moveFormulaRefOffset(diff.rule.formula1, columnDiff, rowDiff) : diff.rule.formula1;
          const newFormula2 = isFormulaString(diff.rule.formula2) ? lexerTreeBuilder.moveFormulaRefOffset(diff.rule.formula2, columnDiff, rowDiff) : diff.rule.formula2;
          if (newFormula !== diff.rule.formula1 || newFormula2 !== diff.rule.formula2 || !isRangesEqual(diff.newRanges, diff.oldRanges)) {
            redoMutations.push({
              id: UpdateDataValidationMutation.id,
              params: {
                unitId,
                subUnitId,
                ruleId: diff.ruleId,
                payload: {
                  type: 3 /* ALL */,
                  payload: {
                    formula1: newFormula,
                    formula2: newFormula2,
                    ranges: diff.newRanges
                  }
                }
              }
            });
            undoMutations.unshift({
              id: UpdateDataValidationMutation.id,
              params: {
                unitId,
                subUnitId,
                ruleId: diff.ruleId,
                payload: {
                  type: 3 /* ALL */,
                  payload: {
                    formula1: diff.rule.formula1,
                    formula2: diff.rule.formula2,
                    ranges: diff.oldRanges
                  }
                }
              }
            });
          } else {
            redoMutations.push({
              id: UpdateDataValidationMutation.id,
              params: {
                unitId,
                subUnitId,
                ruleId: diff.ruleId,
                payload: {
                  type: 1 /* RANGE */,
                  payload: diff.newRanges
                },
                source
              }
            });
            undoMutations.unshift({
              id: UpdateDataValidationMutation.id,
              params: {
                unitId,
                subUnitId,
                ruleId: diff.ruleId,
                payload: {
                  type: 1 /* RANGE */,
                  payload: diff.oldRanges
                },
                source
              }
            });
          }
        } else {
          redoMutations.push({
            id: UpdateDataValidationMutation.id,
            params: {
              unitId,
              subUnitId,
              ruleId: diff.ruleId,
              payload: {
                type: 1 /* RANGE */,
                payload: diff.newRanges
              },
              source
            }
          });
          undoMutations.unshift({
            id: UpdateDataValidationMutation.id,
            params: {
              unitId,
              subUnitId,
              ruleId: diff.ruleId,
              payload: {
                type: 1 /* RANGE */,
                payload: diff.oldRanges
              },
              source
            }
          });
        }
        const rule = sheetDataValidationModel.getRuleById(unitId, subUnitId, diff.ruleId);
        if (rule && rule.type === "checkbox" /* CHECKBOX */) {
          const validator = sheetDataValidationModel.getValidator("checkbox" /* CHECKBOX */);
          const formula = validator.parseFormulaSync(rule, unitId, subUnitId);
          setRangesDefaultValue(diff.newRanges, formula.formula2);
        }
        break;
      }
      case "add": {
        redoMutations.push({
          id: AddDataValidationMutation.id,
          params: {
            unitId,
            subUnitId,
            rule: diff.rule,
            source
          }
        });
        undoMutations.unshift({
          id: RemoveDataValidationMutation.id,
          params: {
            unitId,
            subUnitId,
            ruleId: diff.rule.uid,
            source
          }
        });
        if (diff.rule.type === "checkbox" /* CHECKBOX */) {
          const validator = sheetDataValidationModel.getValidator("checkbox" /* CHECKBOX */);
          const formula = validator.parseFormulaSync(diff.rule, unitId, subUnitId);
          setRangesDefaultValue(diff.rule.ranges, formula.originFormula2);
        }
        break;
      }
      default:
        break;
    }
  });
  if (setRangeValue) {
    const redoSetRangeValues = {
      id: SetRangeValuesMutation.id,
      params: {
        unitId,
        subUnitId,
        cellValue: redoMatrix.getData()
      }
    };
    const undoSetRangeValues = {
      id: SetRangeValuesMutation.id,
      params: SetRangeValuesUndoMutationFactory(accessor, redoSetRangeValues.params)
    };
    redoMutations.push(redoSetRangeValues);
    undoMutations.push(undoSetRangeValues);
  }
  return {
    redoMutations,
    undoMutations
  };
}
var UpdateSheetDataValidationRangeCommand = {
  type: 0 /* COMMAND */,
  id: "sheet.command.updateDataValidationRuleRange",
  handler(accessor, params) {
    if (!params) {
      return false;
    }
    const { unitId, subUnitId, ranges, ruleId } = params;
    const sheetDataValidationModel = accessor.get(SheetDataValidationModel);
    const commandService = accessor.get(ICommandService);
    const undoRedoService = accessor.get(IUndoRedoService);
    const currentRule = sheetDataValidationModel.getRuleById(unitId, subUnitId, ruleId);
    if (!currentRule) {
      return false;
    }
    const matrix = sheetDataValidationModel.getRuleObjectMatrix(unitId, subUnitId).clone();
    matrix.updateRange(ruleId, ranges);
    const diffs = matrix.diff(sheetDataValidationModel.getRules(unitId, subUnitId));
    const { redoMutations, undoMutations } = getDataValidationDiffMutations(unitId, subUnitId, diffs, accessor);
    undoRedoService.pushUndoRedo({
      undoMutations,
      redoMutations,
      unitID: unitId
    });
    sequenceExecute(redoMutations, commandService);
    return true;
  }
};
var AddSheetDataValidationCommand = {
  type: 0 /* COMMAND */,
  id: "sheet.command.addDataValidation",
  handler(accessor, params) {
    if (!params) {
      return false;
    }
    const { unitId, subUnitId, rule } = params;
    const sheetDataValidationModel = accessor.get(SheetDataValidationModel);
    const commandService = accessor.get(ICommandService);
    const undoRedoService = accessor.get(IUndoRedoService);
    const matrix = sheetDataValidationModel.getRuleObjectMatrix(unitId, subUnitId).clone();
    matrix.addRule(rule);
    const diffs = matrix.diff(sheetDataValidationModel.getRules(unitId, subUnitId));
    const validator = sheetDataValidationModel.getValidator(rule.type);
    const mutationParams = {
      unitId,
      subUnitId,
      rule: {
        ...rule,
        ...validator == null ? void 0 : validator.normalizeFormula(rule, unitId, subUnitId)
      }
    };
    const { redoMutations, undoMutations } = getDataValidationDiffMutations(unitId, subUnitId, diffs, accessor);
    redoMutations.push({
      id: AddDataValidationMutation.id,
      params: mutationParams
    });
    undoMutations.unshift({
      id: RemoveDataValidationMutation.id,
      params: {
        unitId,
        subUnitId,
        ruleId: rule.uid
      }
    });
    undoRedoService.pushUndoRedo({
      unitID: unitId,
      redoMutations,
      undoMutations
    });
    sequenceExecute(redoMutations, commandService);
    return true;
  }
};
var UpdateSheetDataValidationSettingCommand = {
  type: 0 /* COMMAND */,
  id: "sheets.command.update-data-validation-setting",
  // eslint-disable-next-line max-lines-per-function
  handler(accessor, params) {
    if (!params) {
      return false;
    }
    const commandService = accessor.get(ICommandService);
    const redoUndoService = accessor.get(IUndoRedoService);
    const sheetDataValidationModel = accessor.get(SheetDataValidationModel);
    const dataValidatorRegistryService = accessor.get(DataValidatorRegistryService);
    const { unitId, subUnitId, ruleId, setting } = params;
    const validator = dataValidatorRegistryService.getValidatorItem(setting.type);
    if (!validator) {
      return false;
    }
    const rule = sheetDataValidationModel.getRuleById(unitId, subUnitId, ruleId);
    if (!rule) {
      return false;
    }
    const newRule = { ...rule, ...setting };
    if (!validator.validatorFormula(newRule, unitId, subUnitId).success) {
      return false;
    }
    const mutationParams = {
      unitId,
      subUnitId,
      ruleId,
      payload: {
        type: 0 /* SETTING */,
        payload: {
          ...setting,
          ...validator.normalizeFormula(newRule, unitId, subUnitId)
        }
      }
    };
    const redoMutations = [{
      id: UpdateDataValidationMutation.id,
      params: mutationParams
    }];
    const undoMutationParams = {
      unitId,
      subUnitId,
      ruleId,
      payload: {
        type: 0 /* SETTING */,
        payload: getRuleSetting(rule)
      }
    };
    const undoMutations = [{
      id: UpdateDataValidationMutation.id,
      params: undoMutationParams
    }];
    if (setting.type === "checkbox" /* CHECKBOX */) {
      const ranges = rule.ranges;
      const univerInstanceService = accessor.get(IUniverInstanceService);
      const target = getSheetCommandTarget(univerInstanceService, { unitId, subUnitId });
      if (target) {
        const redoMatrix = new ObjectMatrix();
        const { worksheet } = target;
        const { formula2: oldFormula2 = CHECKBOX_FORMULA_2, formula1: oldFormula1 = CHECKBOX_FORMULA_1 } = rule;
        const { formula2 = CHECKBOX_FORMULA_2, formula1 = CHECKBOX_FORMULA_1 } = setting;
        let setted = false;
        ranges.forEach((range) => {
          Range.foreach(range, (row, column) => {
            const cellData = worksheet.getCellRaw(row, column);
            const value = getStringCellValue(cellData);
            if ((isBlankCell(cellData) || value === String(oldFormula2)) && !(cellData == null ? void 0 : cellData.p)) {
              redoMatrix.setValue(row, column, {
                v: formula2,
                p: null
              });
              setted = true;
            } else if (value === String(oldFormula1) && !(cellData == null ? void 0 : cellData.p)) {
              redoMatrix.setValue(row, column, {
                v: formula1,
                p: null
              });
              setted = true;
            }
          });
        });
        if (setted) {
          const redoSetRangeValues = {
            id: SetRangeValuesMutation.id,
            params: {
              unitId,
              subUnitId,
              cellValue: redoMatrix.getData()
            }
          };
          const undoSetRangeValues = {
            id: SetRangeValuesMutation.id,
            params: SetRangeValuesUndoMutationFactory(accessor, redoSetRangeValues.params)
          };
          redoMutations.push(redoSetRangeValues);
          undoMutations.push(undoSetRangeValues);
        }
      }
    }
    const res = sequenceExecute(redoMutations, commandService);
    if (res.result) {
      redoUndoService.pushUndoRedo({
        unitID: unitId,
        redoMutations,
        undoMutations
      });
      return true;
    }
    return false;
  }
};
var UpdateSheetDataValidationOptionsCommand = {
  type: 0 /* COMMAND */,
  id: "sheets.command.update-data-validation-options",
  handler(accessor, params) {
    if (!params) {
      return false;
    }
    const commandService = accessor.get(ICommandService);
    const redoUndoService = accessor.get(IUndoRedoService);
    const sheetDataValidationModel = accessor.get(SheetDataValidationModel);
    const { unitId, subUnitId, ruleId, options } = params;
    const rule = sheetDataValidationModel.getRuleById(unitId, subUnitId, ruleId);
    if (!rule) {
      return false;
    }
    const mutationParams = {
      unitId,
      subUnitId,
      ruleId,
      payload: {
        type: 2 /* OPTIONS */,
        payload: options
      }
    };
    const redoMutations = [{
      id: UpdateDataValidationMutation.id,
      params: mutationParams
    }];
    const undoMutationParams = {
      unitId,
      subUnitId,
      ruleId,
      payload: {
        type: 2 /* OPTIONS */,
        payload: getRuleOptions(rule)
      }
    };
    const undoMutations = [{
      id: UpdateDataValidationMutation.id,
      params: undoMutationParams
    }];
    redoUndoService.pushUndoRedo({
      unitID: unitId,
      redoMutations,
      undoMutations
    });
    commandService.executeCommand(UpdateDataValidationMutation.id, mutationParams);
    return true;
  }
};
var ClearRangeDataValidationCommand = {
  type: 0 /* COMMAND */,
  id: "sheets.command.clear-range-data-validation",
  handler(accessor, params) {
    if (!params) {
      return false;
    }
    const { unitId, subUnitId, ranges } = params;
    const commandService = accessor.get(ICommandService);
    const univerInstanceService = accessor.get(IUniverInstanceService);
    const target = getSheetCommandTarget(univerInstanceService, { unitId, subUnitId });
    const sheetDataValidationModel = accessor.get(SheetDataValidationModel);
    if (!target) return false;
    const undoRedoService = accessor.get(IUndoRedoService);
    const matrix = sheetDataValidationModel.getRuleObjectMatrix(unitId, subUnitId).clone();
    matrix.removeRange(ranges);
    const diffs = matrix.diff(sheetDataValidationModel.getRules(unitId, subUnitId));
    const { redoMutations, undoMutations } = getDataValidationDiffMutations(unitId, subUnitId, diffs, accessor);
    undoRedoService.pushUndoRedo({
      unitID: unitId,
      redoMutations,
      undoMutations
    });
    return sequenceExecute(redoMutations, commandService).result;
  }
};
var RemoveSheetAllDataValidationCommand = {
  type: 0 /* COMMAND */,
  id: "sheet.command.remove-all-data-validation",
  handler(accessor, params) {
    if (!params) {
      return false;
    }
    const { unitId, subUnitId } = params;
    const commandService = accessor.get(ICommandService);
    const sheetDataValidationModel = accessor.get(SheetDataValidationModel);
    const undoRedoService = accessor.get(IUndoRedoService);
    const currentRules = [...sheetDataValidationModel.getRules(unitId, subUnitId)];
    const redoParams = {
      unitId,
      subUnitId,
      ruleId: currentRules.map((rule) => rule.uid)
    };
    const redoMutations = [{
      id: RemoveDataValidationMutation.id,
      params: redoParams
    }];
    const undoMutations = [{
      id: AddDataValidationMutation.id,
      params: {
        unitId,
        subUnitId,
        rule: currentRules
      }
    }];
    undoRedoService.pushUndoRedo({
      redoMutations,
      undoMutations,
      unitID: unitId
    });
    commandService.executeCommand(RemoveDataValidationMutation.id, redoParams);
    return true;
  }
};
var removeDataValidationUndoFactory = (accessor, redoParams) => {
  const sheetDataValidationModel = accessor.get(SheetDataValidationModel);
  const { unitId, subUnitId, ruleId, source } = redoParams;
  if (Array.isArray(ruleId)) {
    const rules = ruleId.map((id) => sheetDataValidationModel.getRuleById(unitId, subUnitId, id)).filter(Boolean);
    return [{
      id: AddDataValidationMutation.id,
      params: {
        unitId,
        subUnitId,
        rule: rules,
        source
      }
    }];
  }
  const undoMutations = [{
    id: AddDataValidationMutation.id,
    params: {
      unitId,
      subUnitId,
      rule: {
        ...sheetDataValidationModel.getRuleById(unitId, subUnitId, ruleId)
      },
      index: sheetDataValidationModel.getRuleIndex(unitId, subUnitId, ruleId)
    }
  }];
  return undoMutations;
};
var RemoveSheetDataValidationCommand = {
  type: 0 /* COMMAND */,
  id: "sheet.command.remove-data-validation-rule",
  handler(accessor, params) {
    if (!params) {
      return false;
    }
    const { unitId, subUnitId, ruleId } = params;
    const commandService = accessor.get(ICommandService);
    const undoRedoService = accessor.get(IUndoRedoService);
    const sheetDataValidationModel = accessor.get(SheetDataValidationModel);
    const redoMutations = [{
      id: RemoveDataValidationMutation.id,
      params
    }];
    const undoMutations = [{
      id: AddDataValidationMutation.id,
      params: {
        unitId,
        subUnitId,
        rule: {
          ...sheetDataValidationModel.getRuleById(unitId, subUnitId, ruleId)
        },
        index: sheetDataValidationModel.getRuleIndex(unitId, subUnitId, ruleId)
      }
    }];
    undoRedoService.pushUndoRedo({
      undoMutations,
      redoMutations,
      unitID: params.unitId
    });
    commandService.executeCommand(RemoveDataValidationMutation.id, params);
    return true;
  }
};

// ../packages/sheets-data-validation/src/common/const.ts
var DATA_VALIDATION_PLUGIN_NAME2 = "SHEET_DATA_VALIDATION_PLUGIN";

// ../packages/sheets-data-validation/src/config/config.ts
var SHEETS_DATA_VALIDATION_PLUGIN_CONFIG_KEY = "sheets-data-validation.config";
var configSymbol5 = Symbol(SHEETS_DATA_VALIDATION_PLUGIN_CONFIG_KEY);
var defaultPluginConfig5 = {};

// ../packages/sheets-data-validation/src/controllers/dv-formula-ref-range.controller.ts
var DataValidationFormulaRefRangeController = class extends Disposable {
  constructor(_dataValidationModel, _formulaRefRangeService, _validatorRegistryService) {
    super();
    __publicField(this, "_dataValidationModel", _dataValidationModel);
    __publicField(this, "_formulaRefRangeService", _formulaRefRangeService);
    __publicField(this, "_validatorRegistryService", _validatorRegistryService);
    __publicField(this, "_disposableMap", /* @__PURE__ */ new Map());
    __publicField(this, "registerRule", (unitId, subUnitId, rule) => {
      if (!shouldOffsetFormulaByRange(rule.type, this._validatorRegistryService)) {
        return;
      }
      this.register(unitId, subUnitId, rule);
    });
    this._initRefRange();
  }
  _getIdWithUnitId(unitID, subUnitId, ruleId) {
    return `${unitID}_${subUnitId}_${ruleId}`;
  }
  // eslint-disable-next-line max-lines-per-function
  register(unitId, subUnitId, rule) {
    const oldRanges = rule.ranges;
    const oldFormula1 = rule.formula1;
    const oldFormula2 = rule.formula2;
    const disposable = this._formulaRefRangeService.registerRangeFormula(unitId, subUnitId, oldRanges, [oldFormula1 != null ? oldFormula1 : "", oldFormula2 != null ? oldFormula2 : ""], (res) => {
      if (res.length === 0) {
        return {
          undos: [{
            id: AddDataValidationMutation.id,
            params: {
              unitId,
              subUnitId,
              rule,
              source: "patched"
            }
          }],
          redos: [{
            id: RemoveDataValidationMutation.id,
            params: {
              unitId,
              subUnitId,
              ruleId: rule.uid,
              source: "patched"
            }
          }]
        };
      }
      const redos = [];
      const undos = [];
      const first2 = res[0];
      redos.push({
        id: UpdateDataValidationMutation.id,
        params: {
          unitId,
          subUnitId,
          ruleId: rule.uid,
          payload: {
            type: 3 /* ALL */,
            payload: {
              ranges: first2.ranges,
              formula1: first2.formulas[0],
              formula2: first2.formulas[1]
            }
          },
          source: "patched"
        }
      });
      undos.push({
        id: UpdateDataValidationMutation.id,
        params: {
          unitId,
          subUnitId,
          ruleId: rule.uid,
          payload: {
            type: 3 /* ALL */,
            payload: {
              ranges: oldRanges,
              formula1: oldFormula1,
              formula2: oldFormula2
            }
          },
          source: "patched"
        }
      });
      for (let i = 1; i < res.length; i++) {
        const item = res[i];
        const id2 = generateRandomId();
        redos.push({
          id: AddDataValidationMutation.id,
          params: {
            unitId,
            subUnitId,
            rule: {
              ...rule,
              uid: id2,
              formula1: item.formulas[0],
              formula2: item.formulas[1],
              ranges: item.ranges
            },
            source: "patched"
          }
        });
        undos.push({
          id: RemoveDataValidationMutation.id,
          params: {
            unitId,
            subUnitId,
            ruleId: id2,
            source: "patched"
          }
        });
      }
      return {
        undos,
        redos
      };
    });
    const id = this._getIdWithUnitId(unitId, subUnitId, rule.uid);
    this._disposableMap.set(id, disposable);
  }
  _initRefRange() {
    const allRules = this._dataValidationModel.getAll();
    for (const [unitId, subUnitMap] of allRules) {
      for (const [subUnitId, rules] of subUnitMap) {
        for (const rule of rules) {
          this.registerRule(unitId, subUnitId, rule);
        }
      }
    }
    this.disposeWithMe(
      this._dataValidationModel.ruleChange$.subscribe((option) => {
        const { unitId, subUnitId, rule } = option;
        switch (option.type) {
          case "add": {
            const rule2 = option.rule;
            this.registerRule(option.unitId, option.subUnitId, rule2);
            break;
          }
          case "remove": {
            const disposeSet = this._disposableMap.get(this._getIdWithUnitId(unitId, subUnitId, rule.uid));
            if (disposeSet) {
              disposeSet.dispose();
            }
            break;
          }
          case "update": {
            const rule2 = option.rule;
            const disposeSet = this._disposableMap.get(this._getIdWithUnitId(unitId, subUnitId, rule2.uid));
            if (disposeSet) {
              disposeSet.dispose();
            }
            this.registerRule(option.unitId, option.subUnitId, rule2);
            break;
          }
        }
      })
    );
    this.disposeWithMe(toDisposable(() => {
      this._disposableMap.forEach((item) => {
        item.dispose();
      });
      this._disposableMap.clear();
    }));
  }
};
DataValidationFormulaRefRangeController = __decorateClass([
  __decorateParam(0, Inject(SheetDataValidationModel)),
  __decorateParam(1, Inject(FormulaRefRangeService)),
  __decorateParam(2, Inject(DataValidatorRegistryService))
], DataValidationFormulaRefRangeController);

// ../packages/sheets-data-validation/src/controllers/dv-formula.controller.ts
var DataValidationFormulaController = class extends Disposable {
  constructor(_univerInstanceService, _permissionService, _lexerTreeBuilder) {
    super();
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_permissionService", _permissionService);
    __publicField(this, "_lexerTreeBuilder", _lexerTreeBuilder);
  }
  getFormulaRefCheck(formulaString) {
    var _a, _b;
    const sequenceNodes = this._lexerTreeBuilder.sequenceNodesBuilder(formulaString);
    if (!sequenceNodes) {
      return true;
    }
    for (let i = 0; i < sequenceNodes.length; i++) {
      const node = sequenceNodes[i];
      if (typeof node === "string") {
        continue;
      }
      const { token } = node;
      const sequenceGrid = deserializeRangeWithSheetWithCache(token);
      const workbook = this._univerInstanceService.getCurrentUnitOfType(2 /* UNIVER_SHEET */);
      let targetSheet = workbook.getActiveSheet();
      const unitId = workbook.getUnitId();
      if (sequenceGrid.sheetName) {
        targetSheet = workbook.getSheetBySheetName(sequenceGrid.sheetName);
        if (!targetSheet) {
          return false;
        }
        const subUnitId = targetSheet == null ? void 0 : targetSheet.getSheetId();
        const viewPermission = this._permissionService.getPermissionPoint(new WorksheetViewPermission(unitId, subUnitId).id);
        if (!viewPermission) return false;
      }
      if (!targetSheet) {
        return false;
      }
      const { startRow, endRow, startColumn, endColumn } = sequenceGrid.range;
      for (let i2 = startRow; i2 <= endRow; i2++) {
        for (let j = startColumn; j <= endColumn; j++) {
          const permission = (_b = (_a = targetSheet.getCell(i2, j)) == null ? void 0 : _a.selectionProtection) == null ? void 0 : _b[0];
          if ((permission == null ? void 0 : permission[0 /* View */]) === false) {
            return false;
          }
        }
      }
    }
    return true;
  }
};
DataValidationFormulaController = __decorateClass([
  __decorateParam(0, IUniverInstanceService),
  __decorateParam(1, IPermissionService),
  __decorateParam(2, Inject(LexerTreeBuilder))
], DataValidationFormulaController);

// ../packages/sheets-data-validation/src/controllers/dv-ref-range.controller.ts
var DataValidationRefRangeController = class extends Disposable {
  constructor(_dataValidationModel, _injector, _refRangeService, _dataValidationFormulaService, _formulaRefRangeService, _validatorRegistryService) {
    super();
    __publicField(this, "_dataValidationModel", _dataValidationModel);
    __publicField(this, "_injector", _injector);
    __publicField(this, "_refRangeService", _refRangeService);
    __publicField(this, "_dataValidationFormulaService", _dataValidationFormulaService);
    __publicField(this, "_formulaRefRangeService", _formulaRefRangeService);
    __publicField(this, "_validatorRegistryService", _validatorRegistryService);
    __publicField(this, "_disposableMap", /* @__PURE__ */ new Map());
    __publicField(this, "registerRule", (unitId, subUnitId, rule) => {
      if (shouldOffsetFormulaByRange(rule.type, this._validatorRegistryService)) {
        return;
      }
      this.register(unitId, subUnitId, rule);
      this.registerFormula(unitId, subUnitId, rule);
    });
    this._initRefRange();
  }
  _getIdWithUnitId(unitID, subUnitId, ruleId) {
    return `${unitID}_${subUnitId}_${ruleId}`;
  }
  // eslint-disable-next-line max-lines-per-function
  registerFormula(unitId, subUnitId, rule) {
    var _a;
    const ruleId = rule.uid;
    const id = this._getIdWithUnitId(unitId, subUnitId, ruleId);
    const disposeSet = (_a = this._disposableMap.get(id)) != null ? _a : /* @__PURE__ */ new Set();
    const handleFormulaChange = (type2, formulaString) => {
      const oldRule = this._dataValidationModel.getRuleById(unitId, subUnitId, ruleId);
      if (!oldRule) {
        return { redos: [], undos: [] };
      }
      const oldFormula = oldRule[type2];
      if (!oldFormula || oldFormula === formulaString) {
        return { redos: [], undos: [] };
      }
      const redoParams = {
        unitId,
        subUnitId,
        ruleId: rule.uid,
        payload: {
          type: 0 /* SETTING */,
          payload: {
            type: oldRule.type,
            formula1: oldRule.formula1,
            formula2: oldRule.formula2,
            [type2]: formulaString
          }
        },
        source: "patched"
      };
      const undoParams = {
        unitId,
        subUnitId,
        ruleId: rule.uid,
        payload: {
          type: 0 /* SETTING */,
          payload: {
            type: oldRule.type,
            formula1: oldRule.formula1,
            formula2: oldRule.formula2
          }
        },
        source: "patched"
      };
      const redos = [
        {
          id: UpdateDataValidationMutation.id,
          params: redoParams
        }
      ];
      const undos = [
        {
          id: UpdateDataValidationMutation.id,
          params: undoParams
        }
      ];
      return { redos, undos };
    };
    const currentFormula = this._dataValidationFormulaService.getRuleFormulaInfo(unitId, subUnitId, ruleId);
    if (currentFormula) {
      const [formula1, formula2] = currentFormula;
      if (formula1) {
        const disposable = this._formulaRefRangeService.registerFormula(
          unitId,
          subUnitId,
          formula1.text,
          (newFormulaString) => handleFormulaChange("formula1", newFormulaString)
        );
        disposeSet.add(() => disposable.dispose());
      }
      if (formula2) {
        const disposable = this._formulaRefRangeService.registerFormula(
          unitId,
          subUnitId,
          formula2.text,
          (newFormulaString) => handleFormulaChange("formula2", newFormulaString)
        );
        disposeSet.add(() => disposable.dispose());
      }
    }
  }
  register(unitId, subUnitId, rule) {
    var _a;
    const handleRangeChange = (commandInfo) => {
      const oldRanges = [...rule.ranges];
      const resultRangesOrigin = oldRanges.map((range) => {
        return handleCommonDefaultRangeChangeWithEffectRefCommands(range, commandInfo);
      }).filter((range) => !!range);
      const resultRanges = resultRangesOrigin.flat();
      const isEqual = isRangesEqual(resultRanges, oldRanges);
      if (isEqual) {
        return { redos: [], undos: [] };
      }
      if (resultRanges.length) {
        const redoParams = {
          unitId,
          subUnitId,
          ruleId: rule.uid,
          payload: {
            type: 1 /* RANGE */,
            payload: resultRanges
          },
          source: "patched"
        };
        const redos = [{ id: UpdateDataValidationMutation.id, params: redoParams }];
        const undos = [{
          id: UpdateDataValidationMutation.id,
          params: {
            unitId,
            subUnitId,
            ruleId: rule.uid,
            payload: {
              type: 1 /* RANGE */,
              payload: oldRanges
            },
            source: "patched"
          }
        }];
        return { redos, undos };
      } else {
        const redoParams = { unitId, subUnitId, ruleId: rule.uid };
        const redos = [{ id: RemoveDataValidationMutation.id, params: redoParams }];
        const undos = removeDataValidationUndoFactory(this._injector, redoParams);
        return { redos, undos };
      }
    };
    const disposeList = [];
    rule.ranges.forEach((range) => {
      const disposable = this._refRangeService.registerRefRange(range, handleRangeChange, unitId, subUnitId);
      disposeList.push(() => disposable.dispose());
    });
    const id = this._getIdWithUnitId(unitId, subUnitId, rule.uid);
    const current = (_a = this._disposableMap.get(id)) != null ? _a : /* @__PURE__ */ new Set();
    current.add(() => disposeList.forEach((dispose) => dispose()));
    this._disposableMap.set(id, current);
  }
  _initRefRange() {
    const allRules = this._dataValidationModel.getAll();
    for (const [unitId, subUnitMap] of allRules) {
      for (const [subUnitId, rules] of subUnitMap) {
        for (const rule of rules) {
          this.registerRule(unitId, subUnitId, rule);
        }
      }
    }
    this.disposeWithMe(
      this._dataValidationModel.ruleChange$.subscribe((option) => {
        const { unitId, subUnitId, rule } = option;
        switch (option.type) {
          case "add": {
            const rule2 = option.rule;
            this.registerRule(option.unitId, option.subUnitId, rule2);
            break;
          }
          case "remove": {
            const disposeSet = this._disposableMap.get(this._getIdWithUnitId(unitId, subUnitId, rule.uid));
            if (disposeSet) {
              disposeSet.forEach((dispose) => dispose());
            }
            break;
          }
          case "update": {
            const rule2 = option.rule;
            const disposeSet = this._disposableMap.get(this._getIdWithUnitId(unitId, subUnitId, rule2.uid));
            if (disposeSet) {
              disposeSet.forEach((dispose) => dispose());
            }
            this.registerRule(option.unitId, option.subUnitId, rule2);
            break;
          }
        }
      })
    );
    this.disposeWithMe(toDisposable(() => {
      this._disposableMap.forEach((item) => {
        item.forEach((dispose) => dispose());
      });
      this._disposableMap.clear();
    }));
  }
};
DataValidationRefRangeController = __decorateClass([
  __decorateParam(0, Inject(SheetDataValidationModel)),
  __decorateParam(1, Inject(Injector)),
  __decorateParam(2, Inject(RefRangeService)),
  __decorateParam(3, Inject(DataValidationFormulaService)),
  __decorateParam(4, Inject(FormulaRefRangeService)),
  __decorateParam(5, Inject(DataValidatorRegistryService))
], DataValidationRefRangeController);

// ../packages/sheets-data-validation/src/controllers/dv-sheet.controller.ts
var SheetDataValidationSheetController = class extends Disposable {
  constructor(_sheetInterceptorService, _univerInstanceService, _sheetDataValidationModel) {
    super();
    __publicField(this, "_sheetInterceptorService", _sheetInterceptorService);
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_sheetDataValidationModel", _sheetDataValidationModel);
    this._initSheetChange();
  }
  // eslint-disable-next-line max-lines-per-function
  _initSheetChange() {
    this.disposeWithMe(
      this._sheetInterceptorService.interceptCommand({
        // eslint-disable-next-line max-lines-per-function
        getMutations: (commandInfo) => {
          var _a;
          if (commandInfo.id === RemoveSheetCommand.id) {
            const params = commandInfo.params;
            const unitId = params.unitId || this._univerInstanceService.getCurrentUnitOfType(2 /* UNIVER_SHEET */).getUnitId();
            const workbook = this._univerInstanceService.getUniverSheetInstance(unitId);
            if (!workbook) {
              return { redos: [], undos: [] };
            }
            const subUnitId = params.subUnitId || ((_a = workbook.getActiveSheet()) == null ? void 0 : _a.getSheetId());
            if (!subUnitId) {
              return { redos: [], undos: [] };
            }
            const rules = this._sheetDataValidationModel.getRules(unitId, subUnitId);
            if (rules.length === 0) {
              return { redos: [], undos: [] };
            }
            const ids = rules.map((i) => i.uid);
            const redoParams = {
              unitId,
              subUnitId,
              ruleId: ids,
              source: "patched"
            };
            const undoParams = {
              unitId,
              subUnitId,
              rule: [...rules],
              source: "patched"
            };
            return {
              redos: [{
                id: RemoveDataValidationMutation.id,
                params: redoParams
              }],
              undos: [{
                id: AddDataValidationMutation.id,
                params: undoParams
              }]
            };
          } else if (commandInfo.id === CopySheetCommand.id) {
            const params = commandInfo.params;
            const { unitId, subUnitId, targetSubUnitId } = params;
            if (!unitId || !subUnitId || !targetSubUnitId) {
              return { redos: [], undos: [] };
            }
            const rules = this._sheetDataValidationModel.getRules(unitId, subUnitId);
            if (rules.length === 0) {
              return { redos: [], undos: [] };
            }
            const newRules = rules.map((rule) => {
              const newRule = { ...rule, uid: generateRandomId(6) };
              return newRule;
            });
            return {
              redos: [
                {
                  id: AddDataValidationMutation.id,
                  params: {
                    unitId,
                    subUnitId: targetSubUnitId,
                    rule: newRules,
                    source: "patched"
                  }
                }
              ],
              undos: [
                {
                  id: RemoveDataValidationMutation.id,
                  params: {
                    unitId,
                    subUnitId: targetSubUnitId,
                    ruleId: newRules.map((r) => r.uid),
                    source: "patched"
                  }
                }
              ]
            };
          }
          return { redos: [], undos: [] };
        }
      })
    );
  }
};
SheetDataValidationSheetController = __decorateClass([
  __decorateParam(0, Inject(SheetInterceptorService)),
  __decorateParam(1, Inject(IUniverInstanceService)),
  __decorateParam(2, Inject(SheetDataValidationModel))
], SheetDataValidationSheetController);

// ../packages/sheets-data-validation/src/validators/any-validator.ts
var AnyValidator = class extends BaseSheetValidator {
  constructor() {
    super(...arguments);
    __publicField(this, "id", "any" /* ANY */);
    __publicField(this, "title", "sheets-data-validation.any.title");
    __publicField(this, "operators", []);
    __publicField(this, "scopes", ["sheet"]);
    __publicField(this, "order", 0);
    __publicField(this, "offsetFormulaByRange", false);
  }
  async parseFormula(rule, unitId, subUnitId) {
    return {
      formula1: rule.formula1,
      formula2: rule.formula2,
      isFormulaValid: true
    };
  }
  validatorFormula(rule, unitId, subUnitId) {
    return {
      success: true
    };
  }
  async isValidType(cellInfo, formula, rule) {
    return true;
  }
  generateRuleErrorMessage(rule) {
    return this.localeService.t("sheets-data-validation.any.error");
  }
};

// ../packages/sheets-data-validation/src/validators/custom-validator.ts
var CustomFormulaValidator = class extends BaseSheetValidator {
  constructor() {
    super(...arguments);
    __publicField(this, "id", "custom" /* CUSTOM */);
    __publicField(this, "title", "sheets-data-validation.custom.title");
    __publicField(this, "operators", []);
    __publicField(this, "scopes", ["sheet"]);
    __publicField(this, "order", 60);
    __publicField(this, "_customFormulaService", this.injector.get(DataValidationCustomFormulaService));
    __publicField(this, "_lexerTreeBuilder", this.injector.get(LexerTreeBuilder));
  }
  validatorFormula(rule, unitId, subUnitId) {
    var _a;
    const success = isFormulaString(rule.formula1);
    const formulaText = (_a = rule.formula1) != null ? _a : "";
    const result = this._lexerTreeBuilder.checkIfAddBracket(formulaText);
    const valid = result === 0 && formulaText.startsWith("=" /* EQUALS */);
    return {
      success: success && valid,
      formula1: success && valid ? "" : this.localeService.t("sheets-data-validation.validFail.formula")
    };
  }
  async parseFormula(_rule, _unitId, _subUnitId) {
    return {
      formula1: void 0,
      formula2: void 0,
      isFormulaValid: true
    };
  }
  async isValidType(cellInfo, _formula, _rule) {
    const { column, row, unitId, subUnitId } = cellInfo;
    const cellData = await this._customFormulaService.getCellFormulaValue(unitId, subUnitId, _rule.uid, row, column);
    const formulaResult = cellData == null ? void 0 : cellData.v;
    if (!isLegalFormulaResult(String(formulaResult))) {
      return false;
    }
    if (Tools.isDefine(formulaResult) && formulaResult !== "") {
      if (cellData.t === 3 /* BOOLEAN */) {
        return Boolean(formulaResult);
      }
      if (typeof formulaResult === "boolean") {
        return formulaResult;
      }
      if (typeof formulaResult === "number") {
        return Boolean(formulaResult);
      }
      if (typeof formulaResult === "string") {
        return isLegalFormulaResult(formulaResult);
      }
      return Boolean(formulaResult);
    }
    return false;
  }
  generateRuleErrorMessage(rule) {
    return this.localeService.t("sheets-data-validation.custom.error");
  }
  generateRuleName(rule) {
    var _a;
    return this.localeService.t("sheets-data-validation.custom.ruleName").replace("{FORMULA1}", (_a = rule.formula1) != null ? _a : "");
  }
};

// ../packages/sheets-data-validation/src/validators/list-multiple-validator.ts
var ListMultipleValidator = class extends ListValidator {
  constructor() {
    super(...arguments);
    __publicField(this, "id", "listMultiple" /* LIST_MULTIPLE */);
    __publicField(this, "title", "sheets-data-validation.listMultiple.title");
    __publicField(this, "offsetFormulaByRange", false);
    __publicField(this, "skipDefaultFontRender", () => {
      return true;
    });
  }
};

// ../packages/sheets-data-validation/src/validators/whole-validator.ts
var WholeValidator = class extends BaseSheetValidator {
  constructor() {
    super(...arguments);
    __publicField(this, "_customFormulaService", this.injector.get(DataValidationCustomFormulaService));
    __publicField(this, "_lexerTreeBuilder", this.injector.get(LexerTreeBuilder));
    __publicField(this, "id", "whole" /* WHOLE */);
    __publicField(this, "title", "sheets-data-validation.whole.title");
    __publicField(this, "order", 10);
    __publicField(this, "operators", [
      "between" /* BETWEEN */,
      "equal" /* EQUAL */,
      "greaterThan" /* GREATER_THAN */,
      "greaterThanOrEqual" /* GREATER_THAN_OR_EQUAL */,
      "lessThan" /* LESS_THAN */,
      "lessThanOrEqual" /* LESS_THAN_OR_EQUAL */,
      "notBetween" /* NOT_BETWEEN */,
      "notEqual" /* NOT_EQUAL */
    ]);
    __publicField(this, "scopes", ["sheet"]);
  }
  _isFormulaOrInt(formula) {
    return !Tools.isBlank(formula) && (isFormulaString(formula) || !Number.isNaN(+formula) && Number.isInteger(+formula));
  }
  async isValidType(cellInfo, _formula, _rule) {
    const { value: cellValue } = cellInfo;
    const num = getCellValueNumber(cellValue);
    return !Number.isNaN(num) && Number.isInteger(num);
  }
  transform(cellInfo, _formula, _rule) {
    const { value: cellValue } = cellInfo;
    return {
      ...cellInfo,
      value: getCellValueNumber(cellValue)
    };
  }
  _parseNumber(formula) {
    if (formula === void 0 || formula === null) {
      return Number.NaN;
    }
    return +formula;
  }
  async parseFormula(rule, unitId, subUnitId, row, column) {
    const res1 = await this._customFormulaService.getCellFormulaValue(unitId, subUnitId, rule.uid, row, column);
    const res2 = await this._customFormulaService.getCellFormula2Value(unitId, subUnitId, rule.uid, row, column);
    const { formula1, formula2 } = rule;
    const formula1Result = isFormulaString(formula1) ? res1 == null ? void 0 : res1.v : formula1;
    const formula2Result = isFormulaString(formula2) ? res2 == null ? void 0 : res2.v : formula2;
    const isFormulaValid = isLegalFormulaResult(`${formula1Result}`) && isLegalFormulaResult(`${formula2Result}`);
    const info = {
      formula1: this._parseNumber(formula1Result),
      formula2: this._parseNumber(formula2Result),
      isFormulaValid
    };
    return info;
  }
  validatorFormula(rule, _unitId, _subUnitId) {
    const operator = rule.operator;
    if (!operator) {
      return {
        success: true
      };
    }
    const formula1Success = Tools.isDefine(rule.formula1) && this._isFormulaOrInt(rule.formula1);
    const formula2Success = Tools.isDefine(rule.formula2) && this._isFormulaOrInt(rule.formula2);
    const isTwoFormula = TWO_FORMULA_OPERATOR_COUNT2.includes(operator);
    const errorMsg = this.localeService.t("sheets-data-validation.validFail.number");
    if (isTwoFormula) {
      return {
        success: formula1Success && formula2Success,
        formula1: formula1Success ? void 0 : errorMsg,
        formula2: formula2Success ? void 0 : errorMsg
      };
    }
    return {
      success: formula1Success,
      formula1: errorMsg
    };
  }
  generateRuleErrorMessage(rule, position) {
    if (!rule.operator) {
      return this.localeService.t(OperatorErrorTitleMap2.NONE).replace("{TYPE}", this.titleStr);
    }
    const { transformedFormula1, transformedFormula2 } = getTransformedFormula(this._lexerTreeBuilder, rule, position);
    const errorMsg = this.localeService.t(OperatorErrorTitleMap2[rule.operator]).replace(FORMULA13, transformedFormula1 != null ? transformedFormula1 : "").replace(FORMULA23, transformedFormula2 != null ? transformedFormula2 : "");
    return `${errorMsg}`;
  }
};

// ../packages/sheets-data-validation/src/controllers/dv.controller.ts
var DataValidationController = class extends RxDisposable {
  constructor(_univerInstanceService, _dataValidatorRegistryService, _injector, _selectionManagerService, _sheetInterceptorService, _sheetDataValidationModel) {
    super();
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_dataValidatorRegistryService", _dataValidatorRegistryService);
    __publicField(this, "_injector", _injector);
    __publicField(this, "_selectionManagerService", _selectionManagerService);
    __publicField(this, "_sheetInterceptorService", _sheetInterceptorService);
    __publicField(this, "_sheetDataValidationModel", _sheetDataValidationModel);
    this._init();
  }
  _init() {
    this._registerValidators();
    this._initCommandInterceptor();
  }
  _registerValidators() {
    [
      AnyValidator,
      DecimalValidator,
      WholeValidator,
      TextLengthValidator,
      DateValidator,
      CheckboxValidator,
      ListValidator,
      ListMultipleValidator,
      CustomFormulaValidator
    ].forEach((Validator) => {
      const validator = this._injector.createInstance(Validator);
      this.disposeWithMe(this._dataValidatorRegistryService.register(validator));
      this.disposeWithMe(toDisposable(() => this._injector.delete(Validator)));
    });
  }
  _initCommandInterceptor() {
    this._sheetInterceptorService.interceptCommand({
      getMutations: (commandInfo) => {
        var _a;
        if (commandInfo.id === ClearSelectionAllCommand.id) {
          const workbook = this._univerInstanceService.getCurrentUnitOfType(2 /* UNIVER_SHEET */);
          const unitId = workbook.getUnitId();
          const worksheet = workbook.getActiveSheet();
          if (!worksheet) {
            throw new Error("No active sheet found");
          }
          const subUnitId = worksheet.getSheetId();
          const selections = (_a = this._selectionManagerService.getCurrentSelections()) == null ? void 0 : _a.map((s) => s.range);
          const ruleMatrix = this._sheetDataValidationModel.getRuleObjectMatrix(unitId, subUnitId).clone();
          if (selections) {
            ruleMatrix.removeRange(selections);
          }
          const diffs = ruleMatrix.diff(this._sheetDataValidationModel.getRules(unitId, subUnitId));
          const { redoMutations, undoMutations } = getDataValidationDiffMutations(unitId, subUnitId, diffs, this._injector, "patched");
          return {
            undos: undoMutations,
            redos: redoMutations
          };
        }
        return {
          undos: [],
          redos: []
        };
      }
    });
  }
};
DataValidationController = __decorateClass([
  __decorateParam(0, IUniverInstanceService),
  __decorateParam(1, Inject(DataValidatorRegistryService)),
  __decorateParam(2, Inject(Injector)),
  __decorateParam(3, Inject(SheetsSelectionsService)),
  __decorateParam(4, Inject(SheetInterceptorService)),
  __decorateParam(5, Inject(SheetDataValidationModel))
], DataValidationController);

// ../packages/sheets-data-validation/src/services/dv-validator.service.ts
var SheetsDataValidationValidatorService = class extends Disposable {
  constructor(_univerInstanceService, _sheetDataValidationModel, _dataValidationCacheService, _lifecycleService) {
    super();
    __publicField(this, "_univerInstanceService", _univerInstanceService);
    __publicField(this, "_sheetDataValidationModel", _sheetDataValidationModel);
    __publicField(this, "_dataValidationCacheService", _dataValidationCacheService);
    __publicField(this, "_lifecycleService", _lifecycleService);
    this._initRecalculate();
  }
  _initRecalculate() {
    const handleDirtyRanges = (ranges) => {
      if (ranges.length === 0) {
        return;
      }
      const workbook = this._univerInstanceService.getCurrentUnitOfType(2 /* UNIVER_SHEET */);
      const worksheet = workbook == null ? void 0 : workbook.getActiveSheet();
      const map2 = /* @__PURE__ */ Object.create(null);
      ranges.flat().forEach((range) => {
        if (!map2[range.unitId]) {
          map2[range.unitId] = /* @__PURE__ */ Object.create(null);
        }
        if (!map2[range.unitId][range.subUnitId]) {
          map2[range.unitId][range.subUnitId] = [];
        }
        const workbook2 = this._univerInstanceService.getUnit(range.unitId, 2 /* UNIVER_SHEET */);
        const worksheet2 = workbook2 == null ? void 0 : workbook2.getSheetBySheetId(range.subUnitId);
        if (!worksheet2) {
          return;
        }
        map2[range.unitId][range.subUnitId].push(...range.ranges.map((range2) => Range.transformRange(range2, worksheet2)));
      });
      Object.entries(map2).forEach(([unitId, subUnitMap]) => {
        Object.entries(subUnitMap).forEach(([subUnitId, ranges2]) => {
          if ((workbook == null ? void 0 : workbook.getUnitId()) === unitId && (worksheet == null ? void 0 : worksheet.getSheetId()) === subUnitId) {
            this.validatorRanges(unitId, subUnitId, ranges2);
          } else {
            requestIdleCallback(() => {
              this.validatorRanges(unitId, subUnitId, ranges2);
            });
          }
        });
      });
    };
    this.disposeWithMe(this._dataValidationCacheService.dirtyRanges$.pipe(bufferWhen(() => this._lifecycleService.lifecycle$.pipe(skip(1), filter((stage) => stage === 2 /* Rendered */)))).subscribe(handleDirtyRanges));
    this.disposeWithMe(this._dataValidationCacheService.dirtyRanges$.pipe(filter(() => this._lifecycleService.stage >= 2 /* Rendered */), bufferDebounceTime(20)).subscribe(handleDirtyRanges));
  }
  async _validatorByCell(workbook, worksheet, row, col) {
    const unitId = workbook.getUnitId();
    const subUnitId = worksheet.getSheetId();
    if (!Tools.isDefine(row) || !Tools.isDefine(col)) {
      throw new Error(`row or col is not defined, row: ${row}, col: ${col}`);
    }
    let _row = row;
    let _col = col;
    const mergedCell = worksheet.getMergedCell(row, col);
    if (mergedCell) {
      _row = mergedCell.startRow;
      _col = mergedCell.startColumn;
    }
    const rule = this._sheetDataValidationModel.getRuleByLocation(unitId, subUnitId, _row, _col);
    if (!rule) {
      return "valid" /* VALID */;
    }
    return new Promise((resolve) => {
      this._sheetDataValidationModel.validator(rule, { unitId, subUnitId, row: _row, col: _col, worksheet, workbook }, (status) => {
        resolve(status);
      });
    });
  }
  async validatorCell(unitId, subUnitId, row, col) {
    const workbook = this._univerInstanceService.getUnit(unitId, 2 /* UNIVER_SHEET */);
    if (!workbook) {
      throw new Error(`cannot find current workbook, unitId: ${unitId}`);
    }
    const worksheet = workbook.getSheetBySheetId(subUnitId);
    if (!worksheet) {
      throw new Error(`cannot find current worksheet, sheetId: ${subUnitId}`);
    }
    return this._validatorByCell(workbook, worksheet, row, col);
  }
  async validatorRanges(unitId, subUnitId, ranges) {
    if (!ranges.length) {
      return Promise.resolve([]);
    }
    const workbook = this._univerInstanceService.getUnit(unitId, 2 /* UNIVER_SHEET */);
    if (!workbook) {
      throw new Error(`cannot find current workbook, unitId: ${unitId}`);
    }
    const worksheet = workbook.getSheetBySheetId(subUnitId);
    if (!worksheet) {
      throw new Error(`cannot find current worksheet, sheetId: ${subUnitId}`);
    }
    const allRules = this._sheetDataValidationModel.getRules(unitId, subUnitId);
    const ruleRanges = [];
    for (const rule of allRules) {
      ruleRanges.push(...rule.ranges);
    }
    const intersectRanges = [];
    for (const range of ranges) {
      for (const ruleRange of ruleRanges) {
        const intersect = getIntersectRange(range, ruleRange);
        if (intersect) {
          intersectRanges.push(intersect);
        }
      }
    }
    const mergeCells = [];
    const result = await Promise.all(
      intersectRanges.map((range, resultRowIndex) => {
        const promises = [];
        for (let row = range.startRow; row <= range.endRow; row++) {
          for (let col = range.startColumn; col <= range.endColumn; col++) {
            promises.push(this._validatorByCell(workbook, worksheet, row, col));
            const mergedCell = worksheet.getMergedCell(row, col);
            if (mergedCell) {
              mergeCells.push({
                resultRowIndex,
                resultColIndex: promises.length - 1,
                row: mergedCell.startRow,
                col: mergedCell.startColumn
              });
            }
          }
        }
        return Promise.all(promises);
      })
    );
    if (mergeCells.length) {
      mergeCells.forEach(({ resultRowIndex, resultColIndex, row, col }) => {
        var _a;
        if (result[resultRowIndex][resultColIndex] === "validating" /* VALIDATING */) {
          result[resultRowIndex][resultColIndex] = (_a = this._dataValidationCacheService.getValue(unitId, subUnitId, row, col)) != null ? _a : "valid" /* VALID */;
        }
      });
    }
    return result;
  }
  async validatorWorksheet(unitId, subUnitId) {
    const workbook = this._univerInstanceService.getUnit(unitId, 2 /* UNIVER_SHEET */);
    if (!workbook) {
      throw new Error(`cannot find current workbook, unitId: ${unitId}`);
    }
    const worksheet = workbook.getSheetBySheetId(subUnitId);
    if (!worksheet) {
      throw new Error(`cannot find current worksheet, sheetId: ${subUnitId}`);
    }
    const rules = this._sheetDataValidationModel.getRules(unitId, subUnitId);
    await Promise.all(
      rules.map((rule) => {
        return Promise.all(
          rule.ranges.map((range) => {
            const promises = [];
            Range.foreach(range, (row, col) => {
              promises.push(this._validatorByCell(workbook, worksheet, row, col));
            });
            return Promise.all(promises);
          })
        );
      })
    );
    return this._dataValidationCacheService.ensureCache(unitId, subUnitId);
  }
  async validatorWorkbook(unitId) {
    const sheetIds = this._sheetDataValidationModel.getSubUnitIds(unitId);
    const results = await Promise.all(sheetIds.map((id) => this.validatorWorksheet(unitId, id)));
    const map2 = /* @__PURE__ */ Object.create(null);
    results.forEach((result, i) => {
      map2[sheetIds[i]] = result;
    });
    return map2;
  }
  getDataValidations(unitId, subUnitId, ranges) {
    const ruleMatrix = this._sheetDataValidationModel.getRuleObjectMatrix(unitId, subUnitId);
    const ruleIdSet = /* @__PURE__ */ new Set();
    ranges.forEach((range) => {
      Range.foreach(range, (row, col) => {
        const ruleId = ruleMatrix.getValue(row, col);
        if (ruleId) {
          ruleIdSet.add(ruleId);
        }
      });
    });
    const rules = [];
    for (const id of ruleIdSet) {
      const rule = this._sheetDataValidationModel.getRuleById(unitId, subUnitId, id);
      if (rule) {
        rules.push(rule);
      }
    }
    return rules;
  }
  getDataValidation(unitId, subUnitId, ranges) {
    return this.getDataValidations(unitId, subUnitId, ranges)[0];
  }
};
SheetsDataValidationValidatorService = __decorateClass([
  __decorateParam(0, IUniverInstanceService),
  __decorateParam(1, Inject(SheetDataValidationModel)),
  __decorateParam(2, Inject(DataValidationCacheService)),
  __decorateParam(3, Inject(LifecycleService))
], SheetsDataValidationValidatorService);

// ../packages/sheets-data-validation/src/plugin.ts
var UniverSheetsDataValidationPlugin = class extends Plugin {
  constructor(_config = defaultPluginConfig5, _injector, _commandService, _configService) {
    super();
    __publicField(this, "_config", _config);
    __publicField(this, "_injector", _injector);
    __publicField(this, "_commandService", _commandService);
    __publicField(this, "_configService", _configService);
    const { ...rest } = merge_default(
      {},
      defaultPluginConfig5,
      this._config
    );
    this._configService.setConfig(SHEETS_DATA_VALIDATION_PLUGIN_CONFIG_KEY, rest);
  }
  onStarting() {
    [
      [DataValidationCacheService],
      [DataValidationListCacheService],
      [DataValidationFormulaService],
      [DataValidationCustomFormulaService],
      [SheetsDataValidationValidatorService],
      [SheetDataValidationModel],
      [DataValidationController],
      [DataValidationFormulaController],
      [SheetDataValidationSheetController],
      [DataValidationRefRangeController],
      [DataValidationFormulaRefRangeController]
    ].forEach((dep) => {
      this._injector.add(dep);
    });
    [
      AddSheetDataValidationCommand,
      UpdateSheetDataValidationRangeCommand,
      UpdateSheetDataValidationSettingCommand,
      UpdateSheetDataValidationOptionsCommand,
      RemoveSheetDataValidationCommand,
      RemoveSheetAllDataValidationCommand,
      ClearRangeDataValidationCommand
    ].forEach((command) => {
      this._commandService.registerCommand(command);
    });
    this._injector.get(DataValidationCacheService);
    this._injector.get(SheetsDataValidationValidatorService);
    this._injector.get(DataValidationController);
    this._injector.get(DataValidationFormulaRefRangeController);
    this._injector.get(DataValidationRefRangeController);
  }
  onReady() {
    this._injector.get(SheetDataValidationSheetController);
  }
  onRendered() {
    this._injector.get(DataValidationFormulaController);
  }
};
__publicField(UniverSheetsDataValidationPlugin, "pluginName", DATA_VALIDATION_PLUGIN_NAME2);
__publicField(UniverSheetsDataValidationPlugin, "packageName", package_default5.name);
__publicField(UniverSheetsDataValidationPlugin, "version", package_default5.version);
__publicField(UniverSheetsDataValidationPlugin, "type", 2 /* UNIVER_SHEET */);
UniverSheetsDataValidationPlugin = __decorateClass([
  DependentOn(UniverSheetsFormulaPlugin, UniverDataValidationPlugin),
  __decorateParam(1, Inject(Injector)),
  __decorateParam(2, ICommandService),
  __decorateParam(3, IConfigService)
], UniverSheetsDataValidationPlugin);

// ../packages/sheets-data-validation/src/utils/create.ts
function createDefaultNewRule(accessor) {
  const selectionManagerService = accessor.get(SheetsSelectionsService);
  const currentRanges = selectionManagerService.getCurrentSelections().map((s) => s.range);
  const uid = generateRandomId(6);
  const rule = {
    uid,
    type: "decimal" /* DECIMAL */,
    operator: "equal" /* EQUAL */,
    formula1: "100",
    ranges: currentRanges != null ? currentRanges : [{ startColumn: 0, endColumn: 0, startRow: 0, endRow: 0 }]
  };
  return rule;
}

// ../packages/sheets-data-validation/src/index.ts
var CUSTOM_FORMULA_INPUT_NAME = "sheets-data-validation.custom-formula-input";
var BASE_FORMULA_INPUT_NAME = "sheets-data-validation.formula-input";
var LIST_FORMULA_INPUT_NAME = "sheets-data-validation.list-formula-input";
var CHECKBOX_FORMULA_INPUT_NAME = "sheets-data-validation.checkbox-formula-input";

export {
  DRAWING_IMAGE_WIDTH_LIMIT,
  DRAWING_IMAGE_HEIGHT_LIMIT,
  DRAWING_IMAGE_COUNT_LIMIT,
  getDrawingImageAllowSize,
  DRAWING_IMAGE_ALLOW_IMAGE_LIST,
  IDrawingManagerService,
  SetDrawingSelectedOperation,
  UnitDrawingService,
  UniverDrawingPlugin,
  getDrawingShapeKeyByDrawingSearch,
  getImageSize,
  SetTextSelectionsOperation,
  DocSelectionManagerService,
  DocSkeletonManagerService,
  RichTextEditingMutation,
  UniverDocsPlugin,
  DOC_INTERCEPTOR_POINT,
  DocInterceptorService,
  addCustomRangeBySelectionFactory,
  deleteCustomRangeFactory,
  replaceSelectionFactory,
  DOCS_COMPONENT_MAIN_LAYER_INDEX,
  getDocObject,
  addCustomDecorationBySelectionFactory,
  deleteCustomDecorationFactory,
  docDrawingPositionToTransform,
  getRichTextEditPath2 as getRichTextEditPath,
  BreakLineCommand,
  getCustomBlockIdsInSelections,
  InnerPasteCommand,
  CutContentCommand,
  InsertCommand,
  DeleteCommand,
  DeleteLeftCommand,
  DeleteRightCommand,
  convertBodyToHtml,
  DocContentInsertService,
  HorizontalLineCommand,
  IMEInputCommand,
  SetInlineFormatBoldCommand,
  SetInlineFormatItalicCommand,
  SetInlineFormatUnderlineCommand,
  SetInlineFormatStrikethroughCommand,
  SetInlineFormatSubscriptCommand,
  SetInlineFormatSuperscriptCommand,
  SetInlineFormatFontSizeCommand,
  SetInlineFormatFontFamilyCommand,
  SetInlineFormatTextColorCommand,
  SetInlineFormatCommand,
  ReplaceSnapshotCommand,
  CoverContentCommand,
  ReplaceTextRunsCommand,
  BulletListCommand,
  OrderListCommand,
  SetDocZoomRatioOperation,
  getOneTextSelectionRange,
  NodePositionConvertToCursor,
  TEXT_RANGE_LAYER_INDEX,
  getAnchorBounding,
  getLineBounding,
  getCanvasOffsetByEngine,
  DocSelectionRenderService,
  DocCreateTableOperation,
  MoveCursorOperation,
  MoveSelectionOperation,
  IEditorService,
  EMPTY_PARAGRAPH_MENU_ID,
  INSERT_BELLOW_MENU_ID,
  DOC_CONTENT_INSERT_MENU_ID,
  DOC_VERTICAL_PADDING,
  calcDocRangePositions,
  DocCanvasPopManagerService,
  DocEventManagerService,
  whenDocAndEditorFocused,
  DocBackScrollRenderController,
  DocRenderController,
  DocPrintInterceptorService,
  UniverDocsUIPlugin,
  useKeyboardEvent,
  useResize,
  RichTextEditor,
  getRuleSetting,
  getRuleOptions,
  DataValidationModel,
  DataValidatorRegistryService,
  TWO_FORMULA_OPERATOR_COUNT,
  DataValidationCacheService,
  getFormulaResult,
  isLegalFormulaResult,
  getCellValueOrigin,
  DataValidationFormulaService,
  SheetDataValidationModel,
  CHECKBOX_FORMULA_1,
  CHECKBOX_FORMULA_2,
  transformCheckboxValue,
  getDataValidationCellValue,
  getDataValidationDiffMutations,
  UpdateSheetDataValidationRangeCommand,
  AddSheetDataValidationCommand,
  UpdateSheetDataValidationSettingCommand,
  UpdateSheetDataValidationOptionsCommand,
  ClearRangeDataValidationCommand,
  RemoveSheetAllDataValidationCommand,
  RemoveSheetDataValidationCommand,
  DATA_VALIDATION_PLUGIN_NAME2 as DATA_VALIDATION_PLUGIN_NAME,
  DataValidationFormulaController,
  SheetsDataValidationValidatorService,
  UniverSheetsDataValidationPlugin,
  createDefaultNewRule,
  CUSTOM_FORMULA_INPUT_NAME,
  BASE_FORMULA_INPUT_NAME,
  LIST_FORMULA_INPUT_NAME,
  CHECKBOX_FORMULA_INPUT_NAME
};
