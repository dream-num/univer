coverage:
    status:
        project:
            default:
                # basic
                target: 50%
                threshold: 0
                base: auto
                # advanced settings
                if_ci_failed: error
                informational: false
                only_pulls: false
