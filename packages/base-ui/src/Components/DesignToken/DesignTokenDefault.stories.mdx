import { Meta } from '@storybook/blocks';
import { Palette } from './Palette.tsx';
import { defaultTheme } from '../../Basics/CSS';

<Meta title="Design / Design Token / Default" />

# Design Token (Default)

The global Token of the component library.

## Palette

<Palette theme={defaultTheme} />
