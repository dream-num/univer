import { Meta } from '@storybook/blocks';
import { Palette } from './Palette.tsx';
import { greenTheme } from '../../Basics/CSS';

<Meta title="Design / Design Token / Green" />

# Design Token (Green)

The global Token of the component library.

## Palette

<Palette theme={greenTheme} />
