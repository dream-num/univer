import { ILocale } from './interface';

const locale: ILocale = {
    design: {
        ColorPicker: {
            custom: 'CUSTOM',
            cancel: 'CANCEL',
        },
        Confirm: {
            cancel: 'cancel',
            confirm: 'ok',
        },
    },
};

export default locale;
