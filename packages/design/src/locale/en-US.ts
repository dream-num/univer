import type { ILocale } from './interface';

const locale: ILocale = {
    design: {
        Confirm: {
            cancel: 'cancel',
            confirm: 'ok',
        },
        Slider: {
            resetTo: 'Reset to',
        },
    },
};

export default locale;
