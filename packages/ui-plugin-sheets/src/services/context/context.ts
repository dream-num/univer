/**
 * The situation where sheet editor is activated (cursor blinking).
 *
 * Note tha sheet cell editor is always focused when a UniverSheet instance is focused (and other input element
 * is not `document.activeElement`).
 */
export const SHEET_EDITOR_ACTIVATED = 'SHEET_EDITOR_ACTIVATED';
