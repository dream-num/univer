{
    "$schema": "https://turbo.build/schema.json",
    "tasks": {
        "test": {
        },

        "coverage": {
            "outputs": ["coverage/**"]
        },

        "build": {
            "outputs": ["lib/**"]
        },

        "build:e2e": {
            "outputs": ["local/**"]
        },

        "typecheck": {}
    }
}
